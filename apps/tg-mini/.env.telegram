# ===========================================
# Telegram Mini-App - Real Backend Mode
# ===========================================
# Используется для работы с реальным backend (порт 4001)
# и валидацией Telegram initData

# ===========================================
# Telegram Mini-App - Real Backend Mode
# ===========================================
# Используется для работы с реальным backend через Vite proxy

# API backend URL - ПУСТАЯ СТРОКА = относительные пути через Vite proxy
# Запросы идут на /api/... → Vite проксирует на localhost:4001
# Работает и локально, и через ngrok (один домен!)
VITE_API_URL=

# Отключить моки - использовать реальный backend
VITE_USE_CLIENT_MOCKS=false
