// Возражения снимаются там, где принимается решение, а не в подвале страницы.
//
// В версии A эти же ответы живут в FAQ внизу — то есть после того, как человек
// уже посмотрел цену и ушёл. Здесь три самых дорогих сомнения стоят прямо под
// тарифами, в одну строку каждое.
//
// Ответы совпадают по смыслу с FAQ версии A: ничего нового не обещаем.

const OBJECTIONS = [
  {
    q: 'Придётся считать самому?',
    a: 'Нет. Дневник ведёт куратор — вы присылаете фото, текст или голосовое.',
  },
  {
    q: 'А если уеду или собьюсь?',
    a: 'Это обычная неделя, а не провал. Куратор помогает вернуться в ритм без старта заново.',
  },
  {
    q: 'Что после бесплатной недели?',
    a: 'Ничего не спишется: карту не привязываем, продление — только по вашему решению.',
  },
];

export default function PriceObjections() {
  return (
    <div className="mt-6 space-y-3">
      {OBJECTIONS.map((item) => (
        <div key={item.q} className="rounded-2xl bg-slate-50 px-4 py-3">
          <p className="text-[14px] font-semibold text-slate-900">{item.q}</p>
          <p className="mt-1 text-[14px] leading-5 text-slate-600">{item.a}</p>
        </div>
      ))}
    </div>
  );
}
