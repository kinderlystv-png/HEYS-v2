// Покадровый разбор вкладки «Инсайты» — половина зоны reports-insights.
//
// Файл заведён отдельно от `reports-insights-v4-canvas-geometry.test.js`
// намеренно: над зоной работают две сессии, и общий файл гейта был вторым по
// частоте местом, где правка одной уезжала в коммит другой. Пары «Отчётов» и
// пары «Инсайтов» независимы — общий у них только помощник разбора.
//
// Что здесь НЕ живёт: реестр отступлений зоны и проверки общего канона (ярусы,
// карточки, кнопки). Они смотрят обе вкладки сразу и остаются в общем файле.
//
// Метод тот же, что в «Отчётах»: ключ раздела «Разбор кадров» — «<метка кадра>
// · NN», привязка идёт по data-screen-label, а не по классам канваса.
//
// Прежняя причина не трогать эту половину — «зона инлайновая» — относится к
// КАНВАСУ (3836 инлайновых стилей в .dc.html), а не к продукту: у продукта
// инсайтов 1654 обращения к классам против 79 инлайнов, и привязка работает.
import fs from 'node:fs';
import path from 'node:path';

import { describe, expect, it } from 'vitest';

import { compare, coverage, readRazbor, readRules } from './canvas-razbor-helpers.js';

const canvas = fs.readFileSync(
  path.resolve(
    __dirname,
    '../../../docs/ui/handoff-v4/canvas/Переработка дизайна приложения/design_handoff_heys_v4/reports-insights.v4.dc.html',
  ),
  'utf8',
).replace(/\r\n/g, '\n');

const insightsCss = fs.readFileSync(
  path.resolve(__dirname, '../styles/modules/734-ui-v4-insights.css'), 'utf8');

// Отступления инсайтовой половины — поимённо, как в «Отчётах». Строка, попавшая
// сюда, в пары не идёт: пара молча зелёная там, где решение принято осознанно,
// хуже отсутствия пары.
const RAZBOR_EXCEPTIONS = new Map([
  // Счётчик зрелости: строка «вид · шапка зрелости» просит «18 дней данных»
  // 10,5/600 тоном --ac, кадр и строка «счётчик в шапке» дают «18 дней данных
  // из 30» чернилами. Два источника из трёх сходятся на кадре.
  ['Инсайты · 15|*', 'счётчик зрелости: два источника из трёх дают чернила'],
  // Лестница чернил заморожена ступенями 55/45/38/30 — кадр просит 35 % у
  // общего порога и 60 % у прозы «Где они работают», таких ступеней нет.
  // Междустрочие фразы «Стоит внимания»: кадр просит 1,45, продукт держит 1,4
  // — тот же шаг, что у соседних строк карточки.
  ['Инсайты · 52|lineHeight', 'кадр 1,45, у продукта шаг 1,4 на всю карточку'],
  // Подпись «окно наблюдения»: кадр наследует ей 10,5 от ряда, продукт задаёт
  // 9,5 отдельным правилом — подпись мельче засечек, чтобы не спорить с ними.
  ['Инсайты · 61|fontSize', 'кадр наследует 10,5 от ряда, у продукта своё 9,5'],
  // Дорожка оси фенотипа: кадр просит чернила 8 %, роль --v4-track держит 12 %.
  ['Инсайты · метаболизм · 09|background', 'кадр 8 %, роль --v4-track держит 12 %'],
  // Правое слово оси: кадр набирает 600, продукт 500 — тем же весом набраны
  // все правые подписи списков зоны.
  ['Инсайты · метаболизм · 11|fontWeight', 'кадр 600, у продукта 500 на всех правых подписях'],
  // Дорожка ритма приёмов: кадры зоны рисуют её по-разному, строки контракта
  // о ней нет — спорят сами кадры, и это уже записано в вердиктах зоны рядом
  // с таким же спором о кнопке «Ещё N». Продукт следует второму кадру:
  // высота 6, общий тон дорожек, зазор ряда 6.
  ['Ярус Питание · после последнего приёма · 13|gap', 'кадры спорят: 4 здесь против 6 у второго, продукт держит 6'],
  ['Ярус Питание · после последнего приёма · 14|height', 'кадры спорят: 8 здесь против 6 у второго, продукт держит 6'],
  ['Ярус Питание · после последнего приёма · 14|background', 'кадры спорят: --gr2 здесь, общий --v4-track у второго'],
  ['Инсайты · персональные пороги · 16|color', 'нет ступени 35 %, стоит ближайшая 38 %'],
  ['Инсайты · персональные пороги · 24|color', 'нет ступени 60 %, стоит ближайшая 55 %'],
]);

// Сколько строк разбора главного кадра гейт реально берёт в пары. Заморожено:
// падение значит, что строка выпала из сверки и вердикт на неё больше ничем не
// подкреплён; рост — что охват расширили и число пора поднять.
const COVERAGE_FLOOR = 12;

const HEAD_AND_HERO = [
  [13, '.insights-v4-meta__row', ['align', 'gap']],
  [14, '.insights-v4-meta__title', ['fontWeight', 'fontSize', 'lineHeight', 'color']],
  [15, '.insights-v4-meta__days', ['fontWeight', 'fontSize', 'lineHeight', 'color']],
  [17, '.insights-v4-hero', ['background', 'radius', 'padding', 'marginTop']],
  [19, '.insights-v4-hero__phrase',
    ['fontWeight', 'fontSize', 'lineHeight', 'color', 'marginTop']],
  // Окно наблюдения: ряд и выбранная засечка. Кегль подписи «окно наблюдения»
  // в пары не идёт — кадр наследует ей 10,5 от ряда, продукт задаёт 9,5
  // отдельным правилом; названо ниже.
  [60, '.insights-v4-window', ['align', 'gap']],
  [62, '.insights-v4-window__chip', ['minHeight', 'padding', 'radius']],
  // Строка «Стоит внимания» и её метка зрелости. Междустрочие фразы в пары не
  // идёт: кадр просит 1,45, продукт держит 1,4 — названо ниже.
  [52, '.insights-v4-attention__line', ['fontWeight', 'fontSize', 'color']],
  [54, '.insights-v4-maturity',
    ['fontWeight', 'fontSize', 'lineHeight', 'tracking', 'transform', 'padding', 'radius']],
  // Список найденных правил: метка «правило» набрана тем же начертанием, что
  // «наблюдение», и отличается только тоном — в пары идёт форма.
  [68, ['.insights-v4-maturity', '.insights-v4-maturity--rule'],
    ['fontWeight', 'fontSize', 'lineHeight', 'tracking', 'transform', 'padding', 'radius']],
  // Строка-вход во второй слой.
  [71, '.insights-v4-detail-link',
    ['align', 'justify', 'background', 'radius', 'padding']],
  [72, '.insights-v4-detail-link__text',
    ['fontWeight', 'fontSize', 'lineHeight', 'color']],
];

// Пары растут вместе с разбором: кадр, закрытый вердиктами, закрепляется здесь,
// иначе «сведено» держится только на том, что кто-то однажды посмотрел глазами.
// Счётчик охвата показал, что гейт брал один кадр из двадцати, — эти два первые,
// добавленные по следам разбора.
const NEW_USER = [
  [6, '.insights-v4-stub__hero', ['background', 'radius', 'padding']],
  [8, '.insights-v4-stub__title', ['fontWeight', 'fontSize', 'lineHeight']],
  [10, '.insights-v4-stub__progress', ['height', 'radius']],
  [12, '.insights-v4-stub__count', ['fontWeight', 'fontSize', 'lineHeight', 'color']],
  [27, '.insights-v4-stub__ladder-text', ['fontWeight', 'fontSize', 'lineHeight']],
];

const VOICE = [
  [9, '.insights-v4-attention__risk-badge', ['fontWeight', 'fontSize', 'lineHeight', 'tracking', 'transform']],
  [10, '.insights-v4-attention__basis', ['fontWeight', 'fontSize', 'lineHeight', 'color']],
  [11, '.insights-v4-attention__more', ['minHeight', 'radius', 'background', 'fontWeight', 'fontSize']],
];

const SHEET2 = [
  [3, '.insights-v4-sheet', ['background', 'radius', 'padding']],
  [4, '.insights-v4-sheet__title', ['fontWeight', 'fontSize', 'lineHeight', 'color']],
  [5, '.insights-v4-sheet__text', ['fontWeight', 'fontSize', 'lineHeight']],
  [10, '.insights-v4-sources__strength', ['fontWeight', 'fontSize', 'lineHeight', 'color']],
];

const NUTRITION = [
  [9, '.insights-v4-nutrition__rhythm-track', ['align', 'gap']],
  [10, '.insights-v4-nutrition__rhythm-time', ['fontWeight', 'fontSize', 'lineHeight']],
  [24, '.insights-v4-nutrition__legend-item', ['align', 'gap']],
];

const PRAISE = [
  [6, '.insights-v4-hero', ['background', 'radius', 'marginTop']],
  [8, '.insights-v4-hero__praise', ['fontWeight', 'fontSize', 'lineHeight', 'color']],
  [12, '.insights-v4-attention__basis', ['fontWeight', 'fontSize', 'lineHeight', 'color']],
];

const RISK = [
  [8, '.insights-v4-attention__risk-head', ['gap']],
  [10, '.insights-v4-attention__risk-window', ['fontWeight', 'fontSize', 'lineHeight', 'color']],
  [14, '.insights-v4-attention__basis', ['fontWeight', 'fontSize', 'lineHeight', 'color']],
];

const PHENO = [
  [7, '.insights-v4-pheno__row', ['align', 'gap', 'padding']],
  [8, '.insights-v4-pheno__name', ['fontWeight', 'fontSize', 'lineHeight', 'width']],
  [10, '.insights-v4-pheno__dot', ['width', 'height', 'radius', 'background']],
  // Дорожка оси: форма совпадает, тон — нет. Кадр просит чернила 8 %, у
  // продукта роль --v4-track держит 12 %; названо в реестре отступлений.
  [9, '.insights-v4-pheno__track', ['flex', 'height', 'radius']],
  // Слово справа: ширина и выключка по кадру, вес — нет (кадр 600, продукт
  // 500; тем же весом набраны все правые подписи списка).
  [11, '.insights-v4-pheno__word', ['flex', 'width', 'textAlign', 'fontSize']],
  [13, '.insights-v4-pheno__note', ['fontWeight', 'fontSize', 'lineHeight', 'marginTop']],
];

// Панель «Ещё» листа «Стоит внимания»: та же карточка сигнала, что на первом
// экране, но без лимита в три. Вердикты по кадру стояли на имени файла, без
// пар — сверка здесь и появляется.
const MORE = [
  [8, '.insights-v4-attention__meta', ['align']],
  [9, '.insights-v4-maturity',
    ['fontWeight', 'fontSize', 'lineHeight', 'tracking', 'transform', 'padding', 'radius']],
  [10, '.insights-v4-attention__basis', ['fontWeight', 'lineHeight', 'color']],
];

// Ярус «Питание» после последнего приёма: дорожка ритма и строка под ней.
// Дорожка в пары идёт формой, но не размером и не тоном: кадр даёт ей высоту
// 8 и зелёный --gr2, продукт — 6 и общий --v4-track; зазор ряда 4 против 6.
// Всё три названы в реестре отступлений.
const RHYTHM = [
  [13, '.insights-v4-nutrition__rhythm-track', ['align']],
  [14, '.insights-v4-nutrition__rhythm-bar', ['flex', 'radius']],
  [17, '.insights-v4-nutrition__rhythm-line',
    ['fontWeight', 'fontSize', 'lineHeight', 'color']],
];

// Экран «Подробно». Пар по нему не было, и первый же вердикт оказался ложным:
// строка 01 утверждала поля 16/18/0 и ссылалась на .insights-v4-detail__head,
// который их не задаёт, — таких полей нет во всём файле зоны. Вердикт
// исправлен на «≠», а сверяемые строки закреплены здесь.
const DETAIL = [
  [2, '.insights-v4--detail .insights-v4-detail__head', ['align', 'gap']],
  [3, '.insights-v4-detail__title', ['fontWeight', 'fontSize', 'color']],
];

const FAIL = [
  [6, '.insights-v4-fail__title', ['fontWeight', 'fontSize', 'lineHeight', 'color']],
  [14, '.insights-v4-fail__row-state', ['fontWeight', 'fontSize', 'lineHeight']],
  [17, '.insights-v4-fail__note', ['fontWeight', 'fontSize', 'lineHeight']],
];

const SHEET = [
  [3, '.insights-v4-sheet', ['background', 'radius', 'padding']],
  [4, '.insights-v4-sheet__title', ['fontWeight', 'fontSize', 'lineHeight', 'color']],
  [5, '.insights-v4-sheet__text', ['fontWeight', 'fontSize', 'lineHeight']],
  [10, '.insights-v4-sources__strength', ['fontWeight', 'fontSize', 'lineHeight', 'color']],
];

const THRESHOLDS = [
  [7, '.insights-v4-thresh__count', ['fontWeight', 'fontSize', 'lineHeight', 'color']],
  [14, '.insights-v4-thresh__name', ['fontWeight', 'fontSize', 'lineHeight', 'flex']],
  [15, '.insights-v4-thresh__mine', ['fontWeight', 'fontSize', 'lineHeight', 'color', 'width']],
  // Тон общего порога и прозы «Где они работают» в пары не идёт: кадр просит
  // чернила 35 % и 60 %, а лестница набора заморожена ступенями 55/45/38/30.
  // Код берёт ближайшие — 38 % и 55 %, и это названо в списке отступлений.
  [16, '.insights-v4-thresh__common', ['fontWeight', 'fontSize', 'lineHeight', 'width']],
  [24, '.insights-v4-thresh__where', ['fontWeight', 'fontSize', 'lineHeight']],
];

describe('Инсайты · разбор кадров канваса', () => {
  const razbor = readRazbor(canvas);
  const rules = readRules(insightsCss);

  it('шапка и герой кадра «Инсайты» совпадают с продуктом', () => {
    expect(compare({
      razbor, rules, frame: 'Инсайты', pairs: HEAD_AND_HERO,
    })).toEqual([]);
  });

  it('кадр «новый пользователь» совпадает с продуктом', () => {
    expect(compare({
      razbor, rules, frame: 'Инсайты · новый пользователь', pairs: NEW_USER,
    })).toEqual([]);
  });

  it('кадр «персональные пороги» совпадает с продуктом', () => {
    expect(compare({
      razbor, rules, frame: 'Инсайты · персональные пороги', pairs: THRESHOLDS,
    })).toEqual([]);
  });

  // Охват называется вслух: без этого «сверено гейтом» в обосновании вердикта
  // нельзя проверить иначе как чтением этого файла. Счётчик полезен ровно тем,
  // что его можно сравнить с числом вердиктов, которые на гейт ссылаются, —
  // расхождение между «сколько сверено» и «сколько на это ссылается» иначе
  // видно только владельцу зоны и только вручную.
  //
  // Знаменатель — строки своего кадра, а не всего канваса: отчётные кадры того
  // же файла сверяет reports-insights-v4-canvas-geometry.test.js, и класть их
  // сюда значило бы называть чужую работу своим непокрытием.
  it('кадр «риск срыва» совпадает с продуктом', () => {
    expect(compare({ razbor, rules, frame: 'Инсайты · риск срыва', pairs: RISK })).toEqual([]);
  });

  it('кадр «метаболизм» совпадает с продуктом', () => {
    expect(compare({ razbor, rules, frame: 'Инсайты · метаболизм', pairs: PHENO })).toEqual([]);
  });

  it('панель «Ещё» совпадает с продуктом', () => {
    expect(compare({
      razbor, rules, frame: 'Стоит внимания · панель Ещё', pairs: MORE,
    })).toEqual([]);
  });

  it('ярус «Питание» после последнего приёма совпадает с продуктом', () => {
    expect(compare({
      razbor, rules, frame: 'Ярус Питание · после последнего приёма', pairs: RHYTHM,
    })).toEqual([]);
  });

  it('шапка экрана «Подробно» совпадает с продуктом', () => {
    expect(compare({
      razbor, rules, frame: 'Инсайты · подробно', pairs: DETAIL,
    })).toEqual([]);
  });

  it('кадр «не посчиталось» совпадает с продуктом', () => {
    expect(compare({ razbor, rules, frame: 'Инсайты · не посчиталось', pairs: FAIL })).toEqual([]);
  });

  it('лист «Как считается долг» совпадает с продуктом', () => {
    expect(compare({
      razbor, rules, frame: 'Раскрывашка · Как считается долг', pairs: SHEET,
    })).toEqual([]);
  });

  it('кадр «ярус Питание» совпадает с продуктом', () => {
    expect(compare({ razbor, rules, frame: 'Инсайты · ярус Питание', pairs: NUTRITION })).toEqual([]);
  });

  it('кадр «день без заданий» совпадает с продуктом', () => {
    expect(compare({ razbor, rules, frame: 'Инсайты · день без заданий', pairs: PRAISE })).toEqual([]);
  });

  it('кадр «голос куратора» совпадает с продуктом', () => {
    expect(compare({
      razbor, rules, frame: 'Стоит внимания · голос куратора', pairs: VOICE,
    })).toEqual([]);
  });

  it('лист «Как посчитано» совпадает с продуктом', () => {
    expect(compare({
      razbor, rules, frame: 'Раскрывашка · Как посчитано', pairs: SHEET2,
    })).toEqual([]);
  });

  it('гейт называет свой охват', () => {
    const { perFrame } = coverage({ razbor });
    const mine = perFrame.filter((f) => f.frame === 'Инсайты');
    expect(mine).toHaveLength(1);
    const [f] = mine;
    // Границу половины берём списком кадров, а не именем: пять инсайтовых
    // кадров названы без слова «Инсайты» («Раскрывашка · …», «Стоит внимания ·
    // …», «Ярус Питание · …»), и отбор по имени разъезжается — на этом сегодня
    // дважды разошёлся реестр отступлений соседнего гейта.
    const REPORTS = [
      'Визуал v4 · Отчёты', 'Разбор Score', 'Отчёты · мало данных',
      'Отчёты · нулевая строка матрицы', 'Отчёты · нет веса',
      'Неделя к неделе · одна закрытая',
    ];
    const insightFrames = perFrame.filter((x) => !REPORTS.includes(x.frame));
    const untouched = insightFrames.filter((x) => !x.covered).map((x) => x.frame);
    console.info(
      `[инсайты] сверено ${f.covered} из ${f.rows} строк разбора кадра «${f.frame}» `
      + `(${((f.covered / f.rows) * 100).toFixed(1)} %). Инсайтовых кадров в канвасе `
      + `${insightFrames.length}, парами этот гейт не берёт ${untouched.length}: `
      + `${untouched.join(' · ')}; отчётные кадры сверяет `
      + 'reports-insights-v4-canvas-geometry',
    );
    // Храповик охвата: строка, выпавшая из пар, больше не сверяется ничем, а
    // вердикт на неё продолжает ссылаться. Падение числа означает именно это.
    expect(f.covered).toBeGreaterThanOrEqual(COVERAGE_FLOOR);
    if (f.covered > COVERAGE_FLOOR) {
      throw new Error(
        `Охват вырос: сверяется ${f.covered} строк вместо ${COVERAGE_FLOOR}. `
        + 'Поднимите COVERAGE_FLOOR, иначе следующее падение пройдёт незаметно.',
      );
    }
  });

  it('отступления инсайтовой половины названы и не разрастаются молча', () => {
    expect(RAZBOR_EXCEPTIONS.size).toBe(10);
  });

  // Реестр отступлений разделён так же, как гейт: ключи кадров «Инсайты · NN»
  // живут здесь, отчётные — в reports-insights-v4-canvas-geometry.test.js.
  // Именованные строки контракта («роли цвета», «карточка · планер») остались
  // общими: они описывают обе вкладки, и присваивать их одной половине неверно.
  //
  // Ярус «Каскад», элементы 25–51, с 1 сентября держит отдельная v4-разметка:
  // модель читает реальные CRS/события, а geometry-тест сверяет её с live HTML.
  // Здесь остаются только принятые отступления других кадров.
  const MY_FRAMES = [
    'Инсайты',
    'Раскрывашка · ',
    'Стоит внимания · ',
    'Ярус Питание · ',
  ];

  const DEVIATIONS = [
    // Шапка экрана «Подробно»: кадр даёт ей поля 16/18/0, которых нет ни у
    // одного правила зоны — экран держится общими отступами .insights-v4__content.
    'Инсайты · подробно · 01',
    'Инсайты · 21',
    // Кадр «Инсайты · ярус Питание», элемент 14: кадр рисует границу окна
    // приёмов («до 20:30») акцентом, а продукт показывает время последнего
    // приёма тем же классом, что и первое. Расхождение смысла, а не
    // оформления: «до какого времени есть» и «когда ели в последний раз» —
    // разные величины, одним классом их не показать. Кегль приведён к кадру,
    // остальное уходит вопросом дизайнеру.
    'Инсайты · ярус Питание · 14',
    // Кадры «Стоит внимания»: текст сигнала и отступ строки меты. Класс
    // .insights-v4-attention__text один на оба состояния карточки — обычное и
    // «всё хорошо»; кадры просят 600 против 500 в коде, а развести их значит
    // завести второй вид карточки, чего строка «карточка · сигнал» не просит.
    // Отступ меты 6 против 10 и 9 — те же 4 px в двух кадрах одного элемента.
    'Стоит внимания · голос куратора · 06',
    'Стоит внимания · голос куратора · 07',
    'Стоит внимания · голос куратора · 08',
    'Стоит внимания · панель Ещё · 07',
    'Стоит внимания · панель Ещё · 08',
    // Дорожка ритма приёмов: два кадра одного элемента дают ей разную
    // геометрию. В «Инсайты · ярус Питание» это перемычка 6 px тоном --c2,
    // в «Ярус Питание · после последнего приёма» — сегменты 8 px тоном --gr2
    // и чернил 10 %. Строки контракта о дорожке в зоне нет, спорят сами кадры.
    'Ярус Питание · после последнего приёма · 14',
    'Ярус Питание · после последнего приёма · 15',
    'Ярус Питание · после последнего приёма · 16',
    // Кадр «Инсайты · риск срыва»: карточка риска стоит на --v4-accent-bg,
    // кадр просит --tint. Обе роли заведены во всех наборах, строки контракта
    // о карточке риска в зоне нет — решить, какая верна, по коду нельзя.
    // Ещё три строки — то же расхождение начертания текста сигнала и отступа
    // меты, что в кадрах «Стоит внимания»: класс на все карточки один.
    'Инсайты · риск срыва · 07',
    'Инсайты · риск срыва · 11',
    'Инсайты · риск срыва · 12',
    'Инсайты · риск срыва · 16',
    // Прогноз веса не переведён на v4, поэтому его строки остаются честным
    // `?` в snapshot и не входят в реестр принятых отступлений этого теста.
    // Кадр «Инсайты · считаем» целиком: состояния загрузки в продукте нет.
    // SkeletonCard объявлен и экспортирован, но не вызывается ни разу —
    // проверено поиском вызовов и по классам insights-v4-skel*, которых в CSS
    // нет вовсе. Кадр рисует скелет с полосами, продукт этот экран не
    // показывает: пока разбор считается, вкладка просто пуста.
    'Инсайты · считаем · 01',
    'Инсайты · считаем · 02',
    'Инсайты · считаем · 03',
    'Инсайты · считаем · 04',
    'Инсайты · считаем · 05',
    'Инсайты · считаем · 06',
    'Инсайты · считаем · 07',
    'Инсайты · считаем · 08',
    'Инсайты · считаем · 09',
    'Инсайты · считаем · 10',
    'Инсайты · считаем · 11',
    'Инсайты · считаем · 12',
    'Инсайты · считаем · 13',
    'Инсайты · считаем · текст',
    // Панель «Что если» тоже остаётся `?` до собственной v4-реализации; тест
    // не должен превращать незакрытый продуктовый долг в вечное исключение.
    'Инсайты · день без заданий · 11',
    // Кнопка «Ещё N» описана двумя кадрами по-разному: в «голосе куратора» она
    // высотой от 48, 700 13/1 на --c2, в «риске срыва» — от 44 и 600 12 без
    // фона. Код следует первому. Строки контракта о кнопке нет, спорят сами
    // кадры — второй такой случай в зоне после дорожки ритма приёмов.
    'Инсайты · риск срыва · 17',
    // Строка ритма: «Инсайты · ярус Питание» даёт ей 600 13/1,45 чернилами,
    // «Ярус Питание · после последнего приёма» — 500 11/1,5 тоном 45 %. Код
    // следует второму. Третий спор кадров в зоне.
    'Инсайты · ярус Питание · 08',
    // Рекомендация приёма и объяснение под ней рисуются .meal-rec-card__* из
    // 720-predictive-insights.css — пре-v4 файла соседней зоны, где своих
    // кеглей нет вовсе. Прежние вердикты ссылались на .__head и .__head-note,
    // но это шапка карточки и модификатор внутри неё, а не рекомендация.
    'Инсайты · ярус Питание · 03',
    'Инсайты · ярус Питание · 06',
    // Поля карточки героя: «день без заданий» просит 24/20, «Инсайты» —
    // 24/20/20. Четвёртый спор кадров; код следует основному.
    'Инсайты · день без заданий · 06',
  ];

  it('отступления инсайтовых кадров названы поимённо', () => {
    const verdicts = JSON.parse(fs.readFileSync(
      path.resolve(__dirname, '../../../docs/ui/verdicts/reports-insights.json'), 'utf8'));
    const notEqual = Object.entries(verdicts.rows)
      .filter(([, row]) => row.v === '≠')
      .map(([key]) => key)
      // Граница — список кадров, а не форма ключа. Дважды подряд имя оказалось
      // не тем признаком: сперва фильтр «Инсайты · NN» не поймал подкадры
      // («Инсайты · подробно · 06»), потом префикс «Инсайты · » не поймал пять
      // кадров, названных без этого слова, — раскрывашки, «Стоит внимания» и
      // ярус Питание. Шестьдесят строк не проверял никто.
      //
      // Признак принадлежности — кто рисует: все пять собираются в
      // apps/web/insights/ (pi_ui_dashboard.js, pi_early_warning.js,
      // pi_ui_meal_rec_card.js), из отчётного CSS не приходит ни одна.
      // Проверено по коду, а не по метке.
      //
      // Цена решения названа честно: список пополняется руками при каждом новом
      // кадре пакета. Регулярка этого не требовала, но и не работала — оба раза
      // молчала в чужую сторону, краснел чужой гейт на чужой работе.
      .filter((key) => MY_FRAMES.some((frame) => key.startsWith(frame)));

    expect([...new Set(DEVIATIONS)]).toHaveLength(DEVIATIONS.length);

    // Стороны названы порознь: починки у них разные. «Нет в реестре» —
    // отступление появилось молча, его надо назвать причиной. «Нет в снимке» —
    // либо вердикт не поставлен, либо расхождение уже сведено.
    const missingInRegistry = notEqual.filter((key) => !DEVIATIONS.includes(key));
    const missingInSnapshot = DEVIATIONS.filter((key) => !notEqual.includes(key));
    expect(missingInRegistry, 'в снимке есть «≠» инсайтовых кадров, которых нет '
      + 'в этом реестре — назовите отступление здесь или сведите экран').toEqual([]);
    expect(missingInSnapshot, 'в реестре есть ключи, у которых в снимке нет «≠» — '
      + 'поставьте вердикт или уберите ключ отсюда').toEqual([]);
  });
});
