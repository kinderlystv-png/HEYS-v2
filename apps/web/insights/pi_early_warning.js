/**
 * HEYS Predictive Insights — Early Warning System v3.1
 * 
 * Проактивная детекция негативных трендов до того, как они станут проблемой.
 * 
 * v3.1 improvements:
 * - Pipeline logging standard (`ews /` filter: start/input/compute/result/ui)
 * - Actionable Steps (2-3 concrete actions per warning)
 * - Warning Trends tracking (14/30 days frequency)
 * - Priority Queue (severity × frequency × impact)
 * 
 * Сценарии:
 * 1. Health Score падает 3 дня подряд
 * 2. Критичный паттерн (C1-C22) ухудшился на 20%+ за 7 дней OR низкий score
 * 3. Status Score (0-100) падает 3 дня подряд
 * 4. Sleep debt накопился (3+ дня <7ч)
 * 5. Caloric debt >1500 kcal 2 дня подряд
 * + 10 новых checks (v2.0-v3.0): Weight Spike, Hydration, Logging Gap, Protein, Stress, Meal Skip, Binge, Mood, Plateau, Weekend
 * 
 * Dependencies: pi_patterns.js, pi_advanced.js, heys_status_v1.js
 * @param global
 */

(function (global) {
    'use strict';

    const HEYS = global.HEYS = global.HEYS || {};
    HEYS.InsightsPI = HEYS.InsightsPI || {};

    // Thresholds for warnings
    const THRESHOLDS = {
        HEALTH_SCORE_DECLINE_DAYS: 3,
        HEALTH_SCORE_MIN_DELTA: 10, // minimum total drop over N days (positive = decline)
        STATUS_SCORE_DECLINE_DAYS: 3,
        STATUS_SCORE_MIN_DELTA: 10, // minimum total drop over N days (positive = decline)
        CRITICAL_PATTERN_DEGRADATION: -0.2, // -20%
        SLEEP_DEFICIT_DAYS: 3,
        SLEEP_DEFICIT_HOURS: 7,
        CALORIC_DEBT_DAYS: 2,
        CALORIC_DEBT_THRESHOLD: 1500,
        MIN_DAYS_FOR_ANALYSIS: 7
    };

    // Critical patterns (C1-C22) that require immediate attention
    // Based on health impact: inflammation, metabolic health, performance
    const CRITICAL_PATTERNS = [
        // C1-C10: Timing & Behavior
        'meal_timing',          // C1: Meal spacing affects insulin
        'wave_overlap',         // C2: Fat burning windows
        'late_eating',          // C3: Sleep & weight impact
        'meal_quality_trend',   // C4: Diet quality trajectory
        'sleep_weight',         // C5: Recovery & metabolism
        'sleep_hunger',         // C6: Appetite regulation
        'training_kcal',        // C7: Energy balance
        'steps_weight',         // C8: NEAT activity
        'protein_satiety',      // C9: Satiety optimization
        'fiber_regularity',     // C10: Gut transit

        // C11-C22: Nutrition Quality (high health impact)
        'nutrition_quality',    // C15: Overall diet quality score
        'omega_balancer',       // C16: Inflammation control
        'protein_distribution', // C17: Muscle protein synthesis
        'training_type_match',  // C19: Performance optimization
        'hydration',            // C20: Basic physiological need
        'gut_health'            // C14: Microbiome & immunity
    ];

    // Low score thresholds for pattern alerts
    const PATTERN_LOW_SCORE_THRESHOLDS = {
        critical: 35,  // Critical patterns below this = high severity warning
        important: 45  // Important patterns below this = medium severity warning
    };

    // localStorage key for warning trends tracking (v3.1)
    const TRENDS_STORAGE_KEY = 'heys_ews_trends_v1';

    // Trends tracking configuration
    const TRENDS_CONFIG = {
        MAX_AGE_DAYS: 30,        // Keep occurrences for 30 days
        FREQUENCY_WINDOW_14D: 14, // Calculate 14-day frequency
        FREQUENCY_WINDOW_30D: 30, // Calculate 30-day frequency
        TOP_CHRONIC_COUNT: 3     // Show top-3 chronic warnings
    };

    // Health impact scores for priority calculation (0-100 scale)
    // Based on long-term health consequences and scientific evidence
    const HEALTH_IMPACT_SCORES = {
        // High Impact (80-100): Cardiovascular, metabolic, mental health
        SLEEP_DEBT: 95,              // CVD risk +40-80%, metabolic dysfunction
        STRESS_ACCUMULATION: 90,     // CVD risk +40-80%, burnout
        MOOD_WELLBEING_DECLINE: 85,  // Depression risk +60%, chronic stress
        CALORIC_DEBT: 80,            // Metabolic damage, hormone disruption
        BINGE_RISK: 85,              // Obesity +50%, diabetes +35%, depression +60%

        // Medium-High Impact (60-79): Metabolic health, performance
        HEALTH_SCORE_DECLINE: 75,    // Integrates 50+ metrics, multi-system
        STATUS_SCORE_DECLINE: 70,    // Recovery, adaptation, overtraining
        WEIGHT_SPIKE: 65,            // Rapid weight gain, inflammation
        PROTEIN_DEFICIT: 65,         // Muscle loss, satiety issues
        HYDRATION_DEFICIT: 60,       // Basic physiological function

        // Medium Impact (40-59): Behavior patterns
        MEAL_SKIP_PATTERN: 55,       // Metabolic syndrome +25%
        WEEKEND_PATTERN: 50,         // Binge-restrict cycle risk
        LOGGING_GAP: 45,             // Accountability, awareness

        // Lower Impact (20-39): Weight management
        WEIGHT_PLATEAU: 30,          // Adaptation, not health risk

        // Pattern-specific (varies)
        CRITICAL_PATTERN_DEGRADATION: 70  // Depends on specific pattern
    };

    // Severity weights for priority calculation
    const SEVERITY_WEIGHTS = {
        high: 3,
        medium: 2,
        low: 1
    };

    // Human-friendly pattern descriptions (v1.0)
    // Формат: простое название + дружелюбное описание + научная база
    const PATTERN_HUMAN_MESSAGES = {
        // C1-C10: Timing & Behavior
        'meal_timing': {
            title: 'Промежутки между едой',
            message: 'Слишком частые или редкие приёмы пищи нарушают обмен веществ',
            insight: 'Оптимальные интервалы 3-5 часов между приёмами пищи поддерживают стабильный уровень инсулина и предотвращают накопление жира.',
            science: 'Исследования показывают, что частые перекусы (каждые 1-2 часа) приводят к постоянно повышенному инсулину, что блокирует жиросжигание. Длинные промежутки более 6-7 часов вызывают чрезмерный голод и переедание.'
        },
        'wave_overlap': {
            title: 'Инсулиновые волны',
            message: 'Новые приёмы пищи не дают завершиться предыдущим — жир не сжигается',
            insight: 'Между приёмами пищи нужно время, чтобы инсулин вернулся к базовому уровню и началось использование жиров как источника энергии.',
            science: 'После еды инсулин повышается на 3-5 часов. Если есть раньше, чем завершится предыдущая «волна», организм не успевает переключиться на жиросжигание. Это основа метода интервального питания.'
        },
        'late_eating': {
            title: 'Поздние приёмы пищи',
            message: 'Еда перед сном мешает восстановлению и набору веса',
            insight: 'Поздние углеводы и большие порции за 2-3 часа до сна нарушают выработку мелатонина и гормона роста, ухудшают качество сна.',
            science: 'Ночью организм должен восстанавливаться, а не переваривать пищу. Еда позже 20:00-21:00 смещает циркадные ритмы, снижает утреннюю чувствительность к инсулину и увеличивает риск набора веса.'
        },
        'meal_quality_trend': {
            title: 'Качество питания',
            message: 'Рацион становится менее сбалансированным — не хватает важных веществ',
            insight: 'Разнообразие продуктов и натуральные источники питательных веществ — ключ к здоровью и энергии.',
            science: 'Монотонное питание приводит к дефициту микронутриентов. Исследования показывают, что разнообразие (30+ разных продуктов в неделю) связано с лучшим здоровьем микробиома и иммунитета.'
        },
        'sleep_weight': {
            title: 'Связь сна и веса',
            message: 'Недосып нарушает контроль аппетита и замедляет жиросжигание',
            insight: 'При недостатке сна растёт кортизол и грелин (гормон голода), падает лептин (гормон насыщения).',
            science: 'Мета-анализы показывают: сон <7 часов повышает риск ожирения на 40%. Недосып усиливает тягу к сладкому и калорийному, снижает базовый метаболизм на 5-15%.'
        },
        'sleep_hunger': {
            title: 'Сон и голод',
            message: 'Плохой сон усиливает аппетит и тягу к быстрым углеводам',
            insight: 'После бессонной ночи мозг требует больше глюкозы, чтобы компенсировать усталость.',
            science: 'Недосып активирует зоны мозга, отвечающие за награду, и усиливает реакцию на калорийную пищу. Грелин (голод) растёт на 15-20%, лептин (сытость) падает на 15%.'
        },
        'training_kcal': {
            title: 'Баланс: тренировки и еда',
            message: 'Тренировки есть, но еды слишком мало или наоборот — эффект снижается',
            insight: 'Для роста мышц нужен профицит калорий, для жиросжигания — небольшой дефицит. Важен баланс.',
            science: 'Избыточный дефицит при тренировках (>30% от нормы) приводит к потере мышц и замедлению метаболизма. Избыточный профицит без тренировок — к набору жира.'
        },
        'steps_weight': {
            title: 'Активность и вес',
            message: 'Мало повседневной активности — организм тратит меньше калорий',
            insight: 'NEAT (Non-Exercise Activity Thermogenesis) — это все движения кроме тренировок: ходьба, работа по дому, стояние.',
            science: 'NEAT может давать от 200 до 800 ккал в день. Люди с сидячей работой тратят на 30-40% меньше энергии. 10 000 шагов в день — научно обоснованная норма для здоровья.'
        },
        'protein_satiety': {
            title: 'Белок и сытость',
            message: 'Недостаточно белка — голод наступает быстрее',
            insight: 'Белок насыщает лучше всех макронутриентов и помогает сохранять мышцы при дефиците калорий.',
            science: 'Исследования показывают: диета с 25-30% белка снижает спонтанное потребление калорий на 10-15%. Белок увеличивает термогенез (затраты на переваривание) на 20-30%.'
        },
        'fiber_regularity': {
            title: 'Клетчатка и регулярность',
            message: 'Мало клетчатки — проблемы с пищеварением и сытостью',
            insight: 'Клетчатка питает полезные бактерии, улучшает работу кишечника и замедляет усвоение углеводов.',
            science: 'Норма клетчатки: 25-35 г/день. Каждые дополнительные 10 г клетчатки снижают риск диабета 2 типа на 10%, сердечно-сосудистых заболеваний на 15-20%.'
        },

        // C11-C22: Nutrition Quality
        'nutrition_quality': {
            title: 'Общее качество рациона',
            message: 'Питание не сбалансировано — не хватает полезных веществ',
            insight: 'Интегральный показатель учитывает разнообразие, обработку продуктов, баланс макронутриентов и плотность микронутриентов.',
            science: 'Индексы качества диеты (HEI, DASH) показывают: чем выше качество питания, тем ниже риск хронических заболеваний, выше продолжительность жизни и энергия.'
        },
        'omega_balancer': {
            title: 'Баланс омега-3 и омега-6',
            message: 'Слишком много омега-6 (растительные масла) — усиливается воспаление',
            insight: 'Оптимальное соотношение омега-6 к омега-3: от 1:1 до 4:1. Современная диета даёт 15:1 и выше.',
            science: 'Избыток омега-6 (подсолнечное, кукурузное масло) усиливает воспалительные процессы. Омега-3 (рыба, льняное масло) работают противовоспалительно. Дисбаланс связан с ожирением, диабетом, болезнями сердца.'
        },
        'protein_distribution': {
            title: 'Распределение белка',
            message: 'Белок неравномерно распределён в течение дня',
            insight: 'Для максимального синтеза мышечного белка нужно 20-40 г качественного белка каждые 3-5 часов.',
            science: 'Механизм mTOR (рост мышц) активируется при пороговой дозе лейцина (2.5-3 г). Исследования показывают: равномерное распределение белка эффективнее для мышц, чем одна большая порция.'
        },
        'training_type_match': {
            title: 'Питание и тип тренировки',
            message: 'Питание не соответствует виду нагрузки — эффект снижается',
            insight: 'Силовым тренировкам нужен белок и углеводы. Кардио — углеводы до и жиры после. Длительной выносливости — стратегия углеводной загрузки.',
            science: 'Гликоген (запасённые углеводы) — основное топливо для интенсивных тренировок. 60-90 мин интенсива истощают запасы. Восстановление гликогена требует 1-1.2 г углеводов на кг веса.'
        },
        'hydration': {
            title: 'Водный баланс',
            message: 'Недостаточно жидкости — падает работоспособность и метаболизм',
            insight: 'Даже лёгкое обезвоживание (1-2% массы тела) снижает физическую и умственную производительность.',
            science: 'Норма: 30-40 мл/кг веса. Вода регулирует температуру, транспортирует питательные вещества, выводит продукты обмена. Обезвоживание замедляет жиросжигание и увеличивает усталость.'
        },
        'gut_health': {
            title: 'Здоровье кишечника',
            message: 'Мало клетчатки и бобовых — страдает микробиом',
            insight: 'Разнообразие микробиома связано с иммунитетом, весом, настроением и энергией.',
            science: 'Клетчатка (15+ г/день) и пребиотики питают полезные бактерии. Исследования связывают разнообразие микробиома с защитой от воспаления, ожирения, депрессии. Высокобелковая диета без клетчатки вредит бактериям.'
        },

        // Additional patterns (not in C1-C22 but used)
        'added_sugar_dependency': {
            title: 'Зависимость от сахара',
            message: 'Слишком много добавленного сахара — скачки энергии и тяга к сладкому',
            insight: 'Избыточный сахар перегружает печень, вызывает инсулинорезистентность и усиливает воспалительные процессы.',
            science: 'ВОЗ рекомендует <25 г добавленного сахара в день (<5% калорий). Превышение связано с риском диабета, ожирения печени, сердечно-сосудистых заболеваний. Сахар активирует зоны награды в мозге, как наркотики.'
        }
    };

    /**
     * Human-friendly messages for non-pattern warnings
     * 
     * Structure: { title, message, insight, science }
     * - title: короткое понятное название (2-5 слов)
     * - message: friendly объяснение что происходит
     * - insight: подробный совет что делать
     *- science: научное обоснование с механизмами и исследованиями
     */
    const WARNING_HUMAN_MESSAGES = {
        HEALTH_SCORE_DECLINE: {
            title: 'Общий показатель снижается',
            message: 'Health Score падает несколько дней подряд — это комплексный сигнал о накопленном дисбалансе.',
            insight: 'Падение интегрирует 50+ метрик питания, сна, активности. Проверьте паттерны с низкими scores — там причина.',
            science: 'Health Score объединяет ключевые паттерны питания (timing, quality, balance), восстановления (сон, стресс) и активности (NEAT, тренировки). Снижение показателя 3+ дня указывает на системный дисбаланс, требующий внимания к конкретным слабым зонам. Исследования показывают: раннее выявление трендов (до появления симптомов) улучшает долгосрочные результаты на 40-60%.',
            actions: [
                'Откройте раздел Insights → Паттерны, найдите 3 паттерна с наименьшим score — это ключевые зоны риска',
                'Проверьте сон за 3 дня: если <7ч — приоритет на восстановление, а не новые нагрузки',
                'Сравните калории с нормой: дефицит >500 ккал/день — слишком агрессивно, профицит >300 — проверьте читмилы'
            ]
        },
        STATUS_SCORE_DECLINE: {
            title: 'Показатель состояния падает',
            message: 'Status Score снижается — организм сигнализирует о накопленной усталости и недовосстановлении.',
            insight: 'Status Score (0-100) учитывает сон, стресс, восстановление, готовность. Падение 3+ дня означает: нужен отдых, а не новые нагрузки.',
            science: 'Status Score отражает текущую физиологическую готовность организма через баланс симпатической и парасимпатической систем. Хроническое снижение (consecutive decline) указывает на дефицит восстановления: HRV падает, cortisol растёт, иммунитет слабеет. Исследования подтверждают: игнорирование сигналов усталости увеличивает риск перетренированности, болезней, травм (J Sports Sci, 2020).',
            actions: [
                'Добавьте 1 дополнительный день отдыха от тренировок на этой неделе',
                'Повысьте калории на 10-15% на 2-3 дня для восстановления гормонов',
                'Уделите время релаксации: медитация, прогулка, яншие практики 15-20 мин/день'
            ]
        },
        SLEEP_DEBT: {
            title: 'Накопился недосып',
            message: 'Несколько дней подряд сон меньше 7 часов — организм не успевает восстановиться.',
            insight: 'Sleep debt снижает метаболизм на 10-15%, усиливает голод и тягу к сладкому, ухудшает работу инсулина. Приоритет: вернуть регулярный сон 7-9 часов.',
            science: 'Сон <7 часов 3+ ночи подряд нарушает гормональный баланс: растёт ghrelin (гормон голода), падает leptin (гормон сытости), снижается чувствительность к инсулину на 20-30%. Риск ожирения увеличивается на 40%, риск диабета 2 типа — на 28% (Sleep Medicine Reviews, 2019). Недосып 1 ночи компенсируется 2-3 ночами полноценного сна; хронический debt требует 7-10 дней восстановления.',
            actions: [
                'Установите будильник на отбой сон за 8 часов до подъёма (не на подъём!)',
                'Откажитесь от экранов за 1 час до сна (синий свет подавляет мелатонин)',
                'Если недосып >3 дней — добавьте 15-30 мин дневного сна до 15:00 чдля быстрого восстановления'
            ]
        },
        CALORIC_DEBT: {
            title: 'Слишком большой дефицит калорий',
            message: 'Два дня подряд недобор более 1500 ккал — это экстремальный дефицит, замедляющий метаболизм.',
            insight: 'Резкий дефицит включает режимэкономии: организм расходует меньше энергии, теряет мышцы вместо жира. Оптимальный дефицит для похудения: 300-500 ккал/день.',
            science: 'Дефицит >30% от нормы (>1500 ккал) запускает adaptive thermogenesis: базовый метаболизм падает на 10-15%, активность NEAT снижается, растёт cortisol (катаболизм мышц). Потеря мышечной массы при агрессивном дефиците достигает 40-50% от общей потери веса (vs 20-30% при умеренном дефиците). Исследования показывают: быстрая потеря веса (>1 кг/неделю) ведёт к rebound эффекту в 80% случаев (Obesity Reviews, 2021).',
            actions: [
                'Повысьте калории на 300-500 ккал/день за счёт протеина и сложных углеводов',
                'Добавьте 1-2 перекуса по 150-200 ккал (орехи, кефир, банан)',
                'Планируйте refeed day каждые 7-10 дней: +10-15% ккал для перезапуска метаболизма'
            ]
        },
        WEIGHT_SPIKE: {
            title: 'Резкий скачок веса',
            message: 'Вес увеличился на 1+ кг за 1-2 дня — это может быть задержка воды или переедание.',
            insight: 'Быстрый набор веса чаще всего связан с задержкой воды (соль, углеводы, менструальный цикл). Если это не вода — проверьте калории и приёмы пищи за последние дни.',
            science: 'Набор 1 кг жира требует избытка ~7700 ккал, что нереально за 1-2 дня. Быстрое увеличение веса обычно вызвано задержкой воды: 1 г гликогена удерживает 3-4 г воды, 1 г натрия — до 200 мл жидкости. Гормональные колебания (менструальный цикл) могут добавить 1-3 кг воды. Однако частые скачки могут указывать на нестабильное питание (binge-restrict cycle) или гормональные проблемы (кортизол, инсулин).',
            actions: [
                'Проверьте потребление соли за 2 дня: ограничьте до 2000 мг/день',
                'Увеличьте воду до 2.5-3 л/день для вывода задержавшейся жидкости',
                'Если скачки повторяются часто — проверьте гормоны (кортизол, щитовидка, инсулин)'
            ]
        },
        HYDRATION_DEFICIT: {
            title: 'Недостаток жидкости',
            message: 'Несколько дней подряд вы пьёте меньше 1.5 л воды — это влияет на энергию и метаболизм.',
            insight: 'Вода критична для всех процессов: транспорт питательных веществ, терморегуляция, вывод токсинов, работоспособность. Даже лёгкое обезвоживание (1-2%) снижает физическую и умственную продуктивность.',
            science: 'Норма: 30-40 мл/кг веса (для 70 кг — 2.1-2.8 л). Обезвоживание 1-2% массы тела снижает физическую выносливость на 10-20%, когнитивные функции на 10% (European Journal of Clinical Nutrition, 2003). Вода участвует в липолизе (жиросжигании): дефицит замедляет метаболизм на 3-5%. Хроническое обезвоживание повышает кортизол, ухудшает сон, усиливает голод (путается с жаждой).',
            actions: [
                'Поставьте бутылку 1.5 л на видное место и выпейте до конца дня',
                'Установите напоминание пить воду каждые 2 часа',
                'Добавьте травяной чай, огурец, лимон для разнообразия'
            ]
        },
        LOGGING_GAP: {
            title: 'Пропуски в дневнике',
            message: 'Вы не записывали питание 2+ дня — теряется контроль и осознанность.',
            insight: 'Регулярное ведение дневника — ключ к результатам. Пропуски ломают привычку, снижают осознанность выборов, приводят к перееданию и потере прогресса.',
            science: 'Исследования показывают: люди, ведущие food diary ежедневно, теряют в 2 раза больше веса, чем те, кто записывает нерегулярно (American Journal of Preventive Medicine, 2008). Self-monitoring повышает осознанность на 40-60%, снижает импульсивные выборы. Пропуск >2 дней коррелирует с возвратом к старым паттернам питания в 70% случаев (Obesity, 2017).',
            actions: [
                'Установите напоминание записывать приёмы пищи сразу после еды (пока не забыли)',
                'Начните с упрощённого формата: фото еды + примерные порции',
                'Свяжите запись с триггером: чашка кофе → открываю HEYS → записываю завтрак'
            ]
        },
        PROTEIN_DEFICIT: {
            title: 'Дефицит белка',
            message: 'Несколько дней подряд белка меньше нормы — это угрожает мышцам и замедляет восстановление.',
            insight: 'Белок критичен для сохранения мышечной массы, особенно при дефиците калорий. Минимум 1.2-1.6 г/кг веса, распределённые равномерно в течение дня.',
            science: 'Норма белка при похудении: 1.6-2.2 г/кг для сохранения мышц (JISSN, 2017). При дефиците <1.2 г/кг × 3+ дня организм использует мышечный белок как источник энергии (глюконеогенез). Потеря мышечной массы снижает базовый метаболизм на 10-50 ккал на каждый потерянный кг, усиливает эффект йо-йо. Белок обеспечивает насыщение (thermic effect 20-30% vs 5-10% углеводов), поддерживает синтез гормонов и иммунитет.',
            actions: [
                'Добавьте 20-40 г протеина в каждый основной приём пищи (курица, рыба, творог, яйца)',
                'Используйте протеиновые перекусы: греческий йогурт, орехи, протеиновый коктейль',
                'Простое правило: треть тарелки = белок, треть = овощи, треть = углеводы'
            ]
        },
        STRESS_ACCUMULATION: {
            title: 'Накопление стресса',
            message: 'Высокий стресс без полноценного восстановления — растёт кортизол, падает эффективность.',
            insight: 'Хронический стресс нарушает гормональный баланс: кортизол блокирует жиросжигание, усиливает тягу к сладкому, ухудшает сон и восстановление.',
            science: 'Хронический стресс повышает кортизол на 30-50% от базового уровня. Высокий кортизол стимулирует липогенез (накопление жира) в висцеральной области, усиливает инсулинорезистентность, подавляет тестостерон/эстроген, ухудшает quality сна (Psychoneuroendocrinology, 2019). Стресс активирует reward pathways — тяга к comfort food (сахар, жиры). Дефицит восстановления >3 дней увеличивает риск перетренированности, снижает иммунитет на 20-40% (J Sports Sci, 2020).',
            actions: [
                'Внедрите ежедневную практику релаксации: дыхательные упражнения, прогулка, йога (15-30 мин)',
                'Снизьте интенсивность тренировок на 20-30% или добавьте 1 день отдыха',
                'Проверьте сон: стресс часто идёт рука об руку с недосыпом — приоритет на 7-9 часов'
            ]
        },
        MEAL_SKIP_PATTERN: {
            title: 'Регулярные пропуски еды',
            message: 'Вы часто пропускаете приёмы пищи — это приводит к перееданию и нарушению метаболизма.',
            insight: 'Длительные перерывы без еды (>6-8 часов) вызывают чрезмерный голод, переедание, скачки инсулина. Регулярность важнее количества приёмов.',
            science: 'Пропуск приёмов пищи снижает метаболическую гибкость, увеличивает реактивный голод. Исследования показывают: нерегулярное питание повышает риск метаболического синдрома на 25%, ухудшает гликемический контроль (Proc Nutr Soc, 2016). Длительные перерывы (>8ч) стимулируют компенсаторное переедание (+20-40% калорий за следующий приём), нарушают циркадные ритмы метаболизма. Пропуск завтрака коррелирует с +5-8% массы тела за год.',
            actions: [
                'Планируйте 3-4 приёма пищи каждые 3-5 часов в одно и то же время',
                'Подготовьте здоровые перекусы заранее: орехи, фрукты, йогурт — всегда под рукой',
                'Если пропускаете завтрак/обед из-за работы — ставьте напоминание каждые 4 часа'
            ]
        },
        BINGE_RISK: {
            title: 'Риск переедания',
            message: 'Обнаружены признаки binge eating: большие порции после длительных перерывов.',
            insight: 'Binge-restrict цикл разрушает метаболизм и психологическое здоровье. Ключ — регулярное дробное питание без экстремальных ограничений.',
            science: 'Binge Eating Disorder (BED) характеризуется эпизодами быстрого потребления большого количества пищи (>2000 ккал за 2 часа) с последующим чувством вины. Физиологические триггеры: длительные перерывы (>8ч), агрессивный дефицит калорий (>30%), дефицит сна. Психологические: стресс, ограничительный mindset. BED повышает риск ожирения на 50%, диабета на 35%, депрессии на 60% (Int J Eat Disord, 2018). Лечение: когнитивно-поведенческая терапия + структурированное питание без запретов.',
            actions: [
                'Увеличьте суточные калории на 200-300 ккал, распределяя равномерно через все приёмы пищи',
                'Уберите запреты: разрешите любимую еду 1-2 раза в неделю небольшими порциями (200-300 ккал)',
                'Если чувство вины после еды или компульсивное переедание — рассмотрите консультацию психолога КПТ'
            ]
        },
        MOOD_WELLBEING_DECLINE: {
            title: 'Падение настроения',
            message: 'Настроение и самочувствие снижаются 7+ дней — сигнал burnout и перегрузки.',
            insight: 'Продолжительное снижение мотивации и энергии часто связано с перегрузкой, недосыпом, дефицитом питания или стрессом. Внимание: не игнорируйте сигналы тела.',
            science: 'Mood и wellbeing скоры коррелируют с серотонином, дофамином, HRV, уровнем воспаления. Снижение 7+ дней может указывать на: 1) хронический стресс (кортизол подавляет серотонин), 2) дефицит сна (<7ч снижает серотонин на 20%), 3) гипогликемия (дефицит углеводов снижает серотонин), 4) дефицит микронутриентов (B12, магний, omega-3). Исследования показывают: low mood в течение 2+ недель — критерий депрессивного эпизода (DSM-5). Burnout увеличивает риск сердечно-сосудистых заболеваний на 40-80% (J Psychosom Res, 2020).',
            actions: [
                'Проверьте сон (7-9ч) + стресс (практики релаксации 15-20 мин ежедневно)',
                'Добавьте 30 мин прогулки на свежем воздухе при дневном свете каждый день',
                'Если снижение настроения >14 дней — обратитесь к специалисту (депрессия требует профессиональной помощи)'
            ]
        },
        WEIGHT_PLATEAU: {
            title: 'Плато веса',
            message: 'Вес не меняется 14+ дней при цели снижения — тело адаптировалось.',
            insight: 'Плато — нормальная фаза похудения. Организм снизил метаболизм и NEAT. Решение: refeed day (+10-15% калорий), изменение тренировок, проверка точности записей.',
            science: 'Плато возникает из-за metabolic adaptation: базовый метаболизм падает на 10-25% при длительном дефиците, NEAT снижается на 15-30%, лептин падает, ghrelin растёт. Плато 14+ дней — сигнал пересмотреть стратегию. Решения: 1) Refeed day каждые 7-10 дней восстанавливает лептин/гормоны, 2) Diet break (2 недели на maintenance) перезапускает метаболизм, 3) Увеличение NEAT/тренировок (Int J Obes, 2019).',
            actions: [
                'Устройте refeed day: +10-15% калорий через углеводы (300-500 ккал) на 1 день',
                'Пересчитайте норму калорий: введите текущий вес в профиль, возможно нужен новый target',
                'Увеличьте NEAT: паркуйтесь дальше, используйте лестницу, гуляйте на созвонах (+200-400 ккал/день)'
            ]
        },
        WEEKEND_PATTERN: {
            title: 'Выходные срывы',
            message: 'Паттерны питания в Сб/Вс хуже, чем в будни — алкоголь, углеводы, читмилы сбивают прогресс.',
            insight: 'Выходные срывы +1500 ккал могут перекрыть дефицит 5 будних дней. Стратегия: гибкая диета (80/20), планирование refeed дней, контроль алкоголя.',
            science: 'Исследования показывают: потребление калорий в выходные на 15-20% выше, чем в будни, алкоголь добавляет 300-800 ккал (Obesity, 2008). Weekend warrior pattern — очень строго Пн-Пт, отказ Сб-Вс — ведёт к binge-restrict cycle. Алкоголь блокирует жиросжигание на 12-36 часов, ухудшает сон, снижает синтез белка на 20-30%. Стратегия: включить выходные в недельное планирование (calorie cycling), планировать приёмы пищи заранее (Nutr J, 2017).',
            actions: [
                'Включите выходные в недельный план: calorie cycling 80/20 (5 дней дефицит, 2 дня maintenance)',
                'Ограничьте алкоголь до 1-2 порций: вино 150 мл или пиво 330 мл (блокирует жиросжигание на 12-36ч)',
                'Планируйте приёмы пищи заранее: составьте menu на Сб-Вс в пятницу вечером'
            ]
        }
    };

    // ========================================
    // Warning Trends Tracking (v3.1)
    // ========================================

    /**
     * Load warning trends from localStorage
     * @returns {Object} Trends data structure
     */
    function loadTrends() {
        console.info('ews / trends 🚀 load:', { key: TRENDS_STORAGE_KEY });

        try {
            const stored = localStorage.getItem(TRENDS_STORAGE_KEY);
            if (!stored) {
                console.info('ews / trends 📥 load.empty:', { reason: 'no_stored_data' });
                return { version: 1, trends: {}, lastUpdated: null };
            }

            const parsed = JSON.parse(stored);
            console.info('ews / trends ✅ load.success:', {
                warningTypes: Object.keys(parsed.trends || {}).length,
                lastUpdated: parsed.lastUpdated
            });
            return parsed;
        } catch (error) {
            console.error('ews / trends ❌ load.error:', error.message);
            return { version: 1, trends: {}, lastUpdated: null };
        }
    }

    /**
     * Save warning trends to localStorage
     * @param {Object} trendsData - Trends data structure
     */
    function saveTrends(trendsData) {
        console.info('ews / trends 🚀 save:', {
            warningTypesCount: Object.keys(trendsData.trends || {}).length
        });

        try {
            localStorage.setItem(TRENDS_STORAGE_KEY, JSON.stringify(trendsData));
            console.info('ews / trends ✅ save.success:', {
                size: JSON.stringify(trendsData).length,
                lastUpdated: trendsData.lastUpdated
            });
        } catch (error) {
            console.error('ews / trends ❌ save.error:', error.message);
        }
    }

    /**
     * Track warning occurrences and calculate frequency
     * @param {Array} warnings - Current warnings from detectEarlyWarnings
     * @returns {Object} Top chronic warnings with frequency data
     */
    function trackWarningTrends(warnings) {
        console.info('ews / trends 🚀 start:', {
            currentWarnings: warnings.length,
            types: warnings.map(w => w.type)
        });

        // Load existing trends
        const trendsData = loadTrends();
        const today = new Date().toISOString().split('T')[0]; // 'YYYY-MM-DD'
        const todayTimestamp = new Date(today).getTime();

        console.info('ews / trends 📥 input:', {
            date: today,
            existingTypes: Object.keys(trendsData.trends).length
        });

        // Update occurrences for each warning
        warnings.forEach(warning => {
            const warningType = warning.type;

            if (!trendsData.trends[warningType]) {
                trendsData.trends[warningType] = {
                    occurrences: [],
                    frequency14d: 0,
                    frequency30d: 0
                };
            }

            const trend = trendsData.trends[warningType];

            // Add today's occurrence if not already recorded
            const alreadyRecorded = trend.occurrences.some(occ => occ.date === today);
            if (!alreadyRecorded) {
                trend.occurrences.push({
                    date: today,
                    timestamp: todayTimestamp,
                    severity: warning.severity
                });
            }
        });

        console.info('ews / trends 🧮 compute:', {
            phase: 'cleanup_old_occurrences',
            maxAge: TRENDS_CONFIG.MAX_AGE_DAYS
        });

        // Cleanup old occurrences (>30 days) and calculate frequencies
        const cutoff30d = todayTimestamp - (TRENDS_CONFIG.MAX_AGE_DAYS * 24 * 60 * 60 * 1000);
        const cutoff14d = todayTimestamp - (TRENDS_CONFIG.FREQUENCY_WINDOW_14D * 24 * 60 * 60 * 1000);

        Object.keys(trendsData.trends).forEach(warningType => {
            const trend = trendsData.trends[warningType];

            // Keep only occurrences within 30 days
            trend.occurrences = trend.occurrences.filter(occ => occ.timestamp >= cutoff30d);

            // Calculate frequencies
            trend.frequency30d = trend.occurrences.length;
            trend.frequency14d = trend.occurrences.filter(occ => occ.timestamp >= cutoff14d).length;

            // Remove trend if no occurrences left
            if (trend.occurrences.length === 0) {
                delete trendsData.trends[warningType];
            }
        });

        // Update metadata
        trendsData.lastUpdated = today;

        console.info('ews / trends 🧮 compute.frequencies:', {
            activeTypes: Object.keys(trendsData.trends).length,
            totalOccurrences: Object.values(trendsData.trends).reduce((sum, t) => sum + t.frequency30d, 0)
        });

        // Save updated trends
        saveTrends(trendsData);

        // Calculate top-3 chronic warnings (by 30-day frequency)
        const chronicWarnings = Object.entries(trendsData.trends)
            .map(([type, trend]) => ({
                type,
                frequency14d: trend.frequency14d,
                frequency30d: trend.frequency30d,
                lastOccurrence: trend.occurrences[trend.occurrences.length - 1]?.date
            }))
            .sort((a, b) => b.frequency30d - a.frequency30d)
            .slice(0, TRENDS_CONFIG.TOP_CHRONIC_COUNT);

        console.info('ews / trends ✅ result:', {
            topChronicCount: chronicWarnings.length,
            topTypes: chronicWarnings.map(w => w.type),
            frequencies: chronicWarnings.map(w => `${w.frequency14d}d14/${w.frequency30d}d30`)
        });

        console.group('ews / trends 🖥️ ui.chronic_warnings - Top 3');
        console.table(chronicWarnings.map(w => ({
            'Warning Type': w.type,
            '14d': w.frequency14d,
            '30d': w.frequency30d,
            'Last': w.lastOccurrence
        })));
        console.groupEnd();

        console.info('ews / trends 🖥️ ui.complete:', {
            chronicDisplayed: chronicWarnings.length
        });

        return {
            chronicWarnings,
            allTrends: trendsData.trends
        };
    }

    /**
     * Prioritize warnings by severity × frequency × health impact
     * @param {Array} warnings - Current warnings from detectEarlyWarnings
     * @param {Object} trendsData - Trends data with frequency information
     * @returns {Array} Prioritized warnings with priority scores
     */
    function prioritizeWarnings(warnings, trendsData) {
        console.info('ews / priority 🚀 start:', {
            warningsCount: warnings.length,
            hasTrends: !!trendsData
        });

        if (!warnings || warnings.length === 0) {
            console.info('ews / priority ⚠️ input.empty:', { reason: 'no_warnings' });
            return [];
        }

        console.info('ews / priority 📥 input:', {
            warnings: warnings.map(w => w.type),
            trendsAvailable: Object.keys(trendsData?.trends || {}).length
        });

        console.info('ews / priority 🧮 compute:', {
            phase: 'calculating_scores',
            formula: 'severity_weight × frequency14d × health_impact'
        });

        // Calculate priority score for each warning
        const prioritizedWarnings = warnings.map(warning => {
            const warningType = warning.type;
            const severityWeight = SEVERITY_WEIGHTS[warning.severity] || 1;
            const healthImpact = HEALTH_IMPACT_SCORES[warningType] || 50; // default 50 if not defined
            const frequency14d = trendsData?.trends?.[warningType]?.frequency14d || 1; // at least 1 (today)

            // Priority formula: severity (1-3) × frequency (1-14) × health impact (0-100)
            const priorityScore = severityWeight * frequency14d * healthImpact;

            return {
                ...warning,
                priorityScore,
                frequency14d,
                healthImpact,
                severityWeight
            };
        });

        // Sort by priority score descending
        prioritizedWarnings.sort((a, b) => b.priorityScore - a.priorityScore);

        // Mark top-3 as critical priority
        const top3 = prioritizedWarnings.slice(0, 3);
        top3.forEach(w => {
            w.criticalPriority = true;
            w.priorityLabel = '🔥 Fix First!';
        });

        console.info('ews / priority ✅ result:', {
            totalWarnings: prioritizedWarnings.length,
            top3Types: top3.map(w => w.type),
            top3Scores: top3.map(w => w.priorityScore),
            averageScore: Math.round(prioritizedWarnings.reduce((sum, w) => sum + w.priorityScore, 0) / prioritizedWarnings.length)
        });

        console.group('ews / priority 🖥️ ui.top3 - Critical Priority');
        console.table(top3.map(w => ({
            'Type': w.type,
            'Severity': w.severity,
            'Freq 14d': w.frequency14d,
            'Health Impact': w.healthImpact,
            'Priority Score': w.priorityScore,
            'Label': w.priorityLabel
        })));
        console.groupEnd();

        console.info('ews / priority 🖥️ ui.complete:', {
            criticalPriorityCount: top3.length,
            totalPrioritized: prioritizedWarnings.length
        });

        return prioritizedWarnings;
    }

    /**
     * Detect consecutive decline in values
     * @param {number[]} values - Array of numeric values (most recent last)
     * @param {number} consecutiveDays - Number of consecutive days to check
     * @returns {boolean}
     */
    function isConsecutiveDecline(values, consecutiveDays) {
        if (!Array.isArray(values) || values.length < consecutiveDays) {
            return false;
        }

        const recent = values.slice(-consecutiveDays);

        for (let i = 1; i < recent.length; i++) {
            if (recent[i] >= recent[i - 1]) {
                return false; // Not declining
            }
        }

        return true;
    }

    /**
     * Calculate average decline rate
     * @param {number[]} values
     * @returns {number} - Average change per day
     */
    function calculateDeclineRate(values) {
        if (!Array.isArray(values) || values.length < 2) return 0;

        let totalChange = 0;
        for (let i = 1; i < values.length; i++) {
            totalChange += (values[i] - values[i - 1]);
        }

        return totalChange / (values.length - 1);
    }

    /**
     * Warning 1: Health Score declining 3+ days
     * @param {object[]} days - Array of day objects (sorted oldest to newest)
     * @param {object} profile
     * @param {object} pIndex
     * @param {object} options - Additional options with currentPatterns/previousPatterns
     * @returns {object|null}
     */
    function checkHealthScoreDecline(days, profile, pIndex, options = {}) {
        console.log('ews / detect 📉 checking health score decline...');
        if (days.length < THRESHOLDS.MIN_DAYS_FOR_ANALYSIS) {
            console.log('ews / detect ⏭️ skip: insufficient days', days.length, '<', THRESHOLDS.MIN_DAYS_FOR_ANALYSIS);
            return null;
        }

        // Check if we have pattern data passed from UI
        const { currentPatterns, previousPatterns } = options;

        // Fallback path: detect decline from day-level health score calculation
        // (keeps backward compatibility for environments without pattern snapshots)
        if (!currentPatterns || !previousPatterns) {
            const calcDayHealthScore = HEYS.InsightsPI?.calculations?.calculateHealthScore;
            if (!calcDayHealthScore) {
                console.log('ews / detect ⏭️ skip: no pattern snapshots and no day-level health score calculator');
                return null;
            }

            try {
                const sortedDays = days
                    .slice()
                    .sort((a, b) => new Date(b.date || 0) - new Date(a.date || 0)); // newest first

                const currentPeriod = sortedDays.slice(0, THRESHOLDS.HEALTH_SCORE_DECLINE_DAYS);
                const previousPeriod = sortedDays.slice(
                    THRESHOLDS.HEALTH_SCORE_DECLINE_DAYS,
                    THRESHOLDS.HEALTH_SCORE_DECLINE_DAYS * 2
                );

                if (currentPeriod.length < THRESHOLDS.HEALTH_SCORE_DECLINE_DAYS || previousPeriod.length < THRESHOLDS.HEALTH_SCORE_DECLINE_DAYS) {
                    return null;
                }

                const currentScores = currentPeriod
                    .map(day => calcDayHealthScore(day, profile, pIndex, days))
                    .map(score => score?.overall ?? score?.total ?? 0)
                    .filter(score => Number.isFinite(score) && score > 0);

                const previousScores = previousPeriod
                    .map(day => calcDayHealthScore(day, profile, pIndex, days))
                    .map(score => score?.overall ?? score?.total ?? 0)
                    .filter(score => Number.isFinite(score) && score > 0);

                if (currentScores.length < THRESHOLDS.HEALTH_SCORE_DECLINE_DAYS || previousScores.length < THRESHOLDS.HEALTH_SCORE_DECLINE_DAYS) {
                    console.log('ews / detect ⏭️ skip: insufficient valid scores after filtering');
                    return null;
                }

                const currentScore = currentScores.reduce((sum, v) => sum + v, 0) / currentScores.length;
                const previousScore = previousScores.reduce((sum, v) => sum + v, 0) / previousScores.length;
                const totalDrop = previousScore - currentScore;

                console.log(`ews / detect 📊 health scores (fallback): prev=${Math.round(previousScore)}, curr=${Math.round(currentScore)}, drop=${Math.round(totalDrop)}`);

                if (totalDrop >= THRESHOLDS.HEALTH_SCORE_MIN_DELTA) {
                    const percentChange = Math.round((totalDrop / previousScore) * 100);

                    const humanMsg = WARNING_HUMAN_MESSAGES.HEALTH_SCORE_DECLINE;

                    const warning = {
                        type: 'HEALTH_SCORE_DECLINE',
                        severity: totalDrop >= 20 ? 'high' : 'medium',
                        days: THRESHOLDS.HEALTH_SCORE_DECLINE_DAYS,
                        currentScore: Math.round(currentScore),
                        previousScore: Math.round(previousScore),
                        totalDrop: Math.round(totalDrop),
                        percentChange,
                        patternName: humanMsg.title,
                        message: `📉 ${humanMsg.title}`,
                        detail: `Было ${Math.round(previousScore)}, стало ${Math.round(currentScore)} (−${percentChange}% за ${THRESHOLDS.HEALTH_SCORE_DECLINE_DAYS} дня)`,
                        insight: humanMsg.insight,
                        science: humanMsg.science,
                        actionable: true
                    };
                    console.log(`ews / detect ✅ HEALTH_SCORE_DECLINE warning: severity=${warning.severity}, drop=${warning.totalDrop}`);
                    return warning;
                }

                console.log('ews / detect ➖ no significant health score decline (fallback)');
                return null;
            } catch (e) {
                console.error('ews / detect ❌ error in day-level health score fallback:', e);
                return null;
            }
        }

        // Use calculateHealthScore to compute scores from patterns
        const calculateHealthScore = HEYS.PredictiveInsights?.calculateHealthScore;
        if (!calculateHealthScore) {
            console.warn('ews / detect ⚠️ HEYS.PredictiveInsights.calculateHealthScore not available');
            return null;
        }

        try {
            // Calculate health scores from patterns
            const currentHealthScore = calculateHealthScore(currentPatterns, profile);
            const previousHealthScore = calculateHealthScore(previousPatterns, profile);

            const currentScore = currentHealthScore?.total || 0;
            const previousScore = previousHealthScore?.total || 0;

            console.log('ews / detect 📊 health scores:', {
                previous: Math.round(previousScore),
                current: Math.round(currentScore),
                delta: Math.round(currentScore - previousScore)
            });

            if (currentScore === 0 || previousScore === 0) {
                console.log('ews / detect ⏭️ health score calculation returned 0, skipping');
                return null;
            }

            // Check for decline (previous > current = decline)
            const totalDrop = previousScore - currentScore; // Positive = decline

            if (totalDrop >= THRESHOLDS.HEALTH_SCORE_MIN_DELTA) {  // Significant drop (e.g., >= 10 points)
                const percentChange = Math.round((totalDrop / previousScore) * 100);

                const humanMsg = WARNING_HUMAN_MESSAGES.HEALTH_SCORE_DECLINE;

                return {
                    type: 'HEALTH_SCORE_DECLINE',
                    severity: totalDrop >= 20 ? 'high' : 'medium',
                    days: 7,
                    currentScore: Math.round(currentScore),
                    previousScore: Math.round(previousScore),
                    totalDrop: Math.round(totalDrop),
                    percentChange,
                    patternName: humanMsg.title,
                    message: `📉 ${humanMsg.title}`,
                    detail: `Было ${Math.round(previousScore)}, стало ${Math.round(currentScore)} (−${percentChange}% за 7 дней)`,
                    insight: humanMsg.insight,
                    science: humanMsg.science,
                    actionable: true
                };
            }

            console.log('ews / detect ➖ health score stable (drop < threshold)');
            return null;

        } catch (e) {
            console.error('ews / detect ❌ error calculating health score decline:', e);
            return null;
        }
    }

    /**
     * Warning 2: Critical pattern degraded significantly OR current low scores
     * @param {object[]} days
     * @param {object} profile
     * @param {object} pIndex
     * @param {object} previousPatterns - patterns from 7 days ago (optional)
     * @param {object} currentPatterns - patterns from today (required)
     * @returns {object[]|null}
     */
    function checkCriticalPatternDegradation(days, profile, pIndex, previousPatterns, currentPatterns) {
        if (!currentPatterns) return null;

        const warnings = [];

        // Check 1: Pattern score degradation (change over time) - ONLY if we have previous data
        if (previousPatterns) {
            for (const patternId of CRITICAL_PATTERNS) {
                const prev = previousPatterns[patternId];
                const curr = currentPatterns[patternId];

                if (!prev || !curr || !prev.available || !curr.available) continue;

                const prevScore = prev.score || 0;
                const currScore = curr.score || 0;

                if (prevScore === 0) continue; // Avoid division by zero

                const relativeChange = (currScore - prevScore) / prevScore;

                if (relativeChange <= THRESHOLDS.CRITICAL_PATTERN_DEGRADATION) {
                    const absoluteChange = currScore - prevScore;

                    warnings.push({
                        type: 'CRITICAL_PATTERN_DEGRADATION',
                        severity: relativeChange <= -0.3 ? 'high' : 'medium',
                        pattern: patternId,
                        patternName: prev.pattern || patternId,
                        previousScore: Math.round(prevScore),
                        currentScore: Math.round(currScore),
                        absoluteChange: Math.round(absoluteChange),
                        relativeChange: Math.round(relativeChange * 100),
                        message: `⚠️ ${prev.pattern || patternId} ухудшился на ${Math.abs(Math.round(relativeChange * 100))}%`,
                        detail: `С ${Math.round(prevScore)} до ${Math.round(currScore)} за 7 дней`,
                        action: `Изучить insight: «${curr.insight || 'N/A'}»`,
                        actionable: true
                    });
                }
            }
        }

        // Check 2: Current low pattern scores (absolute values) - ALWAYS check this
        console.log('ews / detect 🔍 checking current pattern scores...');

        // Convert patterns array to id-indexed object for easier lookup
        const patternsById = {};
        if (Array.isArray(currentPatterns)) {
            for (const p of currentPatterns) {
                if (p.pattern) patternsById[p.pattern] = p;
            }
        } else {
            // Already an object
            Object.assign(patternsById, currentPatterns);
        }

        const allPatternIds = Object.keys(patternsById);

        for (const patternId of allPatternIds) {
            const pattern = patternsById[patternId];
            if (!pattern || !pattern.available) continue;

            const score = pattern.score || 0;
            const isCritical = CRITICAL_PATTERNS.includes(patternId);

            console.log(`ews / detect 📊 ${patternId}: score=${score}, critical=${isCritical}`);

            // Get human-friendly messages
            const humanMsg = PATTERN_HUMAN_MESSAGES[patternId] || {
                title: pattern.pattern || patternId,
                message: `Показатель требует внимания (score ${Math.round(score)})`,
                insight: pattern.insight || 'Обратите внимание на этот аспект питания',
                science: 'Подробности доступны в разделе Insights'
            };

            // Critical patterns: HIGH severity if below 35, MEDIUM if 35-50
            if (isCritical && score > 0 && score < PATTERN_LOW_SCORE_THRESHOLDS.critical) {
                warnings.push({
                    type: 'LOW_PATTERN_SCORE',
                    severity: 'high',
                    pattern: patternId,
                    patternName: humanMsg.title,
                    currentScore: Math.round(score),
                    threshold: PATTERN_LOW_SCORE_THRESHOLDS.critical,
                    message: `🚨 ${humanMsg.title}`,
                    detail: humanMsg.message,
                    insight: humanMsg.insight,
                    science: humanMsg.science,
                    actionable: true
                });
            }
            // Critical patterns: MEDIUM severity if 35-50 (still needs attention)
            else if (isCritical && score >= PATTERN_LOW_SCORE_THRESHOLDS.critical && score < 50) {
                warnings.push({
                    type: 'LOW_PATTERN_SCORE',
                    severity: 'medium',
                    pattern: patternId,
                    patternName: humanMsg.title,
                    currentScore: Math.round(score),
                    threshold: 50,
                    message: `⚠️ ${humanMsg.title}`,
                    detail: humanMsg.message,
                    insight: humanMsg.insight,
                    science: humanMsg.science,
                    actionable: true
                });
            }
            // Non-critical patterns: warn if below 45
            else if (!isCritical && score > 0 && score < PATTERN_LOW_SCORE_THRESHOLDS.important) {
                warnings.push({
                    type: 'LOW_PATTERN_SCORE',
                    severity: 'medium',
                    pattern: patternId,
                    patternName: humanMsg.title,
                    currentScore: Math.round(score),
                    threshold: PATTERN_LOW_SCORE_THRESHOLDS.important,
                    message: `⚠️ ${humanMsg.title}`,
                    detail: humanMsg.message,
                    insight: humanMsg.insight,
                    science: humanMsg.science,
                    actionable: true
                });
            }
        }

        console.log(`ews / detect ✅ found ${warnings.length} pattern-related warnings`);

        return warnings.length > 0 ? warnings : null;
    }

    /**
     * Warning 3: Status Score decline
     * @param {object[]} days - Array of day objects with statusScore property
     * @returns {object|null}
     */
    function checkStatusScoreDecline(days) {
        console.log('ews / detect 📊 checking status score decline...');

        if (days.length < THRESHOLDS.STATUS_SCORE_DECLINE_DAYS) {
            console.log('ews / detect ⏭️ skip: insufficient days', days.length, '<', THRESHOLDS.STATUS_SCORE_DECLINE_DAYS);
            return null;
        }

        // Get Status Score for recent days
        const recentDays = days.slice(-7); // Most recent 7 days
        const statusScores = [];

        for (const day of recentDays) {
            // Try to get cached status or calculate
            let status = null;

            if (day.statusScore !== undefined) {
                status = day.statusScore;
            } else if (day._statusCalculated && day._statusCalculated.score !== undefined) {
                status = day._statusCalculated.score;
            } else if (typeof HEYS !== 'undefined' && HEYS.Status && typeof HEYS.Status.calculate === 'function') {
                // Try to calculate status Score on the fly
                try {
                    const result = HEYS.Status.calculate({ dayData: day });
                    if (result && result.score !== undefined) {
                        status = result.score;
                    }
                } catch (e) {
                    console.warn('ews / detect ⚠️ failed to calculate status for day', day.date, e.message);
                }
            }

            if (status !== null && status !== undefined) {
                statusScores.push({
                    date: day.date,
                    score: status
                });
            }
        }

        console.log(`ews / detect 📊 collected ${statusScores.length} status scores from ${recentDays.length} days`);

        if (statusScores.length < THRESHOLDS.STATUS_SCORE_DECLINE_DAYS) {
            console.log('ews / detect ⏭️ skip: not enough status scores', statusScores.length, '<', THRESHOLDS.STATUS_SCORE_DECLINE_DAYS);
            return null;
        }

        // Check consecutive decline
        const recent = statusScores.slice(-THRESHOLDS.STATUS_SCORE_DECLINE_DAYS);
        const scores = recent.map(d => d.score);
        const isDecline = isConsecutiveDecline(scores, THRESHOLDS.STATUS_SCORE_DECLINE_DAYS);

        console.log(`ews / detect 📊 status scores (${recent.length}d):`, scores.map(s => Math.round(s)));

        if (isDecline) {
            const totalDrop = scores[0] - scores[scores.length - 1];
            const avgDailyDrop = calculateDeclineRate(scores);

            console.log(`ews / detect 📉 decline detected: totalDrop=${totalDrop.toFixed(1)}, avgDaily=${avgDailyDrop.toFixed(1)}`);

            if (totalDrop >= THRESHOLDS.STATUS_SCORE_MIN_DELTA) {
                const startScore = Math.round(scores[0]);
                const endScore = Math.round(scores[scores.length - 1]);

                const humanMsg = WARNING_HUMAN_MESSAGES.STATUS_SCORE_DECLINE;

                const warning = {
                    type: 'STATUS_SCORE_DECLINE',
                    severity: totalDrop >= 20 ? 'high' : (totalDrop >= 15 ? 'medium' : 'low'),
                    days: THRESHOLDS.STATUS_SCORE_DECLINE_DAYS,
                    startScore,
                    endScore,
                    totalDrop: Math.round(totalDrop),
                    avgDailyDrop: Math.round(avgDailyDrop * 10) / 10,
                    patternName: humanMsg.title,
                    message: `📊 ${humanMsg.title}`,
                    detail: `С ${startScore} до ${endScore} (падение ${Math.round(totalDrop)} баллов за ${THRESHOLDS.STATUS_SCORE_DECLINE_DAYS} дня)`,
                    insight: humanMsg.insight,
                    science: humanMsg.science,
                    currentScore: endScore,
                    actionable: true
                };

                console.log(`ews / detect ✅ STATUS_SCORE_DECLINE warning: severity=${warning.severity}, drop=${warning.totalDrop}`);
                return warning;
            }
        }

        console.log('ews / detect ➖ no status score decline detected');
        return null;
    }

    /**
     * Warning 4: Sleep debt accumulation
     * @param {object[]} days - Array of day objects (sorted oldest to newest)
     * @param {object} profile
     * @returns {object|null}
     */
    function checkSleepDebt(days, profile) {
        console.log('ews / detect 🛌 checking sleep debt...');
        if (days.length < THRESHOLDS.SLEEP_DEFICIT_DAYS) {
            console.log('ews / detect ⏭️ skip: insufficient days', days.length, '<', THRESHOLDS.SLEEP_DEFICIT_DAYS);
            return null;
        }

        const targetSleep = profile?.sleepHours || 8;
        const recentDays = days.slice(-7); // Most recent first

        const sleepData = recentDays.map(day => {
            const sleep = day.sleepHours || (day.sleepStart && day.sleepEnd
                ? calculateSleepHours(day.sleepStart, day.sleepEnd)
                : null);
            return { date: day.date, sleep, deficit: sleep ? Math.max(0, targetSleep - sleep) : 0 };
        }).filter(d => d.sleep !== null);

        if (sleepData.length < THRESHOLDS.SLEEP_DEFICIT_DAYS) {
            console.log('ews / detect ⏭️ skip: not enough sleep data after filter', sleepData.length, '<', THRESHOLDS.SLEEP_DEFICIT_DAYS);
            return null;
        }

        // Check last N consecutive days (most recent first)
        const recent = sleepData.slice(0, THRESHOLDS.SLEEP_DEFICIT_DAYS);
        const allDeficit = recent.every(d => d.sleep < THRESHOLDS.SLEEP_DEFICIT_HOURS);

        console.log(`ews / detect 📊 sleep stats: recent=${recent.length}d, avgSleep=${(recent.reduce((s, d) => s + d.sleep, 0) / recent.length).toFixed(1)}h, target=${targetSleep}h, allDeficit=${allDeficit}`);

        if (allDeficit) {
            const totalDeficit = recent.reduce((sum, d) => sum + d.deficit, 0);
            const avgSleep = recent.reduce((sum, d) => sum + d.sleep, 0) / recent.length;

            const humanMsg = WARNING_HUMAN_MESSAGES.SLEEP_DEBT;

            // Sleep quality score: 0-100 based on how close to target
            // If avgSleep < target: score shows percentage of target met
            // If avgSleep >= target: score is 100 (fully met)
            const sleepQualityScore = Math.min(100, Math.round((avgSleep / targetSleep) * 100));

            const warning = {
                type: 'SLEEP_DEBT',
                severity: totalDeficit > 6 ? 'high' : 'medium',
                days: THRESHOLDS.SLEEP_DEFICIT_DAYS,
                avgSleep: Math.round(avgSleep * 10) / 10,
                targetSleep,
                totalDeficit: Math.round(totalDeficit * 10) / 10,
                patternName: humanMsg.title,
                message: `💤 ${humanMsg.title}`,
                detail: `Средний сон: ${Math.round(avgSleep * 10) / 10}ч (цель: ${targetSleep}ч), дефицит: ${Math.round(totalDeficit * 10) / 10}ч`,
                insight: humanMsg.insight,
                science: humanMsg.science,
                currentScore: sleepQualityScore,
                actionable: true
            };
            console.log(`ews / detect ✅ SLEEP_DEBT warning: severity=${warning.severity}, totalDeficit=${warning.totalDeficit}h`);
            return warning;
        }

        console.log('ews / detect ➖ no sleep debt detected');
        return null;
    }

    /**
     * Warning 4: Caloric debt accumulation
     * @param {object[]} days - Array of day objects (sorted oldest to newest)
     * @param {object} profile
     * @param {object} pIndex
     * @returns {object|null}
     */
    function checkCaloricDebt(days, profile, pIndex) {
        console.log('ews / detect 🍽️ checking caloric debt...');
        if (days.length < THRESHOLDS.CALORIC_DEBT_DAYS) {
            console.log('ews / detect ⏭️ skip: insufficient days', days.length, '<', THRESHOLDS.CALORIC_DEBT_DAYS);
            return null;
        }

        const calculateItemKcal = HEYS.InsightsPI?.calculations?.calculateItemKcal;
        if (!calculateItemKcal) {
            console.warn('ews / detect ⏭️ skip: calculateItemKcal not available');
            return null;
        }

        const recentDays = days.slice(-7); // Most recent first
        const optimum = profile?.optimum || profile?.norm?.kcal || 2000;

        const caloricData = recentDays.map(day => {
            let eaten = day.savedEatenKcal || 0;

            if (!eaten && day.meals) {
                eaten = day.meals.reduce((sum, meal) => {
                    if (!meal.items) return sum;
                    return sum + meal.items.reduce((mealSum, item) => {
                        return mealSum + calculateItemKcal(item, pIndex);
                    }, 0);
                }, 0);
            }

            const debt = Math.max(0, optimum - eaten);
            return { date: day.date, eaten, optimum, debt };
        });

        // Check last N consecutive days (most recent first)
        const recent = caloricData.slice(0, THRESHOLDS.CALORIC_DEBT_DAYS);
        const totalDebt = recent.reduce((sum, d) => sum + d.debt, 0);

        console.log(`ews / detect 📊 caloric stats: recent=${recent.length}d, optimum=${optimum}kcal, totalDebt=${Math.round(totalDebt)}kcal, threshold=${THRESHOLDS.CALORIC_DEBT_THRESHOLD}kcal`);

        if (totalDebt > THRESHOLDS.CALORIC_DEBT_THRESHOLD) {
            const avgEaten = recent.reduce((sum, d) => sum + d.eaten, 0) / recent.length;
            const avgDebt = totalDebt / recent.length;

            const humanMsg = WARNING_HUMAN_MESSAGES.CALORIC_DEBT;

            // Caloric intake score: 0-100 based on how close to optimum
            // Shows percentage of target calories consumed
            const calorieIntakeScore = Math.min(100, Math.round((avgEaten / optimum) * 100));

            const warning = {
                type: 'CALORIC_DEBT',
                severity: totalDebt > 2500 ? 'high' : 'medium',
                days: THRESHOLDS.CALORIC_DEBT_DAYS,
                avgEaten: Math.round(avgEaten),
                optimum,
                totalDebt: Math.round(totalDebt),
                avgDebt: Math.round(avgDebt),
                patternName: humanMsg.title,
                message: `🍽️ ${humanMsg.title}`,
                detail: `Средний недобор: ${Math.round(avgDebt)} ккал/день (всего ${Math.round(totalDebt)} ккал)`,
                insight: humanMsg.insight,
                science: humanMsg.science,
                currentScore: calorieIntakeScore,
                actionable: true
            };
            console.log(`ews / detect ✅ CALORIC_DEBT warning: severity=${warning.severity}, totalDebt=${warning.totalDebt}kcal`);
            return warning;
        }

        console.log('ews / detect ➖ no caloric debt detected');
        return null;
    }

    /**
     * Warning 6: Weight spike (Tier 1 — v2.0)
     * Detects rapid weight gain (1+ kg in 1-2 days)
     * @param {object[]} days - Array of day objects (sorted oldest to newest)
     * @param {object} profile
     * @returns {object|null}
     */
    function checkWeightSpike(days, profile) {
        console.info('ews / detect 🔍 checking weight spike...');
        if (days.length < 3) {
            console.info('ews / detect ⏭️ skip weight spike: insufficient days', days.length);
            return null;
        }

        const recentDays = days.slice(-3).reverse(); // Most recent first
        const weightData = recentDays
            .map(day => ({
                date: day.date,
                weight: day.weightMorning || null
            }))
            .filter(d => d.weight !== null && d.weight > 0);

        if (weightData.length < 2) {
            console.info('ews / detect ⏭️ skip weight spike: insufficient weight data', weightData.length);
            return null;
        }

        // Check difference between most recent and previous
        const current = weightData[0].weight;
        const previous = weightData[1].weight;
        const delta = current - previous;
        const daysDiff = 1; // Консервативно: считаем что измерения за 1-2 дня

        console.info('ews / detect 📊 weight data:', {
            current: current.toFixed(1),
            previous: previous.toFixed(1),
            delta: delta.toFixed(2),
            daysDiff
        });

        // Threshold: +1 kg or more
        if (delta >= 1.0) {
            const humanMsg = WARNING_HUMAN_MESSAGES.WEIGHT_SPIKE;
            const percentChange = Math.round((delta / previous) * 100 * 10) / 10;

            // Score: 100 = no spike, lower = bigger spike
            // Formula: max(0, 100 - (delta * 30)) where delta in kg
            const spikeScore = Math.max(0, Math.round(100 - (delta * 30)));

            const warning = {
                type: 'WEIGHT_SPIKE',
                severity: delta >= 1.5 ? 'high' : 'medium',
                delta: Math.round(delta * 10) / 10,
                current: Math.round(current * 10) / 10,
                previous: Math.round(previous * 10) / 10,
                percentChange,
                patternName: humanMsg.title,
                message: `⚖️ ${humanMsg.title}`,
                detail: `Вес вырос с ${previous.toFixed(1)} до ${current.toFixed(1)} кг (+${delta.toFixed(1)} кг, +${percentChange}%)`,
                insight: humanMsg.insight,
                science: humanMsg.science,
                currentScore: spikeScore,
                actionable: true
            };
            console.info('ews / detect ✅ WEIGHT_SPIKE warning:', {
                severity: warning.severity,
                delta: warning.delta,
                currentScore: warning.currentScore
            });
            return warning;
        }

        console.info('ews / detect ➖ no weight spike detected');
        return null;
    }

    /**
     * Warning 7: Hydration deficit (Tier 1 — v2.0)
     * Detects consistently low water intake (<1.5L for 3+ days)
     * @param {object[]} days - Array of day objects (sorted oldest to newest)
     * @param {object} profile
     * @returns {object|null}
     */
    function checkHydrationDeficit(days, profile) {
        console.info('ews / detect 💧 checking hydration deficit...');
        if (days.length < 3) {
            console.info('ews / detect ⏭️ skip hydration: insufficient days', days.length);
            return null;
        }

        const targetWater = profile?.waterTarget || 2000; // ml
        const minThreshold = 1500; // ml (critical minimum)
        const consecutiveDays = 3;

        const recentDays = days.slice(-7); // Check last week
        const hydrationData = recentDays
            .map(day => ({
                date: day.date,
                water: day.waterMl || 0
            }))
            .filter(d => d.water >= 0); // Keep even 0 values (logged but no water)

        if (hydrationData.length < consecutiveDays) {
            console.info('ews / detect ⏭️ skip hydration: insufficient data', hydrationData.length);
            return null;
        }

        // Check last N consecutive days
        const recent = hydrationData.slice(-consecutiveDays);
        const allBelowThreshold = recent.every(d => d.water < minThreshold);

        console.info('ews / detect 📊 hydration data:', {
            recent: recent.length,
            avgWater: Math.round(recent.reduce((s, d) => s + d.water, 0) / recent.length),
            target: targetWater,
            threshold: minThreshold,
            allBelow: allBelowThreshold
        });

        if (allBelowThreshold) {
            const avgWater = recent.reduce((s, d) => s + d.water, 0) / recent.length;
            const humanMsg = WARNING_HUMAN_MESSAGES.HYDRATION_DEFICIT;

            // Score: 0-100 based on % of target met
            const hydrationScore = Math.min(100, Math.round((avgWater / targetWater) * 100));

            const warning = {
                type: 'HYDRATION_DEFICIT',
                severity: avgWater < 1000 ? 'high' : 'medium',
                days: consecutiveDays,
                avgWater: Math.round(avgWater),
                targetWater,
                deficit: Math.round(targetWater - avgWater),
                patternName: humanMsg.title,
                message: `💧 ${humanMsg.title}`,
                detail: `Средний объём: ${Math.round(avgWater)} мл (цель: ${targetWater} мл, дефицит: ${Math.round(targetWater - avgWater)} мл)`,
                insight: humanMsg.insight,
                science: humanMsg.science,
                currentScore: hydrationScore,
                actionable: true
            };
            console.info('ews / detect ✅ HYDRATION_DEFICIT warning:', {
                severity: warning.severity,
                avgWater: warning.avgWater,
                currentScore: warning.currentScore
            });
            return warning;
        }

        console.info('ews / detect ➖ no hydration deficit detected');
        return null;
    }

    /**
     * Warning 8: Logging gap (Tier 1 — v2.0)
     * Detects missing food diary entries (2+ consecutive days)
     * @param {object[]} days - Array of day objects (sorted oldest to newest)
     * @param {object} profile
     * @returns {object|null}
     */
    function checkLoggingGap(days, profile) {
        console.info('ews / detect 📝 checking logging gap...');
        if (days.length < 2) {
            console.info('ews / detect ⏭️ skip logging gap: insufficient days', days.length);
            return null;
        }

        const minConsecutiveGap = 2;
        const recentDays = days.slice(-7); // Check last week

        // Determine if a day has meaningful food logging
        const loggingData = recentDays.map(day => {
            const hasMeals = day.meals && day.meals.length > 0;
            const hasItems = hasMeals && day.meals.some(meal => meal.items && meal.items.length > 0);
            const logged = hasItems || (day.savedEatenKcal && day.savedEatenKcal > 0);
            return {
                date: day.date,
                logged
            };
        });

        // Find consecutive gaps (most recent first)
        let consecutiveGap = 0;
        const reversed = loggingData.slice().reverse();
        for (const entry of reversed) {
            if (!entry.logged) {
                consecutiveGap++;
            } else {
                break; // Stop at first logged day
            }
        }

        console.info('ews / detect 📊 logging data:', {
            totalDays: loggingData.length,
            loggedDays: loggingData.filter(d => d.logged).length,
            consecutiveGap,
            threshold: minConsecutiveGap
        });

        if (consecutiveGap >= minConsecutiveGap) {
            const humanMsg = WARNING_HUMAN_MESSAGES.LOGGING_GAP;

            // Score: 100 = no gap, decreases with gap size
            // Formula: max(0, 100 - (gap * 20))
            const consistencyScore = Math.max(0, Math.round(100 - (consecutiveGap * 20)));

            const warning = {
                type: 'LOGGING_GAP',
                severity: consecutiveGap >= 4 ? 'high' : 'medium',
                consecutiveGap,
                patternName: humanMsg.title,
                message: `📝 ${humanMsg.title}`,
                detail: `Нет записей ${consecutiveGap} ${consecutiveGap === 2 ? 'дня' : 'дней'} подряд`,
                insight: humanMsg.insight,
                science: humanMsg.science,
                currentScore: consistencyScore,
                actionable: true
            };
            console.info('ews / detect ✅ LOGGING_GAP warning:', {
                severity: warning.severity,
                gap: warning.consecutiveGap,
                currentScore: warning.currentScore
            });
            return warning;
        }

        console.info('ews / detect ➖ no logging gap detected');
        return null;
    }

    /**
     * Warning 9: Protein deficit (Tier 2 — v2.0)
     * Detects insufficient protein intake (<1.2 g/kg for 3+ days)
     * @param {object[]} days - Array of day objects (sorted oldest to newest)
     * @param {object} profile
     * @param {object} pIndex
     * @returns {object|null}
     */
    function checkProteinDeficit(days, profile, pIndex) {
        console.info('ews / detect 🥩 checking protein deficit...');
        if (days.length < 3) {
            console.info('ews / detect ⏭️ skip protein: insufficient days', days.length);
            return null;
        }

        const weight = profile?.weight || 70;
        const targetProtein = 1.2; // g/kg (minimum for muscle preservation)
        const minProteinGrams = weight * targetProtein;
        const consecutiveDays = 3;

        const recentDays = days.slice(-7);
        const proteinData = recentDays.map(day => {
            const protein = day.savedEatenProt || 0;
            return {
                date: day.date,
                protein,
                deficit: protein < minProteinGrams
            };
        }).filter(d => d.protein > 0); // Only days with data

        if (proteinData.length < consecutiveDays) {
            console.info('ews / detect ⏭️ skip protein: insufficient data', proteinData.length);
            return null;
        }

        const recent = proteinData.slice(-consecutiveDays);
        const allDeficit = recent.every(d => d.deficit);

        console.info('ews / detect 📊 protein data:', {
            recent: recent.length,
            avgProtein: Math.round(recent.reduce((s, d) => s + d.protein, 0) / recent.length),
            target: Math.round(minProteinGrams),
            allDeficit
        });

        if (allDeficit) {
            const avgProtein = recent.reduce((s, d) => s + d.protein, 0) / recent.length;
            const humanMsg = WARNING_HUMAN_MESSAGES.PROTEIN_DEFICIT;

            // Score: 0-100 based on % of target met
            const proteinScore = Math.min(100, Math.round((avgProtein / minProteinGrams) * 100));

            const warning = {
                type: 'PROTEIN_DEFICIT',
                severity: avgProtein < (minProteinGrams * 0.8) ? 'high' : 'medium',
                days: consecutiveDays,
                avgProtein: Math.round(avgProtein),
                targetProtein: Math.round(minProteinGrams),
                deficit: Math.round(minProteinGrams - avgProtein),
                patternName: humanMsg.title,
                message: `🥩 ${humanMsg.title}`,
                detail: `Средний белок: ${Math.round(avgProtein)}г (цель: ${Math.round(minProteinGrams)}г, дефицит: ${Math.round(minProteinGrams - avgProtein)}г)`,
                insight: humanMsg.insight,
                science: humanMsg.science,
                currentScore: proteinScore,
                actionable: true
            };
            console.info('ews / detect ✅ PROTEIN_DEFICIT warning:', {
                severity: warning.severity,
                avgProtein: warning.avgProtein,
                currentScore: warning.currentScore
            });
            return warning;
        }

        console.info('ews / detect ➖ no protein deficit detected');
        return null;
    }

    /**
     * Warning 10: Stress accumulation (Tier 2 — v2.0)
     * Detects high stress without recovery (stress score or low recovery days)
     * @param {object[]} days - Array of day objects (sorted oldest to newest)
     * @param {object} profile
     * @returns {object|null}
     */
    function checkStressAccumulation(days, profile) {
        console.info('ews / detect 😰 checking stress accumulation...');
        if (days.length < 5) {
            console.info('ews / detect ⏭️ skip stress: insufficient days', days.length);
            return null;
        }

        const recentDays = days.slice(-7);
        const stressData = recentDays.map(day => {
            // Multiple signals of stress:
            // 1. Explicit stress field
            const explicitStress = day.stress || 0;
            // 2. Poor sleep (<6h)
            const poorSleep = day.sleepHours && day.sleepHours < 6;
            // 3. High training load without recovery
            const highTraining = day.training && day.training.duration > 90;
            // 4. Low status score (<50)
            const lowStatus = day.statusScore !== undefined && day.statusScore < 50;

            const stressSignals = [
                explicitStress > 7,
                poorSleep,
                highTraining,
                lowStatus
            ].filter(Boolean).length;

            return {
                date: day.date,
                stressLevel: explicitStress,
                stressSignals,
                highStress: stressSignals >= 2
            };
        });

        // Check for 3+ days with 2+ stress signals
        const consecutiveStress = 3;
        const recent = stressData.slice(-consecutiveStress);
        const highStressDays = recent.filter(d => d.highStress).length;

        console.info('ews / detect 📊 stress data:', {
            recent: recent.length,
            highStressDays,
            avgSignals: (recent.reduce((s, d) => s + d.stressSignals, 0) / recent.length).toFixed(1),
            threshold: consecutiveStress
        });

        if (highStressDays >= consecutiveStress) {
            const avgStressLevel = recent.reduce((s, d) => s + d.stressLevel, 0) / recent.length;
            const humanMsg = WARNING_HUMAN_MESSAGES.STRESS_ACCUMULATION;

            // Score: 100 = no stress, decreases with stress signals
            const avgSignals = recent.reduce((s, d) => s + d.stressSignals, 0) / recent.length;
            const recoveryScore = Math.max(0, Math.round(100 - (avgSignals * 25)));

            const warning = {
                type: 'STRESS_ACCUMULATION',
                severity: avgSignals >= 3 ? 'high' : 'medium',
                days: highStressDays,
                avgStressLevel: Math.round(avgStressLevel),
                avgSignals: Math.round(avgSignals * 10) / 10,
                patternName: humanMsg.title,
                message: `😰 ${humanMsg.title}`,
                detail: `${highStressDays} дней с множественными признаками стресса (сон, нагрузка, состояние)`,
                insight: humanMsg.insight,
                science: humanMsg.science,
                currentScore: recoveryScore,
                actionable: true
            };
            console.info('ews / detect ✅ STRESS_ACCUMULATION warning:', {
                severity: warning.severity,
                highStressDays: warning.days,
                currentScore: warning.currentScore
            });
            return warning;
        }

        console.info('ews / detect ➖ no stress accumulation detected');
        return null;
    }

    /**
     * Warning 11: Meal skip pattern (Tier 2 — v2.0)
     * Detects frequent meal skipping (irregular meal timing or missing meals)
     * @param {object[]} days - Array of day objects (sorted oldest to newest)
     * @param {object} profile
     * @returns {object|null}
     */
    function checkMealSkipPattern(days, profile) {
        console.info('ews / detect 🍽️ checking meal skip pattern...');
        if (days.length < 5) {
            console.info('ews / detect ⏭️ skip meal pattern: insufficient days', days.length);
            return null;
        }

        const recentDays = days.slice(-7);
        const mealData = recentDays.map(day => {
            const meals = day.meals || [];
            const mealCount = meals.length;
            const hasMeals = mealCount > 0;

            // Check for long gaps (>8h) between meals
            let longGaps = 0;
            if (meals.length >= 2) {
                for (let i = 1; i < meals.length; i++) {
                    const prevTime = meals[i - 1].time;
                    const currTime = meals[i].time;
                    if (prevTime && currTime) {
                        const gap = calculateTimeGap(prevTime, currTime);
                        if (gap > 8) longGaps++;
                    }
                }
            }

            const irregular = mealCount < 2 || longGaps > 0;

            return {
                date: day.date,
                mealCount,
                longGaps,
                irregular
            };
        }).filter(d => d.mealCount > 0); // Only days with logged meals

        if (mealData.length < 5) {
            console.info('ews / detect ⏭️ skip meal pattern: insufficient meal data', mealData.length);
            return null;
        }

        const irregularDays = mealData.filter(d => d.irregular).length;
        const irregularRate = irregularDays / mealData.length;

        console.info('ews / detect 📊 meal pattern data:', {
            totalDays: mealData.length,
            irregularDays,
            irregularRate: (irregularRate * 100).toFixed(0) + '%',
            threshold: '60%'
        });

        // Warning if >60% of days have irregular patterns
        if (irregularRate > 0.6) {
            const avgMealCount = mealData.reduce((s, d) => s + d.mealCount, 0) / mealData.length;
            const humanMsg = WARNING_HUMAN_MESSAGES.MEAL_SKIP_PATTERN;

            // Score: 100 = regular, decreases with irregularity
            const regularityScore = Math.max(0, Math.round((1 - irregularRate) * 100));

            const warning = {
                type: 'MEAL_SKIP_PATTERN',
                severity: irregularRate > 0.8 ? 'high' : 'medium',
                irregularDays,
                totalDays: mealData.length,
                irregularRate: Math.round(irregularRate * 100),
                avgMealCount: Math.round(avgMealCount * 10) / 10,
                patternName: humanMsg.title,
                message: `🍽️ ${humanMsg.title}`,
                detail: `${irregularDays} из ${mealData.length} дней с пропусками или длинными перерывами (>8ч)`,
                insight: humanMsg.insight,
                science: humanMsg.science,
                currentScore: regularityScore,
                actionable: true
            };
            console.info('ews / detect ✅ MEAL_SKIP_PATTERN warning:', {
                severity: warning.severity,
                irregularRate: warning.irregularRate + '%',
                currentScore: warning.currentScore
            });
            return warning;
        }

        console.info('ews / detect ➖ no meal skip pattern detected');
        return null;
    }

    /**
     * Warning 12: Binge risk (Tier 2 — v2.0)
     * Detects binge eating patterns (large portions after long gaps)
     * @param {object[]} days - Array of day objects (sorted oldest to newest)
     * @param {object} profile
     * @param {object} pIndex
     * @returns {object|null}
     */
    function checkBingeRisk(days, profile, pIndex) {
        console.info('ews / detect 🍔 checking binge risk...');
        if (days.length < 5) {
            console.info('ews / detect ⏭️ skip binge: insufficient days', days.length);
            return null;
        }

        const calculateItemKcal = HEYS.InsightsPI?.calculations?.calculateItemKcal;
        if (!calculateItemKcal) {
            console.info('ews / detect ⏭️ skip binge: calculateItemKcal not available');
            return null;
        }

        const optimum = profile?.optimum || profile?.norm?.kcal || 2000;
        const recentDays = days.slice(-7);

        const bingeData = recentDays.map(day => {
            const meals = day.meals || [];
            let bingeMeals = 0;

            // Check each meal for binge characteristics:
            // 1. Large meal (>40% daily optimum in one meal)
            // 2. Multiple large meals in same day
            meals.forEach(meal => {
                if (!meal.items) return;
                const mealKcal = meal.items.reduce((sum, item) => sum + calculateItemKcal(item, pIndex), 0);
                const mealPercent = (mealKcal / optimum) * 100;
                if (mealPercent > 40) {
                    bingeMeals++;
                }
            });

            const totalKcal = day.savedEatenKcal || meals.reduce((sum, meal) => {
                if (!meal.items) return sum;
                return sum + meal.items.reduce((mealSum, item) => mealSum + calculateItemKcal(item, pIndex), 0);
            }, 0);

            // Binge indicators:
            // - 2+ large meals in one day
            // - OR single huge meal (>50% optimum)
            // - OR extreme overeating (>150% optimum) after restriction
            const extremeOvereat = totalKcal > (optimum * 1.5);
            const bingeDay = bingeMeals >= 2 || extremeOvereat;

            return {
                date: day.date,
                bingeMeals,
                totalKcal,
                bingeDay
            };
        }).filter(d => d.totalKcal > 0);

        if (bingeData.length < 3) {
            console.info('ews / detect ⏭️ skip binge: insufficient data', bingeData.length);
            return null;
        }

        const bingeDays = bingeData.filter(d => d.bingeDay).length;
        const bingeRate = bingeDays / bingeData.length;

        console.info('ews / detect 📊 binge data:', {
            totalDays: bingeData.length,
            bingeDays,
            bingeRate: (bingeRate * 100).toFixed(0) + '%',
            threshold: '30%'
        });

        // Warning if >30% of days show binge patterns
        if (bingeRate > 0.3) {
            const avgBingeMeals = bingeData.reduce((s, d) => s + d.bingeMeals, 0) / bingeData.length;
            const humanMsg = WARNING_HUMAN_MESSAGES.BINGE_RISK;

            // Score: 100 = no binge, decreases with binge frequency
            const controlScore = Math.max(0, Math.round((1 - bingeRate) * 100));

            const warning = {
                type: 'BINGE_RISK',
                severity: bingeRate > 0.5 ? 'high' : 'medium',
                bingeDays,
                totalDays: bingeData.length,
                bingeRate: Math.round(bingeRate * 100),
                avgBingeMeals: Math.round(avgBingeMeals * 10) / 10,
                patternName: humanMsg.title,
                message: `🍔 ${humanMsg.title}`,
                detail: `${bingeDays} из ${bingeData.length} дней с признаками переедания (крупные порции >40% нормы)`,
                insight: humanMsg.insight,
                science: humanMsg.science,
                currentScore: controlScore,
                actionable: true
            };
            console.info('ews / detect ✅ BINGE_RISK warning:', {
                severity: warning.severity,
                bingeRate: warning.bingeRate + '%',
                currentScore: warning.currentScore
            });
            return warning;
        }

        console.info('ews / detect ➖ no binge risk detected');
        return null;
    }

    /**
     * Warning 13: Mood/wellbeing decline (Tier 3 — v3.0)
     * Detects prolonged mood or wellbeing decline (7+ days)
     * @param {object[]} days - Array of day objects (sorted oldest to newest)
     * @param {object} profile
     * @returns {object|null}
     */
    function checkMoodWellbeingDecline(days, profile) {
        console.info('ews / detect 😔 checking mood/wellbeing decline...');
        if (days.length < 7) {
            console.info('ews / detect ⏭️ skip mood: insufficient days', days.length);
            return null;
        }

        const recentDays = days.slice(-14); // Check last 2 weeks
        const moodData = recentDays.map(day => ({
            date: day.date,
            mood: day.mood || null,
            wellbeing: day.wellbeing || null,
            energy: day.energy || null
        })).filter(d => d.mood !== null || d.wellbeing !== null || d.energy !== null);

        if (moodData.length < 7) {
            console.info('ews / detect ⏭️ skip mood: insufficient mood data', moodData.length);
            return null;
        }

        // Check for consecutive decline trend (7+ days)
        const recent = moodData.slice(-7);
        const avgMood = recent.filter(d => d.mood !== null).length > 0
            ? recent.filter(d => d.mood !== null).reduce((s, d) => s + d.mood, 0) / recent.filter(d => d.mood !== null).length
            : null;

        const avgWellbeing = recent.filter(d => d.wellbeing !== null).length > 0
            ? recent.filter(d => d.wellbeing !== null).reduce((s, d) => s + d.wellbeing, 0) / recent.filter(d => d.wellbeing !== null).length
            : null;

        console.info('ews / detect 📊 mood data:', {
            recent: recent.length,
            avgMood: avgMood !== null ? avgMood.toFixed(1) : 'N/A',
            avgWellbeing: avgWellbeing !== null ? avgWellbeing.toFixed(1) : 'N/A',
            threshold: '< 5.0'
        });

        // Warning if average mood or wellbeing <5 over 7 days (scale 1-10)
        const lowMood = avgMood !== null && avgMood < 5;
        const lowWellbeing = avgWellbeing !== null && avgWellbeing < 5;

        if (lowMood || lowWellbeing) {
            const humanMsg = WARNING_HUMAN_MESSAGES.MOOD_WELLBEING_DECLINE;
            const score = avgMood !== null ? Math.round(avgMood * 10) : Math.round(avgWellbeing * 10);

            const warning = {
                type: 'MOOD_WELLBEING_DECLINE',
                severity: score < 40 ? 'high' : 'medium',
                days: 7,
                avgMood: avgMood !== null ? Math.round(avgMood * 10) / 10 : null,
                avgWellbeing: avgWellbeing !== null ? Math.round(avgWellbeing * 10) / 10 : null,
                patternName: humanMsg.title,
                message: `😔 ${humanMsg.title}`,
                detail: `Средний уровень: ${avgMood !== null ? 'mood ' + avgMood.toFixed(1) : ''}${avgMood && avgWellbeing ? ', ' : ''}${avgWellbeing !== null ? 'wellbeing ' + avgWellbeing.toFixed(1) : ''} (норма: ≥5)`,
                insight: humanMsg.insight,
                science: humanMsg.science,
                currentScore: score,
                actionable: true
            };
            console.info('ews / detect ✅ MOOD_WELLBEING_DECLINE warning:', {
                severity: warning.severity,
                score: warning.currentScore
            });
            return warning;
        }

        console.info('ews / detect ➖ no mood/wellbeing decline detected');
        return null;
    }

    /**
     * Warning 14: Weight plateau (Tier 3 — v3.0)
     * Detects weight not changing for 14+ days when goal is weight loss
     * @param {object[]} days - Array of day objects (sorted oldest to newest)
     * @param {object} profile
     * @returns {object|null}
     */
    function checkWeightPlateau(days, profile) {
        console.info('ews / detect ⚖️ checking weight plateau...');
        if (days.length < 14) {
            console.info('ews / detect ⏭️ skip plateau: insufficient days', days.length);
            return null;
        }

        // Only check if goal is weight loss
        const goal = profile?.goal;
        if (!goal || goal !== 'loss') {
            console.info('ews / detect ⏭️ skip plateau: goal is not weight loss', goal);
            return null;
        }

        const recentDays = days.slice(-21); // Check last 3 weeks
        const weightData = recentDays.map(day => ({
            date: day.date,
            weight: day.weightMorning || null
        })).filter(d => d.weight !== null && d.weight > 0);

        if (weightData.length < 14) {
            console.info('ews / detect ⏭️ skip plateau: insufficient weight data', weightData.length);
            return null;
        }

        // Check if weight variation is <1kg over 14 days
        const recent = weightData.slice(-14);
        const weights = recent.map(d => d.weight);
        const minWeight = Math.min(...weights);
        const maxWeight = Math.max(...weights);
        const variation = maxWeight - minWeight;

        console.info('ews / detect 📊 weight plateau data:', {
            days: recent.length,
            minWeight: minWeight.toFixed(1),
            maxWeight: maxWeight.toFixed(1),
            variation: variation.toFixed(2),
            threshold: '< 1.0 kg'
        });

        // Warning if variation <1kg (plateau)
        if (variation < 1.0) {
            const humanMsg = WARNING_HUMAN_MESSAGES.WEIGHT_PLATEAU;
            const avgWeight = weights.reduce((s, w) => s + w, 0) / weights.length;

            // Score: lower = plateau (less variation)
            // 100 = normal progress (>2kg variation), 0 = complete plateau
            const progressScore = Math.min(100, Math.round(variation * 50));

            const warning = {
                type: 'WEIGHT_PLATEAU',
                severity: variation < 0.5 ? 'high' : 'medium',
                days: 14,
                variation: Math.round(variation * 100) / 100,
                avgWeight: Math.round(avgWeight * 10) / 10,
                patternName: humanMsg.title,
                message: `⚖️ ${humanMsg.title}`,
                detail: `Вес колеблется в пределах ${variation.toFixed(1)} кг за 14 дней (среднее: ${avgWeight.toFixed(1)} кг)`,
                insight: humanMsg.insight,
                science: humanMsg.science,
                currentScore: progressScore,
                actionable: true
            };
            console.info('ews / detect ✅ WEIGHT_PLATEAU warning:', {
                severity: warning.severity,
                variation: warning.variation,
                currentScore: warning.currentScore
            });
            return warning;
        }

        console.info('ews / detect ➖ no weight plateau detected');
        return null;
    }

    /**
     * Warning 15: Weekend pattern (Tier 3 — v3.0)
     * Detects worse nutrition patterns on Sat/Sun (excess calories, alcohol, carbs)
     * @param {object[]} days - Array of day objects (sorted oldest to newest)
     * @param {object} profile
     * @param {object} pIndex
     * @returns {object|null}
     */
    function checkWeekendPattern(days, profile, pIndex) {
        console.info('ews / detect 🍻 checking weekend pattern...');
        if (days.length < 14) {
            console.info('ews / detect ⏭️ skip weekend: insufficient days', days.length);
            return null;
        }

        const calculateItemKcal = HEYS.InsightsPI?.calculations?.calculateItemKcal;
        if (!calculateItemKcal) {
            console.info('ews / detect ⏭️ skip weekend: calculateItemKcal not available');
            return null;
        }

        const optimum = profile?.optimum || profile?.norm?.kcal || 2000;
        const recentDays = days.slice(-21); // Last 3 weeks

        const weekdayData = [];
        const weekendData = [];

        recentDays.forEach(day => {
            const dayOfWeek = new Date(day.date).getDay(); // 0=Sun, 6=Sat
            const isWeekend = dayOfWeek === 0 || dayOfWeek === 6;

            let totalKcal = day.savedEatenKcal || 0;
            if (!totalKcal && day.meals) {
                totalKcal = day.meals.reduce((sum, meal) => {
                    if (!meal.items) return sum;
                    return sum + meal.items.reduce((mealSum, item) => mealSum + calculateItemKcal(item, pIndex), 0);
                }, 0);
            }

            const excessKcal = totalKcal > optimum ? totalKcal - optimum : 0;

            const dataPoint = {
                date: day.date,
                totalKcal,
                excessKcal,
                hasAlcohol: day.alcohol || false
            };

            if (isWeekend) {
                weekendData.push(dataPoint);
            } else {
                weekdayData.push(dataPoint);
            }
        });

        if (weekendData.length < 4 || weekdayData.length < 10) {
            console.info('ews / detect ⏭️ skip weekend: insufficient data', {
                weekendDays: weekendData.length,
                weekdays: weekdayData.length
            });
            return null;
        }

        const avgWeekendKcal = weekendData.reduce((s, d) => s + d.totalKcal, 0) / weekendData.length;
        const avgWeekdayKcal = weekdayData.reduce((s, d) => s + d.totalKcal, 0) / weekdayData.length;
        const weekendExcess = avgWeekendKcal - avgWeekdayKcal;
        const weekendExcessPercent = (weekendExcess / avgWeekdayKcal) * 100;

        const weekendAlcoholDays = weekendData.filter(d => d.hasAlcohol).length;
        const weekendAlcoholRate = weekendAlcoholDays / weekendData.length;

        console.info('ews / detect 📊 weekend pattern data:', {
            avgWeekendKcal: Math.round(avgWeekendKcal),
            avgWeekdayKcal: Math.round(avgWeekdayKcal),
            weekendExcess: Math.round(weekendExcess),
            weekendExcessPercent: weekendExcessPercent.toFixed(0) + '%',
            alcoholRate: (weekendAlcoholRate * 100).toFixed(0) + '%',
            threshold: '+20% or alcohol >50%'
        });

        // Warning if weekend excess >20% OR alcohol >50% of weekends
        if (weekendExcessPercent > 20 || weekendAlcoholRate > 0.5) {
            const humanMsg = WARNING_HUMAN_MESSAGES.WEEKEND_PATTERN;

            // Score: 100 = consistent week, lower = worse weekends
            const consistencyScore = Math.max(0, Math.round(100 - weekendExcessPercent));

            const warning = {
                type: 'WEEKEND_PATTERN',
                severity: weekendExcessPercent > 30 || weekendAlcoholRate > 0.7 ? 'high' : 'medium',
                avgWeekendKcal: Math.round(avgWeekendKcal),
                avgWeekdayKcal: Math.round(avgWeekdayKcal),
                weekendExcess: Math.round(weekendExcess),
                weekendExcessPercent: Math.round(weekendExcessPercent),
                weekendAlcoholRate: Math.round(weekendAlcoholRate * 100),
                patternName: humanMsg.title,
                message: `🍻 ${humanMsg.title}`,
                detail: `Выходные: ${Math.round(avgWeekendKcal)} ккал (будни: ${Math.round(avgWeekdayKcal)} ккал, +${Math.round(weekendExcessPercent)}%), алкоголь: ${Math.round(weekendAlcoholRate * 100)}% дней`,
                insight: humanMsg.insight,
                science: humanMsg.science,
                currentScore: consistencyScore,
                actionable: true
            };
            console.info('ews / detect ✅ WEEKEND_PATTERN warning:', {
                severity: warning.severity,
                excess: warning.weekendExcessPercent + '%',
                currentScore: warning.currentScore
            });
            return warning;
        }

        console.info('ews / detect ➖ no weekend pattern detected');
        return null;
    }

    /**
     * Helper: Calculate time gap between two time strings
     * @param {string} time1 - "08:30"
     * @param {string} time2 - "18:45"
     * @returns {number} - Hours between times
     */
    function calculateTimeGap(time1, time2) {
        if (!time1 || !time2) return 0;
        try {
            const [h1, m1] = time1.split(':').map(Number);
            const [h2, m2] = time2.split(':').map(Number);
            const minutes1 = h1 * 60 + m1;
            const minutes2 = h2 * 60 + m2;
            let diff = minutes2 - minutes1;
            if (diff < 0) diff += 24 * 60; // Handle overnight
            return diff / 60;
        } catch (e) {
            return 0;
        }
    }

    /**
     * Helper: Calculate sleep hours from start/end time
     * @param {string} start - "23:00"
     * @param {string} end - "07:30"
     * @returns {number}
     */
    function calculateSleepHours(start, end) {
        if (!start || !end) return 0;

        try {
            const [startH, startM] = start.split(':').map(Number);
            const [endH, endM] = end.split(':').map(Number);

            let hours = endH - startH;
            let minutes = endM - startM;

            if (hours < 0) hours += 24; // Overnight
            if (minutes < 0) {
                hours -= 1;
                minutes += 60;
            }

            return hours + minutes / 60;
        } catch (e) {
            return 0;
        }
    }

    /**
     * Main API: Detect all early warning signals
     * @param {object[]} days - Array of day objects (sorted oldest to newest)
     * @param {object} profile - User profile
     * @param {object} pIndex - Product index
     * @param {object} [options] - Optional previous/current pattern snapshots
     * @returns {object}
     */
    function detectEarlyWarnings(days, profile, pIndex, options = {}) {
        console.info('ews / detect 🚀 start:', {
            windowDays: days?.length || 0,
            checks: 15,
            tiers: '5 Baseline + 3 Tier1 + 4 Tier2 + 3 Tier3'
        });

        if (!Array.isArray(days) || days.length < THRESHOLDS.MIN_DAYS_FOR_ANALYSIS) {
            console.warn('ews / detect ⚠️ input.invalid:', {
                reason: 'insufficient_data',
                daysReceived: days?.length || 0,
                minRequired: THRESHOLDS.MIN_DAYS_FOR_ANALYSIS
            });
            return {
                available: false,
                reason: 'insufficient_data',
                minDaysRequired: THRESHOLDS.MIN_DAYS_FOR_ANALYSIS,
                warnings: [],
                trends: null
            };
        }

        console.info('ews / detect 📥 input.valid:', {
            daysCount: days.length,
            profileId: profile?.id,
            hasPIndex: !!pIndex,
            hasPatternSnapshot: !!options.previousPatterns
        });

        const warnings = [];

        console.info('ews / detect 🧮 compute:', { phase: 'running_15_checks', checksTotal: 15 });

        // Warning 1: Health Score decline
        const scoreWarning = checkHealthScoreDecline(days, profile, pIndex, options);
        if (scoreWarning) {
            console.info('ews / detect 🧮   ✅ check_1:', { name: 'HealthScore', status: 'WARNING' });
            warnings.push(scoreWarning);
        } else {
            console.info('ews / detect 🧮   ➖ check_1:', { name: 'HealthScore', status: 'clean' });
        }

        // Warning 2: Critical pattern degradation OR low pattern scores
        if (options.currentPatterns) {
            const patternWarnings = checkCriticalPatternDegradation(
                days,
                profile,
                pIndex,
                options.previousPatterns,  // optional
                options.currentPatterns    // required
            );
            if (patternWarnings) {
                console.info(`ews / detect 🧮   ✅ check_2:`, { name: 'Patterns', status: 'WARNING', count: patternWarnings.length });
                warnings.push(...patternWarnings);
            } else {
                console.info('ews / detect 🧮   ➖ check_2:', { name: 'Patterns', status: 'clean' });
            }
        } else {
            console.info('ews / detect 🧮   ⏭️ check_2:', { name: 'Patterns', status: 'skipped', reason: 'no_currentPatterns' });
        }

        // Warning 3: Status Score decline
        const statusWarning = checkStatusScoreDecline(days);
        if (statusWarning) {
            console.info('ews / detect 🧮   ✅ check_3:', { name: 'Status', status: 'WARNING' });
            warnings.push(statusWarning);
        } else {
            console.info('ews / detect 🧮   ➖ check_3:', { name: 'Status', status: 'clean' });
        }

        // Warning 4: Sleep debt
        const sleepWarning = checkSleepDebt(days, profile);
        if (sleepWarning) {
            console.info('ews / detect 🧮   ✅ check_4:', { name: 'Sleep', status: 'WARNING' });
            warnings.push(sleepWarning);
        } else {
            console.info('ews / detect 🧮   ➖ check_4:', { name: 'Sleep', status: 'clean' });
        }

        // Warning 5: Caloric debt
        const caloricWarning = checkCaloricDebt(days, profile, pIndex);
        if (caloricWarning) {
            console.info('ews / detect 🧮   ✅ check_5:', { name: 'Caloric', status: 'WARNING' });
            warnings.push(caloricWarning);
        } else {
            console.info('ews / detect 🧮   ➖ check_5:', { name: 'Caloric', status: 'clean' });
        }

        // === Tier 1 Warnings (v2.0) ===

        // Warning 6: Weight spike
        const weightWarning = checkWeightSpike(days, profile);
        if (weightWarning) {
            console.info('ews / detect 🧮   ✅ check_6:', { name: 'WeightSpike', status: 'WARNING' });
            warnings.push(weightWarning);
        } else {
            console.info('ews / detect 🧮   ➖ check_6:', { name: 'WeightSpike', status: 'clean' });
        }

        // Warning 7: Hydration deficit
        const hydrationWarning = checkHydrationDeficit(days, profile);
        if (hydrationWarning) {
            console.info('ews / detect 🧮   ✅ check_7:', { name: 'Hydration', status: 'WARNING' });
            warnings.push(hydrationWarning);
        } else {
            console.info('ews / detect 🧮   ➖ check_7:', { name: 'Hydration', status: 'clean' });
        }

        // Warning 8: Logging gap
        const loggingWarning = checkLoggingGap(days, profile);
        if (loggingWarning) {
            console.info('ews / detect 🧮   ✅ check_8:', { name: 'LoggingGap', status: 'WARNING' });
            warnings.push(loggingWarning);
        } else {
            console.info('ews / detect 🧮   ➖ check_8:', { name: 'LoggingGap', status: 'clean' });
        }

        // === Tier 2 Warnings (v2.5) ===

        // Warning 9: Protein deficit
        const proteinWarning = checkProteinDeficit(days, profile, pIndex);
        if (proteinWarning) {
            console.info('ews / detect 🧮   ✅ check_9:', { name: 'ProteinDeficit', status: 'WARNING' });
            warnings.push(proteinWarning);
        } else {
            console.info('ews / detect 🧮   ➖ check_9:', { name: 'ProteinDeficit', status: 'clean' });
        }

        // Warning 10: Stress accumulation
        const stressWarning = checkStressAccumulation(days, profile);
        if (stressWarning) {
            console.info('ews / detect 🧮   ✅ check_10:', { name: 'Stress', status: 'WARNING' });
            warnings.push(stressWarning);
        } else {
            console.info('ews / detect 🧮   ➖ check_10:', { name: 'Stress', status: 'clean' });
        }

        // Warning 11: Meal skip pattern
        const mealSkipWarning = checkMealSkipPattern(days, profile);
        if (mealSkipWarning) {
            console.info('ews / detect 🧮   ✅ check_11:', { name: 'MealSkip', status: 'WARNING' });
            warnings.push(mealSkipWarning);
        } else {
            console.info('ews / detect 🧮   ➖ check_11:', { name: 'MealSkip', status: 'clean' });
        }

        // Warning 12: Binge risk
        const bingeWarning = checkBingeRisk(days, profile, pIndex);
        if (bingeWarning) {
            console.info('ews / detect 🧮   ✅ check_12:', { name: 'BingeRisk', status: 'WARNING' });
            warnings.push(bingeWarning);
        } else {
            console.info('ews / detect 🧮   ➖ check_12:', { name: 'BingeRisk', status: 'clean' });
        }

        // === Tier 3 Warnings (v3.0) ===

        // Warning 13: Mood/wellbeing decline
        const moodWarning = checkMoodWellbeingDecline(days, profile);
        if (moodWarning) {
            console.info('ews / detect 🧮   ✅ check_13:', { name: 'MoodDecline', status: 'WARNING' });
            warnings.push(moodWarning);
        } else {
            console.info('ews / detect 🧮   ➖ check_13:', { name: 'MoodDecline', status: 'clean' });
        }

        // Warning 14: Weight plateau
        const plateauWarning = checkWeightPlateau(days, profile);
        if (plateauWarning) {
            console.info('ews / detect 🧮   ✅ check_14:', { name: 'WeightPlateau', status: 'WARNING' });
            warnings.push(plateauWarning);
        } else {
            console.info('ews / detect 🧮   ➖ check_14:', { name: 'WeightPlateau', status: 'clean' });
        }

        // Warning 15: Weekend pattern
        const weekendWarning = checkWeekendPattern(days, profile, pIndex);
        if (weekendWarning) {
            console.info('ews / detect 🧮   ✅ check_15:', { name: 'WeekendPattern', status: 'WARNING' });
            warnings.push(weekendWarning);
        } else {
            console.info('ews / detect 🧮   ➖ check_15:', { name: 'WeekendPattern', status: 'clean' });
        }

        // Sort by severity
        warnings.sort((a, b) => {
            const severityOrder = { high: 0, medium: 1, low: 2 };
            return severityOrder[a.severity] - severityOrder[b.severity];
        });

        console.info('ews / detect ✅ result:', {
            warningsDetected: warnings.length,
            types: warnings.map(w => w.type),
            severities: warnings.map(w => w.severity),
            highSeverity: warnings.filter(w => w.severity === 'high').length,
            mediumSeverity: warnings.filter(w => w.severity === 'medium').length,
            lowSeverity: warnings.filter(w => w.severity === 'low').length
        });

        // Visual summary table of all 15 checks (v3.0 verification +  pipeline logs v3.1)
        console.group('ews / detect 🖥️ ui.summary - All 15 Checks');
        const checkMapping = [
            { num: 1, name: 'Health Score Decline', tier: 'Baseline', types: ['HEALTH_SCORE_DECLINE'] },
            { num: 2, name: 'Pattern Issues', tier: 'Baseline', types: ['CRITICAL_PATTERN_DEGRADATION', 'LOW_PATTERN_SCORE'] },
            { num: 3, name: 'Status Score Decline', tier: 'Baseline', types: ['STATUS_SCORE_DECLINE'] },
            { num: 4, name: 'Sleep Debt', tier: 'Baseline', types: ['SLEEP_DEBT'] },
            { num: 5, name: 'Caloric Debt', tier: 'Baseline', types: ['CALORIC_DEBT'] },
            { num: 6, name: 'Weight Spike', tier: 'Tier 1', types: ['WEIGHT_SPIKE'] },
            { num: 7, name: 'Hydration Deficit', tier: 'Tier 1', types: ['HYDRATION_DEFICIT'] },
            { num: 8, name: 'Logging Gap', tier: 'Tier 1', types: ['LOGGING_GAP'] },
            { num: 9, name: 'Protein Deficit', tier: 'Tier 2', types: ['PROTEIN_DEFICIT'] },
            { num: 10, name: 'Stress Accumulation', tier: 'Tier 2', types: ['STRESS_ACCUMULATION'] },
            { num: 11, name: 'Meal Skip Pattern', tier: 'Tier 2', types: ['MEAL_SKIP_PATTERN'] },
            { num: 12, name: 'Binge Risk', tier: 'Tier 2', types: ['BINGE_RISK'] },
            { num: 13, name: 'Mood/Wellbeing Decline', tier: 'Tier 3', types: ['MOOD_WELLBEING_DECLINE'] },
            { num: 14, name: 'Weight Plateau', tier: 'Tier 3', types: ['WEIGHT_PLATEAU'] },
            { num: 15, name: 'Weekend Pattern', tier: 'Tier 3', types: ['WEEKEND_PATTERN'] }
        ];
        const warningTypes = new Set(warnings.map(function (w) { return w.type; }));
        const checkSummary = checkMapping.map(function (check) {
            var hasWarning = check.types.some(function (t) { return warningTypes.has(t); });
            return {
                '#': check.num,
                'Check Name': check.name,
                'Tier': check.tier,
                'Status': hasWarning ? '⚠️ WARNING' : '✓ Clean'
            };
        });
        console.table(checkSummary);
        console.info('ews / detect 🖥️ ui.legend:', 'Baseline(5) + Tier1(3) + Tier2(4) + Tier3(3) = 15 checks total');
        console.groupEnd();
        console.info('ews / detect 🖥️ ui.complete:', { warningsDisplayed: warnings.length, checksTotal: 15 });

        // Track warning trends (v3.1)
        const trendsResult = trackWarningTrends(warnings);

        // Prioritize warnings by severity × frequency × health impact (v3.1)
        const prioritizedWarnings = prioritizeWarnings(warnings, trendsResult.allTrends);

        return {
            available: true,
            count: warnings.length,
            warnings: prioritizedWarnings, // Prioritized warnings with scores
            summary: warnings.length === 0
                ? '✅ Негативных трендов не обнаружено'
                : `⚠️ Обнаружено ${warnings.length} warning signal${warnings.length > 1 ? 's' : ''}`,
            highSeverityCount: warnings.filter(w => w.severity === 'high').length,
            mediumSeverityCount: warnings.filter(w => w.severity === 'medium').length,
            // Trends tracking (v3.1)
            trends: {
                chronicWarnings: trendsResult.chronicWarnings,
                allTrends: trendsResult.allTrends
            },
            // Priority queue (v3.1)
            criticalPriority: prioritizedWarnings.filter(w => w.criticalPriority)
        };
    }

    // Export API
    HEYS.InsightsPI.earlyWarning = {
        detect: detectEarlyWarnings,
        trackTrends: trackWarningTrends,
        prioritize: prioritizeWarnings,
        thresholds: THRESHOLDS,
        healthImpact: HEALTH_IMPACT_SCORES,
        version: '3.1.0'
    };

    console.log('[HEYS.InsightsPI] ✅ Early Warning System v3.1 loaded (15 checks + trends + priority queue: severity × frequency × impact)');

})(typeof window !== 'undefined' ? window : global);
