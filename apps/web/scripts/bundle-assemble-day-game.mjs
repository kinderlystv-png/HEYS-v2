import { build } from 'esbuild';
import path from 'node:path';
import { fileURLToPath } from 'node:url';

const scriptDir = path.dirname(fileURLToPath(import.meta.url));
const webDir = path.resolve(scriptDir, '..');

await build({
  entryPoints: [path.join(webDir, 'assemble-day/heys_assemble_day_game_v1.ts')],
  outfile: path.join(webDir, 'heys_planning_game_assemble_day_v1.js'),
  bundle: true,
  format: 'iife',
  platform: 'browser',
  target: ['es2020'],
  legalComments: 'none',
  banner: { js: '// Auto-generated by apps/web/scripts/bundle-assemble-day-game.mjs. Do not edit manually.' },
});

console.log('Built heys_planning_game_assemble_day_v1.js');
