# 🐛 Telegram Mini App Authorization Debug Guide

## 📸 Текущая проблема (скриншот)

**Ошибка:** "Failed to fetch"  
**Контекст:** Авторизация не выполнена при запуске mini-app в Telegram

---

## 🔍 Результаты аудита

### ✅ Что работает ПРАВИЛЬНО:

1. **Telegram WebApp подключен** — initData передается корректно
2. **Пользователь идентифицирован:**
   - ID: `1393964759`
   - Username: `@poplanton`
   - Имя: `ant`
3. **Frontend корректно формирует запрос** к `/api/telegram/auth/verify`
4. **Vite proxy настроен** → `localhost:4001`
5. **Backend endpoint существует** в `packages/core/src/server/router.ts`

### ❌ Что НЕ работает:

**API сервер (порт 4001) НЕ ЗАПУЩЕН** ⚠️

```bash
# Проверка показала:
lsof -i :4001 | grep LISTEN
# Результат: Порт 4001 свободен
```

---

## 🛠️ Решение проблемы

### Вариант 1: Запустить API сервер (production режим)

```bash
# Запуск backend API
pnpm run dev:api

# ИЛИ через task:
# Start API Server (Port 4001)
```

**Проверка:**
```bash
curl http://localhost:4001/health
# Должно вернуть: {"status":"OK",...}
```

### Вариант 2: Использовать моки (для тестирования без backend)

Измените `.env.telegram`:
```env
VITE_USE_CLIENT_MOCKS=true  # Включить моки
```

Перезапустите mini-app:
```bash
pnpm --filter @heys/tg-mini dev
```

---

## 🔥 Network Flow (как это работает)

### Боевой режим (с backend):

```
┌─────────────────────┐
│ Telegram WebApp     │
│ (Android)           │
└──────────┬──────────┘
           │ initData
           ↓
┌─────────────────────┐
│ Mini-app Frontend   │
│ (Vite dev: 3002)    │
└──────────┬──────────┘
           │ POST /api/telegram/auth/verify
           ↓
┌─────────────────────┐
│ Vite Proxy          │
│ → localhost:4001    │
└──────────┬──────────┘
           │
           ↓
┌─────────────────────┐
│ API Server          │  ← ❌ НЕ ЗАПУЩЕН
│ (Express: 4001)     │
└──────────┬──────────┘
           │
           ↓
┌─────────────────────┐
│ Проверка подписи    │
│ HMAC-SHA256         │
└──────────┬──────────┘
           │
           ↓
     ✅ Token + Session
```

### Dev режим (моки):

```
┌─────────────────────┐
│ Mini-app Frontend   │
│ (USE_MOCKS=true)    │
└──────────┬──────────┘
           │
           ↓ Пропускает backend
           │
     ✅ Dev Session
     (без проверки подписи)
```

---

## 🐛 Новый Debug Console

Теперь **внутри Telegram mini-app** есть **визуальная консоль**:

### Как использовать:

1. **Открой mini-app в Telegram** (Android/iOS)
2. **Нажми на иконку 🐛** внизу справа
3. **Смотри логи в реальном времени:**
   - 🔵 **INFO** — обычные события
   - ✅ **SUCCESS** — успешные операции  
   - ⚠️ **WARN** — предупреждения
   - ❌ **ERROR** — ошибки

### Что логируется:

- ✅ HTTP запросы: URL, метод, статус
- ✅ Авторизация: initData, верификация
- ✅ Ошибки: network errors, validation errors
- ✅ Переходы между экранами

### Функции консоли:

- **Клик на лог** → полные детали + JSON data
- **Clear** → очистить консоль
- **× (крестик)** → свернуть консоль

---

## 📊 Примеры логов в Debug Console

### ✅ Успешная авторизация:

```
🔵 INFO | 16:06:12
Начинаем верификацию Telegram
data: { initDataLength: 487 }

🔵 INFO | 16:06:12
HTTP Request
data: {
  url: "http://localhost:4001/api/telegram/auth/verify",
  method: "POST",
  hasAuth: false,
  useMocks: false
}

✅ SUCCESS | 16:06:13
HTTP 200
data: { status: 200, statusText: "OK" }

✅ SUCCESS | 16:06:13
Авторизация успешна
data: { curatorId: "telegram-1393964759" }
```

### ❌ Ошибка "Failed to fetch":

```
🔵 INFO | 16:06:12
HTTP Request
data: {
  url: "http://localhost:4001/api/telegram/auth/verify",
  method: "POST"
}

❌ ERROR | 16:06:15
HTTP Error
data: {
  url: "http://localhost:4001/api/telegram/auth/verify",
  error: "Failed to fetch"
}

❌ ERROR | 16:06:15
Ошибка авторизации
data: { error: "Не удалось подтвердить авторизацию в Telegram" }
```

---

## 🚀 Быстрая инструкция (TL;DR)

### Проблема: "Failed to fetch"

**Причина:** API сервер не запущен

### Решение за 30 секунд:

```bash
# Терминал 1: Запусти API
pnpm run dev:api

# Терминал 2: Запусти mini-app
pnpm run dev:tg-mini

# Проверь:
curl http://localhost:4001/health
# ✅ {"status":"OK"}
```

### Или используй моки:

```bash
# .env.telegram
VITE_USE_CLIENT_MOCKS=true

pnpm run dev:tg-mini
```

---

## 📝 Проверочный чеклист

- [ ] API сервер запущен на порту 4001
- [ ] `curl http://localhost:4001/health` возвращает OK
- [ ] Mini-app запущен на порту 3002
- [ ] Vite proxy настроен на localhost:4001
- [ ] Debug console 🐛 доступна в mini-app
- [ ] В консоли видны HTTP запросы
- [ ] Telegram initData передается корректно
- [ ] Авторизация проходит успешно

---

## 🔧 Технические детали

### Environment Variables (.env.telegram):

```env
# API backend URL
VITE_API_URL=http://localhost:4001

# Отключить моки
VITE_USE_CLIENT_MOCKS=false
```

### Vite Proxy (vite.config.ts):

```typescript
server: {
  port: 3002,
  proxy: {
    '/api': {
      target: 'http://localhost:4001',
      changeOrigin: true
    }
  }
}
```

### Backend Environment (.env или tasks.json):

```env
PORT=4001
API_PORT=4001
TELEGRAM_BOT_TOKEN=8219830734:AAH...
TELEGRAM_ALLOWED_USER_IDS=1393964759
NODE_ENV=development
```

### Telegram HMAC Verification Flow:

```typescript
// Backend: packages/core/src/server/router.ts
serverRouter.post('/api/telegram/auth/verify', async (req, res) => {
  const { initData } = req.body;
  
  // 1. Проверяем подпись HMAC-SHA256
  const verification = verifyTelegramInitData(initData, TELEGRAM_BOT_TOKEN);
  
  // 2. Проверяем user в whitelist
  const allowedCheck = ensureUserAllowed(userPayload.id, TELEGRAM_ALLOWED_IDS);
  
  // 3. Создаем сессию
  const sessionPayload = createSessionPayload(userPayload);
  
  return res.json(sessionPayload);
});
```

---

## 📚 Полезные команды

### Проверка портов:

```bash
# Проверить порт 4001 (API)
lsof -i :4001

# Проверить порт 3002 (mini-app)
lsof -i :3002

# Убить процесс на порту
kill -9 $(lsof -ti :4001)
```

### Логирование backend:

```bash
# Запустить API с подробными логами
DEBUG=* pnpm run dev:api

# Смотреть логи в реальном времени
tail -f logs/api.log
```

### Тестирование endpoint:

```bash
# Проверить health
curl http://localhost:4001/health

# Тест авторизации (mock initData)
curl -X POST http://localhost:4001/api/telegram/auth/verify \
  -H "Content-Type: application/json" \
  -d '{"initData":"mock-data"}'
```

---

## 🎯 Следующие шаги

После исправления:

1. ✅ Запустить API сервер
2. ✅ Проверить авторизацию в Telegram
3. ✅ Убедиться что Debug Console работает
4. 🔄 Протестировать список клиентов
5. 🔄 Протестировать просмотр дня клиента

---

## 📞 Troubleshooting

### Проблема: "Origin not allowed by CORS"

**Решение:** Добавь ngrok домен в environment:

```env
API_ALLOWED_ORIGINS=https://tressy-cotyledonoid-vergie.ngrok-free.dev
```

### Проблема: "Сессия истекла"

**Причина:** TTL сессии = 12 часов

**Решение:** Нажми "Повторить проверку" в mini-app

### Проблема: Debug console не появляется

**Проверь:**
1. `<DebugPanel />` добавлен в `App.tsx`
2. HMR обновил компонент (перезагрузи страницу)
3. Иконка 🐛 видна внизу справа

---

## ✅ Резюме

**Главная проблема:** API сервер не запущен → fetch() не может подключиться к `localhost:4001`

**Решение:**
1. Запустить `pnpm run dev:api`
2. Использовать Debug Console 🐛 для отладки
3. Проверять логи HTTP запросов в реальном времени

**Теперь у тебя есть полноценная консоль внутри Telegram mini-app! 🚀**
