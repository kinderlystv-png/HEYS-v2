# Протокол реализации лендинга — 2026-06-22

> **Статус:** протокол локальной реализации лендинга; не является source of
> truth для статусов задач.

## Статус

Source-ready, local-dev проверен на `http://localhost:3003/`, без commit/push.
Production build не запускался.

## Сделано

1. Hero переведён на оффер «куратор ведёт питание с минимумом усилий».
2. Порядок секций приведён к новой логике: Hero → Как устроено → Demo → Pain →
   Первый месяц → Comparison → Navigator/RiskRadar → Trust → Lifestyle →
   Objections → Formats → Pricing → Trial → FAQ.
3. Демо оставлено, но открывается как зеркало виджетов (`defaultTab=widgets`).
4. Куратор обезличен: опыт более 20 лет в питании и сопровождении, без
   имени/фото.
5. SLA и скорость ответа вычищены из pricing/legal/form success-copy.
6. Claim по науке приведён к честной формуле: «41 паттерн с научными
   источниками», без публичного обещания PMID для каждого источника.
7. `pi_patterns.js` приведён к фактическому count: 41 экспорт и devLog
   `41 pattern analyzers`.
8. Добавлен lifestyle-блок как опциональная глубина, не ядро оффера.
9. Пустой social proof не рендерится, пока нет реальных отзывов.
10. Tablet-nav переведён на burger до `lg`, чтобы длинная навигация не ломала
    header.
11. Демо-модалка переведена на Вы-форму и спокойный тон.
12. Trust-блок очищен от служебных guardrail-слов: нет «обезличенно», «без
    фото», «без медицинских обещаний» как клиентского текста.
13. Hero укорочен до `calc(100dvh - 48px)`, чтобы в первом viewport был виден
    намёк следующей секции.

## Проверки

- `pnpm --filter='@heys/landing' run lint` — PASS.
- `pnpm exec tsc --noEmit --project apps/landing/tsconfig.json` — PASS.
- `pnpm verify:legacy-bundles` — PASS.
- `git diff --check` — PASS.
- SSR sanity: порядок ключевых секций PASS, запрещённые формулировки `[]`,
  пустой social proof отсутствует.
- Guardrail grep по `apps/landing/src` — PASS.
- Count `HEYS.InsightsPI.patterns` — 41.
- jsdom interaction smoke: hero CTA открывает выбор пола; после выбора iframe
  получает `gender=male&defaultTab=widgets&tab=widgets&view=widgets`.
- `pnpm dev:landing` — запущен, `curl -I http://localhost:3003/` вернул
  `200 OK`.
- Playwright visual smoke desktop `1440x900` + mobile `390x844` — PASS: 90
  проверок, 0 fail; нет горизонтального overflow; следующая секция видна в
  первом viewport; Demo iframe открывается с `defaultTab=widgets`.
- Скриншоты QA: `/private/tmp/heys-landing-desktop-top.png`,
  `/private/tmp/heys-landing-desktop-trust-content-fixed-clean.png`,
  `/private/tmp/heys-landing-desktop-after-demo.png`,
  `/private/tmp/heys-landing-mobile-top.png`,
  `/private/tmp/heys-landing-mobile-trust-content-fixed-clean.png`,
  `/private/tmp/heys-landing-mobile-after-demo.png`.

## Ограничение проверки

Production build не запускался по агентским правилам. Визуальный локальный QA
закрыт через Next dev-server и Playwright.

## Что ревьюеру смотреть

- Первый экран и tablet header: оффер, CTA, отсутствие переполнения навигации.
- DemoSection: открытие с `defaultTab=widgets`, не с ручного ввода.
- Pricing/legal/FAQ: нет SLA, medical claim, PMID claim и сравнения «мы лучше
  нутрициолога».
- Trust: нет фейковых отзывов, счётчиков и имени/фото куратора.
