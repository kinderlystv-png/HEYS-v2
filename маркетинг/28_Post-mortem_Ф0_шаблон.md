# 28 · Post-mortem Фазы 0 (шаблон)

> Реализация п.4.7 плана `22`. Заполняется **в конце Фазы 0**. Владелец —
> основатель. Строится строго по таблице гейтов `25`: что было, как прошло, где
> сработали красные триггеры. Цель — извлечь уроки до найма внешних кураторов и
> до масштабирования трафика.

---

## 0. Шапка

- Период Ф0: `<дата старта>` → `<дата завершения>`
- Итог: набрано оплат `<N>` из 15 · перешли в Ф1? `да/нет`
- Заполнил: основатель · дата: `<…>`

---

## 1. Гейты — факт по таблице `25`

| Гейт / метрика       | Цель   | **Факт** | Сколько занял | 🔴 Срабатывал? Что делали |
| -------------------- | ------ | -------- | ------------- | ------------------------- |
| Объём: оплат за фазу | 15     | `<…>`    | `<…>`         | `<…>`                     |
| Триал → оплата       | ≥ 30%  | `<…>`    | —             | `<…>`                     |
| Лид → триал          | ≥ 40%  | `<…>`    | —             | `<…>`                     |
| Отток / мес          | ≤ 9%   | `<…>`    | —             | `<…>`                     |
| SLA-нарушения        | ≤ 5%   | `<…>`    | —             | `<…>`                     |
| Отзывы / кейсы       | 5+     | `<…>`    | `<…>`         | `<…>`                     |
| Плейбук куратора     | готов  | `<…>`    | `<…>`         | `<…>`                     |
| Юр-структура НПД     | готова | `<…>`    | `<…>`         | `<…>`                     |

Ранняя сигналка (активный триал) — насколько помогла поймать красное раньше:
`<…>`

---

## 2. Каналы и измеримость

- Лучший источник по `неделя→оплата`: `<…>` · худший: `<…>` (по
  `18`/KPI-трекеру).
- CPL по каналам: `<…>`. Где деньги жглись зря: `<…>`.
- Контур измеримости (`18`) реально работал? Чего не хватило: `<…>`.

---

## 3. Два свободных поля (главное)

**Что нас удивило** (чего не было в методологии — рынок/клиенты/процесс):

> `<…>`

**Что выкинуть из плейбука/процесса** (что не сработало или съедало время зря —
кандидаты на удаление из `23`/`22`/воронки):

> `<…>`

---

## 4. Выводы → действия

- В плейбук куратора (`23`) внести: `<…>`
- В методологию/канон (`15` журнал §2): `<…>`
- Решения по развилкам Этапа 4 на основе данных Ф0 (Lite, Solo, цена Ф1): `<…>`
- Готовы ли к Ф1 (все гейты зелёные)? `да/нет` — если нет, что добиваем: `<…>`
