#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "🧪 Запуск тестов перед push..."

# Запускаем тесты sync-race-condition (критические для синхронизации)
pnpm --filter @heys/web test -- __tests__/sync-race-condition.test.js

# Проверяем код выхода
if [ $? -ne 0 ]; then
  echo "❌ Тесты не прошли! Push отменён."
  exit 1
fi

echo "✅ Тесты прошли успешно!"
