#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "🧪 Запуск критических тестов sync/auth/storage/products..."

# Запускаем тесты для синхронизации, авторизации, storage layer и защиты продуктов
pnpm --filter @heys/web test -- \
  __tests__/sync-race-condition.test.js \
  __tests__/merge-day-data.test.js \
  __tests__/auth-session.test.js \
  __tests__/storage-layer.test.js \
  __tests__/products-protection.test.js

# Проверяем код выхода
if [ $? -ne 0 ]; then
  echo "❌ Тесты не прошли! Push отменён."
  exit 1
fi

echo "✅ Все критические тесты прошли (130 tests)!"
