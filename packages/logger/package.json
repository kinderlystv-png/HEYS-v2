{
  "name": "@heys/logger",
  "version": "1.0.0",
  "description": "Centralized logging system for HEYS",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsup src/index.ts --format esm",
    "build:types": "tsup src/index.ts --format esm --dts",
    "dev": "tsup src/index.ts --format esm --watch",
    "type-check": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "pino": "^9.9.4",
    "pino-http": "^10.5.0",
    "pino-multi-stream": "^6.0.0",
    "pino-pretty": "^13.1.1"
  },
  "devDependencies": {
    "tsup": "^7.3.0",
    "typescript": "^5.9.2"
  }
}
