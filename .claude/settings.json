{
  "$comment": "Project-level Claude Code settings (committed, team-shared). Personal overrides go in .claude/settings.local.json (gitignored).",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "node $CLAUDE_PROJECT_DIR/scripts/hooks/block-subagent-push.mjs"
          }
        ]
      }
    ]
  }
}
