# HEYS — Краткий бриф (Layer 0/1/2)

> **Версия:** v1.1  
> **Дата:** 2025-12-26  
> **Статус:** 🟡 SOFT LAUNCH — триал-машина задеплоена, ожидается E2E + ЮKassa  
> **Полная версия:** `HEYS_BRIEF_FULL.md` (1200+ строк)

---

## Layer 0 — One-pager (операционный дэшборд)

### Что такое HEYS

**HEYS = PWA + умные подсказки + живой куратор (Pro/Pro+) + протоколы
сопровождения.**  
Суть: _внешний контур ответственности_ + снятие рутины (дневник можно вести за
клиента).

### Прод-ссылки

- PWA: `https://app.heyslab.ru`
- Landing: `https://heyslab.ru`
- API: `https://api.heyslab.ru`

### Тарифы (v1)

| Тариф    | Цена/мес | Что входит                                                  |
| -------- | -------: | ----------------------------------------------------------- |
| **Base** |  1 990 ₽ | Приложение + подсказки + **1 чек-ин/неделю (async)**        |
| **Pro**  | 12 990 ₽ | Base + **ведение дневника куратором** + чат + созвон/неделя |
| **Pro+** | 19 990 ₽ | Pro + **7/7** + приоритетный SLA + mid-week чек-ин          |

**Триал:** 7 дней Pro бесплатно, старт = **первый утренний чек-ин
(регистрационный ✅)**.  
**Оплата:** ежемесячно, **автосписаний нет**, продление вручную.  
**Возврат:** пропорционально неоказанным дням.  
**Заморозка:** до 2 недель (форс-мажор).

### SLA (коротко)

- Часы: 09:00–21:00 (время клиента)
- Реакция: ≤30 минут (в рабочее время)
- Отметки: 👍 принято → ✅ внесено → ❓ уточнить → ⚠ срочно/анти-срыв
- Pro: выходные дежурный режим; Pro+: полный режим 7/7

### Граница "не медицина"

Сервис **не медицинская услуга**. Куратор **не врач**: не диагнозы, не лечение,
не препараты.

### Compliance checklist (актуально)

- **ПДн первично в РФ:** Yandex Cloud (PostgreSQL, Functions, API Gateway).
- **Трансграничка OFF:** GA4/Meta Pixel выключены.
- **Telegram:** уведомления без ПДн (только `lead_id`).
- **Фото еды:** HEYS не хранит изображения (фото остаются в мессенджере), хранит
  только текст дневника (продукты/граммы/КБЖУ).
- **Спецкатегории (здоровье):** отдельное согласие + отдельный шаг.
- **ПЭП:** телефон+PIN + действие в UI → логируется как `checkbox_after_auth`
  (SMS как усиление позже).

### Блокеры "деньги в прод" (4)

1. **E2E тестирование триал-машины** — полный flow регистрация → триал →
   read_only
2. ЮKassa: `shopId/secretKey`
3. Webhook: `payment_succeeded` → активация подписки
4. Фискализация чеков (54-ФЗ) через ОФД/провайдера

---

## Layer 1 — Продукт и операционка (что делает пользователь)

### Главный экран (цель: 3–5 секунд)

Показываем:

- **Статус 0–100** (топ-2 причины + 1–2 шага)
- окно следующего приёма пищи
- остаток ккал/БЖУ
- риск срыва (светофор)
- лента дня

### Trial → подписка → read-only

- Триал: 7 дней Pro, старт с первого утреннего чек-ина.
- После триала без оплаты: **read-only** (история/аналитика доступны, добавление
  данных заблокировано) + paywall.

### Режимы ведения дневника

- Base: пользователь ведёт дневник сам + еженедельный чек-ин.
- Pro/Pro+: пользователь может вносить сам, но ценность — куратор
  ведёт/корректирует и удерживает курс.

### "Без весов"

Если весов нет: допускаем **approx** (оценка порции по описанию/эталону) — фича
v2, на старте можно решать вручную протоколом.

---

## Layer 2 — Техническая связка (для команды)

### Данные и API (РФ)

- PostgreSQL: Yandex Cloud Managed PostgreSQL (ru-central1)
- API: Yandex Cloud Functions + API Gateway
- PWA: Nginx VM proxy → Object Storage (контроль Cache-Control, чтобы SW
  обновлялся)

### Auth

- Клиент: телефон + PIN (PIN хранится хэшем bcrypt)
- **Session-based auth (v3):** `verify_client_pin_v3` → `session_token` (UUID)
  - Токен хранится в localStorage: `heys_session_token`
  - Все RPC-вызовы передают `session_token` вместо `client_id`
  - Сервер извлекает `client_id` из сессии
  - Сессии истекают: 30 дней или по ручному revoke
- **Rate-limit:** 5 неверных PIN → блокировка 15 мин
- Куратор: email + пароль (`heys-api-auth` + JWT)
- Desktop Gate: по умолчанию выключен для клиентов

🔐 **Безопасность API:** [SECURITY_RUNBOOK.md](./SECURITY_RUNBOOK.md)

### Подписки

**Состояния:** `none → trial → active → read_only → canceled`

**Таблицы PostgreSQL (Yandex Cloud):** ✅ Задеплоены 2025-12-26

- `client_sessions` — сессии клиентов (session_token, client_id, expires_at)
- `subscriptions` — подписки (status, trial_started_at, active_until)
- `trial_queue` — очередь на триал (client_id, status, offer_expires_at)

**RPC-функции:** ✅ Smoke-tests passed

- `verify_client_pin_v3(phone, pin, ip, user_agent)` → { session_token,
  client_id, subscription }
- `get_subscription_status_by_session(session_token)` → { status,
  days_remaining, ... }
- `start_trial_by_session(session_token)` → void (триал 7 дней)
- `revoke_session(session_token)` → void (logout)

**UI-модули:**

- `HEYS.Subscription` — управление подписками (getStatus, startTrial)
- `HEYS.Paywall` — paywall и gating (showPaywall, ReadOnlyBanner, checkAccess)

**Gating:** ✅ Write-guard задеплоен

- Read-only mode: история доступна, добавление данных заблокировано
- Banner в DayTab + блокировка add product / add meal
- **Server-side защита:** `subscription_can_write()` блокирует KV-операции для
  `none`/`read_only`

### Очередь на триал (Trial Queue)

**Проблема:** Куратор может вести 3–5 клиентов на триале. При наплыве 20 человек
— качество падает.

**Решение:** Умная очередь на бесплатный триал + покупка всегда без очереди.

**Таблицы PostgreSQL:**

- `trial_queue` — очередь (client_id UNIQUE, status, queued_at,
  offer_expires_at)
- `curator_trial_limits` — лимиты (max_active_trials, is_accepting_trials)
- `trial_queue_events` — аналитика (event_type, meta)

**Состояния очереди:**
`queued → offer → assigned | expired | canceled | canceled_by_purchase`

**RPC-функции (реализованы):**

- `get_public_trial_capacity()` — публичный виджет (available_slots, queue_size)
- `request_trial(session_token, source)` → offer или позиция в очереди
- `get_trial_queue_status(session_token)` → статус/позиция/таймер
- `claim_trial_offer(session_token)` → старт триала
- `cancel_trial_queue(session_token)` → отмена
- `assign_trials_from_queue()` → выдача offers (cron каждые 5–10 мин)

**Ключевые правила:**

- Очередь **только на бесплатное** — покупка любого тарифа всегда сразу
- Offer window: **2 часа** на подтверждение
- Позиция вычисляется по `(priority DESC, queued_at ASC)` — нельзя перепрыгнуть
- `pg_advisory_xact_lock` защищает от oversubscribe при наплыве
- Покупка автоматически снимает из очереди (`canceled_by_purchase`)

**Антиабьюз:**

- 1 триал на телефон
- Cooldown 30 дней после отмены/истечения
- Повторный `request_trial` НЕ сбрасывает позицию

**Подробная документация:** `docs/tasks/2025-12-24-trial-queue.md`

### Тестирование Trial Queue

**Предусловия:**

1. PostgreSQL миграция применена (`database/2025-12-25_trial_queue.sql`)
2. CF `heys-api-rpc` задеплоена с новыми функциями в `ALLOWED_FUNCTIONS`
3. `curator_trial_limits` настроена (max_active_trials = 3)

**Тест 1: Публичный виджет**

```bash
curl -s "https://api.heyslab.ru/rpc?fn=get_public_trial_capacity" -X POST -H "Content-Type: application/json" -d "{}"
# Ожидается: { available_slots, total_slots, queue_size, is_accepting, offer_window_minutes, trial_days }
```

**Тест 2: Запрос триала (есть слоты)**

```bash
curl -s "https://api.heyslab.ru/rpc?fn=request_trial" -X POST -H "Content-Type: application/json" \
  -d '{"session_token":"<TOKEN>", "source":"test"}'
# Ожидается: { status: "offer", offer_expires_at, position: null }
```

**Тест 3: Запрос триала (нет слотов)**

```bash
# После заполнения лимита:
# Ожидается: { status: "queued", position: N, queue_size: M }
```

**Тест 4: Claim offer**

```bash
curl -s "https://api.heyslab.ru/rpc?fn=claim_trial_offer" -X POST -H "Content-Type: application/json" \
  -d '{"session_token":"<TOKEN>"}'
# Ожидается: { success: true, trial_ends_at }
```

**Тест 5: Проверка позиции**

```bash
curl -s "https://api.heyslab.ru/rpc?fn=get_trial_queue_status" -X POST -H "Content-Type: application/json" \
  -d '{"session_token":"<TOKEN>"}'
# Ожидается: { status, position, offer_expires_at, ... }
```

**Тест 6: Отмена и повторный запрос**

```bash
# cancel_trial_queue → request_trial
# Ожидается: новая запись с новым queued_at (если прошёл cooldown)
```

**E2E сценарий (в браузере):**

1. Авторизоваться как новый клиент (без триала)
2. Проверить виджет мест на лендинге
3. Нажать "Начать триал" → получить offer или позицию
4. Подтвердить offer → триал стартует
5. Проверить subscription_status = 'trial'

### Модули (в проде / ключевые)

- дневник питания, продукты/поиск
- widgets dashboard
- инсулиновая волна
- умные советы (≈182)
- подписки/paywall (без провайдера оплаты пока)
- consent flow (ПЭП + отдельное health consent)

### 🔬 Ключевая аналитика (выдержка)

- **TEF / TDEE** — стандарт Atwater 4/4/9; TEF считается как расход (25% белок /
  7.5% углеводы / 1.5% жиры), TDEE = BMR (Mifflin-St Jeor) + активность + NDTE +
  цикл, норма (optimum) от baseExpenditure без TEF.
- **Дополнения** — каталог добавок с таймингом/синергиями; наукоёмкие подсказки
  (утро/вечер), insulinBonus до −20%.
- **Док-источник** — `DATA_MODEL_REFERENCE.md` (TEF/TDEE/Supplements) +
  `heys_supplements_v1.js`, `heys_supplements_science_v1.js`.

### 📊 Расширенный блок (для лендинга)

**Что собираем (основные сущности и метрики):**

- День (`DayRecord`): дата, сон (начало/конец/качество/часы), вес, шаги, вода
  (объём+время), бытовая активность (минуты/время), комментарий дня, средние
  настроение/самочувствие/стресс, цикл (день фазы), до 3 тренировок (время, тип,
  минуты по 4 HR-зонам), замеры тела, `updatedAt`.
- Питание (`Meal → MealItem`): время, настроение/стресс/самочувствие на приёме;
  продукты и граммы.
- Продукт: БЖУ на 100 г, простые/сложные угли, trans/bad/good fat, клетчатка,
  GI, harm, категория, порции (ручные/авто ~25 паттернов).
- Производные (`dayTot`, `normAbs`): 12 сумм/норм (kcal, carbs, simple, complex,
  prot, fat, bad, good, trans, fiber, gi, harm) + нормы в граммах/ккал.
- Допконтекст: цель (дефицит/профицит), статус подписки
  (trial/active/read-only), namespace localStorage по clientId.

**“Умные” модули:**

- Советы (~182): цель, время, тренировки, стресс, цикл.
- Инсулиновая волна (37 факторов): GL/GI, макросы, форма/жидкость, порции,
  активность до/после, NDTE, циркадные, возраст/BMI/пол, цикл, добавки.
- Status Score 0–100: 9 факторов, 4 категории (nutrition, activity, recovery,
  hydration).
- Gamification: 25 уровней, 30+ достижений, XP-ивенты.
- Caloric analytics: Caloric Debt (3 дня), Caloric Excess (мягкая коррекция,
  акцент на активность), Refeed Day (+35% норма).
- TDEE: BMR (Mifflin-St Jeor) + MET-тренировки + шаги + NEAT + NDTE + TEF.
- TEF: Atwater 4/4/9, коэффициенты 25%/7.5%/1.5%.
- Phenotype & Predictive Insights: 6 фенотипов, 5 traits,
  приоритеты/риски/инсайты.
- Cycle: 4 фазы, множители для калорий/воды/волны, задержка воды.
- Supplements: тайминг/категории/взаимодействия/курсы; science-слой
  (биодоступность, формы, синергии).

**Главные витрины для лендинга:**

- Статус дня 0–100 + причины и шаги.
- Калорийный прогресс и ratio-зоны (crash/low/good/perfect/over/binge) с
  цветами.
- Таймер инсулиновой волны (когда снова «сжигаем жир»), учёт активности до/после
  еды.
- Карточки долг/профицит: мягкая коррекция, рекомендации по активности.
- Вода/сон/шаги/тренировки: цели, прогресс, бейджи.
- Советы и наука: подсказки с PMID, персонализация по
  цели/стрессу/циклу/тренировкам.
- Gamification: streak, уровни, достижения.
- Безопасность и оффлайн: localStorage с clientId namespace, история доступна в
  read-only.

**Ключевые числа для копира:**

- 182+ персональных советов.
- 37 факторов метаболической модели (инсулиновая волна).
- 9 факторов Status Score, 4 категории.
- 25 уровней геймификации, 30+ достижений.
- TEF/TDEE учитывают переваривание и вчерашние тренировки (NDTE).
- Цикл: 4 фазы, корректировки норм и задержка воды.

### Финансы и Чеки (54-ФЗ) `[BLOCKER]`

**Контекст:** ИП на ПСН (фото) + HEYS (онлайн-услуги). **Задача:** Легальный
приём платежей от физлиц + чеки.

**Ключевые решения:**

1. **Юр.лицо:** ИП (текущее или новое).
2. **Налоги:** ПСН (фото) + УСН (HEYS) или только УСН.
3. **ОКВЭД (IT/SaaS):**
   - **63.11** (Основной) — Обработка данных, хостинг (SaaS)
   - **62.01** — Разработка ПО
   - **62.09** — Деятельность в области ИТ прочая
   - **63.99.1** — Информационные услуги
   - 🚫 **86.\*** (Медицина) — ИСКЛЮЧЕНО
4. **Фискализация:**
   - Вариант A: Облачная касса + ОФД + интеграция.
   - Вариант B: "Чеки от ЮKassa" (если подходит).

**План запуска:**

1. Утвердить схему с бухгалтером (ПСН/УСН, ОКВЭД).
2. Регистрация в ЮKassa (`shopId`).
3. Настройка чеков (54-ФЗ).
4. Webhook `payment_succeeded` → активация подписки.

---

## Приложения (куда смотреть за деталями)

- **Полная документация:** `HEYS_BRIEF_FULL.md`
- **🔐 Безопасность API:** `SECURITY_RUNBOOK.md` — чеклист при деплое
- **Юридика:** `docs/legal/`
- **Операционные протоколы куратора:** `docs/operations/CURATOR_PROTOCOLS.md`
- **Cloud Functions:** `yandex-cloud-functions/heys-api-*`
- **Инфра:** `infra/README.md`
