# Протокол реализации: сводка дня на карточке клиента

**Дата:** 2026-08-02 **Область:** экран выбора клиента у куратора **Тип:**
продуктовая фича (миграция + cloud function + legacy UI)

## Зачем

Куратор не видел, кто из клиентов что внёс сегодня, не заходя в каждого. Бейджи
на карточках (`🔥 стрик`, `📅 последний визит`) считались из localStorage, то
есть показывали только клиентов, открытых на этом устройстве
(`CURATOR_WORKSPACE.md`, C30). Серверного источника сводки не существовало.

**Критерий готовности:** на экране выбора клиента под именем видно «🍽 · 💧 · 👟
· 🏋» за сегодня по каждому клиенту, включая тех, кого куратор в этой сессии не
открывал; незаполненное выделено цветом.

## Решение и что отклонено

Взят минимальный путь: один read-only SQL-агрегат + строка на существующей
карточке. Отклонено:

- **отдельный экран/дашборд куратора** — сводка нужна ровно там, где куратор и
  так выбирает клиента; новый экран добавил бы навигацию без пользы;
- **клиентский подсчёт по загруженным дням** — это и есть текущая поломка
  (localStorage виден только по открытым клиентам);
- **материализованная таблица со сводками** — второй источник правды рядом с
  `client_kv_store`, который придётся синхронизировать; при нынешнем числе
  клиентов один запрос по PK дешевле.

## Шаги

| #   | Шаг                                                         | Статус             | Проверено                                                                                  |
| --- | ----------------------------------------------------------- | ------------------ | ------------------------------------------------------------------------------------------ |
| 1   | SQL `get_curator_clients_day_summary(p_curator_id, p_date)` | готово             | миграция 27 применена, `--status` чист                                                     |
| 2   | Сверка расчёта с клиентским                                 | готово             | 2026-08-01: SQL 1935/1417 ккал = JS 1935/1417                                              |
| 3   | Allowlist + типы параметров + audit-skip в `heys-api-rpc`   | готово в исходнике | `node --check` OK                                                                          |
| 4   | Деплой `heys-api-rpc`                                       | готово             | health check зелёный; live-вызов `/rpc?fn=get_curator_clients_day_summary` → 200 с данными |
| 5   | Фасад `YandexAPI.getClientsDaySummary`                      | готово             | 2 новых теста, файл 34/34 зелёный                                                          |
| 6   | Загрузка в `useGateState` + проброс в `buildGate`           | готово             | `node --check` OK                                                                          |
| 7   | Строка сводки на карточке клиента                           | готово             | `node --check` OK                                                                          |
| 8   | Пересборка бандлов                                          | готово             | boot-core `2e6ddfc81c47`, boot-app `045adbb9cd1e`                                          |
| 9   | Фикс: пустые слоты считались тренировками                   | готово             | миграция 28; сегодня 0 вместо ложных 3, за 01.08 у Александры 2 тренировки на 110 мин      |
| 10  | Не звать кураторский RPC из клиентской сессии               | готово             | в браузере с PIN-сессией вызов давал 401 — добавлено условие `cloudUser`                   |
| 11  | Проверка кураторского экрана в браузере                     | **не сделана**     | в доступной вкладке клиентская PIN-сессия; кураторский вход требует пароля владельца       |

## UI-гейт

Цель — за один взгляд понять, у кого сегодня пробел. Главное действие — открыть
такого клиента (карточка кликабельна, как и раньше). Слой 1 — четыре метрики
строкой, незаполненное красным. Слой 2 отдельно не вводился: детали дня живут
внутри клиента, дублировать их на карточке значило бы утяжелить список. Не
скрывается ничего критического — пробелы и есть главный сигнал.

## Риски и открытые вопросы

- **Карточка не проверена глазами.** Функция, деплой и данные подтверждены, но
  сам экран выбора клиента в браузере не открывали: доступная вкладка держит
  клиентскую PIN-сессию, а кураторский вход требует пароля владельца. Первый
  визуальный контроль — за владельцем.
- Пустые слоты тренировок пришлось чинить отдельной миграцией: в дне всегда
  лежат три заготовки `{"z":[0,0,0,0]}`, и `jsonb_array_length` показывал «3
  тренировки» у клиента, который не тренировался. Правило подсчёта теперь
  совпадает с продуктовым каноном (ненулевая зона), но сам факт стоит помнить
  для любых будущих агрегатов по `trainings`.
- Стрик и «последний визит» остались на localStorage. Логично перевести и их на
  сервер, но это отдельная задача: формат стрика требует чтения диапазона дней.
- Обновление раз в 5 минут выбрано без замера нагрузки. Запрос дешёвый (одна
  строка KV на клиента по PK), но при росте числа клиентов это стоит
  перепроверить.

## Что смотреть ревьюеру

- `scripts/db/migrations/2026-08-02_curator_clients_day_summary.sql` — guard'ы
  `jsonb_typeof` на всех числовых полях: дневной блоб исторически хранит и
  строки, и `null`.
- `apps/web/heys_app_gate_state_v1.js` — эффект грузит сводку только когда
  `clientId` пуст, иначе куратор внутри клиента будет дёргать её впустую.
- `yandex-cloud-functions/heys-api-rpc/index.js` — попадание в
  `CURATOR_AUDIT_SKIP` сделано осознанно, обоснование в комментарии.
