# Протокол: лист «куратор обновил дневник» v4

**UI-гейт:** цель — показать правки куратора, не отбирая управление; главное
действие — «Понятно»; слой 1 — шапка, раскрытый свежий день, «Позже» /
«Понятно»; слой 2 — свёрнутые дни, хвост дат, продукты после трёх, точка на
«Питании», строка входа в дне; критическое не скрывать — после двух показов
правки остаются достижимы точкой или строкой дня, после «Понятно» строка дня
остаётся входом перечитать; на «Питании» в чужой дате строка текущего дня ведёт
к правленой дате (точка на активной вкладке по-прежнему скрыта).

Источник: канвас `docs/ui/handoff-v4/canvas/Регистрация и чек-ин v4.dc.html`
(кадры листа + «Точка вместо листа» + «Вход внутри вкладки»).

## Шаги

1. Лист снизу, не карточка по центру. «Понятно» подтверждает показанное и никуда
   не уводит. Строка ведёт в дневник/актив, шеврон, без «Показать».
2. Ack записи — когда по ней не осталось видимых строк или нажали «Понятно». Тап
   по строке прячет её локально на сессию.
3. Шапка даты: первое «было» → последнее «стало» среди неподтверждённых записей
   дня. Нет полей — шапка без ккал (легально, кадр «Без еды»).
4. После двух автопоказов за сессию третий лист не приходит: точка 7 px на
   «Питании», без счётчика. Уже на «Питании» точка недостижима — строка «Куратор
   обновил этот день» в шапке дня открывает тот же лист, отфильтрованный датой.
   Она же — возврат после «Понятно».
5. Не ждать деплоя `day_kcal_*`: верстать на фолбэке.

## Палитры (решение дизайнера 2026-08-16)

Лист — надстройка над приложением: красим по теме экрана, не sand поверх blue.

- Sand light: подложка `rgba(42,26,12,.5)`, лист `#fffaf1`, дельта `#8a4a20`
- Sand dark: подложка `rgba(0,0,0,.62)`, лист `#191613`, дельта `#e2a468`
- Blue light: подложка `rgba(10,22,38,.5)`, лист `#ffffff`, дельта `#1d5e96`
- Blue dark: подложка `rgba(0,8,16,.62)`, лист `#12222f`, дельта `#7fbceb`
- Blur подложки `2.5px` во всех четырёх
- В теле листа цветные только `.ca-modal__date-kcal` и
  `.ca-modal__more-products`; строки/шеврон/подписи нейтральные
- Без имени: заголовок «Ваш куратор обновил дневник»; «Написать куратору» не
  показываем (на листе CTA нет; правило как у экрана ожидания)
- Имя куратора: `clients.curator_id → curators.name`, отдаётся в
  `get_my_curator_changelog_since` / `get_client_data_by_session`, клиент пишет
  в `heys_profile.curatorName` (первое слово — в заголовке листа)

## Проверка

- `pnpm exec vitest run __tests__/curator-actions-banner.test.js __tests__/curator-sheet-palette.test.js __tests__/curator-sheet-canvas-smoke.test.js __tests__/app-nav-v4-frame.test.js`
  — поведение + палитры + **симуляции кадров canvas** (один приём, длинный
  приём, день целиком, две даты, сухие строки, много дней, точка, строка входа,
  пустой лист, fallback имени)
- Решение по копирайту: в макете местами «Шесть изменений», в продукте **цифра**
  («6 изменений») — так и оставляем (владелец 2026-08-16)
- localhost:3001 — смена темы sand/blue ± dark, «Правки куратора (снова)»

## Смотреть ревьюеру

На blue лист не тёплый. Подложка не одна на все темы. Дельта и «и ещё…» —
единственный цвет в теле. Точка на «Питании» — не бейдж. Руками вариации не
собирать — смоук `curator-sheet-canvas-smoke.test.js`.
