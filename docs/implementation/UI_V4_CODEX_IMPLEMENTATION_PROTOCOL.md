# Протокол реализации UI v4 — Codex

Дата начала: 2026-09-01. Рабочая копия: `C:\Users\User\HEYS-v2`.

## Критерий результата

Источник истины — 25 актуальных `*.v4.dc.html` в
`docs/ui/handoff-v4/canvas/Переработка дизайна приложения/design_handoff_heys_v4/`.
Для применимых строк `data-v` код и живой runtime должны подтверждать контракт;
кадры `data-demo="stop"` сверяются на ширине 375 px. Файлы `history/` и кадры
`protocol` в приёмку не входят.

## Baseline

- пять структурных gates проходят;
- `ui-v4-check-contract-drift` красный: актуальный пакет ещё не сведен;
- адресный gate хранит замороженный долг: 18 жёстких и 134 мягких адреса, 3431
  сокращение без имени файла;
- до начала работы: 509 локальных коммитов поверх `origin/main`, рабочее дерево
  содержит чужие staged/unstaged документы и preview-бандлы — не трогать.

## Шаг 1 — `home-widgets`

Статус: в работе.

- Подтверждено по diff `a445a124d^..a445a124d`: 20 осиротевших строк удалены
  самим актуальным канвасом, а не переименованы. Две строки относятся к подвалу
  Главной, остальные 18 — к прежним CTA в хвостах листов разбора.
- Новые строки контракта: `лестница чернил · правило продукта` и
  `вид · полоса цели`; их нельзя закрывать пересъёмкой отпечатка до реализации и
  проверки runtime.

### Ревьюеру

Смотреть: удаления только 20 мёртвых ключей в
`docs/ui/verdicts/home-widgets.json`; новые правила пока не получают `=` без
доказательства в коде и runtime.

### Полоса дневной цели — source/test

Статус: source и точечный тест готовы; runtime-приёмка ещё не выполнена.

- `v4GoalBar` больше не наследует состояние числа: полоса становится `--gr2` от
  67 %, число остаётся положительным только от 100 %;
- дорожка использует `--v4-track`, недобор — отдельную роль `--v4-overlay-fill`
  с четырьмя значениями `--ovl` из макета;
- структурные gates после правки зелёные: undefined roles, foreign fallbacks,
  CSS test guards, bare color literals;
- `widgets-v4-goal-bar-contract.test.js`: 3/3 теста проходят;
- общий `widgets-bd-sheet-canvas-razbor.test.js` сохраняет известный красный
  baseline нового пакета (378/415 и устаревшие пары); порог не понижен и снимки
  не пересняты поверх дефектов.

Ревьюеру смотреть независимость порогов 67/100 и четыре темы. Вердикт строки
`вид · полоса цели` менять на `=` только после scoped bundle и визуального
runtime-smoke.

### Лестница чернил — source/test

Статус: общая палитровая роль и `home-widgets`/login source готовы; runtime-
приёмка ещё не выполнена.

- добавлены точные семантические роли 62/56/45/38/30 и четыре raw
  `--v4-ink-rgb`;
- глобальные старые `--v4-ink-2/3/4` не переписаны: у них более тысячи
  межмодульных применений и другая прежняя семантика;
- весь явно перечисленный мелкий нейтральный текст `home-widgets` и login
  переведён на floor 56 %, назначения 12+ px разведены по смысловым ролям;
- прямой нейтральный `#201e1d` в home заменён палитровым `--v4-ink`;
- `widgets-v4-ink-ladder-contract.test.js`: 5/5; login contract: 12/12 суммарно;
- общий canvas-тест улучшен с пяти до трёх реальных групп ошибок, покрытие
  379/415; оставшиеся 38 геометрических и 126 графических расхождений не
  пересняты и не скрыты снижением порога.

### Обратное покрытие кадров

Статус: индексатор и честный красный gate готовы.

- индексируются ровно 25 активных root canvases: 15 574 строки контракта;
- найдено 810 кадров: 748 продуктовых, 39 `protocol` и 23 `loop` явно исключены;
- CLI поддерживает `--zone`, `--list`, `--json` и выдаёт scaffold недостающих
  `frames/evidence`, ничего не записывая в verdicts;
- 4/4 точечных теста, `node --check`, CLI smoke и `git diff --check` проходят;
- gate остаётся красным, пока verdict schema не хранит явное покрытие кадров;
  дополнительно обнаружены 10 дублирующихся identities в `tips`.

### Обратное покрытие code→canvas

Статус: правильное направление gate реализовано, реестр разбора заморожен.

- сканируются source-файлы `apps/web` без public/tests и выделяются корневые
  literal-классы `modal/sheet/overlay/fullscreen/screen`;
- найдено 127 identities: 4 подтверждённых живых пробела канваса и 123 явных
  `unreviewed`; новые identities уже падают отдельно как `missing`;
- exact mapping `covered` проходит только при существующем product frame в
  указанной зоне; `excluded` требует причины, `gap` остаётся красным;
- 3/3 теста code→canvas и 4/4 теста canvas→verdict проходят; сам gate намеренно
  красный до разбора 123 roots и решения четырёх дизайнерских пробелов.

## Шаг 2 — `strength-builder`

Статус: аудит завершён, реализация разбита на независимые батчи.

- подтверждены дефекты dock отдыха, запуска отдыха связки, сохранения активной
  сессии/timestamps, пользовательской копии RPE, постоянно редактируемых
  закрытых строк, шумных состояний списка и отсутствующего отчёта plan-vs-done;
- handoff частично устарел: `workoutLog.startedAt/completedAt` уже сохраняются,
  но fullscreen-конструктор их не использует;
- пять неоднозначностей вынесены в
  `docs/ui/UI_V4_CODEX_DESIGN_DISCREPANCIES.md`; они не блокируют
  RPE/calm-state, схему metadata/rest и чистую модель исходов.

### Батч `Тяжесть` и спокойный список — source/test

Статус: source и точечные тесты готовы; runtime-приёмка ещё не выполнена.

- пользовательский `RPE` заменён на `Тяжесть`, доступное имя
  `Тяжесть подхода N из 10`, источник отдыха — `по тяжести N`;
- краткая шкала 6–10 раскрывается по тапу на подпись;
- зелёной осталась галочка, а не вся done-карточка/поля; открытая карточка и
  текущие поля получили спокойные рамки; преждевременная кнопка завершения
  вторична;
- активный кадр А1б сильнее старой строки про счётчик: кнопка называется
  `Завершить тренировку`, счёт остаётся в шапке, подтверждение незакрытых
  подходов сохранено; расхождение записано дизайнеру;
- 41/41 strength UI/calm tests проходят, пять UI/CSS gates зелёные.

### Батч отдыха связки и восстановления — source/test

Статус: source и точечные тесты готовы; runtime-приёмка ещё не выполнена.

- отдых связки запускается только на переходе последней клетки раунда
  `не весь закрыт → весь закрыт`, один раз и с максимумом участников;
- полноэкранная `.sb-rest { inset:0 }` заменена dock над нижней панелью: список
  остаётся видимым и интерактивным, кольцо 168 px;
- dock показывает владельца, источник длительности и следующий раунд; действия
  `+10 секунд`, `Пропустить`, `Свернуть`, компактная строка разворачивается;
- `activeRest` сохраняется в том же `workoutLog`, проходит нормализацию и оба
  входа конструктора; незавершённый восстанавливается, истёкший очищается и не
  стартует заново;
- 47/47 strength UI/calm/persistence tests проходят.

### Батч жизненного цикла сессии — source/test

Статус: source и расширенная регрессия готовы; runtime-приёмка ещё не выполнена.

- persisted `workoutLog.startedAt` стал источником таймера после повторного
  входа, а `completedAt` замораживает его;
- первая отметка сохраняет `startedAt`, `firstMarkAt`, `lastMarkAt`, последующие
  двигают только последнюю границу; normalizer эти поля не теряет;
- завершение пишет `completedAt`, очищает `activeRest` и переводит только
  активный план `started → done`;
- счётчики программы считают только `done`, не смешивая начатые и пропущенные
  тренировки;
- расширенная strength/program регрессия: 9 файлов, 133/133.

Ревьюеру смотреть: восстановление после повторного входа, остановку таймера
после завершения и недельный счётчик выполненного.

## Шаг 3 — `checkin-morning`

Статус: безопасный ink/data batch и повреждённый слепок разобраны;
runtime-приёмка ещё не выполнена.

- мелкий data-текст переведён на именованную роль 56 % без изменения графических
  и safety-состояний;
- видимый кадр «холод тип» сохраняет три варианта; волна активного варианта и
  контекстная отметка удаления используют 56 %;
- типографика общей кнопки удаления не подгонялась под сдвинувшуюся строку
  подсказки;
- шесть потерянных/сдвинутых строк повреждённого `data-v` исключены из
  автоматического сравнения, но три продуктовых варианта и тон данных
  проверяются напрямую;
- `checkin-v4-canvas-razbor.test.js`: 23/23.

Ревьюеру смотреть: слой холода, слой замеров и запись о повреждённом слепке в
документе расхождений. Не считать снижение охвата 350→344 регрессией: это
удаление шести ложных сравнений со сдвинутыми/отсутствующими строками.

## Шаг 4 — `reports-insights`

Статус: source, evidence и точечные тесты готовы; runtime-приёмка ещё не
выполнена.

- периоды стали текстовыми вкладками с hit-area 44×44, зазором 16 и underline 2
  px;
- добавлена отдельная роль `--v4-plan` 22 % во всех четырёх темах, линия плана
  больше не маскируется под границу;
- Score приведён к 44 px, зелёному состоянию и типографике кадра;
- безопасные data/zero-row роли дочищены, stale exceptions и verdict evidence
  пересмотрены точечно;
- canvas geometry 42/42, совместные reports-гейты 84/84;
- единственный намеренный `≠`: отрицательная дельта остаётся data-тоном, потому
  что красный зарезервирован для ошибки/опасности; вопрос внесён дизайнеру.

Ревьюеру смотреть: переключатель периода, линию плана во всех темах, Score и
нейтральный тон отрицательной дельты.

## Шаг 5 — `food-meal`

Статус: безопасный ink/data batch готов; крупные структурные расхождения
остаются отдельной работой.

- 27 активных текстовых селекторов приведены к роли 56 %;
  border/background/icons/graphs не затронуты;
- строка списка дня перенумерована 21→20 без пересъёмки семантики;
- добавлен отдельный контракт лестницы чернил; связанные food-тесты 39/39;
- конфликт `food-meal` и `nutrition-tab` по подписи окна назван отдельно, код
  следует зоне-владельцу `food-meal`.

Ревьюеру смотреть: вторичный текст добавления еды и список дня; не переносить
решение 56 % на графические метки или соседние поверхности автоматически.

## Шаг 6 — `product-card`

Статус: безопасный ink/data и harm-copy batch готов; структурные
scanner/portions расхождения остаются отдельной работой.

- 27 подтверждённых строк `=` переведены на `--v4-ink-data` с обязательным
  fallback; графическая и interaction opacity не затрагивалась;
- источники вредности называются нейтрально: `Расчёт по формуле`,
  `Оставить расчёт`, `Из описания продукта`/`Указать значение`; существующая
  логика `system`/`own` сохранена;
- связанные product-card проверки: 67/67; пять CSS guard-гейтов зелёные;
- scanner shell, недостающий вход состояния `Код не распознан` и старый portions
  preview остаются поимёнными расхождениями, а не ложными `=`.

Ревьюеру смотреть: подписи источника вредности и data-тон; не считать
scanner/portions закрытыми этим batch.

## Шаг 7 — `pwa-update`

Статус: две изменившиеся строки пересмотрены, source уже соответствовал;
runtime-приёмка общего UI-batch ещё не выполнена.

- `Готово` уже без восклицательного знака, offline `wifiOff` уже использует
  точную обновлённую кривую canvas;
- evidence осталось честным `=`, отпечатки пересняты только для `pwa-update`,
  zone drift зелёный;
- PWA/spinner проверки: 61/61.

Ревьюеру смотреть: ready-stage и offline banner; source-правка для этих двух
строк не потребовалась.

## Шаг 8 — `undo-bar`

Статус: пять drift-строк пересмотрены; source/test/verdict готовы,
runtime-приёмка общего batch ещё не выполнена.

- runtime-слово `Вернуть` уже соответствовало новой управляющей строке;
- light inactive bottom-nav приведён 55→56 %, dark 63 % сохранён;
- три shell-строки сохранены как чужое окружение (`—`) с evidence общего
  runtime;
- конфликты `Вернуть/Отменить` и `12/11 px` получили честные `≠` и вынесены
  дизайнеру;
- проверки: 74/74, zone drift 100/100, diff-check green.

Ревьюеру смотреть: таймер, слово действия и inactive bottom-nav в светлой/тёмной
темах.

## Шаг 9 — `service-curator`

Статус: новый verdict owner-zone заведен полностью; видимый frame приведён к
ролям, high-level конфликт остаётся дизайнерским блокером.

- все 29 новых строк получили verdict/evidence без `?`; frame advice-service
  проверяется новым owner-canvas тестом;
- data-текст подсказок/footer приведён к 56 %, title — к theme-aware
  `--v4-ink-2`, поверхности и action-tone используют роли всех четырёх палитр;
- curator-only entry и отсутствие клиентских data mutations подтверждены
  существующими behavioral tests;
- `фича`/`вид` оставлены `≠`: они требуют device-service, тогда как единственный
  frame/text snapshot и runtime описывают advice-service;
- целевые source-тесты 19/19; визуальный frame-тест owner-zone 3/3.

Ревьюеру смотреть: служебный advice-лист во всех темах; не считать
device-service реализованным без решения дизайнера.

## Шаг 10 — `spinners`

Статус: семь drift-строк и связанная логика отказов готовы; runtime-приёмка
общего batch ещё не выполнена.

- на первом отказе вторичной строкой доступно `Написать куратору`, на втором обе
  кнопки равны 48 px / `--c2` / 13/700;
- устаревшее `Повторить ещё раз` удалено из source и fallback-разметки;
- пояснительные/decor frame rows получили `—`, старые конфликтующие строки и
  кадр — честные `≠`;
- zone drift 174/174, целевые тесты 45/45, diff-check green.

Ревьюеру смотреть: первый и второй failure-state, включая fallback до загрузки
JS.

## Шаг 11 — `settings-system`

Статус: десять drift-строк пересмотрены; source/test/verdict готовы,
runtime-приёмка общего batch ещё не выполнена.

- девять активных строк приведены к canvas: data/meta 56 %, build diagnostic
  mt14, iOS-note 10.5/1.45 и padding 2/2/0;
- bottom-nav `Питание` оставлен `—` как protocol/context, не ownership settings;
- конфликт 50/56 % у пояснения тумблера зафиксирован дизайнеру, код следует
  именованной строке 56 %;
- zone drift 239/239, шесть тестовых файлов 111/111, canvas coverage 15→22.

Ревьюеру смотреть: список настроек, раскрытую диагностику и iOS-ветку
уведомлений.

## Шаг 12 — `login`

Статус: 47 изменившихся строк пересмотрены; source/test/verdict готовы,
визуальная runtime-приёмка общего batch ещё не выполнена.

- принятие соглашения перенесено на первый шаг создания собственного кода;
  подтверждение показывает точную сноску о прошлом шаге;
- слабый и несовпавший код стали отдельными достижимыми состояниями с точными
  заголовками, объяснениями, требованиями и возвратом к первому вводу;
- подписи карточек палитры приведены к 700 11px/1.3, а внутренняя подсказка
  дополнена полной механикой режима `Как в системе` и геометрией 16px;
- четыре stale-вердикта свёрнутого выбора исправлены: runtime уже подписывает
  его `Оформление`;
- строка с точным остатком попыток оставлена честным `≠`: `login_client_v1` не
  возвращает это число, поэтому UI его не выдумывает;
- точечные проверки 44/44, zone drift 788/788, diff-check green.

Ревьюеру смотреть: первый шаг соглашения, подтверждение, оба отказа кода и theme
picker; отдельно проверить решение по серверному счётчику попыток.

## Шаг 13 — `nutrition-tab`

Статус: changed drift и связанные неизменённые строки советов пересмотрены;
source/test/verdict готовы, runtime-приёмка общего batch ещё не выполнена.

- timeline использует именованные роли `--v4-track` и `--v4-ok-fill`,
  hero/data-текст и геометрия приведены к актуальным строкам;
- `MealOptimizer.getMealOptimization()` теперь явно возвращает `scope: 'meal'`;
  контракт и reference dossier защищены отдельным тестом;
- старые verdict по формату/количеству/скрытию советов не сохранены механически:
  недостающие UI-семантики оставлены `≠`;
- zone drift 524/524, связанные проверки 57/57, diff-check green;
- `pnpm docs:reference:check` доходит до известного unrelated fail в
  `SUBSCRIPTION_AND_PAYMENTS.md`.

Ревьюеру смотреть: timeline и owner-scope meal advice; не считать persistence
`Понятно`/`Не показывать` реализованным.

## Шаг 14 — `questionnaire`

Статус: 22 drift-строки и семь связанных stale `=` пересмотрены;
source/test/verdict готовы, runtime-приёмка общего batch ещё не выполнена.

- служебные данные анкеты переведены на роль 56 %, добавлена точная подсказка
  про динамику веса и исправлены интервалы helper/error copy;
- монотонный счётчик времени защищён контрактным тестом;
- 13 изменившихся строк сведены, 9 и семь зависимых кадровых строк оставлены
  честными `≠`;
- zone drift 324/324, финальный повтор 63/63, CSS structural gates green,
  diff-check green.

Ревьюеру смотреть: время заполнения, момент появления статуса/сводки и три
конфликтующих вида информационных блоков.

## Шаг 15 — `norm-correction`

Статус: 20 drift-строк пересмотрены; source/test/verdict готовы, runtime-приёмка
общего batch ещё не выполнена.

- quiet/data facts, легенда и история приведены к 56 %, generic facts получили
  явный `tone`, cold-start — 11/600/1;
- curator history переставлена в порядке canvas `SVG → легенда → даты`;
- 13 строк сведены, семь недостающих продуктовых состояний оставлены `≠`,
  исчезнувшая строка удалена rehash;
- zone drift 479/479, семь файлов и 215/215 тестов, diff-check green.

Ревьюеру смотреть: curator history и семь не реализованных detail-state; не
считать их закрытыми общей correction-card.

## Шаг 16 — `date-remainders`

Статус: owner-зона очищена от 171 исчезнувшей строки чужих экранов; актуальные
date/calendar source/test/verdict готовы.

- стрелки стали реальными кругами 44×44 без псевдо-hit-area, включая narrow
  override;
- легенда календаря приведена к start/wrap, gap 10×14, margin-top 16 и data-тону
  56 %;
- устаревшие service/undo/rating проверки удалены из date-owner теста: эти кадры
  уже живут в собственных verdict-зонах;
- rehash оставил 368 актуальных строк, zone drift green; date/calendar проверки
  30/30, diff-check green;
- три старые строки кадра `Дата · сегодня` остаются `≠` против обновлённого
  именованного owner-контракта.

Ревьюеру смотреть: круги стрелок и легенду; переснять старую today-капсулу по
owner-строке.

## Шаг 17 — `water-add`

Статус: последняя строка без вердикта разобрана; source уже соответствовал
позднему решению canvas.

- `вид · плитка воды 1×1` переведён из `?` в честный `=`: label и факт стоят
  сверху на 8px, норма отсутствует;
- старое начало строки `раскладка плитки` остаётся единственным `≠`, потому что
  та же строка хранит снятую нижнюю раскладку;
- zone drift 134/134 без `?`, water проверки 30/30.

Ревьюеру смотреть: плитку 1×1 с длинным значением; дизайнеру удалить снятую
редакцию из общей строки.

## Шаг 18 — `cycle`

Статус: 24 drift-строки пересмотрены; source/test/verdict готовы,
runtime-приёмка общего batch ещё не выполнена.

- day-29 copy, chart caption, data-роль 56 % и безопасная геометрия приведены к
  актуальным строкам;
- логически невозможный insight, shared label отчётов, canvas-аннотация,
  supplements stack, reset action и backdate copy оставлены `≠`;
- новый ink-ladder contract и связанные проверки: пять файлов, 76/76;
- zone drift 278/278, diff-check green.

Ревьюеру смотреть: подписи графика/особого периода и решения по шести
продуктовым конфликтам, не только косметику.

## Шаг 19 — `app-splash`

Статус: 60 строк owner-зоны повторно проверены; бесспорные
accessibility/safe-area расхождения исправлены, внутренние конфликты canvas
получили честные `≠`.

- скрытая live-region копия приведена к дословному `Загрузка`;
- круг теперь позиционируется на 45 % реально доступной высоты между верхней и
  нижней safe-area, а не сдвигается на полную верхнюю врезку;
- reduced-motion не выдан за совпадение: runtime следует общему spinner-owner и
  дышит 1,6 с, тогда как app-splash просит полностью статичный диск;
- snapshot 64/58/0,13 оставлен `≠` против именованного общего знака 56/50/0,28;
  оба конфликта вынесены дизайнеру.
- focused boot/offline проверки 65/65, zone drift 60/60, verdict-address gate и
  diff-check green; `docs:reference:check` дошёл до известного unrelated fail в
  `SUBSCRIPTION_AND_PAYMENTS.md`.

Ревьюеру смотреть: cold start на устройстве с верхней и нижней врезками,
объявление screen reader и reduced-motion; дизайнеру унифицировать конфликтующие
строки и snapshot.

## Шаг 20 — `tips`

Статус: 75 изменившихся и 103 новых строки пересмотрены; source/test/verdict
готовы, reverse frame gate честно заблокирован десятью duplicate labels канваса.

- новая data-лестница 56 % применена к панели оценки, детали, научному описанию,
  настройкам, всплывающему совету и отмене; поверхности/action-тона используют
  theme roles;
- перенесённые кадры `Совет · оценка после свайпа` и `Совет · отмена с таймером`
  заведены в owner-тест: сверяются строки 21–28 обеих панелей, общий охват
  разбора вырос 83→99;
- все 700 строк имеют финальный verdict без `?`; 46/46 frame occurrences имеют
  evidence, но reverse gate остаётся красным только из-за десяти неуникальных
  `data-screen-label` между песочным и синим наборами;
- размещение undo внутри открытой шторки сохранено как более цельный flow и
  вынесено дизайнеру; frame-shell не выдан за совпадение;
- focused canvas test 16/16; полный связанный прогон восьми файлов 91/91, zone
  drift 700/700 green, diff-check green.

Ревьюеру смотреть: обе свайп-панели, data-текст во всех четырёх палитрах и
отсутствие потери позиции после скрытия; дизайнеру уникализировать десять frame
labels.

## Шаг 21 — `tab-activity`

Статус: новые кадры и 375 строк owner-зоны разобраны; source/test/verdict
готовы, runtime-приёмка общего batch ещё не выполнена.

- ползунок фактических шагов получил видимую ручку 20 px с hit-area 44 px,
  системные роли трека и отдельное пунктирное состояние оценки; data-текст
  сведён к актуальной лестнице;
- идущая силовая теперь показывает прогресс подходов, текущие упражнение/подход
  и одно главное действие `Продолжить тренировку`; строка яруса `Сегодня`
  обновляется по живому прогрессу;
- существующая kernel-модель нагрузки подключена к реальной 42-дневной истории:
  силовая и кардио не смешиваются, незапущенные назначения исключаются, тоннаж
  групп мышц учитывает основной вес и коэффициент синергиста конструктора;
- предупреждение остаётся советом с двумя равноправными выходами, а расчётные
  детали открываются вторым слоем `Нагрузка`; внутренние CTL/ATL/TSB и
  непроверяемый процент готовности в копии не показываются;
- verdict: `=` 259 / `≠` 53 / `—` 63 / `?` 0; zone drift и address gate green,
  15 связанных файлов и 217/217 тестов green, diff-check green.

Ревьюеру смотреть: шаги с фактом/оценкой, незавершённую силовую, предупреждение
повторной нагрузки и раскрытие `Нагрузка`; дизайнеру проверить решения по
календарю, ownership слоя нагрузки и действиям совета.

## Шаг 22 — `strength-builder`

Статус: актуальный canvas 1 сентября переснят во время работы; 1812 строк
адресованы, критичный lifecycle паузы реализован. Последующий семантический
аудит признал пакетное закрытие неподтверждённых кадров через `≠` ошибочным и
повторно открыл их как `?`.

- при разрыве больше 45 минут builder ничего не решает сам: показывает
  сохранённый прогресс, время последней отметки и выбор `Продолжить` /
  `Завершить в HH:MM`;
- итоговая длительность считается от первой до последней отметки; завершение
  после длинной паузы сохраняет `completedAt` последней отметкой и не
  растягивает работу на время отсутствия;
- текущие механики подходов, разминок, drops, arbitrary-size supersets, отдыха,
  discomfort, предложения куратора и единиц измерения перепроверены по
  kernel/source и focused tests;
- отсутствующие owner-flow программы, вчерашней сессии, per-approach
  offline/authoring и клиентского комментария куратора не имитировались
  локальным UI;
- честный verdict после повторного аудита: `=` 87 / `≠` 251 / `—` 98 / `?` 1376;
  57 общих оснований не доказывали построчное расхождение и больше не считаются
  постоянными исключениями;
- две строки про drop получили отдельное evidence: linked-ветка рендерит
  `SupersetBlock` без `+ Сброс`, а drop остаётся ступенью того же подхода. Это
  закреплено focused DOM-тестом.

Ревьюеру смотреть: paused-session на 45+ мин, длительность 1-я→последняя
отметка, спокойные состояния builder; дизайнеру — раздел `Strength builder` в
документе расхождений.

## Шаг 23 — `checkin-morning` повторный аудит

Статус: актуальные 970 строк и 35 кадров повторно адресованы;
source/test/verdict приведены к canvas, без `?` и без ложных `=`.

- подписи качества сна, настроения и самочувствия переведены с оценочных
  `плохо / отлично` на нейтральную пятиступенчатую лексику; интенсивность
  стресса сохранена фактической;
- строка `слова на экране` повторно подтверждена новой source-проверкой, а
  обновлённый контракт дорожки подтверждён существующими тремя состояниями
  `normal / under / over`;
- исчезнувшая строка `Чек-ин · холод тип · 28` удалена rehash; 37 устаревших `≠`
  новой data-роли и холодовой копии исправлены, все оставшиеся verdict-строки
  имеют финальный статус (`=` 902 / `≠` 19 / `—` 49 / `?` 0);
- заведена явная адресация 35/35 продуктовых кадров с evidence; zone drift,
  reverse coverage и адресный срез green; 23 связанных файла и 206/206 тестов
  green, diff-check green;
- два дефекта самого canvas — незаданная полная шкала слов и повреждённая
  нумерация snapshot холодового вопроса — вынесены дизайнеру, а не замаскированы
  тестом.
- `pnpm docs:reference:check` дошёл до известного unrelated fail в
  `SUBSCRIPTION_AND_PAYMENTS.md`; morning-reference не менялся, потому что
  контракт данных и владельцы не изменились.

Ревьюеру смотреть: словесные подписи возле значений сна/настроения/самочувствия,
три состояния дорожки и холодовой вопрос; дизайнеру подтвердить полную шкалу и
переснять повреждённый snapshot.

## Шаг 24 — `reports-insights` повторный аудит

Статус: 130 drift-строк и исчезнувшие строки повторно сверены; 1010/1010 строк и
104/104 продуктовых кадров имеют точный verdict/evidence, без `?`.

- тихие пояснения, основания, риск-окна, подписи ритма, порогов, источников и
  завершённой рекомендации приведены к общей data-роли 56 %;
- геометрия периода и пояснения карточки `Динамика` синхронизирована с
  актуальными строками canvas;
- две снятые строки старой нулевой матрицы удалены и из verdict, и из
  именованного реестра расхождений;
- продуктовые противоречия не замаскированы: responsive-графики, shared spinner
  вместо skeleton, структура ритма и несобранный `Что если` остаются честными
  `≠` до решения владельца;
- zone drift и reverse coverage green; 11 связанных файлов, 158/158 тестов и
  diff-check green. Address gate сохранил только ранее известный долг
  сокращённых/перемещённых адресов, новых ошибок не добавлено.

Ревьюеру смотреть: data-тон тихих строк, карточку `Динамика`, состояния мало
данных/нет веса и переход к подробностям; дизайнеру — решения по графикам,
skeleton/spinner, ритму и `Что если`.

## Шаг 25 — `product-card` повторный аудит

Статус: 50 изменённых строк canvas повторно разобраны; verdict 372 строки (`=`
229 / `≠` 67 / `—` 76 / `?` 0), 13/13 кадров имеют evidence.

- вредность сохранена по свежему owner-решению: одна карточка
  `Расчёт по формуле` и явный выбор источника, без устаревшей атрибуции
  `оценка ИИ`;
- порции остаются сразу редактируемыми полями, заполненными рекомендацией;
  старый read-only snapshot не возвращён;
- scanner сохраняет прямой ручной ввод EAN/UPC и показывает состояния по
  реальным событиям, без выдуманного timeout для `Код не распознан`;
- zone drift, address gate и reverse coverage green; семь focused suites, 40/40
  тестов, diff-check green.

Ревьюеру смотреть: порции, вредность и scanner; дизайнеру переснять устаревшие
snapshots и устранить конфликт трёх/четырёх шагов.

## Шаг 26 — `curator-edits` повторный аудит

Статус: все 220 строк и 8 кадров адресованы (`=` 190 / `≠` 1 / `—` 29 / `?` 0).

- локальное исчерпание действия при навигации сохранено вместе с серверным ack,
  чтобы уже обработанная рекомендация не возвращалась после синхронизации;
- неуказанные лимиты, пагинация и формат списка не выдуманы и оставлены `—`;
- owner source/CSS и 88 focused tests проверены, zone drift/address/reverse
  gates и diff-check green.

Ревьюеру смотреть: одноразовость кураторского действия после навигации и
повторной синхронизации; дизайнеру/архитектору подтвердить ack semantics и
пределы списка.

## Шаг 27 — `home-widgets` повторный аудит

Статус: самая крупная зона повторно сведена целиком: 4059/4059 строк (`=` 2574 /
`≠` 127 / `—` 1358 / `?` 0) и 143/143 продуктовых кадров с реальным
category-specific evidence.

- 787 изменённых строк перечитаны и перехешированы; две новые owner-строки
  ink-ladder и полосы цели заведены в verdict;
- ритм воды теперь держит короткую шапку `Вода`, объём — в body рядом с
  фиксированными 62px столбиками; недельный долг сна показывает
  `Недосып · 7 дней` и `7,5 ч` без устаревшего слова `норма`;
- новые ink/goal owner-контракты подтверждены source и тестами; старые кадры
  drag, воды и сна оставлены честными `≠`, а не возвращены в runtime;
- zone drift и reverse coverage green; 15 focused files, 172/172 тестов,
  финальный узкий повтор 45/45, diff-check green. Address debt уменьшен до двух
  absent и 24 moved старых фактов без нового превышения baseline.

Ревьюеру смотреть: drag без long-press, шторки воды/сна, 62px water rhythm и
пороги goal-bar; дизайнеру переснять три устаревших кадра и очистить historical
tail у листа веса.

## Шаг 28 — `food-meal` повторный аудит

Статус: 68 изменённых строк повторно разобраны, исчезнувшая строка удалена;
после семантического аудита verdict 1009 строк (`=` 748 / `≠` 170 / `—` 90 / `?`
1), 32/32 кадров имеют evidence.

- десять ложных `=` исправлены на честные `≠`: zonal WheelPicker override,
  отсутствующие context chips, конфликты владельцев food/nutrition, геометрия
  времени, пустой приём, palette-role и confirm gap;
- shared source/tests уже содержали рабочую реализацию и не переписывались ради
  старых или противоречивых кадров;
- zone drift, reverse coverage и address gate green; 12 test files, 65/65
  тестов, diff-check green.

Ревьюеру смотреть: time/wheel, activity context, фото, copy-flow и удаление
набора; дизайнеру/владельцу снять перечисленные конфликты между строками и
соседней nutrition-zone.

## Шаг 29 — двустороннее покрытие canvas ↔ runtime

Статус: построены два независимых read-only gate и проверен весь текущий набор.

- canvas→runtime: все 15 574 контрактные строки имеют адресованный verdict, но
  1377 из них честно остаются `?`; 748/748 product-frame occurrences имеют
  evidence адресации, а не доказательство визуального совпадения. Отдельный
  внешний blocker — 10 duplicate labels в `tips`, которые нельзя однозначно
  адресовать без правки canvas;
- runtime→canvas: извлечены 127 живых root-классов; 28 имеют точные frames, 21
  исключён как renderer/backdrop/dev/внутренний слой, 78 подтверждены как
  реальные gaps, pending/missing/stale/invalid = 0;
- соседний похожий кадр нигде не использован как доказательство; mixed roots
  `move-modal` и `sb-sheet` оставлены gap, потому что canvas покрывает не все их
  режимы;
- оба gate добавлены в тесты, reverse gate включён в общий `ui:v4:check`;
  code→canvas остаётся намеренно red, пока дизайнер не нарисует или явно не
  снимет 78 экранов.

Ревьюеру смотреть: `UI_V4_SCREEN_COVERAGE.json` как полный машинный реестр;
дизайнеру сначала закрывать user-facing guards, activity/food pickers, затем
Planning/Chrono и специализированные модули.

## Шаг 30 — scoped runtime и визуальный smoke

Статус: текущий затронутый bundle-scope собран локально без full rebuild;
dev-stack жив на `localhost:3001/4001`, login/theme smoke выполнен в реальном
runtime.

- scoped legacy build пересобрал `boot-app`, `boot-calc`, `boot-day`,
  `postboot-1-game-lazy`, `postboot-3-ui-lazy` из текущего состояния
  соответствующих bundle scope; staging/commit/push не выполнялись;
- DOM login после reload доступен: телефон, четыре PIN-поля, keypad, support и
  theme-picker имеют ожидаемые роли; console warn/error пуст;
- visual smoke обнаружил скрытый hardcoded paint: синяя палитра меняла controls,
  но fullscreen shell оставался `#efe3cf`, а blue-dark использовал коричневые
  sand-dark literals;
- shell/card/field/key/focus/support paint переведён на общие
  `--v4-hero/bg/surface/act/act-text/ok-fill`; повторный runtime замер в blue:
  shell `rgb(226, 236, 246)`, ink `rgb(16, 24, 38)`, `data-theme-id=blue`;
  screenshot визуально подтверждает полностью синюю оболочку;
- login focused tests 30/30, line/activity tests 24/24, инфраструктурные
  coverage/theme tests 36/36 green; bare-literal debt в login уменьшен 104→94 и
  baseline обновлён.

## Шаг 31 — исправление семантики verdict

Статус: прежнее заявление «цель закрыта» отозвано; неподтверждённость больше не
маскируется постоянным исключением `≠`.

- независимый пересчёт подтвердил дефект: в `strength-builder` 1627 `≠`
  опирались всего на 68 общих оснований; 1376 строк на 57 основаниях сообщали о
  непроведённой проверке, а не о конкретном установленном расхождении;
- 1376 строк `strength-builder` и одна составная строка `food-meal` переведены в
  `?`; глобальная сводка теперь `=` 9232 / `≠` 1475 / `?` 1377 / `—` 3490;
- добавлен fail-closed `ui-v4-check-verdict-semantics.mjs`: фразы
  `не подтверждено`, `не проверено`, `нужен review`,
  `точное совпадение не заявляется` и непроверенный frame-level вывод больше
  нельзя записать как `≠`;
- семантический гейт включён в общий `ui:v4:check`, его focused suite проходит
  11/11.

Ревьюеру смотреть: 1377 строк `?` как фактический открытый долг, а не как
принятые исключения; дальнейшую сверку `strength-builder` вести построчно по
механике/DOM/copy и отдельно по visual/pixel группам.

Ревьюеру смотреть: переключение sand↔blue на login и dark palettes на реальном
устройстве; generated bundles/manifests/index hash — только локальный QA-output,
не source для commit.

## Шаг 32 — восстановление контрактов cycle / curator-edits перед push

Статус: два падения preflight разобраны от канваса к коду; соседний reports-гейт
проверен и не принесён в жертву локальному зелёному тесту.

- `curator-edits`: CSS с полями строки повтора 11/13/12 оставлен как точное
  совпадение с кадром `· 09`; устаревший тест переведён с прежнего `≠` на
  проверку текущего `=`;
- `cycle`: период и подпись графика требуют 10px и 600 10px/1 с отступом 7px, но
  общий reports-кадр того же компонента требует 10,5px и 500 11px/1.5 с отступом
  10px;
- состояния разведены по `isCycleTrackingEnabled(profile)`: cycle-модификатор и
  copy появляются только при включённой функции, общий reports-контракт
  сохраняется для остальных пользователей;
- конфликт двух канвасов записан в дизайнерские расхождения с точными строками и
  просьбой явно назвать условие состояния.

Ревьюеру смотреть: оба профиля графика — с включённым и выключенным циклом;
дизайнеру подтвердить, что различие copy/типографики между состояниями
намеренно.

## Шаг 33 — Registration · персональные данные: парное визуальное сведение

Статус: живой первый шаг регистрации сведен с точным Canvas-кадром `REG1`
визуально и по геометрии, без изменения общих колес выбора.

- добавлен детерминированный read-only сценарий `registration-personal`, который
  снимает один и тот же modal root и точный Canvas frame; evidence green,
  Figtree загружен, console errors 0, клиентские данные не записываются;
- нормализованный исходный диагностический diff был 11,4368%; после правок
  точная парная метрика составляет 4,3259% (11 469 из 265 125 пикселей);
- исправлены высота и отступы полей, вертикальная геометрия шапки и формы,
  трехстрочное колесо, порядок лет, типографика возраста и disabled footer;
- scoped legacy bundle `postboot-3-ui-lazy` пересобран для локального QA; четыре
  focused test-файла проходят, 40/40 тестов green;
- Canvas показывает стрелку назад на первом шаге, но runtime-план не имеет
  предыдущего шага: текущий callback при индексе 0 ничего не делает. Фиктивная
  кнопка не добавлена; вопрос о реальном переходе вынесен дизайнеру/владельцу.

Ревьюеру смотреть: парные изображения и diff в
`tmp/ui-v4-visual/2026-09-01T22-04-47-313Z`; дизайнеру определить реальное
назначение стрелки первого шага либо убрать ее из Canvas.

## Шаг 34 — fail-closed evidence для REG1 / А1б / Б2

Статус: продуктовые правки трёх кадров не объявлены совпадением 1в1 только по
факту наличия коммита или низкого pixel-diff. Каждая из 104 строк получила
отдельный вердикт с DOM/computed-style/semantic evidence либо осталась `?`.

- batch до проверки: `=` 16 / `≠` 12 / `?` 76; после проверки: `=` 7 / `≠` 0 /
  `?` 97. Все прежние 12 общих `≠` REG1 сняты: после scoped-правки колеса они
  уже не описывали текущий код, а неподтверждённые строки нельзя сохранять как
  исключения;
- глобально: `=` 9452 → 9443, `≠` 1100 → 1088, `?` 1532 → 1553, `—` 3490.
  Снижение `=` здесь намеренное: старые утверждения REG1 не имели достаточного
  построчного evidence;
- exact evidence подтверждает 2 строки REG1 (капсула колеса и возраст), 2 строки
  А1б (пилюли прошлого подхода и рекорда) и 3 строки Б2 (имя экрана, строка
  списка, сноска). Остальные строки не пакетно повышались по статусу кадра;
- diagnostic pixel-diff остаётся только индикатором направления: REG1 ≈ 4,33%,
  А1б ≈ 17,70%, Б2 ≈ 8,48%; ни одно из этих чисел не означает равенство;
- 4 focused test-файла зелёные, 85/85; semantic gate зелёный для registration и
  strength-builder. Общий `LEGACY_SCHEMA_BASELINE.registration.mismatch`
  уменьшен с 79 до 67 и перепривязан к текущему digest — это только фиксация
  снятых 12 старых `≠`, без изменения валидатора или новой архитектуры.

Ревьюеру смотреть: 97 открытых строк трёх кадров — это фактическая очередь
дальнейших UI-правок, а не завершённые экраны. Следующий продуктовый проход
должен брать их по визуальному diff и только затем повышать конкретные строки до
`=`.

## Шаг 35 — Cycle · другой день: живой календарь сведен с Canvas

Статус: исправлен сам `CycleDatePickerSheet`, а не только verdict или тест.
Точный живой кадр `Цикл · другой день, календарь` теперь имеет воспроизводимую
пару с Canvas `CYC1` и заметно меньший pixel-diff.

- исходный runtime был структурно другим: 341×488 против Canvas 341×470,
  заголовок и месяц стояли иначе, присутствовали дни недели и календарное
  выравнивание с пустыми ячейками, отсутствовали ручка и навигация по месяцам;
- после правки размер пары одинаковый — 341×470; changed ratio 4,4450% (7 124 из
  160 270 пикселей). Нормализованный диагностический baseline до правки был
  13,0461% (20 909 пикселей), то есть расхождение уменьшилось на 66%;
- навигация не декоративная: доступен только месяц, пересекающий 28-дневное
  окно; для состояния 26 августа разрешены 30–31 июля, 29 июля и 27–31 августа
  заблокированы. Добавлены реальный `disabled`, roving focus, стрелки
  клавиатуры, PageUp/PageDown, Escape, `aria-current` и `aria-selected`;
- текст результата приведён к кадру: `Седьмой день — 24 августа` и отдельное
  объяснение пересчёта 18–24 августа; подтверждение по-прежнему передаёт
  выбранную ISO-дату в существующий продуктовый callback;
- focused suite: 4 test-файла, 43/43 green. Paired capture evidence ready,
  Figtree ready, console errors 0;
- scoped legacy build пересобрал только `boot-calc` из текущего состояния его
  bundle scope. Staging/commit/push не выполнялись.

Ревьюеру смотреть: пару runtime/Canvas/diff в
`tmp/ui-v4-visual/2026-09-01T22-27-07-288Z`; отдельно проверить на устройстве
переход август → июль → август и выбор 30/31 июля. Дизайнеру подтвердить, что
последовательная сетка без привязки первого числа к дню недели намеренна.

## Шаг 36 — Food meal · копирование без целей: парное визуальное сведение

Статус: реальный empty-target вариант листа копирования получил отдельное
состояние раскладки и честную пару с Canvas `FM10A`; логика выбора и копирования
не менялась.

- исходный exact-size baseline составлял 15,5029% (41 044 из 264 750 пикселей):
  flex-раскладка прижимала цель и действия к низу листа, оставляя около 370 px
  пустоты;
- empty-target состояние теперь имеет отдельный class-модификатор, поэтому
  продукты, пояснение, выбранная новая цель и действия идут подряд, как в
  Canvas. Общий long-content контракт existing-target не ослаблен;
- глобальное mobile-правило полей с `min-height: 36px !important` и
  `padding: 6px 10px !important` больше не загрязняет range: локальный control
  снова имеет 14 px, а строка граммов — 28 px;
- итоговый changed ratio 4,7585% (12 598 из 264 750 пикселей), evidence ready,
  Figtree ready, console errors 0. Снижение расхождения — 69,3%;
- long-content smoke подтверждает 8 продуктов, настоящий scroll и видимый CTA;
  focused suite проходит 27/27. Scoped QA bundle затронул только
  `postboot-3-ui-lazy`; generated output в source-only commit не входит.

Ревьюеру смотреть: пару в `tmp/ui-v4-visual/2026-09-01T22-49-49-418Z`; отдельно
проверить выбор/снятие продукта, ±10, range и cancel. Позиция бегунка в Canvas
не соответствует реальному значению 100 г на диапазоне 5–500 г и вынесена
дизайнеру.

## Шаг 37 — Food meal · копирование в существующий приём: exact-size сведение

Статус: живой long-content лист сопоставлен с точным Canvas `FM10`; исправлена
раскладка скроллируемого списка и нижней части без изменения selection/N26 и
callback-контрактов копирования.

- exact-size пара имеет размер 375×706. После нормализации fixture по данным
  кадра baseline составлял 19,4327% (51 448 из 264 750 пикселей); итоговый
  changed ratio — 6,0438% (16 001 пиксель), снижение расхождения на 68,9%;
- viewport списка продуктов ограничен 262 px: в кадре видны ровно первые три
  карточки из восьми, а фактический DOM содержит 8 элементов и сохраняет
  вертикальный scroll (`scrollHeight=712`, `clientHeight=262`);
- снят унаследованный global margin с target-label. Три карточки, ярус цели, обе
  radio-цели, summary и кнопки теперь совпадают с Canvas по координатам;
- fixture воспроизводит `Продукты 8/8`, первые 157/81/50 ккал, цель
  `Перекус · 10:40` и живой итог `17 → 1 610 ккал (+1 593)`. CTA остаётся
  видимым и называется `Копировать (8)`;
- реальные положения range-thumb сохранены по значениям 137/30/300 г, а не
  подогнаны под одинаковую декоративную отметку Canvas;
- focused suite: 3 файла, 27/27 green; paired capture evidence ready, Figtree
  ready, console errors 0. Regression-пара `FM10A` улучшилась с 4,7585% до
  4,0619%;
- scoped legacy auto-check подтвердил, что изменённый модульный CSS не требует
  пересборки legacy bundle. Staging/commit/push не выполнялись.

Ревьюеру смотреть: runtime/Canvas/diff в
`tmp/ui-v4-visual/2026-09-01T22-59-39-530Z`; отдельно пройти scroll до восьмого
продукта, select-all/individual, ±10/range, existing/new target, cancel и оба
callback-варианта.

## Шаг 38 — Food meal · перенос в существующий приём: exact-size сведение

Статус: живой лист переноса получил точную пару с Canvas `FM11`; исправлена
только раскладка move-варианта, без изменения транзакции переноса и N26.

- exact-size пара имеет размер 375×706. Baseline составлял 14,1186% (37 379 из
  264 750 пикселей): действия были прижаты к низу листа, дата находилась выше
  кадра и была толще;
- после правки changed ratio — 4,6727% (12 371 пиксель), снижение расхождения на
  66,9%. Координаты и высоты даты, обоих ярусов, трёх целей, предупреждения и
  самих кнопок совпадают с Canvas;
- fixture fail-closed воспроизводит 26 августа, `Завтрак · 12:00` и выбранный
  `Обед · 14:00`; capture требует одновременно `checked`, `is-selected`, точный
  target id `visual-lunch`, предупреждение и обе кнопки;
- логика не подгонялась под снимок: existing-цель по-прежнему передаёт
  `targetMode: 'existing'` и стабильный `dstMealId`; create-new не выдумывает
  id, а двухшаговая запись/rollback остаются под прежними тестами;
- независимый focused suite: 4 файла, 48/48 green, включая `day-mutation-guard`;
  отдельный meal-transfer suite — 20/20. Paired capture evidence ready, Figtree
  ready, console errors 0;
- соседние пары не изменились: `FM10` — 6,0438% (16 001), `FM10A` — 4,0619% (10
  754). Scoped legacy auto-check подтвердил, что модульный CSS не требует
  пересборки legacy bundle;
- нового конфликта логики с Canvas не найдено, поэтому дизайнерский журнал не
  пополнялся. Push не выполнялся.

Ревьюеру смотреть: baseline в `tmp/ui-v4-visual/2026-09-01T23-08-04-637Z`,
итоговую пару в `tmp/ui-v4-visual/2026-09-01T23-10-24-532Z`; отдельно пройти
выбор обеих существующих целей, create-new, смену даты, cancel и фактический
перенос с rollback при отказе второй записи.

## Шаг 39 — Norm correction · применённое снижение: кириллическая метрика сведена с NC5

Статус: экран применённого решения куратора `Сверка · норма снизилась`
сопоставлен с точным Canvas `NC5`. Логика расчёта, данные карточки и действия не
менялись.

- исходная exact-size пара 375×505 имела changed ratio 9,8962% (18 741 из 189
  375 пикселей); геометрия уже совпадала, но почти каждый кириллический глиф и
  перенос строки отличались;
- причина была в fallback-цепочке: Canvas задаёт `Figtree, sans-serif`, а
  runtime добавлял перед generic fallback `HEYS Manrope` и `system-ui`. Для
  кириллицы это давало другую метрику при тех же заявленных 12/500 и 16/700;
- экран теперь использует буквальную цепочку Canvas `Figtree, sans-serif`.
  Итоговый changed ratio — 5,2763% (9 992 пикселя), снижение расхождения на
  46,7%. Первые 20 символов пояснения имеют одинаковую ширину 116,53 px; три
  строки совпадают по координатам и ширинам: 298,11 / 245,95 / 133,31 px;
- visual fixture после готовности lazy bundle повторно загружает текущий
  `heys_weekly_reports_v2.js`, поэтому paired capture не может молча проверить
  устаревший generated bundle. В отчёт добавлены парные box/font/line-метрики;
- продуктовые guardrails сохранены: показано уже применённое решение куратора,
  Pro не получает отмену, `Понятно` закрывает экран, `Написать куратору`
  открывает messenger, лимит 3% и текст о невозможности отмены остаются
  видимыми;
- независимый focused suite: 3 файла, 107/107 green. Итоговая пара
  evidence-ready, Figtree ready, runtime/Canvas console errors 0.
  `docs:reference:check` дошёл до общего гейта и остановился на двух
  существовавших вне scope досье без раздела `Не подтверждено/Не охвачено`:
  `BACKGROUND_JOBS.md` и `SUBSCRIPTION_AND_PAYMENTS.md`;
- dossier не менялся: поведение, владелец данных и инварианты nutrition/norm
  correction не изменились. Generated/public artifacts не входят в source-only
  scope; push не выполнялся.

Ревьюеру смотреть: baseline `tmp/ui-v4-visual/2026-09-01T23-19-58-268Z`,
итоговую пару `tmp/ui-v4-visual/2026-09-01T23-40-21-049Z`; отдельно проверить
оба действия на живом устройстве и не трактовать оставшиеся 5,2763% как полное
pixel-equality — текущий gate этой зоны остаётся diagnostic.

## Шаг 40 — Reports insights · «Что если»: exact Canvas pair и мобильный route-smoke

Статус: второй слой `Инсайты → Подробнее → Что если` получил строгую живую пару
с Canvas `RI-WI1`; продуктовая динамика оценки сохранена.

- кадру `Инсайты · что если` добавлен единственный технический oid `RI-WI1`;
  fixture теперь снимает уникальный runtime-root и точный Canvas frame в
  одинаковом окне 375×706 без resize и без масок;
- исходная официальная пара после одной только нормализации capture имела
  changed ratio 20,2806% (53 693 из 264 750 пикселей): кириллический fallback
  держал четыре вкладки в одной строке и смещал весь нижний контент;
- scoped CSS использует буквальный Canvas fallback `Figtree, sans-serif`, а
  строки альтернатив получили canvas-min-height 44 px. Теперь bounding boxes
  шапки, двух рядов chips, сценария, score, alternatives и пояснения совпадают с
  Canvas до сотых пикселя;
- итоговый changed ratio — 2,1756% (5 760 пикселей), снижение расхождения на
  89,3%. Остаток — raster/antialias diagnostic внутри уже совпавших boxes и края
  camera-shell; pixel-gate всей зоны не включался;
- fixture не хардкодит результат: real `add_protein` с `30 г`, завтраком, шестью
  observed scores `69` и `requireObserved: true` проходит через живые `simulate`
  и `calculateHealthScore`, который возвращает `75`; DOM показывает
  `72 → 75 / +3`. Capture fail-closed сохраняет аргументы и результат обоих
  engine-вызовов;
- mobile route-smoke при viewport 375×706 прошёл реальный путь
  `Инсайты → Подробнее`: панель видима, `Активность` переносится во второй ряд,
  обе alternative-строки имеют 44 px, а пользовательские данные дали живую пару
  `63 → 64 / +1`, то есть Canvas-литералы в production не подменяют расчёт;
- дизайнерское расхождение записано отдельно: числа кадра — fixture example,
  acceptance для произвольного пользователя должен проверять вычисляемую связь и
  формат, а не конкретные литералы.
- независимый focused suite: 2 файла, 15/15 green; visual evidence-ready,
  runtime/Canvas console errors 0. `docs:reference:check` остановился только на
  двух существовавших вне scope досье: `BACKGROUND_JOBS.md` и
  `SUBSCRIPTION_AND_PAYMENTS.md`. Generated/public artifacts не входят в
  source-only scope; push не выполнялся.

Ревьюеру смотреть: baseline `tmp/ui-v4-visual/2026-09-01T23-44-59-287Z`,
итоговую пару `tmp/ui-v4-visual/2026-09-01T23-49-21-511Z`; отдельно проверить
переход в `Подробнее`, переключение четырёх категорий и сохранение fail-closed
сообщения при отсутствии observed-patterns.

## Шаг 41 — Home · дефолтная сетка: живые данные отделены от pixel-контракта

Статус: вся дефолтная сетка из 13 плиток получила строгую exact-size пару с
Canvas `HW1`; геометрия сведена, продуктовые вычисления не заменены числами
макета.

- точная пара ограничена изоморфной областью: runtime `.widgets-grid` ↔ Canvas
  `.g` с технической padding-boundary. Размер обеих сторон — `375×598`, сетка —
  четыре колонки, ряд `64 px`, gap `8 px`, padding `14px 16px 16px`;
- до подстановки одного и того же сценария changed ratio был `12,4557%`
  (`27 932 / 224 250`). Детерминированная fixture задаёт данные всех 13 плиток
  только на render boundary, вызывает штатный `Widgets.data.refresh()` и
  fail-closed ждёт девять ключевых токенов;
- после одинаковых данных и исключения двух fixed FAB только из grid-crop
  changed ratio — `8,0678%` (`18 092 / 224 250`), снижение на `35,2%`. Layout и
  роли фонов совпадают; остаток включает raster/antialias и сознательно
  сохранённый нейтральный тон `−1` из-за противоречия Canvas;
- исправлены однозначные продуктовые расхождения: top padding сетки, точная
  форма/толщина/маркер compact health spark, стабильная подпись
  `Тренд здоровья · 7 дней`, типографский минус БЖУ, счёт heatmap только по дням
  в норме и no-history copy `Первые дни / нужна неделя`;
- живой browser-smoke на fixture-клиенте прошёл вход, утренний чек-ин и переход
  на «Главную». В DOM видны 13 плиток, динамические нормы, реальный риск, кнопки
  настройки/добавления и пять вкладок; секреты пользователя не нужны;
- focused suite: 4 файла, `34/34` green. Paired capture evidence-ready, Figtree
  ready, runtime/Canvas console errors `0`. Scoped legacy bundle собран из
  текущего состояния `postboot-3-ui-lazy`; generated/public файлы не входят в
  source-only scope;
- следующие отдельные Home-состояния ещё открыты: калории/БЖУ/белок/клетчатка
  без данных должны показывать прочерк, а не ноль. Это не объявлено завершённым
  вместе с filled-state.

Ревьюеру смотреть: baseline `tmp/ui-v4-visual/2026-09-02T00-11-34-116Z`,
итоговую пару `tmp/ui-v4-visual/2026-09-02T00-30-09-425Z`; отдельно проверить
живой Home с динамическими данными и пять новых записей расхождений HW1 в
дизайнерском журнале.

## Шаг 42 — Home · честные пустые состояния и fail-closed графики

Статус: пустой день проверен на живом fixture-клиенте; UI больше не выдаёт нули
и выдуманную графику за реальные наблюдения.

- `getCaloriesData()` и `getMacrosData()` явно передают `hasData`; при
  отсутствии записанного продукта плитки сохраняют подписи, но показывают
  прочерки без полос и колец. Записанный продукт с нулевыми нутриентами остаётся
  настоящим нулём; вода остаётся разрешённым исключением `0`;
- живой browser-smoke после утреннего check-in подтвердил все 13 плиток на
  местах: calories/macros/protein/fiber/steps используют прочерки, вода
  показывает `0`, layout не заменяется отдельным onboarding-экраном;
- crash-risk теперь различает `insufficient_history`, `provider_unavailable` и
  `provider_error`. Только первый случай имеет право показывать Canvas-состояние
  `Первые дни / нужна неделя`; ошибка источника не маскируется нехваткой
  истории;
- Health Trend больше не строит hardcoded-кривую и не соединяет текущие
  категории как временной ряд. SVG появляется только из явного
  `data.sparkline.points`; повреждённые/нетекстовые points отклоняются
  fail-closed; точный ряд Canvas остаётся в visual fixture, а не в production
  data owner;
- встречное ревью поймало риск нарушения порядка React hooks при переходе
  `пустой день → первый приём` у activity-варианта калорий. Motion hook
  перенесён выше fail-closed return и теперь вызывается неизменно; порядок
  закреплён тестом;
- в журнал дизайнера добавлены два расхождения: отсутствующий production-owner
  временного ряда Health Score и конфликт измеренного padding `14px 16px 16px` с
  текстовой строкой `16px`;
- этот шаг не уменьшает прежний filled-grid diff `8,0678%`: он закрывает другое
  runtime-состояние. Визуальный прогресс всего Home впредь измеряется составными
  областями header/date, grid, FAB и bottom navigation, а не числом разобранных
  строк.

Проверки: focused Home suite `74/74`, visual/reverse coverage `14/14`, verdict
semantics green; живой smoke 375×812 без console errors. Повторная filled-пара
`tmp/ui-v4-visual/2026-09-02T00-48-40-995Z` неизменна: `18 092 / 224 250`,
`8,0678%`, evidence-ready, console errors `0`.

Ревьюеру смотреть: пустой Home на 375×812, отсутствие SVG у живого Health Trend
без ряда, корректный `Первые дни` только при `emptyReason=insufficient_history`,
а также неизменность exact filled-fixture, где sparkline передан явно.

## Шаг 43 — Home · пустой день получил воспроизводимый visual evidence

Статус: semantic smoke шага 42 превращён в отдельный автоматический diagnostic,
чтобы его можно было повторить без пароля и ручной подготовки клиента.

- `home-widgets-empty-day` снимает полный viewport 375×812 с header, date, 13
  плитками, обоими FAB и нижней навигацией; exact Canvas frame ему намеренно не
  назначен, поэтому сценарий не выдаётся за pixel equality;
- fail-closed checks требуют 13 плиток, прочерки
  calories/macros/protein/fiber/steps, отсутствие calorie bars, macro rings и
  Health SVG, а также разрешённый ноль воды;
- filled fixture перестал shallow-merge будущие production-поля: для всех 13
  известных типов используются точные объекты кадра, неизвестный тип отдельно
  откатывается к штатному provider;
- evidence: `tmp/ui-v4-visual/2026-09-02T00-52-39-964Z`, все проверки true,
  Figtree ready, console errors `0`.

Это первый полный скриншот Главной в новой метрике, но не первый full-screen
pixel pair: Canvas HW1 не содержит FAB/nav. Следующий шаг — отдельные строгие
пары header/date и сохранение grid как самостоятельной области с diff `8,0678%`.

## Шаг 44 — Home · каркас экрана совмещён с Canvas HW1

Статус: исправлена измеренная геометрия полного viewport 375×812; бизнес-данные
и состояния плиток не менялись.

- до правки runtime ставил header/date/grid на `y=20/80/126`, тогда как Canvas
  HW1 задаёт `16/46/82`; после правки diagnostic подтверждает `16/46/82`;
- header и date теперь имеют канонические `x=18`, `width=339`, дата `height=36`;
- FAB пришли с `y=678/670` на `686.5/678.5`, то есть ровно на расстояния
  `18/14 px` от фактического верхнего края nav;
- нижняя навигация сохранила `y=744.5`, `h=67.5`, а её рабочая строка расширена
  с `x=16, width=343` до канонических `x=10, width=355`;
- FAB привязаны не к литералу одного viewport, а к измеренной через
  `ResizeObserver` фактической высоте nav, поэтому сохраняют зазор при safe-area
  и увеличенном тексте;
- оттенки Home FAB в blue/blue-dark и water-вариантах приведены к литералам
  Canvas; спорная подложка активной вкладки не менялась и записана дизайнеру;
- функциональное облако синхронизации не удалено ради снимка: конфликт кадра с
  действующим контрактом также записан дизайнеру.

Evidence после правки:
`tmp/ui-v4-visual/2026-09-02T01-11-31-428Z/home-widgets-empty-day.png`; все
геометрические и empty-state checks прошли, Figtree ready, console errors `0`.
Отдельный narrow-smoke 320×700 подтвердил тот же каркас и 44×44 px cloud-target:
`tmp/ui-v4-visual/2026-09-02T01-11-41-531Z`. До правки полный diagnostic:
`tmp/ui-v4-visual/2026-09-02T00-52-39-964Z/home-widgets-empty-day.png`.

Ревьюеру смотреть: компактную строку уровня без потери 44 px touch-targets, стык
date → grid без прежних 44 px пустоты, положение обеих FAB и ширину пяти пунктов
nav. Строгий grid crop остаётся отдельной метрикой содержимого плиток.

Проверки: shell/widgets/visual/reverse suite `62/62`; повторная filled-grid пара
`tmp/ui-v4-visual/2026-09-02T01-12-33-166Z` evidence-ready, console errors `0`,
содержимое ожидаемо осталось `18 092 / 224 250 = 8,0678%` — crop сетки не
включает исправленный каркас вокруг неё.

## Шаг 45 — Home · первый доказанный проход по содержимому плиток

Статус: строгая runtime ↔ Canvas пара впервые использована как основной гейт
правки, а не только как инвентаризация расхождений.

- `.widgets-grid` получил Figtree, который уже загружался harness, но runtime
  фактически не использовал; проверка теперь сохраняет computed `fontFamily` и
  требует Figtree первым;
- дневной SVG инсулиновой волны отделён от остальных её видов и возвращён на
  канонический `margin-top: 8px`; прежний `auto` опускал купола на 13–14 px;
- кольца БЖУ используют фактическую долю окружности без legacy-компенсации round
  caps; отдельная дуга перебора и её продуктовый смысл сохранены;
- риск-радар снял числовой tracking со слова `низкий`, а активный сегмент шкалы
  использует тот же тёмный шалфей Canvas, что и уровень;
- добавлен явный парный neutral-тон для прочерка БЖУ, чтобы fail-closed гейт
  `класс + тон` снова был зелёным.

Evidence до правки: `tmp/ui-v4-visual/2026-09-02T01-12-33-166Z`,
`18 092 / 224 250 = 8,0678%`. После Figtree и геометрии волны:
`tmp/ui-v4-visual/2026-09-02T01-23-01-295Z`, `14 987 / 224 250 = 6,6832%`. После
дуг БЖУ и риск-радара: `tmp/ui-v4-visual/2026-09-02T01-29-08-997Z`,
`14 716 / 224 250 = 6,5623%`. Итого снято 3 376 изменённых пикселей, или 18,7 %
исходного визуального долга этой пары. Вне прямоугольников 13 плиток diff
остаётся нулевым.

Вода не подгонялась под вторичный кадр HW1: главный `water-add` одновременно
содержит снятую нижнюю раскладку и позднее решение
`label/value сверху, без нормы`. Конфликт уже находится в журнале дизайнера;
owner-контракт прямо требует остановиться при таком расхождении.

Проверки: 7 focused suites, `83/83`; paired capture evidence-ready, Figtree
используется, console errors `0`. Локальный scoped bundle собран из текущего
состояния `postboot-3-ui-lazy`; generated/public в source-коммит не входит.

## Шаг 46 — Home · измеритель и малые плитки сведены по HW1

Статус: paired capture теперь сравнивает runtime и Canvas в одинаковом
mobile/touch-контексте. Поскольку целевой `.ph` находится внутри широкого
дизайн-документа, перед снимком он изолируется у начала viewport; без этой
изоляции Playwright снимал соседнюю область и ложно сообщал `90,09%` отличий.

- fail-closed тест требует `isMobile: true` и `hasTouch: true` у обеих сторон
  пары, а также фиксированную изоляцию точного Canvas frame;
- кольца БЖУ повторяют фактический SVG HW1: отдельной дуги перебора в кадре нет,
  состояние остаётся в числе/остатке; inline SVG получает канонический line-box;
- у калорий дорожка получила 10% чернил, а левое число — 85%; табличные цифры
  сохранены по текстовому контракту, даже если inline `font` кадра визуально
  сбрасывает их;
- шаги теперь округляются сразу в целые пиксели высоты из 30 px, поэтому fixture
  воспроизводит канонические `26/21/30/17/29/25/27 px`, а не дробные проценты;
- счёт тепловой карты и месячная динамика веса используют тёмный шалфей HW1;
  значение месячной динамики — `21px/600`;
- мини-плитки белка/клетчатки вкладывают единицу в число как Canvas и используют
  8% дорожку вместо 12%.

Сопоставимый capture после исправления контекста, но до этого пакета:
`tmp/ui-v4-visual/2026-09-02T01-38-23-707Z`, `13 826 / 224 250 = 6,1654%`. После
пакета: `tmp/ui-v4-visual/2026-09-02T01-41-00-663Z`,
`13 703 / 224 250 = 6,1106%`. От исходной пары шага 45
`18 092 / 224 250 = 8,0678%` снято 4 389 пикселей, или 24,3% исходного
визуального долга. Остаток не объявлен совпадением: крупнейшие области — БЖУ,
риск-радар и типографика малых плиток — продолжают разбираться по computed CSS.

Проверки: 5 focused suites, `60/60`; повторный `widgets-v4-sphere` — `19/19`;
paired capture evidence-ready, Figtree ready, console errors `0`. Scoped
`postboot-3-ui-lazy` пересобран из текущего состояния bundle scope;
generated/public в source-коммит не входит.

## Шаг 47 — Home · устранён шум fallback-шрифта и compositing

Статус: основной остаток paired diff отделён от двух ошибок самого измерителя;
реальные локальные роли исправлены, противоречия Canvas не замаскированы.

- локальный Figtree не содержит кириллицу: Canvas после него использует
  `sans-serif` (Arial), а runtime раньше уходил в `system-ui` (Segoe UI). Сетка
  теперь повторяет канонический fallback `Figtree, sans-serif`; контрольные
  ширины русских строк совпали с Canvas;
- обе стороны pixel-pair теперь получают одинаковый `translate3d(...)` после
  округления origin. До этого Canvas оказывался в composited layer, runtime —
  нет, и один Chromium рисовал один и тот же текст разным AA;
- локальный `vertical-align: baseline` отменяет Tailwind-reset
  `svg { vertical-align: middle }` только у колец БЖУ; diff макроплитки снизился
  с 3 482 до 1 585 ещё до исправления compositing;
- у тепловой карты снят legacy `padding-inline: 6px`: семь полос снова занимают
  всю внутреннюю ширину HW1;
- добавлена самостоятельная четырёхпалитровая роль `--v4-val-bad`
  (`#a8382b/#e08a72`) и адресно применена к bad-значениям Home; footer
  инсулиновой волны использует тёмный шалфей;
- сырой кадр всё ещё конфликтует со своими строками по `.n`, 42% колец, Health
  `−1`, текущему весу и воде. Эти случаи оставлены в журнале дизайнеру, а не
  подогнаны до зелёного ценой семантики.

Evidence по одной и той же паре:

- после шага 46: `13 703 / 224 250 = 6,1106%`;
- после baseline SVG: `11 806 / 224 250 = 5,2647%`;
- после heatmap и кириллического fallback: `9 960 / 224 250 = 4,4415%`;
- после симметричного compositing: `6 580 / 224 250 = 2,9342%`;
- после точных value-ролей: `tmp/ui-v4-visual/2026-09-02T01-58-15-396Z`,
  `6 546 / 224 250 = 2,9191%`.

От исходных `18 092` изменённых пикселей осталось `6 546`: снято `11 546`, или
63,8% исходного визуального долга Home HW1. Риск-радар достиг `0` изменённых
пикселей внутри своей плитки. Остаток не объявлен завершённым.

## Шаг 48 — Food meal · листы копирования и переноса сведены

Статус: три точных пары `FM10A/FM10/FM11` приведены к измеряемому состоянию;
перенос целого приёма проходит строгий pixel-порог, а остаток двух copy-пар
локализован в зафиксированном конфликте динамических ползунков.

- лист получил канонический `Figtree, sans-serif`, сглаживание, двухслойную тень
  Canvas и точную baseline-геометрию строки `Продукты / Снять всё`;
- вертикальная сетка списка выровнена: tier имеет 11 px измеряемой высоты,
  карточки начинаются с канонического `y=69`, дальнейшие цели и footer больше не
  сдвинуты на полпикселя;
- незаполненная дорожка range использует `rgba(var(--v4-ink-rgb), 0.1)`, как
  Canvas, при сохранении честного динамического положения бегунка;
- visual fixture теперь заранее фиксирует факт показа optional-feature offer, а
  capture fail-closed останавливается, если consent всё же перекрыл целевой
  экран;
- измеритель маскирует только пиксели за пределами скругления 28 px. Это фон
  окружения, которого нет внутри product surface: runtime показывает настоящий
  modal backdrop, а изолированный Canvas — светлую страницу макета.

Evidence одной последовательной серии после всех правок:

- `FM10A`: `tmp/ui-v4-visual/2026-09-02T02-15-51-534Z` —
  `726 / 264 086 = 0,2749%`;
- `FM10`: `tmp/ui-v4-visual/2026-09-02T02-16-03-135Z` —
  `2 315 / 264 086 = 0,8766%`;
- `FM11`: `tmp/ui-v4-visual/2026-09-02T02-16-13-499Z` —
  `81 / 264 086 = 0,0307%`.

До шага те же последовательные пары давали `9 529 + 12 450 + 8 485 = 30 464`
изменённых пикселя. После продуктовых правок и честной boundary-маски осталось
`3 122`: снято `27 342`, или `89,8%` исходного долга этого набора. Маска
отдельно называет `664` внешних пикселя каждой пары и не скрывает границу самого
листа.

Проверки: 3 focused suites, `31/31`; три paired capture evidence-ready, console
errors `0`; optional consent не перекрывает ни одну пару. Scoped legacy
auto-sync подтвердил, что затронутый CSS-модуль не требует legacy-bundle;
generated/public в source-коммит не входит.
