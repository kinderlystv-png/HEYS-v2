# Конструктор тренировки: что уже реализовано — доля «программа и перенос»

Восемь кадров канваса `docs/ui/handoff-v4/canvas/…/strength-builder.v4.dc.html`
(63 кадра, 1755 строк спеки; в этой доле 267 строк). Снято 1 сентября 2026 перед
построчным разбором.

**Зачем эта таблица.** Вводная к разбору говорила «продукта под канвасом ещё
нет», и вердикты предполагалось ставить `?` с пометкой «не реализовано». Это
неверно: продукта **5690 строк** — `750-strength-builder.css` (1865 строк, 128
классов `.sb-*`), пять модулей `apps/web/strength/`, ядро
`_kernel/heys_kernel_strength_v1.js` (1243 строки). Ставить `?` подряд значило
бы записать сделанное в долг.

**Как проверялось.** По имени сущности, а не по фразе кадра: сначала список
существующих классов `.sb-*` и функций ядра, потом поиск по ним. Поиск по копии
кадра даёт ложное «нет», когда механика в продукте названа иначе — на этой доле
он ошибся дважды (см. «Шторка ⋯» и «Круговой режим»).

## Кадр за кадром

| кадр                                     | статус      | чем подтверждено                                                                                                                                                                                                                                                                                                                                   |
| ---------------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **План в ленте дня**                     | реализовано | `PlanCard` рендерится в ленте дня при `plan.status === 'assigned'` — `heys_day_trainings_v1.js:3885`, с `moveOptions` и `weekPlace`. Стили `.sb-plan-card`, `.sb-plan-badge`, `.sb-plan-meta`, `.sb-plan-cta`, `.sb-plan-actions`                                                                                                                  |
| **День не состоялся**                    | реализовано | тот же `PlanCard` при `plan.status === 'skipped'`; `.sb-plan-skip`, `.sb-move-skip`, `.sb-skip-reason-input`; копия «Не смогу сегодня», «Отпустить», «Что помешало» — в продукте                                                                                                                                                                   |
| **Конструктор · итоги**                  | реализовано | `strength/heys_strength_finish_ui_v1.js`: «Подходов», «Макс. вес», «Максимум · Эпли», «Последние тренировки», «Под нагрузкой», «Без тоннажа»; классы `.sb-finish`, `.sb-stats`, `.sb-stat`, `.sb-total`                                                                                                                                            |
| **Программа пройдена**                   | частично    | `programGrowth` — `_kernel/heys_kernel_strength_v1.js` и `strength/heys_strength_proposal_ui_v1.js`; копия «Программа пройдена» и «Что выросло» в продукте. Не найдено: «цикл закрыт», разбивка «9 из 12» и блок «Написать Артёму»                                                                                                                 |
| **Перенос · след в обе стороны**         | частично    | перенос есть: `moveOptions` (`heys_day_trainings_v1.js:3895`), классы `.sb-move-day`, `.sb-move-days`. Не найдено: экран выбора дня «Куда перенести» с занятыми днями и **след с обеих сторон** — ни «перенесено с», ни отметки в дне-источнике                                                                                                    |
| **Шторка ⋯**                             | частично    | лист есть: `.sb-sheet`, `.sb-sheet-row`, `.sb-sheet-grip`, `.sb-sheet-icon`, `.sb-sheet-back`; рендер — `strength/heys_strength_builder_ui_v1.js:489`. Из шести пунктов кадра в продукте три: «Каталог упражнений», «История и рекорды», «Заметка к тренировке». Нет пунктов «Шаблоны тренировок», «Повторить прошлую», «Назначено против сделано» |
| **Программа · цикл**                     | частично    | тоннаж есть (`dayTonnage`, `trainingTonnage` в ядре; `tonnage` в `heys_day_activity_v1.js`). **Фаз цикла нет**: `rampUp`, `mesoCycle` — ноль вхождений; `deload` и `mesocycle` встречаются только в модуле пальцев, к силовому контуру отношения не имеют                                                                                          |
| **Программа · назначено против сделано** | нет         | ни фразы, ни имени: «Назначено против сделано», `planVsDone`, `assigned_vs` — ноль вхождений. Отдельного экрана сверки плана с фактом в продукте нет                                                                                                                                                                                               |

## Две ошибки поиска по фразе, чтобы их не повторили

1. **«Шторка ⋯» сначала получила «нет».** Искала по её пунктам — «Шаблоны
   тренировок», «Повторить прошлую», «Круговой режим» — и все три дали ноль. Сам
   лист при этом существует и рендерится; отсутствуют только три пункта из
   шести.
2. **«Круговой режим» — не отсутствует, а называется иначе.** В продукте это
   связки: `makeSuperset`, `addSupersetRound`, `supersetRounds` в ядре, классы
   `.sb-ss`, `.sb-ss-head`, `.sb-ss-member`, `.sb-round`, `.sb-round-num`.
   Механика есть, нет пункта в шторке.

Отсюда правило для остальных долей: **фраза кадра — первый признак, имя сущности
— решающий.** Ноль по фразе означает «надо проверить вторым способом», а не «в
продукте нет».

## Чего эта таблица не говорит

Она отвечает «есть ли реализация», а не «совпадает ли она с кадром». У всех
четырёх «частично» и трёх «реализовано» построчная сверка ещё не делалась:
расхождения геометрии, копии и поведения там могут быть любые. Долю «нет» (один
кадр, 45 строк) построчно сверять не с чем — там нужен вердикт «кода нет».
