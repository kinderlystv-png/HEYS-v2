# Протокол реализации · Вкладка «Питание» v4

Источник правды — контракт канваса
`docs/ui/handoff-v4/canvas/Переработка дизайна приложения/design_handoff_heys_v4/nutrition-tab.v4.dc.html`
(`[data-contract="nutrition-tab"]`, 103 строки, 40 кадров). Приёмочный лист —
`ACCEPTANCE-nutrition-tab.md` там же. Исходное состояние кода —
[NUTRITION_TAB_AS_IS.md](NUTRITION_TAB_AS_IS.md).

Документ ведётся по ходу работы: после каждого крупного шага — что сделано, чем
проверено, что смотреть ревьюеру, какие риски остались.

## Решения, принятые по ходу

| #   | Развилка                               | Решение                                                                                                                             | Почему                                                                                                                                                                                                        |
| --- | -------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | Где живут чип-зависимые блоки          | Внутри `NutritionTabV4`, порядок задан контрактом                                                                                   | Контракт «порядок блоков» ставит их между «качеством еды» и водой; `diarySection` рендерится в page shell **после** карточки и после воды/чипов, туда порядок контракта не укладывается                       |
| 2   | Ранний выход `renderDiarySection`      | Снят как «мобильный fast path». Функция теперь владеет только десктопным легаси-дневником; вкладка «Питание» её не использует вовсе | Требование задачи — шесть блоков видны на телефоне; они переехали в карточку. Секция перестала быть «мобильной веткой рендера» и стала десктопным легаси                                                      |
| 3   | Расхождение бюджета (`displayOptimum`) | Нормы экрана пересчитываются от `displayOptimum` отдельным мемо `displayNormAbs` после `caloricDebt`                                | `caloricDebt → displayOptimum` считаются позже `buildNutritionState` (зависят от `dayTot → sparklineData`); перенос хуков вверх невозможен. Советы остались на базовых нормах — это открытый пункт, см. риски |
| 4   | Шкала вредности в советах              | `advice/_core.js`: правило-стрик делит на 10 (доля шкалы 0–10), дневной `harmPct` переводит факт в проценты профильного `harmPct`   | Владелец: «harmPct остаётся входом советов»; порог экрана — константа 5 = 0,5 шкалы                                                                                                                           |
| 5   | Вторичный тон                          | Локальные роли `--nut-dim` / `--nut-dim-icon` на `.nutrition-v4`                                                                    | Глобальная правка `--v4-ink-2` задела бы всё приложение; тот же приём уже применён в `water-add` (`--wr-dim`)                                                                                                 |
| 6   | Новые ролевые токены                   | `--v4-warn-text`, `--v4-tint`, `--v4-past` во всех шести наборах                                                                    | Контракт требует отдельный текстовый шаг предупреждения и покрытие синих наборов; существующих ролей с этими значениями нет                                                                                   |
| 7   | Запасные значения ролей в CSS          | У каждой роли записано, что покажет каноничная палитра                                                                              | Гейт `scripts/ui-v4-check-classic-drift.mjs` требует записи; запасное значение не срабатывает никогда (роли определены во всех наборах), литералом в смысле контракта не является                             |
| 8   | «Энергия» в блоке «Голод и энергия»    | `day.wellbeingAvg` → `day.wellbeingMorning`                                                                                         | Отдельной метрики «энергия 1–10» в данных нет; самочувствие — единственная шкала 1–10 того же смысла. Помечено как допущение                                                                                  |
| 9   | «Полноценный приём» для строки серии   | Приём с продуктами, берущий ≥ 15 % бюджета дня                                                                                      | Контракт называет факт «три полноценных приёма подряд», но определения не даёт; без порога в серию попадал бы кофе с молоком                                                                                  |
| 10  | Белок в зонах перебора                 | Зон нет вовсе                                                                                                                       | Контракт «зоны у макросов» называет пороги 110/130 у жиров, углеводов и клетчатки; белок в списке отсутствует, а «чем больше, тем лучше» — не ошибка                                                          |
| 11  | Гейт `canUsePostReleaseLabs`           | Оставлен; снят только раздел «Ещё → Дневник», который он держал                                                                     | Тот же гейт держит вкладки «Задачи» и «Доска». Снять его целиком значило бы выкатить их всем клиентам — это другой продукт, а не эта вкладка                                                                  |

## Шаги

| #   | Шаг                                                 | Статус   | Чем проверено                                                            |
| --- | --------------------------------------------------- | -------- | ------------------------------------------------------------------------ |
| 1   | Дефекты чисел (B)                                   | сделано  | `nutrition-tab-v4-states.test.js`, ручная сверка формул                  |
| 2   | Палитры и токены (D)                                | сделано  | `ui-v4-classic-drift.test.js` (был красным на 20 сдвигах — стал зелёным) |
| 3   | Каркас вкладки и чип-зависимые блоки (A)            | сделано  | `nutrition-v4-structure.test.js`, `nutrition-tab-v4-states.test.js`      |
| 4   | Лист правки приёма и действия (C)                   | сделано  | те же + `day-diary-section-mobile-fast-path.test.js`                     |
| 5   | Прокрутка, снятие «Ещё → Дневник», поля профиля (C) | сделано  | `nutrition-v4-structure.test.js`                                         |
| 6   | Сборка, локалка, ревью, сверка с приёмкой           | в работе |                                                                          |

## Что сделано по шагам

### Шаг 1 — числа

- `calculateDayTotals` читал `pIndex` как плоский объект, хотя это
  `{ byId: Map, … }`: дневные ГИ и вредность годами были нулевыми. Резолвер
  теперь один — модельный, тот же что у `mealTotals`.
- ГИ дня взвешен по углеводам (как в модели волн), вредность — по граммам.
- Порог вредности — константа `HARM_THRESHOLD = 5` в `heys_day_calculations.js`.
- Советы переведены на шкалу продукта 0–10 (см. решение 4).
- Дефолт `giPct` 50 → 55, как при онбординге.
- Нормы экрана считаются от `displayOptimum` (решение 3).
- `removeWater` закрыт тем же гейтом подписки, что и `addWater`.

### Шаг 2 — палитры

- Новые роли `--v4-warn-text`, `--v4-tint`, `--v4-past`.
- Капсула даты, плашка куратора и FAB еды переведены на роли; синие наборы
  покрыты. Каждая намеренная перекраска помечена `v4-intentional`.
- Розовая рамка карточек дневника в тёмных наборах (`rgba(244,63,94,.42)`)
  заменена линией набора.

### Шаги 3–5 — каркас, лист правки, настройки

- `NutritionTabV4` переписан: порядок блоков по контракту, ярусов нет, итоги —
  пять строк с полосами, «Качество еды» — вредность + ГИ, семь чип-зависимых
  блоков, ряд чипов внизу.
- Строка приёма нажимается целиком и открывает лист правки приёма; лист собран
  из существующих обработчиков дневника, своих записей в день не делает.
- Удаление приёма и продукта — без подтверждающего вопроса, тост 5 с.
- Название приёма фиксируется только явным касанием чипа типа
  (`mealTypePinned`).
- Раздел «Ещё → Дневник» и пять его переключателей сняты.
- Положение прокрутки вкладки сохраняется при возврате и сбрасывается сменой
  дня.

## Риски и открытые пункты

1. **Советы дня считаются от базовых норм**, а экран — от норм с рефидом и
   долгом. Контракт «бюджет дня» говорит про экран (герой и строка «норма»), но
   в рефид-день текст совета может опираться на другое число. Свести можно
   только переносом порядка хуков в `heys_day_tab_impl_v1.js` — отдельная
   задача.
2. **«Энергия» — это самочувствие** (решение 8). Если нужна отдельная метрика,
   её надо завести в данных.
3. **Строка «Советы» в листе правки не реализована** — движка советов по приёму
   в коде нет, контракт помечает строку как отдельную задачу.
4. **Внутренности карточки воды** (компактный и полный вид по состоянию кнопки
   воды) правятся своим канвасом `water-add.v4.dc.html` — вне scope этой задачи.
5. **Классическая палитра**: у неё `--v4-surface` совпадает с `--v4-bg`
   (#ffffff), поэтому капсула даты после перевода на роль сливается с фоном —
   ровно как все остальные v4-поверхности в этом наборе. Контракт покрывает
   четыре набора sand/blue; классика остаётся как есть.

## Вопросы дизайнеру

Двенадцать записей журнала разобраны дизайном 21.08 и уехали в
[`UI_V4_FINDINGS_HISTORY.md`](../ui/UI_V4_FINDINGS_HISTORY.md) — открытых
вопросов по вкладке не осталось. Журнал
[`UI_V4_FINDINGS.md`](../ui/UI_V4_FINDINGS.md) владеет очередью вопросов,
протокол — тем, что сделано в коде.

## Сверка с приёмочным листом

Вердикт по каждой из 102 строк живёт данными в
[`docs/ui/ui-v4-contract-verdicts.json`](../ui/ui-v4-contract-verdicts.json)
вместе с отпечатком значения строки:
`node scripts/ui-v4-check-contract-drift.mjs --list`. Приёмочный лист
`ACCEPTANCE-nutrition-tab.md` руками не правится.

**После двух пакетов канваса 21.08: 87 `=` · 1 `≠` · 13 `—` · 1 `?` из 102.**
Дизайн разобрал все двенадцать записей журнала: восемь закрыл правкой контракта
или кадров, шесть строк пометил «ОТДЕЛЬНОЙ ЗАДАЧЕЙ», строку `ночь до 03:00` снял
с вкладки — правило живёт только в `date-remainders.v4.dc.html`, у капсулы свой
файл и своя приёмка. Код догнал: карточка «Голод и самочувствие», обводка «+
ещё» 2 px тоном акцента, отступ карточек качества 10 px.

Не закрыто:

| Строка                   | Вердикт | Почему                                                                                                                                                                |
| ------------------------ | ------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `запись на прошлый день` | ≠       | Вопрос до шторки с двумя выходами; лист канваса с выбором дня и «Отменой» не перерисован                                                                              |
| 13 строк                 | —       | Шесть помечены «ОТДЕЛЬНОЙ ЗАДАЧЕЙ» самим контрактом (советы приёма, лист выбора цели, замена продукта, оценка приёма); остальные — чужие канвасы: вода и капсула даты |

### Бар отмены (21.08, вечер) — переделан по сквозному канвасу

Первая версия свела бар с кадром «Питание · отмена удаления»: полоса 3 px,
радиус 16, текстовое «Отменить». Она прожила несколько часов. Разбор шести
вопросов журнала закончился тем, что дизайн завёл **сквозной канвас**
`undo-bar.v4.dc.html` — 22 строки и три кадра, — а прежние описания отмены в
`nutrition-tab`, `date-remainders` и `tips` отменил и заменил ссылкой.

Что стало вместо полосы: плашка радиусом 22 над навигацией с зазором 12 px (и
той же врезкой по бокам), слева кольцо-таймер 30 px с цифрой, справа пилюля
«Отменить» высотой 34. Дуга и цифра убывают — показывают остаток окна. 5 с на
все двенадцать точек вызова: свои длительности сняты у статистики (было 7 с),
голода и самочувствия (6 с), задач (4,6 с), приёмов и тренировок (5 с явно).

**Полоса проиграла по нашему же аргументу.** Она была выбором дизайна в
«Питании», но возражение из контракта капсулы даты сильнее: полоса вдоль нижней
кромки читается как прогресс загрузки — «идёт работа» вместо «у вас есть время».

**Пачка вместо необратимости.** Аудит вызывающих показал, что посылка «два
удаления подряд почти не происходит» неверна: чистка приёма от нескольких
продуктов и разбор списка задач идут свайпом и без подтверждения. Теперь
удаления одного вида внутри живого окна собираются в один бар — «Удалено 3
продукта», таймер перезапускается, «Отменить» возвращает все в обратном порядке
(иначе индексы соседей разъезжаются). Пачки заведены у продуктов приёма, приёмов
и задач. Разные виды не собираются.

Отступления, названные вслух:

| Что                     | Как в контракте             | Как в коде                                    | Почему                                                                                                                                                    |
| ----------------------- | --------------------------- | --------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Обводка плашки          | `rgba(--ink, .12)`          | `var(--v4-line)`                              | В тёмных наборах роль и есть .12; в светлых .08, но там, как говорит сама строка, читается тень                                                           |
| Строка удаления проекта | «что именно исчезло»        | «Проект «X» удалён, задачи — в «Без проекта»» | Подписи в баре больше нет, а последствие назвать надо: иначе человек узнаёт о переезде задач после истечения окна                                         |
| «тостов в v4 нет»       | компонент снимается целиком | снят только зелёный «Действие отменено»       | 503 вызова, в основном ошибки. Молча проглотить ошибку хуже: каждому месту нужен показ на месте действия. Вердикт строки — `≠`, заведена отдельная задача |

Попутно нашлась вторая отмена, о которой контракт не знает: копирование и
перенос в новый приём (`day/_meals.js:6740`) показывают свою кнопку «Отменить»
внутри тоста. Уйдёт вместе со снятием тостов.

Проверено симуляцией:
[`undo-bar-v4-contract.test.js`](../../apps/web/__tests__/undo-bar-v4-contract.test.js)
— 23 кейса: окно ровно 5 с без невидимого запаса, порядок «кольцо → текст →
пилюля», убывание дуги и цифры, пачка со склонением числа и возвратом всех в
обратном порядке, перезапуск таймера пачки, разные виды в пачку не идут,
отсутствие подтверждающего тоста, отступ над навигацией и его пересчёт на живом
баре. Плюс
[`app-header-tab-switch.test.js`](../../apps/web/__tests__/app-header-tab-switch.test.js)
— переключение вкладки не трогает висящую отмену.

Геометрия отдельным тестом не закреплена намеренно: кадры этого канваса рисуют
бар инлайновыми стилями, а не классами, — сверка по парам «класс кадра → правило
CSS» тут ничего не даёт (правило «Метод выбирается по канвасу»). Заводить
привязку по метке кадра стоит, если геометрия начнёт разъезжаться.

## Сверка геометрии с кадрами

Прогон по кадрам `data-demo="stop"` на 375 px: 68 пар «блок кадра → правило
продуктового CSS», около двадцати свойств на пару. Сверка закреплена тестом
[`nutrition-v4-canvas-geometry.test.js`](../../apps/web/__tests__/nutrition-v4-canvas-geometry.test.js)
— он читает сам канвас, поэтому расхождение всплывёт при следующей правке любой
из сторон, а не через полгода на глаз.

Найдено и починено:

| Что                                     | Было                                         | Кадр                                                                                  |
| --------------------------------------- | -------------------------------------------- | ------------------------------------------------------------------------------------- |
| Пустой список дневника                  | вес наследовался                             | 500                                                                                   |
| Полоса итогов без модификатора          | без заливки                                  | шалфей дефолтом                                                                       |
| Единица в мини-карточке «Голод/Энергия» | без line-height                              | 10px/1                                                                                |
| Слово оценки дня                        | line-height 1.15                             | 20px/1                                                                                |
| Закрашенный сегмент шкалы риска         | `--v4-ok-fill`                               | тёмный шалфей `--v4-ok-text`                                                          |
| Лист правки приёма                      | без тени                                     | `0 18px 50px`                                                                         |
| «Удалить» в свайпе по продукту          | акцентная плашка                             | красная подложка 14 %                                                                 |
| Плашка «только чтение»                  | ⏰ + янтарный градиент + «оформить подписку» | кадр «Питание · только чтение»: тон набора, без эмодзи и стрелки, «напишите куратору» |

Осознанные отступления от кадра (в тесте перечислены поимённо, каждое — со
строкой контракта, которая старше кадра):

1. **Цвет строки окна приёмов.** В кадре шалфей стоит дефолтом, нейтральный —
   модификатором. У нас наоборот: контракт «цвет состояния» требует шалфей
   только когда окно правда открыто.
2. **Цвет подписи блока** — та же причина: «сегодня» это метка, не оценка.
3. **Отступ карточек «Качества еды»** — 8 px по инлайн-стилю самого кадра, а не
   10 px по правилу.
4. **Кнопка в строке приёма** — базовый вид обводка, заливка приходит
   модификатором пустого приёма: контракт «кнопка в строке» разводит «+ продукт»
   и «+ ещё», кадр рисует оба залитыми.
