# Силовой конструктор: редизайн «карточка-сводка + полноэкранный конструктор»

Статус на 2026-08-09 (ночь): **макет принят, версия 5.1, 26 экранов, блокеров
нет.** Шесть кругов ревью закрыты, все решения владельца зафиксированы ниже.
Часть работ уже в проде (см. «С чего начинать» → «Что уже в проде»), интерфейс
конструктора не начат.

Это протокол решений и контракт на разработку. При расхождении с макетом прав
он. Начинать чтение — с раздела «С чего начинать», он написан так, чтобы войти в
задачу без переписки.

Смежный источник правды по нагрузке —
[`TRAINING_LOAD_MODEL_PROMPT.md`](TRAINING_LOAD_MODEL_PROMPT.md); он владеет
моделью Банистера и калибровкой шкалы, этот документ — интерфейсом и схемой
данных конструктора.

## Что произошло

Дизайнер передал хэндоф (`Downloads/handoff/`, 9 августа): конструктор силовой
вынесен из карточки активности в полноэкранный режим, 15 экранов в двух темах,
10 решений, 7 спорных состояний. Ревью сверило каждое инженерное обещание макета
с фактическим кодом — ядром, конструктором дня, механизмом слияния и
MCP-коннектором. Пять обещаний разошлись с кодом, который лёг 8 августа, за день
до сдачи хэндофа.

Документ для дизайнера — `Downloads/handoff/Ревью и решения — 2026-08-09.md`
(вне репозитория, у владельца).

## Версия 2 макета — проверена 2026-08-09 вечером

`Downloads/handoff 2/`. Все пять расхождений закрыты не декларативно, а в самой
сборке (проверено сканом разметки и данных, не по README):

- общей шкалы нагрузки нет, `trace` переписан на динамику, вклад сессии показан
  тоннажем «в силовом ряду, своей единицей»;
- RPE вернулся на упражнение, отдых подписан как его выход, ручной режим на
  месте;
- экран 14 — метка авторства плюс объяснение, почему не диалог; «куратор не
  правит открытую сессию» вынесено и на экран 09;
- градиент главной кнопки затемнён до `#c2410c → #9a3412`; приглушение цифр
  прозрачностью убрано (остались только `.85–.9` на вторичных подписях и `.5` на
  декоративном маркере);
- мишеней ниже 36 px не осталось; фикстуры сведены — экран 07 показывает те же 4
  рабочих подхода жима лёжа, что список (тоннаж 2 980 кг сходится: 2 620 кг
  рабочих плюс 360 кг дропа), экран 12 явно взят из жима гантелей с экранов 04 и
  05;
- скриншоты пересобраны полными рядами (6302 px).

Осталось три мелочи, не блокирующие разработку:

1. **Подпись «Отдых 1:30 — выведен из RPE 7» неверна.** По коду
   [`restSecForRpe`](../../apps/web/heys_day_trainings_v1.js) RPE ≥ 7 даёт 120 с
   (2:00); 90 с (1:30) — это значение по умолчанию при незаполненном RPE. Либо
   исправить подпись на 2:00, либо менять формулу в коде — но тогда это правка
   поведения, а не макета. Заодно стоит решить: шкала грубая (180/120/60 плюс
   дефолт 90), промежуточных значений в ней нет.
2. **Кнопка переключения отдыха «⏱ 1:30» — 42 px** при новом правиле «44 или
   36, промежуточных нет». Единственное исключение в сборке, и правило прямо
   называет таймер.
3. **Кружок номера текущего подхода остался `#ea580c` с белым текстом** — 3,56 :
   1 при норме 4,5. Главную кнопку затемнили, этот элемент пропустили.

## Найденные расхождения макета и кода (проверено чтением 2026-08-09)

| Обещание макета                                                        | Что в коде                                                                                                                                                           | Ссылка                                                                                                                                                              |
| ---------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Экран 08: «вклад сессии +42, тоннаж приведён к общей шкале»            | Единого коэффициента «кг = MET-мин» нет и он отклонён; кардио и силовая — раздельные ряды, способ слияния не выбран                                                  | [`heys_kernel_load_v1.js:14-21`](../../apps/web/_kernel/heys_kernel_load_v1.js), `sessionLoad` для strength возвращает 0 — там же :66                               |
| RPE отсутствует на всех 15 экранах                                     | Поле `rpe` (0–10) есть у упражнения, контрол есть в UI, отдых считается из него (180/120/60/90) с ручным переключением                                               | [`heys_day_trainings_v1.js:169`](../../apps/web/heys_day_trainings_v1.js), :518 `restSecForRpe`, :1730 контрол, :1758 «авто по RPE»                                 |
| Экран 14: две версии одного подхода с выбором                          | Конфликт решается автоматически, целиком по тренировке, по «богатству» записи; проигравшая отбрасывается (переносятся только mood/wellbeing/stress)                  | [`heys_sync_merge_v1.js:937-950`](../../apps/web/heys_sync_merge_v1.js), :999-1017                                                                                  |
| «Приоритет у записи из зала»                                           | MCP-коннектор намеренно повышает себе richness (`zoneMinutes` даёт +100), чтобы выигрывать у приложения                                                              | `yandex-cloud-functions/heys-mcp/lib/day.js:919-921`                                                                                                                |
| Разминка вне тоннажа, дроп внутри подхода, у записи есть автор и время | Подход — это `{weightKg, reps, done}`; полей типа, автора и времени нет (`isWarmup\|setType\|approachType` — ноль совпадений)                                        | [`heys_day_trainings_v1.js:161-168`](../../apps/web/heys_day_trainings_v1.js), [`heys_kernel_strength_v1.js:46`](../../apps/web/_kernel/heys_kernel_strength_v1.js) |
| Минуты по зонам считаются сами, «веса зон из hr_zones»                 | `workoutLog.zoneMinutes` пишет и коннектор (`[0, minutes, 0, 0]`), поле кормит калории через TDEE; MET-веса лежат отдельно от пульсовых границ (`DEFAULT_ZONE_METS`) | `heys-mcp/lib/day.js:919-931`, [`heys_kernel_load_v1.js:54`](../../apps/web/_kernel/heys_kernel_load_v1.js)                                                         |

Дополнительно в самом макете: инвариант «44 px» нарушен (24 px у крестика,
удаляющего тренировку; 30 у клеток раунда и ▾ таймера; 40 у ✓; 42 у кнопок
добавления), инвариант «только `var()`» нарушен 55 раз и невыполним как
сформулирован, число подходов не сходится между экранами 04/07/12, контраст
белого на главной кнопке 2,8 : 1 при норме 4,5.

## Принятые решения (решение владельца, 2026-08-09)

| №   | Вопрос                                   | Решение                                                                                                               | Следствие для кода                                                                                                                                |
| --- | ---------------------------------------- | --------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | Сведение силовой и кардио-нагрузки       | Не сводить. Два раздельных ряда; общей шкалы в UI нет. До калибровки клиенту показывать динамику, не абсолютные числа | Закрывает открытый пункт «шкала не откалибрована» в части UI: клиентский экран нагрузки не показывает сырые CTL/ATL/TSB                           |
| 2   | RPE                                      | Вернуть контрол на упражнение; отдых остаётся авто-по-RPE с ручным режимом                                            | Существующее поведение сохраняется, макет приводится к коду                                                                                       |
| 3   | Конфликт «куратор поверх зала»           | Куратор не правит идущую тренировку. Правка после завершения — с автором и временем. Экран двух версий откладывается  | Нужен признак «сессия идёт» на стороне записи коннектора + метка авторства на подходе. Механизм слияния в первой очереди не переписывается        |
| 4   | Типы подхода (разминка / рабочий / дроп) | Делаем                                                                                                                | Новое поле у подхода; `trainingTonnage` учитывает тип (разминка вне тоннажа, дроп вкладывается). Миграция старых данных не нужна: пусто = рабочий |
| 5   | Авто-минуты по зонам у силовой           | Считаем, но до включения сверяем расчётные калории с текущими на живых днях                                           | Поле уже кормит калории и его пишет коннектор; тихая смена формулы сдвинет калории у всех                                                         |
| 6   | Оффлайн                                  | Проектируем сейчас: очередь отправки, повтор, состояние «не сохранилось»                                              | Не откладывается в «всплывёт в разработке» — зал без сети это основной сценарий                                                                   |
| 7   | Правило 44 px                            | 44 для действий с подходом, связкой и таймером; 36 для служебных кнопок шапки                                         | Инвариант в хэндофе переписывается                                                                                                                |
| 8   | Контраст главной кнопки                  | Градиент затемняется до `#c2410c → #9a3412` (5,2 и 7,3 против нормы 4,5)                                              | Приглушение строк — цветом текста, не `opacity`                                                                                                   |
| 9   | Недостающие экраны                       | Отдаём сейчас; шторка ⋯ и история упражнения дособираются параллельно; «Все тренировки» убрать                        | Кнопка в пустоту не уходит в разработку                                                                                                           |
| 10  | Дискомфорт                               | Отметка ведёт к действию: снизить вес или пропустить упражнение                                                       | По `Product UI invariants` безопасность не заканчивается записью в журнал                                                                         |
| 11  | Связка с разным числом подходов          | Запрещена: подходов поровну, «+ Подход» добавляет раунд целиком                                                       | Восьмое спорное состояние, в хэндофе его не было                                                                                                  |
| 12  | Название сессии                          | Авто из групп упражнений, можно перебить руками                                                                       | Те же группы идут в движок, подпись не должна врать                                                                                               |
| 13  | Расчётный максимум                       | Эпли (95 кг из 75×8)                                                                                                  | Формула фиксируется, иначе разработка возьмёт Бржицки и число разъедется                                                                          |
| 14  | Где хранится решение                     | Этот документ                                                                                                         | —                                                                                                                                                 |

## Сверка калорий по решению 5 — сделана 2026-08-09, на расчётах

Решение 5 требовало сверить расчётные калории с текущими до включения авто-зон.
Сверка проведена по коду и на расчётах; на выгрузке живых дней — **нет**, это
осталось (см. «Открытое»). Все числа ниже проверены чтением кода.

**Как устроено сейчас.** Калории тренировки считает только `training.z`
([`heys_tdee_v1.js:126-132`](../../apps/web/heys_tdee_v1.js));
`workoutLog.zoneMinutes` — редактируемая копия конструктора, которая зеркалится
в `z` при каждой правке (`applyWorkoutLogToTraining`,
[`heys_day_trainings_v1.js:2823-2831`](../../apps/web/heys_day_trainings_v1.js)).
Минуты по зонам человек вводит руками; автоматики нет ни в одном пути.

Для назначенного, но ещё не начатого плана с 2026-09-01 действует отдельный
fail-closed контракт: состав находится только в `planSnapshot`, а
`workoutLog.zoneMinutes` и `training.z` равны `[0, 0, 0, 0]`. Это draft, не факт
и не скрытая минута нагрузки. Live-log появляется только после явного выбора
пользователя, подтверждённого владельцем дня; правка snapshot разрешена лишь до
`started`, после старта он служит базой сравнения с живым журналом.

**Найдено попутно: расхождение в 50% существует уже сегодня.** Конструктор
кладёт всю длительность в зону 2, а быстрый пресет мастера тренировки делит
пополам между зонами 2 и 3
([`heys_training_step_v1.js:958-970`](../../apps/web/heys_training_step_v1.js)).
Одна и та же 60-минутная силовая даёт 147 ккал через конструктор и 221 ккал
через мастер (клиент 70 кг, MET `[2,3,5,8]`). Это не связано с авто-зонами и не
создано ими — но чинить это надо вместе, иначе сверка «до и после» будет мерить
две разные точки отсчёта.

**Прямой сдвиг оптимума мал.** На трёх правдоподобных распределениях зон
(консервативное 60/30/10, среднее 40/35/20/5, плотное 25/35/30/10) суточный
оптимум меняется от −1,4% до +3,0%. Порог «больше 10%» прямым эффектом не
достигается: весь вклад силовой сегодня — 147–284 ккал при оптимуме около 2400.

**Опасны три вещи, и ни одна не про величину сдвига.**

1. **Знак сдвига зависит от того, сохранены ли у клиента `heys_hr_zones`.** У
   кого сохранены (дефолт формы профиля — MET `[2,3,5,8]`,
   [`heys_user_v12.js:535-543`](../../apps/web/heys_user_v12.js)) авто-зоны
   калории поднимут: +11…+47% на тренировку. У кого не сохранены и работает
   fallback кода `[2.5,6,8,10]` — те же распределения дадут −16…−38%. Один и тот
   же код разведёт разных клиентов в противоположные стороны.
2. **Порог NDTE — ступенька, а не наклон.** Буст следующего дня включается по
   гросс-калориям при жёстко зашитом весе 70 кг
   ([`heys_iw_constants.js:2900`](../../apps/web/heys_iw_constants.js)), порог
   не зависит от веса клиента. Силовые на 80–100 минут стоят прямо на границе, и
   авто-зоны переводят их через неё в обе стороны. Цена переключения — 46–175
   ккал на следующий день, то есть больше всего прямого эффекта.
3. **Разброс между распределениями шире, чем сам факт включения.** Между
   консервативным и плотным вариантом на одной тренировке — 72–176 ккал. Значит
   выбирать надо не «включать ли авто», а какие доли зашить.

**Что из этого следует для порядка работ.** Сначала свести к одной точке отсчёта
(конструктор и мастер должны давать одинаковые зоны), затем выбрать
распределение, и только потом включать авто-расчёт. Выбор распределения —
решение владельца, оно не выводится из кода.

## Что из этого меняет контракты

Три решения затрагивают данные, а не только интерфейс, и должны быть
спроектированы до кода интерфейса:

1. **Тип подхода** (решение 4) — новое поле в
   `workoutLog.exercises[].approaches[]` и правка `TK.strength.trainingTonnage`.
   Ядро сейчас типов не знает и считает все подходы одинаково.
2. **Авторство и время у подхода** (решение 3) — сейчас метка авторства
   существует только для полей утреннего чек-ина (`CHECKIN_AUTHORED_FIELDS` в
   `heys-mcp/lib/day.js`), тренировки в неё не входят.
3. **Владелец `zoneMinutes`** (решение 5) — поле пишут и приложение, и
   коннектор, и оно кормит калорийный расчёт. Нужен явный контракт, кто считает
   и что происходит при расхождении.

## Связки, дроп-сеты, свой вес и группы — решения владельца 2026-08-09 (вечер)

Пять вопросов по схеме данных закрыты. Решения приняты **в продуктовом объёме**,
а не как первый заход: владелец отдельно оговорил, что урезанных версий здесь не
делаем.

### Связки: суперсет, трисет, круговая

**Раунд не хранится, а выводится из позиции.** Раунд номер k — это k-й рабочий
подход у каждого участника. Новых полей ноль, миграции ноль. Модель держится на
решении 11 (подходов поровну): без него она невозможна, с ним она и есть это
решение, записанное в данных. Апгрейд до явного индекса раунда остаётся возможен
позже без потери данных — обратный переход невозможен, поэтому начинаем отсюда.

Отвергнуто: индекс раунда на подходе (второй экземпляр того же факта — при
удалении подхода в середине данные молча противоречат сами себе, это инвариант
№7 из `CLAUDE.md`); отдельная сущность связки со своим списком раундов (у
каждого подхода появляется второй путь чтения, и любой пропущенный потребитель
тихо теряет тоннаж).

Правила:

- **Число участников не ограничено.** В коде жёсткой двойки нет нигде — только
  минимум два (`heys_day_trainings_v1.js:801`, `heys-mcp/lib/day.js:914`).
  Трисет и круговая — та же связка, отличается числом колонок, отдельного режима
  не заводим.
- **Участники идут подряд** в списке упражнений. Инвариант держится у писателей:
  связка перетаскивается целиком одним блоком, вставка внутрь чужой связки
  прилипает к её границе, коннектор валидирует смежность там же, где сейчас
  валидирует размер. На чтении — только оборонительный фолбэк: переставлять
  упражнения при открытии дня значит писать на чтении.
- **Разминка входит в связку, но не в раунды.** Раунд считается по подходам с
  типом ≠ разминка. Разминочная строка общая для связки, в тоннаж не идёт.
  Следствие: подписи «N подходов» переписываются на «N раундов + M разминочных».
- **Отдых связки — максимум из значений участников**, пишется во всех сразу. Не
  «первый участник»: первым можно стать перетаскиванием, и отдых поедет молча.
  Внутри раунда таймера нет — это и есть смысл связки; он стартует один раз,
  после закрытия всех клеток раунда.
- **Перестановка участников внутри связки нужна** — отдельным контролом
  («Местами»), данные подходов едут за упражнением. Перетаскиванием связку
  разорвать нельзя, для этого явное «Разъединить».
- **Старые связки с неравным числом подходов не переписываем.** Показываем
  плоскими списками без раундов, тоннаж и счётчик считаются как обычно. Раунды
  появятся, если человек сам выровняет число подходов. Дописывать подходы задним
  числом запрещено: это изменение истории.
- **Участник, добавленный по ходу**, получает прочерк в уже прошедших раундах.
  Уточнено 2026-08-09 после финального ревью: прочерк — это **реальный пустой
  подход**, а не отсутствие подхода. У добавленного участника подходов столько
  же, сколько раундов; в прошедших они существуют, но не заполнены. Иначе
  позиционная модель ломается — единственный заполненный подход оказался бы
  первым раундом вместо третьего. Закрыть такой подход нельзя по уже
  действующему правилу «повторы обязательны для галочки», отдельного признака не
  нужно: «не участвовал» и «не сделал» — одно и то же состояние, а различает их
  контекст (у остальных раунд закрыт). Прочерк — не ноль и не пропуск: клетка не
  закрывается, в счётчик и в тоннаж не идёт. Отдельного поля «не участвовал» не
  заводим.
- **Дроп внутри связки запрещён** (решение 11). При этой модели запрет
  становится проверяемым: дроп — лишний подход, он нарушает равенство.

### Дроп-сеты

**Сброс — ступень внутри подхода, а не отдельный подход.** Иначе счётчик дня
поедет у каждого, кто сделал дроп, и число перестанет сходиться между экранами.

- В счётчике дня подход со сбросами — **один** подход, своего номера у ступени
  нет.
- **В тоннаж идут все ступени**: работа сделана вся. Пример из макета: подход
  75×8 плюс сброс 60×6 даёт 960 кг (600 + 360) — проверено по версии 5.1 макета.
  Прежний пример на три ступени (1265 кг) устарел: в 5.1 дроп сведён к одному
  сбросу, потому что два разных числа для одного подхода расходились между
  экранами и ломали тоннаж дня.
- **Отметка у каждой ступени своя**, а подход считается закрытым, когда закрыты
  все его ступени. Это решение дизайнера, принято: иначе не видно, докуда
  человек дошёл, если он сбросил вес и ещё работает.
- **Вес каждой следующей ступени только ниже предыдущей**, подставляется −20% с
  возможностью поправить.
- **Таймер между ступенями не запускается** — в этом весь смысл приёма. Стартует
  после последней.
- **Максимум три ступени.** Ограничение экранное, а не модельное: четыре строки
  внутри строки подхода — предел читаемости в зале.
- **Рекорд считается по основной ступени, а не по сумме.** Иначе любой дроп-сет
  автоматически станет личным рекордом, хотя человек не поднял больше — он
  просто дольше не отдыхал.
- Разминочный подход со сбросами целиком вне тоннажа: тип один на все ступени.

### Единица измерения и свой вес

**Единица — первое обязательное поле упражнения**, от неё зависит всё остальное:
килограммы × повторы, свой вес, время, метры.

- **Коэффициент спрашивается только при «своём весе»** и не числом, а вопросом
  «на что похоже движение» с выбором из упражнений, где коэффициент известен.
  Коэффициент — физический факт про движение, а не пользовательская настройка;
  человек не может ответить «какая доля массы тела приходится на это движение»,
  а неверный ответ молча испортит тоннаж навсегда.
- **Формула:** `(масса тела × коэффициент + довес) × повторы`. Довес — свойство
  **подхода**, а не упражнения: сегодня человек подтягивается со своим весом,
  завтра с блином на поясе.
- **Коэффициенты живут в справочнике как данные**, пополняются без выката.
  Ориентиры из макета: подтягивания 1,00, брусья 0,95, отжимания 0,64,
  приседания без веса 0,55, обратные отжимания 0,40.
- **Дефолт не выдумываем.** Если коэффициент неизвестен — упражнение создаётся
  явной кнопкой «Создать · без тоннажа», и в итогах стоит строка «не посчитали».
  Одно правило с уже принятым случаем «в профиле нет массы тела».
- **Время и метры в тоннаж не идут.** Перемножать килограммы на метры физически
  бессмысленно. Объём по ним копится отдельными величинами — секунды под
  нагрузкой и дистанция; смешивать три единицы в одно число нельзя. Вес при этом
  записывается: человеку нужен прогресс, даже если тоннажа нет.

### Группы мышц

**Группа обязательна, выбор только из фиксированного списка** — свои названия не
заводятся, иначе фильтр каталога и объём по группам рассыпаются.

**Групп у упражнения может быть несколько: одна основная и любое число
дополнительных.** Продуктовое решение: экран 04 подписывает жим гантелей как
«Плечи · трицепс», и это правда — одной группой базовое движение не описать.

- Объём по группам считается **с долями**: основная получает полный вес
  упражнения, дополнительные — половину. Это стандартная практика учёта
  синергистов; конкретный коэффициент — параметр справочника, а не константа в
  коде.
- **«Всё тело» из списка групп убирается.** Это не группа мышц, а следствие
  множественного выбора: комплексное движение помечается несколькими группами.
- Название сессии (решение 12) собирается из основных групп упражнений.

## С чего начинать — точка входа для новой сессии

Макет принят после шести кругов ревью (версия 5.1, 26 экранов). Блокеров нет.
Этот раздел написан так, чтобы начать можно было, не читая переписку.

### Где что лежит

- **Механика и решения** — этот документ. Он источник правды по интерфейсу и
  схеме данных конструктора; при расхождении с макетом прав он.
- **Программа куратора** —
  [`CURATOR_TRAINING_PROGRAM_PROTOCOL_2026-08-09.md`](CURATOR_TRAINING_PROGRAM_PROTOCOL_2026-08-09.md),
  там же порядок слоёв и отметка о выполненном.
- **Модель нагрузки** —
  [`TRAINING_LOAD_MODEL_PROMPT.md`](TRAINING_LOAD_MODEL_PROMPT.md).
- **Сам макет** — `Downloads/handoff 8/Сборка экранов.dc.html` у владельца, вне
  репозитория. Механика из него перенесена сюда целиком, но вёрстка, состояния и
  микрокопия — только там. Если папки нет, запросить артефакт до начала
  UI-работ. Файл турнов из того же пакета в работу **не брать**: он рендерит
  отменённые решения (общая шкала нагрузки, «Всё тело» в группах, старый дроп на
  три ступени) без пометок.

### Что уже в проде, переделывать не нужно

Коммит `711b303ad`, деплой зелёный (`9c4641e36` — сопутствующий фикс сборки
лендинга, к тренировкам отношения не имеет):

- назначенная куратором тренировка не считается фактом нигде — ни в калориях, ни
  в тоннаже, ни в нагрузке, ни в счётчиках. **В проде предикат называется
  `TK.load.isPlannedTraining` и ловит только статус `assigned`;**
- мастер тренировки больше не уничтожает журнал упражнений при переключении в
  «пульсовые зоны» и не теряет `id`, `source`, `intensity`;
- NDTE ожил на клиенте и перестал завышать объём вдвое; пороги согласованы на
  300;
- быстрый пресет длительности раскладывает время 40 / 35 / 20 / 5.

### Что закоммичено, но ещё не запушено

Исправлено 2026-08-09 после сверки соседней сессией: раньше здесь было написано,
что расширение на `skipped` уже в проде. Это было неверно — на момент той записи
оно лежало незакоммиченным.

Изменения в `apps/web/_kernel/heys_kernel_load_v1.js`,
`heys_kernel_strength_v1.js`, `heys_tdee_v1.js`, `heys_iw_constants.js`,
`yandex-cloud-functions/heys-mcp/lib/` и зеркалах:

- предикат переименован `isPlannedTraining` → **`isNotPerformedTraining`** и
  расширен на статус `skipped` (решение владельца: пропущенная тренировка
  остаётся в дне как история, но нагрузки не создаёт). Старое имя врало бы:
  «запланированная» и «пропущенная» — разные вещи;
- локальные запасные проверки в модулях приведены к ядру (иначе отстали бы на
  один статус и разошлись молча);
- тесты: три новых кейса в `apps/web/__tests__/kernel-training-plan.test.js`,
  проверены мутацией — при сужении предиката обратно падают два.

Прогоны зелёные: 12 в ядре, 991 в коннекторе. **До push этого нет в проде; прод
пока живёт со старым `isPlannedTraining`.**

### Порядок работ

Порядок продиктован зависимостями, а не важностью.

**1. Справочник упражнений.** Группы мышц (основная + синергисты, фиксированный
список без «Всё тело»), единица измерения, коэффициент своего веса. Это
фундамент — от единицы зависит и тоннаж, и вид карточки подхода. Существующие
упражнения получают группы разово; у новых поля обязательны.

> **Сделано 2026-08-09 (данные и API; UI справочника — в шаге 5).**
> [`apps/web/heys_exercise_catalog_v1.js`](../../apps/web/heys_exercise_catalog_v1.js),
> проверка —
> [`apps/web/__tests__/exercise-catalog-meta.test.js`](../../apps/web/__tests__/exercise-catalog-meta.test.js)
> (20 тестов, зелёные; `npx vitest run __tests__/exercise-catalog-meta.test.js`
> из `apps/web`).
>
> - 14 групп мышц фиксированным списком, «Всё тело» нет (тест это стережёт); все
>   100 каталожных упражнений размечены основной группой и синергистами.
> - Единицы: `weight_reps`, `bodyweight`, `time`, `distance`. Время и метры
>   помечены `volume: 'seconds' | 'meters'` — в тоннаж не идут.
> - Коэффициенты проставлены там, где они известны (опорные значения макета:
>   1,00 / 0,95 / 0,64 / 0,40 плюс выведенные для тех же движений); в остальных
>   строках `null` — «без тоннажа», дефолт не выдумывается. Для вопроса «на что
>   похоже движение» справочник отдаёт `bodyweightReferences()`.
> - Доля синергистов вынесена параметром `synergistShare` (0,5) и принимается
>   аргументом `groupWeights(meta, share)`, а не зашита у потребителя. Кто ей
>   управляет из UI — по-прежнему не решено (см. «Где остановиться и спросить»).
> - Свои упражнения клиента пишутся client-scoped через `HEYS.utils.lsSet`
>   (`heys_exercise_meta_v1`) и потому уезжают в облако; каталожную запись своя
>   перекрывает. Неполная запись не сохраняется (fail-closed).
>
> **Решение реализации (агент, владельцем не подтверждалось): единица и
> коэффициент копируются в упражнение журнала снимком** — `snapshot(name)`, — а
> справочник даёт только дефолты на момент добавления. Причина: иначе правка
> коэффициента задним числом переписала бы тоннаж уже проведённых тренировок (то
> самое «изменение истории», которое протокол запрещает в разделе про связки), а
> ядро тоннажа стало бы зависеть от клиентского каталога. Шаг 3 должен читать
> снимок упражнения, а не справочник.

**2. Схема подхода.** Тип (разминка / рабочий), довес, ступени дроп-сета. Всё
аддитивно, миграции не нужно: пустое поле читается как «рабочий», отсутствие
ступеней — как обычный подход.

**3. Тоннаж и объём.** `TK.strength.trainingTonnage` учит типы (разминка вне
тоннажа), свой вес по формуле `(масса тела × коэффициент + довес) × повторы` и
ступени дропа. Время и метры копятся отдельными величинами, в тоннаж не входят.
Рекорд считается по основной ступени.

> **Шаги 2 и 3 сделаны 2026-08-09 (данные и расчёт; UI конструктора — шаг 5).**
> Ядро
> [`apps/web/_kernel/heys_kernel_strength_v1.js`](../../apps/web/_kernel/heys_kernel_strength_v1.js)
> (+ побайтовое зеркало heys-mcp), клиентские читатели в
> [`apps/web/heys_day_trainings_v1.js`](../../apps/web/heys_day_trainings_v1.js),
> коннектор — `heys-mcp/lib/day.js` и `lib/tools.js`. Проверка:
> [`kernel-strength-approach-schema.test.js`](../../apps/web/__tests__/kernel-strength-approach-schema.test.js)
> (20 тестов) плюс 4 новых в `heys-mcp/__tests__/day.test.cjs`; прогнаны полный
> набор web (3764 теста) и heys-mcp (995) — падений нет, старые записи считаются
> ровно как раньше.
>
> - Подход: `type` (пусто = рабочий), `extraWeightKg` (довес — свойство
>   подхода), `drops[]` (ступени сброса), `durationSec` / `distanceM` для
>   времени и метров. Writers НЕ пишут `type` у рабочих подходов: пустое поле и
>   есть значение по умолчанию, поэтому миграции нет.
> - Правила ступеней — в ядре одним экземпляром (`validateApproach`): вес каждой
>   следующей ступени строго ниже предыдущей, всего не больше трёх ступеней,
>   сброс от неуказанного веса не принимается, дроп внутри связки запрещён.
>   Коннектор зовёт ту же функцию через зеркало, своего набора условий у него
>   нет.
> - Тоннаж: разминка вне тоннажа (и планового тоже), в объём идут все ступени,
>   рекорд — по основной ступени, подход закрыт только когда закрыты все его
>   ступени. Свой вес считается при `trainingTonnage(t, { bodyWeightKg })`; без
>   массы тела или коэффициента упражнение попадает в `unmeasuredExercises` — ту
>   самую строку «не посчитали». Время и метры вернулись отдельными полями
>   `seconds` / `meters`.
> - Счётчик подходов не поехал: подход со сбросами — один подход.
>
> **Исправлено попутно:** описание `heys_log_strength_workout` учило куратора
> отклонённой модели — «дропсет это просто несколько подходов с убывающим весом,
> отдельного поля не нужно». Через неё счётчик подходов завышался на каждом
> дроп-сете. Инструмент приведён к схеме: `set_type`, `extra_weight_kg`,
> `drops`.
>
> **Не сделано и важно для шага 5:** UI конструктора новые поля пока не пишет и
> не рисует — ступени сброса, тип подхода и довес может завести только куратор
> через коннектор. Клиентские читатели их уже понимают. Функция `heys-mcp` в
> облаке не переразвёрнута: приём новых полей заработает после деплоя.

**4. Связки.** Инварианты смежности и равенства числа рабочих подходов держатся
у писателей: перетаскивание двигает связку целиком, вставка внутрь прилипает к
границе, коннектор валидирует смежность. Раунд выводится из позиции, отдых —
максимум из участников.

> **Сделано 2026-08-09 (модель и писатели; экраны связки — шаг 5).** Ядро
> [`heys_kernel_strength_v1.js`](../../apps/web/_kernel/heys_kernel_strength_v1.js),
> проверка —
> [`kernel-strength-supersets.test.js`](../../apps/web/__tests__/kernel-strength-supersets.test.js)
> (12 тестов) плюс тест смежности в `heys-mcp/__tests__/day.test.cjs`.
>
> - Чтение: `supersetGroups` (участники, смежность, баланс, число раундов и
>   разминочных), `supersetRounds` (раунд k = k-й рабочий подход каждого
>   участника; `null` у несбалансированной связки), `supersetRestSec` (максимум
>   из участников — не «первый участник»). Новых полей ноль.
> - Разминка входит в связку, но не в раунды: раунды считаются по подходам с
>   типом ≠ разминка.
> - Старые связки с неравным числом подходов не переписываются: раундов у них
>   нет, показываются плоскими списками. Дописывать подходы задним числом ядро
>   не умеет намеренно.
> - Писатели: `addSupersetRound` («+ Подход» добавляет раунд целиком),
>   `addSupersetMember` (участник по ходу получает реальные пустые подходы по
>   числу раундов), `swapSupersetMembers` («Местами»), `moveSupersetGroup`
>   (перетаскивание двигает связку целиком), `insertRespectingGroups` (вставка
>   внутрь чужой связки прилипает к ближней границе).
> - Прочерк — реальный пустой подход (`isBlankApproach`): в счётчик подходов и в
>   тоннаж он не идёт, отдельного поля «не участвовал» нет.
> - Перетаскивание в конструкторе переведено на ядро: связку им теперь не
>   разорвать (для этого есть «Разъединить»), а создание связки собирает
>   участников смежно тут же, при записи.
> - Коннектор валидирует смежность там же, где проверял размер связки, — той же
>   функцией ядра через зеркало.

**5. Интерфейс конструктора.** Экраны из сборки. К этому моменту вся арифметика
уже считается правильно, и UI показывает готовые числа, а не считает их сам.

> **В работе с 2026-08-09. Сделано: экраны 04, 05, 07, 13, 23, 24, 26.**
> [`apps/web/strength/heys_strength_builder_ui_v1.js`](../../apps/web/strength/heys_strength_builder_ui_v1.js),
> [`heys_strength_superset_ui_v1.js`](../../apps/web/strength/heys_strength_superset_ui_v1.js),
> стили
> [`750-strength-builder.css`](../../apps/web/styles/modules/750-strength-builder.css),
> проверка —
> [`strength-builder-ui.test.js`](../../apps/web/__tests__/strength-builder-ui.test.js)
> (14 тестов).
>
> - Полноэкранный каркас вынесен в ядро
>   [`_kernel/heys_kernel_fullscreen_v1.js`](../../apps/web/_kernel/heys_kernel_fullscreen_v1.js):
>   у пальцев и мобильности он уже написан по-своему, силовая не стала третьей
>   копией. Их переезд на общий каркас — отдельная задача, в этот редизайн не
>   входит.
> - Модули грузятся лениво через `heys_day_stats_bundle_loader_v1.js`, а не
>   через правку `legacy-bundle-config.mjs`: тот файл форсит полную пересборку
>   бандлов.
> - Вход — кнопка «Открыть конструктор» в карточке дня. Инлайновый список
>   подходов пока остаётся рабочим путём: strangler-переход, а не разовая
>   замена.
>
> **Расхождение с макетом, принято владельцем 2026-08-09:** групп мышц 14, а не
> 11 как в сборке. «Ноги» одной группой не показывают перекос квадрицепс/задняя
> цепь, а «Шея» не используется ни одним упражнением каталога. По правилу «при
> расхождении с макетом прав протокол» список из 14 — действующий.
>
> **Добавлено 2026-08-09 (вторая итерация, сверка со скриншотами хэндофа):**
> экран 04 приведён к прототипу (название сессии из групп, человеческая дата,
> таймер тренировки в шапке вместо метрик, номер подхода кружком в строке,
> зелёные закрытые и оранжевая текущая строка, «Вручную» у отдыха, «+» слева);
> экран 01 — карточка дня со сводкой, прогрессом и одним действием; экраны 03
> (каталог с поиском и фильтром по 14 группам) и 25 (новое упражнение: единица
> первым обязательным полем, группы с основной и синергистами, коэффициент по
> вопросу «на что похоже», кнопка «Создать · без тоннажа»).
>
> Инлайновый конструктор на карточке дня скрыт, когда полноэкранный доступен:
> прототип показывает там только сводку. Фолбэк остался — если модуль не
> загрузился, старый блок рисуется как раньше.
>
> **Ещё не сделано:** экраны 02 (пустая тренировка), 03 (каталог), 06 (режим
> порядка), 08 (нагрузка), 10 (финал), 11 (не потерять сессию), 12
> (перенумерация), 14 (правка куратора), 20 (шторка ⋯), 21 (история упражнения),
> 22 (создание связки), 25 (новое упражнение). Экраны 16–19 — программа
> куратора, шаг 6.

> **Итог 2026-08-09 (ночь): шаг 5 закрыт почти целиком.** Сделаны все
> перечисленные выше экраны, кроме двух явно заблокированных (см. ниже), плюс
> связки (22, 23, 26 — суперсет/трисет/круговая, раунды из позиции), режим
> порядка (06, блоками — связку не разорвать), финал с расчётным максимумом по
> Эпли (10), история упражнения (21), шторка ⋯ (20, четыре рабочих входа из семи
> в макете), диалог незакрытых подходов и пуш об окончании отдыха с уже выданным
> разрешением (оба — часть экрана 11).
>
> **Осознанно не сделано, с причиной:**
>
> - **Экран 08 (нагрузка)** — модель Банистера и калибровка шкалы остаются
>   открытым вопросом `TRAINING_LOAD_MODEL_PROMPT.md`, не этого документа.
> - **Полная правка куратора после старта тренировки (экраны 09/14)** — для
>   тренировок, назначенных планом, защита уже двухслойная: `setStrengthWorkout`
>   отказывает, если план `started`/`done` (прямая MCP- запись), и правило
>   слияния не даёт remote-версии выиграть по свежести/ richness, если приоритет
>   её `plan.status` ниже (офлайн-гонка) — оба сделаны 2026-08-09 (ночь), см.
>   `CURATOR_TRAINING_PROGRAM_PROTOCOL_2026-08-09.md`, слои 2 и 5. Для
>   тренировок **без плана** (обычная сессия конструктора без программы
>   куратора) такой защиты по-прежнему нет — там нет `plan.status`, не от чего
>   вывести признак «сессия идёт», а метка авторства на подходе не сделана.
>   Экран 14 (диалог двух версий) остаётся не начатым в любом случае — решение
>   владельца прямо откладывает его до этой же инфраструктуры.
> - **Разбор вчерашней незакрытой тренировки** (экран 11, третье состояние) —
>   требует кросс-day чтения и записи, которых конструктор сегодня сознательно
>   не получил: отдельная поверхность риска, не входящая в этот проход.
> - **«Собрать связку» на конструкторе без плана, «Дописать» из банера чужого
>   дня** и подобные кросс-day действия — та же причина, что выше.
>
> Шаг 6 (программа куратора) продвинут за рамки одного интерфейса: слои 1–3
> смежного протокола реализованы в этом же проходе, потому что экран 02 макета
> («Начать по плану») требовал схемы `plan`/`planSnapshot` до кода интерфейса —
> см. `CURATOR_TRAINING_PROGRAM_PROTOCOL_2026-08-09.md`.

**6. Программа куратора**, слои 2–6 из смежного протокола.

### Где остановиться и спросить

- **Прогулка фермера и подобное в метрах** — правило «в тоннаж не идёт, копится
  отдельно» принято, но как показывать прогресс по метрам в истории упражнения,
  не решено.
- **Доля синергистов в объёме по группам** — принято «половина», конкретное
  число должно стать параметром справочника, а не константой; кто им управляет,
  не решено.
- **Коэффициент у своего упражнения**, если человек пропустил вопрос «на что
  похоже»: упражнение создаётся без тоннажа. Что делать, если он захочет
  дозаполнить позже, не решено.
- **Помощь резиной и противовес** (гравитрон, подтягивания с петлёй) — довес
  сейчас принимается только неотрицательным, отрицательного значения нет.
  Гравитрон в справочнике стоит без коэффициента, то есть просто не считается.
  Открыто с 2026-08-09: делать ли отрицательный довес или отдельное поле помощи.
  Пока не решено — эти подходы остаются без тоннажа.

## Открытое

- **Калибровка шкалы нагрузки** — остаётся открытой, см.
  [`TRAINING_LOAD_MODEL_PROMPT.md`](TRAINING_LOAD_MODEL_PROMPT.md) § «Что
  осталось открытым». Решение 1 снимает срочность для клиентского UI, но не
  отменяет калибровку.
- **Экран двух версий подхода** — отложен, не отменён. Вернётся, когда механизм
  слияния научится хранить проигравшую версию.
- **План от куратора** («Начать по плану · День B») — в макете есть, в коде нет
  ни схемы, ни читателя. Аудит и план работ:
  [`CURATOR_TRAINING_PROGRAM_PROTOCOL_2026-08-09`](CURATOR_TRAINING_PROGRAM_PROTOCOL_2026-08-09.md).
  Схема плана (`plan`, `planSnapshot`) и экран «Начать по плану» делаются в том
  же проходе, что этот редизайн: иначе экран 02 придётся переделывать.
- **Сверка калорий по решению 5** — расчётная часть сделана (см. раздел выше),
  осталась проверка на выгрузке живых дней: сколько силовых заведено
  конструктором против режима «зоны пульса» и у скольких клиентов вообще нет
  сохранённых `heys_hr_zones`. Второе решает, в какую сторону поедут калории.
- **Распределение минут по зонам для авто-расчёта** — не выбрано. Три
  посчитанных варианта расходятся между собой на 72–176 ккал на тренировке, то
  есть цена решения выше, чем цена самого включения. Выбор за владельцем.
- **Расхождение конструктора и мастера тренировки** (одна и та же силовая даёт
  147 или 221 ккал в зависимости от пути ввода) — предсуществующий дефект, не
  связанный с редизайном. Свести к одной точке отсчёта надо до включения
  авто-зон, иначе сверять будет не с чем.
