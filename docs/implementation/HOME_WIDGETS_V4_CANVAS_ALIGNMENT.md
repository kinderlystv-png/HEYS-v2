# Протокол: сведение виджетов Главной с канвасом v4 до 100 %

**Дата старта:** 2026-08-21  
**Канвас (источник правды):**
`docs/ui/handoff-v4/canvas/Переработка дизайна приложения/design_handoff_heys_v4/home-widgets.v4.dc.html`
— контракт `[data-contract]`, **101 строка**, 80 кадров (75 годных для
сверки).  
**Предыдущий проход:**
[HOME_WIDGETS_V4_PROTOCOL.md](HOME_WIDGETS_V4_PROTOCOL.md) (2026-08-19, сведение
по 39 строкам контракта).  
**Приёмочный лист:** `…/design_handoff_heys_v4/ACCEPTANCE.md` — заполнен до
строки 32; строки 33–101 пустые, а строка 32 закрыта по формулировке, которой в
текущем контракте уже нет.  
**Задача владельца (2026-08-21):** довести код до 100 % совпадения с канвасом,
идти подряд по всем отклонениям.  
**Разрешения:** commit — по ходу работы; push/deploy — только по отдельной
прямой команде.

## UI-гейт

`цель — экран виджетов ведёт себя ровно так, как нарисовано в канвасе; главное действие — тап в разбор; слой 1 — число и его состояние; слой 2 — лист смены вида по удержанию и режим расстановки; критическое не скрывать — перебор нормы, отсутствие данных и выход из режима расстановки.`

## Что изменилось в канвасе после прошлого прохода

Две пересборки: `eacb1482` (2026-08-20) и `741581e2` (2026-08-21, +99 строк
разметки). Контракт вырос с 39 до 101 строки. Содержательных сдвига два:

1. **Размер стал свойством вида.** Отдельного переключателя размеров нет; вид и
   размер лежат в одной записи, карточка в листе нарисована в своём формате и
   применяет вид вместе с размером.
2. **Раскладка задаётся порядком, а не координатами.** Появился явный алгоритм
   пересборки (порядок чтения, поиск вперёд не глубже двух, дырка допустима) и
   четыре кадра `Расстановка · до смены / рост / сжатие / дырка`.

## Порядок работ

| Блок | Тема                                   | Строки контракта                       | Статус            |
| ---- | -------------------------------------- | -------------------------------------- | ----------------- |
| A    | Flow-раскладка: порядок, не координаты | 33–37, 62, 63, 69, 70, 75, 80          | готово            |
| B    | Размер как свойство вида               | 31, 32, 38, 73, 74, 79, 82, 83, 84, 86 | готово            |
| C    | Лист выбора вида                       | 27, 28, 72, 81, 89                     | кроме воды        |
| D    | Режим расстановки                      | 49–61, 93                              | готово            |
| E    | Тап по плитке                          | 47, 48                                 | закрыт контрактом |
| F    | Правила покраски                       | 94, 95, 97                             | готово            |
| G    | Сетка и адаптив                        | 8, 71, 77, 78                          | готово            |
| H    | Состояния данных                       | 39–46, 65–68, 99                       | готово            |

## Отклонения

Вес: 🔴 ядро · 🟡 поведение · 🟢 мелочь/вопрос.

| #   | Строки         | Канвас требует                                                                           | Факт в коде                                                   | Вес | Статус |
| --- | -------------- | ---------------------------------------------------------------------------------------- | ------------------------------------------------------------- | --- | ------ |
| 1   | 62, 63, 33–37  | раскладка — порядок чтения; поиск вперёд не глубже двух; дырка допустима                 | координатная сетка `col/row` + gravity-pack                   | 🔴  | готово |
| 2   | 32, 79         | выбор вида применяет вид **и размер** одной записью                                      | пишется только `settings.displayVariant`                      | 🔴  | готово |
| 3   | 37, 73, 74, 83 | анимация пересборки 220 мс, лист закрывается раньше сетки, догон скролла, reduced-motion | механики нет                                                  | 🔴  | готово |
| 4   | 39             | из коробки шесть плиток                                                                  | `DEFAULT_LAYOUT` = 11 плиток                                  | 🔴  | готово |
| 5   | 27, 81         | карточки листа по возрастанию формата                                                    | порядок каталога                                              | 🟡  | готово |
| 6   | 28, 89         | текущий вид виден в листе; дефолт «Динамика веса» — «За месяц» с кривой                  | дефолт `chart` помечен `sheet:false` — в листе его нет        | 🟡  | готово |
| 7   | 72             | у воды один формат 1×1                                                                   | виды `by_hour`, `rhythm` объявлены 2×1                        | 🟢  | вопрос |
| 8   | 54, 55         | уже стоящие виджеты в каталоге не показываются; превью в дефолтном виде                  | серая строка «уже добавлен», превью нет                       | 🟡  | готово |
| 9   | 51             | плитка приглушена до 50 %, дрожания нет                                                  | `widget-wiggle`; в режиме отключается v4-палитра плитки       | 🟡  | готово |
| 10  | 52             | кружок 22 px с минусом, тач-цель 44 pt                                                   | 32 px красный круг с крестиком                                | 🟡  | готово |
| 11  | 56             | перетаскивание сразу при касании и сдвиге                                                | требуется долгое нажатие, подсказка в футере                  | 🟡  | готово |
| 12  | 58             | автопрокрутка у края экрана                                                              | нет                                                           | 🟡  | готово |
| 13  | 61             | капсула даты остаётся, но не переключает дни                                             | капсула скрыта в режиме расстановки                           | 🟡  | готово |
| 14  | 53, 93         | добавление — пунктирной рамкой в конце сетки, только в расстановке                       | кнопка «Добавить» видна вне режима; каталог — полосой снизу   | 🟢  | готово |
| 15  | 47             | контракт переписан владельцем: «ОТКРЫТО, реализовывать нечего»                           | модалки у трёх типов, у остальных ничего — так и оставлено    | 🟢  | снят   |
| 16  | 94             | тренд здоровья: мёртвая зона ±2 пункта                                                   | красится любое `delta ≠ 0`                                    | 🟡  | готово |
| 17  | 95, 97         | инсулин — по текущему состоянию; серия и оценка дня по значению не красятся              | `v4InsulinWaveStatusState` даёт `act`; серию/оценку проверить | 🟡  | готово |
| 18  | 84             | после смены вида — фраза для вспомогательных технологий                                  | `aria-live` в виджетах нет                                    | 🟡  | готово |
| 19  | 78, 71         | сетка тянется до 480 px, дальше поля; минимум 320 px                                     | ограничения ширины нет                                        | 🟢  | готово |
| 20  | 8              | паддинг сетки 16 px                                                                      | `padding: 14px 16px 16px`                                     | 🟢  | готово |
| 21  | 43–46, 65–68   | прочерк вместо нуля, «нужно N дней», длинное значение не сжимается кеглем                | эмодзи-заглушки вместо прочерка                               | 🟡  | готово |
| 22  | 99             | положение прокрутки сохраняется при возврате                                             | памяти прокрутки не было                                      | 🟢  | готово |
| 23  | —              | в канвасе нет                                                                            | undo/redo в футере расстановки, оверлей с номерами ячеек      | 🟢  | снят   |

## Канвас спорит сам с собой — что взято

Пересборка 21 августа добавила 99 строк контракта, но кадры расстановки и шторок
остались от 19–20 августа. Там, где рисунок и `data-v` расходятся, взят контракт
— правило INDEX: «Числа в заметках — пересказ, при расхождении верен контракт».
Каждое такое место названо, чтобы дизайнер поправил канвас:

| Строка | Контракт говорит                             | Кадр рисует                                                  | Что сделано                                                                      |
| ------ | -------------------------------------------- | ------------------------------------------------------------ | -------------------------------------------------------------------------------- |
| 27, 81 | карточки листа по возрастанию формата        | шторки «Калории», «Вес», «Тепловая карта» — порядок каталога | взят контракт: лист сортирует по площади формата                                 |
| 56     | перетаскивание сразу при касании и сдвиге    | подпись футера «Долгое нажатие — взять виджет»               | взят контракт; подпись переписана, иначе она врёт о жесте                        |
| 72     | у воды один формат 1×1, размеров в листе нет | кадры «Вода · К этому часу» и «Ритм дня» — `span 2`          | владелец поправил контракт: три вида, заливка и пороги — механика 1×1; код верен |
| 54     | каталог с превью в формате дефолтного вида   | каталог — плитки «название + плюс», без превью               | владелец подтвердил контракт: превью рисуется тем же кодом, что Главная          |
| 47     | тап ведёт в «Отчёты» на разбор показателя    | разборов по показателям в «Отчётах» нет                      | владелец переписал строку в «ОТКРЫТО, реализовывать нечего» — код не менялся     |
| —      | —                                            | undo/redo в футере расстановки нарисованы                    | пункт 23 снят: механика в канвасе есть, код ей соответствует                     |

Строка 72 подкреплена главным источником: контракт `water-add` в строке
«носители» тоже говорит «плитка "Вода" 1×1 на Главной». То есть спорят кадры, а
не контракты — но два вида воды нарисованы под 2×1, и их судьба это продуктовое
решение, а не приведение кода к макету.

## Решения и риски

- **Спорные пункты 7 и 23** трогают продукт шире канваса. Владелец сказал «100 %
  совпадение с макетом» — веду к канвасу, каждое такое решение отмечаю в журнале
  отдельной строкой, чтобы его было видно и можно было отменить.
- **Риск блока A.** Переход от координат к порядку меняет формат хранения
  раскладки. Нужна миграция сохранённых layout-ов без потери пользовательского
  порядка и без перекладки чужих экранов — строка 82 контракта это прямо
  запрещает.

## Журнал

| Дата       | Что сделано                                                                                                                                                                                                                                                                                      | Чем проверено                                                                                                                                             |
| ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2026-08-21 | Бриф заведён, отклонения сняты с контракта на 101 строку                                                                                                                                                                                                                                         | сверка кода с `[data-contract]`                                                                                                                           |
| 2026-08-21 | Блок A: flow-раскладка — позиции стали производной от порядка, `computeFlowLayout` с поиском вперёд на две плитки, `reorderWidget`, перетаскивание меняет порядок, а не координаты                                                                                                               | `widgets-flow-layout-v4.test.js` — 10 проверок по кадрам «до смены / рост / сжатие / дырка»                                                               |
| 2026-08-21 | Блок B: выбор вида применяет вид и размер одной записью, пересборка после закрытия листа, FLIP-анимация 220 мс, догон скролла, `aria-live` фраза                                                                                                                                                 | тесты виджетов зелёные; смоук на живом экране — впереди                                                                                                   |
| 2026-08-21 | Блок C: лист сортирует карточки по возрастанию формата; дефолт «Динамики веса» — «Кривая» («За месяц»), а не невидимый в листе «График»                                                                                                                                                          | `widget-variants-v4.test.js`                                                                                                                              |
| 2026-08-21 | Блок D: каталог открывается пунктирной рамкой и прячет уже стоящие виджеты, плитка приглушается без дрожания, тач-цель минуса 44 pt, перетаскивание без удержания, автоскролл у края, запись по «Готово», капсула даты заблокирована                                                             | `widgets-v4-sphere.test.js` обновлён под новые контракты                                                                                                  |
| 2026-08-21 | Блоки F и G: мёртвая зона тренда ±2, волна красится по текущему состоянию, серия — чернила, сетка 16 px / до 480 px с центрированием                                                                                                                                                             | `widgets-v4-sphere.test.js`                                                                                                                               |
| 2026-08-21 | Блок H (часть): набор из коробки — шесть плиток без координат, `LAYOUT_PRESET_VERSION` 3 → 4                                                                                                                                                                                                     | `widgets-layout-persistence.test.js`                                                                                                                      |
| 2026-08-21 | Владелец поправил контракт по воде: три вида (1×1 дефолт + два 2×1), заливка и пороги 31 / 89 % остаются механикой 1×1. Код уже соответствовал — правок не потребовалось                                                                                                                         | `getSheetCatalog('water')` на живом бандле: `mini:1x1`, `by_hour:2x1`, `rhythm:2x1`                                                                       |
| 2026-08-21 | Каталог получил превью виджета в формате его дефолтного вида на живых данных дня — решение владельца в пользу контракта против кадра                                                                                                                                                             | разметка и стили превью; визуальная сверка на клиентском входе за пользователем                                                                           |
| 2026-08-21 | Блок H целиком: пустые плитки показывают прочерк вместо эмодзи-заглушек, «нужно N дней» вместо графика, снятый с продукта виджет уходит из раскладки, длинное значение не сжимается кеглем, прокрутка Главной переживает уход на другую вкладку                                                  | `widgets-flow-layout-v4.test.js` — 15 проверок                                                                                                            |
| 2026-08-21 | Строка 47 (тап → «Отчёты») по решению владельца отложена: модалки-разборы остаются, разборов по показателям в «Отчётах» нет                                                                                                                                                                      | —                                                                                                                                                         |
| 2026-08-21 | Строка 47 переписана владельцем в «ОТКРЫТО, реализовывать нечего»: разбор показателя — отдельный продуктовый блок. Код оставлен как есть                                                                                                                                                         | сверка `data-v`                                                                                                                                           |
| 2026-08-21 | Строка 54 подтверждена владельцем в пользу контракта: превью каталога рисуется тем же `WidgetContent`, что плитка Главной, в дефолтном виде и формате, на числах открытого дня                                                                                                                   | `widgets-catalog-preview-v4.test.js` — 4 проверки в jsdom                                                                                                 |
| 2026-08-21 | Вид плитки v4 больше не отключается в расстановке (32 селектора): там плитка только приглушается, а превью каталога обязано быть настоящей плиткой                                                                                                                                               | правка `730-widgets-dashboard.css`, смоук каталога                                                                                                        |
| 2026-08-21 | Каталог проверен на живом стенде 3001: превью — настоящие плитки (фон #f7efe2, радиус 16, паддинг 11, форматы 143×136 и 157×98 для ужатого 3×2), уже стоящий виджет скрыт, числа берутся из дня                                                                                                  | монтирование `CatalogStrip` в живом бандле, консоль без ошибок                                                                                            |
| 2026-08-21 | Найдено на живом экране: карточки листа рисовались в формате плитки, стоящей на экране, — «Семь дней» показывался строкой 2×1, а виды воды все выглядели как «Как сейчас». Превью теперь строится на виртуальном виджете формата карточки и носит классы настоящей плитки                        | шторки «Вода» и «Оценка дня» смонтированы на живом бандле: 68×64 / 143×64 / 143×136 со своим содержимым; `widget-variants-v4.test.js` — 2 новые регрессии |
| 2026-08-21 | Полная сверка всех 101 строки контракта. Гейт геометрии `widgets-v4-canvas-geometry.test.js` (пары «класс кадра → класс продукта») нашёл расхождения листа смены вида: радиус 20 вместо 26, тень, заголовок 15/800 вместо 14/700, подзаголовок 12 вместо 11, подложка без блюра — всё поправлено | 5 проверок гейта + живой замер в браузере: radius 26, blur 2.5, title 14/700/1.25                                                                         |
| 2026-08-21 | `pnpm ui:v4:check` после всех правок: сдвигов классики нет, неопределённых ролей 29 — список не вырос                                                                                                                                                                                            | гейты палитры                                                                                                                                             |
| 2026-08-21 | Решение владельца: `insulin`, `steps`, `streak`, `cascade` помечены `inDevelopment` — в каталоге гасятся подписью «в разработке», добавить нельзя                                                                                                                                                | `widgets-catalog-preview-v4.test.js` + живой замер: 11 кнопок с превью, 4 погашенные строки opacity 0.45                                                  |
| 2026-08-21 | Виджеты `inDevelopment` уходят и с уже собранных экранов при загрузке (строка 41): фильтр в `init`, порядок сжимается flow-укладкой                                                                                                                                                              | `widgets-flow-layout-v4.test.js` + живой прогон: из пяти сохранённых плиток остались три, раскладка сомкнулась                                            |
| 2026-08-21 | Все 101 строка контракта получили вердикт в общем снимке `docs/ui/ui-v4-contract-verdicts.json`: 100 «=» и одна «≠» — разовая миграция координат в порядок чтения                                                                                                                                | `node scripts/ui-v4-check-contract-drift.mjs`, обёртка `ui-v4-contract-drift.test.js` в общем прогоне                                                     |
| 2026-08-21 | Дизайнер ответил по четырём виджетам: «Инсулин», «Streak» и «Каскад» сняты с продукта как дубли уже нарисованного, «Шаги» получили два вида — кадры 35 и 36                                                                                                                                      | гейт дрейфа показал одну изменённую и десять новых строк; после разбора — `--rehash`                                                                      |
| 2026-08-21 | `retired` у трёх типов: из каталога убраны, с сохранённых экранов уходят. «Шаги» переведены на v4 — свой `StepsVariantBody`, покраска только шалфей/чернила, прочерк вместо нуля; мёртвый тумблер «Показывать км» снят вместе с legacy-рендером                                                  | `widget-variants-v4`, `widgets-data-missing-sources`, живой каталог на 3001                                                                               |
| 2026-08-21 | Каталог получил два состояния ожидания вместо «в разработке»: «скоро» — полная яркость, пилюля и обещание, нажатие ничего не делает; «мало истории» — приглушение 55 %, «нужно N дней», «собрано M из N», добавить можно. Строк ожидания не больше двух, идут последними                         | `widgets-catalog-preview-v4` — 7 проверок; на живом стенде «Тренд здоровья» показал «собрано 0 из 3»                                                      |
| 2026-08-21 | Дизайн дописал строку «готовится» с именем «Клетчатка», фразой и форматом 1×1; иллюстративный кадр снят, вторая строка «Клетчатки» с превью из каталога убрана. Код уже соответствовал — вердикт «—» сменился на «=»                                                                             | гейт дрейфа показал одну изменённую строку; после разбора — `--rehash`, вердикты 111 «=» и 1 «≠»                                                          |

## Гейты этого экрана

Схема та же, что у вкладки «Питание»:

| Гейт                                                                                     | Что ловит                                                                           | Команда                                                                                |
| ---------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------- |
| `scripts/ui-v4-check-classic-drift.mjs`                                                  | цвет, написанный мимо палитры и не меняющийся в тёмной/синей теме                   | `pnpm ui:v4:check`                                                                     |
| `scripts/ui-v4-check-undefined-roles.mjs`                                                | новые неопределённые роли: список заморожен на 29 и умеет только уменьшаться        | `pnpm ui:v4:check`                                                                     |
| `apps/web/__tests__/widgets-v4-canvas-geometry.test.js`                                  | геометрию: отступы, кегли, радиусы, тени против кадров канваса                      | `npx vitest run apps/web/__tests__/widgets-v4-canvas-geometry.test.js`                 |
| `apps/web/__tests__/widgets-flow-layout-v4.test.js`                                      | поведение раскладки: порядок чтения, поиск вперёд на две, дырка, запись по «Готово» | тот же `vitest run`                                                                    |
| `apps/web/__tests__/widgets-catalog-preview-v4.test.js`                                  | каталог: превью настоящей плиткой, скрытие уже добавленных                          | тот же `vitest run`                                                                    |
| `scripts/ui-v4-check-contract-drift.mjs` (общий на 14 канвасов, сделан соседней сессией) | дизайнер поменял строку контракта, а вердикт остался прежним                        | `node scripts/ui-v4-check-contract-drift.mjs`, после разбора — `--rehash home-widgets` |

Контракт вырос до 112 строк и закрыт целиком: 111 «=» и одна «≠».

«≠» — «снятие флага не двигает раскладки»: перейти на модель порядка без разовой
миграции координат нельзя, дальше пересборку запускает только действие человека.

Осознанных отступлений в гейте геометрии — три, каждое названо в самом тесте:
паддинг сетки 16 px против 14 px кадра (строка контракта 8), блюр подложки 2.5
px из токена против 2 px кадра и её dim 0.45 против 0.42 (инвариант
product-модалок).

## Состав каталога — решено

**Решение владельца 2026-08-21.** Четыре виджета, чей вид не сведён с канвасом
v4 — `insulin`, `steps`, `streak`, `cascade` — помечены в реестре флагом
`inDevelopment`. В каталоге расстановки они стоят последними, погашены до 45 %,
вместо превью — подпись «в разработке», и это не кнопка: добавить их нельзя. Так
же их показывает кадр «Главная · расстановка» (там подпись «чинится»).

**Дополнение того же дня.** Плитки этих типов уходят и с уже собранных экранов
при первой загрузке: строка 41 контракта прямо описывает этот случай — виджет,
убранный из продукта, исчезает из раскладки, порядок сжимается тем же
алгоритмом, сообщения нет. Оставлять их было бы «показать и не дать»: вид не
сведён, починить нельзя, а после удаления вернуть неоткуда — в каталоге строка
погашена.

Строка 82 («снятие флага не двигает раскладки») этому не противоречит: она про
пересборку от смены размера, а не про состав продукта.

Когда вид виджета доведут до канваса, флаг снимается и он появляется в каталоге
— но на экраны сам не встаёт (строка 40): человек добавляет его сам.

## Дефолтный набор, сброс и защита раскладки — 22 августа

**Почему.** 21 августа раскладка Главной потеряла плитки дважды: фильтр «виджеты
в разработке» выбрасывал любой незнакомый тип и делал это при каждой загрузке,
сохраняя результат. Фикс 026ec0db закрыл сам фильтр, но не класс проблемы —
раскладку по-прежнему можно было уменьшить молча, а восстанавливать её человек
мог только по памяти.

### Защита записи (heys_widgets_core_v1.js)

- **Shrink-guard.** Уменьшение состава проходит только от названной операции:
  `user-remove`, `user-reset`, `apply-preset`, `undo`, `redo`, `edit-done`,
  `edit-cancel`, `retired-migration`. Причину ставит сама операция
  (`_debouncedSave(reason)` или `saveLayout(null, { reason })`), она переживает
  debounce. Всё остальное отбивается с `console.warn`, где перечислено, что
  пропало бы. Kill-switch — `__heys_disable_widget_shrink_guard__`, как у
  продуктов.
- **Не писать, если нечего писать.** `saveLayout` держит отпечаток последнего
  записанного состава и молча выходит, если он не изменился. Отпечаток ставится
  после `init` и после приёма облачной раскладки.

  Это закрывает сценарий, который и был настоящим риском: вкладка со старым
  кодом, которую просто **свернули**, отправляла своё состояние со свежим
  `updatedAt` и честно выигрывала last-write-wins у того, что человек собрал на
  другом устройстве. Двигать в ней ничего не требовалось — `visibilitychange`
  писал всегда.

### Дефолтный набор — строка «состав дефолта»

`DEFAULT_LAYOUT` — одиннадцать плиток в порядке чтения, вид у каждой задан
`settings.displayVariant` явно (не флагом каталога: дефолт первого экрана не
должен молча меняться от правки каталога видов). Позиции не заданы — их считает
`computeFlowLayout`, порядок и есть раскладка.

Два отступления, оба по кадру «Главная · дефолтная раскладка» и оба названы в
`UI_V4_FINDINGS.md`:

- «Тепловая карта» стоит видом «Как сейчас» (`week_bar`): кадр рисует в этой
  клетке неделю полосами «5 из 7», а вид «Серия» и в кадре, и в каталоге — 1×1,
  тогда как строка называет 2×1;
- «Динамика веса» стоит видом «Кривая» (`curve`, окно «за месяц») по строке
  «дефолт вида» и решению владельца 20 августа: вида «F компакт» из строки
  «состав дефолта» в каталоге не существует.

Существующие раскладки не трогаются: при `hasSavedLayout` init бэкфиллит только
meta. Новый дефолт видят чистый старт и сброс.

### «Вернуть рекомендуемый экран» (heys_widgets_ui_v1.js)

- Блок `RecommendedScreenBlock` после `CatalogStrip` — заголовок, строка про
  замену состава и видов, кнопка «Вернуть» обводкой 44 px.
- **Подтверждения нет** — `ResetLayoutConfirmModal` снят вместе с обработчиками:
  сброс отменяется той же стрелкой расстановки, что и перенос плитки
  (`resetLayout` делает `_pushHistory`). Второй механизм отмены рядом с первым —
  ровно то, что вычищали в баре удаления.
- Пустое состояние получило второй путь — «Вернуть стандартный экран» текстом
  под заливкой «Добавить виджет».

### Проверено

`widgets-layout-shrink-guard.test.js` — 11 кейсов: молчаливое уменьшение
отбивается, снятие плитки человеком проходит, вкладка без правок в storage не
ходит, принятая из облака раскладка не уезжает обратно, kill-switch снимает
guard, дефолт даёт одиннадцать плиток в порядке контракта, каждый вид дефолта
существует в каталоге и совпадает по размеру, снятые типы и «Шаги» в дефолт не
попадают, строка контракта всё ещё называет одиннадцать плиток (тест читает сам
канвас), сброс возвращает состав целиком, один шаг назад после сброса возвращает
прежний состав.

`widgets-flow-layout-v4.test.js` обновлён под новую сигнатуру `_debouncedSave` —
проверка привязана к тексту, а не к номеру строки.

Гейт дрейфа контракта зелёный: восемь новых строк получили вердикты, снимок
переснят (`--rehash home-widgets`), дубль ключа «пустой экран» разведён.

### Пять расхождений закрыты владельцем — 22 августа

Все пять записей тира 4 закрыты в тот же день и все в пользу кода; разбор уехал
в [`UI_V4_FINDINGS_HISTORY.md`](../ui/UI_V4_FINDINGS_HISTORY.md). Контракт стал
120 строк:

- «набор из коробки» снята — двух описаний первого экрана быть не может;
- тепловая карта — «Как сейчас», с оговоркой, что «Серия» живёт только в 1×1;
- динамика веса — «За месяц» кривой; дописано правило, что буквенных имён кадров
  (B, C, F, G) в контракте и каталоге не существует;
- старая «пустой экран» снята, ключ возвращён к `пустой экран`;
- «страницы» убраны: «состав и порядок восстанавливаются целиком», страниц у
  раскладки нет.

Отступлений в дефолтном наборе больше нет: контракт и `DEFAULT_LAYOUT` совпадают
построчно. Тест дефолта дополнен проверкой обеих закрытых формулировок прямо в
канвасе — если они уедут назад, состав разойдётся не молча.

## Шесть виджетов пакета 22 августа — кадры 37–51

Пакет добавил в контракт 24 строки и вывел «Клетчатку» из «скоро». Реализованы
все шесть типов: клетчатка, белок, окно до сна, качество еды, ритм приёмов,
готовность ко сну — пятнадцать видов, контракт вырос до 144 строк.

**Второго алгоритма нет нигде — это главное правило пакета.** Норма белка
берётся у `getMacrosData` (той же, что кормит кольцо БЖУ), качество еды — у
`dayCalculations.calculateDayTotals`, той же вредности, что показывает карточка
на «Питании». Пороги «Готовности ко сну» не свои: вода ≥ 90 % нормы, окно до сна
≥ 3 ч из `getSleepWindowData`, шаги ≥ 100 % цели из `getStepsData`. Норма
клетчатки — единственное новое число, и оно прямо названо контрактом: 14 г на
1000 ккал бюджета дня.

Побочно: `_getDayTotalsFor(day)` — итоги произвольного дня тем же расчётом, что
и текущего. Без него недельные виды считали бы вредность локальным fallback,
который её вовсе не знает, и «Качество · 7 дней» молча показывало бы пустоту.

Цвет: недобор не красится ни у одного из шести. Красный есть ровно один — «ел
перед сном» у окна до сна, когда приём был меньше чем за час до отбоя.

В дефолтный набор не вошёл ни один: добавляются руками из каталога.

**Проверено симуляцией** — `widgets-new-six-v4.test.js`, 19 кейсов: норма
клетчатки от бюджета, белок против нормы кольца, окно от отбоя чек-ина и от
23:00 без него, красный только при еде за час до сна, итог вечера вместо
отрицательного времени, индекс качества из той же вредности, интервалы ритма,
пороги готовности и выпадение пункта без данных из счётчика. Плюс реестр и
каталог видов: шесть типов в каталоге без «скоро», ни одного в дефолте, виды и
дефолты по кадрам 37–51, размеры видов сходятся с реестром.

**Три расхождения отданы дизайнеру** записями в `UI_V4_FINDINGS.md`: словарь
«чем добрать» хранит категории, а кадр 38 рисует продукты; словаря источников
белка не существует вовсе; «индекс качества» на «Питании» существует только как
вредность, и виджет показывает её же, повёрнутую шкалой.

### Три расхождения закрыты владельцем — 22 августа

Все три записи тира 5 закрыты в тот же день, разбор уехал в
[`UI_V4_FINDINGS_HISTORY.md`](../ui/UI_V4_FINDINGS_HISTORY.md).

- **Клетчатка · подсказка** — берём словарь, который в продукте есть: категории
  без граммовок. Кадр 38 перерисован на «овощи · бобовые · злаки», второй
  словарь запрещён контрактом. Код не менялся: он с самого начала звал
  `getFiberSources()`.
- **Белок · подсказка** — строки нет вовсе. Убрана и из вида 41, и из данных:
  поле `sources` у белка снято целиком, потому что пустой массив был бы
  приглашением однажды наполнить его овощами под именем белка.
- **Качество еды** — индекс = 10 − вредность, как и предлагалось. Порог шалфея 8
  отменён: теперь 5 из 10, то есть вредность ≤ 5 — тот же `HARM_THRESHOLD`, что
  у карточки «Питания». Своего порога у виджета нет, и тест проверяет это прямо
  в исходнике: спор виджета с карточкой о том, хороший ли день, был бы дороже
  любой экономии на проверке.

### Плитка добавления — новый раздел контракта, 22 августа

Пунктирная «Добавить» перестала быть деталью расстановки: контракт завёл под неё
отдельный раздел из семи строк, и главное в нём — она стоит в сетке **всегда**,
в обычном режиме тоже. Раньше её рисовало условие `isEditMode`, то есть человек
без входа в расстановку добавить виджет не мог вовсе.

- **Ширина** считается от хвоста последнего ряда: `addTileSpan()` даёт 2 колонки
  при хвосте в две клетки и больше, 1 — при хвосте ровно в одну, и снова 2
  (новым рядом) при полностью занятом ряде. Трёх- и четырёхколоночной кнопки не
  бывает, высота всегда один ряд.
- **Содержимое** зависит от ширины: в две колонки «+» 14 px и слово, в одну —
  только «+» 18 px, зазор снят классом `--span1`. Слово в 88 px не встаёт.
- **Нажатие** открывает каталог, не включая расстановку, поэтому `CatalogStrip`
  рендерится по `catalogOpen` без `isEditMode`.
- **Не виджет**: в `DEFAULT_LAYOUT` её нет, `resetLayout` не трогает, в потолок
  одиннадцати плиток не считается.

Проверено: `widgets-add-tile-v4.test.js` — 11 кейсов, включая плитку высотой в
два ряда (она занимает клетки и в последнем из них) и полностью занятый ряд.
Геометрия сверена в браузере: 2 колонки — 182 px, 1 колонка — 88 px, обе высотой
64 px, пунктир 2 px акцентным тоном на 32 %, радиус 16 px, фон прозрачный.

### Инсулиновая волна — схема вместо таймлайна, 22 августа

Контракт завёл под виджет отдельный раздел из восьми строк, и первая же из них
меняла модель: **волны больше не раскладываются по оси времени**. Раньше
`minToX` ставил каждую волну на её место в сутках — между ними зияли пустые
промежутки, а ширина зависела от длительности. Теперь это схема: волны вплотную,
равной ширины, в порядке приёмов; полоса 122 px делится на число волн.

- **Склейка.** Волны с нулевым зазором (стык) и с отрицательным (нахлёст) дают
  одну фигуру с двумя вершинами. Провал считается от высоты соседней волны: 42 %
  у стыка, 68 % у нахлёста — числа сверены с кадром, где при амплитуде 24 провал
  стоит ровно на 35,9.
- **Метки разные.** Стык — точка 2,2 px нейтральным тоном без подписи. Нахлёст —
  полоса 0,9 слота тёплым акцентом внутри фигуры (обрезается `clipPath` по самой
  фигуре, а не прямоугольником), скоба 2,4 px под ней, подпись «N волн
  наложились» и справа счётчик стыков.
- **Тёплый акцент — новая роль** `--v4-wave-overlap` с четырьмя значениями
  контракта. Красным нахлёст не красится: это факт про день, а не ошибка. Заодно
  закрылся долг `--v4-sand-act-overlap` из списка неопределённых ролей — их
  стало 28.
- **Базовая линия** осталась только в схеме 27: по ней стоят риски-разделители
  3,5 px. В видах 28 и 29 её нет, и обводка идёт по незамкнутому пути (1,2 px) —
  контур сам ограничивает форму, а лишняя линия читалась бы как рамка.
- **День без приёмов** получил свой кадр вместо общего прочерка: силуэта нет,
  счётчик говорит «приёмов не было», снизу «покой N ч от подъёма». Данные
  прошлого дня не подставляются никуда.
- **Текущая волна** заливается на 80 % против 45 % у закрытых, снизу «под волной
  HH:MM»; когда все закрыты — «покой N ч».
- **Потолок восемь фигур:** рисуются восемь последних, счётчик остаётся полным
  («12 приёмов»), фигура не сжимается уже 12 px.

Проверено: `widgets-insulin-scheme-v4.test.js` — 17 кейсов, из них три сверяют
кривую первой фигуры с кадром посимвольно (`C10.8,46.0 9.4,22.0 16.2,22.0` и
далее). В браузере сверены все три вида 2×2: в схеме базовая линия одна, в 28 и
29 её нет, обводка везде 1,2 px и незамкнутая.

**Отступление, названное вслух:** в схеме все вершины одной высоты, тогда как
кадр рисует их разными (22, 16, 26, 19, 24). Величины, от которой высота
зависела бы, контракт не называет ни в одной строке, а выдумывать её — значит
показать человеку число, которого никто не считал. Если разброс задуман, это
отдельная строка контракта.

---

## Проход 2026-08-30 — раздел «Разбор кадров» и шесть листов из заглушек

**Что приехало.** Канвас `home-widgets.v4.dc.html` вырос на 3 793 строки: сами
кадры не изменились ни на пиксель, добавились три машинно-читаемых раздела —
«Разбор кадров · элемент за элементом» (каждый нарисованный элемент со своими
числами), «Разбор графики · SVG в кадрах» (viewBox, точки кривых, радиусы) и
«Текст кадров дословно». Плюс четыре строки «снято ·» под решение владельца 29
августа «кнопок не боимся»: зоны углов, запрет 1×1 в углу, молчаливый сдвиг
прежних раскладок и смещение 2×1 сняты.

**UI-гейт:** цель — довести листы разбора до кадра; главное действие в листе —
кнопка внизу; слой 1 — герой, вывод, картинка, разбор числами; слой 2 — норма
строкой под разбором; критическое не скрывать — «нет данных» у кофеина стоит
первым слоем, а не прячется.

### Шаг 1 · решения владельца уже в коде

Четыре снятых правила про углы проверены по коду: спец-обработки углов в
flow-укладке нет, `DEFAULT_LAYOUT` держит те же тринадцать плиток в том же
порядке, что и строка «состав дефолта». Правок не потребовалось — канвас догонял
код, а не наоборот.

Одна правка нашлась: карточка «Вернуть рекомендуемый экран» говорила
«Одиннадцать плиток», хотя дефолт — тринадцать, и строка контракта сама называет
счёт исправленным 29 августа. Кадр и его дословный текст всё ещё говорят
«Одиннадцать» — отступление названо в
[`UI_V4_FINDINGS.md`](../ui/UI_V4_FINDINGS.md).

### Шаг 2 · каркас листа разбора сведён с кадром

Каркас общий у всех восемнадцати кадров «Разбор · …», и в нём нашлось
одиннадцать расхождений: ручка 12 → 14, зазор шапки 8 → 12, интерлиньяж
заголовка 1,25 → 1, крестик 44 px без круга → круг 30 px на `--c1`, кикер
получил верхний отступ 16, герой — 9 и интерлиньяж .9, вывод 12/500/чернила-2 →
12,5/600/`--tx`, столбики 72/зазор 4 → 56/зазор 6 с радиусом 6/3 и заливкой
`--acs`, риска нормы .22 → .18 и высота 55 % → 62 %, разбор числами и норма
переведены на верхний отступ 16 и 14.

Заодно вся раскладка листа переведена с нижних отступов на верхние — кадры
задают каждому блоку свой `margin-top`, и при пропущенном блоке отступ уходит
вместе с ним.

**Отступление, названное вслух:** кадр рисует крестик кругом 30×30 без зоны
нажатия. Круг оставлен 30 px, зона растянута до 44 px псевдоэлементом `::before`
— палец меньше 44 не ловит, и это инвариант продукта старше кадра.

### Шаг 3 · ось недели вместо подписи графика

Кадры «Разбор · Калории / Шаги / БЖУ / Оценка дня» рисуют под столбиками семь
подписей дней, последняя тоном `--ac`; подписи графика («7 дней», «Съедено за 7
дней») в кадрах нет. Ось `chartAxis` научилась и паре «слева — справа» (кривые
веса и тренда), и семи подписям дня.

Тогда же сведена копия моделей: кикер калорий «Осталось» → «Осталось на
сегодня», кикер волны «Свободное окно» → «Свободных окон сегодня», строка БЖУ
«Белок — % нормы в среднем» → «Белок в среднем», недели динамики веса из «Неделя
1..4» в «Эта неделя · Прошлая · Две назад · Три назад» и порядком от текущей.

### Шаг 4 · шесть листов из заглушек в полные

Клетчатка, Белок, Окно до сна, Качество еды, Ритм приёмов и Готовность ко сну
открывали заглушку: герой и кнопка, без картинки и разбора. Все шесть собраны по
своим кадрам; заглушечный путь `buildStubBreakdown` и признак `stubOnly` удалены
вместе с их CSS.

Четыре новых вида графика: источники вклада строками (клетчатка), распределение
по приёмам с риской равной доли (белок), составные столбики дней (качество еды),
циферблат суток с двумя кольцами и ночным сектором 21:00 → 03:00 (ритм приёмов).
Окно до сна переиспользует ленту сна на своей оси 18:00 → 06:00.

**Два листа упирались в данные, которых в продукте нет.** Решение владельца 30
августа:

- **Качество еды.** Третья часть столбика — «обработанное» — признак продукта, а
  не число; база не размечена, и столбик собран из двух частей: простые углеводы
  тоном `--warn` и остальное тоном `--gr2`, высота столбика — калории дня. Герой
  сменён с выдуманного балла «7,2 / 10» на реальную вредность дня «3,4 из 10 ·
  порог 5» — то же число и тот же порог, что на карточке вкладки «Питание».
  Разбор числами тоже переведён на вредность: «Средняя вредность», «Худший
  день», «После 19:00 доля простых».
- **Готовность ко сну.** Кофеин добавлен четвёртым пунктом и честно говорит «нет
  данных» до первого заполнения — словами, а не пустой дорожкой: пустая читалась
  бы как «пункт открыт». Порог считается от личного отбоя, а не от часа («не
  позже 16:00» врёт тем, кто ложится в час): последний кофе не позже восьми
  часов до отбоя. Экранное время не заводим — самоотчёт, а чек-ин сознательно
  укорачивали. Герой — счётчик «2 из 3 · кофеин ждёт данных», а не балл: балла
  продукт не считает. Семь точек недели считаются по тем же четырём правилам за
  каждый день, а не берутся из хранилища — готовность прошлых вечеров продукт не
  хранит.

**Кадр пришёл под эти решения тем же днём.** Владелец переписал строки «разбор ·
Качество еды» и «разбор · Готовность ко сну» и перерисовал оба кадра; код сведён
уже с новой редакцией. Два отступления названы: кадр держит строку «Средняя
готовность — 6,4», но балла в продукте нет — строка говорит «Закрыто в среднем N
из M», как велит сама строка контракта.

### Шаг 5 · дефект, который нашло только превью

Столбики недели в листах разбора не рисовались вовсе — схлопывались в линию.
Причина: высота в процентах стояла на вложенном `<i>` внутри флекс-элемента, у
которого своей высоты нет, — процентам не от чего считаться. Кадр рисует столбик
одним элементом; так и сделано, а вложенный элемент и его правило убраны. Дефект
был в коде до этого прохода и тестами не ловился — его видно только глазами,
поэтому в гейт добавлена строка, которая запрещает возвращать `<i>`.

### Проверки

- `apps/web/__tests__/widgets-bd-sheet-canvas-razbor.test.js` — новый гейт:
  читает раздел «Разбор кадров» из самого канваса и сверяет каркас листа и
  четыре новых вида графика с продуктовым CSS. Три отступления перечислены
  поимённо.
- `apps/web/__tests__/home-widgets-breakdown-batch2-v4.test.js` — смоук шести
  листов на четырнадцати живых днях с приёмами в разное время, отбоем из чек-ина
  и продуктами с клетчаткой и простыми углеводами. Симуляцией найдены два
  дефекта: «Между обед и ужин» без творительного падежа и норма белка, которую
  код искал в поле `norm` вместо `target`.
- `pnpm vitest run apps/web/__tests__/widgets apps/web/__tests__/home-widgets apps/web/__tests__/widget-`
  — 26 файлов, 299 тестов.
- `node scripts/ui-v4-check-contract-drift.mjs --zone home-widgets` — зона
  чиста.
- `node scripts/ui-v4-check-undefined-roles.mjs` — 25 известных ролей, голых
  `var()` нет.
- Глазами: статичный стенд на продуктовом CSS со всеми шестью листами (чек-ин
  фикстуры E2E залипает на «Сохраняю…» и до Главной не пускает — это не правка
  этого прохода).

### Вердикты

Слепок зоны переснят: 4 078 строк. Закрыты вердиктом каркас листа во всех
восемнадцати кадрах, четыре новых вида графика и семь строк про углы и состав
дефолта; двадцать строк стоят `≠` с причиной. Остальные строки нового раздела
остаются `?` — «никто не смотрел», и это долг следующего прохода, а не «и так
понятно».

### Догон пакета 31 августа

Дизайнер закрыл все три находки прохода: счёт плиток в кадрах исправлен на
«Тринадцать» (код уже так говорил), кикер листа «Тренд здоровья» перестал
дублировать соседнюю «Динамику веса», порядок плиток в кадре дефолтной раскладки
остаётся за контрактом и правится следующим заходом дизайнера — код трогать не
нужно.

Правка кикера завела новый дубль: кадр поставил «Тренд здоровья · 30 дней», а
заголовок листа — «Тренд здоровья», двумя строками выше. Приём кикера виден
только по всем восемнадцати листам сразу: семнадцать называют срез («Осталось на
сегодня», «Утром», «Этой ночью», «Сейчас», «Сегодня вечером»), и лишь этот —
виджет. В коде стоит «За 30 дней»: тот же срез, тот же приём, и совпадает с осью
графика этого же листа. Отступление названо строкой `≠` в снимке и записью в
[`UI_V4_FINDINGS.md`](../ui/UI_V4_FINDINGS.md).

### Из чего состоит долг снимка

Строк с вердиктом `?` — 3 492, но читать их подряд действительно бессмысленно.
Разложены они так:

| Класс                                                                            | Строк | Чем закрывается                                                                                                                                                                 |
| -------------------------------------------------------------------------------- | ----- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Подложка главного экрана внутри восемнадцати листов «Разбор · …»                 | 1 296 | 1 044 из них **побайтово совпадают** со строками кадра «Главная · дефолтная раскладка»: тот же экран под шторкой. Закрытие 76 числовых строк главного кадра закрывает их следом |
| Строки без единого числа («область прокрутки», «сетка плиток», «плитка», «ключ») | 357   | вердикт `—`: адресация разметки, к коду не сводится                                                                                                                             |
| Остальное — числа кадров, которых никто не сводил                                | 2 037 | по кадрам: Шторка (10 кадров, 320 строк), Смена вида (14 кадров, ~250), Быстрые действия (11 кадров, ~230), Кнопка настройки, Каталог, Расстановка                              |

Вывод для следующего прохода: рычаг не в построчном чтении, а в паре «кадр →
таблица классов» — так же, как сделано для каркаса листа. Один кадр главной
раскладки стоит 1 120 строк снимка.

## Проход 2026-08-31 — кадр «Главная · дефолтная раскладка»

Сведён сам кадр Главной: 40 числовых строк разбора, плитка за плиткой. Он же
подложка восемнадцати листов разбора, поэтому его закрытие закрывает снимок
пачкой, а не построчно.

**UI-гейт:** цель — числа плиток совпадают с кадром; главное действие не
меняется; слои не меняются; критическое не скрывать — состояние по-прежнему
несут число и линия.

### Что нашла сверка

Семь расхождений, каждое числом из строки разбора:

| Кадр                      | Было в коде              | Стало                                                         |
| ------------------------- | ------------------------ | ------------------------------------------------------------- |
| 17 · «осталось» у Калорий | зазор колонки 2 px       | свой отступ сверху 7 px                                       |
| 23 · факт под полосой     | сплошные чернила         | чернила 85 %                                                  |
| 29 · подпись волны        | отступ сверху 9 px       | прижата к низу (`auto`)                                       |
| 39 · шапка плитки         | по центру, зазор 8       | по базовой линии, зазор 6                                     |
| 41 · число в шапке        | одно на все — 9,5 px/700 | сравнение 9 px/600 чернилами, счётчик — модификатор `--count` |
| 62 · низ плитки «Вес»     | baseline, прижат вправо  | по нижнему краю, space-between                                |
| 69 · спарклайн «Веса»     | плитки не было вовсе     | вернулся: 58 × 22, точка на последнем дне                     |

Про **41**: кадр держит в шапке два разных числа. Счётчик («3 приёма», «5 из 7»)
крупный и жирный; сравнение («в среднем 8 940», «−0,9 за неделю», «до цели 3,6»)
мельче и тише. Код красил и то и другое одинаково, а тон-класс дополнительно
раздувал сравнение до кегля счётчика. Теперь тон меняет только цвет.

Про **69**: спарклайн убрали прошлым проходом, потому что дельта уезжала в левый
нижний угол, который старое правило «зоны углов» просило держать пустым. Правило
снято 29 августа решением «кнопок не боимся», а строка «состав дефолта» прямо
требует спарклайн — он вернулся.

Про **среднее по шагам**: строка «вид · столбики шагов» задаёт подпись «в
среднем 8 940» тоном чернил 42 %; код красил её состоянием. Состояние несут
столбики, подпись — нет.

### Второй дефект, который видно только глазами

Спарклайны «Веса за месяц» и «Тренда здоровья» рисовались чернилами вместо тона:
`color: currentColor` стоял на самом элементе и перебивал тон-класс. Строка
контракта «значение» говорит прямо — «роль красит число и линию». Тестами это не
ловилось: свойство в CSS присутствовало и было валидным.

Оба дефекта прохода — и схлопнутые столбики листов, и бесцветные спарклайны —
нашёл стенд на продуктовом CSS, а не тесты. Стенд стоит держать: он собирается
за минуту и показывает то, чего числа в правилах не показывают.

### Гейт

`widgets-bd-sheet-canvas-razbor` вырос с четырёх проверок до семи: добавились
таблица пар кадра Главной (`MAIN`) и проверка, что подложка листов — тот же
экран. Сверка у всех трёх таблиц теперь одна функция; проверено, что гейт
кусается — подмена `align-items` в шапке плитки его роняет.

### Снимок

Долг снимка упал с 3 492 строк `?` до 2 376. Закрыто 1 116:

- **11** — шапка приложения: строка «границы» отдаёт её продукту и зоне «шапка и
  дата», в кадре Главной она нарисована для контекста (вердикт `—`);
- **356** — строки без чисел и ролей: адресация разметки (`—`);
- **51** — сам кадр Главной: 40 строк таблицей пар и 11 повторов вида внутри
  кадра (столбики шагов, полосы карты, сегменты радара);
- **702** — подложка листов разбора: строка побайтово равна строке кадра
  Главной, вердикт взят оттуда со ссылкой на строку-донора.

Остаток 2 376 — числа кадров, которых никто не сводил: Шторка (10 кадров), Смена
вида (14), Быстрые действия (11), Кнопка настройки, Каталог, Расстановка.

## Проход 2026-08-31 (второй) — десять кадров «Шторка · …»

Сведён лист смены вида. Каркас у всех десяти кадров один — подложка, лист,
ручка, подпись «Как показывать плитку», список и строка варианта, — поэтому
таблица пар собирается из самого разбора по форме строки: подпись, галочку и
размер превью гейт находит сам и переживает перенумерацию кадра.

**UI-гейт:** цель — числа листа смены вида совпадают с кадром; главное действие
не меняется; слои не меняются; критическое не скрывать — выбранный вид
по-прежнему назван галочкой и подложкой, а не только цветом.

### Что нашла сверка

Каркас совпал везде, кроме одного места, и это оказался настоящий пробел.

**Строка выбора для 3×2 не складывалась.** Превью колец БЖУ шириной 218 px
занимает почти весь лист, и кадр «Шторка · Кольца БЖУ» ставит строку колонкой:
превью сверху, подпись и галочка своим рядом под ним. В коде был задел —
`wideStack` и обёртка `.widget-wd-sheet__preview-stack`, — но правила под неё не
существовало: обёртка ничего не делала, строка оставалась горизонтальной, и
подпись с галочкой жались справа от 218-пиксельного превью. Теперь у строки есть
модификатор `--stacked`, а подпись с галочкой уходят своим рядом
`.widget-wd-sheet__opt-row`; мёртвая обёртка убрана.

### Два отступления, названные вслух

- **Подложка.** Кадры рисуют dim `rgba(42,26,12,.42)`, продукт берёт его из
  токена `--v4-modal-backdrop-dim` (0.45) — инвариант product-модалок из
  `CLAUDE.md` старше кадра.
- **Нижнее поле листа.** Кадр даёт `18px`, код —
  `calc(18px + env(safe-area-inset-bottom))`. Лист стоит на нижнем крае экрана:
  без врезки его низ уезжает под домашний индикатор, а кадр рисует телефон без
  выреза.

Оба записаны в списке отступлений гейта; их стало пять.

### Снимок

Долг упал с 2 376 до 2 227 строк `?`. Закрыто 149:

- **70** — каркас листа в десяти кадрах (ручка, подпись, список, строка
  варианта) плюс подпись, галочка и размер превью в каждом;
- **20** — два отступления × десять кадров, вердикт `≠` с причиной;
- **50** — высота кадра, подложка Главной под шторкой и строки без чисел (`—`);
- **9** — превью вида «Как сейчас»: та же плитка, что на Главной, вердикт взят у
  строки-донора кадра Главной.

**Что осталось в шторке.** Около 250 строк — содержимое превью остальных видов
(«Строка», «Хватит на ужин», «К норме», «Окно сна» и прочие). Это не отдельная
работа: у каждого такого вида есть свой кадр в канвасе («Калории · Строка», «Сон
· Окно сна»), и превью повторяет его строка в строку. Следующий рычаг — свести
кадры видов; шторка закроется вместе с ними тем же наследованием по
строке-донору, каким закрылись подложки листов.

## Проход 2026-08-31 (третий) — доведение кадра Главной и урок про якоря

### Кадр переставили под контракт — и таблица по номерам сломалась

Дизайнер прислал обещанную правку: порядок плиток в кадре дефолтной раскладки
вернулся к контракту (Тренд → Вес → Динамика), элементы 61–73 перенумеровались.
Гейт это поймал — три пары показали расхождение, которого в коде не было: они
просто стали указывать на соседние элементы.

Вывод сильнее самой правки. Таблица шторки, собранная **по форме строки**,
перенумерацию пережила; таблица Главной, стоявшая **на номерах**, — нет. Поэтому
кадр Главной переведён на якоря: плитка опознаётся по своей приметной строке
(«плитка: фон var(--c2), поля 14px», ««в среднем 8 940»», ««низкий»»), остальное
берётся смещением внутри неё. Якорь, переставший быть единственным, гейт
называет вслух, а не сверяет молча чужой элемент.

### Что нашла сверка остатка

| Кадр               | Было в коде                     | Стало                                         |
| ------------------ | ------------------------------- | --------------------------------------------- |
| кольца БЖУ         | svg 56 × 56                     | 46 × 46 — строка «вид · кольца БЖУ»           |
| шапка риск-радара  | отступ 10 от общей строки героя | свои 9 px                                     |
| подписи под волной | 10 px/600 от подвала плитки     | 9,5 px/700 — строка «вид · инсулиновая волна» |

Кольца были на 22 % крупнее кадра и съедали воздух плитки 3×2 — это самое
заметное из трёх. Тест `widgets-v4-sphere` держал 56 px с прошлого прохода; он
обновлён по строке контракта.

Оба правила потребовали поднять вес селектора: `.widget-risk-scale-hero` и
`.widget-v4-hero-num` висят на одном элементе, а общее правило стоит ниже по
файлу; та же история у подписи волны против подвала плитки. Числа в правилах
были верные — побеждал каскад, и увидел это только стенд.

### Ложная тревога, которую сняли машиной

На снимке стенда «низкий» в риск-радаре читался капслоком. Проверка вычисленного
стиля показала `text-transform: none` и текст «низкий» строчными: в кириллице н,
и, з, к — буквы высотой в строчную, и при 24 px/600 слово выглядит как капитель.
Правка не понадобилась. Полезное правило: спорное на глаз проверять вычисленным
стилем, а не вторым взглядом.

### Снимок

Долг упал с 2 227 до 1 994 строк `?`. Закрыто 233: остаток кадра Главной (кольца
БЖУ поэлементно, сон, риск-радар, пустой экран, повторы вида) и каскадом 216
строк подложки в восемнадцати листах разбора — рычаг «один кадр → тысяча строк»
сработал второй раз.

Две находки ушли дизайнеру, обе структурные и кодом не закрываются: контракт
называет счётчику приёмов у волны два разных места (три строки — угол, одна —
под графиком), и кадр рисует нижний бар расстановки со стрелками отмены,
которого в продукте нет, хотя строка «сброс к дефолту» на эту стрелку ссылается.

## Проход 2026-08-31 (четвёртый) — одиннадцать кадров «Быстрые действия»

Сведена карточка плавающей кнопки: подложка, сама карточка, строка пункта,
разделитель, секция воды с чипами, обе кнопки и режим правки со скрытыми чипами.

**UI-гейт:** цель — числа карточки совпадают с кадрами; главное действие не
меняется (кнопка «+» остаётся доминирующей); слои не меняются; критическое не
скрывать — снятие пункта по-прежнему обратимо чипом возврата.

### Что нашла сверка

Четыре расхождения, все в состояниях, куда редко смотрят:

| Кадр                  | Было                                   | Стало                   |
| --------------------- | -------------------------------------- | ----------------------- |
| раскрыто · 22         | ряд чипов воды без отступа             | отбит от строки на 9 px |
| правка · скрытые · 22 | у ряда чипов возврата нет выравнивания | по центру ряда          |
| правка · скрытые · 23 | зазор внутри чипа 4                    | 5                       |
| правка · скрытые · 23 | высота чипа жёсткие 28                 | нижняя граница 28       |

Отступ у чипов воды — самое заметное: ряд «200 / 500» вплотную примыкал к строке
«Вода», и секция читалась одним комком.

### Две проверки, снявшие ложную тревогу

- Круг 22 px в режиме правки я сперва принял за иконку строки, которой в коде
  нет. Разметка кадра показала внутри знак минуса: это кнопка снятия пункта,
  `.widgets-quick-minus`, и она на месте.
- На стенде не рисовался разделитель карточки. Вычисленный стиль дал
  `display: inline` и высоту 0 — но у продукта это `<div>`, а `<span>` стоял в
  самом стенде. Правка не понадобилась.

Оба случая — про одно: спорное на глаз проверять разметкой и вычисленным стилем,
прежде чем звать это дефектом.

### Снимок

Долг упал с 1 994 до 1 811 строк `?`. Закрыто 183:

- **62** — элементы карточки по таблице пар в одиннадцати кадрах;
- **110** — подложка Главной под карточкой, высоты кадров и строки без чисел;
- **11** — копия карточки дословно: `labels` в `WidgetsQuickActionsFab` («Еда»,
  «Голод и энергия», «Активность», «Мессенджер») и чипы воды `[200, 500]`
  совпали со строкой «текст» каждого кадра.

Элементных строк в зоне «Быстрые действия» не осталось вовсе. Оставшиеся 93 —
контуры иконок из раздела «Разбор графики»: канвас рисует свои, и совпадение
путей отдельным решением не назначалось.

## Проход 2026-08-31 (пятый) — сверка копии, 47 кадров

Сведены не числа, а слова: раздел «Текст кадров дословно» даёт копию каждого
кадра по порядку, и здесь она сверена со строками продукта. Проверка отдельная
от геометрии по простой причине — расхождение в словах не ловит ни один гейт по
числам, а человек читает именно слова.

**UI-гейт:** цель — слова кадров произносит и продукт; главное действие не
меняется; слои не меняются; критическое не скрывать.

### Метод

Числа кадра — демо-данные, в коде на их месте подстановка, поэтому сравнивается
скелет: цифры и `${…}` схлопываются в один знак вместе с пробелами вокруг. Что
осталось не найдено — разложено на три класса, и каждый список закрытый:

- **не наши слова** (26) — имена продуктов из дневника человека, значения
  движка, демо-подписи стенда;
- **собрано подстановкой** (6) — фраза в коде та же, но подставляется не число,
  а число с единицей или со склонением, и скелет её не сводит;
- **отступления** (9) — каждое стоит на строке контракта или решении владельца.

### Что нашла сверка

Из 414 слов не совпало 65; после разбора настоящих правок вышло восемнадцать.

| Место                                | Было                       | Стало                                                |
| ------------------------------------ | -------------------------- | ---------------------------------------------------- |
| подписи видов в листе выбора         | 14 подписей своими словами | дословно по кадрам                                   |
| «Сон · к норме», «Недосып за 7 дней» | обе подписаны просто «Сон» | вид назван                                           |
| «Оценка · 7 дней»                    | подписи не было вовсе      | добавлена                                            |
| «Тренд · N дней»                     | короткое имя               | «Тренд здоровья · N дней» по строке «состав дефолта» |
| таблица дней в разборе шагов         | «вс», «пн», «сб»           | полные имена                                         |
| «нужно N клетки» в каталоге          | со строчной                | «Нужно» — это отдельное предложение                  |
| кикер ритма воды                     | ` Вода · 1,7 / 2,7 л`      | лишний пробел в начале убран                         |
| круглый час                          | «6 ч 0 мин»                | «6 ч», а во фразе «6 часов»                          |

Самое заметное — четырнадцать подписей видов: человек читает их в листе смены
вида каждый раз, когда меняет плитку, и они расходились с кадром почти все.
Самое стыдное — «сб — самый слабый день недели»: таблица разбора шагов брала
сокращения оси, и вывод читался обрывком.

### Две находки дизайнер закрыл в тот же день

- **Счётчик приёмов у волны.** Контракт называл ему два места — три строки
  говорили «правый верхний угол», одна «под графиком». Дизайнер переписал все
  три: счётчик под графиком, «угол занимает кружок удаления в режиме
  расстановки, и два элемента в одной точке спорят за касание». Код переехал;
  слово состояния («идёт волна») снято — кадр его не рисует, а тон счётчика и
  правая подпись говорят то же.
- **Бар расстановки со стрелками отмены.** «Не заводим: в продукте бара нет, а
  два механизма отмены рядом означают два разных понимания, что именно
  отменяется». Код не меняется.

### Гейт

`widgets-canvas-copy` — четыре проверки: раздел на месте, слова кадров
произносит продукт, счёт плиток в карточке сброса равен составу дефолта, списки
отступлений не разрослись. Проверено, что роняется подменой любой подписи вида.

### Снимок

Долг 1 811 → 1 765. Закрыто 47 строк «текст»: 42 совпали дословно, 5 помечены
`≠` с причиной (лист карты активности, каталог без места, качество еды, кикер
тренда, подпись кольца). Элементных строк это не касается — сверка слов идёт
своим измерением.

## Проход 2026-08-31 (шестой) — кнопка настройки: сведение не требовалось

Зона «Кнопка настройки» — 95 строк — сведена нулём правок кода, и это верный
результат, а не отговорка. Все три кадра помечены `data-demo="protocol"`, а
подпись протокола говорит: «Выбранный вид будет нарисован в четырёх палитрах,
два оставшихся — протокол сравнения». Строка «демо» и `CLAUDE.md` запрещают
реализовывать такие кадры: это отвергнутые варианты, а не задание.

Поведение кнопки задают не эти кадры, а строки контракта, и они давно закрыты:
«карандаш и кнопка настройки» (кнопка скрыта, пока раскрыта карточка; зазор 6
px), «пустой экран · кнопка настройки», «вход в расстановку» — все три `=` с
указанием на код.

### Что закрылось заодно

Правило `protocol` относится не только к этой зоне. По всему канвасу тринадцать
кадров помечены `protocol` и один — `loop`; их строки закрыты вердиктом `—`
одним проходом:

| Класс                                            | Кадров | Строк |
| ------------------------------------------------ | ------ | ----- |
| `protocol` — отвергнутые варианты                | 13     | 92    |
| `loop` — «живой прогон», петля только на канвасе | 1      | 29    |

Среди отвергнутых, кроме трёх кнопок: четыре кадра «Добавление · N колонок», два
хвоста раскладки и четыре варианта «Калории без заголовка».

### Машинная защита вместо памяти

Правило перестало держаться на внимании: гейт `widgets-bd-sheet-canvas-razbor`
получил проверку «ни одна таблица пар не сводит код с отвергнутым кадром» — она
падает, если в таблицу попадёт кадр, не помеченный `stop`. Проверено
подстановкой «Кнопка настройки · 40 малая»: гейт называет его поимённо.

### Открытый вопрос дизайнеру

В продукте стоит третий вариант, 40 px, тот самый, чей минус кадр называет сам —
«легко не заметить». Это единственный вход в расстановку экрана. Запись заведена
в [`UI_V4_FINDINGS.md`](../ui/UI_V4_FINDINGS.md): выбор нужно назначить, а не
оставить по умолчанию.

### Снимок

Долг 1 765 → 1 644.

## Проход 2026-08-31 (седьмой) — смена вида, десять кадров

Зона распалась надвое, и это главное наблюдение прохода: шесть кадров из десяти
оказались тем же листом выбора вида, что и «Шторка», просто названным иначе —
«Шторка · …» для видов первого пакета и «Смена вида · <виджет>» для шести видов
пакета 22 августа. Разметка у них одна.

**UI-гейт:** цель — состояния смены вида совпадают с кадрами; главное действие
не меняется; слои не меняются; критическое не скрывать — подтверждение смены
остаётся видимым.

### Одна таблица вместо двух

`shutterPairs` расширена с десяти кадров до шестнадцати. Каркас искался по
фиксированным номерам 11–14, но у «Смена вида» нумерация другая: кадры «Шторка»
показывают экран под листом и считают с него, а «Смена вида» — только лист.
Поиск переведён на форму строки: каркас находится по своей первой строке, ручке
36 × 4, и дальше идёт смещением. Все шесть новых кадров совпали без единой
правки кода — лист уже был сведён.

### Три состояния

| Кадр         | Что проверяли                                       | Итог          |
| ------------ | --------------------------------------------------- | ------------- |
| удержание    | плитка scale(.965) + обводка 2 px, пилюля подсказки | совпало       |
| выбор принят | плитка на выходе: прозрачность и scale(.94)         | совпало       |
| новый вид    | подтверждение «вид сохранён»                        | **разошлось** |

Подтверждение было тёмным тостом внизу экрана, а кадр рисует его **шалфейным
близнецом пилюли удержания**: тот же силуэт (поля 0 14, высота от 30, радиус
999, 10,5 px/700), другой тон — `rgba(122,138,94,.18)` и `--gr` вместо
терракоты. Один силуэт на два сообщения читается как одна механика: держишь —
терракота, сменил — шалфей. Тост убран вместе со своим CSS и анимацией.

### Подсказка жеста: контракт переписан

Строка «вид подсказки жеста» больше не описывает тёмную плашку. Теперь это
«полоса над нижней навигацией: заливка `--tint`, значок 22 px тоном `--ac`,
заголовок 12 px/700 чернилами, подпись 11 px/1,45 чернилами 60 %». В коде стоял
`#201e1d` с комментарием «контракт задаёт плашке зашитый фон» — комментарий
относился к снятой редакции строки и убран вместе с ней. Значок был 17 px.

### Снимок

Долг 1 644 → 1 426. Закрыто 218: лист смены вида в шести кадрах (138), состояния
и подложка под ними (70), разметка (10).

## Проход 2026-08-31 (восьмой) — графики листов разбора

Самая большая зона: 426 открытых элементов в восемнадцати листах. Разложилась на
три класса, и только третий потребовал ручной работы.

### Хвост листа — один на восемнадцать

Разбор числами, норма и действие устроены одинаково во всех листах, но номера
элементов у каждого свои. Таблица `SHEET_TAIL` собирается из разбора по форме
строки: каждая форма внутри кадра встречается один раз («повторы одного вида
внутри кадра свёрнуты»), поэтому якорь однозначен. Шесть форм × восемнадцать
листов сошлись с одной правкой — запасное значение у кнопки было `#cf8144`,
значение тёмного набора, при том что все соседние обращения к той же роли берут
песочное `#c67139`. Роль определена всегда, так что цвет был верен; неверна была
подпись, и это ловушка для следующего читателя.

### Повтор с другим числом

65 строк — тот же элемент кадра, отличающийся только данными: четырнадцать полос
сна разной ширины, двенадцать столбиков воды разной высоты. Они закрываются
вместе с первой строкой своей формы: правило одно, число разное.

### Графики: пять расхождений

| Кадр                  | Было                                            | Стало                                        |
| --------------------- | ----------------------------------------------- | -------------------------------------------- |
| БЖУ · сетка попаданий | зазор 4 и между строками, и внутри              | 5 между строками, 6 внутри                   |
| БЖУ · клетка          | радиус 3, попадание — 35 % шалфея               | радиус 4, попадание сплошное `--gr2`         |
| БЖУ · дорожка героя   | без отступа от подписи                          | 5 px                                         |
| Карта активности      | CSS-сетка 7 колонок, зазор 3, квадратные клетки | строки недель, зазор 4, клетка 15 радиусом 4 |
| **Шаги · столбики**   | скругление 2 по всем углам                      | **2 px только сверху**                       |

Последнее нашлось не глазами и не сразу: разбор радиуса брал из фразы «радиус
2px 2px 0 0» только первое число и сравнивал его с `border-radius: 2px` — оба
давали «2», и гейт молчал. Починил разбор многозначного радиуса — и он тут же
показал, что столбики скруглены снизу, хотя стоят на общей базовой линии.

Строка контракта «разбор · БЖУ» говорит «клетка тоном --gr2 при попадании» — код
разбавлял его до 35 %, и попадание читалось бледнее промаха соседней строки.

### Снимок

Долг 1 426 → 1 218. Закрыто 208: хвост во всех листах (67), головы графиков
(22), повторы с другим числом (65), подложка и разметка (54).

Осталось 433 элемента и 785 контуров иконок. Элементы — внутренности графиков
шести листов, где кадр рисует то, чего в продукте нет отдельным правилом
(спарклайны, циферблат, профили по часам): их сведение идёт лист за листом.

## Проход 2026-08-31 (девятый) — разбор таблицей

Контракт называет два разных способа показать числа под графиком, и код знал
только один. «Разбор числами» — две-три производных величины с зазором 11 и
светлой подписью. «Разбор таблицей» — строки «разбор · Шаги» (семь дней недели)
и «разбор · Динамика веса» (четыре недели): строк много, они однородны, и между
ними нужна сетка отступов, а не зазор.

|         | разбор числами            | разбор таблицей                      |
| ------- | ------------------------- | ------------------------------------ |
| строки  | зазор 11                  | отступы 6 сверху и снизу, зазора нет |
| подпись | 500 11/1,3 чернилами 50 % | 600 11/1 чернилами, красится тоном   |
| число   | 700 12/1                  | 700 11,5/1                           |
| итог    | нет                       | своя строка через линию, отступ 10   |

Тон в таблице красит и подпись, и число: в кадре «Четверг» и «10 100» оба
шалфейные, «Суббота» и «4 900» оба красные. В разборе числами красится только
число — там строк мало, и цветная подпись читалась бы как ошибка.

Профиль по часам у риск-радара — тот же, что у воды, только со своими номерами
элементов; он закрылся парами без правок.

### Снимок

Долг 1 218 → 1 193.

Осталось 408 элементов и 785 контуров иконок. Крупное — подложка главного экрана
внутри листов и шторок в редакции до перестановки плиток: кадры показывают
прежний порядок и виды 2×2, которых на Главной сейчас нет, поэтому наследование
по строке-донору на них не работает. Это не дефект кода: те же элементы описаны
своими кадрами видов, и закроются вместе с ними.

## Проход 2026-08-31 (десятый) — повторяющиеся формы

Остаток перестал быть списком строк и стал списком форм: 408 открытых элементов
дали всего 170 разных значений, а верхние семь встречались по восемнадцать-
девятнадцать раз — это один и тот же элемент продукта, нарисованный в подложке
каждого листа и каждой шторки.

Отсюда третий вид таблицы в гейте, после «кадр → элемент» и «кадр → форма внутри
кадра»: **форма → правило, во всех кадрах сразу**. Четыре формы — крупное число
героя 26 px, столбик листа разбора, дорожка тройного героя и сегмент недели
волны — закрыли 60 строк одной парой каждая.

Ещё 19 строк — шапка приложения внутри кадров виджетов: строка «границы» отдаёт
её зоне «шапка и дата», в этих кадрах она нарисована для контекста.

Таблица форм отсеивает отвергнутые кадры сама, не полагаясь на соседний гейт:
`shapePairs` берёт только кадры `data-demo="stop"`.

### Снимок

Долг 1 193 → 1 111. Элементов осталось 326, контуров иконок — 785.

## Проход 2026-08-31 (одиннадцатый) — дельта веса и лента окна до сна

Две правки и конец рычагов.

**Дельта недели у веса 2×2** шла кеглем 10 px, тогда как на плитке 2×1 та же
строка «−0,9 за неделю» стоит 9 px, и кадры дают 9 в обоих видах. Класс
`.widget-v4-delta` используется ровно в одном месте, так что правка точечная.
Одна строка закрыла 18 подложек: она нарисована в каждом листе разбора.

**Лента «Окно до сна»** — та же лента, что у сна, только со своими номерами
элементов; закрылась парами без правок кода.

### Где кончается рычаг

Долг 1 111 → 1 079: элементов 294, контуров иконок 785. На 294 строки приходится
**157 разных форм** — то есть почти каждая оставшаяся строка теперь единичная.
Три вида таблиц, которые дали основной результат, здесь уже не работают:

- «кадр → элемент» требует пары на каждую строку;
- «кадр → форма внутри кадра» ловит повторы, а повторов почти не осталось;
- «форма → правило во всех кадрах» ловит общее, а общее закрыто.

Оставшееся — содержимое превью отдельных видов в шторках («Калории · Строка»,
«Сон · Долг за неделю», «Кольца БЖУ · Три полосы») и подложка листов в редакции
до перестановки плиток. Это работа лист за листом и вид за видом, без множителя;
её честнее делать отдельной задачей на конкретный виджет, а не сплошным
проходом.

785 контуров иконок остаются вопросом решения, а не работы: должны ли пути в
продукте совпадать с канвасом побайтово, никем не назначено.

## Проход 2026-08-31 (двенадцатый) — контуры иконок

785 строк раздела «Разбор графики» до сих пор считались вопросом решения: надо
ли, чтобы пути в продукте совпадали с канвасом побайтово. Оказалось, что это не
вопрос, а непроверенная проверка — и она нашла настоящий дефект.

### Две иконки из пяти были не те

| Действие        | Кадр   | Было в продукте |
| --------------- | ------ | --------------- |
| Голод и энергия | молния | круг с часами   |
| Активность      | пульс  | ломаная графика |

Часы говорят «время», ломаная — «отчёт», а карточка быстрых действий про
действие сейчас. Молния и пульс не украшение: они называют то, ради чего человек
нажимает. Иконку с другим контуром не поймает ни один гейт по размерам — размер
и там и там 17 px.

### Что показала сверка

Из 96 контуров в кадрах `stop` **89 совпали дословно**, семь названы поимённо:
пять принадлежат шапке и капсуле даты (строка «границы» отдаёт их своим зонам),
два — шеврон строки и галочка выбранного вида — та же форма, записанная от
другой точки пути.

Продукт вправе разбить один контур кадра на несколько `<path>`: сверка это
учитывает и разрезает путь кадра по `M`.

### Классы остальных строк раздела

| Класс                                                                  | Строк | Вердикт |
| ---------------------------------------------------------------------- | ----- | ------- |
| контур совпал дословно                                                 | 89    | `=`     |
| чужая зона или та же форма от другой точки                             | 103   | `—`     |
| форма считается по данным человека (ломаные, линии, точки спарклайнов) | 305   | `—`     |
| поле рисунка стоит в коде тем же числом                                | 125   | `=`     |
| осталось непроверенным                                                 | 163   | `?`     |

### Снимок

Долг 1 079 → 457: элементов 294, контуров 163. Впервые за сессию доля закрытого
перевалила за девять десятых зоны.

## Проход 2026-08-31 (тринадцатый) — поля рисунков

Раздел графики закрыт до одной строки. По пути нашлась вторая находка того же
рода, что иконки: невидимая числам разница, которую видно только если спросить.

**График веса в листе рисовался в поле 300, а профиль воды и сплайн тренда —
в 268.** Само по себе это не мешало бы: у всех трёх
`preserveAspectRatio="none"`, и поле растягивается в контейнер. Но именно
поэтому ширина поля задаёт масштаб линии по горизонтали: при 300 та же толщина
1,6 px сжималась сильнее, чем при 268, и одна линия читалась в трёх листах
по-разному. Кадр даёт 268 везде.

Гейт получил проверку «графики листов рисуются в поле одной ширины»: все
`viewBox` шире 200 обязаны иметь одну ширину. Это правило, а не число, и оно
переживёт смену размера — сломается только на расхождении между листами.

### Снимок

Долг 457 → 295. Контуров осталось **одна строка**, элементов 294.

## Проход 2026-08-31 (четырнадцатый) — четыре вида БЖУ и каталог

Два кадра из остатка: «Шторка · Кольца БЖУ» (27 строк) и «Каталог · нет места»
(16). Оба выглядели бухгалтерией — оказались работой.

### Ложная тревога, которую стоит записать

Первое, что нашлось в кольцах: подпись под кольцом вместо подписи над ним, факт
внутри кольца вместо остатка, «/ 150» отдельной строкой. Строка контракта
«кольцо БЖУ» говорит ровно обратное — «подписи нутриентов над кольцами, остаток
внутри, „факт / норма“ под кольцом», — и на секунду показалось, что флагманская
плитка съехала целиком.

Не съехала. Я читал `.macro-ring-*` — старое семейство, которое обслуживает
размеры вне v4 (3×1, запасной путь `canUseMacroRings`). Кадры рисуют
`v4SageRing`, и он собран верно: подпись сверху, `svg` 46×46, радиус 18, толщина
5, дуга от −90°, остаток внутри, «96 / 150» снизу. Урок тот же, что с
`.widget-v4-heat__bar--dN`: **имя класса, похожее на нужное, — ещё не тот код,
который рисует кадр.** Проверять надо от точки входа вида, а не от поиска по
имени.

### Что действительно разошлось

| где                        | было                                 | стало                          | чем подтверждено                                    |
| -------------------------- | ------------------------------------ | ------------------------------ | --------------------------------------------------- |
| кольца, «96 / 150»         | косая красилась тоном состояния      | косая и норма — чернила 42 %   | кадр пишет « / 150» одним узлом                     |
| три полосы 2×1, заполнение | недобор белка оставался зелёным      | тот же компаратор, что у чисел | строка контракта «БЖУ»                              |
| три полосы, числа          | янтарь на отклонении, зелень в норме | `--val-bad` и чернила          | элементы 33 и 35                                    |
| три полосы, «Б»            | обычный вес строки                   | класс ключа, интерлиньяж 1     | элемент 30                                          |
| «что выбивается»           | тон по знаку дельты                  | тон по компаратору нутриента   | элементы 44 и 45                                    |
| «что выбивается», герой    | отступ сверху 6px                    | 9px                            | элемент 40                                          |
| «Только белок» 1×1         | «96 г» — ни нормы, ни полосы         | «96 /150» и дорожка до нормы   | элементы 48–50                                      |
| «96 / 150» в кольце        | ломалось на две строки при 198/180   | `white-space: nowrap`          | строка контракта «длинное значение в узком формате» |
| каталог, строка нехватки   | зазор 8px                            | 10px                           | элемент 15                                          |

Главное здесь — не отступы. **Знак дельты не говорит, плохо это или нет**:
недобор белка плох, недобор жиров — нет. Три вида БЖУ из четырёх решали это
каждый по-своему, и два решали неверно; строка контракта «БЖУ» прямо требует
один компаратор с флагом направления, и теперь все три зовут
`macroDeviationBad`. Вид «Только белок» вовсе не отвечал на свой единственный
вопрос — далеко ли до нормы.

### Каталог: кадр спорит с контрактом

Кадр рисует каталог одной колонкой, каждое превью 68×64, справа имя виджета и
строка «вид · формат». Продукт рисует сетку в две колонки, где превью — сама
плитка своего размера. Верен продукт: строка контракта «каталог» требует
рисовать плитку «в её дефолтном виде и формате… тем же кодом, что на Главной», а
единая рамка 68×64 этого сделать не может. Кадр к тому же спорит сам с собой —
подписывает строку «Месяц целиком · 2×2», рисуя рядом превью размером 1×1.
Записано вопросом дизайнеру; строки 09, 13 и 14 снимка стоят `≠` с этой
причиной.

### Гейт

Таблица `MACROS` — 18 пар по четырём видам, привязанных к якорям кадра. По пути
закрылась дыра в разборщике: кадр пишет интерлиньяж и внутри шрифта
(`700 9px/1`), и отдельным словом («интерлиньяж 1»), а читалась только первая
форма — то же самое, что было с радиусом из четырёх чисел.

### Снимок

Долг 295 → 252. Закрыто 43 строки двух кадров; из них пять — `≠` с названной
причиной.
