# Вкладка «Актив» — сведение с канвасом v4

Протокол реализации для ревьюера. Начат 2026-08-31.

**Канвас:** `tab-activity.v4.dc.html` — 279 строк контракта в 10 разделах, 10
кадров `data-demo="stop"` на 375 px, 2 кадра `protocol` (не реализуются).
**Приёмка:** `ACCEPTANCE-tab-activity.md`. **Разбор «как есть»:**
[ACTIVITY_TAB_AS_IS.md](ACTIVITY_TAB_AS_IS.md).

Метод сверки — пары «класс кадра → правило продуктового CSS»: геометрия канваса
лежит в классах его `<style>` (`.grp`, `.cd`, `.row`, `.k`, `.v`), как у питания
и виджетов, а не инлайном в кадрах.

---

## 0. Чем это оказалось

Не косметическим сведением. Контракт содержит два решения владельца от 31
августа, которые меняют поведение, и переносит на вкладку три механики, которых
на ней не было. Полный список работ — §1; он выведен из контракта, а не
придуман.

Предусловия сняты до начала: масса тела в тоннаже дня и механизм оценки шагов
закрыты 31 августа (ACTIVITY_TAB_AS_IS.md §15.1, §16), поэтому кадры «шаги
оценены» и строка тоннажа больше не помечаются `—`.

---

## 1. План по блокам

| Блок | Что                                                                           | Строки контракта                                               | Статус     |
| ---- | ----------------------------------------------------------------------------- | -------------------------------------------------------------- | ---------- |
| 1    | Разбор цели приходит к «Цель дня»; состав строк; максимум две строки поправки | 8–11, 28, кадр «разбор цели»                                   | **сделан** |
| 2    | Ярус «Сегодня» — три строки вместо аккордеона «Кардио»                        | 6, 15, 32, кадр «день собран»                                  | **сделан** |
| 3    | Программа куратора выше яруса: три элемента, порядок, взаимоисключение        | 7, 31, кадры «план назначен», «правка куратора», «день отдыха» | **сделан** |
| 4    | Шаги: пилюля «оценка», правка цели тапом с датой открытого дня                | 12–14, 29, 30, кадр «шаги оценены»                             | **сделан** |
| 5    | История: календарь с горизонтом, легендой и листанием месяцев                 | 16, 19, 20, 23, кадр «Календарь зарядки»                       | **сделан** |
| 6    | История: рост рабочих весов и два пустых состояния                            | 17, 18, 25, кадр «Рабочие веса»                                | **сделан** |
| 7    | Тренировки за месяц от открытой даты; планы не в счёт                         | 26                                                             | **сделан** |
| 8    | Лист действия: три пункта, занятый слот исчезает                              | 22                                                             | **сделан** |
| 9    | Причина пропуска зарядки: статус начинает писаться                            | 21                                                             | **сделан** |
| 10   | Пустые состояния яруса «История» и строк «не отмечено»                        | 33, кадр «новый человек»                                       | **сделан** |
| 11   | Тест сверки + вердикты в снимок (`--rehash tab-activity`)                     | —                                                              | **сделан** |

Блоки 9 и 5 — не сведение, а новая механика: контракт вернул листание календаря
и завёл запись статуса пропуска, которая до сих пор была недостижима
(ACTIVITY_TAB_AS_IS.md §9 P). Названы отдельно, чтобы ревьюер не искал их в
диффе сведения.

---

## 2. UI-гейт

`UI-гейт: цель — человек видит, из чего вышла цель дня и что сегодня с активностью; главное действие — «Добавить активность»; слой 1 — цель, программа, три строки яруса «Сегодня», кнопка действия; слой 2 — разбор цели, карточка силовой с журналом, календарь и месяц, лист действия; критическое не скрывать — назначенная тренировка и правка куратора стоят на первом слое выше яруса.`

---

## 3. Ход работ

### Блок 1 · Разбор цели — сделан 2026-08-31

Было: цепочка «BMR → + Шаги → + Быт → + Тренировки → База (без TEF) → + TEF →
Затраты → Дефицит» кончалась процентом. В день с долгом человек видел 2 210
сверху и приходил к 1 940 внизу.

Стало, по строке 9 контракта: «Базовый обмен → Шаги → Быт → Тренировки →
[Тренировка вчера] → База без термического эффекта (подпись «от неё считается
цель») → Термический эффект еды (подпись «в затратах есть, в цели нет») →
Затраты → Дефицит по договорённости → [Цикл] → [одна причина уровня] → Цель
дня».

Причина уровня считается как разница показанной цели и базовой (`displayOptimum`
минус `optimum`), а не пересобирается из слагаемых: так строка сходится с числом
под ней, чем бы поправка ни была посчитана. Ветки взаимоисключающие, как в
`displayOptimum` — держит тест «день загрузки вытесняет долг».

Проброшены `optimum` и `cycleKcalMultiplier`: без первого нечем показать
величину поправки, без второго — строку цикла.

Разметка переехала с `.activity-v4-hero__breakdown` на `.activity-v4-breakdown`
списком `.cd`: на первой поверхности, радиус 20, поля 2/16, строка 13 px по
вертикали, разделитель 1 px, у последней нет — строка 28 контракта.

Проверено: 37 сценариев в
[activity-numbers-net-and-plan.test.js](../../apps/web/__tests__/activity-numbers-net-and-plan.test.js),
из них 10 на разбор — состав и порядок строк, появление строки только при
значении больше нуля, ровно одна причина уровня, дословные подписи под ключами.
Цветовой гейт `scripts/ui-v4-check-undefined-roles.mjs` зелёный: новых ролей не
заведено.

### Блок 2 · Ярус «Сегодня» — сделан 2026-08-31

Аккордеон «Кардио» снят. Под этим именем лежали семь сущностей — программа,
назначенная силовая, правка куратора, силовые с журналом, хобби, «Пальцы» и
бытовая активность; кардио было одной из них. Блок целиком пропадал в пустой
день и уносил с собой строку программы.

Стало: список `.cd` из трёх строк, которые стоят всегда — «Тренировки», «Бытовая
активность», «Зарядка». При отсутствии данных — «не отмечено» 11 px/600 чернил
38 %, а не исчезновение строки.

Значение строки тренировок читается четырьмя состояниями:

| Что в дне                 | Значение                                                        | Почему                                                                             |
| ------------------------- | --------------------------------------------------------------- | ---------------------------------------------------------------------------------- |
| выполненные тренировки    | сумма ккал, под именем состав («силовая 45 мин · 1,9 т объёма») | тоннаж фактический, по отмеченным подходам                                         |
| только назначенный план   | «не начаты»                                                     | план до старта не даёт расхода, и ноль читался бы как «тренировался и не потратил» |
| ничего, но программа есть | «день отдыха»                                                   | **переезжает в блок 3** — состояние требует состояния программы                    |
| ничего и программы нет    | «не отмечено»                                                   | —                                                                                  |

Аккордеон остался только внутри тренировок: тап по строке раскрывает карточки с
журналом подходов.

Проверено вживую на 375 px: ярус даёт «Тренировки · силовая 45 мин · 1,9 т
объёма · 385 ккал», «Бытовая активность · 60 мин · 126 ккал», «Зарядка · была»
тоном роста — построчно как в кадре «день собран». Разминочные подходы в объём
не пошли (1900 кг из 2300 сырых). 47 сценариев смоука, из них 10 на ярус.

**Следствие, которое стоит увидеть:** строка «Голод и энергия» из яруса ушла —
контракт называет три строки, и её среди них нет. Точка входа сохранилась в
FAB-стопке (`onOpenHunger`, heys_day_page_shell.js) и на «Питании». Вместе с ней
убран `readHungerSummary`, читавший хранилище на каждый рендер — это заодно
закрывает дефект O разбора «как есть».

Свёрнутая строка «Отметить», собиравшая пустые пункты в один ряд, снята: по
контракту каждая строка стоит своя.

### Блок 3 · Программа куратора — сделан 2026-08-31

Три элемента переехали выше яруса: правка куратора, карточка назначенного плана,
строка «Следующая тренировка». Назначенная на сегодня тренировка — самое важное
на экране, и она не может жить за свёрнутым чевроном.

**Как сделано.** Не вынесением карточек из `renderTrainingsBlock`, а третьим
режимом того же прохода — `trainingFilterMode: 'program'`. Карточки остались
там, где живёт вся их машинерия (`patchTraining`, `openBuilder`,
`moveOptionsFor`, `Parts.*`); разделился только отбор:

| Режим     | Что берёт                                                                                                                      |
| --------- | ------------------------------------------------------------------------------------------------------------------------------ |
| `program` | назначенное (`assigned`, `skipped`) и то, на что клиент не ответил (`pendingPlanProposal`), плюс строка «Следующая тренировка» |
| `regular` | всё остальное — факт дня                                                                                                       |

Отбор развёл `isProgramEntry`: без него одна и та же карточка встала бы дважды —
над ярусом и внутри него.

**Строка «Следующая тренировка» больше не исчезает.** Раньше режим возвращал
`null`, когда в дне нет ни тренировок, ни быта, — и строка программы пропадала
вместе с блоком. Теперь режим `program` возвращает `null` только когда нет
клиента: строка и есть его содержимое в день без назначенного.

**«День отдыха».** Состояние строки тренировок, обещанное блоком 2: программа
ведётся, назначенного на сегодня нет, сделанного нет. «Не отмечено» здесь
читалось бы как забывчивость. Приоритет: назначенное → день отдыха → не
отмечено.

**Мёртвый блок закрыт попутно.** Блок программы занял слот
`chargeTrainingBlock`, который считался на каждой смене тренировок и
выбрасывался — `ActivityTabV4` его не разбирал (дефект M разбора «как есть»).
Одна вычислительная ветка вместо двух.

Проверено вживую на 375 px, три дня подряд:

| День                        | Порядок на экране                               | Строка тренировок |
| --------------------------- | ----------------------------------------------- | ----------------- |
| программа, назначенного нет | ПРОГРАММА → Сегодня → ярус → Действие → История | «день отдыха»     |
| программы нет               | Сегодня → ярус → Действие → История             | «не отмечено»     |
| назначен план               | ПРОГРАММА → Сегодня → ярус → Действие → История | «не начаты»       |

Блок программы отрисован ровно один раз в каждом случае. 56 сценариев смоука (9
новых), смежные наборы зелёные — 174 теста, включая `program-week-overview`,
`kernel-training-plan`, `strength-proposal-ui` и `curator-actions-banner`.

### Блок 4 · Шаги — сделан 2026-08-31

**Оценка помечена.** Механизм подстановки медианы включён 31 августа (решение
«Б», ACTIVITY_TAB_AS_IS.md §16), и состояние наконец существует — до этого
рисовать пометку было не на чем. Признак `stepsEstimated` проброшен от
`buildEnergyContext` до карточки.

| Что               | Обычный день                     | Оценка                                                                                                                |
| ----------------- | -------------------------------- | --------------------------------------------------------------------------------------------------------------------- |
| перед числом      | —                                | пилюля «оценка» 9 px моноширинной                                                                                     |
| число             | чернила основные                 | приглушено                                                                                                            |
| заливка полосы    | шалфейная                        | гаснет                                                                                                                |
| подпись справа    | «факт — ползунком, цель — тапом» | «поставьте факт ползунком»                                                                                            |
| сноска в карточке | —                                | «Числа за этот день нет — взята медиана ваших последних 14 дней. Она участвует в расходе и в цели, поэтому помечена.» |

Подпись при оценке меняется намеренно: править ползунком подставленное число
бессмысленно, пока его не заменили своим.

**Цель правится тапом, и правится нужному дню.** Тап по числу цели открывает тот
же шаг чек-ина — цель это план дня, а не настройка: её спрашивают каждое утро,
считают от медианы с модификаторами по сну и энергии, и поля цели в профиле нет
вовсе.

Починена передача даты: `showCheckin.steps` был единственным из `showCheckin.*`
без `context.dateKey`, поэтому с прошлой даты план уезжал сегодняшнему дню.
Порядок аргументов сделан как у `weight` и `sleep` — первым может прийти
`onComplete`, старые вызовы не ломаются.

**Живая проверка поймала два расхождения**, которых тесты не видели:

1. полоса была 5 px против 8 в контракте — поправлено;
2. приглушение заливки не побеждало базовое правило: одинаковая специфичность, а
   базовое стоит ниже по файлу. Селектор из двух классов, причина названа
   комментарием.

Оба — случай, ради которого живой прогон и делается: смоук проверяет разметку и
текст, а вычисленный стиль — только браузер.

65 сценариев смоука (9 новых), из них два источниковых — на передачу даты в шаг
и в обработчик дня.

### Блок 5 · Календарь зарядки — сделан 2026-08-31

Первая из двух новых механик контракта. Три вещи.

**Горизонт от первой отметки.** Прежде любой прошлый день без записи считался
пропуском — включая дни до установки приложения: первый экран нового человека
давал 27 красных точек и «0 из 28». Теперь `findHabitHorizonKey` ищет первый
день с записью; всё раньше него — серое «не вели», отдельный статус `none`, а не
нейтраль «сегодня». Заглядываем назад на 180 дней: если запись есть и раньше
окна, горизонта нет и считается всё окно.

**Знаменатель считает веденные дни.** «Зарядка · 4 из 5», а не «4 из 28»:
считать «из 28» там, где половина окна старше первой отметки, значит записывать
в пропуски время до установки.

**Легенда и листание вернулись в раскладку вкладки.** Обе были собраны в ветке
`!isActivityV4` — то есть кнопка «Месяц» вела в сетку без подписи, какой это
месяц, и без стрелок. Теперь строка периода общая для обеих раскладок, легенда —
ряд из четырёх пар «точка + слово».

Подпись месяца собирается своим форматтером: `toLocaleDateString` с month+year в
русской локали даёт «август 2026 г.» — кадр просит «Август 2026», а
`text-transform` чинил бы только регистр, но не « г.».

Живая проверка на 375 px, четыре дня привычки из 28-дневного окна:

| Что       | Результат                                                          |
| --------- | ------------------------------------------------------------------ |
| клетки    | 4 сделано, 23 «не вели», 1 нейтральная (сегодня), **красных ноль** |
| заголовок | «Зарядка · 4 из 5»                                                 |
| легенда   | сделана / тренировкой / пропуск / не вели, тоном каждая            |
| листание  | «Август 2026» → стрелка назад → «Июль 2026»                        |

Два зелёных в легенде («сделана» и «тренировкой») в песочной палитре совпадают —
так устроен сам набор, и это ровно та причина, по которой контракт просит
подписи словами, а не различение тоном.

10 сценариев в
[activity-calendar-horizon.test.js](../../apps/web/__tests__/activity-calendar-horizon.test.js):
горизонт, знаменатель, снятие горизонта записью раньше окна, отсутствие записей
вовсе, замена тренировкой, легенда, листание, отсутствие строки листания в
режиме 28 дней. Смежные наборы зелёные — 105 тестов.

### Блок 6 · Рост рабочих весов — сделан 2026-08-31

Метрика была написана и жила только внутри поправки на факт, где она **довод** и
звучит отрицательно: «норму не трогаем». На вкладке она **факт о тренировках**,
и формулировка положительная — «Рабочие веса · +4 %» с составом «за 4 недели · 6
общих упражнений».

Окно собирается в дне: 28 дней от открытой даты, дни без записи пропускаются.
Модуль лежит в постбут-чанке, порядок загрузки не гарантирован — пока его нет,
строки просто нет, и deferred-рендер вернётся сюда сам.

**Два пустых состояния, и это разные фразы:**

| Причина          | Под именем                       | Справа                 |
| ---------------- | -------------------------------- | ---------------------- |
| данных мало      | «данных 9 дней из 14»            | «рано сравнивать»      |
| сменил программу | «программа сменилась 15 августа» | «нет общих упражнений» |

Тона у обеих одинаковые и приглушённые: ни одна не результат, и красить их как
плохой результат нельзя. «Нет общих упражнений» не значит «веса не растут» —
сравнивать нечего, а не результат плохой.

**Дата смены программы отдаётся самой метрикой**, а не считается на поверхности:
определение принадлежит владельцу метрики. Ею считается первый силовой день
второй половины окна — раньше него сравнивать было с чем, позже начинается то,
что сравнивать не с чем. Туда же добавлены `haveDays`/`needDays`, чтобы
поверхность могла сказать «9 дней из 14», а не просто «рано».

Ноль отличён от падения: приглушён, а не покрашен тревогой.

Живая проверка на 375 px, три окна подряд: рост 80→85 и 40→44 по двум общим
упражнениям дал «+7,5 %» тоном роста; смена программы — «программа сменилась 15
августа · нет общих упражнений»; девять дней — «данных 9 дней из 14 · рано
сравнивать». Обе пустоты одним тоном.

75 сценариев смоука (10 новых), из них два на саму метрику. Цветовой гейт
зелёный.

### Блок 7 · Тренировки за месяц — сделан 2026-08-31

Окно кончалось сегодняшним днём независимо от того, какой день открыт: при
листании на 10 августа шапка была про этот день, а список — про последние 30
дней от сегодня. Два разных окна на одном экране.

Теперь `collectMonthTrainingRows` принимает `anchorDate` — открытую дату; без
неё поведение прежнее, от сегодня.

Вторая половина строки 26 — «назначенные и пропущенные планы в счёт не идут» —
была закрыта раньше, дефектом E разбора «как есть» (коммит `c2eff04e2`).

Живая проверка на 375 px, открытый день 10 августа при сегодня 31-м:

| Проверка                                | Результат                    |
| --------------------------------------- | ---------------------------- |
| тренировка в открытый день              | попала                       |
| тренировка 20 августа (позже открытого) | не попала                    |
| она же при якоре на сегодня             | попала — значит дело в якоре |
| граница окна: 12 июля / 11 июля         | попала / не попала           |
| назначенный план                        | в счёт не идёт               |

79 сценариев смоука (4 новых).

### Блоки 8 и 10 · Лист действия и пустая «История» — сделаны 2026-08-31

Взяты одним заходом: оба мелкие и оба трогают один файл.

**Лист действия.** Три пункта в порядке контракта, у каждого под именем сказано,
что именно спросят: «Тренировка · время, тип, минуты по зонам», «Зарядка ·
сделал · сделаю · не сегодня», «Бытовая активность · минуты». Без подписи
«Зарядка» и «Тренировка» выглядят одинаково весомо, хотя спрашивают разное.

Занятый третий слот **убирает** пункт «Тренировка», а не гасит его: погашенная
кнопка обещает действие, которого нет. Это работало и раньше — контракт
подтвердил, тест закрепил.

Зарядка стала показываться всегда, а не только пока не отвечена: «сделал ·
сделаю · не сегодня» — выбор, который человек вправе переменить.

**Пустая «История».** У нового человека ярус целиком заменяется заголовком
«История начнётся с первой отметки» и одной прозой про то, когда появятся
календарь и веса. Без кнопок: показывать нечего и предлагать нечего.

Пустота определена как «нет истории вовсе»: ни одного дня привычки, ни одной
тренировки за месяц, весам нечего сравнивать. Число дней привычки спрашиваем у
самого календаря (`buildMorningActivationCalendarData(...).trackedCount`), а не
заводим свой скан: горизонт — его понятие.

Строки яруса «Сегодня» при этом остаются на месте со значением «не отмечено» —
пустота меняет текст, а не вид.

**Смоук нашёл настоящее взаимодействие блоков.** Тесты блока 6 (строка «Рабочие
веса») упали: пустая «История» вытесняет весь ярус вместе со строкой весов.
Разобрано — правы обе стороны, неверна была фикстура: человек, у которого
метрика вообще что-то говорит, историю уже ведёт. В фикстуру добавлен день
привычки, причина названа в тесте.

Живая проверка на 375 px, три состояния: новый человек — проза без кнопок и без
календаря; один день привычки — календарь на месте, прозы нет; три занятых слота
— в листе два пункта, погашенных кнопок ноль. Строки яруса во всех трёх
одинаковые.

86 сценариев смоука (7 новых).

### Блок 9 · Причина пропуска зарядки — сделан 2026-08-31

Вторая новая механика контракта — и оказалась она не написанием механики, а
одной недостающей записью.

**Вся ветка была построена и недостижима.** Список из пяти причин, шаг
`morning_activation_skip_reason`, флаги `skipReasonPending` / `skipReasonId`,
два обработчика событий, защита от повторного открытия — всё существовало. Не
работало одно: открытие требует `status === 'missed'`, а «не сегодня» писало
`'skipped'`, которого не знал никто ниже (ACTIVITY_TAB_AS_IS.md §9 P).

**Что изменено — три строки смысла:**

1. «Не сегодня» пишет `status: 'missed'` и `skipReasonPending: true`, а следом
   шлёт `heys:ma-skip-reason-check` с датой дня. Обработчик сам дождётся первого
   приёма пищи: два вопроса подряд — это допрос, а не сбор данных.
2. `'missed'` внесён в список отвеченных статусов чек-ина наравне с остальными:
   это ответ человека, а не отсутствие ответа. `'skipped'` из списка **не
   убран** — дни, записанные прежней версией, продолжают читаться, историю
   задним числом не переписываем.
3. Открытие вопроса отказывает для чужой даты. Проверка стоит **до** чтения дня:
   спрашивать нечего — читать незачем.

**Побочно выправился календарь.** День, отмеченный «не сегодня», теперь красится
пропуском по собственному статусу, а не по фолбэку «прошлый день без записи»:
проверено вживую — клетка отдаёт `missed`, соседняя с зарядкой `done`.

**Отказ отвечать ничего не блокирует**: шаг причины пишет только сам ответ,
статус пропуска уже стоит и от молчания не меняется. Закреплено тестом.

**Обновлён чужой тест.** `morning-activation-followup.test.js` проверял, что «не
сегодня» сохраняется как `'skipped'` — это прежний контракт. Смысл сохранён
(ответ сохраняется), изменился статус; причина названа в самом тесте. Туда же
добавлено утверждение, что событие вопроса о причине уходит — прежде оно не
приходило ни разу.

8 сценариев в
[activity-charge-skip-reason.test.js](../../apps/web/__tests__/activity-charge-skip-reason.test.js)
плюс два в наборе followup. Смежные наборы зелёные — 325 тестов, включая
`sync-merge-shared`: `'missed'` стал терминальным для слияния, и это намеренно —
явный ответ человека должен выигрывать спор между устройствами.

### Блок 11 · Тест сверки и вердикты — сделан 2026-08-31

**Тест сверки.**
[activity-v4-canvas-geometry.test.js](../../apps/web/__tests__/activity-v4-canvas-geometry.test.js)
— шестнадцать пар «класс кадра → правило продуктового CSS», нормализация форм
записи (шорткат `font:` против раскладки, `.04em` против `0.04em`, роли канваса
против ролей палитры, альфа чернил против ступеней набора) и поимённый список из
шести исключений. Тест читает сам канвас, поэтому расхождение всплывает при
правке любой из сторон.

Первый прогон дал 26 расхождений. Из них девять оказались дефектами кода — их
починил, остальное было моей же ошибкой в нормализации (роли ступеней сводились
к ролям линий и для цвета тоже) и одной ложной парой (`.badge` служит пилюле «от
куратора», а не пилюле оценки).

**Что тест поймал в коде.** Крупное — прежняя редакция шапки: число цели было 64
px/600 с трекингом −.045em вместо 30/800/−.02em кадра, а карточка стояла
радиусом 26 полями 26/22/22 вместо `.grp` 20/16. Плюс подложка карточки шагов
(вторая поверхность вместо первой), цвет яруса, отсутствие общего шрифта у строк
списков.

**Что нашла сама расстановка вердиктов.** Сверка пар покрывает шестнадцать
классов, а вердикт ставится по каждой из 286 строк — и там всплыло то, что пары
не видят:

- Ярус «История» стоял карточкой `.grp` с полями 16 и разворачивал календарь
  всегда. Кадр «день собран» даёт список `.cd` из трёх строк, а календарь живёт
  за строкой «Зарядка · 18 из 24 / 28 дней ›». Перестроил; календарь открывается
  вторым слоем той же строки, а не отдельным экраном — это названо в вердикте
  строки `Календарь зарядки · 02`.
- Точки календаря и легенда красились разными ролями за одно и то же состояние:
  ячейка «сделана» брала `--v4-ok-fill`, а точка легенды — `--v4-good`;
  «пропуск» в ячейке был `--v4-warn-soft` (жёлтый), а в легенде `--v4-bad-text`
  (красный). Свёл к ролям кадра: `--gr` → `--v4-ok-text`, `--gr2` →
  `--v4-ok-fill`, `--val-bad` → `--v4-bad-text`.
- Чипы режима были текстом с подчёркиванием, кадр просит пилюли `.badge`.
  Переделал; площадь нажатия добита прозрачной вставкой `::after`, потому что
  пилюля кадра ниже 44 px.
- Кнопка «Добавить активность» разносила текст и «+» по краям полями 17/22 —
  `.btn` кадра центрирует текст при высоте от 48. Заодно ушёл литерал `#ea580c`:
  акцент берётся ролью с запасным значением, и маркер намеренности ему больше не
  нужен.
- Имя «Шаги» и число красились приглушёнными чернилами, кадр даёт `--tx`; кегль
  числа был 13/700 вместо 12,5/600.
- Мёртвые правила `.activity-v4-rows`, `.activity-v4-row*`,
  `.activity-v4-cardio*` остались от редакции до сведения — разметка их больше
  не создаёт. Удалил: это последствия моей же правки, а не чужой мёртвый код.
  Вместе с ними ушли последние жёстко вписанные `#e2e8f0` и `#475569`.

**Вердикты.** Зона `tab-activity` заведена в снимок
[ui-v4-contract-verdicts.json](../ui/ui-v4-contract-verdicts.json): 286 строк,
`=` 139 · `≠` 84 · `—` 53 · `?` 10.

- `—` — строки про сам канвас (правила адресации, «как читать»), шапка
  приложения и капсула даты (вынесены за границы зоны строкой `границы`),
  контейнеры без чисел и сноски кадров.
- `≠` — две группы. Первая, и она же самая частая: кадр называет ступень тона,
  которой в наборе нет (42, 50, 60, 16, 10, 7 %), и взята ближайшая
  существующая. Вторая: вид блока программы и карточки силовой не сведён — см.
  «Что осталось».
- `?` — десять строк вида `… · текст`: это копия кадра целиком, вместе со
  сносками канваса. Продуктовые куски сверены по строкам вида и по кадрам, но
  построчно вся запись не сверялась.

**Проверено:** `pnpm ui:v4:check` зелёный (роли, стражи CSS-тестов, дрейф
контракта по 21 зоне); 115 сценариев в пяти файлах `__tests__/activity*`;
геометрия шапки, списка «История» и кнопки снята с живого `main.css` на 3001
через `getComputedStyle` — числа сошлись с кадром.

---

## 3a. Что осталось

Не дефекты и не отступления — работа, до которой сведение не дошло. Названа
поимённо в вердиктах соответствующих строк, чтобы её нельзя было потерять.

1. **Вид блока программы куратора** — сведён 31 августа целиком, все три
   элемента. Блок 3 до этого свёл только поведение: порядок, взаимоисключение
   строки и карточки, «не начаты».

   **Строка «Следующая тренировка»** — список `.cd` кадра «день отдыха», имя и
   под ним состав («Грудь и руки · по программе» из `dayLabel` дня программы —
   прежде строка говорила только «когда»), справа «программа ›». 11 сценариев в
   [activity-program-next-row.test.js](../../apps/web/__tests__/activity-program-next-row.test.js).

   **Карточка назначенного плана** — `.grp` кадра, заголовок «Сегодня по
   программе», пилюля «от куратора», состав прозой вместе с «В расход не идёт,
   пока не начнёте», «Начать» и ряд из двух. Главное не геометрия: перенос и
   пропуск прятались за одной кнопкой, и слово на ней зависело от того, есть ли
   куда переносить. Разнесены; переносить некуда — кнопки нет, а не погашена. 14
   сценариев в
   [activity-plan-card-frame.test.js](../../apps/web/__tests__/activity-plan-card-frame.test.js).

   **Карточка правки куратора** — `.grp` на `--v4-tint`, заголовок «Куратор
   поправил сегодняшнюю тренировку» тоном `--v4-warn-text` (роль `--ac2` кадра),
   суть правки прозой, «Принять» и «Оставить прежнюю» на грунте с обводкой.
   Здесь тоже дело не в виде: главной кнопкой был переход в разбор — ответить с
   карточки было нельзя, хотя правка перехватывает день. 12 сценариев в
   [activity-proposal-card-frame.test.js](../../apps/web/__tests__/activity-proposal-card-frame.test.js).

   Сверх кадра сохранено намеренно и названо в тесте: обещание «Сделанное не
   тронется» стоит в прозе всегда, когда подходы уже закрыты, а список изменений
   остаётся на первом слое — иначе «Принять» было бы вслепую. Полный разбор кадр
   не рисует, но он единственный вход в список замороженных упражнений: оставлен
   ссылкой под кнопками, не главным действием.

   Геометрия всех трёх задана внутри `.activity-v4-program`; общие
   `.program-next-line`, `.sb-plan-card` и `.sb-proposal-card` не тронуты — они
   живут на других экранах своими рамками. Разметку `PlanCard` и `ProposalCard`
   правка меняет для всех, поэтому тесты `strength-builder-ui` и
   `strength-proposal-ui` переведены на новые слова.

   Не реализовано и не выдумано: «ориентировочно 50 минут» из прозы кадра — поля
   длительности в данных плана нет. Названо вердиктом строки
   `Актив · план назначен · 09`.

2. **Вид карточки силовой** (строка 31, кадр «Тренировки · журнал подходов»).
   Блок 2 свёл аккордеон и фактический тоннаж; чипы зон, вложенный список
   упражнений на `--bg` и кнопка «Открыть конструктор» — прежние. Это
   единственное, что осталось открытым.

3. **Подпись пункта листа действия** — сведена 31 августа: 600 11/1 по кадру
   (была 500 11/1,3). Тон остался 45 % вместо 42 % кадра — ступени 42 в наборе
   нет.

4. **Сетка календаря** — сведена 31 августа: семь колонок зазором 9 вместо двух
   строк по четырнадцать, точка фиксированные 9 px по центру колонки. В прежней
   раскладке неделя не читалась вовсе; теперь вертикаль и есть ось недель.
   Ячейки некликабельны, поэтому площадь нажатия им не нужна.

5. **Приглушение чисел при нуле** — сведено 31 августа: когда факта нет и оценки
   тоже, число шагов и калории берут 38 % кадра «новый человек». Оценка нулём не
   считается — у подставленной медианы свой тон 55 %, число там настоящее.

   Модификатор пришлось поставить после базового правила: специфичность равная,
   и порядок решает. Поймано живой проверкой — калории оставались 55 %.

Всё перечисленное закрыто 8 сценариями в
[activity-zero-and-calendar-grid.test.js](../../apps/web/__tests__/activity-zero-and-calendar-grid.test.js).

---

## 4. Отступления от кадров

Восемь штук. Шесть из них — про пары «класс кадра → правило продукта», и они
названы поимённо в `EXCEPTIONS` теста сверки
`apps/web/__tests__/activity-v4-canvas-geometry.test.js`; два остальных пара не
описывает, поэтому живут только здесь.

Отдельно: округление тона до ступени набора встречается по всей зоне, а не
только в этих восьми — 42, 50, 60, 16, 10 и 7 % кадра существующих ролей не
имеют. Каждый такой случай назван вердиктом `≠` в снимке контракта; здесь
перечислено только то, что видит тест сверки.

### Названы в списке исключений теста

1. **Подпись в шапке — чернила 45 %, а не 42 % кадра.**
   `.activity-v4-hero__label|color`. Роли на 42 % в наборе нет, а список ролей
   заморожен и может только уменьшаться
   (`scripts/ui-v4-check-undefined-roles.mjs`). Взята ближайшая существующая
   `--v4-ink-3`. Заводить роль ради одной подписи дороже, чем отступить.
2. **Сноска шагов — те же 45 % вместо 42 %.** `.activity-v4-steps__note|color`,
   причина та же: класс кадра `.sm` красит сноски в 42 %.
3. **Пустая «История» — 55 % вместо 60 % кадра.**
   `.activity-v4-history-empty__text|color`. Класс кадра `.p` красит абзацы в 60
   %; ближайшая ступень набора — `--v4-ink-2` (55 %).
4. **Сноска шагов отбита на 10 px, а класс кадра просит 12 px.**
   `.activity-v4-steps__note|margin-top`. Десять — её собственное число из
   разбора кадра («Актив · шаги оценены · 18»), двенадцать у `.sm` общие для
   всех сносок канваса. Число элемента старше числа его класса.
5. **Фон шапки цели дня парой не сверяется.** `.activity-v4-hero|background`.
   Кадр берёт `.grp` и тут же переопределяет фон прямо в разметке на `--c2`
   (контракт строка 34). Класс несёт `--c1`, поэтому фон из пары выпадает — это
   граница метода, а не отступление; само значение проверено живьём.
6. **Текст на кнопке действия назван поимённо.** `.activity-v4-cta|color`.
   `.btn` кадра красит его ролью `--on-acs`, а роли «текст на акценте» в
   продуктовом наборе нет и заводить её ради одной кнопки дороже, чем назвать
   цвет.

### Пары не описывают — названы только здесь

7. **Строка «Тренировка вчера» (NDTE) в кадре не нарисована.** В контракте она
   не названа, но она — такая же часть базы, и правило «строка только при
   значении больше нуля» к ней то же. Без неё столбик не сходился бы с
   «Затратами» в день после тяжёлой тренировки. Это чтение правила, а не
   отступление от него, но названо здесь, чтобы ревьюер не искал её в кадре.
8. **Разделитель строк разбора — 8 %, а не 7 % кадра, и заливка полосы при
   оценке — 30 %, а не 16 %.** Обе ступени в наборе фиксированы: линии
   `--v4-line` 8 % / `--v4-track` 12 % / `--v4-edge` 18 %, чернила 55 / 45 / 38
   / 30 %. Взята ближайшая существующая, запасные значения в CSS приведены к
   ней, чтобы код не обещал ступень, которой в наборе нет. Тест этих двух не
   видит: `border-bottom` не входит в список сверяемых свойств, а у модификатора
   `--estimated` нет класса-пары в кадре.

Общее у семи из восьми одно: набор ролей заморожен и может только уменьшаться,
поэтому там, где кадр называет ступень тона, которой в наборе нет, берётся
ближайшая существующая роль, а не заводится новая. Записывать ступень тона
именем (`--v4-ink-42`, `--v4-hairline`) — ошибка: для тонов уже есть словарь
ролей, а канвасные имена (`--gr`, `--c1`) отображаются на продуктовые, а не
копируются.
