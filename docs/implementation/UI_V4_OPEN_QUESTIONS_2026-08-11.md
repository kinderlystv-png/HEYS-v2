# UI v4 — открытые вопросы (2026-08-11)

Формат: экран · вопрос · варианты · решение.

---

**Инсайты · Подробно · Метаболизм** — ~~открыто~~  
**Решено 2026-08-11 (владелец): 1А** — один collapsible «Метаболизм и тип» с
тремя блоками внутри. Три входа в legacy — три двери в одно место; выкидывать
фенотип — терять уже собранное.

**Инсайты · Подробно · Итоги недели** — ~~открыто~~  
**Решено 2026-08-11 (владелец): 2Б** — убрать с Подробно. Дубль Отчётов
подтверждён по коду: `WeekTable` и «Съедено vs цель (7 дней)» в
`heys_reports_tab_impl_v1.js` (:601, :857).  
**Сделано `5a8c16261`:** с detail-слоя сняты `WeeklyWrap` / `WeeklyReportCard`
(движки на месте).

**Инсайты · герой «Сделай сегодня»** — ~~открыто~~  
**Решено 2026-08-11 (владелец): 3А** — цепочка `why → text → EWS → fallback`
(`buildInsightsCuratorPhrase`).  
**Сделано `5a8c16261`:** в `variant: 'v4'` у действий нет `why` — фраза только в
шапке героя.

**Шторка даты · легенда точек** — ~~открыто~~  
**Решено 2026-08-11 (владелец + канвас): 4А** — точка = «есть записи»; качество
и заливка по ratio из календаря убраны (живут в Отчётах); заливка клетки —
навигация (сегодня = акцент, выбран = поверхность + обводка); цикл/загрузка —
полосы по краю, без emoji. Пороги `ratioZones` не трогаем.  
**Канвас:** `docs/ui/handoff-v4/canvas/Дата и остатки v4.dc.html` (секция
«Календарь · легенда сведена с сеткой»).  
**Сделано:** `heys_day_pickers.js` (sheet) + sheet-CSS в
`000-base-and-gamification.css`; тест
`__tests__/date-picker-sheet-v4-structure.test.js`. Legacy `Calendar` с
ratio-fill не трогали.

**Каскад · синяя заливка zonebar** — ~~открыто~~  
**Решено 2026-08-11 (владелец): 5Б с оговоркой** — ступени плотности акцента, не
ok/warn.  
**Сделано `32243ebed`:** `--heys-score-tone-*` / `--heys-score-zone-step-*` от
`--v4-act`; fill-классы
`heys-score-zonebar__fill--{base|accelerating|growing|peak}`; `STATE_CONFIG` и
границы 25/45/70 не тронуты.
