# UI v4 — открытые вопросы (2026-08-11)

Формат: экран · вопрос · варианты · что сделано временно.

---

**Инсайты · Подробно · Метаболизм** — пересекаются ли «Метаболизм»,
«Метаболический фенотип» и «Продвинутая аналитика» настолько, что их надо свести
в один раздел без подзаголовков? · (А) один collapsible «Метаболизм и тип» с
тремя блоками внутри, как сейчас · (Б) только метаболизм, фенотип и advanced
убрать с Подробно · (В) три отдельных входа, как в legacy · **Временно:**
вариант А — один `CollapsibleSection` «Метаболизм и тип» с тремя компонентами.

**Инсайты · Подробно · Итоги недели** — SPEC: погранично (горизонт = Отчёты),
канвас «Подробно» показывает неделю · (А) оставить на Подробно · (Б) убрать,
дублирует Отчёты · **Временно:** вариант А — `WeeklyWrap` / `WeeklyReportCard`
на слое Подробно.

**Инсайты · герой «Сделай сегодня»** — одна фраза куратора: брать из `why`
первого действия, из EWS или статичный fallback? · (А) why → text → EWS →
fallback · (Б) отдельный copy-engine · **Временно:** вариант А
(`buildInsightsCuratorPhrase`).

**Шторка даты · легенда точек** — канон не сверен с канвасом «Дата и остатки v4»
· цветные точки в легенде vs bg+emoji в state · **Временно:** не трогал (хвост 1
вне сессии).
