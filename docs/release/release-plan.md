# HEYS · план до релиза

> **Штамп канона: 12.08.2026 14:48 MSK.** Живая копия — `docs/release/` на
> рабочей машине владельца. Копия в git может отставать: канон правится по ходу
> дня, а коммитится попутно. Если у вас файл со штампом старше даты вашего
> задания — у вас не та копия, остановитесь и спросите.

Сверка канона и статусов B1. **Дата релиза — 31 августа 2026.** Статусы: **✅
сделано** · **🔵 в работе** · **🟡 решено, не реализовано** · **⬜ не начато** ·
**⏸ отложено осознанно**

Документ самодостаточный: по нему можно восстановить, что за проект, где он
находится и куда идёт, без истории переписки.

---

## 0.5 Что закрыто 11–12.08

День шёл не по плану: вскрылись линии, которых в нём не было, и они оказались
важнее запланированного.

**Безопасность.** Дыра во входе по PIN — счётчики ключевались на подделываемый
заголовок, защиты от перебора не было; закрыто, счётчик восстановлен по номеру,
проверен на живой функции. Публичный доступ к фото и голосовым — закрыт на
уровне бакета и всех семидесяти объектов, чтение переведено на авторизованный
путь, запрет защищён проверкой в pre-push. Три пути обхода гейта деплоя закрыты.

**Персональные данные.** Разобран весь узел: минимизация решена и реализована в
коде, опциональные функции спроектированы, трансграничная передача снята с
повестки, разбор юриста получен и внесён в план. Условий готовности стало десять
вместо семи — два новых пункта пришли от юриста.

**Вход.** Новая схема реализована на сервере, восемь property-тестов, политика
sunset для старого пути, двусторонний гейт.

**Дизайн.** Канвасы входа и цикла закрыты, лендинг разобран по карточкам и ждёт
поблочного согласования.

**Юрист.** Три круга разбора за сутки. Подтверждено письменно: после минимизации
специальная категория уходит из продукта полностью, семь условий, шесть разовых.
Получены готовые проекты текстов, разбор девяти документов с таблицей на 21
правку, сверка карточки реестра с фактом.

**Решения, сузившие продукт.** Трекинг цикла снят до релиза. Telegram убран из
каналов связи с куратором, кандидатский флоу оставлен и вынесен вопросом юристу.
Оба решения приняты после проверки цифрами, а не по ощущению.

**Процесс.** План, промпты с преамбулой для исполнителей без контекста, семь
правил работы, выведенных из практики. Правило единственного писателя плана —
после того как расхождение случилось на деле.

Отдельно стоит отметить: восемь ошибок за день нашли и назвали сами исполнители
— в собственной работе, до того как их нашли другие. Это надёжнее любой проверки
сверху.

---

## 0. Что за проект

HEYS — сервис сопровождения по режиму питания. Отличие от трекеров и от разовых
консультаций одно: дневник ведёт не клиент, а куратор. Человек присылает фото,
голосовое или пару фраз, куратор переносит это в приложение, смотрит неделю
целиком и раз в семь дней разбирает, где режим начинает сбиваться.

Юридически — ИП Поплавский Антон Сергеевич, ИНН 263517141102, оператор
персональных данных, рег. номер 26-22-005319. Не медицинская услуга, диагнозы не
ставятся.

Куратор сейчас один — сам владелец. Потолок внимания — пятнадцать клиентов.
Дальше наём кураторов с комиссией 40–50% от стоимости ведения.

**Тарифы.** Self 490 ₽/мес (инструмент, дневник ведёт сам клиент; на старте
акция 0 ₽ до 31 октября). Pro 11 990 ₽/мес, для первого набора 7 990 на три
месяца, пятнадцать мест. Pro Спорт 19 990 ₽/мес, пилот до четырёх участников.

**Состояние на 11.08.2026: платящих клиентов нет, релиз не состоялся.**
Планировался на 8 августа, дата прошла. Пять аккаунтов в системе — свои и
тестовые. Отзывов и кейсов нет и до запуска взяться неоткуда.

## 1. Как устроено технически

Приложение — `app.heyslab.ru`, лендинг — `heyslab.ru`, репозиторий `HEYS-v2`.
Облачные функции `heys-api-*` в Yandex Cloud, Managed PostgreSQL, бакет
`heys-photos` в объектном хранилище, расшифровка голосовых через Yandex
SpeechKit. Первичная запись данных — в российской базе, локализация соблюдена,
проверено по всем пяти каналам.

Вход клиента: телефон плюс четырёхзначный PIN, выданный куратором. Меняется на
схему «одноразовый код куратора → собственный код клиента → устройство помнится
30 дней».

Кураторский MCP на `api.heyslab.ru/mcp/curator` — канал, через который куратор
ведёт дневники из чата с моделью. Отдельная функция `heys-mcp` для внешних
LLM-клиентов убрана из прода.

Граница суток в приложении — 03:00 по Москве, а не полночь. Инварианты проекта в
`CLAUDE.md`: облако — источник правды, запись только через `OverlayStore`,
никаких автоматических решений по форме данных, любой скан `localStorage`
фильтрует чужие ключи.

## 2. Кто делает

**Владелец** — все решения, отправка сообщений исполнителям, живые проверки в
браузере, разговор с юристом.

**Кодер** — доступ к репозиторию и проду. Правило: миграции и правки
legal-документов только по прямой команде, SQL показывается до применения, push
и deploy отдельной командой.

**Дизайнер** — канвасы v4 (спека экранов) и сборка «Лендинг D». Не имеет доступа
к коду; всё, что он знает о решениях, приходит файлом от владельца.

**Юрист-агент** — разбор по 152-ФЗ. Профильный юрист-человек пока не нанят, это
открытая позиция и единственный внешний срок.

**Модель в чате** — держит план актуальным, ловит расхождения между артефактами,
пишет промпты. Не источник истины: чат не переживает сессию.

## 3. Карта артефактов

| Что                                  | Где                                                                                                                                                                           | Роль                                    |
| ------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------- |
| Этот план                            | файл у владельца                                                                                                                                                              | что делать и в каком порядке            |
| «Отдать кодеру и дизайнеру»          | файл у владельца                                                                                                                                                              | готовые тексты промптов                 |
| «Правка карточек для дизайнера»      | файл у владельца                                                                                                                                                              | канон по решениям для лендинга          |
| «Правка карточек претензий» (полная) | файл у владельца                                                                                                                                                              | архив разбора, включая ПДн-журнал       |
| Журнал решений                       | `маркетинг/15_Ревизия_и_лог_решений.md`                                                                                                                                       | нумерованные решения по продукту        |
| Приёмка флоу еды                     | `docs/implementation/ADD_FOOD_FLOW_AUDIT_2026-08-09.md`                                                                                                                       | решения по добавлению еды               |
| План имплементации v4                | `docs/implementation/UI_V4_IMPLEMENTATION_PLAN_2026-08-10.md`                                                                                                                 | этапы перекраски интерфейса             |
| Журнал ошибок                        | `apps/web/BUGS_HISTORY.md`                                                                                                                                                    | классы дефектов и рецепты               |
| Юридические тексты                   | `docs/legal/`                                                                                                                                                                 | оферта, политика, согласия, обработчики |
| Стенограмма                          | задачник через `heys-mcp`: ключ `transcript/YYYY-MM-DD.md` (не каталог в `HEYS-v2`; зеркало — репозиторий `tasks`). Путь строго в ед.ч.: `transcripts/` отвечает ok, но пусто | ход работы по дням                      |
| Гайд по тону                         | `COPY_VOICE.md`                                                                                                                                                               | как писать тексты на витрине            |

**Порядок старшинства при расхождении:** код → этот план → журнал решений → всё
остальное. Сборка «Лендинг D» источником истины не является ни для чего.

**Как ведётся канон — механика, а не пожелание.**

Живая копия трёх файлов (`release-plan.md`, `handoff-prompts.md`,
`landing-cards.md`) лежит в `docs/release/` на рабочей машине.

**Кто что пишет — правило изменено 12.08 по факту работы.** `handoff-prompts.md`
и `landing-cards.md` пишет только модель в чате. В `release-plan.md` исполнитель
может перевести в ✅ или 🟡 строку **своей** работы — после явного «закрывай» от
модели и с доказательством прямо в строке: коммит, файл со строкой, имя
миграции, вывод команды. Статус без доказательства не считается. Всё остальное —
решения, отменённое, ⛔, условия готовности, даты, чужие строки — по-прежнему
только модель.

Основание для изменения: за 12.08 исполнитель правил план четырежды, все правки
были верны, три столкновения разрешились без потерь. Обе ошибки в плане за день
были со стороны модели — объявлен написанным файл, которого не было, и в
«отправить» попали два уже исполненных промпта. Правило защищало от риска,
который не случился, и не защищало от того, который случился дважды.

Перед каждой правкой модель перечитывает файл с диска и пишет его назад с
проверкой времени изменения. Если файл кто-то тронул в промежутке, запись
**отклоняется**, и модель об этом узнаёт. Это одновременно защита от потери
чужой правки и датчик нарушения правила: отдельного гейта заводить не нужно,
нарушение видно само.

Копия в git отстаёт — на 12.08 закоммиченный план был на 28 КБ короче рабочего.
Поэтому канон **ходит вместе с заданием**, а не берётся исполнителем из
репозитория, и у каждого файла в шапке стоит штамп с датой и временем.
Коммитится канон попутно, когда кто-то и так коммитит; отдельной обязанностью
«не забыть закоммитить» это не делается — забытая обязанность хуже честно
объявленного отставания.

## 4. Правила работы, выведенные из практики

**Сверено с кодом.** Ни один документ не считается актуальным, пока его
утверждения не проверены против кода, с датой сверки в шапке. За один день
найдено одиннадцать расхождений документов с фактом.

**Сверено с юридическими текстами.** Меняя схему, перечисление или состав полей
— проверить, не назван ли этот перечень в оферте, политике или согласиях.
Юридический документ такой же потребитель схемы, как код, только молча его не
поправить: у версий зарегистрирован хэш, правка требует нового цикла и
переподписания.

**Чем эту проверку обойти.** У каждого нового механизма первым вопросом идёт не
«работает ли на счастливом пути», а «каким способом его обойти». За день найдено
четыре случая, где механизм построен правильно и стоит не на пути: гейт деплоя
мимо явного списка, ПЭП за выключенным флагом, счётчик попыток, потерянный при
обновлении функции, маскирование, снятое миграцией.

**Непроверенный предохранитель — предохранитель с неизвестным состоянием.**
Тест, который никто не пробовал сломать, не считается работающим.

**Цифра за окно не описывает текущее состояние.** Агрегат за тридцать дней
ничего не говорит о том, что происходит сейчас; нужно распределение.

**Счёт открытых пунктов ведётся по строкам-задачам, а не по значкам.** На 12.08,
поздний вечер: 136 строк `⬜`, 8 строк `🔵`/`🟡` — всего 144 открытых, а не
«около сорока», как было написано в задании аудитору. Значки встречаются и
внутри абзацев-пояснений, поэтому считать надо `^- ⬜` и `^  - ⬜`. Цифру в
задании обновлять вместе с планом: занижённая оценка объёма заставляет
исполнителя работать выборочно и молчать об этом.

**Отменённые предложения помечаются отменёнными, а не удаляются.** Через месяц
должно быть видно, что рассматривали и почему отказались.

**Юридический вывод устойчив ровно настолько, насколько устойчива фактура под
ним.** Выведено 12.08 из двух самопоправок юриста подряд. Обе были не шлифовкой:
первая касалась перечня получателей, вторая — вопроса, от которого зависел вход
в продукт, и по ней чуть не остановили кандидатский флоу на две недели без
всякой нужды. Ошибка в обоих случаях была не в норме, а в том, что норма
применялась к воображаемому состоянию продукта вместо реального. Это тот же
класс, что три расхождения по `heys-mcp` и обещание журналирования доступа в
политике: документ описывает не то, что есть.

Рабочий приём — вопрос «а на ком это происходит прямо сейчас». Он ловит такие
вещи быстрее, чем перепроверка права, и задаётся за секунду: клиентов нет,
значит и передачи чьих-либо данных нет. Оба раза поймали именно им.

**Уверенность тона — не признак проверенности вывода.** Второй маркер наравне с
первым, и он относится ко всем: к юристу, к исполнителям, к модели в чате.
Формулировка «нарушение уже существует» звучала твёрдо и была неверна.

**Правила пересматриваются вместе с планом, 20.08.** За 12.08 их прибавилось
четыре — писатели канона, разделение стенограммы, штампы, делегирование
статусов. Это много для одного дня, и дальше добавлять не надо: каждое правило
стоит внимания, а невыполняемое правило хуже отсутствующего, потому что создаёт
ложное чувство закрытого вопроса. На пересмотре по каждому один вопрос —
срабатывало ли оно за две недели. Не срабатывало ни разу — либо не нужно, либо
не работает; и то и другое требует решения, а не молчаливого продления.

**Разница между правилом и механизмом, выведенная из дня.** Сработали в
реальности не дисциплины, а механизмы, стоящие на пути: защита по времени
изменения отклонила три записи, гейт деплоя не пустил красный коммит в бакет,
дрифт-гейт поймал двадцать сдвигов классики, pre-push не дал уехать публичной
ссылке в бандле. Ни одно из «договорились так делать» само по себе ничего не
поймало. Поэтому при выборе, чем закрывать очередную дыру, механизм
предпочтительнее правила — а правило имеет смысл там, где механизм невозможен
или дороже последствий.

**Решения со знаком ⛔ закрыты окончательно.** Их не предлагают заново — ни
исполнители, ни консультанты, ни модель в чате, — даже если появилось новое
основание. Если основание действительно меняет картину, об этом говорят
владельцу одной строкой и ждут его слова, а не возвращают вопрос в работу.

---

**Смысловые правки плана делает модель в чате; статусы своей работы исполнитель
закрывает сам, с доказательством и после «закрывай».** Граница проходит между
«отметить сделанное» и «решить, что считается сделанным» — второе остаётся у
модели. Исполнители не пишут остального: всё, что нужно внести, сообщается в
отчёте и вносится здесь. У отдельной задачи нет картины целиком — план держит
десять треков, календарь и связи между решениями. Один документ с двумя
писателями расходится за часы; 12.08 это уже случилось, правки агента в
репозитории разошлись с рабочей версией. То же правило для `handoff-prompts.md`.

**Даты у пунктов.** Дата стоит у каждого открытого подпункта, мелким шрифтом в
конце строки: _— до 15.08_. Если срок сместился — старая зачёркивается и рядом
ставится новая: _· ~~до 15.08~~ → до 19.08_. Историю переносов не стирать: по
ней видно реальный темп, а не намерение. У выполненного пункта — фактическая
дата: _— ✅ 11.08_. У отложенного осознанно — _— после релиза_.

## 4.5 Порядок на 12.08

План на день исполнен целиком, кроме одной живой проверки.

1. ~~**Login.**~~ ✅ 11.08 вечером: три миграции по отдельности, 9/9 property на
   проде до деплоя функций, три функции задеплоены, smoke на тестовом клиенте
   пройден, коммит `975a2c070`, push закрыт. Серверная часть входа закончена,
   открыт только фронт — см. A3.
2. 🟡 **Health minimization.** Миграции на проде, `disable` вызван, purge
   выполнен с актом — см. B1. **Остаток один: живая анкета end-to-end руками
   владельца.** Он же держит условие готовности 3.
3. ~~**Inventory → purge.**~~ ✅ 12.08: инвентарь снят и показан, purge выполнен
   с исключением семейного аккаунта, акт
   `health-data-destruction-act-2026-08-12.md`.

**Smoke делать на отдельном тестовом клиенте**, не на собственном аккаунте:
первый вход сжигает одноразовый код, и посреди проверки придётся задавать себе
новый.

**Дата релиза: 31 августа 2026.** Назначена владельцем 11.08, задача
heys/66a97c.

Дата амбициозная и это осознанно: моя оценка от зависимостей давала первую
половину сентября, конец августа достижим только при ежедневном закрытии пунктов
и при условии, что юрист ответит быстро и не вернёт неожиданного.

Что должно сойтись, чтобы дата состоялась (сверено вечером 12.08):

- ✅ пакет юристу ушёл 12 августа;
- ✅ разбор от юриста получен 12 августа — на восемь дней раньше срока, три
  круга вместо одного. **Это единственная позиция, где мы обогнали календарь, и
  обогнали заметно:** критический путь шёл через юриста, и он больше не самое
  узкое место. Правки в тексты и версия 1.11 с переподписанием — по-прежнему до
  27-го;
- удаление по отзыву согласия (условие 8) — отдельная задача кодеру, начать не
  позже 15-го;
- фронт входа, минимизация на проде, развилка по циклу — параллельно, каждая по
  два-три дня;
- лендинг: поблочный разбор владельцем на этой неделе, отрисовка и вёрстка —
  следующая;
- живые проверки перед релизом — 29–30 августа, одним заходом.

**Что делает дату недостижимой.** Прежний список — задержка юриста, объём
правок, device-only по циклу — устарел на две трети: юрист ответил досрочно,
цикл снят решением. Осталось одно из трёх, и добавилось новое:

- неожиданный объём правок в текстах при сборке 1.11;
- **запертая выкладка.** Пока `ui-v4-classic-drift` красный, `main` не
  деплоится, и версия 1.11 не сможет доехать до прода тем же путём, что и всё
  остальное. Пункт свежий, до 12.08 его не было — см. A5.

Пересматривать 20 августа: к этому моменту будет видно, сколько правок реально
легло в тексты и разблокирована ли выкладка.

**Откуда первые три клиента.** Задача heys/f4fda9. Висит с утра, не двигалась.
Даже при полной готовности брать будет некого.

---

## 5. Что дальше

**Этот раздел — очередь, а не вторая доска статусов.** Статус живёт в треке,
здесь только порядок и ссылка. Прежняя редакция дублировала статусы и за день
протухла целиком: пять пунктов из девяти были уже закрыты в треках. Если пункт
хочется описать здесь подробно — значит его место в треке.

### Прямо сейчас, по порядку

1. ~~**Дрифт классической палитры.**~~ ✅ закрыт, прод `41955372`, труба
   открыта. 1а. ~~**Починить вход по выданному PIN**~~ ✅ **12.08.** Диагностика
   `prompt-login-pin-diag`, три наслоенных дефекта, миграция
   `2026-08-12_fix_client_pin_login_mismatch.sql`, smoke «пупс» (вход +
   согласия + регистрация). Коммит `d0d0fe896`, push — 12.08 вечер. Условие
   готовности 2 — ✅ после push.
2. **Smoke флоу еды на проде** — 🟡 **вход разблокирован 12.08, чек-лист трека D
   не прогнан.** Десять минут, под тестовым клиентом; закрывает heys/1ab6a1, не
   путать с входом. См. трек D.
3. **Живая анкета на проде тестовым кандидатом** — ваши пять минут. Последнее,
   что держит условие готовности 3. См. 4.5.
4. **heys/555464, вход на замороженную копию.** Скоуп другой, можно параллельно.
   См. A5.

### Ваше, не делегируется

4. **Payload push** — согласие обещает уведомления «о сообщениях куратора»,
   юристу заявлено, что они обезличены. Одно из двух неверно. См. C6 _— 13.08_
5. **Support-бот** — принимает заявления на возврат с номером телефона и суммой,
   входящий канал через зарубежную инфраструктуру. См. C6 _— 13.08_
6. **Поблочный разбор лендинга** — трек C стоит целиком, без него не уходят два
   промпта.
7. **Откуда придут первые три клиента**, heys/f4fda9 — не двигалось ни разу.

### Закрыто, вынесено отсюда, чтобы не путало

Скриншоты экранов согласий — сняты, юрист разобрал шестнадцать снимков, все
шесть пунктов его запроса закрыты (C5). Ящик `support@heys.app` — проверен,
домена не существует, `dig MX` пустой, решение принято, правка идёт в 1.11 (C5,
C6). Миграции минимизации, `disable`, purge — B1. CI и веб — выкачены, но
`version.json` уже не `44195e62`, а `a7fd195a`: цифру здесь больше не держим,
живая в A5.

### Цепочка, которая блокирует первых клиентов

**новый дизайн приложения → демо-ролик в герое → рабочий лендинг → привлечение**

Первое звено — вёрстка v4, самая длинная из оставшихся работ, не начата. Промпт
по еде покрывает 33 экрана одного флоу.

31 августа — дата технической готовности принять клиента. Дата появления клиента
наступает позже на длину этой цепочки; считать раздельно.

**Стоит посчитать до запуска вёрстки целиком:** можно ли снять ролик по одному
сценарию — прислал фото, куратор занёс, к вечеру видна неделя — и перекрасить
только путь по нему. Тогда цепочка сокращается вдвое.

---

## 6. Критический путь

| Что                                                    | Статус                                                                                                                                                               |
| ------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| ~~CI красный, деплой веба не проходит~~                | ✅ 12.08. Цифру версии здесь не держим, она менялась пять раз за день — живая в A5                                                                                   |
| ~~Минимизация: код готов, на проде нет~~               | ✅ выкачена 12.08. `disable` вызван, purge выполнен, акт составлен. Осталось только smoke анкеты владельцем. Изменение сведений в реестре отложено осознанно, см. B1 |
| Пакет документов 1.11: 21 правка                       | 🟡 разбор получен, два документа создаются заново, один изымается                                                                                                    |
| Лендинг: 6 отрисовок и 9 текстовых правок              | ⬜ ждёт поблочного согласования владельцем                                                                                                                           |
| ~~`main` не деплоится: `ui-v4-classic-drift` красный~~ | ✅ 12.08. Двадцать сдвигов закрыты, роль `--v4-sand-accent-bg` заведена, рецепт в `BUGS_HISTORY.md`. Прод `41955372`, труба открыта — 1.11 поедет обычным push       |
| Вёрстка v4 → демо-ролик → лендинг                      | ⬜ не начата. Блокирует привлечение клиентов, не техническую готовность                                                                                              |

Закрыто за 11–12.08: фото и аудио, вход целиком до прода, git-синхронизация,
цикл снят из кода, три круга разбора юриста.

---

## 7. Трек A · Доступ и безопасность

### A1 · Фото — 🔵 в работе _— до 13.08_

- ✅ Инвентарь прямых ссылок: не только мессенджер, но и галерея дня
  (`heys_day_gallery.js:263` и три связанных файла)
- ✅ Установлено, что `url` — сохранённое поле в
  `client_messages.attachments[]`, полный публичный адрес, а не путь
- ✅ Найден источник: `POST /photos` сам конструирует публичный URL и отдаёт
  фронту
- ✅ Проверено, что DSAR-экспорт файлы вложений не отдаёт
- ✅ `url` убран из ответа `/photos/upload` — единственный путь чтения теперь
  авторизованный `/photos/read` по `path`
- ✅ Чтение переведено в обоих контурах: мессенджер, галерея дня и полноэкранный
  просмотр
- ✅ Кэш блобов: LRU на 60 записей, отрицательный кэш 30 секунд, ключ включает
  тип медиа
- ✅ **Аудио оказалось той же дырой** — голосовые лежат в том же бакете с такой
  же публичной ссылкой. Добавлен `fetchAudioBlob`, серверная поддержка `audio/*`
  в `/photos/read`
- ✅ Тест на отсутствие `storage.yandexcloud.net` в бандле — в
  `verify-legacy-bundles.mjs`, то есть в pre-push гейте. Проверено, что падает:
  строка подставлена, гейт упал, строка убрана — зелено
- ✅ Три регрессии от собственной правки найдены и починены: очистка base64 из
  localStorage держалась на `photo.url`; `result.url` использовался как признак
  завершения загрузки; **проверка `!result?.url` как условия успеха отправки
  голосового — без этой находки отправка голосовых сломалась бы полностью**
- ✅ 39 тестов мессенджера и 4 теста облачной функции зелёные
- ✅ `heys-api-photos` задеплоена, health-check прошёл, `/photos/read` без
  токена по-прежнему отдаёт 401
- ✅ Публичное чтение бакета `heys-photos` выключено
- ✅ **Найдено при проверке: закрытия бакета недостаточно.** У каждого объекта
  был собственный грант `AllUsers: READ`, поставленный при загрузке и живущий
  независимо от настройки бакета — первая проверка старой ссылки после закрытия
  бакета всё ещё отдавала 200. Грант снят со всех 70 объектов, проверено тремя
  случайными выборками и тремя разными ссылками — везде 403
- ✅ `ACL: 'public-read'` убран из кода загрузки, чтобы новые объекты не
  получали грант заново. Коммит `437826a06`, SEC-006 закрыт
- ⬜ Проверка на мобильном и во встроенном браузере Telegram — за владельцем,
  вместе с C0 _— 29–30.08_
- 🔴 **Рабочее дерево разрослось до 71 файла и мешает четыре работы сразу.**
  Проверено 12.08: незакоммиченными лежат клиентская часть фото и галереи,
  правки гейта согласий под stable, восемь канвасов дизайнера (часть удалена,
  часть добавлена), пересобранные бандлы, `todo.md`, `AGENTS.md` и
  `heys_tasks_kv.cjs` в двух функциях. `docs/release/prompt-stable-consents.md`
  вообще не в git.

  Пока так, `git commit -a` кем угодно выложит на прод половину незаконченного —
  а push теперь публикует сразу. Коммитить только явным списком путей,
  `HEYS_COMMIT_SOURCE_ONLY=1`. Разобрать дерево по работам и закоммитить по
  частям — отдельная задача, и она стала срочнее, чем была утром _— 13.08_

- ⬜ Закоммитить клиентскую часть фото и галереи (мессенджер, галерея, тест,
  гейт на бандл) — своим списком путей, не общим коммитом _— до 13.08_
- ⬜ Проверить кураторскую сторону отправки вложений _— до 13.08_
- ⏸ Затирание поля `url` в исторических записях — после закрытия чтения и
  записи

### A2 · Вход — частично закрыт _— до 15.08_

- ✅ Подтверждена подделка `X-Forwarded-For` на живом проде
- ✅ Найдено, что оба счётчика ключевались на IP; защиты от перебора не было
- ✅ Логи проверены — следов перебора нет
- ✅ Вход по PIN закрывался на время правки, оригиналы функций сохранены
- ✅ Счётчик по номеру восстановлен из v2, добавлены нарастающая пауза и
  заморозка; миграция применена
- ✅ Тест на свойство: пять неудач с пяти адресов дают блокировку; проверено на
  живой функции
- ✅ Вход открыт обратно
- ✅ Три пути обхода гейта деплоя закрыты, `ALLOW_DISABLED_FUNCTIONS` требует
  причину
- ✅ `heys-api-sms` удалена из прода
- ✅ Причина отказа сервера доходит до экрана; словарь кодов, контракт-тест,
  предохранители теста
- ✅ Счётчик неудач срабатывает только на реально неверный код
- ⬜ Живая проверка «отказ сервера → текст на экране» и блокировки целиком —
  ждёт освобождения бандла _— 29–30.08_
- ⬜ Перевернуть приоритет источников IP: `sourceIp` основной, `x-forwarded-for`
  запасной. Пять функций _— до 15.08_
- ⬜ Блокировка не должна давать постороннему запирать чужой аккаунт: вход с
  зарегистрированного устройства её игнорирует _— до 15.08_
- ⬜ Проверить `messages` и `payments` на те же лимиты _— до 15.08_
- ⏸ Гейт на метаданные согласий — до прогона шести сценариев, задача
  heys/5e5736, окно 01–15.09

### A3 · Новая схема входа и ПЭП — 🔵 сервер на проде, фронт открыт _— до 18.08_

- ✅ Установлено: ПЭП построена и выключена флагом `SMS_VERIFICATION_ENABLED`
- ✅ Черновик соглашения об использовании ПЭП написан
- ✅ Канвас входа: пять новых экранов и четыре правки внесены
- ✅ Серверная часть реализована 11.08, миграция
  `2026-08-11_client_login_scheme_v2.sql` применена. Одноразовый PIN на трое
  суток со сгоранием и гашением при перевыдаче; код доступа клиента с
  bcrypt-хэшем и проверкой слабых; разделение входа и подписания через
  `sign_consents_with_access_code_by_session`, `log_consents` отклоняет
  `pin_confirm` без кода; журнал дополнен `device_id`, `session_auth_method`,
  `document_text_snapshot`; таблица `client_devices` на 30 дней; очередь push;
  доверенное устройство снимает блокировку. Тесты 2/2
- ✅ Добавлен обратный property: клиент без `access_code_hash` через
  `verify_client_pin_v3` входит успешно. Вместе с блокирующим кейсом —
  двусторонняя проверка гейта, страхует от сценария «закрыли путь всем»
- ✅ Тесты доведены с двух до восьми property, отдельный кейс на каждое
  свойство, 8/8 на staging: одноразовый не срабатывает второй раз; перевыдача
  гасит старый; слабый код отклоняется; сброс куратором рвёт сессии; доверенное
  устройство снимает блокировку; legacy-путь закрыт при наличии кода доступа;
  checkbox-подписание запрещено после миграции; **стыковка — доверенный вход
  проходит, но подписание без кода отклоняется**
- ✅ По дороге найден баг: `NULL` в `sign_consents_with_access_code_by_session`
  не отклонялся, потому что `crypt(NULL, …)` в SQL возвращает `NULL`, а не
  ошибку. Исправлено отдельной миграцией
- ✅ Политика sunset зафиксирована в миграции и в `todo.md`:
  `verify_client_pin_v3` работает только пока `access_code_hash IS NULL`; после
  задания собственного кода — только новый путь; checkbox-подписание через
  `log_consents*` запрещено при наличии кода доступа
- ✅ **Выкачено на прод 11.08 вечером, серверная часть login закрыта.** Три
  миграции применены по отдельности, exit 0 у каждой. Окно legacy проверено:
  `verify_client_pin_v3` отдал `success` и `session_token` без
  `access_code_login_required`. Pre-check подтвердил обе RPC в `pg_proc` — тесты
  не прошли вхолостую; 9/9 на проде до деплоя функций. Деплой трёх функций
  выполнен. Smoke на отдельном тестовом клиенте: trusted login, подписание с
  кодом проходит, без кода — `access_code_required`. Коммит `975a2c070`
- ✅ **Push закрыт 11.08.** `origin/main` = `88433da59`, локальное и удалённое
  сошлись. По дороге: merge с двумя чужими коммитами через ветку-страховку,
  конфликт `index.html` разобран вручную, скоуп `localStorage` доведён до нуля
  ошибок, дрейф палитры разобран по семьям и закрыт вариантом 1B — `--v4-sand-*`
  заведены во всех палитрах, classic остаётся синим. Сканер перестал считать
  совпадения в комментариях. WARN сведены в строку `160 allowlisted, 0 new` с
  падением гейта при росте выше baseline
- ⬜ **Проверить, жив ли WIP виджетов.** `730-widgets-dashboard.css` с
  `--v4-wgt-*` был незакоммичен и переписывался с HEAD — правки могли пропасть.
  Палитра возвращена уже с sand-ролями, но сами наработки по виджетам надо найти
  в другом чате или бэкапе _— 12.08_
- ⬜ **Текст ошибки входа развести на два случая.** Предложение кодера,
  принимаю: сейчас при ненайденном клиенте и при неверном коде экран говорит
  одно и то же — «проверьте код». Надо отличать «код не подошёл» от «вход не
  настроен, попросите куратора перевыпустить», не раскрывая при этом, существует
  ли клиент с таким номером. Именно эта неразличимость стоила сегодня двух часов
  _— до 18.08_

- ✅ **Выданный PIN не срабатывает — причина найдена 12.08.** См. условие
  готовности 2. Прежняя формулировка: Владелец наблюдает его давно, в том числе
  на приглашениях в анкету. Сегодня получена воспроизводимая пара: клиент `пупс`
  (`6b9e20c0-4757-4993-991d-0de24247a2bc`), телефон в панели `78888888888`, PIN
  `1212` выдан дважды — из панели при создании и отдельно кураторским
  инструментом. Вход отклоняется с «PIN не подошёл», в `heys_get_client_health`
  ноль попыток за час.

  Что уже известно по коду: `verify_client_pin_v3` ищет клиента по
  `regexp_replace(phone,'[^0-9]','','g') = <введённые цифры>` **и**
  `pin_hash IS NOT NULL`. Если хеш не записан, клиент не находится вовсе — и
  человек получает «PIN не подошёл» вместо «клиент не найден». Отсюда рабочая
  гипотеза: PIN пишется не туда или не пишется, а сообщение об ошибке маскирует
  причину. Проверять надо одним запросом, гадать бессмысленно.

  **Это не тестовая мелочь.** Если выданный PIN не работает у клиента, не
  работает вход в продукт — все остальные условия готовности при этом не имеют
  значения. Поднято до блокера релиза _— 13.08_

- 🔴 **Создание клиента в панели: сервер отработал, интерфейс завис на
  «Создание…».** Найдено 12.08 при попытке завести тестового клиента. Клиент
  `пупс` в базе создан (`6b9e20c0-4757-4993-991d-0de24247a2bc`), ошибок в
  консоли нет, но модалка не закрывается и кнопка остаётся в состоянии загрузки.
  Владелец при этом не знает, создан клиент или нет, и естественное действие —
  нажать ещё раз, то есть завести дубль.

  Это тот же класс, что «тост „Приём создан“ без проверки записи» из аудита флоу
  еды, только зеркальный: там интерфейс отчитывался об успехе без подтверждения,
  здесь не отчитывается при фактическом успехе. Общая причина — ответ сервера не
  доходит до состояния кнопки _— до 18.08_

- 🔴 **Дефект heys/51cd22 шире, чем записано, и он блокирует любой ручной
  smoke.** Проверено 12.08 по панели куратора: телефоны у тестовых клиентов не
  отсутствуют, а записаны мусором. `+7900163655556` и `+7900165216038` —
  тринадцать цифр, `ant` — `+79999999999`, двенадцать. Российский номер это
  одиннадцать цифр вместе с семёркой. Такой номер невозможно ввести в поле
  входа: маска его не принимает, и человек получает «PIN не подошёл» вместо
  «неверный номер» — то есть сообщение врёт о причине.

  Валидные номера только у трёх: `Александра` (её аккаунт для проверок не
  годится), `Login Smoke Test` `79995550888` (закрыт sunset, `access_code_hash`
  проставлен) и пары `E2E-TestAlex` / `E2E-TestPopl` `70000000001` и
  `70000000002` — они фикстуры автотестов.

  Чинить надо две вещи: валидацию номера при создании клиента и текст ошибки на
  входе. Второе важнее для клиента: сейчас при плохом номере он видит «PIN не
  подошёл» и будет бесконечно просить новый PIN _— до 18.08_

- ⬜ В отчёте по окну legacy не указан UUID проверенного клиента, только
  описание «без `access_code_hash`». Для аудита добить _— до 18.08_
- ⬜ Фронт: `heys_auth_v1.js` переключить на новые RPC по канвасу «Вход v4» _—
  до 18.08_
- ✅ `signature_method`: миграция
  `2026-08-11_consents_pin_confirm_signature.sql` применена к проду. Добавлено
  `pin_confirm`, вычищены `sms_code` и `messenger_code`. Пре-чек подтвердил
  отсутствие строк с удаляемыми значениями (159 в `consents`, 100 в
  `trial_candidate_consents` — везде `checkbox`). Найдено и исправлено третье
  место с тем же перечислением: функция `public.log_consents` дублировала
  whitelist своей проверкой, без её правки ALTER ничего бы не дал. Мёртвая
  ссылка на `sms_code` убрана из `heys_consents_v1.js:474`
- ✅ Проверен весь § 8 соглашения против кода. Три пункта из четырёх сходятся:
  ключ ПЭП и формулировки кнопок совпадают дословно, аутентификация
  действительно предшествует подписи, состав полей журнала совпадает.
  Единственное расхождение — `sms_code` в примере § 8.3, созданное сегодняшней
  же миграцией
- 🟡 Правка § 8.3 уходит в версию 1.11 пакетом, точечно не чинится: текст версии
  1.10 зарегистрирован хэшем, правка разметки без нового цикла разошлась бы с
  зарегистрированным хэшем
- ✅ Push при входе с нового устройства и при сбросе кода куратором — очередь и
  обработчик готовы
- ⬜ Деплой `heys-api-rpc`, `heys-api-auth`, `heys-cron-reminders` — ждёт
  команды владельца _— до 18.08_
- ⬜ Фронт: экраны по канвасу «Вход v4» подключить к новым RPC _— до 18.08_
- ✅ Две правки из трёх сделаны в версии 1.1 и приняты 12.08: пункт 4.1 «б»
  требует ввода кода непосредственно перед подписанием, трансграничная передача
  убрана из 8.1. См. C6
- ⬜ Третья правка: согласовать § 6.3 с политикой — журнал подписания хранится
  дольше данных, и оба документа должны это объяснять. Идёт в пакете 1.11 _— до
  27.08_
- ⬜ Заполнить реквизиты и положить соглашение в `docs/legal/` _— до 18.08_

### A4 · Хвосты по данным — ⬜ _— до 22.08_

**CI разблокирован 12.08, веб выкачен.** `version.json` = `44195e62`, деплой
зелёный, коммиты `b3d6dd4f8` → `44195e627`. Новый фронт на проде — анкету теперь
можно проходить и там.

- ✅ **Legal gate сработал правильно.** В `a62717bfd` из политики убрали слово
  «цикл», не поднимая версию — хэш разошёлся со снапшотом 1.7 и деплой встал.
  Правка содержательная, не мусор. Откачена вместе с такими же в
  `health-data-consent.md`, `consent-forms.md` и на лендинге; содержание уходит
  в пакет 1.11 после минимизации, не отдельным 1.8 — два переподписания подряд
  не делаем
- ✅ Три отставших теста починены 12.08. Четвёртый всплыл уже в CI —
  `messenger-states` по `data-theme$="dark"` и `--v4-ink-2`, закрыт вторым
  коммитом
- ✅ **Мёртвые селекторы в баннере починены 12.08:** `training-summary` и
  `.month-trainings-card` заменены на живой `activity`. Прежняя формулировка:
  `heys_curator_actions_banner_v1.js:160-162` для `training_removed` ищет
  `[data-curator-target="training-summary"]` и `.month-trainings-card` — в JS
  после Activity v4 ни того, ни другого нет. Живой fallback только `activity`,
  поэтому клик «к правке» скроллит на корень вкладки, а не на карточку. У
  `training_added` тот же мёртвый селектор. **Чинить и тест, и селекторы: иначе
  тест позеленеет, а кнопка останется тупой** _— 13.08_

- ⬜ Гейт на хэш согласий. Условие: реестр версий становится единственным
  источником, приложение не может показать версию, которой в реестре нет.
  Миграция написана, не применена _— до 22.08_
- ⬜ Покрыть гейтом обе таблицы: `consents` и `trial_candidate_consents` _— до
  22.08_
- ⬜ Аудит чтений куратором — сейчас не логируется вовсе _— после релиза_
- ⬜ Разбор майской миграции, снявшей маскирование в `audit_logs`; журнал
  перестаёт хранить копию дневников _— до 22.08_
- ✅ `heys-mcp` снималась дважды и дважды возвращалась. Решение владельца от
  11.08, вечер: функция работает как рабочий инструмент куратора (дневник,
  задачник, стенограмма), возвращается через `ALLOW_DISABLED_FUNCTIONS` с
  записанной причиной, в инвентаре остаётся `autoDeploy: false`
- ✅ В письме юристу состояние описано как есть: функция активна, обслуживает
  доступ оператора, внешний OAuth-канал не открыт, обращений от внешних клиентов
  ноль, аккаунтов клиентов через неё не ведут
- ✅ **Пункт 0 закрыт: картина Б.** Данные клиентов в зарубежную модель не
  уходят, трансграничной передачи данных клиентов нет, отмена легализации
  остаётся в силе. Через канал проходят данные супруги. Юрист: исключение для
  личных и семейных нужд на эту конструкцию не ложится — тот же инструмент
  встроен в рабочий контур оператора, разделить «здесь я муж, здесь оператор» в
  одном техническом канале не выйдет. Формально обязанность уведомить по ст. 12
  существует, решение её не исполнять остаётся принятым осознанно. Рамка риска:
  субъект один и он же член семьи, вероятность жалобы нулевая, без жалобы состав
  самостоятельно не выявляется. Риск перестаёт быть низким ровно в одном случае
  — если в канал попадает кто-то ещё
- ⬜ **Свести формулировку к одной во всех документах:** «канал обслуживает
  собственный аккаунт оператора и аккаунт члена семьи; аккаунты клиентов сервиса
  через него не ведутся». Она верна при обеих ролях функции и не устареет при
  следующем изменении. Сейчас в письме одно, в разделе 1 плана другое _— до
  12.08_
- ⛔ **Гейт на два `client_id` не делается. Решение окончательное, к обсуждению
  не возвращаться.** Предлагался дважды — как страховка формулировки и как
  способ сделать триггер исполнимым, — отклонён владельцем оба раза. Не
  поднимать вновь ни по каким основаниям
- ⬜ Взамен гейта: триггер записывается правилом поведения куратора в ту же
  инструкцию, где правило про свободный текст. Формулировка: данные любого
  клиента сервиса в кураторский чат с моделью не попадают ни структурно, ни
  копированием текста; канал обслуживает только собственный аккаунт оператора и
  аккаунт члена семьи _— до 22.08_
- ⬜ **Разнести кураторский контур и внешний OAuth в разные функции.** Юрист
  поднимает приоритет: пока обе роли в одной функции, любое описание состояния
  устаревает при первом изменении. За вечер выбор между инструментом и чистой
  формулировкой вставал дважды _— после релиза_
- ⬜ **Задеплоить `heys-mcp` с правкой порядка стенограммы — но сначала
  воспроизвести симптом.** Проверено 12.08 через облачный контур:
  `transcript/2026-08-12.md` идёт хронологически — первая запись 10:04 сверху,
  последняя 14:55 внизу, запись ревьюера через облачный MCP легла в конец.
  Симптом «блоки сверху» не воспроизвёлся. Значит либо он уже закрыт, либо
  касается другого пути записи. Деплоить ради починки, которой не видно, не
  надо: сначала показать симптом, потом деплой _— до 22.08_
- ✅ Стенограмма и журнал ведутся, проверено 12.08: `transcript/2026-08-12.md` —
  910 строк, `journal/2026-08.md` — 1289 строк с блоками «Вводная / Разбор /
  Итог / Открыто». Путь чувствителен к букве: `transcripts/` во множественном
  отвечает `ok`, но отдаёт пустоту и `rev: 0` — молчаливая ловушка, описана в
  карте артефактов
- ⬜ **Два писателя в стенограмме.** С 12.08 ревьюер-чат пишет
  `tasks_checkpoint` сам, а кодер писал туда же пересказ его сообщений.
  Разделить по природе события, а не по очереди: обмены в чате ревьюера пишет
  ревьюер, работу в кодовых сессиях — кодер. Иначе один обмен ложится дважды, и
  стенограмма протухнет тем же способом, что план _— 13.08_
- ⬜ Закоммитить правки gateway и инвентаря после возврата функции — иначе
  инвентарь и облако снова разойдутся _— до 22.08_

### A5 · Публикация и замороженная копия — 🔵 _— 13.08_

**Инцидент 12.08.** Push в `main` вынес на прод часть незаконченного v4: в схеме
деплоя `push` в `main` = старт публикации, отдельного «выложить код без
публикации» нет. Живой прод — `a7fd195a` (сборка 13:17 MSK); флоу еды
`a7fcc9202` не выкатился, его деплой упал. Решение по откату — в разделе 13,
прод не трогаем.

- ✅ **Упавший деплой `a7fcc9202` разобран 12.08: частичной заливки не было.**
  Run 31587621406, упал `Unit tests (deploy gate) 1/2`, шаг
  `Deploy artifact to Yandex Cloud` пропущен. Проверено по workflow:
  `deploy-artifact` объявлен через `needs: [detect, test, build-artifact]`, то
  есть при красных тестах загрузка физически не стартует. `app.heyslab.ru` и
  ORIGIN оба `a7fd195a`. Задача heys/418cde закрыта. Гейт отработал ровно как
  задуман — это тот редкий случай, когда предохранитель проверили не на
  счастливом пути
- 🔵 **Но из этого следует блокер, которого приёмка не назвала: `main` сейчас не
  деплоится.** Красный тест — `ui-v4-classic-drift`, 20 сдвигов. Скрипт
  `scripts/ui-v4-check-classic-drift.mjs` считает не формальность, а то, что
  реально увидит человек: подставляет значение роли из каноничной палитры вместо
  прежнего литерала и ловит расхождение. То есть коммит еды сдвинул двадцать
  цветов в классической теме. Пока гейт красный, **ни один push в `main` ничего
  не выложит** — включая правки под 1.11, фронт входа и любую срочную починку
  прода. Это выше по приоритету, чем всё остальное в A5 _— 13.08_
- ✅ **Починено и выложено 12.08.** Коммит `679a69769`, деплой зелёный, прод
  `2026.08.12.1451.679a6976` — проверено живым запросом `version.json`. Труба
  открыта, версия 1.11 поедет обычным путём. Прежняя формулировка пункта: Все
  двадцать сдвигов оказались в одном месте — APS food (`600-steps-and-aps.css` и
  `heys_add_product_step_v1.js`). Причина — вложенный
  `var(--v4-sand-ink, var(--v4-ink, #201e1d))`: гейт меряет внутреннюю
  classic-роль, и классика получает синий или зелёный вместо песка. `--fix`
  намеренную перекраску не отменяет — снимает classic-слой, оставляя sand-роль и
  литерал, то есть ровно то решение, которое уже принято: свои sand-роли,
  классику не подгонять. Задача heys/1ab6a1 _— 13.08_
- ⬜ **Антипаттерн повторился, значит это класс, а не случай.** Вложенный
  `var(--sand, var(--classic, #литерал))` уже ловили на палитре виджетов.
  Записать в `apps/web/BUGS_HISTORY.md` рецептом: во внешнем `var()` только
  sand-роль и литерал, classic-роль внутрь не вкладывать — иначе перекраска
  молча меняет цвет в классике, а гейт краснеет на следующем пуше _— 13.08_
- ⬜ Режим работы над v4 до релиза: ветка, а не `main`. Push в ветку деплой не
  запускает, `main` — к релизу. Гейт «skip deploy» не заводим: обходимый гейт
  хуже честного правила _— 13.08_

**Замороженная копия `stable.heyslab.ru`** — сборка прода от 11.08 (`36df9ce3`),
держится ради эталонных скринов старого интерфейса при перекраске. Её природу
надо понимать: **это не песочница.** Тот же API, та же живая база, оба адреса
стоят в CORS-allowlist всех функций. От прода её отделяет только клиентский
`__HEYS_READONLY_MODE__`.

- ✅ Механизм записи проверен 12.08 и сделан правильно: hostname-гейт без
  query-параметра (`index.html`), белый список RPC и отсечка любого не-GET на
  REST (`heys_yandex_api_v1.js:601` и `:696`), баннер в шапке. Белый список, а
  не чёрный — новый пишущий метод не просочится молча
- ✅ **Реализовано и выложено на stable 12.08.** Коммит `4a7ced768` в `main` —
  локальный, не запушен. Гейт согласий в readonly не блокирует вход,
  `log_consents` не вызывается, постоянный баннер «Замороженная копия — только
  просмотр» с `data-testid`. Тесты `consent-readonly-stable.test.js` 4/4.
  Выложено только в бакет `stable-heyslab-ru`, `app.heyslab.ru` остался на
  `41955372` — проверено. Осталась живая проверка входа с PIN
- ✅ **Рецепт сборки записан 12.08** —
  `docs/release/stable-heyslab-build-recipe.md`, коммит `2c8f2ce15`, и копия
  рядом с самой сборкой на `stable.heyslab.ru/STABLE-BUILD-RECIPE.md`. Второе
  важнее первого: рецепт лежит там же, где артефакт, и не потеряется при смене
  репозитория
- ✅ **`version.json` на stable больше не врёт:**
  `36df9ce3+consent-readonly-20260812`, с блоком `stableRebuild` — список патчей
  и имя бандла. Сборка воспроизводима скриптом
  `scripts/stable-rebuild-version.mjs`. `app.heyslab.ru` не тронут
- ~~🔴 Как собран нынешний stable — записать.~~ Прежняя формулировка:
  Cherry-pick на `36df9ce3` дал конфликт в `index.html` и CSS, сборка собрана
  вручную в worktree: база `36df9ce3` + readonly-коммиты `d75ec593d` и
  `3d1904513` + правка согласий. Это не воспроизводится одной командой, и
  человек, который через месяц захочет пересобрать копию, восстановит рецепт
  только из этого абзаца. Положить рецепт рядом с самой копией — в
  `docs/release/` файлом, а не только в план _— 13.08_
- ~~🔴 `version.json` на stable теперь врёт.~~ Закрыто, см. выше. Прежняя
  формулировка: Хеш там остался `36df9ce3`, хотя бандл новый
  (`boot-app.bundle.526ae52da151.js`). Линию сохранили намеренно, но побочный
  эффект: определить, что реально лежит на копии, по версии больше нельзя.
  Сегодня мы уже дважды обожглись на том, что версия отстаёт от факта. Добавить
  в `version.json` копии суффикс сборки или отдельное поле — иначе следующий
  вопрос «какая версия на stable» получит неверный ответ _— 13.08_
- ⬜ **Тупики того же класса, найдены и не чинились** (§1a задания): `AgeGate` —
  `confirmAge()` пишет в API; включение опциональных health-функций —
  `logConsentsBySession`; первый вход и чек-ин — запись в KV. На типичном
  PIN-входе в копию встречаются реже согласий, но встречаются. Чинить тем же
  приёмом, когда понадобится _— к следующему касанию stable_
- ~~🔵 Реализовано локально 12.08, не закоммичено.~~ Прежняя формулировка: В
  рабочем дереве правки в `heys_app_gate_flow_v1.js` (+9) и
  `heys_consents_v1.js` (+69/−21): в readonly гейт не держит вход,
  `log_consents` не вызывается, на экране постоянный баннер «Замороженная копия
  — только просмотр. Согласия не сохраняются» с `data-testid`. То есть основная
  часть задания сделана; осталась трудная — коммит, cherry-pick на `36df9ce3` и
  выкладка только stable-бакета, плюс §1a про тупики того же класса и тесты _—
  13.08_
- 🔵 **Вход на копию заблокирован.** Purge 12.08 отозвал `health_data`, а сборка
  от 11.08 доминимизационная — согласие на здоровье в ней обязательное. Гейт
  требует переподписать, запись блокируется, экран становится тупиком. Лечится
  не разово: после 1.11 поднимутся версии документов и упрётся снова. Задача
  heys/555464, промпт `prompt-stable-consents` _— 13.08_
- ⬜ **Правка идёт двумя путями:** коммит в `main`, где она инертна, и отдельный
  cherry-pick на `36df9ce3` с выкладкой только stable-бакета. Пересборка копии
  из текущего `main` уничтожает эталон — ради него всё и делается _— 13.08_
- ⬜ **Проверить остальные тупики того же класса:** где ещё продвижение зависит
  от успешной записи — первичная инициализация профиля, онбординг, чек-ин.
  «Проверил, других нет» — тоже результат _— 13.08_
- ⬜ Штатного скрипта выкладки stable в репозитории нет: в `deploy-yandex.yml`
  перечислены четыре бакета, stable среди них не значится. Значит выкладка
  ручная — записать команды, которыми она делается _— 13.08_
- ⬜ **Копия публична и отдаёт доминимизационную сборку** — в ней ещё живут
  анкета с вопросами о здоровье и трекинг цикла. Писать она не может, но
  проверить, закрыта ли она от индексации, и решить, нужен ли ей публичный адрес
  вообще _— до 18.08_

---

## 8. Трек B · Персональные данные и документы

### B1 · Минимизация — ✅ выкачена и исполнена 12.08, остались хвосты _— до 20.08_

**Сверка статусов 12.08, поздний вечер.** Аудит показал, что треть строк здесь
стояла ⬜ при уже сделанной работе. Каждая закрытая ниже проверена по коду или
по коммиту, не по отчёту: `heys_health_features_v1.js`
(`CYCLE_TRACKING_IN_RELEASE = false`, `internalAccount`, отклонение `custom_*`),
`heys_trial_intake_v1.js` (`WARNING_TEXT_VERSION = '1.0'`), коммиты `a62717bfd`,
`2bbf90146`, `ce346d7e9`, акт об уничтожении.

- ✅ Решение: медицинские вопросы из анкеты убираются полностью
- ✅ Решение: цикл, замеры и добавки — опциональные функции с отдельным
  согласием
- ✅ **Миграции применены на прод 12.08.** Блокирующий гейт снят:
  `save_trial_intake_by_session` больше не требует согласия на здоровье. По ходу
  починены два дефекта самих миграций — clamp `current_step` 5→4 до CHECK и JSON
  cast в audit. Коммиты `a5ba31a09`, `91078c98e`. Редеплой функций не
  потребовался. `disable_optional_health_features_v1` на этом шаге создана, но
  не вызвана, purge не запускался — и то и другое сделано позже в тот же день,
  см. ниже по треку
- ⏸ **Изменение сведений в реестре — решено не подавать сейчас.** Владелец
  оставляет возможность вернуть сбор данных о здоровье и цикла к релизу. Заявить
  в реестре больше, чем обрабатываешь, нарушением не является — обязанность
  возникает при обратном. Значит по составу данных подавать нечего, и при
  возврате менять тоже не придётся. Подача — один раз, после окончательного
  решения о составе продукта, а не по календарю _— по факту решения_
- ✅ Код написан 11.08, к проду не применялся. Анкета: 5 шагов вместо 6, health
  и safety сведены в один экран предупреждения (schema 1.2), хранится только
  `warning.acknowledged_at` и `warning.text_version`. Убраны health*data consent
  на шаге 1, `INTAKE_ATTENTION_RULES` и флаги «Требует внимания», чек-лист
  куратора переформулирован. Опциональные функции: модуль
  `heys_health_features_v1.js`, цикл/замеры/добавки выключены по умолчанию,
  гейты записи и в приложении, и в MCP, `custom*\*` добавки отклоняются. Тесты
  34/34 и 122/122. ~~Три миграции написаны, ни одна не применена~~ — применены
  на прод 12.08, см. выше
- ✅ Шаги «Здоровье и ограничения» и «Проверка безопасности» сведены в один
  экран предупреждения с подтверждением. Анкета на пяти шагах, текст версии 1.0
  в коде, коммит `2bbf90146`
- ✅ Условия валидности подтверждения выполнены: вариант ответа один, сценарий
  не ветвится, хранятся только `warning.acknowledged_at` и
  `warning.text_version`. **Инвариант остаётся действующим и после закрытия
  пункта:** любое последующее «улучшение» — пометка куратору, отдельный тариф,
  второй вариант ответа — возвращает запись в специальную категорию. Проверять
  при каждой правке анкеты и экрана предупреждения Pro Спорт
- ✅ Флаги «Требует внимания» и `INTAKE_ATTENTION_RULES` убраны, пункт чек-листа
  куратора переформулирован под новый экран
- ✅ Канвас «Цикл» готов (отдельный файл `Цикл_v4_dc.html`): пять экранов —
  зачем включать, отдельное согласие, подпись кодом, включённое состояние, отзыв
  с удалением. Подпись той же шторкой, что у общих документов; отключение и
  удаление одним действием
- ⏸ Канвас «Цикл» уходит в архив вместе с функцией — расхождение обещаний с
  кодом становится неактуальным до возврата. Прежняя формулировка: **обещания
  канваса не обеспечены кодом.** Куратор видит только цель дня, но не фазу и не
  даты; даты хранятся отдельно и не попадают в выгрузки куратора; при отключении
  удаляются сразу. Кураторский контур сейчас умеет писать `cycle_day` и читать
  профиль. Юрист: обещание в согласии, не обеспеченное кодом, хуже отсутствия
  обещания — это довод о недостоверности предоставленной субъекту информации.
  Либо код приводится в соответствие до выпуска согласия, либо обещание
  убирается из текста _— до 20.08_
- ✅ **Проверка закрыта 11.08: вариант «даты не покидают устройство» не
  держится.** Все четыре условия юриста — «да», подтверждено кодом с `file:line`
  и SQL. На проде уже 44 строки с `cycleDay` и 3 профиля с включённым трекингом.
  Артефакт: `docs/release/lawyer-cycle-device-check.md`
- ✅ **Подтверждено вторым разбором юриста:** он независимо предложил тот же
  третий вариант — не выпускать функцию к релизу. Против device-only высказался
  прямо: при объёме L и десятке точек записи «сделать наполовину» не риск, а
  вероятный исход, а обещание в согласии, не обеспеченное кодом, хуже честного
  «да». Purge сорока четырёх строк убирает специальную категорию из продукта
  целиком, и из записи в реестре снимается «здоровье»
- ✅ **Решение владельца 11.08: трекинг цикла выключается до релиза совсем.** Не
  device-only и не паспортные реквизиты — функция снимается с витрины и из
  продукта до запуска, возвращается позже, когда будет время сделать device-only
  спокойно. Обоснование: до релиза восемнадцать дней, неделя из них на переделку
  функции, которой пользуются три собственных профиля, — плохой размен. Плюс
  продуктовый довод владельца: обязательный трекинг цикла может отталкивать
  часть аудитории, отсутствие функции на старте не потеря
- ✅ **Исполнено 12.08, локально.** Kill-switch
  `CYCLE_TRACKING_IN_RELEASE = false`, toggle скрыт, enable отклоняется; убраны
  входы в профиле и регистрации; шаг чек-ина, виджет и запись дня цикла не
  требуют; MCP отвечает `cycle_tracking_removed` на три поля; канвас в архив;
  цикл убран из согласия на здоровье, политики и форм. MCP 123/123, release-gate
  4/4. Не закоммичено, не задеплоено — на проде MCP пока со старым гейтом
- ✅ Закоммичено и задеплоено 12.08: kill-switch в `a62717bfd`, флаг и MCP-гейт
  в `ce346d7e9`, прод `2026.08.12.1140.ce346d7e`. Кураторский контур `cycle_day`
  больше не принимает, кроме профиля с `internalAccount`
- ✅ **Исключение для аккаунта супруги, решение владельца 12.08.** Все три
  опциональные функции — цикл, замеры, добавки — остаются включёнными и данные
  не удаляются. Реализация: флаг `internalAccount` на профиле, не список
  идентификаторов в коде. Kill-switch, гейты MCP и purge смотрят на флаг. Промпт
  `prompt-internal-account`
- ✅ Purge выполнен 12.08 с исключением по UUID
  `4545ee50-4f5f-4fc0-b862-7ca45fa1bafc`, акт составлен. Текст предупреждения
  версии 1.0 в коде
- ✅ **Флаг `internalAccount` выкачен 12.08.** Миграция применена, `heys-mcp`
  задеплоена, push прошёл, CI зелёный, прод `2026.08.12.1140.ce346d7e`.
  Проверено через MCP: у обычного профиля запись цикла отклоняется на всех
  путях, у профиля с флагом проходит. В боевом allowlist идентификаторов нет —
  только флаг; UUID остался в seed-миграции, исторических purge и документах,
  это не рантайм
- ⬜ Web UI глазами не проверялся — гейт подтверждён через MCP и по коду.
  Посмотреть при следующей живой проверке _— 29–30.08, вместе с C0_
- **К сведению, не задача.** UUID `4545ee50-4f5f-4fc0-b862-7ca45fa1bafc` остался
  в истории коммитов. Переписывать историю ради этого не надо, но знать стоит:
  это идентификатор, а не имя, и он ни с чем не связан вне вашей базы _— к
  сведению_
- ⬜ **К первому клиенту решить: оформить функции как надо или свернуть.** Флаг
  `internalAccount` при этом остаётся в любом случае — служебные и тестовые
  аккаунты нужны всегда, это не костыль на время.

  Что значит «оформить» для цикла: письменная форма согласия, паспортные
  реквизиты у тех, кто включает, повышенные требования к защите, изменение
  сведений в реестре. Замеры и добавки проще — замеры не специальная категория
  вовсе, добавки на закрытом справочнике тоже.

  Работа на несколько дней плюс юрист. Оценить объём заранее, а не в день
  появления клиента _— оценка до 20.08, решение к первому клиенту_

- ⬜ Сказать юристу одной строкой: в базе останутся данные о цикле одного члена
  семьи. Его подтверждение «спецкатегория ушла из продукта полностью» надо
  сверить с этим фактом _— 13.08_
- ✅ Инвентарь снят 12.08: числа менялись трижды по мере правок. Актуальные на
  12.08 после вызова `disable_optional_health_features_v1` (сбросил 2 профиля):
  `cycleTrackingEnabled=true` — 0; профилей с любым ключом цикла — 3, все
  `false`; дней с непустым payload — 45; дней с любым ключом цикла, включая
  пустые, — 426; замеры — 7 дней; добавки — 264 дня; ответы анкет со
  спецкатегорией — 0; согласия `health_data` — 4 клиентских и 5 кандидатских.

  **Purge выполнен 12.08 в 11:15** по команде владельца, с исключением семейного
  аккаунта. Фактически очищено: 2 профиля с ключами цикла, 245 дней с ключами
  цикла, 5 замеров, 191 день добавок; отозвано 4 клиентских и 5 кандидатских
  согласия `health_data`. Секции `health` и `safety` в анкетах к моменту вызова
  были уже нулевыми — их сняла intake-миграция _— ✅ 12.08_

- ⬜ Юристу: согласие на цикл не разбирать, функции в релизе не будет. Замеры и
  добавки остаются опциональными как решено _— ~~12.08~~ → 13.08, одним
  сообщением вместе с пунктом про цикл члена семьи_
- ⬜ Вернуть цикл после релиза, сразу в архитектуре device-only: даты на
  устройстве, на сервер только готовая цель дня. Смета L, 6–10 дней, лежит в
  `lawyer-cycle-device-check.md` _— после релиза_
- ✅ Канвас «Цикл» дополнен до семи экранов: добавлены отметка начала цикла и
  состояние «функция неприменима». Первый экран разгружен, блок «Кто это видит»
  укладывается в корпус
- ✅ Цифра по лютеиновой фазе исправлена на диапазон «по разным оценкам, 5–10 %»
  вместо точных 8 %
- ✅ Формулировка про выгрузки уточнена: даты не попадают в отчёты и выгрузки
  **для куратора**, в собственной выгрузке клиента они есть — это его данные
- ✅ Решено: замеры и добавки отдельных канвасов не требуют — замеры остаются в
  базовом согласии, добавки закрываются справочником. Это решение, а не работа;
  строка стояла ⬜ по недосмотру
- ✅ Цикл, замеры, добавки выключены по умолчанию у всех, включая существующие
  аккаунты. `DEFAULTS` в `heys_health_features_v1.js`, исключение только по
  флагу `internalAccount`
- ✅ Пока функция выключена, поля не пишутся ничем, включая MCP. Проверено 12.08
  через MCP на двух профилях: обычный отклоняется на всех путях, с флагом
  проходит
- 🟡 Выключение равно отзыву согласия: механика есть — при снятом флаге поля
  вычищаются из дня и профиля (`purgeCycleDataFrom*`, `purgeMeasurementsFrom*`,
  `purgeSupplementsFrom*`). **Не проверено предупреждение до отключения** — по
  решению оно должно показываться раньше удаления, а не после. Сверить на экране
  _— до 20.08_
- ✅ Добавки: закрытый справочник, произвольная строка отклоняется —
  `value.startsWith('custom_')` возвращает `false` в
  `heys_health_features_v1.js`
- ✅ **Уже собранные данные о здоровье удалены 12.08, акт составлен** —
  `health-data-destruction-act-2026-08-12.md`. Порядок соблюдён полностью:
  миграции → живая анкета → инвентарь владельцу → команда → purge → акт.
  Исключение семейного аккаунта зафиксировано в акте отдельной строкой, с
  основанием и составом сохранённого
- 🟡 Свободные текстовые поля: подход выбран юристом — оставить, запретить в
  правилах, удалять при обнаружении. Четыре условия, без которых это декларация:
  - ⬜ прямой запрет в правилах и в согласии: сервис не предназначен для
    сообщения сведений о заболеваниях, диагнозах и препаратах, такие сведения не
    запрашиваются и не хранятся _— до 20.08_
  - ⬜ письменная инструкция куратору — документ, а не строка плана: сведения о
    здоровье из разговора не переносятся в структурированные поля и в саммари _—
    до 20.08_
  - ⬜ процедура удаления при обнаружении, с журналом: что удалено, когда, кем.
    Без журнала процедуры не существует _— до 20.08_
  - ⬜ отсутствие структурированных полей под такие сведения — закрывается
    минимизацией _— до 20.08_ Процедура должна покрывать расшифровку голосовых и
    фото: на снимке может оказаться упаковка препарата

### B2 · Документы против факта — ⬜ _— до 27.08_

**Хвосты, всплывшие при работе 12.08 и не относящиеся к другим трекам:**

- ⬜ Строка про срок ответа куратора убрана из анкеты, потому что обещания нет
  ни в оферте, ни в договорённостях. Если срок разбора анкеты нужен — сначала
  решить его и внести в оферту, потом показывать на экране _— до 27.08_
- ✅ Проверено 12.08: автосохранение двигает `updated_at`

**Telegram — трансграничная передача, опорный факт пакета оказался неверным.**
Второй разбор юриста 11.08: если клиент выбрал Telegram, сообщение идёт через
Telegram, и он же назван в таблице внешних получателей. Оговорка «канал по
выбору клиента» правового значения не имеет — бот развернули вы, отвечаете через
него вы. Решение из раздела «Отменено» принималось про зарубежные LLM и Telegram
не покрывает. Заодно делает недостоверной запись в реестре «трансграничная
передача не осуществляется».

- ✅ **Решение владельца 11.08, уточнённое:** Telegram убирается из вариантов
  канала связи с куратором — переписка, фото и голосовые целиком в мессенджере
  приложения. **Кандидатский флоу остаётся на Telegram как есть:** выдача PIN,
  ссылка на вход, доведение человека до анкеты. Он уже настроен и работает, а
  получить доступ иначе кандидату негде — приложения у него ещё нет.

Разница между двумя конструкциями существенная: в первой через зарубежную
инфраструктуру шло содержание — что человек ел, как спал, на что жалуется. Во
второй — код доступа и ссылка.

- ✅ **Решение владельца 12.08: в форме заявки на пробную неделю выбор удобного
  канала связи ОСТАЁТСЯ.** Прежняя формулировка «убрать из выбора канала везде»
  была шире, чем нужно, и я её сузил по возражению владельца. Обоснование: это
  поле не обещает вести там переписку — оно отвечает на вопрос «куда вам
  написать, чтобы начать». Через выбранный канал идёт приглашение: код доступа и
  ссылка, без содержания о питании и здоровье. Ровно та же конструкция, что уже
  принята для кандидатского флоу.

  Граница, за которую это решение не распространяется: как только через канал
  пойдёт содержание — что ел, как спал, фото, голосовые, — он перестаёт быть
  способом первого контакта и попадает под всё, из-за чего Telegram убирали.
  Поле остаётся полем «куда написать», не превращается в «где мы будем
  общаться».

  **Ручная отправка в WhatsApp и MAX — решение владельца 12.08: делаем, проблемы
  нет.** Интеграции у этих двух каналов нет, куратор пишет руками со своего
  телефона; для клиентского сервиса это нормально и отказываться от удобства
  ради единообразия не стоит. Юрист 12.08 признал доставку приглашения через бот
  допустимой, а ручная отправка — та же конструкция без автоматики.

  Единственное, что к этому прилагается: граница та же, и держать её теперь
  нечем, кроме дисциплины. У бота есть текст, который не меняется, и
  предупреждение «не отправляйте сведения о здоровье»; у ручного сообщения нет
  ни того, ни другого. Поэтому граница пишется в ту же инструкцию куратору, где
  правило про свободный текст: в мессенджер уходит приглашение и статус заявки,
  содержание — никогда. Механизма здесь не будет, и это осознанно принятая
  дисциплина, а не недосмотр _— решено 12.08_

- ⬜ **Переписка с куратором — только в мессенджере приложения.** Это касается
  профиля и всего, что после старта: содержание услуги идёт там, где дневник.
  Проверить, что в профиле не осталось выбора канала для переписки _— 13.08_
- ⬜ **Посмотреть, что бот сейчас отправляет кандидату.** Если только PIN и
  ссылка — вопрос к юристу простой. Если есть имя, детали заявки или что-то
  содержательное — сузить, тогда и ответ вероятнее в нашу пользу _— 13.08_
- ✅ **Ответ юриста получен 12.08: бот при регистрации допустим.** Кандидатский
  флоу остаётся как есть — доставка кода доступа и ссылки через Telegram, без
  имени и содержания. Вопрос закрыт, флоу не перестраиваем.
- ✅ **Уточнение получено 12.08, ответ — «передача есть».** Юрист: адресация
  сама по себе уже передача — вы сообщаете Telegram, что этот пользователь
  является кандидатом HEYS; плюс PIN как аутентификационный реквизит и токен в
  ссылке. Он же признал, что в четвёртом разборе был неточен: служебное
  сообщение без содержания уменьшает объём передачи почти до нуля, но не
  устраняет её. Единственное состояние без передачи — не использовать внешний
  мессенджер для связи с клиентами вообще.

  **Следствие:** обе формулировки недостоверны — «передача на территорию
  иностранных государств не осуществляется» в согласии 1.0 и «трансграничная
  передача: нет» в записи реестра. И это не только про Telegram: push через FCM
  и APNs — та же конструкция.

- ✅ **РЕШЕНО 12.08: подаём одно уведомление по ст. 12 на три канала.**
  Владелец. Ниже — порядок работ и границы решения. Прежняя развилка:
  Рекомендация юриста — подавать. Что это даёт: push можно наполнить
  содержанием, ручная отправка приглашений в WhatsApp и MAX перестаёт держаться
  на дисциплине куратора, три документа становятся достоверными правкой одной
  формулировки. Цена: десять рабочих дней рассмотрения, идут параллельно работе.

  **Уточнение юриста 12.08, вечер: рубеж — не дата релиза, а первое приглашение
  постороннему человеку.** Сегодня передачи чьих-либо персональных данных нет:
  собственный аккаунт оператора, аккаунт члена семьи и тестовые номера под
  определение не подпадают. Значит ретроспективного дефекта нет,
  приостанавливать кандидатский флоу и push не нужно, а уведомление подаётся о
  намерении — ровно как норма задумана. Квалификация при этом в силе: отправка
  кода через Telegram остаётся трансграничной передачей, и «передача не
  осуществляется» станет недостоверным — но с первого реального кандидата, а не
  сегодня.

  **К сведению на будущее: юрист за день дважды поправил себя** — сначала по
  таблице получателей, потом по «нарушение уже существует». Обе поправки в нашу
  пользу и сделаны им самим, но вывод простой: его ответы — такой же артефакт,
  как документы и код, и к ним применяется то же правило. Опираться на них
  можно, считать окончательными до перепроверки — нет.

  **Это не возврат отменённого решения**, хотя выглядит похоже. В разделе 14
  отменена легализация передачи _данных клиентов в зарубежную модель_ — там речь
  шла о содержании дневников и о необеспечимой обязанности по ч. 14 ст. 12.
  Здесь состав другой: номер, факт кандидатства и код доступа. На таком составе
  обязанность обеспечить уничтожение исполнима, и довод, который год назад топил
  идею, почти ничего не весит _— решение владельца, 13.08_

- 🔴 **WhatsApp принадлежит Meta и в России заблокирован — из состава
  уведомления и из выбора каналов убрать.** Проверено 12.08 по открытым
  источникам: с февраля 2026 Роскомнадзор снял домены WhatsApp из национальной
  системы доменных имён, доступ остался только через VPN. Значит канал не
  работает у клиента и не работает у куратора без VPN — обещать его в форме
  заявки нельзя независимо от 152-ФЗ. И отдельно: заявлять в уведомлении
  передачу сервису, который сам регулятор блокирует, — лишний повод к отказу по
  всему уведомлению. Заявляем только то, чем реально пользуемся _— 13.08_

- 🔴 **Вся воронка входа стоит на Telegram, а его ограничивают.** Первое
  касание, выдача кода, ссылка на анкету, drip-напоминания — всё через бота.
  Telegram работает, но под нарастающими ограничениями, и это риск не
  юридический, а операционный: если канал деградирует, вход кандидата в продукт
  исчезает целиком, а запасного пути нет. **Решение владельца 12.08: резерв —
  СМС.** В уведомление он не идёт: SMS.ru российский, трансграничной передачи
  нет, провайдер уже назван в `subprocessors.md` §4. Значит уведомление остаётся
  на двух получателях — Telegram и Google с Apple.

  Три вещи, без которых это не резерв, а намерение:

  **Уточнение 12.08 по вопросу владельца «почему куратор не может написать СМС
  сам».** Может, и на нынешнем масштабе этого достаточно: пятнадцать мест,
  приглашение — код и ссылка, отправляется руками за минуту. Мои три пункта про
  срочное восстановление функции были переусложнением: автоматика нужна не для
  того, чтобы отправить сообщение, а для того, что руками не делается.
  - ⬜ **Ручная отправка объявляется резервом прямо сейчас** — ничего
    восстанавливать не надо, канал доступен всегда. Условие одно: приглашение
    отправляется с рабочего номера, а не с личного, и в тексте прямо сказано,
    что ответы — в приложении. Иначе клиент ответит на СМС, переписка начнётся в
    телефоне куратора, и правило «всё в одном месте» тихо умрёт в первый же день
    _— 13.08_
  - ⬜ **Записать триггер переключения:** что считается «телеграм не работает» и
    кто это объявляет. Дёшево и делается один раз _— 13.08_
  - ⬜ **Функция `heys-api-sms` — к первому набору, не раньше.** Руками не
    делается автоматика: drip-напоминания и предупреждение за два дня до чистки
    незавершённых анкет. Пока кандидатов нет, доставлять нечего. Помнить, что
    функция удалена из прода и в инвентаре у неё `autoDeploy: false` — возврат
    осознанный, не в аварии _— к первому набору_

  Обоснование, чтобы не выглядело возвратом отменённого: в разделе 14 отменена
  **СМС как канал доставки кода для ПЭП** — там довод был «ключом подписи
  объявлен собственный код клиента, доставка законом не предписана». Здесь
  другое назначение: доставка приглашения кандидату, у которого приложения ещё
  нет. Прежний довод к этому не относится _— решено 12.08_

- 🔴 **Ответ юриста 12.08: факта подачи недостаточно, ждать надо окно.** Ни
  одного получателя нет в перечне государств с адекватной защитой (приказ РКН №
  128 от 05.08.2022): США там не было никогда, ОАЭ не включены — из
  ближневосточных в перечне только Катар, — Британских Виргинских островов тоже
  нет. Значит по ч. 11 ст. 12 передача не допускается до истечения срока
  рассмотрения, и режимы Telegram и push не разделяются: ждём одинаково по всем
  трём. **Подача открывает отсчёт, а не канал.** При подаче 13.08 без запроса
  сведений окно закрывается около 27.08; при запросе — плюс до трёх недель.
  Рубеж «первый настоящий кандидат» наступает позже, запас есть, но меньше, чем
  казалось. Точное лицо со стороны Telegram (`Telegram FZ-LLC`, Дубай, либо
  `Telegram Messenger Inc.`, БВО) установить для формы всё равно надо — на режим
  это не влияет, обе юрисдикции вне перечня

- ✅ **Как устроены push — проверено по коду 12.08: собственный веб-пуш с VAPID,
  Firebase нет.** Клиент вызывает `pushManager.subscribe` с
  `applicationServerKey` (`heys_push_v1.js:218`), сервер шлёт библиотекой
  `web-push` через `setVapidDetails` и хранит сырой `endpoint` браузера
  (`heys-api-push`, `push_subscriptions`). Единственное упоминание firebase в
  репозитории — кэш сборки старого андроид-приложения. **Следствие: получателя
  выбирает не оператор, а браузер клиента** — Chrome и Android уходят к Google,
  Safari к Apple, Firefox к Mozilla, Edge к Microsoft

- ✅ **Решение владельца 12.08: поддерживаем Safari и Chrome.** В уведомлении по
  push заявляются два получателя — Google LLC и Apple Inc., оба США. Mozilla и
  Microsoft не заявляются

- 🔴 **В оценке правового режима два утверждения описывают меры, которых пока
  нет. До подписи это надо развести.** Найдено при вычитке документа 12.08.

  Раздел 5, пункт 5: «код доступа, направляемый через внешний канал, является
  одноразовым» — **сейчас неверно.** Кандидатский код работает повторно и
  открывает сессию на семь дней, это проверено сегодня и записано в C4. Там же и
  в разделе 1: «приём подписки ограничен перечнем допустимых адресов служб
  доставки» — **гейта тоже пока нет**, проверено по коду `heys-api-push`.

  Обе меры приняты решением и обе не реализованы. Подписывать документ, где они
  описаны в настоящем времени, нельзя: это ровно тот класс «документ описывает
  не то, что есть», который юрист сам назвал час назад, — только теперь в его
  собственном тексте и с подписью оператора под ним.

  **Развязка есть и она дешёвая.** Передача не начинается до истечения окна, то
  есть до конца августа. Значит либо переформулировать оба пункта как меры,
  обеспечиваемые до начала передачи, либо реализовать их до подписи. Второе
  честнее и успевается: гейт на хосты — несколько строк, одноразовый код —
  задача, которая и так стоит по ст. 19 _— до подписи оценки_

- ⬜ **Отклонённую подписку логировать, а не ронять молча** (просьба юриста к
  реализации гейта). Иначе смена адреса службы доставки на стороне браузера
  выключит push у всех, и узнаете вы об этом от клиента _— вместе с гейтом_

- 🔴 **Но решение о поддержке двух браузеров надо сделать механизмом, иначе оно
  ложное.** Сейчас код подпишет любой браузер и сервер отправит на любой
  `endpoint` — Firefox или Edge подпишутся молча, и заявленный перечень
  получателей разойдётся с фактом в первый же день. Это ровно тот класс, который
  мы ловили сегодня весь день: документ описывает не то, что есть. Нужен
  серверный белый список хостов на `/push/subscribe` — принимать только
  `fcm.googleapis.com` и `web.push.apple.com`, остальные отклонять с внятной
  причиной. Несколько строк, и заявленное становится обеспеченным _— до подачи
  или сразу после, но до первой отправки постороннему_

- 🔴 **Проверено по коду 12.08: отказ от согласия ничего не выключает — а по
  заявленному основанию должен.** Основанием передачи в форме заявляется
  согласие, значит отказавшийся не должен получать ни приглашения, ни push.
  Фактически: в `heys-api-push` проверки согласия нет вовсе, ни одного
  упоминания; клиентская сторона лишь не подписывает устройство в момент отказа
  (`notificationsOptIn` в `heys_consents_v1.js`). То есть если токен устройства
  уже есть, а согласие потом отозвано, push уйдёт. У лидов гейт как раз есть —
  `heys-api-leads` отклоняет заявку без корректного consent-payload. Чинить надо
  серверную отправку push: отказ и отзыв должны гасить доставку, а не только
  запись _— до первой отправки постороннему_

- ⬜ **Что на владельце до подачи, по списку юриста:** выгрузить в PDF с датами
  условия Telegram, Google и Apple (меры защиты, условия прекращения обработки,
  реквизиты); установить точное лицо Telegram по действующим условиям; составить
  и подписать оценку правового режима — датой раньше подачи, структура в
  разборе; подписать документ о принятии риска по ч. 14 ст. 12; сверить
  фактический состав push с тем, что заявляем. Первые три на критическом пути,
  юрист вычитает оценку перед подачей _— 13.08_

- ⬜ **Текст для согласия 1.0 готов** — строка «передача не осуществляется»
  удаляется, вместо неё таблица трёх получателей с составом и прямая фраза, что
  переписка, фото, голосовые и дневник за границу не идут. **Абзац про
  направленное уведомление вставляется только после фактической подачи** — до
  этого он недостоверен. Раздел обработчиков дополняется теми же тремя _— в
  пакете 1.11_

- ⬜ **Порядок работ по уведомлению — три шага, первый не наш.**
  1. _Собрать сведения о получателях, до подачи._ По каждому из трёх — Telegram,
     Google, Apple — нужны принимаемые меры защиты, условия прекращения
     обработки, наименование и адреса, и сведения о правовом регулировании в его
     юрисдикции. Плюс собственная оценка правового режима как документ с датой:
     в уведомлении указывается дата её проведения, значит она должна
     существовать артефактом, а не мнением. Это работа юриста, он же и
     рекомендовал подачу — просить у него, а не собирать самим _— 13.08_
  2. _Подать уведомление_ на портале РКН, отдельной формой, не правкой записи в
     реестре. Состав — узкий, как записано в разделе 13 _— 13.08_
  3. _Держать сведения готовыми к запросу:_ регулятор вправе истребовать их в
     течение десяти рабочих дней _— после подачи_

  Отдельно проверить перечень государств с адекватной защитой на дату подачи:
  критерий менялся после 265-ФЗ. Рассчитывать на присутствие там нужных
  юрисдикций не стоит, но посмотреть надо — от этого зависит, обязательно ли
  ждать окно до начала передачи _— 13.08_

- ⬜ **Пока окно не истекло — передача не начинается.** Практически это значит:
  до закрытия окна ничего не меняем, кандидатский флоу работает как сейчас, push
  остаются обезличенными. Наполнять push содержанием и расширять переписку —
  только после _— до 27.08_

- ⬜ **Хвост по ст. 19, проверяемый и дешёвый.** Юрист: код, отправленный в
  мессенджер, — это доступ к аккаунту в чужом канале, и он должен быть
  одноразовым. У кандидата сейчас **не одноразовый**: тот же `pin_hash` работает
  повторно и выдаёт новую сессию на семь дней (проверено 12.08, C4). То есть
  условие юриста на кандидатском флоу не выполняется. Для клиентского входа это
  закрывает новая схема, но её фронт не выкачен — ещё одна причина не тянуть с
  A3 _— 13.08_

- ~~⬜ **Уточнить одну формулировку у юриста — от неё зависят два документа.**~~
  Отвечено, см. выше. Прежняя формулировка: «Допустимо» и «трансграничной
  передачи нет» — разные утверждения. Если он квалифицировал доставку кода как
  не являющуюся трансграничной передачей, то фраза «передача на территорию
  иностранных государств не осуществляется» в согласии на обработку ПДн и запись
  в реестре остаются верными и правки не требуют. Если же он сказал «передача
  есть, но допустима на таком основании», обе формулировки становятся
  недостоверными, и их надо менять — в пакете 1.11 и при ближайшей подаче
  сведений. Спросить одной строкой, ответ короткий _— 13.08_
- ⬜ Запись в реестре «трансграничная передача не осуществляется» проверить
  после ответа юриста: от него зависит, надо ли её менять _— до 20.08_
- ⬜ Существующая переписка через Telegram, если она есть, — в инвентарь и в
  общий purge _— до 20.08_
- ⬜ **Лендинг: `FaqD.tsx:76` обещает Telegram, WhatsApp и MAX как место
  переписки.** **Решение владельца 12.08: переписать, спозиционировав как
  преимущество, а не как ограничение.** Вся переписка в одном месте вместе с
  дневником — куратор видит ваш день и вашу неделю там же, где отвечает, ничего
  не теряется между приложением и мессенджером, история разбора лежит рядом с
  историей еды. Это сильнее исходного списка мессенджеров: список обещал
  удобство, а единое место даёт качество работы. Формулировать от выгоды
  клиента, без слов «нельзя», «только» и «в целях безопасности» _— 13.08_

  **Уточнение 12.08, после вопроса владельца «почему нельзя просто удобный канал
  связи».** Возражение справедливо по сути, но к этому тексту не относится, и
  различать надо три вещи.

  _Первое — что там написано на самом деле._ Не «как с нами связаться», а
  «Куратор на связи ежедневно, 09:00–21:00, ориентир ответа и внесения данных в
  дневник — 1–2 часа». Это обещание вести через мессенджер саму услугу:
  содержание питания, самочувствия, фото. Дверной звонок — это «напишите, чтобы
  начать», и именно поэтому кандидатский флоу на Telegram оставлен: там идут код
  и ссылка, без содержания.

  _Второе — двух каналов из трёх не существует._ Проверено по коду: в схеме
  `messenger IN ('telegram','whatsapp','max')` — это поле предпочтения;
  интеграция есть только у Telegram (бот, `telegram_chat_id`, drip). `whatsapp`
  и `max` — просто подписи в уведомлении куратору. То есть витрина обещает два
  канала, которых в продукте нет; это дефект обещания сам по себе, до всякого
  152-ФЗ.

  _Третье — MAX юридически не равен двум другим._ Правило юриста звучит как
  «через инфраструктуру за пределами РФ персональные данные не проходят ни в
  одну сторону». MAX российский, под это правило не подпадает и запись в реестре
  про трансграничную передачу не ломает. Если удобный внешний канал для
  содержания принципиально нужен — рабочий кандидат один, и это MAX: его надо
  раскрыть как получателя и внедрить, а не просто оставить подпись в списке _—
  решение владельца_

- ⬜ Оферта и политика: описание канала связи привести к факту, в пакете версии
  1.11 _— до 27.08_

**Доставка вместо Telegram: push плюс приложение на домашнем экране.**
Функционально мессенджер приложения не хуже — переписка, фото, голосовые.
Разница была в том, что телеграм и так открыт; закрывается push-уведомлением
«куратор ответил» со ссылкой прямо в диалог. Уведомления уже работают и
обезличены.

- ⬜ **Проверить активацию по всей воронке, а не только у клиента.** Кандидат
  проходит анкету до того, как у него появится приложение: он получает PIN,
  входит по ссылке в браузере, заполняет шесть шагов, ждёт решения куратора.
  Уведомления в этот момент недоступны — а именно тут человек ждёт ответа
  сильнее всего. Понять, чем его уведомлять о решении по заявке и о просьбе
  уточнить ответ _— до 18.08_
- ⬜ Проверить, на каком шаге предлагается добавить на домашний экран и не
  приходится ли это на момент, когда человек ещё не понимает, зачем ему
  приложение _— до 18.08_
- ⬜ iOS: push в PWA работает только после добавления на домашний экран.
  Убедиться, что без этого шага клиент не остаётся без уведомлений молча _— до
  18.08_
- ⬜ Оферта и политика: описание канала связи привести к факту, в пакете версии
  1.11 _— до 27.08_
- ⬜ Telegram-бот остаётся в перечне обработчиков, но с изменённым назначением:
  доставка уведомлений, не переписка _— до 27.08_

- ✅ `subprocessors.md` §4 приведён к факту, SMS-провайдер назван
- ✅ §6 остаётся в силе: клиенты через зарубежную модель не ведутся
- ✅ Уведомление в РКН не подаётся — решение владельца
- ⬜ Срок действия в текстах согласий. Самая дешёвая правка, одна строка _— до
  27.08_
- ⬜ Активные согласия от 09.08 имеют `ip ::1` и `user_agent node` — созданы
  скриптом. Для своих аккаунтов нормально, на боевом контуре такая запись
  доказательством волеизъявления не является. Поставить проверку на запись _— до
  20.08_
- ⬜ **Возраст.** Юрист: корректнее ограничить, а не собирать согласие
  представителя — проверка полномочий и обработка данных ещё одного субъекта
  несоразмерны масштабу. Нужно: возрастной ценз в оферте прямой строкой (идёт в
  пакет 1.11); отказ при несоответствии на входе, а не просто запись года
  рождения; заявление клиента о достижении возраста в том же акте, где
  принимается оферта; процедура на случай выявления после начала обслуживания —
  прекращение обработки и удаление _— до 27.08_
- ⬜ **Платёжный провайдер.** Юрист: в большинстве случаев он не обработчик по
  поручению, а самостоятельный оператор — обрабатывает данные плательщика в силу
  закона о национальной платёжной системе, а не по вашему указанию. Раскрывается
  как получатель с указанием цели передачи. Проверить три точки: назван ли
  поимённо в политике; указаны ли цель и состав передаваемых данных; согласуется
  ли статус в `subprocessors.md` с договором _— до 20.08_
- ⬜ Шифрование health-полей: заявлено, покрывает 74 строки из 451. Делать после
  минимизации _— до 27.08_
- ⬜ Журналирование доступа: заявлено, отсутствует _— до 27.08_
- ⬜ Retention бэкапов: 365 дней против обещанных 14, плюс противоречие 7 против
  14 внутри документов _— до 27.08_
- ✅ Проверено: конфликт «журнал подписания 3 года против 30 дней политики» был
  мнимым. Политика не обещает удаление всего за 30 дней — этот срок относится
  только к отклонённой предтриальной анкете, а доказательства согласий и раньше
  хранились 3 года. Уточнение внесено явно в оба документа, чтобы не всплыло
  снова
- ⬜ **Отзыв согласия: механизм есть, покрытие узкое.** Проверено по коду 12.08,
  `database/2026-07-27_trial_intake_flow.sql:582`. Что
  `revoke_consent_by_session` реально делает: помечает согласие отозванным; при
  `health_data` и `personal_data` гасит все сессии клиента; при `health_data`
  удаляет из `client_kv_store` строки по `is_health_key(k)` и триггером сносит
  отдельно зашифрованную анкету, возвращая `deleted_keys`. То есть «отзыв ничего
  не удаляет» — неверно, и представление, что это сделано давно, имеет
  основание.

  **Чего он не делает:** не трогает переписку `client_messages`, транскрипты
  голосовых, фото в бакете, снапшоты и `audit_logs`. Юрист: это прямое
  неисполнение ч. 5 ст. 21 в продуктивной среде, срабатывает при первом же
  отзыве.

  **И перекос, который виден только теперь.** Удаление привязано к `health_data`
  — к той самой узкой категории, которую минимизация только что опустошила:
  после purge health-ключей почти не осталось, значит `deleted_keys` будет около
  нуля. А дневник, переписка и фото живут под `personal_data`, где отзыв не
  удаляет ничего, кроме сессий. Механизм стоит на категории, где данных больше
  нет, и отсутствует там, где они есть. Это меняет постановку задачи по условию
  8: не «дописать удаление», а перевесить его на верную категорию и покрыть
  остальные хранилища _— до 22.08_

- ✅ Проверено 12.08: `purge_health_data` — другой путь, чистит только
  `client_kv_store` у клиента, к retention анкет отношения не имеет; права у
  `heys_rpc` отозваны. Очистка отклонённых кандидатов делает настоящий `DELETE`,
  шифротекст уходит со строкой — не пометка
- ⬜ Решить, нужен ли `purge_health_data` вообще, раз он не вызывается и прав
  нет _— до 27.08_
- ⬜ DSAR-экспорты через API не регистрируются _— до 27.08_
- ⬜ Версии документов в трёх местах отстают от действующих _— до 27.08_
- ✅ Проверено вторым разбором: хэши в трёх выгрузках сходятся с реестром и
  манифестом — цепочка «версия → хэш → снимок текста» работает, ядро
  доказательственной конструкции построено правильно
- ⬜ Публичный доступ к фото нигде в согласиях не описан — снимается вместе с A1
  _— до 27.08_
- ⬜ Бэкапы, конструкция подтверждена юристом: удаление из продуктивной среды не
  более 30 дней с даты отзыва; **реестр удалённых субъектов, применяемый при
  любом восстановлении** — ядро конструкции, без него остальное косметика; срок
  ротации 35 дней прямо в политике; отсутствие доступа к копиям вне процедуры
  восстановления; шифрование копий (незашифрованные при утечке — отягчающее,
  штрафы оборотные); устранить внутреннее противоречие «7 против 14» _— до
  27.08_

### B3 · При найме кураторов — ⬜ рубеж не первый клиент, а первый наёмный куратор _— после релиза_

- ~~Приказ о назначении ответственного за организацию обработки~~ — не
  требуется: обязанность установлена для операторов-юрлиц (ч. 1 ст. 22.1), у ИП
  её нет
- ⬜ Перечень допущенных лиц с указанием данных и целей доступа _— после релиза_
- ⬜ Обязательство о неразглашении _— после релиза_
- ⬜ Разграничение прав в системе и журналирование доступа, см. A4 _— после
  релиза_
- ⬜ Для внештатных — договор поручения обработки и раскрытие в согласии _—
  после релиза_

---

## 9. Трек C · Лендинг

### C0 · Живые проверки перед релизом — ⬜ за владельцем _— 29–30.08_

Копятся, потому что делаются только с реального телефона и браузера. Делать
одним заходом, минут двадцать, до релиза.

- ⬜ Фото и голосовые открываются из встроенного браузера Telegram
  (авторизованный путь чувствителен к кукам вебвью, оттуда заходят клиенты) _—
  29–30.08_
- ⬜ Цепочка «отказ сервера → текст на экране» после того, как освободится бандл
  _— 29–30.08_
- ⬜ Блокировка по номеру целиком: пять неудач, сообщение ограничителя, что в
  нём сказано делать _— 29–30.08_

### C1 · Подготовка — ✅

- ✅ Четырнадцать карточек разобраны, статусы и источники проставлены
- ✅ Сборка актуализирована, введено правило «при расхождении побеждают код и
  документ решений»
- ✅ Решения по шести развилкам приняты и записаны
- ⬜ **Передать дизайнеру файл «Правка карточек для дизайнера»** — блокирует всё
  ниже

### C2 · На отрисовку — ⬜ ждёт согласования _— до 22.08_

Владелец проходит лендинг блок за блоком по сборке сравнения и утверждает версии
визуально. Решения приняты в разговоре, но глазами не утверждены — до этого
промпты `prompt-landing-canvas` и `prompt-landing-text` остаются черновиками и
не отправляются.

- ⬜ Три состояния карточки Self: во время акции, последние дни, после окончания
  _— до 22.08_
- ⬜ Выбор одной из двух подач цены в акции _— до 22.08_
- ⬜ Блок 05 «Кто ведёт»: компоновка под тезисы, положение фото вполоборота _—
  до 22.08_
- ⬜ Сцена диалога в 02: разрыв ночи между вечерним ответом и утренним разбором
  _— до 22.08_
- ⬜ Экран предупреждения Pro Спорт — **без сбора ответов**, решение 12.08, см.
  раздел 13 — и блок «Чего не делаем» в карточке тарифа _— до 22.08_
- ⬜ Экран подтверждения после заявки _— до 22.08_

### C3 · Текстовые правки, отрисовки не требуют — ⬜ ждёт согласования _— до 24.08_

- ⬜ Цена Pro: 11 990 обычная, 7 990 для первого набора, без зачёркивания _— до
  24.08_
- ⬜ Пересчитать «около 270 ₽ в день» _— до 24.08_
- ⬜ Все «тренер» → «куратор» в карточке Pro Спорт и в сравнительной таблице _—
  до 24.08_
- ⬜ Убрать строку «Более 20 лет практики в питании и сопровождении» _— до
  24.08_
- ⬜ Снять «силу воли» на интро квиза и в тексте результата _— до 24.08_
- ⬜ Новая строка в таблице: «Кто замечает, что режим поехал» _— до 24.08_
- ⬜ Новый вопрос в FAQ: «Мне нужно писать первым?» _— до 24.08_
- ⬜ Переписать описание Self: вход в систему, а не конкурент трекерам _— до
  24.08_
- ⬜ Проверить два непроверенных наблюдения: тёмные блоки встык и три ответа на
  возражение _— до 24.08_
- ⬜ Убрать Telegram, WhatsApp и MAX из FAQ (`FaqD.tsx:76`), заменить аргументом
  «вся переписка там же, где дневник». **Не ждёт поблочного согласования** —
  основание и срок свои, см. трек B2 _— 13.08_

Девять правок выше отрисовки не требуют. Десятая, про Telegram, идёт раньше и
отдельной задачей — держать её в C3 только чтобы она не потерялась между
треками. Промпт `prompt-landing-text` написан, но остаётся черновиком до
поблочного согласования.

### C4 · Канвасы v4 — ✅ вход, цикл и анкета закрыты со стороны дизайна

**Анкета v4 принята 12.08.** Пять шагов, экран предупреждения с подтверждением,
сводка отдельным экраном, возврат к недозаполненной, ошибка отправки. Проверено:
спецкатегории нет ни на одном шаге, блокировка отправки называет причину в обоих
местах, счётчик времени сведён с пятью шагами, строка про хранение приведена к
факту после проверки по коду.

**Хвосты анкеты — не про дизайн, закрыть до вёрстки:**

- ✅ **Текст предупреждения вставлен и принят 12.08.** Версия 1.0, коммит
  `2bbf90146`. На канвасе анкеты остаётся пометка «Проект текста — ждёт юриста»
  — обновить при следующем касании макета
- ✅ **TTL заведён 12.08:** незавершённые кандидаты чистятся через 30 дней без
  активности, hard `DELETE` — шифротекст уходит со строкой. Миграция применена.
  Строка «черновик ждёт 30 дней» на экране стала правдой
- ✅ Проверено: автосохранение двигает `updated_at = NOW()`. Неделя заполнения
  не выглядит бездействием
- ✅ Проверено: код кандидата **не одноразовый** — тот же `pin_hash` работает
  повторно, выдаётся новая сессия на 7 дней. В анкете формулировка исправлена на
  «код из сообщения куратора»
- ✅ **Долг закрыт 12.08.** Поле подписано «Код от куратора» — подпись называет
  источник, а не срок жизни. Слово «одноразовый» осталось только в клиентском
  флоу, где код действительно сгорает. Слот ошибки кандидата исправлен на «код
  не подошёл, проверьте цифры»
- ✅ **Экран блокировки поправлен:** «напишите куратору — он снимет блокировку»
  вместо «выдаст новый код». Если код не сгорает, выдавать новый бессмысленно. В
  продукте формулировка уже была ближе к правде: при `rate_limited` —
  «Попробуйте позже или напишите куратору»
- ✅ Срок черновика показан и на возврате, и в момент закрытия анкеты на
  середине
- ✅ **Предупреждение в бот выкачено и проверено 12.08.** Live smoke: первый
  прогон — `purge_warn.sent:1`, метка `purge_warn_sent_at` проставлена; второй —
  `sent:0`, целей ноль. То есть предупреждение уходит один раз, а не каждый день
  с 28-го по 30-й. Коммит `13fd70a83`, три функции передеплоены
- ⬜ **Шлюз `/bot/send` всё ещё отдаёт 403 на заголовок.** Рабочий путь один —
  `body.cron_token`, так шлют drip и maintenance. Единый путь доставки формально
  есть, но одна из двух дверей закрыта: через месяц кто-то попробует заголовок и
  решит, что сломалось _— до 18.08_
- ⬜ **`HEYS_START_BOT_TOKEN` литералом в локальном `.env`.** Остальные токены —
  плейсхолдеры `__IN_LOCKBOX__`, этот настоящий. Не сегодняшний инцидент, но
  настоящий токен в файле рано или поздно уезжает в git _— 13.08_
- ✅ `function_id` MCP выровнен 12.08: в отслеживаемом `api-gateway-spec.yaml`
  стоял мёртвый `d4egv2a6…`, живой — `d4epjmd9…`. Заменено 46 вхождений, факт
  поправлен в README, коммит `44841e1df` на origin. Живой шлюз не трогали — там
  id уже был верный
- 🔵 **Реализация 12.08**, коммит `cb1ec78b2`, SQL на проде. Окно: неактивность
  28 дней, purge на 30-й. Только начавшие заполнять. Доставка через
  `telegram_chat_id` лида или клиента; у кого канал не Telegram — пропуск с
  пометкой, без повторных попыток. Текст обезличен. Ждёт деплоя трёх функций и
  push
- ✅ Проверено: заглушки токенов и разведение поллеров. `INTERNAL_CRON_TOKEN` в
  `.env` — плейсхолдер на 32 символа, не обрезок 64-символьного из Lockbox,
  ротировать нечего. Три бота крутятся на трёх разных токенах и общий `update`
  не делят; гонка в тесте была артефактом — плейсхолдер принимался за токен, и
  оба поллера делили один mock
- ⬜ **Ограничение: старые лиды без сохранённого `chat_id` предупреждение не
  получат.** Пока их немного и все свои, но при первом наборе это тихо теряет
  кандидатов — проверить, много ли таких, и решить, чем заменить _— до 18.08_

### C5 · Экраны согласий — разбор юриста и канвас 12.08

Юрист разобрал 16 снимков экрана «Согласия и условия» с прода. Это был последний
открытый пункт запроса — все шесть закрыты.

**Что сделано правильно и нельзя сломать при правках:** принудительная прокрутка
с плашкой «Прокрутите до конца» и кнопкой, оживающей после неё; отдельные
чекбоксы под каждый документ; честный отказ («Закрыть без принятия», «Выйти без
регистрации»); медицинский дисклеймер до чекбоксов; пояснения «Коротко и честно»
под каждым пунктом.

**Четыре дефекта:**

- ✅ **Дефолт снят 12.08.** `notificationsOptIn` переведён в `useState(false)`,
  коммит `8e6971dd1`. Обязательные и маркетинговый чекбоксы не тронуты
- ⬜ **Согласие на здоровье обязательное:** звёздочка, без него «Продолжить»
  неактивна. Доступ обусловлен согласием на специальную категорию, без которой
  сервис по новой конструкции работает. Уходит вместе с изъятием документа 1.5,
  но пока он в проде — дефект действующий _— вместе с изъятием 1.5_
- 🔵 **Рендер починен 12.08**, коммит `8e6971dd1`: таблицы, `<br>`, раздельные
  списки, склейка цитат. Байты legal-файлов не трогали — хэши целы. Тесты 14/14.
  Ждёт push и живой проверки в модалках. Прежняя формулировка дефекта: `<br>`
  как текст, звёздочки `**Последнее обновление:**`, подчёркивания вокруг
  курсива, сбитая нумерация («36.» вместо «4.»), а в политике вместо таблицы
  состава данных — строка `| Группа | Примеры | Цель |`. **Таблица — самое
  неприятное: именно она несёт перечень обрабатываемых данных, и пользователь
  его фактически не увидел.** Хэш и доказательственная цепочка не рвутся, но
  довод «мне показали нечитаемый документ с обрывками кода» при споре звучит
  убедительно. Чинить рендер до выката 1.11, иначе новые тексты приедут с теми
  же артефактами _— до 20.08_
- ⬜ **Одна кнопка на пять волеизъявлений.** Отдельные галочки требование
  закрывают, но необязательные согласия стоят в том же потоке и под той же
  кнопкой — отсюда довод об обусловленности. После изъятия здоровья останется
  два обязательных; необязательные вынести на отдельный шаг после регистрации,
  где отказ ничего не блокирует и это видно _— в пакете 1.11_

**Канвас «Согласия v4» — предложение принято, кроме одного пункта.**

Хорошее: пересказ один вместо двух (сейчас каждый пункт объясняется и в списке,
и в модалке); обязательное и необязательное разведены ярусами «Обязательно» /
«По желанию» вместо звёздочки; на экране «всё отмечено» необязательное остаётся
пустым, а кнопка активна — прямо отвечает на первый дефект; кнопка называет
причину; метаданные таблицей вместо курсивного абзаца с невырезанными `<br>`;
пять категорийных цветов и эмодзи сняты.

- ⬜ **Развилка: принудительная прокрутка.** Дизайнер её убрал — «Принимаю»
  активна сразу, довод: прокрутка не делает согласие информированным, а
  раздражает. Юрист в том же разборе назвал её тем, что нельзя ломать: это
  техническое доказательство, что документ пролистан целиком, и при споре «я не
  читал» работает на вас. Два эксперта дали противоположные советы.
  **Компромисс: прокрутка обязательна для обязательных документов, не нужна для
  необязательного.** Решение владельца _— 13.08_
- ⬜ Дефект с предустановленной галочкой канвасом не решён, а вынесен за кадр: в
  предложении три согласия, push и напоминаний нет вовсе. Дефолт в коде
  проверить независимо от макета _— 13.08_
- ⬜ После push проверить глазами, что таблица состава данных в политике
  действительно отображается таблицей — это главный из артефактов _— 13.08_
- 🟡 **`support@heys.app` → `poplanton@mail.ru` — решение принято, правка уходит
  в пакет 1.11.** Домена не существует, `dig MX heys.app` пустой; в реестр подан
  именно `poplanton@mail.ru`, значит замена сводит оферту с уведомлением, а не
  разводит их. Ящик заводить не надо, реестр не трогаем. Внеочередной версии
  ради одной строки не делаем: цикл версионирования дороже. Но пока пакет не
  вышел, контакт в оферте нерабочий — если кто-то напишет туда, письмо не дойдёт
  _— в пакете 1.11_
- ⬜ Проверить, где ещё встречается `heys.app`: оферта §13, макеты, тексты в
  приложении, бот. Всплывал трижды, значит источник один и его надо найти _—
  13.08_
- ⬜ Микротекст «внешний мессенджер работает по своим правилам» под согласием на
  ПДн — третье место, где письменно признаётся передача во внешний мессенджер,
  после политики §5 и таблицы получателей. При решении по Telegram менять надо
  во всех трёх _— в пакете 1.11_

### C4-архив · Канвасы, закрытые ранее

- ✅ «Добавление еды»: 33 экрана, принят, промпт кодеру готов
- ✅ «Вход»: пять новых экранов, четыре правки, синяя палитра решена заливкой,
  `#a75c2b` и `#2f2b25` исправлены
- ✅ Терминология сведена: слово PIN убрано со всех экранов, у клиента «код», у
  выданного куратором «одноразовый код». Проверено — в разметке остались только
  упоминания в заметках о самой правке
- ✅ Четыре недостающих состояния: «Коды не совпали», «Код слишком простой» с
  перечислением требований, ошибка при подписании со счётчиком оставшихся
  попыток, «Подписано» со списком сохранённого в записи
- ✅ Дубль блока про подпись снят с экрана повтора — заодно ушло переполнение
  карточки
- ✅ Дубль текста на экране блокировки убран
- ✅ «Это устройство» вместо «этот телефон»
- ✅ Экран техработ: убраны «попробуйте через час» и «Вход куратора работает»
- ✅ «Мы не узнаём это устройство» — внесено в обе версии экрана

Дизайнер по входу свободен. Следующая его работа — шесть позиций из C2.

### C6 · Пакет документов 1.11 — 🟡 разбор получен 12.08, 21 правка

**Итог пакета: пять новых документов, семь изменённых, один прекращающий
действие.** Полная таблица правок — `lawyer-review-4-texts-2026-08-12.md`,
раздел VI.

**Две структурные проблемы, не редакционные.**

**Три документа получены от юриста 12.08 — готовы, но два блокера перед
регистрацией версий.**

- ✅ **Соглашение о ПЭП 1.1 принято.** Разведение входа и подписания сделано
  двумя пунктами вместо одной правки: 4.1 «б» требует ввода кода непосредственно
  перед подписанием, 4.2 прямо говорит, что вход с зарегистрированного
  устройства без кода допускается, а подписание — нет ни при каких
  обстоятельствах. Трансграничка убрана из перечня подписываемых, вместо неё
  согласия на подключение отдельных функций. Реквизиты подставлены, заглушек нет
- ⬜ Проверить URL в п. 9.1: `app.heyslab.ru/docs/v1.1/pep-agreement.md` —
  поддомен приложения, а не сайта, и `.md` в публичной ссылке. Сверить с тем,
  как публикуются остальные документы _— 13.08_

- 🟡 **Согласие на обработку персональных данных 1.0 — проект готов.** Отдельный
  документ вместо отсылки к политике, со сроком (до отзыва, но не более пяти
  лет) и тремя способами отзыва
- 🟡 **Блокер снимается иначе, чем планировали.** Фразу «передача на территорию
  иностранных государств не осуществляется» из согласия убрать, вместо неё
  назвать получателей — Telegram, Google, Apple — и что именно каждый получает.
  Документ становится выпускаемым не потому, что каналы закрыли, а потому, что
  они названы. Это следствие решения от 12.08 подать уведомление _— в пакете
  1.11_
- ⬜ **Блокер: раздел 3 сверить с фактическим перечнем полей после миграций, а
  не с планом.** Любое поле, оставшееся в базе и не названное в согласии, —
  дефект согласия. Особое внимание: замеры и добавки остались опциональными
  функциями, значит поля в базе есть; плюс аккаунт с `internalAccount`, где
  живёт цикл _— 13.08_
- ⬜ Подтвердить адрес перечня обработчиков `app.heyslab.ru/docs/subprocessors`
  _— 13.08_

- ✅ **Предупреждение анкеты 1.0 вычитано и принято.** Три добавления юриста:
  тренировочная часть — иначе разошлось бы с офертой, где противопоказания для
  тренировок уже появляются; фраза про удаление сведений — превращает
  предупреждение в часть процедуры по свободному вводу; заявление о возрасте
  активным действием, а не выводом из года рождения
- ✅ **Текст уже в коде** — `heys_trial_intake_v1.js`,
  `WARNING_TEXT_VERSION = '1.0'`, коммит `2bbf90146`. Тон принят владельцем
  12.08. Версия пишется в `warning.text_version` при подтверждении
- ⏸ Хвост, не блокер: фикстуры тестов и блоки `lawyer-B` / `lawyer-C` ещё
  ссылаются на `pending-owner-text`, продукт уже на 1.0. Подчистить при
  следующем касании этих файлов _— не срочно_

**Инвариант подтверждения о принятии риска.** Не разовая проверка, а условие
навсегда: вариант ответа один и «нет» не существует; сценарий по ответу не
ветвится — ни тариф, ни программа, ни пометка куратору от него не зависят;
хранится только факт, версия и время; поля для комментария рядом нет. **Любое
будущее улучшение, нарушающее хотя бы один пункт, возвращает запись в
специальную категорию со всеми последствиями.**

- ⬜ **Согласие на здоровье 1.5 изымается, а не правится.** Его раздел 2
  квалифицирует как «данные о здоровье» вес, питание, сон, активность,
  самочувствие и замеры — то есть весь основной набор, про который юрист сказал,
  что он специальной категорией не является. Вы письменно, в подписанном
  клиентом документе, утверждаете обратное; проверяющему достаточно
  процитировать ваш же текст. Отозвать, вывести из набора, снимок в архив _— до
  20.08_
- ⬜ **Создать отдельное «Согласие на обработку персональных данных».** Сейчас
  вместо него отсылка «в соответствии с Политикой конфиденциальности» —
  документом, который вы меняете односторонне и в котором срок действия согласия
  не назван вообще. Единственная правка пакета, создающая новый документ _— до
  20.08_

**Критично, помимо двух выше.**

- ⬜ **Оферта §13: `support@heys.app` → `poplanton@mail.ru`.** Идёт в пакете
  1.11, отдельный хэш ради одной строки не выпускаем. См. C5 Домена у вас нет, в
  остальных восьми документах стоит другой адрес. §4.3 даёт право обратиться в
  поддержку, §12.1 требует переговоров до суда — а канал не работает. ✅
  Проверено 12.08: домена не существует, `dig MX heys.app` пустой; ящик заводить
  не надо, в реестр подан `poplanton@mail.ru`. Открыта только сама правка текста
  в пакете _— до 27.08_
- ⬜ Политика §7: убрать журналирование доступа, поправить формулировку про
  шифрование _— до 20.08_
- ⬜ Политика §5: переписать абзац про внешний мессенджер, убрать Telegram из
  таблицы получателей _— до 20.08_

**Правило, которое юрист расширил из варианта В.** Не «служебные уведомления без
содержания», а общее: через инфраструктуру за пределами РФ персональные данные
не проходят ни в одну сторону. Проверять по нему три канала, а не один.

- ⬜ **Push идут через FCM и APNs.** Согласие на push 1.0 обещает уведомления «о
  сообщениях куратора». В первом письме мы заявили, что push обезличены. Одно из
  двух неверно — проверить фактический payload, а не намерение. Если обезличены,
  править согласие; если нет — второй трансграничный канал _— 13.08_
- ⬜ **Support-бот принимает заявления на возврат** с номером телефона, суммой и
  идентификатором платежа. Вариант В покрывал исходящие; это входящий канал
  через ту же инфраструктуру. Либо бот принимает только сигнал и переводит в
  приложение, либо канал легализуется _— 13.08_

**Прочие правки по таблице юриста** (высокий и средний приоритет): ключ ПЭП и
подписание в §8.2, `sms_code` в §8.3, «при выявлении противопоказаний» в §6.3,
цены в §3.1, состав данных в политике §2 и §3, сроки резервных копий 30 и 35
дней в §6, возраст в §6.1, противопоказания для тренировок в §6.2, проактивность
в §5.1, границы Pro Спорт в §5.2, маркетинг §2, speech §2, куратор 1.0, раздел
Self.

**Что юрист отметил как хорошее:** таблица получателей и обработчиков полная и
честная, ЮKassa и SpeechKit названы поимённо; раздел про сроки структурирован;
разведение обязательного и необязательного сделано правильно; формулировка про
три года хранения доказательств согласий на месте и работает. Документы по
возврату, cookie и маркетингу — хорошие, права потребителя нигде не ущемлены.

**Отдельно, операционное, не юридическое.** §5.4 обещает ориентир реакции 1–2
часа и часы 09:00–21:00 ежедневно. Дефекта нет, но при пятнадцати клиентах и
одном кураторе это обещание будет проверяться каждый день. Если ритм не
выдержится, менять придётся новой версией с уведомлением за 14 дней.

**Ранее собранные правки, входят в тот же пакет:**

Одна версия, один цикл переподписания, один хэш. Выпускать раньше, чем соберутся
все правки, нельзя: у 1.10 зарегистрирован `document_sha256` и лежит снапшот в
`apps/web/public/docs/v1.10/`, любая правка текста требует полного цикла
версионирования.

**Порядок, подтверждённый юристом: тексты правятся ПОСЛЕ реализации минимизации,
не до.** Перечень обрабатываемых данных изменится, и версия 1.11 выйдет уже
устаревшей. Изменения в уведомление по ст. 22 — там же и по той же причине,
иначе подавать придётся дважды. Об изменении сведений оператор уведомляет в
течение десяти рабочих дней, отсчёт от фактического изменения.

- ⬜ § 8.2 переписать под новую схему входа: ключ подписи — собственный код
  клиента, а не выданный PIN; подписание отделено от входа и требует ввода кода
  даже на зарегистрированном устройстве _— до 27.08_
- ⬜ § 8.3: убрать `sms_code` из перечня способов подписи — значение вычищено из
  схемы 11.08 _— до 27.08_
- ⬜ § 5.1: узкая формулировка проактивности («куратор просматривает вашу неделю
  каждый день в рабочее окно», без обещания числа сообщений и скорости) _— до
  27.08_
- ⬜ Раздел Pro Спорт: исчерпывающий перечень того, что не разбирается по видео,
  и при каких состояниях разбор не даётся _— до 27.08_
- ⬜ Раздел 6.2: противопоказания для тренировочной части — операции, грыжи и
  протрузии, травмы суставов, текущая боль _— до 27.08_
- ⬜ Оферта § 13: `support@heys.app` → `poplanton@mail.ru`, идёт в пакете 1.11
  одной строкой. Адрес оператора в реестре при этом не меняется и уведомление об
  изменении сведений не подаётся — см. C8. Прежняя формулировка «этот пункт снят
  из пакета» противоречила C6 и снята _— до 27.08_
- ⬜ Раздел про бесплатный доступ Self: услуга «как есть», без сопровождения и
  сроков ответа, право прекратить, срок жизни неактивного аккаунта _— до 27.08_
- ⬜ Возрастной ценз прямой строкой: услуга оказывается лицам, достигшим
  восемнадцати лет _— до 27.08_
- ⬜ Согласовать § 6.3 соглашения о ПЭП с политикой: журнал подписания хранится
  дольше, чем данные, и это должно быть объяснено в обоих документах _— до
  27.08_

Клиентов на версии 1.10 сейчас пять, все свои — цена ожидания околонулевая, цена
внеочередного цикла реальная.

---

### C7 · Ждёт документов — ⏸

- ⏸ Проактивность в оферту (§ 5.1) — пакетом с Pro Спорт и сменой почты
- ⏸ Почта на своём домене — вместе с офертой, политикой и уведомлением РКН
- ⏸ Полный недельный разбор со скринами — после того, как разбор будет написан

### C8 · Почта на своём домене — ⬜ решение принято 11.08, уточнено 12.08

**Уточнение 12.08.** В публичной карточке реестра адреса электронной почты нет
вовсе — он есть только в самом поданном уведомлении. Владелец подтвердил: подан
`poplanton@mail.ru`. Значит официальный контакт оператора — этот адрес, и все
юридические документы должны называть его же. Новый ящик на своём домене, если
заводится, идёт вторым контактом для общих вопросов, а не заменяет этот. _— до
26.08_

**Реестр не трогаем.** Последнее уведомление рассматривали больше месяца; менять
контакт оператора ради косметики не стоит. Значит уведомление об изменении
сведений не подаётся.

Заводим второй ящик на своём домене и используем его там, где адрес не имеет
юридического значения.

- ⬜ Подключить почту для домена `heyslab.ru`, завести `hello@heyslab.ru`,
  проверить, что письма доходят в обе стороны _— до 26.08_
- ⬜ Поставить его в подвал лендинга как общий контакт: «Написать нам» _— до
  26.08_
- ⬜ Настроить пересылку на основной ящик, чтобы не проверять два места _— до
  26.08_

**Что остаётся без изменений и почему.** В оферте, политике, согласиях и в
реестре — прежний адрес. Он объявлен официальным контактом оператора, и он
должен совпадать во всех трёх местах. Расхождение между реестром, документами и
сайтом — то, что проверяется первым, потому что видно снаружи.

Значит в подвале должно быть ясно, какой адрес для чего: общий — новый,
обращения по персональным данным, документы, оплата и возврат — прежний. Сейчас
в подвале одна строка на всё, её надо развести на две.

- ⬜ Развести в подвале два контакта с явным назначением, не поменять один на
  другой _— до 26.08_

Если когда-нибудь придётся подавать уведомление по другому поводу — при найме
кураторов или смене состава обработки — тогда и заменить адрес заодно, одним
заходом.

## 10. Трек D · Продукт до первых клиентов _— до 25.08_

- ✅ Анкета: адресная ошибка валидации и `unsure` на экране сверки исправлены
- ✅ Промпты «Сохранить только себе» и «Очередь модерации» написаны, развилки
  закрыты решениями
- ✅ `prompt-food-addendum` слит в `prompt-food` — два документа с указаниями по
  одной задаче убраны. Первым шагом задания заводится роль `--v4-btn-on-act`
  отдельным коммитом с остановкой и отчётом владельцу: файл палитры общий для
  всех вкладок v4
- ⬜ Показать срок хранения анкеты отклонённого на самом экране _— до 25.08_
- ⬜ Проверить `purge_expired_trial_intakes()` на живых данных — ни разу не
  отрабатывала _— до 25.08_
- ✅ **`prompt-food`, шаги 2–5 приняты 12.08 с оговорками** (ACCEPT-WITH-GAPS).
  На `origin/main` коммит `a7fcc9202`, тронуты только
  `heys_add_product_step_v1.js` и `600-steps-and-aps.css`; `_meals.js` не
  тронут, чужой scope не задет. Шаг 2 в коде: пять исходов модерации плюс «не
  сохранён», harm без Toast.

  **Хвост приёмки, всплывший позже:** на этом коммите красный деплой-гейт —
  `ui-v4-classic-drift`, 20 сдвигов в классической палитре. В отчёте о приёмке
  этого не было, вскрылось при разборе упавшего деплоя. Разница существенная:
  «принято с оговорками» читается как «лежит и работает», а фактически коммит
  лежит и не выкладывается, заодно запирая выкладку всему остальному. См. A5

- ⬜ **Хвосты по еде, в тот же трек:** `meal_step` (гард даты → время →
  самочувствие → способ), полный экран «Приём собран», `pe-step` v4, наборы,
  модалка штрихкода, browser smoke _— до 25.08_
- ✅ **Smoke пройден на локалке 12.08, 4/4**
  (`playwright.food-smoke.config.ts`): новый продукт, пять исходов модерации,
  выход с черновиком, фото на граммах. Дрифт-гейт чист
- ~~🔴 **Smoke нашёл живой дефект на проде.**~~ Исполнено push
  `1ef2a1370`/`41955372b`. Прежняя формулировка: диалог «Выйти…» не работал
  (`useEscapeToClose` → `StepModal.hide()`, `closeModal` undefined) _— 12.08_
- ⬜ **Ручной smoke на проде — чек-лист** (тестовый клиент, не свой/жена).
  Результат — строка на пункт; закрывает heys/1ab6a1 _— 12.08_

  **До прогона (~2 мин)**
  - ⬜ Версия в UI = `app.heyslab.ru/version.json` (сейчас `41955372`). Не
    совпало → hard reload → unregister SW → reload. Пока не совпало — дальше
    бессмысленно
  - ⬜ Вход тестовым клиентом до любых проверок. PIN + ошибка про код доступа →
    у клиента `access_code_hash`, нужен свежий клиент

  **Прогон**
  - ⬜ **Диалог выхода (главное):** добавить продукт → граммы → Escape →
    «Остаться / Выйти», не молчаливое закрытие. «Остаться» — ввод на месте;
    «Выйти» — без записи
  - ⬜ **Модерация, естественно на проде:** `pending` (новый продукт); `exists`
    (имя как у существующего); `pending_dup` (повтор той же заявки); offline
    (devtools → offline → экран ошибки, ввод сохранён)
  - ⬜ **Модерация `invalid_session`:** за 10 мин не воспроизводится без хака LS
    — пропустить и записать «не проверен», не засчитывать по моку
  - ⬜ **Harm-шаг:** результат экраном/диалогом, не Toast
  - ⬜ **Фото на граммах:** кадр прикладывается и виден

  **Провал vs хвост:** провал — сломанный ежедневный путь (шаг не открывается,
  ввод теряется, приём не создаётся) → revert коммита еды + push. Смешанная
  вёрстка на неперекрашенных экранах — хвост, записать и идти дальше

  **После прогона**
  - ⬜ Отклонить 3–4 живые заявки модерации тестового клиента
  - ⬜ Убедиться, что приёмы остались у тестового клиента, не у вас/жены

  **Каким клиентом заходить — проверено по базе 12.08, не гадать.** У
  `Login Smoke Test` и `login-smoke-deploy` проставлен `access_code_hash`,
  значит legacy-путь по PIN для них закрыт политикой sunset, а фронт нового
  входа на прод не выкачен — ими не зайти вовсе. Кандидаты:
  `HEYS production smoke` (`9bc6f6c3…`, `7397a9db…`) и `Purge Warn Smoke Client`
  — у них `access_code_hash` пуст. **Телефоны из отчёта кодера (`7900163655556`,
  `7900165216038`) — 13 цифр, неверны.** До SQL-fix по heys/51cd22 вход
  невозможен. **Куда заходить:** `https://app.heyslab.ru/` (клиент), **не**
  `/?intake=1` (кандидат); при LS-hint `heys_candidate_cookie_session_hint` —
  инкогнито. Sunset: без проверки по базе это выглядело бы как «PIN сломался».

  **Прогон на старом бандле 12.08 — 3/4, падал ровно «выход с черновиком».**
  Прогон делался до выкладки фикса, на `679a6976`. Как доказательство дефекта он
  полезен: предсказание «третий пункт даст известный красный» подтвердилось
  эмпирически. Как приёмка не засчитывается — нужен чистый прогон на `41955372`.

  **Форма отчёта — строка на пункт.** Прогон засчитывается только в этом виде:
  галочка «всё ок» не отличает проверенное от пропущенного, а пропущенное надо
  видеть. `invalid_session` заранее допускается как «пропущен» — на проде за
  десять минут он не воспроизводится без правки localStorage, и честный пропуск
  лучше зачёта по моку.

  ```
  версия UI: …
  вход тест-клиент: …
  диалог Escape — Остаться: …
  диалог Escape — Выйти: …
  модерация pending: …
  модерация exists: …
  модерация pending_dup: …
  модерация offline: …
  invalid_session: пропущен / …
  harm (не toast): …
  фото на граммах: …
  провалы на пути: … / нет
  заявки модерации отклонены: …
  приёмы у тестового клиента: …
  ```

  **Что происходит с результатом.** Все пункты чисты — `heys/1ab6a1`
  закрывается, флоу еды снимается с 🔵 в этом треке. Есть провал на ежедневном
  пути — revert коммита еды и push, задача остаётся открытой. Хвосты по вёрстке
  на неперекрашенных экранах в решение не входят: они записываются строками сюда
  же и живут своим сроком.

- ⬜ **Автотесты идут с моком `YandexAPI.rpc`** — UI-ветки, не контракт; ручной
  прогон выше закрывает контракт _— 12.08_
- ✅ **Фикс выложен в `main` 12.08:** `1ef2a1370` — диалог выхода через
  `context.onClose`, `StepModal.hide()` убран из Escape; `41955372b` — smoke в
  репозитории и роль `--v4-sand-accent-bg`. Проверено владельцем по репо:
  `600-steps-and-aps.css` и степ-модуль чисты; рецепт в `BUGS_HISTORY.md` лёг с
  `679a69769`. Опасение «source-only не донесёт до бандла» снято: `build:ci` =
  `prebuild → build:react → bundle:legacy → verify-legacy-bundles → build:dist`
- ✅ **Деплой доехал, критерий старта выполнен: прод `2026.08.12.1511.41955372`,
  сборка 15:13 MSK.** Фикс диалога выхода в проде, чистый прогон можно гнать.
  Критерий был именно такой — не «CI зелёный», а сменившийся хеш в
  `app.heyslab.ru/version.json`
- ⬜ **Наблюдение с этой же проверки, годное для всех живых.** `version.json`
  сам отдавался устаревшим: в 15:45 он ещё возвращал `679a6976`, хотя сборка
  `41955372` встала в 15:13. Значит между «CI зелёный», «объект в бакете» и
  «клиент увидел новое» стоят два слоя кэша — CDN и service worker, — и они врут
  в разное время. Единственный честный критерий готовности к живой проверке:
  версия, показанная **в приложении**. Не галочка в Actions и не `version.json`
  _— 12.08_
- ⬜ **Service worker / CDN.** `version.json` может отставать от UI;
  единственный честный критерий — версия **в приложении**. SW может отдавать
  старый бандл даже после смены хеша — см. чек-лист выше _— 12.08_
- ~~⬜ **Проверить, каким клиентом заходить**~~ Включено в чек-лист (PIN /
  access_code_hash)
- ~~⬜ **Smoke-файлы положить в репозиторий**~~ ✅ в `41955372b`
- ✅ Палитра доведена до канона: заведена роль `--v4-sand-accent-bg` по образцу
  `--v4-sand-hero` — только в песочных палитрах, литерал `#f0dcc6` фолбэком;
  вложенные `var(--v4-accent-bg, …)` убраны. Рецепт записан в `BUGS_HISTORY.md`
- ~~🔵 Smoke флоу еды — теперь на проде, а не на локалке.~~ Прежняя
  формулировка: Push `679a69769` прошёл раньше проверки, флоу опубликован.
  Сценарий тот же: новый продукт → все пять исходов модерации → выход с
  черновиком → фото на экране граммов, песочная и sand-dark _— 12.08, сегодня_

  **Делать под отдельным тестовым клиентом, не под своим аккаунтом и не под
  аккаунтом жены.** Правило уже есть в 4.5 для входа, здесь оно нужнее:
  добавление еды пишет в живой дневник, и неудачный прогон оставит мусорные
  приёмы в вашем сегодняшнем дне.

  **Чем рискуем на самом деле — узко.** Задание по еде меняло оболочку,
  состояния и копирайт; логику поиска, ранжирования, overlay и RPC не трогало.
  Значит вероятный отказ — смешанная вёрстка и сломанное взаимодействие на
  полуперекрашенных экранах, а не порча данных. Смотреть прицельно там, где
  менялось поведение: пять исходов модерации вместо тостов и harm-шаг без Toast.

  **Путь отхода есть и он дешёвый.** Если ломается ежедневный путь — revert
  коммита еды и push: гейт зелёный, выкладка проходит за минуты. Это не тот
  откат, который мы отклоняли утром: там предлагалось откатить весь клиент к
  сборке 11.08 и потерять минимизацию, здесь — снять один коммит вперёд по
  истории _— 12.08_

- ✅ **Правило зафиксировано:** smoke → commit → push, когда push разблокирует
  деплой на ветке с несданной работой. Стоило одной публикации непроверенного
  флоу — дешевле, чем могло
- 🟡 **Граница задачи «Сохранить только себе» изменилась.** В промпте она
  описана как макетная кнопка с текущим поведением, а в коде уже зовёт
  `upsertLocalProduct` через `retryLocalOnly` — то есть локальный fallback
  работает. Очереди повторной отправки это не даёт, полный офлайн-контур
  остаётся отдельной задачей. **Переписать задание до запуска:** иначе
  исполнитель начнёт делать то, что частично сделано _— до 25.08_
- ⬜ Запустить «Сохранить только себе» — **после переписывания задания под
  фактическое состояние** (лист «Исходы и отказ»), не параллельно: это один и
  тот же экран. Прежняя формулировка «после работы по входу» была неверной,
  порядок берётся из аудита флоу еды _— до 25.08_
- ⬜ Запустить очередь модерации — после предыдущего _— до 25.08_

---

## 11. Трек E · Внешние _— разборы получены, ждём скриншоты_

- ✅ Вопросы про письменную форму и трансграничную передачу закрыты разбором
- ✅ Юрист найден и задачу взял. Прислал входной пакет из восьми блоков
- ✅ Фактура собрана в `docs/release/`: `lawyer-README.md`,
  `lawyer-A-data-flow.md`, `lawyer-B-fields.md` (главный),
  `lawyer-D-consent-signing.md`, `lawyer-E-access-audit.md`,
  `lawyer-F-backups.md`. В B выделен блок свободного ввода, в E письменно
  зафиксировано отсутствие разграничения прав и журнала обращений
- ✅ Блок H готов: реквизиты собраны, адрес подтверждён как прежний, реестр не
  трогаем. Таблица подстановки заглушек в соглашение о ПЭП составлена
- ✅ Решение по кураторам принято: вне штата, самозанятые или ГПХ, договор
  поручения и раскрытие в согласии до первого допуска
- ✅ Собран `lawyer-owner-handoff.md` — бриф владельцу на полчаса с двумя
  оставшимися блоками
- ✅ Package-2 сдан 11.08: блок C дополнен (ПЭП в реестре, экраны входа, анкеты
  и опциональных функций, оценка «ок/риск» по чекбоксам), блок G дополнен
  разделом расхождений после минимизации, блок B получил секцию «после
  минимизации», проверка по циклу закрыта
- ✅ **Блок G закрыт 11.08: карточка оператора из публичного реестра
  выгружена.** Регистрационный номер 26-22-005319, приказ № 188 от 20.09.2022,
  действующая редакция — приказ № 131 от 24.07.2026. Четыре цели: кадры и
  бухгалтерия; приём и обработка заявок; сопровождение питания; маркетинг. PDF
  исходной формы утрачен, но содержание записи восстановлено из первоисточника —
  этого достаточно для сверки с блоком B

**Семь расхождений записи с фактом, разбор юриста 12.08. Три критических:**

- ⬜ **«Ведётся учёт запросов субъектов и действий с данными» — заявлено
  регулятору, не исполнено.** По блоку B DSAR-экспорты не регистрируются, по
  блоку E журнала обращений нет. Юрист: это перевешивает всё остальное в
  карточке — заявление сделано напрямую в общедоступной записи. Регистрацию DSAR
  внедрить дёшево, одна таблица; учёт действий с данными дорого и до найма не
  нужен, значит его формулировку убрать _— до 27.08_
- ⬜ **«Назначен ответственный за организацию обработки» — не назначен.** Юрист
  развернул прежнюю рекомендацию: обязанности у ИП нет, но в реестре мера
  заявлена, а «почему написали, что назначили» — отдельная претензия. Проще
  назначить себя приказом на одну страницу, чем править абзац; заодно снимает
  вопрос при найме _— до 20.08_
- ⬜ **Цель №3: убрать «сведения о состоянии здоровья» и «данные о менструальном
  цикле».** После минимизации и снятия цикла обе позиции исчезают _— в изменении
  сведений_
- ⬜ «Доступ предоставляется оператору и привлечённым кураторам в объёме,
  необходимом для оказания услуг» — обещание разграничения прав, которого нет. К
  первому найму должно существовать _— до найма_
- ⬜ Дата начала обработки 17.06.2026 против фактических согласий от 21.05 — в
  поле, видимо, попала дата подачи. Риск низкий, но при изменении привести к
  фактической _— в изменении сведений_
- ⬜ Политика заявляет шифрование health-полей; после минимизации предмет почти
  исчезает — формулировку привести к тому, что останется, иначе появится восьмое
  расхождение _— в пакете 1.11_
- ✅ Шифровальные средства «не используется» и юридический адрес без улицы —
  расхождениями не являются. Но контактный адрес в политике, оферте и согласиях
  должен быть полным

**Изменение сведений подаётся один раз, после того как факт изменился.** Срок —
десять рабочих дней с даты фактического изменения. Порядок: применить purge,
снять цикл, выкатить минимизацию на прод, решить по Telegram — и только от этой
даты отсчитывать. Четыре правки одним заходом: цель №3, меры по ст. 18.1 и 19,
дата начала обработки, трансграничная передача.

- ⏸ Подача изменения сведений отложена, см. трек B1. Два пункта из четырёх при
  этом не зависят от решения по продукту и остаются верными при любом составе:
  меры по ст. 18.1 и 19 (учёт запросов субъектов и действий с данными заявлен,
  но не ведётся) и дата начала обработки (17.06 против фактических согласий от
  21.05). Поправить заодно при первой подаче _— по факту решения_

**Подтверждение по специальной категории получено письменно.** Семь условий,
шесть закрываются один раз, седьмое — свободный ввод — остаётся условием
навсегда. **И оговорка юриста, которую нельзя терять: пока миграции не применены
на прод, а не в коде, специальная категория обрабатывается, и всё подтверждение
— про будущее время.**

**В пользу кандидатского флоу:** цель №2 прямо называет «выбранный мессенджер,
ник или идентификатор в мессенджере» — работа через мессенджеры в заявке
заявлена. Приложить к вопросу про Telegram.

- ⬜ Поправить `маркетинг/22` §1.7.3 и `31`: PDF помечен сохранённым, фактически
  его нет. Двенадцатое расхождение документа с фактом за день _— до 20.08_
- ✅ Скриншоты экранов согласий сняты и отправлены 12.08 — юрист разобрал
  шестнадцать снимков экрана «Согласия и условия» с прода, это был последний
  открытый пункт его запроса. Разбор и четыре найденных дефекта — в C5
- ⬜ **Пакет противоречит сам себе по `heys-mcp`** — README и changelog говорят
  «снят с прода», блок A и план «возвращён в тот же день». Пакет 12.08 уже ушёл:
  **проверить, свели ли расхождение до отправки или юрист получил его как
  есть.** От ответа зависит, надо ли досылать уточнение — он делает выводы по
  этому блоку _— 13.08_
- ⬜ Переписать довод в блоке A: «коннектор ходит в тот же российский API» —
  вывод верный, обоснование неверное, значение имеет не расположение эндпоинта,
  а то, куда уходит ответ. Тот же вопрос: ушло исправленным или как было _—
  13.08_
- ⬜ **Сверить, какой пакет ещё ждёт отправки.** `prompt-lawyer-2` отправлен
  12.08, три разбора получены. Строка heys/a32e06 либо про следующий заход —
  скриншоты экранов плюс четыре оставшихся вопроса ниже, — либо мертва. Из плана
  не восстанавливается, нужно слово владельца _— 13.08_
- ⬜ Оставшиеся вопросы: достаточность подтверждения о принятии риска;
  требования к согласию для блока с циклом; обязательный состав текстов при
  выбранной схеме; оформление допуска кураторов _— до 20.08_
- ✅ [11.08] Дата релиза назначена: 31 августа
- ⬜ Решить, откуда придут первые три клиента _— до 18.08_

---

## 11.5 Календарь до 31 августа

Даты у пунктов и разделов расставлены от этого календаря. Смещение любой строки
— повод пересмотреть дату релиза, а не двигать соседние молча.

| Срок     | Что должно быть закрыто                                                                                                                                                                                                        | От кого зависит  |
| -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------- |
| 12.08    | Push коммита login; пакет юристу отправлен; карточка из реестра и скриншоты экранов                                                                                                                                            | владелец + кодер |
| 13.08    | Фото: проверка кураторской отправки, затирание исторических `url`                                                                                                                                                              | кодер            |
| 15.08    | Начата задача по удалению при отзыве согласия — условие 8, самое объёмное из оставшихся                                                                                                                                        | кодер            |
| 13.08    | ~~Развилки по циклу и Telegram~~ — **обе решены 11.08.** Осталось исполнение: снять цикл, сузить Telegram до уведомлений                                                                                                       | кодер            |
| 18.08    | Фронт входа по канвасу; решение, откуда первые три клиента                                                                                                                                                                     | кодер + владелец |
| 20.08    | ~~Разбор юриста~~ и ~~минимизация~~ — закрыты 12.08. **Пересмотр даты релиза**, ревизия правил (какие срабатывали) + повторный `prompt-plan-audit`: к этой точке лягут тексты 1.11 и фронт входа, накопится новый слой отчётов | владелец         |
| 22.08    | Лендинг: макеты сданы; хвосты по данным закрыты                                                                                                                                                                                | дизайнер + кодер |
| 24.08    | Лендинг: текстовые правки в проде                                                                                                                                                                                              | кодер            |
| 27.08    | **Истекает окно рассмотрения уведомления** — до этой даты приглашения посторонним не отправляются. При запросе сведений от РКН срок сдвигается до трёх недель                                                                  | РКН              |
| 13.08    | **Уведомление по ст. 12 подано.** Десять рабочих дней рассмотрения идут параллельно; при подаче 13.08 окно закрывается около 27.08, при задержке уезжает за релиз                                                              | владелец + юрист |
| 27.08    | Версия 1.11 выпущена, согласия переподписаны; документы приведены к факту. От этой даты открывается окно старта акции Self. Сюда же — истечение окна по уведомлению                                                            | владелец + юрист |
| 29–30.08 | Живые проверки одним заходом: Telegram-браузер, блокировка, цепочка ошибок                                                                                                                                                     | владелец         |
| 31.08    | **Техническая готовность принять клиента**                                                                                                                                                                                     |                  |
| после    | **Первые клиенты** — отдельная дата, наступает после цепочки: вёрстка v4 → демо-ролик → лендинг → привлечение. Считать раздельно                                                                                               | владелец         |

Критический путь идёт через юриста: 12.08 → 20.08 → 27.08. Всё остальное
параллельно и запас имеет.

---

## 12. Условие готовности взять первого клиента

Список сверен с разбором юриста 11.08 и дополнен тремя пунктами, которых в нём
не было.

1. ✅ Фото недоступны по прямой ссылке — проверено живым запросом, 403 на трёх
   разных ссылках
2. ✅ **Вход по выданному PIN работает.** Три наслоенных дефекта (phone vs
   phone_normalized, SHA256 vs bcrypt, onetime_pin_hash vs pin_hash) — миграция
   `2026-08-12_fix_client_pin_login_mismatch.sql`, smoke «пупс» 12.08 (вход +
   согласия + регистрация). Коммит `d0d0fe896`, push в `main` — ✅ 12.08 вечер.
   Прежняя формулировка: 🟡 причина найдена и починена, но фикс не в git — prod
   опережал репозиторий.
3. 🟡 Анкета не собирает специальную категорию — миграции на проде 12.08, ждёт
   smoke владельца
4. ✅ Цикл снят из кода 12.08, MCP задеплоен, данные удалены purge с актом.
   Замеры и добавки остаются опциональными, добавки на закрытом справочнике.
   Остаток не блокирующий: гейт в web UI глазами не смотрели, идёт вместе с C0
   29–30.08
5. ⬜ В текстах согласий есть срок действия — переезжает в новый документ
   «Согласие на обработку персональных данных», которого пока нет
6. ⬜ Публичные документы не противоречат факту по составу обработчиков, срокам
   хранения и удалению
7. ⬜ Лендинг говорит правду о ценах и о том, что входит в тарифы. Правовая
   часть: заявления о ценах и составе тарифов обязаны совпадать с офертой — это
   потребительское право, проверять до публикации
8. ⬜ **Отзыв согласия реально удаляет данные** — переписку, транскрипты, фото,
   снапшоты **и `audit_logs`**. Уточнено 12.08 по коду: сам отзыв работает и при
   `health_data` чистит health-ключи и анкету, но перечисленное выше не трогает,
   а под `personal_data` не удаляет вообще ничего; ч. 5 ст. 21 нарушается при
   первом же отзыве первого клиента. По тяжести выше половины пунктов выше. Про
   аудит отдельно: майская миграция сняла маскирование, и журнал хранит копию
   дневников — пока это так, удаление дневника оставляет его копию в аудите
9. ✅ Ранее собранные данные о здоровье удалены 12.08, акт
   `health-data-destruction-act-2026-08-12.md`. Исключение семейного аккаунта в
   акте названо отдельной строкой
10. ⬜ Правило для куратора о свободном тексте оформлено инструкцией, а не
    строкой плана

Закрыто четыре пункта из десяти. Остальное из плана можно доделывать при
работающем сервисе.

---

## 13. Решено и не пересматривается — с обоснованиями

**Личность куратора раскрывается, биография — тезисами.** За 8–12 тысяч покупают
конкретного человека, а его на странице не было. `COPY_VOICE` запрещает
продавать тем, что «ведёт лично основатель», но не запрещает рассказать, кто
такой куратор. Слово «основатель» на странице не звучит.

**Двадцать лет — это практика на себе, а не ведение клиентов.** Формулировка «в
питании и сопровождении» фактурой не подтверждается и уходит. Слово «на себе» в
тексте обязательно.

**Проактивность берётся в оферту узкой формулировкой.** «Куратор просматривает
вашу неделю каждый день в рабочее окно», без обещания числа сообщений и
скорости. Это единственное настоящее отличие от трекера и от нутрициолога в
мессенджере, но § 5.1 действующей оферты от него отказывается — значит сначала
договор, потом витрина.

**Self — акция «0 ₽ до 31 октября, дальше 490».** Цена не была проблемой:
проблема в том, что описание Self совпадало с бесплатными приложениями. Акция
закрывает разрыв 490 → 11 990 без третьего тарифа. Слова «бета» и «тестировщики»
не используются.

**Старт акции Self привязан к пакету 1.11, а не к ПЭП** (решение 12.08). Прежняя
привязка «не раньше включения ПЭП» устарела: ПЭП ушла с критического пути вместе
со снятием цикла. Сам довод при этом от ПЭП никогда не зависел — бесплатному
пользователю нужны те же согласия, что и платному, и открывать доступ под
текстами, которые через неделю переподписываются, значит переподписывать сотни
человек вместо одного. Значит старт не раньше 27.08; конечная дата
пересчитывается от фактического старта и в макет не зашивается.

**Pro — 11 990 обычная, 7 990 для первого набора на три месяца, пятнадцать
мест.** Зачёркивание не используется: по 11 990 не продан ни один месяц.

**Границы видеоразбора Pro Спорт очерчиваются заранее.** Тренерского опыта с
клиентами нет — есть практика со знакомыми и супругой, образования нет.
Юридически заявлять опыт можно, приписывать квалификацию нельзя. Заранее
объявленный отказ от части работы — самая дешёвая защита.

**Скрининг Pro Спорт — предупреждение, а не опрос** (решение 12.08). Экран перед
первой тренировкой остаётся, но ответов не собирает: перечисление состояний, при
которых сначала нужен врач, и одно подтверждение «ознакомлен». Хранится факт,
версия текста и время — та же конструкция, что в анкете, и тот же инвариант:
вариант ответа один, сценарий по нему не ветвится, пометки куратору из него не
возникает, поля для комментария рядом нет. Вопросы об операциях, грыжах, травмах
суставов и текущей боли на экран не возвращаются. Это ровно те сведения, которые
убраны минимизацией, и их сбор вернул бы продукт в специальную категорию — с
письменной формой, паспортными реквизитами у клиентов Pro Спорт и изменением
сведений в реестре.

**Внешняя проверка видеоразборов тренером не вводится** — решение владельца.

**Медицинские вопросы из анкеты убираются полностью.** Вместо них подтверждение
о принятии риска. Это выводит основной путь из-под требования письменной формы:
не нужны паспортные реквизиты, усиленная подпись и повышенные требования к
защите.

**Цикл, замеры и добавки — опциональные функции**, выключены по умолчанию, с
отдельным согласием для тех, кто включил. Основной путь остаётся лёгким,
специальная категория касается меньшинства и включается осознанно.

**Трекинг цикла в релиз не входит вовсе** (решение 11.08). Проверка показала,
что даты уходят на сервер по всем четырём условиям, значит вариант «не покидают
устройство» недоступен без переделки на 6–10 дней. При восемнадцати днях до
релиза и трёх пользователях-своих это плохой размен. Возвращается после релиза
сразу в архитектуре device-only. Замеры и добавки остаются опциональными как
решено.

**Клиенты через MCP в чате не ведутся** — только собственный аккаунт куратора и
аккаунт жены. Уведомление в Роскомнадзор не подаётся. Решение владельца,
принятое с пониманием, что формально обязанность уведомить относится и к данным
члена семьи. При появлении через чат кого-то вне семьи вопрос возвращается
целиком.

**Push в `main` остаётся публикацией, прод не откатываем** (решение 12.08).
Авто-деплой вынес на прод часть незаконченного v4. Откат клиента к `stable`
отклонён: живой прод несёт минимизацию, анкету на пяти шагах, снятую
предустановленную галочку push-согласия и починенный рендер политики — откат
вернул бы в анкету вопросы о здоровье и сломал бы чтение фото, переведённое на
авторизованный путь. Клиентов нет, интерфейс видят только свои аккаунты, то есть
меняли бы невидимую косметику на откат условий готовности. Отдельный «skip
deploy» тоже не заводим: в тот же день закрывали дыру, где деплой обходился
явным списком. Незаконченный интерфейс на проде до релиза допускается осознанно.

**Уведомление о трансграничной передаче подаётся — одно, на три канала**
(решение 12.08). Telegram в кандидатском флоу, push через FCM и APNs,
support-бот. Основание: юрист подтвердил, что передача существует уже на уровне
адресации, и вычистить её из содержания нельзя. Три обхода, которые мы строили,
стоили продуктового удобства и всё равно оставляли документы недостоверными.

**Состав уведомления узкий, и это часть решения, а не деталь.** Заявляем
идентификатор в мессенджере, номер телефона, факт обращения за услугой, статус
заявки и код доступа; для push — текст уведомления. Получателей два, а не три:
Telegram и связка Google с Apple. WhatsApp не заявляем, потому что не используем
— он заблокирован в России с февраля 2026. **Переписка, фото, голосовые и данные
дневника в состав не входят и через внешние каналы не идут.** Уведомление
легализует транспорт приглашений и уведомлений, а не переносит услугу в
мессенджеры: расширять состав до содержания — отдельное решение с другой ценой,
потому что там снова появляется здоровье и необеспечимая обязанность по ч. 14
ст. 12. Формулировка «мы же уже уведомили» основанием для расширения не
является.

**Вход: одноразовый код куратора, затем собственный код клиента.** Подписание
документа всегда требует ввода кода, даже на зарегистрированном устройстве —
иначе подпись зависит от того, у кого в руках телефон.

## 14. Отменено — с причинами

**Промежуточный тариф 2 500–3 500 ₽.** Клиентов нет, неизвестно, сколько людей
отваливается именно по цене. Держится наготове как ответ на «дорого».

**Открытый дневник основателя.** Требует публиковать собственный вес и питание —
форма публичности, которую владелец не берёт. Заменён демонстрационным недельным
разбором.

**Цена «ниже навсегда» для первых участников.** При пятнадцати местах это
навсегда занятая треть ёмкости по низкой цене.

**Внешняя проверка видеоразборов сторонним тренером.** Отклонена владельцем.

**Легализация трансграничной передачи _данных клиентов в зарубежную модель_
через уведомление в РКН.** Отменена вместе с решением не вести клиентов через
чат. **Не путать с решением от 12.08 подать уведомление на мессенджер и push:**
там состав — идентификатор, номер, статус и код, обязанность по ч. 14 ст. 12 на
нём исполнима. Отменённым остаётся именно вывоз содержания дневников. Снимает
вместе с собой двухнедельное окно ожидания, оценку правового режима и
необеспечимую обязанность из ч. 14 ст. 12 — обеспечить уничтожение данных у
иностранного получателя.

**ПЭП под трансграничное согласие.** Письменная форма к вывозу данных не
привязана с марта 2023 года. ПЭП остаётся нужна только для опционального блока с
циклом.

**SMS как канал доставки кода.** Дорого, и не требуется: ключом простой
электронной подписи объявляется собственный код клиента, доставка кода законом
не предписана.

**TTL заявки в очереди модерации.** Пять попыток исчерпываются примерно за два с
половиной часа, семисуточный TTL не сработал бы никогда. Конец один — исчерпание
попыток.

**Гейт на два `client_id` в кураторском MCP — отклонён окончательно.**
Предлагался дважды: как техническая гарантия того, что через чат не пройдёт
клиент, и как способ сделать исполнимым триггер возврата вопроса о
трансграничной передаче. Отклонён владельцем оба раза, 11.08. **Решение закрыто:
не предлагать заново ни по каким основаниям, включая новые доводы юриста.**
Взамен действует правило поведения куратора в инструкции — см. трек B1.

## 15. Перенесено за релиз

Перенос распознавания фото и разбора свободного текста на российского
провайдера. Релиз не блокирует — первых клиентов можно вести руками, — но без
него экономика пятнадцати клиентов не собирается: двадцать пять минут в день на
человека вместо десяти.

---

## 16. Словарь

**Одноразовый код** — код, который куратор выдаёт клиенту для первого входа.
Сгорает после первого использования, живёт трое суток.

**Свой код (код доступа)** — постоянный код, который клиент задаёт сам при
первом входе. Куратору неизвестен. Он же ключ простой электронной подписи.

**ПЭП** — простая электронная подпись. Механизм, которым клиент подписывает
согласия так, чтобы это признавалось равнозначным собственноручной подписи.

**Канвас** — файл-спека экранов от дизайнера
(`docs/ui/handoff-v4/canvas/*.dc.html`): как экран выглядит в каждом состоянии,
включая редкие. Кодер верстает по нему, не гадая.

**Сборка «Лендинг D»** — доска с блоками лендинга и карточками претензий.
Рабочий инструмент дизайнера, не источник истины.

**Карточка** — пронумерованная претензия к лендингу со статусом и указанием
источника проверки.

**Спецкатегория** — данные о состоянии здоровья и другие особые категории по ст.
10 152-ФЗ. Требуют согласия в письменной форме.

**Анкета** — шестишаговый опрос, который кандидат проходит после заявки и до
выдачи пробной недели. Отдельный вход по телефону и PIN.

**Артефакт недели** — блок лендинга, показывающий, как выглядит неделя клиента
после разбора.
