# HEYS · трек A · Доступ и безопасность

> Часть канона. Общий контекст, правила, календарь и решения — в
> `release-plan.md`. Закрытое — в `release-archive.md`. **Штамп: 14.08.2026
> 18:35 MSK.**

## 7. Трек A · Доступ и безопасность

### A1 · Фото — 🔵 в работе _— до 13.08_

- ⬜ Проверка на мобильном и во встроенном браузере Telegram — за владельцем,
  вместе с C0 _— 29–30.08_
- ~~🔴 **Рабочее дерево разрослось до 71 файла.**~~ ✅ **14.08 вечер.**
  Исходники лендинга D, входа, ПЭП, дневника и статусов 1.11 ушли отдельными
  коммитами (`bed1820c`…`38834138`). Локально остаются только preview-бандлы,
  дизайнерский HTML, lawyer zip и `tmp-stable-pin/` — в `main` их нет. Правило
  то же: коммитить явным списком, `HEYS_COMMIT_SOURCE_ONLY=1`. _— 14.08_

- ⬜ Закоммитить клиентскую часть фото и галереи (мессенджер, галерея, тест,
  гейт на бандл) — своим списком путей, не общим коммитом _— до 13.08_
- ⬜ Проверить кураторскую сторону отправки вложений _— до 13.08_
- ⏸ Затирание поля `url` в исторических записях — после закрытия чтения и
  записи

### A2 · Вход — частично закрыт _— до 15.08_

- ⬜ Живая проверка «отказ сервера → текст на экране» и блокировки целиком —
  ждёт освобождения бандла _— 29–30.08_
- ⬜ Перевернуть приоритет источников IP: `sourceIp` основной, `x-forwarded-for`
  запасной. Пять функций _— до 15.08_
- ⬜ Блокировка не должна давать постороннему запирать чужой аккаунт: вход с
  зарегистрированного устройства её игнорирует _— до 15.08_
- ⬜ Проверить `messages` и `payments` на те же лимиты _— до 15.08_
- ⏸ Гейт на метаданные согласий — до прогона шести сценариев, задача
  heys/5e5736, окно 01–15.09

### A3 · Новая схема входа и ПЭП — 🔵 фронт на проде, owner smoke ✅ _— 13.08_

- ✅ **Owner smoke 13.08 закрыт владельцем.** E2e «папа»/«пупсы» на проде
  (`cfc18c4f1` → `e0baf0e3b`): intake → app-path → дневник. Welcome
  remount/flash — fix проверен («всё работает»). **Не гоняем повторно:**
  first-time welcome на новом клиенте, A2 новое устройство — по решению
  владельца 13.08 _— 15:48_

#### Smoke 13.08 · кусок 1 · Лендинг → анкета → решение куратора

**Тестовый кандидат:** «папа», `+7 (666) 666-66-66`, Telegram, 1988, incognito /
iPhone 12 Pro emulation.

| #   | Этап                        | Что делал                                                                           | Ожидание (канон v2)                                                            | Факт                                                                                                                                                                             | Вердикт                            |
| --- | --------------------------- | ----------------------------------------------------------------------------------- | ------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------- |
| 1   | **Заявка на лендинге**      | `heyslab.ru`: имя, телефон, Telegram, год рождения, согласие ПДн 1.7, «Отправляем…» | Заявка уходит в leads                                                          | Форма отправилась, экран «Заявка получена»                                                                                                                                       | ✅                                 |
| 2   | **Подтверждение заявки**    | Экран после submit                                                                  | Текст: куратор свяжется; пробная неделя не гарантирована                       | Совпадает с копией на лендинге                                                                                                                                                   | ✅                                 |
| 3   | **Подготовить приглашение** | Панель куратора → очередь → prepare invite для «папа»                               | Генерируется PIN + текст с `?intake=1`; client row создаётся на v2             | Модалка «Приглашение готово», PIN **4183**, текст совпадает с шаблоном (ссылка `https://app.heyslab.ru/?intake=1`, телефон из заявки, предупреждение про здоровье в мессенджере) | ✅                                 |
| 4   | **Статус в очереди**        | Вкладка «Анкеты»                                                                    | «Приглашение отправлено», ожидание входа кандидата                             | «папа +76666666666», «Анкета: Приглашение отправлено», кнопка «Перевыпустить доступ»                                                                                             | ✅                                 |
| 5   | **Вход в анкету**           | `app.heyslab.ru/?intake=1`, телефон + PIN 4183                                      | Отдельный intake-экран, не A3 app-login; PIN из сообщения куратора             | Экран «Вход в анкету», маска телефона приняла `+7 (666) 666-66-66`, PIN вошёл                                                                                                    | ✅                                 |
| 6   | **Анкета, шаги 1–4**        | Цели «123», автосохранение                                                          | 5 шагов, черновик сохраняется                                                  | «Шаг 1 из 5» … «Сохраняем…» / «Ответы сохранены»                                                                                                                                 | ✅                                 |
| 7   | **Шаг 5 · предупреждение**  | Чекбокс «Я ознакомился с предупреждением…»                                          | Health-disclaimer без сбора диагнозов в анкете                                 | Текст 1.0 на месте, чекбокс обязателен                                                                                                                                           | ✅                                 |
| 8   | **Сводка и отправка**       | «Отправить куратору»                                                                | Итоговый экран, без health-полей                                               | «Проверьте ответы», «Предупреждение подтверждено»                                                                                                                                | ✅                                 |
| 9   | **Подтверждение кандидату** | Экран после submit                                                                  | Три шага: получена → разбор → дата старта                                      | «Анкета отправлена», прогресс 1/3 зелёный                                                                                                                                        | ✅                                 |
| 10  | **Очередь куратора**        | Обновление «Анкеты»                                                                 | Статус «Готово к разбору»                                                      | «папа», «Анкета: Готово к разбору», «Открыть анкету»                                                                                                                             | ✅                                 |
| 11  | **Разбор анкеты**           | Карточка v1.2                                                                       | Куратор видит ответы без health-полей                                          | Цель «123», «Факторов… не отмечено», «Зафиксировать решение»                                                                                                                     | ✅                                 |
| 12  | **Решение**                 | «Одобрить и создать клиента», чек-лист 5×Да                                         | На v2 approval ≠ создание client (client уже при prepare); решение фиксируется | Модалка сохранилась; после — «Клиент создан», **новый PIN 2123** (≠ intake 4183)                                                                                                 | ✅ по факту; ⚠️ копия              |
| 13  | **PIN после одобрения**     | Модалка «Клиент создан»                                                             | Для **app**-входа выдаётся отдельный PIN (не reuse intake PIN)                 | PIN **2123**, инструкция передать PIN и ссылку в мессенджер                                                                                                                      | ✅ — правильная семантика двух PIN |

**Текст приглашения (зафиксирован дословно):**

> Здравствуйте. Куратор HEYS приглашает вас заполнить защищённую анкету перед
> пробной неделей. Анкета: https://app.heyslab.ru/?intake=1. Телефон для входа —
> номер из заявки. PIN: 4183. … Заполнение анкеты не гарантирует начало пробной
> недели.

**Замечания (не блокеры куска 1):**

- ⚠️ **Копия решения:** кнопка «Одобрить и **создать** клиента» и заголовок
  «Клиент создан» — на v2 client создаётся при prepare invite; здесь скорее
  «одобрить и выдать PIN для app». Поведение ок, формулировка вводит в
  заблуждение _— бэклог A3/copy_
- 🟡 **Консоль:** повторяющиеся preload-warnings (`react-bundle`, skeletons) —
  не ломают intake; оптимизация отдельно
- 🟡 **«Система: Критично»** в шапке панели куратора — не проверялось, к intake
  не относится
- ✅ **Номер 666…666** — валидные 11 цифр, маска приняла (в отличие от
  heys/51cd22 мусорных номеров в старых тест-клиентах)

- ~~🟡 **Welcome-триал повторно**~~ ✅ **fix 13.08** (`ff19ed604`, `e0baf0e3b`)
  — owner: «всё работает»; retest first-time welcome не нужен

#### Smoke 13.08 · кусок 2–5 · App-path → дневник

**Клиент** `+76666666666` (intake «папа», карточка «пупсы qwe»). App-PIN
**2123**, код **1212**.

| #   | Этап                 | Что делал                         | Ожидание (A3)                         | Факт                                         | Вердикт     |
| --- | -------------------- | --------------------------------- | ------------------------------------- | -------------------------------------------- | ----------- |
| 14  | **App-login**        | Телефон + PIN **2123**            | Одноразовый PIN сгорает               | Экран setup кода — login прошёл              | ✅ косвенно |
| 15  | **Придумайте код**   | «Подпись документов в приложении» | 4 цифры, текст ПЭП                    | Поля + «Далее»                               | ✅          |
| 16  | **Ввод**             | Код **1212**                      | → подтверждение                       | 1-2-1-2                                      | ✅          |
| 17  | **Повтор + ПЭП**     | Повтор + чекбокс ПЭП              | Обязательная галочка                  | 1212, галочка ✅                             | ✅          |
| 18  | **Сохранение**       | «Продолжить»                      | set_client_access_code                | → экран согласий                             | ✅          |
| 19  | **Согласия**         | Оферта, ПДн, health…              | Progressive disclosure                | «Продолжить» → подпись                       | ✅          |
| 20  | **Подпись кодом**    | access_code_sign                  | Код **1212**, «Подписать»             | Экран «Подпись документов», 1212             | ✅          |
| 21  | **Gate без триала**  | После signConsents                | Ждём activateTrial                    | «Аккаунт готов», пробная неделя не назначена | ✅          |
| 22  | **activateTrial**    | Куратор, дата **13.08.2026**      | 7 дней, карточка активна              | «Активна до **20.08.2026**»                  | ✅          |
| 23  | **Welcome / триал**  | Клиент после gate                 | Экран «добро пожаловать», дата триала | «Триал до 20 августа 2026», «Начать!»        | ✅          |
| 24  | **Онбординг**        | Check-in, вес **70.0 кг**         | flow_complete                         | weight → `flow_complete`                     | ✅          |
| 25  | **Главный экран**    | Вкладка «Питание»                 | Дневник, норма ккал                   | **1664 ккал**, пустой дневник, «Новичок»     | ✅          |
| 26  | **Новое устройство** | A2, incognito                     | Телефон + код **1212**                | не гоняем (owner 13.08)                      | ⏭          |

- ⏭ **A2** и повтор first-time welcome — сознательно не smoke-им (owner 13.08)

- 🟡 **14.08 · setup кода на старом клиенте.** На экране «Подпись документов в
  приложении» ввод с экранной клавиатуры через ~1 с стирался, «Далее» не
  сохраняло код. Причина: после fade оверлея браузер слал пустой `onChange`,
  обработчик принимал его за удаление. Исправлено локально
  (`heys_client_access_code_setup_v1.js`, guard в shared keypad и login): пустой
  `onChange` не затирает уже введённую цифру, после fade остаётся точка, слабый
  код объясняется текстом. Тесты `access-code-setup-keypad.test.js` +
  `auth-pin-keypad-rollout.test.js` — 7/7. Owner retest на старом клиенте ещё не
  закрыт; на прод само не ушло _— 14.08_

- ~~🟡 Smoke PIN 13.08 localhost «пупс» — не закрывает A3 (старый путь).~~
  **Снято:** owner smoke A3 ✅; A2/first-time welcome retest — ⏭ owner 13.08
- ⬜ **Проверить, жив ли WIP виджетов.** `730-widgets-dashboard.css` с
  `--v4-wgt-*` был незакоммичен и переписывался с HEAD — правки могли пропасть.
  Палитра возвращена уже с sand-ролями, но сами наработки по виджетам надо найти
  в другом чате или бэкапе _— 12.08_
- ⬜ **Текст ошибки входа развести на два случая.** Предложение кодера,
  принимаю: сейчас при ненайденном клиенте и при неверном коде экран говорит
  одно и то же — «проверьте код». Надо отличать «код не подошёл» от «вход не
  настроен, попросите куратора перевыпустить», не раскрывая при этом, существует
  ли клиент с таким номером. Именно эта неразличимость стоила сегодня двух часов
  _— до 18.08_

- 🔴 **Создание клиента в панели: сервер отработал, интерфейс завис на
  «Создание…».** Найдено 12.08 при попытке завести тестового клиента. Клиент
  `пупс` в базе создан (`6b9e20c0-4757-4993-991d-0de24247a2bc`), ошибок в
  консоли нет, но модалка не закрывается и кнопка остаётся в состоянии загрузки.
  Владелец при этом не знает, создан клиент или нет, и естественное действие —
  нажать ещё раз, то есть завести дубль.

  Это тот же класс, что «тост „Приём создан“ без проверки записи» из аудита флоу
  еды, только зеркальный: там интерфейс отчитывался об успехе без подтверждения,
  здесь не отчитывается при фактическом успехе. Общая причина — ответ сервера не
  доходит до состояния кнопки _— до 18.08_

- 🔴 **Дефект heys/51cd22 шире, чем записано, и он блокирует любой ручной
  smoke.** Проверено 12.08 по панели куратора: телефоны у тестовых клиентов не
  отсутствуют, а записаны мусором. `+7900163655556` и `+7900165216038` —
  тринадцать цифр, `ant` — `+79999999999`, двенадцать. Российский номер это
  одиннадцать цифр вместе с семёркой. Такой номер невозможно ввести в поле
  входа: маска его не принимает, и человек получает «PIN не подошёл» вместо
  «неверный номер» — то есть сообщение врёт о причине.

  Валидные номера только у трёх: `Александра` (её аккаунт для проверок не
  годится), `Login Smoke Test` `79995550888` (закрыт sunset, `access_code_hash`
  проставлен) и пары `E2E-TestAlex` / `E2E-TestPopl` `70000000001` и
  `70000000002` — они фикстуры автотестов.

  Чинить надо две вещи: валидацию номера при создании клиента и текст ошибки на
  входе. Второе важнее для клиента: сейчас при плохом номере он видит «PIN не
  подошёл» и будет бесконечно просить новый PIN _— до 18.08_

- ⬜ В отчёте по окну legacy не указан UUID проверенного клиента, только
  описание «без `access_code_hash`». Для аудита добить _— до 18.08_
- 🟡 Фронт A3 на проде (`cfc18c4f1`, deploy `31697815190`). Owner smoke 13.08:
  intake ✅ и app-login/код/согласия ✅ (куски 1–2 выше). Остаётся: v4-краска,
  тексты ошибок _— до 18.08_
- 🟡 Экраны 3–5 канваса «Вход v4»: базовая логика на месте (новое устройство,
  подпись документов); v4 paint и prod smoke — открыты _— до 18.08_
- 🟡 Правка § 8.3 уходит в версию 1.11 пакетом, точечно не чинится: текст версии
  1.10 зарегистрирован хэшем, правка разметки без нового цикла разошлась бы с
  зарегистрированным хэшем
- ⬜ Деплой `heys-api-rpc`, `heys-api-auth`, `heys-cron-reminders` — ждёт
  команды владельца _— до 18.08_
- ⬜ Третья правка: согласовать § 6.3 с политикой — журнал подписания хранится
  дольше данных, и оба документа должны это объяснять. Идёт в пакете 1.11 _— до
  27.08_
- ✅ Соглашение ПЭП v1.1 с реквизитами: `docs/legal/pep-agreement.md`, снимок
  `apps/web/public/docs/v1.1/pep-agreement.md`, manifest _— 13.08_

### A4 · Хвосты по данным — ⬜ _— до 22.08_

**CI разблокирован 12.08, веб тогда выкачен** (`44195e62`). **14.08 днём** живой
прод был `037ca554`, HEAD `2f558a19` в бакет не уехал. **14.08 18:29:** прод
`38834138` (Deploy 31814737179) — см. A5.

- ⬜ Гейт на хэш согласий. Условие: реестр версий становится единственным
  источником, приложение не может показать версию, которой в реестре нет.
  Миграция написана, не применена _— до 22.08_
- ⬜ Покрыть гейтом обе таблицы: `consents` и `trial_candidate_consents` _— до
  22.08_
- ⬜ Аудит чтений куратором — сейчас не логируется вовсе _— после релиза_
- ⬜ Разбор майской миграции, снявшей маскирование в `audit_logs`; журнал
  перестаёт хранить копию дневников _— до 22.08_
- ✅ `heys-mcp` снималась дважды и дважды возвращалась. Решение владельца от
  11.08, вечер: функция работает как рабочий инструмент куратора (дневник,
  задачник, стенограмма), возвращается через `ALLOW_DISABLED_FUNCTIONS` с
  записанной причиной, в инвентаре остаётся `autoDeploy: false`
- ⬜ **Свести формулировку к одной во всех документах:** «канал обслуживает
  собственный аккаунт оператора и аккаунт члена семьи; аккаунты клиентов сервиса
  через него не ведутся». Она верна при обеих ролях функции и не устареет при
  следующем изменении. Сейчас в письме одно, в разделе 1 плана другое _— до
  12.08_
- ⛔ **Гейт на два `client_id` не делается. Решение окончательное, к обсуждению
  не возвращаться.** Предлагался дважды — как страховка формулировки и как
  способ сделать триггер исполнимым, — отклонён владельцем оба раза. Не
  поднимать вновь ни по каким основаниям
- ⬜ Взамен гейта: триггер записывается правилом поведения куратора в ту же
  инструкцию, где правило про свободный текст. Формулировка: данные любого
  клиента сервиса в кураторский чат с моделью не попадают ни структурно, ни
  копированием текста; канал обслуживает только собственный аккаунт оператора и
  аккаунт члена семьи _— до 22.08_
- ⬜ **Разнести кураторский контур и внешний OAuth в разные функции.** Юрист
  поднимает приоритет: пока обе роли в одной функции, любое описание состояния
  устаревает при первом изменении. За вечер выбор между инструментом и чистой
  формулировкой вставал дважды _— после релиза_
- ⬜ **Задеплоить `heys-mcp` с правкой порядка стенограммы — но сначала
  воспроизвести симптом.** Проверено 12.08 через облачный контур:
  `transcript/2026-08-12.md` идёт хронологически — первая запись 10:04 сверху,
  последняя 14:55 внизу, запись ревьюера через облачный MCP легла в конец.
  Симптом «блоки сверху» не воспроизвёлся. Значит либо он уже закрыт, либо
  касается другого пути записи. Деплоить ради починки, которой не видно, не
  надо: сначала показать симптом, потом деплой _— до 22.08_
- ⬜ **Два писателя в стенограмме.** С 12.08 ревьюер-чат пишет
  `tasks_checkpoint` сам, а кодер писал туда же пересказ его сообщений.
  Разделить по природе события, а не по очереди: обмены в чате ревьюера пишет
  ревьюер, работу в кодовых сессиях — кодер. Иначе один обмен ложится дважды, и
  стенограмма протухнет тем же способом, что план _— 13.08_
- ⬜ Закоммитить правки gateway и инвентаря после возврата функции — иначе
  инвентарь и облако снова разойдутся _— до 22.08_

### A5 · Публикация и замороженная копия — 🔵 живой прод `38834138` _— 14.08_

**Инцидент 12.08.** Push в `main` вынес на прод часть незаконченного v4: в схеме
деплоя `push` в `main` = старт публикации, отдельного «выложить код без
публикации» нет. Живой прод — `a7fd195a` (сборка 13:17 MSK); флоу еды
`a7fcc9202` не выкатился, его деплой упал. Решение по откату — в разделе 13,
прод не трогаем.

- ~~🔵 **`main` сейчас не деплоится** из-за `ui-v4-classic-drift`.~~ ✅ **12.08,
  сверка 13.08.** Фикс `679a69769` в `main`; рецепт в `BUGS_HISTORY.md`. Живой
  CI: `Deploy to Yandex Cloud` success на HEAD `78d85e727` 13.08 (run
  31685670811). План §6 уже держал это закрытым; эта строка A5 отставала —
  выровнено. How-to не дублировать. Хвост — smoke флоу еды, трек D, не блокер
  выкладки _— ✅ 12.08 / сверка 13.08_
- ~~🔴 **14.08 `main` снова не деплоится.**~~ ✅ **14.08 вечер.** Днём
  `Deploy to Yandex Cloud` failure на `2f558a19` (runs 31807172663,
  31807467873): unit-тесты drift / шапка / сводка `health_data`. Вечер: success
  на `9fbd3cd8` (31811943849), затем два красных unit-гейта (`17f448d0`,
  `0885aa4e`). Живой прод **18:29:** `38834138` (31814737179,
  `app.heyslab.ru/version.json`). Реестр 1.11 и CF на проде. Пять своих —
  переподпись `personal_data` 1.0 _— 14.08_
- ✅ **Антипаттерн записан рецептом 12.08** в `apps/web/BUGS_HISTORY.md`: во
  внешнем `var()` только sand-роль и литерал. _— ✅ 12.08_
- ⏸ **Режим «v4 в ветке» снят решением 12.08:** push в `main` остаётся
  публикацией, см. план §13. Гейт «skip deploy» не заводим. Прежняя ⬜-строка
  про ветку до релиза оставлена здесь как история, не как открытая задача _—
  решение 12.08, пометка 13.08_

**Замороженная копия `stable.heyslab.ru`** — сборка прода от 11.08 (`36df9ce3`),
держится ради эталонных скринов старого интерфейса при перекраске. Её природу
надо понимать: **это не песочница.** Тот же API, та же живая база, оба адреса
стоят в CORS-allowlist всех функций. От прода её отделяет только клиентский
`__HEYS_READONLY_MODE__`.

- 🔴 **Два тупика подтверждены живьём на stable 12.08 и починены 13.08.**
  Возрастной гейт: модалка пыталась записать подтверждение, падала с «Только
  чтение», выйти можно было лишь через «Напомнить позже». Плашка офлайн-очереди
  обещала «ничего не потеряется, отправляю в фоне» при заблокированной записи.
  Плюс постоянные ошибки `mergeSaveKV` и геймификации в консоли — фоновая
  синхронизация билась о readonly

- ✅ **Readonly, круг второй: выложен и принят владельцем 13.08.** Smoke пройден
  глазами: согласия до конца без зависания, возрастной гейт без ошибки API,
  плашка «только чтение» на месте, интерфейс ведёт себя как прод. Приняты
  отчёты, питание, виджеты, модалка времени приёма, список продуктов ужина.
  Закрыты все четыре места: зависание после согласий (`isConsentBlocking` не
  блокирует в readonly, гейт не отдаёт `null` и не показывает ошибку),
  возрастной гейт (своя ветка и баннер, `confirmAge` не ходит в API), честный
  текст плашки «изменения здесь не сохраняются», и шум в консоли — геймификация,
  `mergeSaveKV` и оверлей продуктов теперь не пытаются синхронизироваться вовсе,
  а не ловят ошибку. По §1a: health-переключатели не пишут согласия в readonly;
  чек-ин и онбординг правки не потребовали. Тесты 6/6

  Выложено по рецепту `stable-heyslab-build-recipe.md`: worktree на `36df9ce3`,
  патчи `d75ec593d`, `3d1904513`, `4a7ced768`, `00c443259`, загрузка только в
  `stable-heyslab-ru`, worktree снят. Проверено живьём 13.08 с cache-busting:
  `stable.heyslab.ru/version.json` отдаёт `hash 36df9ce3`,
  `stableRebuild.id readonly-round2-20260813`,
  `bootAppBundle boot-app.bundle.20343613fc3a.js`, buildTime
  `2026-08-12T21:31:07Z`. Прод независимо на `b62698cc` — заморозку не задело.

- 🟡 **Критерий «чистая консоль» на stable не выполнен — остались два писателя,
  которых круг второй не закрыл.** Найдено на smoke 13.08 по скриншотам
  владельца, подтверждено по коду: ни в одном из двух файлов нет ни одного
  упоминания readonly.
  - `apps/web/heys_event_log_v1.js` (~297) —
    `[HEYS.eventLog] flush failed, requeued`, `consecutiveFails` растёт 1 → 2 →
    3 по мере хождения по экранам. Бесконечного цикла нет: есть
    `POISON_PILL_THRESHOLD`, голова очереди отбрасывается. Но предупреждение
    сыпется на каждое новое событие
  - `apps/web/insights/pi_early_warning.js` (~1468, ~1497) —
    `ews / weekly save.cloud.error: Только чтение (замороженная копия)`

  Плюс на загрузке `[SYNC] → отправка 9 записей` и следом
  `Re-queued in-flight client batch (sync-disabled)`: батч формируется и
  складывается обратно. Счётчик warnings за один проход по пяти экранам вырос с
  2 до 9, issues с 2 до 21.

  Сам по себе релиз это не держит: интерфейс верный, запись заблокирована,
  данные не портятся. Цена в другом — на stable нельзя пользоваться консолью для
  сравнения, настоящая ошибка утонет в шуме. Чинить при следующем касании
  stable, отдельного круга ради этого не собирать _— к следующему касанию
  stable_

  То, что в консоли **не** дефект и на прод тоже есть:
  `[HEYS.insights] Недоступные паттерны (17/41)`,
  `MEALREC … Insufficient data for context`, `pIndex empty (race?)`. Это
  продуктовое поведение на неполных данных, к readonly отношения не имеет

  ⚠️ Без строки запроса `stable.heyslab.ru/version.json` отдаёт версию от 11.08
  без блока `stableRebuild` — кеш. Проверять только с `?cb=<число>`, иначе
  выкладка выглядит непрошедшей _— 13.08_

- ⬜ **Тупики того же класса, найдены и не чинились** (§1a задания): `AgeGate` —
  `confirmAge()` пишет в API; включение опциональных health-функций —
  `logConsentsBySession`; первый вход и чек-ин — запись в KV. На типичном
  PIN-входе в копию встречаются реже согласий, но встречаются. Чинить тем же
  приёмом, когда понадобится _— к следующему касанию stable_
- ✅ **Вход на копию работает — owner smoke 14.08.** PIN пустил
  `stable.heyslab.ru`: клиент Антон Поплавский, плашка «Копия для сравнения —
  только чтение», эталон вкладок (не v4). Rebuild `pin-login-v1-20260814`,
  `boot-app.bundle.4592100534db.js` не менялся; мост только на
  `access_code_login_required`. `app.heyslab.ru` 14.08 18:29 — `38834138`.
  heys/555464 и `prompt-stable-consents` — снять. Предыстория 13.08: purge
  `health_data` упирал старую сборку в ConsentScreen; readonly-гейт это снял,
  14.08 отдельно починен PIN после login v2. _— 14.08_
- ⬜ **Правка идёт двумя путями:** коммит в `main`, где она инертна, и отдельный
  cherry-pick на `36df9ce3` с выкладкой только stable-бакета. Пересборка копии
  из текущего `main` уничтожает эталон — ради него всё и делается _— 13.08_
- ⬜ **Проверить остальные тупики того же класса:** где ещё продвижение зависит
  от успешной записи — первичная инициализация профиля, онбординг, чек-ин.
  «Проверил, других нет» — тоже результат _— 13.08_
- ⬜ Штатного скрипта выкладки stable в репозитории нет: в `deploy-yandex.yml`
  перечислены четыре бакета, stable среди них не значится. Значит выкладка
  ручная — записать команды, которыми она делается _— 13.08_
- ⬜ **Копия публична и отдаёт доминимизационную сборку** — в ней ещё живут
  анкета с вопросами о здоровье и трекинг цикла. Писать она не может, но
  проверить, закрыта ли она от индексации, и решить, нужен ли ей публичный адрес
  вообще _— до 18.08_

---
