# HEYS · архив закрытого

> Сюда переезжают выполненные пункты и отменённые формулировки из плана и
> треков. Ничего не удаляется: через месяц должно быть видно, что делали и
> почему. Обратно в план не возвращается. **Штамп: 13.08.2026 01:22 MSK.**

## 4.5 Порядок на 12.08

- ✅ пакет юристу ушёл 12 августа;
- ✅ разбор от юриста получен 12 августа — на восемь дней раньше срока, три
  круга вместо одного. **Это единственная позиция, где мы обогнали календарь, и
  обогнали заметно:** критический путь шёл через юриста, и он больше не самое
  узкое место. Правки в тексты и версия 1.11 с переподписанием — по-прежнему до
  27-го;

## 7. Трек A · Доступ и безопасность

- ✅ Инвентарь прямых ссылок: не только мессенджер, но и галерея дня
  (`heys_day_gallery.js:263` и три связанных файла)
- ✅ Установлено, что `url` — сохранённое поле в
  `client_messages.attachments[]`, полный публичный адрес, а не путь
- ✅ Найден источник: `POST /photos` сам конструирует публичный URL и отдаёт
  фронту
- ✅ Проверено, что DSAR-экспорт файлы вложений не отдаёт
- ✅ `url` убран из ответа `/photos/upload` — единственный путь чтения теперь
  авторизованный `/photos/read` по `path`
- ✅ Чтение переведено в обоих контурах: мессенджер, галерея дня и полноэкранный
  просмотр
- ✅ Кэш блобов: LRU на 60 записей, отрицательный кэш 30 секунд, ключ включает
  тип медиа
- ✅ **Аудио оказалось той же дырой** — голосовые лежат в том же бакете с такой
  же публичной ссылкой. Добавлен `fetchAudioBlob`, серверная поддержка `audio/*`
  в `/photos/read`
- ✅ Тест на отсутствие `storage.yandexcloud.net` в бандле — в
  `verify-legacy-bundles.mjs`, то есть в pre-push гейте. Проверено, что падает:
  строка подставлена, гейт упал, строка убрана — зелено
- ✅ Три регрессии от собственной правки найдены и починены: очистка base64 из
  localStorage держалась на `photo.url`; `result.url` использовался как признак
  завершения загрузки; **проверка `!result?.url` как условия успеха отправки
  голосового — без этой находки отправка голосовых сломалась бы полностью**
- ✅ 39 тестов мессенджера и 4 теста облачной функции зелёные
- ✅ `heys-api-photos` задеплоена, health-check прошёл, `/photos/read` без
  токена по-прежнему отдаёт 401
- ✅ Публичное чтение бакета `heys-photos` выключено
- ✅ **Найдено при проверке: закрытия бакета недостаточно.** У каждого объекта
  был собственный грант `AllUsers: READ`, поставленный при загрузке и живущий
  независимо от настройки бакета — первая проверка старой ссылки после закрытия
  бакета всё ещё отдавала 200. Грант снят со всех 70 объектов, проверено тремя
  случайными выборками и тремя разными ссылками — везде 403
- ✅ `ACL: 'public-read'` убран из кода загрузки, чтобы новые объекты не
  получали грант заново. Коммит `437826a06`, SEC-006 закрыт
- ✅ Подтверждена подделка `X-Forwarded-For` на живом проде
- ✅ Найдено, что оба счётчика ключевались на IP; защиты от перебора не было
- ✅ Логи проверены — следов перебора нет
- ✅ Вход по PIN закрывался на время правки, оригиналы функций сохранены
- ✅ Счётчик по номеру восстановлен из v2, добавлены нарастающая пауза и
  заморозка; миграция применена
- ✅ Тест на свойство: пять неудач с пяти адресов дают блокировку; проверено на
  живой функции
- ✅ Вход открыт обратно
- ✅ Три пути обхода гейта деплоя закрыты, `ALLOW_DISABLED_FUNCTIONS` требует
  причину
- ✅ `heys-api-sms` удалена из прода
- ✅ Причина отказа сервера доходит до экрана; словарь кодов, контракт-тест,
  предохранители теста
- ✅ Счётчик неудач срабатывает только на реально неверный код
- ✅ Установлено: ПЭП построена и выключена флагом `SMS_VERIFICATION_ENABLED`
- ✅ Черновик соглашения об использовании ПЭП написан
- ✅ Канвас входа: пять новых экранов и четыре правки внесены
- ✅ Серверная часть реализована 11.08, миграция
  `2026-08-11_client_login_scheme_v2.sql` применена. Одноразовый PIN на трое
  суток со сгоранием и гашением при перевыдаче; код доступа клиента с
  bcrypt-хэшем и проверкой слабых; разделение входа и подписания через
  `sign_consents_with_access_code_by_session`, `log_consents` отклоняет
  `pin_confirm` без кода; журнал дополнен `device_id`, `session_auth_method`,
  `document_text_snapshot`; таблица `client_devices` на 30 дней; очередь push;
  доверенное устройство снимает блокировку. Тесты 2/2
- ✅ Добавлен обратный property: клиент без `access_code_hash` через
  `verify_client_pin_v3` входит успешно. Вместе с блокирующим кейсом —
  двусторонняя проверка гейта, страхует от сценария «закрыли путь всем»
- ✅ Тесты доведены с двух до восьми property, отдельный кейс на каждое
  свойство, 8/8 на staging: одноразовый не срабатывает второй раз; перевыдача
  гасит старый; слабый код отклоняется; сброс куратором рвёт сессии; доверенное
  устройство снимает блокировку; legacy-путь закрыт при наличии кода доступа;
  checkbox-подписание запрещено после миграции; **стыковка — доверенный вход
  проходит, но подписание без кода отклоняется**
- ✅ По дороге найден баг: `NULL` в `sign_consents_with_access_code_by_session`
  не отклонялся, потому что `crypt(NULL, …)` в SQL возвращает `NULL`, а не
  ошибку. Исправлено отдельной миграцией
- ✅ Политика sunset зафиксирована в миграции и в `todo.md`:
  `verify_client_pin_v3` работает только пока `access_code_hash IS NULL`; после
  задания собственного кода — только новый путь; checkbox-подписание через
  `log_consents*` запрещено при наличии кода доступа
- ✅ **Выкачено на прод 11.08 вечером, серверная часть login закрыта.** Три
  миграции применены по отдельности, exit 0 у каждой. Окно legacy проверено:
  `verify_client_pin_v3` отдал `success` и `session_token` без
  `access_code_login_required`. Pre-check подтвердил обе RPC в `pg_proc` — тесты
  не прошли вхолостую; 9/9 на проде до деплоя функций. Деплой трёх функций
  выполнен. Smoke на отдельном тестовом клиенте: trusted login, подписание с
  кодом проходит, без кода — `access_code_required`. Коммит `975a2c070`
- ✅ **Push закрыт 11.08.** `origin/main` = `88433da59`, локальное и удалённое
  сошлись. По дороге: merge с двумя чужими коммитами через ветку-страховку,
  конфликт `index.html` разобран вручную, скоуп `localStorage` доведён до нуля
  ошибок, дрейф палитры разобран по семьям и закрыт вариантом 1B — `--v4-sand-*`
  заведены во всех палитрах, classic остаётся синим. Сканер перестал считать
  совпадения в комментариях. WARN сведены в строку `160 allowlisted, 0 new` с
  падением гейта при росте выше baseline
- ✅ **Выданный PIN не срабатывает — причина найдена 12.08.** См. условие
  готовности 2. Прежняя формулировка: Владелец наблюдает его давно, в том числе
  на приглашениях в анкету. Сегодня получена воспроизводимая пара: клиент `пупс`
  (`6b9e20c0-4757-4993-991d-0de24247a2bc`), телефон в панели `78888888888`, PIN
  `1212` выдан дважды — из панели при создании и отдельно кураторским
  инструментом. Вход отклоняется с «PIN не подошёл», в `heys_get_client_health`
  ноль попыток за час.

  Что уже известно по коду: `verify_client_pin_v3` ищет клиента по
  `regexp_replace(phone,'[^0-9]','','g') = <введённые цифры>` **и**
  `pin_hash IS NOT NULL`. Если хеш не записан, клиент не находится вовсе — и
  человек получает «PIN не подошёл» вместо «клиент не найден». Отсюда рабочая
  гипотеза: PIN пишется не туда или не пишется, а сообщение об ошибке маскирует
  причину. Проверять надо одним запросом, гадать бессмысленно.

  **Это не тестовая мелочь.** Если выданный PIN не работает у клиента, не
  работает вход в продукт — все остальные условия готовности при этом не имеют
  значения. Поднято до блокера релиза _— 13.08_

- ✅ `signature_method`: миграция
  `2026-08-11_consents_pin_confirm_signature.sql` применена к проду. Добавлено
  `pin_confirm`, вычищены `sms_code` и `messenger_code`. Пре-чек подтвердил
  отсутствие строк с удаляемыми значениями (159 в `consents`, 100 в
  `trial_candidate_consents` — везде `checkbox`). Найдено и исправлено третье
  место с тем же перечислением: функция `public.log_consents` дублировала
  whitelist своей проверкой, без её правки ALTER ничего бы не дал. Мёртвая
  ссылка на `sms_code` убрана из `heys_consents_v1.js:474`
- ✅ Проверен весь § 8 соглашения против кода. Три пункта из четырёх сходятся:
  ключ ПЭП и формулировки кнопок совпадают дословно, аутентификация
  действительно предшествует подписи, состав полей журнала совпадает.
  Единственное расхождение — `sms_code` в примере § 8.3, созданное сегодняшней
  же миграцией
- ✅ Push при входе с нового устройства и при сбросе кода куратором — очередь и
  обработчик готовы
- ✅ Две правки из трёх сделаны в версии 1.1 и приняты 12.08: пункт 4.1 «б»
  требует ввода кода непосредственно перед подписанием, трансграничная передача
  убрана из 8.1. См. C6
- ✅ **Legal gate сработал правильно.** В `a62717bfd` из политики убрали слово
  «цикл», не поднимая версию — хэш разошёлся со снапшотом 1.7 и деплой встал.
  Правка содержательная, не мусор. Откачена вместе с такими же в
  `health-data-consent.md`, `consent-forms.md` и на лендинге; содержание уходит
  в пакет 1.11 после минимизации, не отдельным 1.8 — два переподписания подряд
  не делаем
- ✅ Три отставших теста починены 12.08. Четвёртый всплыл уже в CI —
  `messenger-states` по `data-theme$="dark"` и `--v4-ink-2`, закрыт вторым
  коммитом
- ✅ **Мёртвые селекторы в баннере починены 12.08:** `training-summary` и
  `.month-trainings-card` заменены на живой `activity`. Прежняя формулировка:
  `heys_curator_actions_banner_v1.js:160-162` для `training_removed` ищет
  `[data-curator-target="training-summary"]` и `.month-trainings-card` — в JS
  после Activity v4 ни того, ни другого нет. Живой fallback только `activity`,
  поэтому клик «к правке» скроллит на корень вкладки, а не на карточку. У
  `training_added` тот же мёртвый селектор. **Чинить и тест, и селекторы: иначе
  тест позеленеет, а кнопка останется тупой** _— 13.08_
- ✅ В письме юристу состояние описано как есть: функция активна, обслуживает
  доступ оператора, внешний OAuth-канал не открыт, обращений от внешних клиентов
  ноль, аккаунтов клиентов через неё не ведут
- ✅ **Пункт 0 закрыт: картина Б.** Данные клиентов в зарубежную модель не
  уходят, трансграничной передачи данных клиентов нет, отмена легализации
  остаётся в силе. Через канал проходят данные супруги. Юрист: исключение для
  личных и семейных нужд на эту конструкцию не ложится — тот же инструмент
  встроен в рабочий контур оператора, разделить «здесь я муж, здесь оператор» в
  одном техническом канале не выйдет. Формально обязанность уведомить по ст. 12
  существует, решение её не исполнять остаётся принятым осознанно. Рамка риска:
  субъект один и он же член семьи, вероятность жалобы нулевая, без жалобы состав
  самостоятельно не выявляется. Риск перестаёт быть низким ровно в одном случае
  — если в канал попадает кто-то ещё
- ✅ Стенограмма и журнал ведутся, проверено 12.08: `transcript/2026-08-12.md` —
  910 строк, `journal/2026-08.md` — 1289 строк с блоками «Вводная / Разбор /
  Итог / Открыто». Путь чувствителен к букве: `transcripts/` во множественном
  отвечает `ok`, но отдаёт пустоту и `rev: 0` — молчаливая ловушка, описана в
  карте артефактов
- ✅ **Упавший деплой `a7fcc9202` разобран 12.08: частичной заливки не было.**
  Run 31587621406, упал `Unit tests (deploy gate) 1/2`, шаг
  `Deploy artifact to Yandex Cloud` пропущен. Проверено по workflow:
  `deploy-artifact` объявлен через `needs: [detect, test, build-artifact]`, то
  есть при красных тестах загрузка физически не стартует. `app.heyslab.ru` и
  ORIGIN оба `a7fd195a`. Задача heys/418cde закрыта. Гейт отработал ровно как
  задуман — это тот редкий случай, когда предохранитель проверили не на
  счастливом пути
- ✅ **Починено и выложено 12.08.** Коммит `679a69769`, деплой зелёный, прод
  `2026.08.12.1451.679a6976` — проверено живым запросом `version.json`. Труба
  открыта, версия 1.11 поедет обычным путём. Прежняя формулировка пункта: Все
  двадцать сдвигов оказались в одном месте — APS food (`600-steps-and-aps.css` и
  `heys_add_product_step_v1.js`). Причина — вложенный
  `var(--v4-sand-ink, var(--v4-ink, #201e1d))`: гейт меряет внутреннюю
  classic-роль, и классика получает синий или зелёный вместо песка. `--fix`
  намеренную перекраску не отменяет — снимает classic-слой, оставляя sand-роль и
  литерал, то есть ровно то решение, которое уже принято: свои sand-роли,
  классику не подгонять. Задача heys/1ab6a1 _— 13.08_
- ✅ Механизм записи проверен 12.08 и сделан правильно: hostname-гейт без
  query-параметра (`index.html`), белый список RPC и отсечка любого не-GET на
  REST (`heys_yandex_api_v1.js:601` и `:696`), баннер в шапке. Белый список, а
  не чёрный — новый пишущий метод не просочится молча
- ✅ **Реализовано и выложено на stable 12.08.** Коммит `4a7ced768` в `main` —
  локальный, не запушен. Гейт согласий в readonly не блокирует вход,
  `log_consents` не вызывается, постоянный баннер «Замороженная копия — только
  просмотр» с `data-testid`. Тесты `consent-readonly-stable.test.js` 4/4.
  Выложено только в бакет `stable-heyslab-ru`, `app.heyslab.ru` остался на
  `41955372` — проверено. Осталась живая проверка входа с PIN
- ✅ **Рецепт сборки записан 12.08** —
  `docs/release/stable-heyslab-build-recipe.md`, коммит `2c8f2ce15`, и копия
  рядом с самой сборкой на `stable.heyslab.ru/STABLE-BUILD-RECIPE.md`. Второе
  важнее первого: рецепт лежит там же, где артефакт, и не потеряется при смене
  репозитория
- ✅ **`version.json` на stable больше не врёт:**
  `36df9ce3+consent-readonly-20260812`, с блоком `stableRebuild` — список патчей
  и имя бандла. Сборка воспроизводима скриптом
  `scripts/stable-rebuild-version.mjs`. `app.heyslab.ru` не тронут
- ~~🔴 Как собран нынешний stable — записать.~~ Прежняя формулировка:
  Cherry-pick на `36df9ce3` дал конфликт в `index.html` и CSS, сборка собрана
  вручную в worktree: база `36df9ce3` + readonly-коммиты `d75ec593d` и
  `3d1904513` + правка согласий. Это не воспроизводится одной командой, и
  человек, который через месяц захочет пересобрать копию, восстановит рецепт
  только из этого абзаца. Положить рецепт рядом с самой копией — в
  `docs/release/` файлом, а не только в план _— 13.08_
- ~~🔴 `version.json` на stable теперь врёт.~~ Закрыто, см. выше. Прежняя
  формулировка: Хеш там остался `36df9ce3`, хотя бандл новый
  (`boot-app.bundle.526ae52da151.js`). Линию сохранили намеренно, но побочный
  эффект: определить, что реально лежит на копии, по версии больше нельзя.
  Сегодня мы уже дважды обожглись на том, что версия отстаёт от факта. Добавить
  в `version.json` копии суффикс сборки или отдельное поле — иначе следующий
  вопрос «какая версия на stable» получит неверный ответ _— 13.08_
- ~~🔵 Реализовано локально 12.08, не закоммичено.~~ Прежняя формулировка: В
  рабочем дереве правки в `heys_app_gate_flow_v1.js` (+9) и
  `heys_consents_v1.js` (+69/−21): в readonly гейт не держит вход,
  `log_consents` не вызывается, на экране постоянный баннер «Замороженная копия
  — только просмотр. Согласия не сохраняются» с `data-testid`. То есть основная
  часть задания сделана; осталась трудная — коммит, cherry-pick на `36df9ce3` и
  выкладка только stable-бакета, плюс §1a про тупики того же класса и тесты _—
  13.08_

## 8. Трек B · Персональные данные и документы

- ✅ **Миграции применены на прод 12.08.** Блокирующий гейт снят:
  `save_trial_intake_by_session` больше не требует согласия на здоровье. По ходу
  починены два дефекта самих миграций — clamp `current_step` 5→4 до CHECK и JSON
  cast в audit. Коммиты `a5ba31a09`, `91078c98e`. Редеплой функций не
  потребовался. `disable_optional_health_features_v1` на этом шаге создана, но
  не вызвана, purge не запускался — и то и другое сделано позже в тот же день,
  см. ниже по треку
- ✅ Код написан 11.08, к проду не применялся. Анкета: 5 шагов вместо 6, health
  и safety сведены в один экран предупреждения (schema 1.2), хранится только
  `warning.acknowledged_at` и `warning.text_version`. Убраны health*data consent
  на шаге 1, `INTAKE_ATTENTION_RULES` и флаги «Требует внимания», чек-лист
  куратора переформулирован. Опциональные функции: модуль
  `heys_health_features_v1.js`, цикл/замеры/добавки выключены по умолчанию,
  гейты записи и в приложении, и в MCP, `custom*\*` добавки отклоняются. Тесты
  34/34 и 122/122. ~~Три миграции написаны, ни одна не применена~~ — применены
  на прод 12.08, см. выше
- ✅ Шаги «Здоровье и ограничения» и «Проверка безопасности» сведены в один
  экран предупреждения с подтверждением. Анкета на пяти шагах, текст версии 1.0
  в коде, коммит `2bbf90146`
- ✅ Условия валидности подтверждения выполнены: вариант ответа один, сценарий
  не ветвится, хранятся только `warning.acknowledged_at` и
  `warning.text_version`. **Инвариант остаётся действующим и после закрытия
  пункта:** любое последующее «улучшение» — пометка куратору, отдельный тариф,
  второй вариант ответа — возвращает запись в специальную категорию. Проверять
  при каждой правке анкеты и экрана предупреждения Pro Спорт
- ✅ Флаги «Требует внимания» и `INTAKE_ATTENTION_RULES` убраны, пункт чек-листа
  куратора переформулирован под новый экран
- ✅ Канвас «Цикл» готов (отдельный файл `Цикл_v4_dc.html`): пять экранов —
  зачем включать, отдельное согласие, подпись кодом, включённое состояние, отзыв
  с удалением. Подпись той же шторкой, что у общих документов; отключение и
  удаление одним действием
- (хвост) Прежняя формулировка: **обещания канваса не обеспечены кодом.**
  Куратор видит только цель дня, но не фазу и не даты; даты хранятся отдельно и
  не попадают в выгрузки куратора; при отключении удаляются сразу. Кураторский
  контур сейчас умеет писать `cycle_day` и читать профиль. Юрист: обещание в
  согласии, не обеспеченное кодом, хуже отсутствия обещания — это довод о
  недостоверности предоставленной субъекту информации. Либо код приводится в
  соответствие до выпуска согласия, либо обещание убирается из текста _— до
  20.08_
- ✅ **Проверка закрыта 11.08: вариант «даты не покидают устройство» не
  держится.** Все четыре условия юриста — «да», подтверждено кодом с `file:line`
  и SQL. На проде уже 44 строки с `cycleDay` и 3 профиля с включённым трекингом.
  Артефакт: `docs/release/lawyer-cycle-device-check.md`
- ✅ **Подтверждено вторым разбором юриста:** он независимо предложил тот же
  третий вариант — не выпускать функцию к релизу. Против device-only высказался
  прямо: при объёме L и десятке точек записи «сделать наполовину» не риск, а
  вероятный исход, а обещание в согласии, не обеспеченное кодом, хуже честного
  «да». Purge сорока четырёх строк убирает специальную категорию из продукта
  целиком, и из записи в реестре снимается «здоровье»
- ✅ **Исполнено 12.08, локально.** Kill-switch
  `CYCLE_TRACKING_IN_RELEASE = false`, toggle скрыт, enable отклоняется; убраны
  входы в профиле и регистрации; шаг чек-ина, виджет и запись дня цикла не
  требуют; MCP отвечает `cycle_tracking_removed` на три поля; канвас в архив;
  цикл убран из согласия на здоровье, политики и форм. MCP 123/123, release-gate
  4/4. Не закоммичено, не задеплоено — на проде MCP пока со старым гейтом
- ✅ Закоммичено и задеплоено 12.08: kill-switch в `a62717bfd`, флаг и MCP-гейт
  в `ce346d7e9`, прод `2026.08.12.1140.ce346d7e`. Кураторский контур `cycle_day`
  больше не принимает, кроме профиля с `internalAccount`
- ✅ **Исключение для аккаунта супруги, решение владельца 12.08.** Все три
  опциональные функции — цикл, замеры, добавки — остаются включёнными и данные
  не удаляются. Реализация: флаг `internalAccount` на профиле, не список
  идентификаторов в коде. Kill-switch, гейты MCP и purge смотрят на флаг. Промпт
  `prompt-internal-account`
- ✅ Purge выполнен 12.08 с исключением по UUID
  `4545ee50-4f5f-4fc0-b862-7ca45fa1bafc`, акт составлен. Текст предупреждения
  версии 1.0 в коде
- ✅ **Флаг `internalAccount` выкачен 12.08.** Миграция применена, `heys-mcp`
  задеплоена, push прошёл, CI зелёный, прод `2026.08.12.1140.ce346d7e`.
  Проверено через MCP: у обычного профиля запись цикла отклоняется на всех
  путях, у профиля с флагом проходит. В боевом allowlist идентификаторов нет —
  только флаг; UUID остался в seed-миграции, исторических purge и документах,
  это не рантайм
- ✅ Инвентарь снят 12.08: числа менялись трижды по мере правок. Актуальные на
  12.08 после вызова `disable_optional_health_features_v1` (сбросил 2 профиля):
  `cycleTrackingEnabled=true` — 0; профилей с любым ключом цикла — 3, все
  `false`; дней с непустым payload — 45; дней с любым ключом цикла, включая
  пустые, — 426; замеры — 7 дней; добавки — 264 дня; ответы анкет со
  спецкатегорией — 0; согласия `health_data` — 4 клиентских и 5 кандидатских.

  **Purge выполнен 12.08 в 11:15** по команде владельца, с исключением семейного
  аккаунта. Фактически очищено: 2 профиля с ключами цикла, 245 дней с ключами
  цикла, 5 замеров, 191 день добавок; отозвано 4 клиентских и 5 кандидатских
  согласия `health_data`. Секции `health` и `safety` в анкетах к моменту вызова
  были уже нулевыми — их сняла intake-миграция _— ✅ 12.08_

- ✅ Канвас «Цикл» дополнен до семи экранов: добавлены отметка начала цикла и
  состояние «функция неприменима». Первый экран разгружен, блок «Кто это видит»
  укладывается в корпус
- ✅ Цифра по лютеиновой фазе исправлена на диапазон «по разным оценкам, 5–10 %»
  вместо точных 8 %
- ✅ Формулировка про выгрузки уточнена: даты не попадают в отчёты и выгрузки
  **для куратора**, в собственной выгрузке клиента они есть — это его данные
- ✅ Цикл, замеры, добавки выключены по умолчанию у всех, включая существующие
  аккаунты. `DEFAULTS` в `heys_health_features_v1.js`, исключение только по
  флагу `internalAccount`
- ✅ Пока функция выключена, поля не пишутся ничем, включая MCP. Проверено 12.08
  через MCP на двух профилях: обычный отклоняется на всех путях, с флагом
  проходит
- ✅ Добавки: закрытый справочник, произвольная строка отклоняется —
  `value.startsWith('custom_')` возвращает `false` в
  `heys_health_features_v1.js`
- ✅ **Уже собранные данные о здоровье удалены 12.08, акт составлен** —
  `health-data-destruction-act-2026-08-12.md`. Порядок соблюдён полностью:
  миграции → живая анкета → инвентарь владельцу → команда → purge → акт.
  Исключение семейного аккаунта зафиксировано в акте отдельной строкой, с
  основанием и составом сохранённого
- ✅ Проверено 12.08: автосохранение двигает `updated_at`
- (хвост) Прежняя формулировка «убрать из выбора канала везде» была шире, чем
  нужно, и я её сузил по возражению владельца. Обоснование: это поле не обещает
  вести там переписку — оно отвечает на вопрос «куда вам написать, чтобы
  начать». Через выбранный канал идёт приглашение: код доступа и ссылка, без
  содержания о питании и здоровье. Ровно та же конструкция, что уже принята для
  кандидатского флоу. Граница, за которую это решение не распространяется: как
  только через канал пойдёт содержание — что ел, как спал, фото, голосовые, — он
  перестаёт быть способом первого контакта и попадает под всё, из-за чего
  Telegram убирали. Поле остаётся полем «куда написать», не превращается в «где
  мы будем общаться». **Ручная отправка в WhatsApp и MAX — решение владельца
  12.08: делаем, проблемы нет.** Интеграции у этих двух каналов нет, куратор
  пишет руками со своего телефона; для клиентского сервиса это нормально и
  отказываться от удобства ради единообразия не стоит. Юрист 12.08 признал
  доставку приглашения через бот допустимой, а ручная отправка — та же
  конструкция без автоматики. Единственное, что к этому прилагается: граница та
  же, и держать её теперь нечем, кроме дисциплины. У бота есть текст, который не
  меняется, и предупреждение «не отправляйте сведения о здоровье»; у ручного
  сообщения нет ни того, ни другого. Поэтому граница пишется в ту же инструкцию
  куратору, где правило про свободный текст: в мессенджер уходит приглашение и
  статус заявки, содержание — никогда. Механизма здесь не будет, и это осознанно
  принятая дисциплина, а не недосмотр _— решено 12.08_
- ✅ **Ответ юриста получен 12.08: бот при регистрации допустим.** Кандидатский
  флоу остаётся как есть — доставка кода доступа и ссылки через Telegram, без
  имени и содержания. Вопрос закрыт, флоу не перестраиваем.
- ✅ **Уточнение получено 12.08, ответ — «передача есть».** Юрист: адресация
  сама по себе уже передача — вы сообщаете Telegram, что этот пользователь
  является кандидатом HEYS; плюс PIN как аутентификационный реквизит и токен в
  ссылке. Он же признал, что в четвёртом разборе был неточен: служебное
  сообщение без содержания уменьшает объём передачи почти до нуля, но не
  устраняет её. Единственное состояние без передачи — не использовать внешний
  мессенджер для связи с клиентами вообще.

  **Следствие:** обе формулировки недостоверны — «передача на территорию
  иностранных государств не осуществляется» в согласии 1.0 и «трансграничная
  передача: нет» в записи реестра. И это не только про Telegram: push через FCM
  и APNs — та же конструкция.

- ✅ **Как устроены push — проверено по коду 12.08: собственный веб-пуш с VAPID,
  Firebase нет.** Клиент вызывает `pushManager.subscribe` с
  `applicationServerKey` (`heys_push_v1.js:218`), сервер шлёт библиотекой
  `web-push` через `setVapidDetails` и хранит сырой `endpoint` браузера
  (`heys-api-push`, `push_subscriptions`). Единственное упоминание firebase в
  репозитории — кэш сборки старого андроид-приложения. **Следствие: получателя
  выбирает не оператор, а браузер клиента** — Chrome и Android уходят к Google,
  Safari к Apple, Firefox к Mozilla, Edge к Microsoft
- ~~⬜ **Уточнить одну формулировку у юриста — от неё зависят два документа.**~~
  Отвечено, см. выше. Прежняя формулировка: «Допустимо» и «трансграничной
  передачи нет» — разные утверждения. Если он квалифицировал доставку кода как
  не являющуюся трансграничной передачей, то фраза «передача на территорию
  иностранных государств не осуществляется» в согласии на обработку ПДн и запись
  в реестре остаются верными и правки не требуют. Если же он сказал «передача
  есть, но допустима на таком основании», обе формулировки становятся
  недостоверными, и их надо менять — в пакете 1.11 и при ближайшей подаче
  сведений. Спросить одной строкой, ответ короткий _— 13.08_
- ✅ `subprocessors.md` §4 приведён к факту, SMS-провайдер назван
- ✅ §6 остаётся в силе: клиенты через зарубежную модель не ведутся
- ✅ Уведомление в РКН не подаётся — решение владельца
- ✅ Проверено: конфликт «журнал подписания 3 года против 30 дней политики» был
  мнимым. Политика не обещает удаление всего за 30 дней — этот срок относится
  только к отклонённой предтриальной анкете, а доказательства согласий и раньше
  хранились 3 года. Уточнение внесено явно в оба документа, чтобы не всплыло
  снова
- ✅ Проверено 12.08: `purge_health_data` — другой путь, чистит только
  `client_kv_store` у клиента, к retention анкет отношения не имеет; права у
  `heys_rpc` отозваны. Очистка отклонённых кандидатов делает настоящий `DELETE`,
  шифротекст уходит со строкой — не пометка
- ✅ Проверено вторым разбором: хэши в трёх выгрузках сходятся с реестром и
  манифестом — цепочка «версия → хэш → снимок текста» работает, ядро
  доказательственной конструкции построено правильно
- ~~Приказ о назначении ответственного за организацию обработки~~ — не
  требуется: обязанность установлена для операторов-юрлиц (ч. 1 ст. 22.1), у ИП
  её нет
- ✅ **13.08 · prompt-transfer-measures на проде.** Одноразовый код кандидата:
  миграция `2026-08-13_trial_candidate_onetime_pin_v1.sql`, второй вход →
  `onetime_pin_consumed`, срок 3 суток, перевыдача сбрасывает; копирайт
  «одноразовый» возвращён. Белый список хостов push: `push-endpoint-host.js`,
  гейт до INSERT, лог `[push] subscribe rejected`, код
  `push_endpoint_host_not_allowed`; действующая версия `heys-api-push` =
  `d4ei20s3fblieque3967` (commit `b8e00b87d`), первая выкладка того же кода —
  `d4em5pk67f68af1qi3v5`. Только `fcm.googleapis.com` и `web.push.apple.com`.
  Оба утверждения §5 п.5 оценки правового режима стали правдой — оценку можно
  подписывать
- ✅ **Аудит подписок 13.08:** `push_subscriptions` — 1× `web.push.apple.com`;
  `curator_push_subscriptions` пусто. Посторонних служб нет
- ✅ Отклонённую подписку логируем (не молча) — вместе с гейтом хостов
- ~~🔴 «два утверждения описывают меры, которых пока нет» / «гейта тоже пока
  нет» / «нужен серверный белый список»~~ — снято реализацией 13.08 (см. выше).
  Историческая проверка 12.08 «код кандидата не одноразовый» остаётся выше как
  снимок на дату; с 13.08 поведение другое
- ✅ **13.08 · prompt-push-consent-gate на проде.** Коммит `a900a76ee` на
  `origin/main`. Миграция `2026-08-13_revoke_consent_detach_push_v1.sql`:
  `revoke_consent` и `revoke_consent_by_session` при отзыве `push_notifications`
  или `personal_data` делают `DELETE FROM push_subscriptions`;
  `delete_my_account` тоже. Гейт `clientHasLivePushConsent` до INSERT в
  `handleSubscribe` и до чтения подписок в `handleTest`, `sendPushToClient`
  (`heys-api-messages`) и `sendToClient` (`heys-cron-reminders`) — в промпте
  отправитель был один, по коду их три, закрыты все. Кураторская ветка не
  менялась. Прод до правки: 2 подписки, обе с живым `push_notifications`
  (Александра Apple, Полтавский FCM) — ничего не удаляли. Deploy commit
  `a900a76ee`: push `d4er203c3jmc9pl6kppp`, messages `d4e76p38f6cro19kge6g`,
  cron `d4ep4crjo2o4ojqkcgg8`. Тесты: unit 5/5, host 5/5, DB 2/2

## 9. Трек C · Лендинг

- ✅ Четырнадцать карточек разобраны, статусы и источники проставлены
- ✅ Сборка актуализирована, введено правило «при расхождении побеждают код и
  документ решений»
- ✅ **Текст предупреждения вставлен и принят 12.08.** Версия 1.0, коммит
  `2bbf90146`. На канвасе анкеты остаётся пометка «Проект текста — ждёт юриста»
  — обновить при следующем касании макета
- ✅ **TTL заведён 12.08:** незавершённые кандидаты чистятся через 30 дней без
  активности, hard `DELETE` — шифротекст уходит со строкой. Миграция применена.
  Строка «черновик ждёт 30 дней» на экране стала правдой
- ✅ Проверено: автосохранение двигает `updated_at = NOW()`. Неделя заполнения
  не выглядит бездействием
- ✅ Проверено: код кандидата **не одноразовый** — тот же `pin_hash` работает
  повторно, выдаётся новая сессия на 7 дней. В анкете формулировка исправлена на
  «код из сообщения куратора»
- ✅ **Долг закрыт 12.08.** Поле подписано «Код от куратора» — подпись называет
  источник, а не срок жизни. Слово «одноразовый» осталось только в клиентском
  флоу, где код действительно сгорает. Слот ошибки кандидата исправлен на «код
  не подошёл, проверьте цифры»
- ✅ **Экран блокировки поправлен:** «напишите куратору — он снимет блокировку»
  вместо «выдаст новый код». Если код не сгорает, выдавать новый бессмысленно. В
  продукте формулировка уже была ближе к правде: при `rate_limited` —
  «Попробуйте позже или напишите куратору»
- ✅ Срок черновика показан и на возврате, и в момент закрытия анкеты на
  середине
- ✅ **Предупреждение в бот выкачено и проверено 12.08.** Live smoke: первый
  прогон — `purge_warn.sent:1`, метка `purge_warn_sent_at` проставлена; второй —
  `sent:0`, целей ноль. То есть предупреждение уходит один раз, а не каждый день
  с 28-го по 30-й. Коммит `13fd70a83`, три функции передеплоены
- ✅ `function_id` MCP выровнен 12.08: в отслеживаемом `api-gateway-spec.yaml`
  стоял мёртвый `d4egv2a6…`, живой — `d4epjmd9…`. Заменено 46 вхождений, факт
  поправлен в README, коммит `44841e1df` на origin. Живой шлюз не трогали — там
  id уже был верный
- ✅ Проверено: заглушки токенов и разведение поллеров. `INTERNAL_CRON_TOKEN` в
  `.env` — плейсхолдер на 32 символа, не обрезок 64-символьного из Lockbox,
  ротировать нечего. Три бота крутятся на трёх разных токенах и общий `update`
  не делят; гонка в тесте была артефактом — плейсхолдер принимался за токен, и
  оба поллера делили один mock
- ✅ **Дефолт снят 12.08.** `notificationsOptIn` переведён в `useState(false)`,
  коммит `8e6971dd1`. Обязательные и маркетинговый чекбоксы не тронуты
- (хвост) Прежняя формулировка дефекта: `<br>` как текст, звёздочки
  `**Последнее обновление:**`, подчёркивания вокруг курсива, сбитая нумерация
  («36.» вместо «4.»), а в политике вместо таблицы состава данных — строка
  `| Группа | Примеры | Цель |`. **Таблица — самое неприятное: именно она несёт
  перечень обрабатываемых данных, и пользователь его фактически не увидел.** Хэш
  и доказательственная цепочка не рвутся, но довод «мне показали нечитаемый
  документ с обрывками кода» при споре звучит убедительно. Чинить рендер до
  выката 1.11, иначе новые тексты приедут с теми же артефактами _— до 20.08_
- ✅ «Добавление еды»: 33 экрана, принят, промпт кодеру готов
- ✅ «Вход»: пять новых экранов, четыре правки, синяя палитра решена заливкой,
  `#a75c2b` и `#2f2b25` исправлены
- ✅ Терминология сведена: слово PIN убрано со всех экранов, у клиента «код», у
  выданного куратором «одноразовый код». Проверено — в разметке остались только
  упоминания в заметках о самой правке
- ✅ Четыре недостающих состояния: «Коды не совпали», «Код слишком простой» с
  перечислением требований, ошибка при подписании со счётчиком оставшихся
  попыток, «Подписано» со списком сохранённого в записи
- ✅ Дубль блока про подпись снят с экрана повтора — заодно ушло переполнение
  карточки
- ✅ Дубль текста на экране блокировки убран
- ✅ «Это устройство» вместо «этот телефон»
- ✅ Экран техработ: убраны «попробуйте через час» и «Вход куратора работает»
- ✅ «Мы не узнаём это устройство» — внесено в обе версии экрана
- ✅ **Соглашение о ПЭП 1.1 принято.** Разведение входа и подписания сделано
  двумя пунктами вместо одной правки: 4.1 «б» требует ввода кода непосредственно
  перед подписанием, 4.2 прямо говорит, что вход с зарегистрированного
  устройства без кода допускается, а подписание — нет ни при каких
  обстоятельствах. Трансграничка убрана из перечня подписываемых, вместо неё
  согласия на подключение отдельных функций. Реквизиты подставлены, заглушек нет
- ✅ **Предупреждение анкеты 1.0 вычитано и принято.** Три добавления юриста:
  тренировочная часть — иначе разошлось бы с офертой, где противопоказания для
  тренировок уже появляются; фраза про удаление сведений — превращает
  предупреждение в часть процедуры по свободному вводу; заявление о возрасте
  активным действием, а не выводом из года рождения
- ✅ **Текст уже в коде** — `heys_trial_intake_v1.js`,
  `WARNING_TEXT_VERSION = '1.0'`, коммит `2bbf90146`. Тон принят владельцем
  12.08. Версия пишется в `warning.text_version` при подтверждении
- (хвост) Прежняя формулировка «этот пункт снят из пакета» противоречила C6 и
  снята _— до 27.08_

## 10. Трек D · Продукт до первых клиентов _— до 25.08_

- ✅ Анкета: адресная ошибка валидации и `unsure` на экране сверки исправлены
- ✅ Промпты «Сохранить только себе» и «Очередь модерации» написаны, развилки
  закрыты решениями
- ✅ `prompt-food-addendum` слит в `prompt-food` — два документа с указаниями по
  одной задаче убраны. Первым шагом задания заводится роль `--v4-btn-on-act`
  отдельным коммитом с остановкой и отчётом владельцу: файл палитры общий для
  всех вкладок v4
- ✅ **`prompt-food`, шаги 2–5 приняты 12.08 с оговорками** (ACCEPT-WITH-GAPS).
  На `origin/main` коммит `a7fcc9202`, тронуты только
  `heys_add_product_step_v1.js` и `600-steps-and-aps.css`; `_meals.js` не
  тронут, чужой scope не задет. Шаг 2 в коде: пять исходов модерации плюс «не
  сохранён», harm без Toast.

  **Хвост приёмки, всплывший позже:** на этом коммите красный деплой-гейт —
  `ui-v4-classic-drift`, 20 сдвигов в классической палитре. В отчёте о приёмке
  этого не было, вскрылось при разборе упавшего деплоя. Разница существенная:
  «принято с оговорками» читается как «лежит и работает», а фактически коммит
  лежит и не выкладывается, заодно запирая выкладку всему остальному. См. A5

- ✅ **Smoke пройден на локалке 12.08, 4/4**
  (`playwright.food-smoke.config.ts`): новый продукт, пять исходов модерации,
  выход с черновиком, фото на граммах. Дрифт-гейт чист
- ~~🔴 **Smoke нашёл живой дефект на проде.**~~ Исполнено push
  `1ef2a1370`/`41955372b`. Прежняя формулировка: диалог «Выйти…» не работал
  (`useEscapeToClose` → `StepModal.hide()`, `closeModal` undefined) _— 12.08_
- ✅ **Фикс выложен в `main` 12.08:** `1ef2a1370` — диалог выхода через
  `context.onClose`, `StepModal.hide()` убран из Escape; `41955372b` — smoke в
  репозитории и роль `--v4-sand-accent-bg`. Проверено владельцем по репо:
  `600-steps-and-aps.css` и степ-модуль чисты; рецепт в `BUGS_HISTORY.md` лёг с
  `679a69769`. Опасение «source-only не донесёт до бандла» снято: `build:ci` =
  `prebuild → build:react → bundle:legacy → verify-legacy-bundles → build:dist`
- ✅ **Деплой доехал, критерий старта выполнен: прод `2026.08.12.1511.41955372`,
  сборка 15:13 MSK.** Фикс диалога выхода в проде, чистый прогон можно гнать.
  Критерий был именно такой — не «CI зелёный», а сменившийся хеш в
  `app.heyslab.ru/version.json`
- ~~⬜ **Проверить, каким клиентом заходить**~~ Включено в чек-лист (PIN /
  access_code_hash)
- ~~⬜ **Smoke-файлы положить в репозиторий**~~ ✅ в `41955372b`
- ✅ Палитра доведена до канона: заведена роль `--v4-sand-accent-bg` по образцу
  `--v4-sand-hero` — только в песочных палитрах, литерал `#f0dcc6` фолбэком;
  вложенные `var(--v4-accent-bg, …)` убраны. Рецепт записан в `BUGS_HISTORY.md`
- ~~🔵 Smoke флоу еды — теперь на проде, а не на локалке.~~ Прежняя
  формулировка: Push `679a69769` прошёл раньше проверки, флоу опубликован.
  Сценарий тот же: новый продукт → все пять исходов модерации → выход с
  черновиком → фото на экране граммов, песочная и sand-dark _— 12.08, сегодня_

  **Делать под отдельным тестовым клиентом, не под своим аккаунтом и не под
  аккаунтом жены.** Правило уже есть в 4.5 для входа, здесь оно нужнее:
  добавление еды пишет в живой дневник, и неудачный прогон оставит мусорные
  приёмы в вашем сегодняшнем дне.

  **Чем рискуем на самом деле — узко.** Задание по еде меняло оболочку,
  состояния и копирайт; логику поиска, ранжирования, overlay и RPC не трогало.
  Значит вероятный отказ — смешанная вёрстка и сломанное взаимодействие на
  полуперекрашенных экранах, а не порча данных. Смотреть прицельно там, где
  менялось поведение: пять исходов модерации вместо тостов и harm-шаг без Toast.

  **Путь отхода есть и он дешёвый.** Если ломается ежедневный путь — revert
  коммита еды и push: гейт зелёный, выкладка проходит за минуты. Это не тот
  откат, который мы отклоняли утром: там предлагалось откатить весь клиент к
  сборке 11.08 и потерять минимизацию, здесь — снять один коммит вперёд по
  истории _— 12.08_

- ✅ **Правило зафиксировано:** smoke → commit → push, когда push разблокирует
  деплой на ветке с несданной работой. Стоило одной публикации непроверенного
  флоу — дешевле, чем могло
- (хвост) Прежняя формулировка «после работы по входу» была неверной, порядок
  берётся из аудита флоу еды _— до 25.08_

## 11. Трек E · Внешние _— разборы получены, ждём скриншоты_

- ✅ Вопросы про письменную форму и трансграничную передачу закрыты разбором
- ✅ Юрист найден и задачу взял. Прислал входной пакет из восьми блоков
- ✅ Фактура собрана в `docs/release/`: `lawyer-README.md`,
  `lawyer-A-data-flow.md`, `lawyer-B-fields.md` (главный),
  `lawyer-D-consent-signing.md`, `lawyer-E-access-audit.md`,
  `lawyer-F-backups.md`. В B выделен блок свободного ввода, в E письменно
  зафиксировано отсутствие разграничения прав и журнала обращений
- ✅ Блок H готов: реквизиты собраны, адрес подтверждён как прежний, реестр не
  трогаем. Таблица подстановки заглушек в соглашение о ПЭП составлена
- ✅ Собран `lawyer-owner-handoff.md` — бриф владельцу на полчаса с двумя
  оставшимися блоками
- ✅ Package-2 сдан 11.08: блок C дополнен (ПЭП в реестре, экраны входа, анкеты
  и опциональных функций, оценка «ок/риск» по чекбоксам), блок G дополнен
  разделом расхождений после минимизации, блок B получил секцию «после
  минимизации», проверка по циклу закрыта
- ✅ **Блок G закрыт 11.08: карточка оператора из публичного реестра
  выгружена.** Регистрационный номер 26-22-005319, приказ № 188 от 20.09.2022,
  действующая редакция — приказ № 131 от 24.07.2026. Четыре цели: кадры и
  бухгалтерия; приём и обработка заявок; сопровождение питания; маркетинг. PDF
  исходной формы утрачен, но содержание записи восстановлено из первоисточника —
  этого достаточно для сверки с блоком B
- ✅ Шифровальные средства «не используется» и юридический адрес без улицы —
  расхождениями не являются. Но контактный адрес в политике, оферте и согласиях
  должен быть полным
- ✅ Скриншоты экранов согласий сняты и отправлены 12.08 — юрист разобрал
  шестнадцать снимков экрана «Согласия и условия» с прода, это был последний
  открытый пункт его запроса. Разбор и четыре найденных дефекта — в C5
- ✅ [11.08] Дата релиза назначена: 31 августа
