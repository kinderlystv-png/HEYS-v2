# HEYS · трек D · Продукт до первых клиентов

> Часть канона. Общий контекст, правила, календарь и решения — в
> `release-plan.md`. Закрытое — в `release-archive.md`. **Штамп: 13.08.2026
> 15:55 MSK.**

## 10. Трек D · Продукт до первых клиентов _— до 25.08_

- ⬜ Показать срок хранения анкеты отклонённого на самом экране _— до 25.08_
- ⬜ Проверить `purge_expired_trial_intakes()` на живых данных — ни разу не
  отрабатывала _— до 25.08_

- ⬜ **Хвосты по еде, в тот же трек:** `meal_step` (гард даты → время →
  самочувствие → способ), полный экран «Приём собран», `pe-step` v4, наборы,
  модалка штрихкода, browser smoke _— до 25.08_
- 🟡 **Частичный автопрогон на проде 12.08: главный регресс зелёный.** Диалог
  выхода по Escape работает, обе кнопки — «Остаться» сохраняет граммы, «Выйти»
  закрывает без записи. Плюс зелёные: версия `41955372`, вход, `pending`,
  harm-шаг экраном без тоста. На stable подтверждены вход без тупика на
  согласиях, баннер копии и `__HEYS_READONLY_MODE__`. То есть то, ради чего
  выкладывали фикс, работает
- ⬜ **Осталось руками, 10–15 минут:** offline, фото на экране граммов; на
  stable — открыть сам экран согласий и проверить плашку и «Продолжить».
  Автоматизация упёрлась в тайминги и desktop-оверлей, ручной прогон дешевле
  доводки скрипта _— 13.08_
- 🔴 **`exists` дефект подтверждён на проде 13.08 (smoke D10).** «Молокo 3.2» →
  pending «заявка ушла куратору», не «уже есть». То же что 12.08. **Разбор
  13.08:** `createPendingProduct` не unwrap'ит
  `{ create_pending_product_by_session: … }` и ставит
  `status: data?.status || 'pending'` — UI «заявка ушла» даже при
  `exists`/`error`; MCP+куратор: очередь пуста, SmokeTest только в личной базе.
  **Фикс 13.08:** unwrap RPC + fail-closed status в
  `heys_storage_supabase_v1.js` (и зеркало `heys_cloud_shared_v1.js`);
  `publishProductModerationOutcome` не маскирует пустой status. **После deploy —
  re-smoke D10/D11/D16** _— до 18.08_
- ⬜ **Поле телефона просит 10 цифр после +7, а не 11.** Ввод `78888888888`
  превращается в `77888888888` и вход не проходит. Ловушка того же класса, что
  мусорные номера у тестовых клиентов: поле молча принимает неверное и отдаёт
  ошибку про код. Проверить, что вставка полного номера из буфера обрабатывается
  корректно — люди копируют номер целиком _— до 18.08_
- 🟡 **Ручной smoke на проде — owner 13.08 в процессе** (тестовый клиент, не
  свой/жена). Результат — строка на пункт; закрывает heys/1ab6a1. Таблица ниже
  §Smoke 13.08.

  **До прогона (~2 мин)**
  - ⬜ Версия в UI = `app.heyslab.ru/version.json` (сейчас `41955372`). Не
    совпало → hard reload → unregister SW → reload. Пока не совпало — дальше
    бессмысленно
  - ⬜ Вход тестовым клиентом до любых проверок. PIN + ошибка про код доступа →
    у клиента `access_code_hash`, нужен свежий клиент

  **Прогон**
  - ⬜ **Диалог выхода (главное):** добавить продукт → граммы → Escape →
    «Остаться / Выйти», не молчаливое закрытие. «Остаться» — ввод на месте;
    «Выйти» — без записи
  - ⬜ **Модерация, естественно на проде:** `pending` (новый продукт); `exists`
    (имя как у существующего); `pending_dup` (повтор той же заявки); offline
    (devtools → offline → экран ошибки, ввод сохранён)
  - ⬜ **Модерация `invalid_session`:** за 10 мин не воспроизводится без хака LS
    — пропустить и записать «не проверен», не засчитывать по моку
  - ⬜ **Harm-шаг:** результат экраном/диалогом, не Toast
  - ⬜ **Фото на граммах:** кадр прикладывается и виден

  **Провал vs хвост:** провал — сломанный ежедневный путь (шаг не открывается,
  ввод теряется, приём не создаётся) → revert коммита еды + push. Смешанная
  вёрстка на неперекрашенных экранах — хвост, записать и идти дальше

  **После прогона**
  - ⬜ Отклонить 3–4 живые заявки модерации тестового клиента
  - ⬜ Убедиться, что приёмы остались у тестового клиента, не у вас/жены

  **Каким клиентом заходить — проверено по базе 12.08, не гадать.** У
  `Login Smoke Test` и `login-smoke-deploy` проставлен `access_code_hash`,
  значит legacy-путь по PIN для них закрыт политикой sunset, а фронт нового
  входа на прод не выкачен — ими не зайти вовсе. Кандидаты:
  `HEYS production smoke` (`9bc6f6c3…`, `7397a9db…`) и `Purge Warn Smoke Client`
  — у них `access_code_hash` пуст. **Телефоны из отчёта кодера (`7900163655556`,
  `7900165216038`) — 13 цифр, неверны.** До SQL-fix по heys/51cd22 вход
  невозможен. **Куда заходить:** `https://app.heyslab.ru/` (клиент), **не**
  `/?intake=1` (кандидат); при LS-hint `heys_candidate_cookie_session_hint` —
  инкогнито. Sunset: без проверки по базе это выглядело бы как «PIN сломался».

  **Прогон на старом бандле 12.08 — 3/4, падал ровно «выход с черновиком».**
  Прогон делался до выкладки фикса, на `679a6976`. Как доказательство дефекта он
  полезен: предсказание «третий пункт даст известный красный» подтвердилось
  эмпирически. Как приёмка не засчитывается — нужен чистый прогон на `41955372`.

  **Форма отчёта — строка на пункт.** Прогон засчитывается только в этом виде:
  галочка «всё ок» не отличает проверенное от пропущенного, а пропущенное надо
  видеть. `invalid_session` заранее допускается как «пропущен» — на проде за
  десять минут он не воспроизводится без правки localStorage, и честный пропуск
  лучше зачёта по моку.

  ```
  версия UI: …
  вход тест-клиент: …
  диалог Escape — Остаться: …
  диалог Escape — Выйти: …
  модерация pending: …
  модерация exists: …
  модерация pending_dup: …
  модерация offline: …
  invalid_session: пропущен / …
  harm (не toast): …
  фото на граммах: …
  провалы на пути: … / нет
  заявки модерации отклонены: …
  приёмы у тестового клиента: …
  ```

  **Что происходит с результатом.** Все пункты чисты — `heys/1ab6a1`
  закрывается, флоу еды снимается с 🔵 в этом треке. Есть провал на ежедневном
  пути — revert коммита еды и push, задача остаётся открытой. Хвосты по вёрстке
  на неперекрашенных экранах в решение не входят: они записываются строками сюда
  же и живут своим сроком.

- ⬜ **Автотесты идут с моком `YandexAPI.rpc`** — UI-ветки, не контракт; ручной
  прогон выше закрывает контракт _— 12.08_
- ⬜ **Наблюдение с этой же проверки, годное для всех живых.** `version.json`
  сам отдавался устаревшим: в 15:45 он ещё возвращал `679a6976`, хотя сборка
  `41955372` встала в 15:13. Значит между «CI зелёный», «объект в бакете» и
  «клиент увидел новое» стоят два слоя кэша — CDN и service worker, — и они врут
  в разное время. Единственный честный критерий готовности к живой проверке:
  версия, показанная **в приложении**. Не галочка в Actions и не `version.json`
  _— 12.08_
- ⬜ **Service worker / CDN.** `version.json` может отставать от UI;
  единственный честный критерий — версия **в приложении**. SW может отдавать
  старый бандл даже после смены хеша — см. чек-лист выше _— 12.08_

- 🟡 **Граница задачи «Сохранить только себе» изменилась.** В промпте она
  описана как макетная кнопка с текущим поведением, а в коде уже зовёт
  `upsertLocalProduct` через `retryLocalOnly` — то есть локальный fallback
  работает. Очереди повторной отправки это не даёт, полный офлайн-контур
  остаётся отдельной задачей. **Переписать задание до запуска:** иначе
  исполнитель начнёт делать то, что частично сделано _— до 25.08_
- ⬜ Запустить «Сохранить только себе» — **после переписывания задания под
  фактическое состояние** (лист «Исходы и отказ»), не параллельно: это один и
  тот же экран.
- ⬜ Запустить очередь модерации — после предыдущего _— до 25.08_

#### Smoke 13.08 · флоу еды на проде · owner «пупсы qwe»

**Клиент:** «пупсы qwe», `+76666666666`, код доступа **1212**, триал до 20.08.
Уже в приложении после A3-smoke (шаг 25 ✅). **Не** свой / не жена.

**Правило:** провал = ежедневный путь сломан (шаг не открывается, ввод теряется,
приём не создаётся) → revert + push. Смешанная v4-вёрстка на старых экранах =
хвост, записать и идти дальше.

##### Кусок 0 · Префлайт

| #   | Этап            | Что делать                                                    | Ожидание                                                                                       | Факт                                                     | Вердикт |
| --- | --------------- | ------------------------------------------------------------- | ---------------------------------------------------------------------------------------------- | -------------------------------------------------------- | ------- |
| D0  | **Версия в UI** | Hard reload; версия **в приложении** (не только version.json) | Свежий бандл после A3 (`cfc18c4f1` / `2026.08.13.1457…`); если старое → unregister SW → reload |                                                          | ⬜      |
| D1  | **Клиент**      | Вкладка «Питание», имя «пупсы»                                | Тот же тест-клиент, пустой или с приёмами только его                                           | «пупсы qwe», вкладка «Питание», до приёма пустой дневник | ✅      |

##### Кусок 1 · Базовый приём (happy path)

| #   | Этап                    | Что делать                     | Ожидание                             | Факт                                                                                                                                      | Вердикт            |
| --- | ----------------------- | ------------------------------ | ------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------- | ------------------ |
| D2  | **Открыть добавление**  | «+» / добавить еду             | Экран поиска/добавления открывается  | Сначала plate-guide «Это — важно» → «Создать приём» → поиск открылся                                                                      | 🟡 хвост + ✅ путь |
| D3  | **Поиск продукта**      | Любой из базы (напр. «молоко») | Карточка, граммы                     | Список общих продуктов; выбран **Молоко 3.2**                                                                                             | ✅                 |
| D4  | **Граммы и сохранение** | Ввести граммы → сохранить      | Приём в дневнике, ккал пересчитались | **100 г** → **56 ккал**; «✓ Добавить»; дневник **16:00 breakfast · Молоко 3.2 · 56**; остаток **1623** из **1679**; `[MA.saveDone] SAVED` | ✅                 |

**🔴 Дефект smoke 13.08 · v4 mobile:** в готовый приём **нельзя** добавить
продукт. `NutritionTabV4` рисует строки дневника без `onClick`; кнопки «⚡
Добавить 1 продукт» живут в `nutrition-v4-legacy-meals` с `display: none`
(`732-ui-v4-nutrition.css:404`). CTA и 🍽️ FAB зовут только `addMeal()` →
**новый** приём. План этапа 4 это знает, замены в v4 UI нет. Обход для smoke:
**ширина desktop** (legacy-карточки) или Escape-тест через **новый** приём
(D5–D8).

##### Кусок 2 · Диалог выхода (главный регресс 12.08)

| #   | Этап                | Что делать                                  | Ожидание                                       | Факт                                                                                        | Вердикт |
| --- | ------------------- | ------------------------------------------- | ---------------------------------------------- | ------------------------------------------------------------------------------------------- | ------- |
| D5  | **Черновик**        | Новый продукт → экран граммов, ввести число | Поле заполнено                                 | **Молоко 3.2**, граммы **100→140** (+/-), черновик не сохранён                              | ✅      |
| D6  | **Escape → диалог** | Escape                                      | **Не** молчаливое закрытие; «Остаться / Выйти» | «**Выйти и потерять выбор?**» · Молоко 3.2, **140 г** · «Остаться» / «Выйти без сохранения» | ✅      |
| D7  | **«Остаться»**      | Нажать «Остаться»                           | Граммы на месте, можно продолжить              | «Остаться» проверено owner: **граммы сохранились**                                          | ✅      |
| D8  | **«Выйти»**         | Снова Escape → «Выйти»                      | Закрыто **без** записи приёма                  | После выхода дневник **56 ккал** (только первое молоко 100 г), **140 г не записались**      | ✅      |

##### Кусок 3 · Модерация (естественно на проде)

| #   | Этап            | Что делать                                 | Ожидание                                                                     | Факт                                                                                                                                                                                                                                                | Вердикт |
| --- | --------------- | ------------------------------------------ | ---------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------- |
| D9  | **pending**     | Создать **новый** продукт (уникальное имя) | Заявка в модерацию, не молча в базу                                          | **SmokeTest** · «на модерации» при создании · **16:15 snack1 · 32 ккал** · съедено **88/1679** · `[MA.saveDone]` по цепочке · 🟡 штрихкод OBJECTOBJECT → **fix 13.08 ~20:00** (reject garbage barcode, sanitize pending RPC, draft без fallback)    | ✅/🟡   |
| D10 | **exists**      | Имя **как у существующего** в базе         | Экран «уже есть» / выбор существующего, **не** pending                       | re-smoke 13.08 ~18:13 prod e8ab4cb8c: «**Такой продукт уже есть в общей базе — заявка не нужна**» → Продолжить → граммы **Молоко 3.2**                                                                                                              | ✅      |
| D11 | **pending_dup** | Повторить **ту же** pending-заявку         | Pass1: pending «заявка ушла куратору»; pass2: «Такая заявка уже на проверке» | RPC ✅ (~19:15). **13.08 ~20:30 prod 1186baef:** pass1 «Продукт сохранён, заявка ушла куратору» · pass2 «Такая заявка уже на проверке» (Playwright `d11-smoke-pass.mjs`, qwe). Fix: garbage barcode OBJECTOBJECT + empty-filter guard (`1186baefe`) | ✅      |

##### Кусок 4 · Offline, harm, фото

| #   | Этап                | Что делать                         | Ожидание                                        | Факт                                                                                                                                                                                          | Вердикт |
| --- | ------------------- | ---------------------------------- | ----------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------- |
| D12 | **offline**         | DevTools → Offline на экране ввода | Экран ошибки; ввод **сохранён** после online    | Баннер **«Оффлайн режим — данные сохраняются локально»** · йогурт 100 г offline · после online **«Сеть восстановлена — синхронизация…»** · дневник **16:30 · 79 ккал** · съедено **167/1679** | ✅      |
| D13 | **harm**            | Продукт с harm-предупреждением     | **Экран/диалог**, не Toast                      | Полноэкранный шаг **«Вредность»** · **Молоко 3.2** · AI **1.0** / Расчёт **0.4** · формула v3.0, штрафы/бонусы, NOVA 1                                                                        | ✅      |
| D14 | **фото**            | Прикрепить фото на экране граммов  | Кадр виден в UI                                 | v4 mobile: экран 100 г / «✓ Добавить» — **кнопки 📷 нет** (миниатюра у названия = карточка продукта). Smoke ⏭; хвост UI / другой экран                                                       | ⏭      |
| D15 | **invalid_session** | —                                  | На проде за 10 мин без LS-hack **не проверяем** | пропущен                                                                                                                                                                                      | ⏭      |

##### Кусок 5 · Уборка

| #   | Этап          | Что делать                                     | Ожидание                       | Факт                                                                                                                                                        | Вердикт |
| --- | ------------- | ---------------------------------------------- | ------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------- | ------- |
| D16 | **Модерация** | Куратор: отклонить 3–4 тестовые заявки «пупса» | После pass1 pending в MCP list | ~19:15: **D11Pending1308rporap** reject · ~20:31: **D11Pending1308rsni40** reject (smoke pass1) · очередь пуста                                             | ✅      |
| D17 | **Изоляция**  | Дневник «пупса» vs ваш/жена                    | Приёмы только у тест-клиента   | Дневник **Антон Полтavский** — только свои (кофе, сырники); **нет** SmokeTest/молоко smoke/йогурт «пупса» · owner: «нет тестовых приёмов, всё изолированно» | ✅      |

**Итог smoke 13.08 (owner, «пупсы qwe»):** прогон **закрыт** по таблице D1–D17
кроме **D0 ⬜** (версия в UI не снимали).

| Блок                                | Статус                                                            |
| ----------------------------------- | ----------------------------------------------------------------- |
| Happy path + Escape (D1–D8)         | ✅ главный регресс 12.08 зелёный                                  |
| Модерация (D9–D11)                  | D9 ✅/🟡 OBJECTOBJECT fix в коде · D10 ✅ · **D11 ✅** (1186baef) |
| Offline/harm (D12–D13)              | ✅                                                                |
| D14 фото / D15 session / D16 заявки | D16 ✅ очередь пуста · D14 ⏭ · D15 ⏭                            |
| Изоляция (D17)                      | ✅                                                                |

**Провалы на ежедневном пути:** нет (revert не нужен). **Хвосты:** plate-guide
(D2 🟡), v4 add-to-meal 🔴, D14 📷.

**heys/1ab6a1:** daily path зелёный; D10 ✅; **D11 ✅** prod 1186baef; D16 ✅;
**D0** — опционально; D9 — fix OBJECTOBJECT в коде (smoke pass1 pending ok).

---
