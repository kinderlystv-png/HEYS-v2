# HEYS · трек B · Персональные данные и документы

> Часть канона. Общий контекст, правила, календарь и решения — в
> `release-plan.md`. Закрытое — в `release-archive.md`. **Штамп: 16.08.2026
> 13:15 MSK — бэкапы и опциональные функции 15.08. Предыдущий: 14.08.2026
> 18:35.**

## 8. Трек B · Персональные данные и документы

### B1 · Минимизация — ✅ выкачена и исполнена 12.08, остались хвосты _— до 20.08_

**Сверка статусов 12.08, поздний вечер.** Аудит показал, что треть строк здесь
стояла ⬜ при уже сделанной работе. Каждая закрытая ниже проверена по коду или
по коммиту, не по отчёту: `heys_health_features_v1.js`
(`CYCLE_TRACKING_IN_RELEASE = false`, `internalAccount`, отклонение `custom_*`),
`heys_trial_intake_v1.js` (`WARNING_TEXT_VERSION = '1.0'`), коммиты `a62717bfd`,
`2bbf90146`, `ce346d7e9`, акт об уничтожении.

- ✅ **Smoke анкеты на проде 13.08.** Кандидат «лала», PIN 3819: приглашение
  куратора → текст в Telegram → вход `?intake=1` → 5 шагов → предупреждение 1.0
  (без спецкатегории) → «Отправить куратору» → «Анкета отправлена». Скрины
  владельца. Панель куратора не смотрели — необязательный хвост.

- ✅ Решение: медицинские вопросы из анкеты убираются полностью
- ✅ Решение: цикл, замеры и добавки — опциональные функции с отдельным
  согласием
- ✅ **Исполнено 15.08** (`6c8e7ec5`): замеры и добавки вернулись в продукт с
  отдельными согласиями `body_measurements` 1.0 и `supplements_tracking` 1.0,
  выключены по умолчанию. Живой проверки не было — идёт с C0 29–30.08
- ⏸ **Изменение сведений в реестре — решено не подавать сейчас.** Владелец
  оставляет возможность вернуть сбор данных о здоровье и цикла к релизу. Заявить
  в реестре больше, чем обрабатываешь, нарушением не является — обязанность
  возникает при обратном. Значит по составу данных подавать нечего, и при
  возврате менять тоже не придётся. Подача — один раз, после окончательного
  решения о составе продукта, а не по календарю _— по факту решения_
- ⏸ Канвас «Цикл» уходит в архив вместе с функцией — расхождение обещаний с
  кодом становится неактуальным до возврата.
- ✅ **Решение владельца 11.08: трекинг цикла выключается до релиза совсем.** Не
  device-only и не паспортные реквизиты — функция снимается с витрины и из
  продукта до запуска, возвращается позже, когда будет время сделать device-only
  спокойно. Обоснование: до релиза восемнадцать дней, неделя из них на переделку
  функции, которой пользуются три собственных профиля, — плохой размен. Плюс
  продуктовый довод владельца: обязательный трекинг цикла может отталкивать
  часть аудитории, отсутствие функции на старте не потеря
- ⬜ Web UI глазами не проверялся — гейт подтверждён через MCP и по коду.
  Посмотреть при следующей живой проверке _— 29–30.08, вместе с C0_
- **К сведению, не задача.** UUID `4545ee50-4f5f-4fc0-b862-7ca45fa1bafc` остался
  в истории коммитов. Переписывать историю ради этого не надо, но знать стоит:
  это идентификатор, а не имя, и он ни с чем не связан вне вашей базы _— к
  сведению_
- ⬜ **К первому клиенту решить: оформить функции как надо или свернуть.** Флаг
  `internalAccount` при этом остаётся в любом случае — служебные и тестовые
  аккаунты нужны всегда, это не костыль на время.

  Что значит «оформить» для цикла: письменная форма согласия, паспортные
  реквизиты у тех, кто включает, повышенные требования к защите, изменение
  сведений в реестре. Замеры и добавки проще — замеры не специальная категория
  вовсе, добавки на закрытом справочнике тоже.

  Работа на несколько дней плюс юрист. Оценить объём заранее, а не в день
  появления клиента _— оценка до 20.08, решение к первому клиенту_

- ⬜ Сказать юристу одной строкой: в базе останутся данные о цикле одного члена
  семьи. Его подтверждение «спецкатегория ушла из продукта полностью» надо
  сверить с этим фактом _— 13.08_

- ⬜ Юристу: согласие на цикл не разбирать, функции в релизе не будет. Замеры и
  добавки остаются опциональными как решено _— ~~12.08~~ → 13.08, одним
  сообщением вместе с пунктом про цикл члена семьи_
- ⬜ Вернуть цикл после релиза, сразу в архитектуре device-only: даты на
  устройстве, на сервер только готовая цель дня. Смета L, 6–10 дней, лежит в
  `lawyer-cycle-device-check.md` _— после релиза_
- ✅ Решено: замеры и добавки отдельных канвасов не требуют — замеры остаются в
  базовом согласии, добавки закрываются справочником. Это решение, а не работа;
  строка стояла ⬜ по недосмотру
- 🟡 Выключение равно отзыву согласия: механика есть — при снятом флаге поля
  вычищаются из дня и профиля (`purgeCycleDataFrom*`, `purgeMeasurementsFrom*`,
  `purgeSupplementsFrom*`). **Не проверено предупреждение до отключения** — по
  решению оно должно показываться раньше удаления, а не после. Сверить на экране
  _— до 20.08_
- 🟡 Свободные текстовые поля: подход выбран юристом — оставить, запретить в
  правилах, удалять при обнаружении. Четыре условия, без которых это декларация:
  - ⬜ прямой запрет в правилах и в согласии: сервис не предназначен для
    сообщения сведений о заболеваниях, диагнозах и препаратах, такие сведения не
    запрашиваются и не хранятся _— до 20.08_
  - ⬜ письменная инструкция куратору — документ, а не строка плана: сведения о
    здоровье из разговора не переносятся в структурированные поля и в саммари _—
    до 20.08_
  - ⬜ процедура удаления при обнаружении, с журналом: что удалено, когда, кем.
    Без журнала процедуры не существует _— до 20.08_
  - ⬜ отсутствие структурированных полей под такие сведения — закрывается
    минимизацией _— до 20.08_ Процедура должна покрывать расшифровку голосовых и
    фото: на снимке может оказаться упаковка препарата

### B2 · Документы против факта — ⬜ _— до 27.08_

**Хвосты, всплывшие при работе 12.08 и не относящиеся к другим трекам:**

- ⬜ Строка про срок ответа куратора убрана из анкеты, потому что обещания нет
  ни в оферте, ни в договорённостях. Если срок разбора анкеты нужен — сначала
  решить его и внести в оферту, потом показывать на экране _— до 27.08_

**Telegram — трансграничная передача, опорный факт пакета оказался неверным.**
Второй разбор юриста 11.08: если клиент выбрал Telegram, сообщение идёт через
Telegram, и он же назван в таблице внешних получателей. Оговорка «канал по
выбору клиента» правового значения не имеет — бот развернули вы, отвечаете через
него вы. Решение из раздела «Отменено» принималось про зарубежные LLM и Telegram
не покрывает. Заодно делает недостоверной запись в реестре «трансграничная
передача не осуществляется».

- ✅ **Решение владельца 11.08, уточнённое:** Telegram убирается из вариантов
  канала связи с куратором — переписка, фото и голосовые целиком в мессенджере
  приложения. **Кандидатский флоу остаётся на Telegram как есть:** выдача PIN,
  ссылка на вход, доведение человека до анкеты. Он уже настроен и работает, а
  получить доступ иначе кандидату негде — приложения у него ещё нет.

Разница между двумя конструкциями существенная: в первой через зарубежную
инфраструктуру шло содержание — что человек ел, как спал, на что жалуется. Во
второй — код доступа и ссылка.

- ✅ **Решение владельца 12.08: в форме заявки на пробную неделю выбор удобного
  канала связи ОСТАЁТСЯ.**

- ⬜ **Переписка с куратором — только в мессенджере приложения.** Это касается
  профиля и всего, что после старта: содержание услуги идёт там, где дневник.
  Проверить, что в профиле не осталось выбора канала для переписки _— 13.08_
- ⬜ **Посмотреть, что бот сейчас отправляет кандидату.** Если только PIN и
  ссылка — вопрос к юристу простой. Если есть имя, детали заявки или что-то
  содержательное — сузить, тогда и ответ вероятнее в нашу пользу _— 13.08_

- ✅ **РЕШЕНО 12.08: подаём одно уведомление по ст. 12 на три канала.**
  Владелец. Ниже — порядок работ и границы решения. Прежняя развилка:
  Рекомендация юриста — подавать. Что это даёт: push можно наполнить
  содержанием, ручная отправка приглашений в WhatsApp и MAX перестаёт держаться
  на дисциплине куратора, три документа становятся достоверными правкой одной
  формулировки. Цена: десять рабочих дней рассмотрения, идут параллельно работе.

  **Уточнение юриста 12.08, вечер: рубеж — не дата релиза, а первое приглашение
  постороннему человеку.** Сегодня передачи чьих-либо персональных данных нет:
  собственный аккаунт оператора, аккаунт члена семьи и тестовые номера под
  определение не подпадают. Значит ретроспективного дефекта нет,
  приостанавливать кандидатский флоу и push не нужно, а уведомление подаётся о
  намерении — ровно как норма задумана. Квалификация при этом в силе: отправка
  кода через Telegram остаётся трансграничной передачей, и «передача не
  осуществляется» станет недостоверным — но с первого реального кандидата, а не
  сегодня.

  **К сведению на будущее: юрист за день дважды поправил себя** — сначала по
  таблице получателей, потом по «нарушение уже существует». Обе поправки в нашу
  пользу и сделаны им самим, но вывод простой: его ответы — такой же артефакт,
  как документы и код, и к ним применяется то же правило. Опираться на них
  можно, считать окончательными до перепроверки — нет.

  **Это не возврат отменённого решения**, хотя выглядит похоже. В разделе 14
  отменена легализация передачи _данных клиентов в зарубежную модель_ — там речь
  шла о содержании дневников и о необеспечимой обязанности по ч. 14 ст. 12.
  Здесь состав другой: номер, факт кандидатства и код доступа. На таком составе
  обязанность обеспечить уничтожение исполнима, и довод, который год назад топил
  идею, почти ничего не весит _— решение владельца, 13.08_

- 🔴 **WhatsApp принадлежит Meta и в России заблокирован — из состава
  уведомления и из выбора каналов убрать.** Проверено 12.08 по открытым
  источникам: с февраля 2026 Роскомнадзор снял домены WhatsApp из национальной
  системы доменных имён, доступ остался только через VPN. Значит канал не
  работает у клиента и не работает у куратора без VPN — обещать его в форме
  заявки нельзя независимо от 152-ФЗ. И отдельно: заявлять в уведомлении
  передачу сервису, который сам регулятор блокирует, — лишний повод к отказу по
  всему уведомлению. Заявляем только то, чем реально пользуемся _— 13.08_

- 🔴 **Вся воронка входа стоит на Telegram, а его ограничивают.** Первое
  касание, выдача кода, ссылка на анкету, drip-напоминания — всё через бота.
  Telegram работает, но под нарастающими ограничениями, и это риск не
  юридический, а операционный: если канал деградирует, вход кандидата в продукт
  исчезает целиком, а запасного пути нет. **Решение владельца 12.08: резерв —
  СМС.** В уведомление он не идёт: SMS.ru российский, трансграничной передачи
  нет, провайдер уже назван в `subprocessors.md` §4. Значит уведомление остаётся
  на двух получателях — Telegram и Google с Apple.

  Три вещи, без которых это не резерв, а намерение:

  **Уточнение 12.08 по вопросу владельца «почему куратор не может написать СМС
  сам».** Может, и на нынешнем масштабе этого достаточно: пятнадцать мест,
  приглашение — код и ссылка, отправляется руками за минуту. Мои три пункта про
  срочное восстановление функции были переусложнением: автоматика нужна не для
  того, чтобы отправить сообщение, а для того, что руками не делается.
  - ⬜ **Ручная отправка объявляется резервом прямо сейчас** — ничего
    восстанавливать не надо, канал доступен всегда. Условие одно: приглашение
    отправляется с рабочего номера, а не с личного, и в тексте прямо сказано,
    что ответы — в приложении. Иначе клиент ответит на СМС, переписка начнётся в
    телефоне куратора, и правило «всё в одном месте» тихо умрёт в первый же день
    _— 13.08_
  - ⬜ **Записать триггер переключения:** что считается «телеграм не работает» и
    кто это объявляет. Дёшево и делается один раз _— 13.08_
  - ⬜ **Функция `heys-api-sms` — к первому набору, не раньше.** Руками не
    делается автоматика: drip-напоминания и предупреждение за два дня до чистки
    незавершённых анкет. Пока кандидатов нет, доставлять нечего. Помнить, что
    функция удалена из прода и в инвентаре у неё `autoDeploy: false` — возврат
    осознанный, не в аварии _— к первому набору_

  Обоснование, чтобы не выглядело возвратом отменённого: в разделе 14 отменена
  **СМС как канал доставки кода для ПЭП** — там довод был «ключом подписи
  объявлен собственный код клиента, доставка законом не предписана». Здесь
  другое назначение: доставка приглашения кандидату, у которого приложения ещё
  нет. Прежний довод к этому не относится _— решено 12.08_

- 🔴 **Ответ юриста 12.08: факта подачи недостаточно, ждать надо окно.** Ни
  одного получателя нет в перечне государств с адекватной защитой (приказ РКН №
  128 от 05.08.2022): США там не было никогда, ОАЭ не включены — из
  ближневосточных в перечне только Катар, — Британских Виргинских островов тоже
  нет. Значит по ч. 11 ст. 12 передача не допускается до истечения срока
  рассмотрения, и режимы Telegram и push не разделяются: ждём одинаково по всем
  трём. **Подача открывает отсчёт, а не канал.** При подаче 13.08 без запроса
  сведений окно закрывается около 27.08; при запросе — плюс до трёх недель.
  Рубеж «первый настоящий кандидат» наступает позже, запас есть, но меньше, чем
  казалось. Точное лицо Telegram на 13.08: **Telegram Messenger Inc.** (БВО) по
  живой политике; FZ-LLC — член группы в п. 8.2. На режим не влияет: обе
  юрисдикции вне перечня

- ✅ **Решение владельца 12.08: поддерживаем Safari и Chrome.** В уведомлении по
  push заявляются два получателя — Google LLC и Apple Inc., оба США. Mozilla и
  Microsoft не заявляются

- _(Гейт согласия на клиентский push — на проде 13.08, см.
  `release-archive.md`.)_

- ✅ **Уведомление ст. 12 — подано 13.08.** Портал
  `pd.rkn.gov.ru/cross-border-transmission/form2/`, ЕСИА. Номер **100383874**,
  ключ **17974226**. Дата оценки в форме **13.08.2026**. Перед подачей: npa
  169785 на обсуждении, не зарегистрирован; действует приказ № 128. Ст. 22 не
  подавали. Окно рассмотрения без запроса сведений — около **27.08**. _— 13.08_

  **Что уже готово.** Контролёр по живой политике `telegram.org/privacy/ru` —
  **Telegram Messenger Inc.** (БВО); FZ-LLC в п. 8.2 — член группы, не
  контролёр. Выгрузки — `docs/release/transborder-dumps-2026-08-13/`. Оценка
  `ocenka-pravovogo-rezhima.md` и риск `prinyatie-riska-ch14-st12.md` подписаны
  **13.08.2026**. Две вычитки юриста 13.08 внесены. Первая: состав по
  получателям, без «текста сервисного уведомления», договор присоединения вместо
  «договорных отношений нет», акт БВО — «принят», без формулы «обязанность
  неисполнима». Вторая: одноразовый код и белый список хостов push в оценке — в
  будущем времени («внедряет до начала передачи»); правовое основание передачи;
  карточка Google Play как адрес издателя FZ-LLC, не контролёра; подзаконные
  акты ОАЭ к закону 45/2021 на дату оценки не изданы. **Решение владельца
  13.08:** обе возможности приглашения — по нику и по номеру через добавление
  контакта. Поэтому номер телефона **в составе Telegram** (прямые и последующие
  лица группы), не у Google и Apple. Третья вычитка юриста 13.08: назначение
  Telegram — Bot API **и** личная переписка куратора; «переписка с куратором»
  уточнена как переписка в приложении HEYS; хвост «текст приглашения может не
  содержать цифр» снят. Это не замена профильного юриста на портале. Состав push
  сверен 13.08 (одна подписка Apple, см. archive). Гейт согласия на клиентский
  push — на проде 13.08. Кандидатский PIN и гейт хостов есть в исходниках
  (`b8e00b87d`); клиентский вход всё ещё `verify_client_pin_v3`
  (`heys_auth_v1.js`). Подписанная оценка поэтому обязуется, а не отчитывается.

  **Как подать** (печать и подпись 13.08 закрыты; дату на листах не трогать).
  1. ~~Распечатать два файла~~ — сделано 13.08. Выгрузки PDF не печатать.
  2. ~~Расписаться вручную~~ — сделано 13.08.
  3. Дату на подписанных листах **не ставить заново** — 13.08.2026, совпадает с
     выгрузками. Исключение юриста 13.08: если до подачи зарегистрирован новый
     перечень (`regulation.gov.ru` npa 169785) — правится фраза в §2, и оценку
     датируют **заново**, не 13-м. Иначе ломается порядок «оценка раньше
     подачи». Без этой регистрации дату не трогать.
  4. ~~Подать уведомление **ст. 12** на `pd.rkn.gov.ru`~~ — **подано 13.08.**
     Номер **100383874**, ключ **17974226**. Отдельная форма, не правка записи в
     реестре. Дата оценки в форме **13.08.2026**. Страны: США, ОАЭ, Британские
     Виргинские острова. Состав — по таблице оценки (телефон у Telegram, у
     Google/Apple — подписка и зашифрованное содержимое).
  5. ~~Подача — **14.08.** Перед отправкой открыть перечень~~ — подано **13.08**
     вечером, раньше плана. npa 169785 на обсуждении, не принят; действует
     приказ № 128. Окно без запроса сведений — около **27.08**.
  6. **Ст. 22 13.08 и дальше не подавать.** Юрист 13.08: одним пакетом после
     минимизации на проде **и** фактической передачи. Четыре правки сразу,
     включая меры по ст. 18.1 и 19, которых в карточке заявлено больше, чем
     есть. Состав пакета — трек E.

  Поля формы сверены на портале 13.08 при подаче.

- ✅ **Текст для согласия 1.0 готов** — строка «передача не осуществляется»
  удалена в `docs/release/soglasie-personal-data-v1.0.md` (раздел 5, редакция
  13.08). Таблица получателей с составом **по каждому**; переписка в приложении
  HEYS за границу не идёт; через Telegram — только приглашение и статус заявки;
  телефон только у Telegram. Абзац про уведомление вставлен (номер 100383874).
  Раздел обработчиков дополнен теми же получателями. **14.08 документ не
  проект:** в реестре `personal_data` 1.0 active (SELECT). Фронт приложения эту
  версию на проде показывает с `38834138` (Deploy 31814737179). Окно
  трансграницы ~27.08 и развилка отказа — отдельные, не снятие регистрации _—
  пакет 1.11, сверка 14.08_

- ⬜ **Развилка отказа от блока трансграничной передачи — аудит кода (13.08
  вечер).** Проверено без deploy. **14.08 тексты 1.11 в реестре и git; UI
  развилки на прод не выехал.** До выкладки фронта остаётся:
  - отдельного `consent_type` / чекбокса для §5 согласия 1.0 **нет** — блок
    только в проекте `soglasie-personal-data-v1.0.md`, не в UI/БД;
  - Telegram drip / trial invite (`heys-bot-client`, `cron-trial-notifications`,
    `admin_invite_trial_intake`, `admin_mark_trial_candidate_invite_sent`) — без
    проверки согласия на трансграничную передачу;
  - push: серверный гейт есть (`heys-api-push/push-consent.js` →
    `clientHasLivePushConsent`), но онбординг при галочке «уведомления» вызывал
    только `push.subscribe`, без записи `push_notifications` в БД — **исправлено
    13.08** (`heys_consents_v1.js` → `setPushConsent(true)` перед subscribe);
  - `docs/legal/subprocessors.md` и живой `privacy-policy.md` — не
    синхронизированы с оценкой 13.08 (пакет 1.11). До ~27.08 передачу не
    расширяем; полная развилка (отдельный чекбокс + гейт Telegram) — вместе с UI
    согласий 1.11. **Решение владельца 13.08 (heys/cc2ed4):** галочку, гейт
    Telegram и тексты сейчас не чинить — ждать пакета 1.11. Push-фикс онбординга
    остаётся локальным до коммита по команде. _— 13.08_

- ⬜ **После подачи.** Держать сведения готовыми к запросу: регулятор вправе
  истребовать их в течение десяти рабочих дней. Перечень приказа № 128 на дату
  оценки ещё действует (265-ФЗ сменил критерии, новый приказ не принят) _—
  13.08_

- ⬜ **Пока окно не истекло — передача не начинается.** Практически это значит:
  до закрытия окна ничего не меняем, кандидатский флоу работает как сейчас, push
  остаются обезличенными. Наполнять push содержанием и расширять переписку —
  только после _— до 27.08_

- ⬜ **Хвост по ст. 19, клиентский вход.** Кандидатский код — одноразовый на
  проде с 13.08 (см. `release-archive.md`). Фронт новой схемы на проде с 13.08
  (A3 owner smoke ✅). **14.08 18:29** живой бандл `38834138`: пакет 1.11 на
  экране. Переподпись пяти своих ещё впереди _— сверка 14.08_

- ⬜ Запись в реестре «трансграничная передача не осуществляется» проверить
  после ответа юриста: от него зависит, надо ли её менять _— до 20.08_
- ⬜ Существующая переписка через Telegram, если она есть, — в инвентарь и в
  общий purge _— до 20.08_
- ~~⬜ **Лендинг: `FaqD.tsx:76` обещает Telegram, WhatsApp и MAX как место
  переписки.**~~ ✅ **14.08 в C3** (`FaqD.tsx`, localhost:3003/#faq). Ниже —
  исходный разбор, не открытая задача. **Решение владельца 12.08: переписать,
  спозиционировав как преимущество, а не как ограничение.** Вся переписка в
  одном месте вместе с дневником — куратор видит ваш день и вашу неделю там же,
  где отвечает, ничего не теряется между приложением и мессенджером, история
  разбора лежит рядом с историей еды. Это сильнее исходного списка мессенджеров:
  список обещал удобство, а единое место даёт качество работы. Формулировать от
  выгоды клиента, без слов «нельзя», «только» и «в целях безопасности» _— 13.08_

  **Уточнение 12.08, после вопроса владельца «почему нельзя просто удобный канал
  связи».** Возражение справедливо по сути, но к этому тексту не относится, и
  различать надо три вещи.

  _Первое — что там написано на самом деле._ Не «как с нами связаться», а
  «Куратор на связи ежедневно, 09:00–21:00, ориентир ответа и внесения данных в
  дневник — 1–2 часа». Это обещание вести через мессенджер саму услугу:
  содержание питания, самочувствия, фото. Дверной звонок — это «напишите, чтобы
  начать», и именно поэтому кандидатский флоу на Telegram оставлен: там идут код
  и ссылка, без содержания.

  _Второе — двух каналов из трёх не существует._ Проверено по коду: в схеме
  `messenger IN ('telegram','whatsapp','max')` — это поле предпочтения;
  интеграция есть только у Telegram (бот, `telegram_chat_id`, drip). `whatsapp`
  и `max` — просто подписи в уведомлении куратору. То есть витрина обещает два
  канала, которых в продукте нет; это дефект обещания сам по себе, до всякого
  152-ФЗ.

  _Третье — MAX юридически не равен двум другим._ Правило юриста звучит как
  «через инфраструктуру за пределами РФ персональные данные не проходят ни в
  одну сторону». MAX российский, под это правило не подпадает и запись в реестре
  про трансграничную передачу не ломает. Если удобный внешний канал для
  содержания принципиально нужен — рабочий кандидат один, и это MAX: его надо
  раскрыть как получателя и внедрить, а не просто оставить подпись в списке _—
  решение владельца_

- ⬜ Оферта и политика: описание канала связи привести к факту, в пакете версии
  1.11 _— до 27.08_

**Доставка вместо Telegram: push плюс приложение на домашнем экране.**
Функционально мессенджер приложения не хуже — переписка, фото, голосовые.
Разница была в том, что телеграм и так открыт; закрывается push-уведомлением
«куратор ответил» со ссылкой прямо в диалог. Уведомления уже работают и
обезличены.

- ⬜ **Проверить активацию по всей воронке, а не только у клиента.** Кандидат
  проходит анкету до того, как у него появится приложение: он получает PIN,
  входит по ссылке в браузере, заполняет шесть шагов, ждёт решения куратора.
  Уведомления в этот момент недоступны — а именно тут человек ждёт ответа
  сильнее всего. Понять, чем его уведомлять о решении по заявке и о просьбе
  уточнить ответ _— до 18.08_
- ⬜ Проверить, на каком шаге предлагается добавить на домашний экран и не
  приходится ли это на момент, когда человек ещё не понимает, зачем ему
  приложение _— до 18.08_
- ⬜ iOS: push в PWA работает только после добавления на домашний экран.
  Убедиться, что без этого шага клиент не остаётся без уведомлений молча _— до
  18.08_
- ⬜ Оферта и политика: описание канала связи привести к факту, в пакете версии
  1.11 _— до 27.08_
- ⬜ Telegram-бот остаётся в перечне обработчиков, но с изменённым назначением:
  доставка уведомлений, не переписка _— до 27.08_

- ⬜ Срок действия в текстах согласий. Самая дешёвая правка, одна строка _— до
  27.08_
- ⬜ Активные согласия от 09.08 имеют `ip ::1` и `user_agent node` — созданы
  скриптом. Для своих аккаунтов нормально, на боевом контуре такая запись
  доказательством волеизъявления не является. Поставить проверку на запись _— до
  20.08_
- ⬜ **Возраст.** Юрист: корректнее ограничить, а не собирать согласие
  представителя — проверка полномочий и обработка данных ещё одного субъекта
  несоразмерны масштабу. Нужно: возрастной ценз в оферте прямой строкой (идёт в
  пакет 1.11); отказ при несоответствии на входе, а не просто запись года
  рождения; заявление клиента о достижении возраста в том же акте, где
  принимается оферта; процедура на случай выявления после начала обслуживания —
  прекращение обработки и удаление _— до 27.08_
- ⬜ **Платёжный провайдер.** Юрист: в большинстве случаев он не обработчик по
  поручению, а самостоятельный оператор — обрабатывает данные плательщика в силу
  закона о национальной платёжной системе, а не по вашему указанию. Раскрывается
  как получатель с указанием цели передачи. Проверить три точки: назван ли
  поимённо в политике; указаны ли цель и состав передаваемых данных; согласуется
  ли статус в `subprocessors.md` с договором _— до 20.08_
- ⬜ Шифрование health-полей: заявлено, покрывает 74 строки из 451. Делать после
  минимизации _— до 27.08_
- ⬜ Журналирование доступа: заявлено, отсутствует _— до 27.08_
- ⬜ Retention бэкапов: 365 дней против обещанных 14, плюс противоречие 7 против
  14 внутри документов _— до 27.08_. **15.08 сдвинулось:** документы переписаны
  честно по двум слоям (`31c91467`, `4b0841e2`, `52b007c8`) —
  `backup-retention.md`, `lawyer-F-backups.md`, retention-политика,
  `BACKUP_AND_REPORTS.md`; включено удаление orphan-фото, проверки
  восстановления ужесточены. Открытым остаётся сам правовой разрыв: слой
  суточных снимков живёт 365 дней, а срок уничтожения по требованию субъекта —
  30
- ✅ **Отзыв согласия: purge по категориям — закрыто 13.08.**
  `prompt-revoke-deletion` выполнен: `purge_personal_data_for_client` + UI
  (список + PrivacySettingsCard); onboarding health summary выровнен 13.08.
  Коммит `3f33938ec`, prod migration `2026-08-13_revoke_personal_data_purge_v1`
  (ledger 29/29), deploy `31692431580`, smoke confirm health/personal на проде.
  Онбординг: порядок profile-шагов на resume — `a97c58cb8`, prod deploy
  `31697815190` (build `2026.08.13.1457.cfc18c4f`). `health_data` —
  `is_health_key` + intake; `personal_data` — дневник, переписка, фото
  (очередь), снапшоты, audit scrub. **Остаток B2:** бэкапы/retention, DSAR,
  журнал доступа, версии документов — ниже.

- ⬜ Решить, нужен ли `purge_health_data` вообще, раз он не вызывается и прав
  нет _— до 27.08_
- ⬜ DSAR-экспорты через API не регистрируются _— до 27.08_
- ⬜ Версии документов в трёх местах отстают от действующих _— до 27.08_
- ⬜ Публичный доступ к фото нигде в согласиях не описан — снимается вместе с A1
  _— до 27.08_
- ⬜ Бэкапы, конструкция подтверждена юристом: удаление из продуктивной среды не
  более 30 дней с даты отзыва; **реестр удалённых субъектов, применяемый при
  любом восстановлении** — ядро конструкции, без него остальное косметика; срок
  ротации 35 дней прямо в политике; отсутствие доступа к копиям вне процедуры
  восстановления; шифрование копий (незашифрованные при утечке — отягчающее,
  штрафы оборотные); устранить внутреннее противоречие «7 против 14» _— до
  27.08_

### B3 · При найме кураторов — ⬜ рубеж не первый клиент, а первый наёмный куратор _— после релиза_

- ⬜ Перечень допущенных лиц с указанием данных и целей доступа _— после релиза_
- ⬜ Обязательство о неразглашении _— после релиза_
- ⬜ Разграничение прав в системе и журналирование доступа, см. A4 _— после
  релиза_
- ⬜ Для внештатных — договор поручения обработки и раскрытие в согласии _—
  после релиза_

---
