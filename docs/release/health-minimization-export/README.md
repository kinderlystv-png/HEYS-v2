# Pre-migration health answers export

Снимок ответов анкеты **до** применения
`2026-08-11_health_minimization_intake_v1.sql`.

| Файл                                    | Содержание                                        |
| --------------------------------------- | ------------------------------------------------- |
| `pre-migration-answers-2026-08-12.json` | 4 intake + 5 candidates, расшифрованные `answers` |
| `inventory-summary.md`                  | счётчики и UUID без текста ответов                |

Назначение: приложение к акту об уничтожении — видно, какие блоки `health` /
`safety` были сняты при переписи schema 1.2. Не для отката.

Расшифровка: `set_config('heys.encryption_key', …, true)` в той же транзакции,
что и `decrypt_health_data` (иначе функция молча отдаёт null).

Содержит спецкатегорию. Не публиковать; в git — только по явной команде.
