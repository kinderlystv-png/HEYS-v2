# 🏗️ HEYS Module Architecture Guidelines

> Правила архитектуры модулей — чтобы не делать рефакторинг потом **v1.0.0** |
> 2026-01-13

---

## ⚠️ КРИТИЧЕСКИЕ ЛИМИТЫ МОДУЛЯ

Эти лимиты **ОБЯЗАТЕЛЬНЫ** при создании или расширении модулей:

| Метрика                 | Лимит    | Критический | Действие при превышении   |
| ----------------------- | -------- | ----------- | ------------------------- |
| **LOC (строки кода)**   | ≤2,000   | >3,000      | Разбить на под-модули     |
| **Функции/компоненты**  | ≤80      | >100        | Выделить отдельный модуль |
| **HEYS.\* зависимости** | ≤50 refs | >80         | Уменьшить связность       |
| **Ответственности**     | 1 домен  | >2          | Разделить по доменам      |

### 🔴 Текущие "God Objects" (НЕ ПОВТОРЯТЬ!)

| Файл              | LOC    | Функции | Проблема                                 |
| ----------------- | ------ | ------- | ---------------------------------------- |
| `heys_day_v12.js` | 23,658 | 1,018   | Смешивает UI + бизнес-логику + утилиты   |
| `heys_app_v12.js` | 8,407  | 429     | Смешивает PWA + routing + auth + network |

### ✅ Хорошие примеры (КОПИРОВАТЬ!)

| Файл                     | LOC      | Функции | Почему хорошо                               |
| ------------------------ | -------- | ------- | ------------------------------------------- |
| `heys_steps_v1.js`       | 3,179    | 142     | Один домен, низкая связность (24 HEYS refs) |
| `heys_advice_v1.js`      | 7,368    | 258     | Изолированный домен, чистые функции         |
| `heys_insulin_wave_*.js` | 8 файлов | —       | Правильно разбит по ответственностям        |

---

## 📐 Принципы модульной архитектуры

### 1. Single Responsibility (Единственная ответственность)

```
❌ ПЛОХО: heys_day_v12.js содержит:
   - UI компоненты (DayTab, MealCard, WaterTracker...)
   - Бизнес-логику (calculateNorms, validateMeal...)
   - Утилиты (formatTime, parseDate...)
   - Звуки (playSound, sounds config...)
   - Фото (PhotoCapture, uploadPhoto...)
   - Хранение (saveDayData, loadDayData...)

✅ ПРАВИЛЬНО: Разбить на:
   - heys_day_ui_v1.js          # UI компоненты
   - heys_day_business_v1.js    # Бизнес-логика
   - heys_day_utils_v1.js       # Утилиты домена
   - heys_sounds_v1.js          # Звуковые эффекты
   - heys_photo_v1.js           # Работа с фото
```

### 2. Low Coupling (Низкая связность)

```javascript
// ❌ ПЛОХО: Прямые зависимости между модулями
function calculateNorms() {
  const profile = HEYS.user.getProfile(); // Жёсткая зависимость
  const settings = HEYS.app.getSettings(); // Жёсткая зависимость
  const products = HEYS.products.getAll(); // Жёсткая зависимость
  // ...
}

// ✅ ПРАВИЛЬНО: Dependency Injection
function calculateNorms({ profile, settings, products }) {
  // Получает зависимости как параметры
}

// Или через конфигурацию при инициализации модуля
HEYS.day.init({
  getProfile: () => HEYS.user.getProfile(),
  getSettings: () => HEYS.app.getSettings(),
});
```

### 3. No Fallback Functions (Нет заглушек)

```javascript
// ❌ ПЛОХО: warnMissing паттерн
const getProfile = HEYS.user?.getProfile || warnMissing('user.getProfile');

// ✅ ПРАВИЛЬНО: Явная инициализация с проверкой
function init() {
  if (!HEYS.user?.getProfile) {
    throw new Error(
      '[heys_day] Required dependency HEYS.user.getProfile is missing',
    );
  }
}
```

### 4. Чистые функции где возможно

```javascript
// ❌ ПЛОХО: Функция с side effects
function getMealTotal() {
  const meals = HEYS.day.getMeals(); // Читает глобальное состояние
  updateLastCalculation(); // Side effect
  logAnalytics('meal_calculated'); // Side effect
  return meals.reduce((sum, m) => sum + m.kcal, 0);
}

// ✅ ПРАВИЛЬНО: Чистая функция
function calculateMealTotal(meals) {
  return meals.reduce((sum, m) => sum + m.kcal, 0);
}
```

---

## 🗂️ Структура нового модуля

### Шаблон файла модуля

```javascript
/**
 * heys_example_v1.js — [Краткое описание домена]
 *
 * Домен: [example]
 * Ответственности:
 *   - [Ответственность 1]
 *   - [Ответственность 2]
 *
 * Зависимости:
 *   - HEYS.utils (утилиты)
 *   - HEYS.store (хранение)
 *
 * Экспортирует: HEYS.example
 */

(function () {
  'use strict';

  // ═══════════════════════════════════════════════════════════════
  // CONSTANTS
  // ═══════════════════════════════════════════════════════════════

  const MODULE_NAME = 'example';
  const VERSION = '1.0.0';

  // ═══════════════════════════════════════════════════════════════
  // PRIVATE STATE (минимум!)
  // ═══════════════════════════════════════════════════════════════

  let _initialized = false;
  let _config = {};

  // ═══════════════════════════════════════════════════════════════
  // PRIVATE FUNCTIONS (не экспортируются)
  // ═══════════════════════════════════════════════════════════════

  function _validateConfig(config) {
    // ...
  }

  // ═══════════════════════════════════════════════════════════════
  // PUBLIC API
  // ═══════════════════════════════════════════════════════════════

  function init(config = {}) {
    if (_initialized) return;

    // Проверка обязательных зависимостей
    if (!window.HEYS?.utils) {
      throw new Error(
        `[${MODULE_NAME}] Missing required dependency: HEYS.utils`,
      );
    }

    _config = config;
    _initialized = true;
    console.log(`[${MODULE_NAME}] v${VERSION} initialized`);
  }

  function calculate(data) {
    // Чистая функция — не зависит от глобального состояния
    return data.map((item) => item.value * 2);
  }

  // ═══════════════════════════════════════════════════════════════
  // EXPORT
  // ═══════════════════════════════════════════════════════════════

  window.HEYS = window.HEYS || {};
  window.HEYS[MODULE_NAME] = {
    init,
    calculate,
    VERSION,
  };
})();
```

---

## 🚦 Чек-лист перед созданием/расширением модуля

### Перед началом работы:

- [ ] **Проверить LOC** — если модуль >2000 строк, сначала обсудить разбиение
- [ ] **Определить домен** — модуль должен отвечать за ОДИН домен
- [ ] **Список зависимостей** — записать все HEYS.\* которые нужны
- [ ] **Проверить дублирование** — нет ли похожей логики в других модулях?

### При написании кода:

- [ ] **Dependency Injection** — передавать зависимости параметрами, не читать
      напрямую
- [ ] **Чистые функции** — где возможно, без side effects
- [ ] **Типизация в JSDoc** — @param, @returns для всех публичных функций
- [ ] **Нет warnMissing** — явные проверки в init()

### Перед коммитом:

- [ ] **LOC ≤2000** — `wc -l heys_new_module.js`
- [ ] **Функции ≤80** — `grep -c "function\|=>" heys_new_module.js`
- [ ] **HEYS refs ≤50** — `grep -o "HEYS\." heys_new_module.js | wc -l`
- [ ] **Один домен** — можно объяснить модуль одним предложением?

---

## 🔄 Как разбить большой модуль

### Стратегия разбиения (пример heys_day_v12.js → 5 модулей):

```
heys_day_v12.js (23,658 LOC)
    │
    ├── heys_day_ui_v1.js (~4,000 LOC)
    │   └── DayTab, MealCard, WaterTracker, StatsCard
    │
    ├── heys_day_business_v1.js (~3,000 LOC)
    │   └── calculateNorms, validateMeal, getDayTotals
    │
    ├── heys_meals_v1.js (~2,500 LOC)
    │   └── MealItem, ProductSearch, AddMealFlow
    │
    ├── heys_water_v1.js (~1,500 LOC)
    │   └── WaterTracker logic, goals, reminders
    │
    └── heys_day_hooks_v1.js (~2,000 LOC)
        └── useDayData, useMeals, useWater (React hooks)
```

### Порядок разбиения:

1. **Выделить чистые функции** — утилиты без зависимостей
2. **Выделить UI** — React компоненты
3. **Выделить бизнес-логику** — расчёты, валидации
4. **Выделить под-домены** — вода, приёмы пищи, тренировки
5. **Оставить координатор** — минимальный "главный" модуль

---

## 📏 Метрики для мониторинга

### Команды для проверки:

```bash
# LOC для файла
wc -l apps/web/heys_*.js | sort -n | tail -15

# Количество функций
grep -c "function\|=>" apps/web/heys_example_v1.js

# HEYS зависимости
grep -o "HEYS\.[a-zA-Z]*" apps/web/heys_example_v1.js | sort | uniq -c | sort -rn

# Все модули с метриками
for f in apps/web/heys_*.js; do
  loc=$(wc -l < "$f")
  funcs=$(grep -c "function\|=>" "$f" 2>/dev/null || echo 0)
  refs=$(grep -o "HEYS\." "$f" 2>/dev/null | wc -l | tr -d ' ')
  echo "$f|$loc|$funcs|$refs"
done | column -t -s'|'
```

### Целевые показатели проекта:

| Метрика       | Текущее | Цель   | Как достичь             |
| ------------- | ------- | ------ | ----------------------- |
| Avg LOC       | ~3,500  | ≤2,000 | Разбить top-3 монолита  |
| Max LOC       | 23,658  | ≤3,000 | Разбить heys_day_v12.js |
| Max functions | 1,018   | ≤100   | Выделить под-модули     |
| Avg HEYS refs | ~80     | ≤50    | Dependency Injection    |

---

## 🔗 Связанные документы

- [ARCHITECTURE.md](./ARCHITECTURE.md) — общая архитектура проекта
- [CODE_STYLE.md](./CODE_STYLE.md) — стиль кода
- [COMMON_ERRORS.md](./COMMON_ERRORS.md) — частые ошибки

---

## Changelog

| Версия | Дата       | Изменения                                 |
| ------ | ---------- | ----------------------------------------- |
| 1.0.0  | 2026-01-13 | Начальная версия на основе аудита модулей |
