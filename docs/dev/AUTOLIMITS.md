# 🧭 Autolimits for Legacy Modules

> Автолимиты для legacy-модулей `heys_*.js` — чтобы они не разрастались и не
> требовали неожиданных рефакторингов.

---

## Что это

Для файлов `apps/web/**/heys_*.js` действуют **индивидуальные лимиты**, рассчитанные
от текущих метрик с буфером $+10\%$. Превышение **блокирует коммит**.

Лимиты покрывают:

- LOC (строки кода)
- Количество функций
- Количество `HEYS.*` зависимостей
- Запрещённые паттерны (`warnMissing`)

---

## Где хранится

- Конфиг лимитов: `config/module-limits.json`
- Генератор лимитов: `scripts/generate-module-limits.js`
- Проверка лимитов: `scripts/check-module-architecture.sh`

---

## Как обновлять лимиты

**Раз в спринт**, после стабилизации фич:

```bash
pnpm arch:limits:generate
```

Это пересчитает лимиты по текущим метрикам и обновит `config/module-limits.json`.

Проверить метрики всех модулей:

```bash
pnpm arch:metrics
```

---

## Правила поведения при нарушении

Если лимит превышен:

1. **Исправить сразу** (рефакторинг/вынос функций),
2. Либо **задать вопрос** о допустимом решении/допуске к росту.

Без этого коммит недопустим.

---

## Частые вопросы

**Q: Почему лимиты индивидуальные?**  
A: Legacy‑код исторически разный по размеру, индивидуальные лимиты с буфером
$+10\%$ предотвращают дальнейший рост без внезапных блокировок.

**Q: Можно ли увеличить лимит?**  
A: Только через обновление лимитов и с явным решением о допустимом росте.

**Q: Как лимиты используются в IDE?**  
A: ESLint‑правило `@heys/module-architecture` читает `config/module-limits.json`
и подсвечивает нарушения сразу при работе.

---

## Связанные документы

- `docs/dev/CODE_STYLE.md`
- `docs/dev/MODULE_ARCHITECTURE.md`
- `docs/dev/ARCHITECTURE.md`
