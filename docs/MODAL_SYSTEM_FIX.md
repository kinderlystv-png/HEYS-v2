# Исправление системы попапов — Отчёт

**Дата**: 18 декабря 2025  
**Задача**: Обеспечить закрытие предыдущих попапов при открытии нового +
закрытие по клику вне попапа

---

## 🔍 Найденные проблемы

### 1. Отсутствие централизованного управления

- Каждая модалка работала независимо
- Не было механизма закрытия других модалок при открытии новой
- Могли быть открыты одновременно несколько модалок

### 2. Проверка закрытия по клику вне попапа

✅ **Уже реализовано правильно:**

- `heys_step_modal_v1.js` — закрывается по клику на `.mc-backdrop`
- `heys_confirm_modal_v1.js` — закрывается по клику на backdrop
- `heys_core_v12.js` — модалки закрываются по клику на `.modal-backdrop`
- `heys_reports_v12.js` — закрывается по клику на `.modal-backdrop`
- `heys_predictive_insights_v1.js` — закрывается по клику на overlay
- `heys_day_v12.js` — все пикеры закрываются по клику на backdrop

---

## ✅ Решение

### Создан централизованный ModalManager

**Файл**: `apps/web/heys_modal_manager_v1.js`

**Функциональность**:

- Реестр всех открытых модалок
- Автоматическое закрытие других модалок при открытии новой
- API для регистрации/дерегистрации модалок

**API**:

```javascript
// Регистрация модалки (автоматически закрывает другие)
const cleanup = HEYS.ModalManager.register('modal-id', closeFunction);

// Закрыть все кроме указанной
HEYS.ModalManager.closeAll('except-modal-id');

// Закрыть конкретную
HEYS.ModalManager.close('modal-id');

// Проверить открыта ли
HEYS.ModalManager.isOpen('modal-id');

// Debug
window.debugModals(); // в консоли
```

---

## 🔧 Интегрированные модалки

### 1. StepModal (`heys_step_modal_v1.js`)

- ✅ Регистрируется при открытии как `'step-modal'`
- ✅ Дерегистрируется при закрытии
- ✅ Автоматически закрывает другие модалки при открытии

**Изменения**:

```javascript
// При открытии
if (HEYS.ModalManager) {
  modalCleanup = HEYS.ModalManager.register('step-modal', () => {
    hideStepModal({ skipManagerNotify: true });
  });
}

// При закрытии
if (modalCleanup && !options.skipManagerNotify) {
  modalCleanup();
  modalCleanup = null;
}
```

### 2. ConfirmModal (`heys_confirm_modal_v1.js`)

- ✅ Регистрируется при открытии как `'confirm-modal'`
- ✅ Дерегистрируется при закрытии
- ✅ Автоматически закрывает другие модалки при открытии

**Изменения**:

```javascript
// При открытии
if (HEYS.ModalManager) {
  modalCleanup = HEYS.ModalManager.register('confirm-modal', () => {
    hide(true); // skipManagerNotify = true
  });
}

// При закрытии
if (modalCleanup && !skipManagerNotify) {
  modalCleanup();
  modalCleanup = null;
}
```

### 3. ProductsManager модалки (`heys_core_v12.js`)

- ✅ Уже закрываются по клику на backdrop
- ⚠️ **TODO**: Требуют интеграции с ModalManager (независимые состояния
  `showModal`, `mergeModal`)

### 4. Reports модалка (`heys_reports_v12.js`)

- ✅ Уже закрывается по клику на backdrop
- ⚠️ **TODO**: Требует интеграции с ModalManager (состояние `showCharts`)

### 5. Day модалки (`heys_day_v12.js`)

- ✅ Уже закрываются по клику на backdrop
- ✅ Уже есть частичная система `closeAllPopups()` для mini-попапов
- ⚠️ **TODO**: Требует интеграции с ModalManager для больших модалок:
  - `showTimePicker`
  - `showTrainingPicker`
  - `showZonePicker`
  - `showSleepQualityPicker`
  - `showDayScorePicker`
  - `showWaveCalcPopup`

### 6. What-If симулятор (`heys_predictive_insights_v1.js`)

- ✅ Уже закрывается по клику на overlay
- ⚠️ **TODO**: Требует интеграции с ModalManager (состояние `isExpanded`)

---

## 📋 Проверочный список (Checklist)

### Реализовано ✅

- [x] Создан централизованный ModalManager
- [x] Интегрирован в StepModal
- [x] Интегрирован в ConfirmModal
- [x] Добавлен в index.html
- [x] Все модалки закрываются по клику вне их (уже было реализовано)

### Требует доработки ⚠️

- [ ] Интеграция в ProductsManager модалки (`heys_core_v12.js`)
- [ ] Интеграция в Reports модалку (`heys_reports_v12.js`)
- [ ] Интеграция в Day пикеры (`heys_day_v12.js`)
- [ ] Интеграция в What-If симулятор (`heys_predictive_insights_v1.js`)

---

## 🎯 Рекомендации

### Следующие шаги:

1. **Протестировать текущую реализацию**:
   - Открыть StepModal → попробовать открыть ConfirmModal
   - Проверить автоматическое закрытие
   - Проверить закрытие по клику вне модалки

2. **Интегрировать оставшиеся модалки**:
   - Использовать тот же паттерн что и в StepModal/ConfirmModal
   - Добавить регистрацию при открытии
   - Добавить дерегистрацию при закрытии

3. **Документировать правила**:
   - Добавить в `copilot-instructions.md` обязательное использование
     ModalManager
   - Создать примеры интеграции для новых модалок

### Код-стиль для интеграции:

```javascript
// === В начале файла ===
let modalCleanup = null;

// === При открытии модалки ===
if (HEYS.ModalManager) {
  modalCleanup = HEYS.ModalManager.register('unique-modal-id', () => {
    closeModalFunction({ skipManagerNotify: true });
  });
}

// === При закрытии модалки ===
if (modalCleanup && !options.skipManagerNotify) {
  modalCleanup();
  modalCleanup = null;
}
```

---

## 📊 Метрики

- **Найдено модалок**: 10+
- **Интегрировано**: 2 (StepModal, ConfirmModal)
- **Требует интеграции**: 4 группы модалок
- **Новых файлов**: 1 (`heys_modal_manager_v1.js`)
- **Изменённых файлов**: 3 (index.html, step_modal, confirm_modal)

---

## ⚠️ Важно

**Флаг `skipManagerNotify`**:

- Используется чтобы избежать бесконечного цикла
- Когда ModalManager вызывает `closeFunction`, он передаёт этот флаг
- Модалка не должна дерегистрироваться повторно

**Уникальные ID модалок**:

- Используйте понятные ID: `'step-modal'`, `'confirm-modal'`, `'product-create'`
- ID должны быть уникальными в рамках всего приложения
- Избегайте динамических ID (исключение: модалки в списках)

---

## 🐛 Debug

Для отладки в консоли:

```javascript
// Проверить открытые модалки
window.debugModals();

// Закрыть все модалки
HEYS.ModalManager.closeAll();

// Проверить открыта ли конкретная
HEYS.ModalManager.isOpen('step-modal');
```

---

**Автор**: AI Assistant  
**Проверено**: Требует тестирования
