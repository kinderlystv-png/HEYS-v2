# Ответ дизайнера — UI v4, отступления «≠»

На срез **2026-09-05**, 933 строки, 17 классов + 4 одиночных.
Отвечено **5 сентября**. Формат ответа тот, что просили: **один ответ на класс причины**.

Прежде трёх ответов — одно правило, которое снимает половину спорного, потому что
почти весь «спор» сводится к одному вопросу.

## Правило источника: общий случай задаёт словарь, частный контекст сужается

Когда два кадра описывают одно состояние продукта и расходятся числами —
**верен тот кадр, который описывает ОБЩИЙ случай**, а частный контекст сужается
в коде до своего селектора. Не наоборот.

Причина не в старшинстве кадров. Частный кадр рисуется, чтобы показать
*отличие*; если его геометрия расползается на общий случай, отличие исчезает —
и вместе с ним смысл кадра. Так вышло с `is-weight-entry`: класс частного
контекста (правка веса куратором) висит на **любом** открытом `weight_reps`, и
поэтому оба вердикта, `=` на А1б/А2 и `=` на Г4, стояли одновременно при том,
что рендерится одно из двух.

Поштучный разбор нужен только там, где общий и частный случай **не
различаются** — то есть где нельзя назвать селектор, который отделит один от
другого. Таких случаев, по моей оценке, единицы, и я жду их списком.

---

## Ответы по классам

| Класс | Строк | Ответ | Чем правится |
| --- | ---: | --- | --- |
| canvas-conflict: геометрия/компонент vs кадр | 146 | **одобрить как правило** (правило источника выше) | код сужает частный селектор; кадры не трогаем |
| текст и копия отличаются от кадра | 130 | **мы починим кодом** | копия — предмет контракта |
| композиция и UX отличаются от кадра | 124 | **спорно — разобрать поштучно**, по одному критерию (ниже) | смешанно |
| цвет и роль vs литерал кадра | 101 | **одобрить как правило** | роль выигрывает у литерала — с оговоркой ниже |
| данных для состояния нет | 94 | **вы правы: `?` до реализации**, не `—` | код, когда дойдёт |
| продуктовое решение, кадр устарел | 70 | **одобрить как правило** — с условием ниже | я обновляю кадры и контракт |
| canvas-conflict: функционал не закрыт | 52 | **мы починим кодом** | код |
| геометрия и типографика: код vs кадр | 44 | **мы починим кодом** | код |
| кегль округлён до пикселя | 39 | **одобрить как правило**, но с порогом (ниже) | код там, где округление до целого |
| график/SVG: масштаб vs фиксированный кадр | 30 | **одобрить как правило** | ничем |
| компонент переиспользован из другой зоны | 25 | **одобрить как правило** — с условием адреса | я дописываю адрес в контракт |
| функциональный поток отличается от кадра | 22 | **спорно — разобрать поштучно**, тот же критерий | смешанно |
| превью палитры: миниатюра vs точки swatch | 17 | **одобрить как правило** | я снимаю точки с кадров |
| элемент кадра отсутствует в коде | 15 | **мы починим кодом** | код |
| геометрия кода vs кадр (EXCEPTION) | 9 | **мы починим кодом** — и уберите слово EXCEPTION | код |
| legacy-поверхность вне v4 | 9 | **`—` с `naKind: designer-removed`** — требование снимаю явно | ничем |
| зазор при 375 не влезает | 2 | **кадр правлю я** | я |

---

## Оговорки, без которых ответ «одобрить» становится дырой

### Цвет и роль (101) — роль выигрывает, но роль ТОГО ЖЕ вида

Литерал в кадре и роль в коде — не спор: роль верна. Но миграция обязана брать
роль **того же вида**, а не ближайшую по имени. Ваш же случай с крестиком шапки
приёма показывает, как это ломается: `#c67139` — это значение роли `--v4-act` в
песочном наборе, и правильная миграция — `var(--v4-act, #c67139)`, а перевод в
`--v4-ink-3` меняет вид.

Поэтому одобряю класс с условием: **литерал сначала опознаётся как значение
роли**, и только если ни одна роль ему не равна — он становится «цветом не из
палитры» и называется строкой контракта. У меня в `contract-audit.js` это
одиннадцатая проверка с допуском ±2 на канал; тот же порог годится и вам.

*(Про сам крестик: кадр рисует его нейтральным — `rgba(var(--ink),.45)`. Значит
терракотовый был дрейфом, миграция в `--v4-ink-3` верна, а красный тест сторожил
литерал устаревшего вида. Строка контракта заведена — см. второй файл ответа.)*

### Кегль (39) — порог: половина пикселя можно, целый нельзя

Округление **до 0,5 px** — норма: 12,5 px в кадре и `12.5px` в CSS это одно и то
же значение. Округление **до целого** — расхождение вида: 12,5 → 12 или 13
меняет плотность строки, и в тексте на 11–12 px это видно. Правило: расхождение
≤ 0,5 px — `=`, больше — `≠`.

### Продуктовое решение, кадр устарел (70) — решение должно быть НАЗВАНО

Одобряю, но «продукт верен, кадр устарел» без имени и даты решения — это
отговорка, а не решение. Условие: у каждой такой строки в контракте появляется
запись «решение <кто> <когда>», и я её вношу. Иначе через месяц никто не отличит
осознанный разнос от дрейфа — ровно то, что уже случилось с
«Перенести»/«Пропустить» в карточке плана.

### Компонент переиспользован (25) — с адресом зоны-владельца

Переиспользование законно, но строка контракта обязана назвать файл-владельца
(«вид … — `<файл>.v4.dc.html`»). Без адреса переиспользование не отличается от
дрейфа, и при расхождении кодер выбирает сам — то, что правило «один кадр — один
источник» запрещает. Адреса дописываю я.

### Данных для состояния нет (94) — первая редакция, НЕВЕРНАЯ

Сохранена названной неверной, а не стёрта — по той же причине, по которой я не стёр ложное обоснование про QR: проза живёт дольше решения, и стёртый довод всплывает через месяц как новый. Действующий ответ — в разделе «Два класса в `—`» ниже: `?` до реализации.

> ~~Состояние нарисовано, поставлять его нечем — значит вердикт **`—` (не в этот
релиз)** с названной причиной, а не `≠`. `≠` означает «код и кадр расходятся»;
здесь кода просто нет. Смешивать нельзя: 94 строки в `≠` создают ощущение
дизайнерского долга там, где его нет, и топят настоящие расхождения. То же для
legacy-поверхностей вне v4 (9).~~

### Два класса в `—` — возражение принимаю, я был неправ

Правка 5 сентября. Я смотрел на `—` как на способ снять с отчёта ложные
тревоги, а `—` означает «этого не должно быть». Разница — в том, что будет
дальше: `?` ждёт реализации и когда-то станет `=`, а `—` закрывает вопрос
навсегда. 94 строки в `—` спрятали бы отсутствующую функциональность за словом
«проверено» — тот же дефект, что проверка, сторожащая литерал вместо правила,
только в масштабе ста строк.

**«Данных для состояния нет» (94) — `?` до реализации.** Требование остаётся: ни
одно из этих состояний я из объёма не снимаю. Кадр нарисован потому, что
состояние в продукте будет; что его пока нечем наполнить — состояние работ, а не
приговор требованию.

**«Legacy вне v4» (9) — `—` с `naKind: designer-removed`.** Здесь я **снимаю
требование** явно — тот самый честный выход, который вы назвали:
legacy-поверхности в v4 не входят, кадра под них не будет никогда, и фича
выпадает из объёма осознанно — моим решением и с моим именем в `naKind`, а не
тихо под видом «данных нет». Пришлите девять ключей — подтвержу каждый
поштучно, чтобы `designer-removed` не стал тем же чёрным ходом под другим
именем.

Цитата из `docs/ui/verdicts/README.md` была к месту: я выдумал вердикт под свою
задачу, вместо того чтобы спросить, какой есть.

### EXCEPTION в имени класса (9)

Правлю кодом, но просьба: исключение должно быть названо **в контракте строкой**,
а не жить в имени класса проверки. Класс с «EXCEPTION» в названии — это
проверка, которая согласилась не проверять, и через полгода никто не вспомнит, на
что именно. Пришлите девять ключей — заведу им строки.

---

## Критерий для двух поштучных классов (124 + 22 = 146 строк)

Разбирать буду я, но не наугад. Критерий один:

- расхождение меняет то, **ЧТО человек может сделать** (появилось или исчезло
  действие, изменился исход, разошлись состояния) → **верен кадр**, правится код;
- расхождение меняет только **порядок и группировку уже нарисованного** →
  **верен продукт**, кадр обновляю я.

Пример из вашего же списка на обе стороны: разнос «Перенести» / «Пропустить» в
карточке плана меняет исходы (`moved` ≠ `skipped`, след в обе стороны) — верен
продукт, кадр Г3 обновляю. А inline-раскрытие приёма в дневнике добавляет второй
способ читать один и тот же приём — там верен код, который его не делает, и
кадр уходит в протокол.

Что нужно от вас для этих 146: **список ключей с одной строкой «что именно
разошлось»** на каждый. Классификация по моему критерию — моя работа, но
угадывать, что стоит за ключом `curator-cabinet · чип «все · N»`, я не могу.

---

## Четыре одиночных

Все четыре мои, отвечу в один заход, но нужны факты:

| Ключ | Что пришлите |
| --- | --- |
| `app-splash · уменьшенное движение` | что делает код при `prefers-reduced-motion` на сплэше — снимает анимацию, ускоряет или игнорирует |
| `curator-edits · переход по строке` | куда ведёт тап по строке правки в коде и что открывает кадр |
| `registration · Документ · конец · 05` | текущий % чернил дорожки чтения в коде |
| `service-curator · фича` | состав служебного листа в коде, по пунктам |

Правило: класс ≥ 2 строк — согласен, хвост из одиночных так и должен жить
списком, а не искусственным классом.

---

## Чего в этом ответе нет намеренно

**Я не одобрил ни одного класса «оптом по строкам».** Одобрено семь классов как
**правила** — то есть правится не 400 строк, а формулировка, из которой эти
строки следуют. Если правило где-то даёт неверный результат, это находка на
правило, и она дороже находки на строку.

**Я не свёл ни один кадр «под зелёный».** Там, где верен продукт, кадр
переделывается и слепок переснимается — числа не подгоняются. У меня это
охраняется проверкой «живой замер против своей строки»; сегодня она поймала
последствия правки тач-целей ровно так, как должна.

---

## Поставка пакета — заводите приёмку

Правило проекта вы приводите верно, и я его подтверждаю: **пакет едет отдельным
коммитом и отдельной приёмкой**, не внутри чужой правки. Заводите.

Состав к поставке: 28 канвасов, три новые зоны (`first-run`, `subscription`,
`messenger`), листы приёмки на все зоны с контрактом, `standalone/` для
просмотра без сборки, `INDEX.md` как карта «фича → файл → метки экранов → код» и
эти два файла ответов.

Условие с моей стороны выполнено: сверка чиста у всех 28 канвасов, слепки
переснятывались после каждой правки кадров — включая сегодняшние правки
`subscription`, `nutrition-tab`, `food-meal`, `product-card`, `strength-builder`
и `first-run`.

## Про склонение к сегодня — подтверждаю равный выбор, но закрываю ваш аргумент

Ваше возражение точнее, чем мой первый ответ: «такую ошибку человек находит не
сразу». Это правда, и защищать надо именно от **незамеченной** ошибки — но
предупреждение до записи мешает всем, кто записывает намеренно, а ловит только
того одного, кто ошибся.

Поэтому оставляю равный выбор (лист сам и есть остановка — он появляется только
на не-сегодняшнем дне) и добавляю то, чего не хватало: **след после записи**.
Заведены две строки контракта в `nutrition-tab`:

- полоса отмены по правилам `undo-bar.v4.dc.html` с текстом «Записано в
  19 августа» и действием «Отменить», 6 с — единственное место зоны, где дата
  стоит **в тексте** полосы, а не подразумевается;
- капсула даты в шапке остаётся на открытом дне и после записи, уже в тоне
  прошедшего дня.

Ошибка находится не в момент выбора, а в момент, когда её видно: сразу после
записи и в самом списке. Если вы считаете, что этого мало — скажите, и я вернусь
к склонению; но тогда честнее склонять **без** слова «всё-таки».
