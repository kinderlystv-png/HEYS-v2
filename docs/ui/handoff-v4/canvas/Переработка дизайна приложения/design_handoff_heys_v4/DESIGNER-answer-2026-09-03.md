# Ответ дизайнера на разбор кодера от 3 сентября

Отвечаю по вашему порядку. Где решение принято — оно и есть строка контракта, и ляжет в названный файл в тот же день. Где ход владельца — сказано прямо.

**Сначала три вещи, которые снимают вопросы полностью.**

1. **6.3 не приехал потому, что пакет не собирался.** `#a83c22` живёт в корневом `v4-canvas.css` с 3 сентября, а в `design_handoff_heys_v4/` лежит копия от прошлой сборки. Собираю пакет сегодня — канвас придёт с новым значением, и гейт дрейфа увидит согласие, а не расхождение. Ваш ход по `--v4-bad-text:199` верен, ловушку с тремя одноимёнными ролями в синем наборе принял к сведению — спасибо, что назвали поимённо.
2. **А1.1 контракт не ломает.** Проверил поиском по всем канвасам: строки «данные дня остаются на устройстве» нет ни в одном. Строка `login` говорит только про палитру, и она верна. Ваша поправка ничего не переписывает — но факт важен сам по себе, и я его записываю строкой «выход · что стирается» в `login`, чтобы следующий не выдумал заново.
3. **Три найденных дефекта принимаю все.** 🌸 в виджете «Цикл», Йод вне `KNOWN_SUPPLEMENT_IDS`, расхождение канваса — чините без обсуждения, это ровно то, для чего контракт и заведён. Расширение гейта на файл виджетов — тем более: слепая зона нашлась не потому, что кто-то ошибся, а потому, что проверка читала два файла из трёх.

Отдельно: **«91 мёртвый селектор classic» — моя запись, и она была ложной.** Спасибо, что пересчитали двумя способами. Вердикт поправлен у вас, у меня строка снимается вместе с оговорками (см. Б.10).

---

# Раздел 2. Особый период

## ② Подпись бара отмены — **две подписи, вы правы**

Принимаю ваш аргумент целиком: «Не идут» снимает неделю, «Закончились» закрывает её на текущем дне и оставляет счёт фаз идти дальше. Одна подпись на два разных исхода читалась бы неверно — а бар отмены существует, чтобы человек понял, **что именно** он сейчас отменяет.

Строка контракта переписывается так (`cycle.v4.dc.html`, строка «отмена разрушительных ответов»):

> Оба разрушительных ответа зовут общий бар на 5 с, но подписи **разные**, потому что исходы разные: «Не идут» → `Особые дни сняты · N дней` (снята вся отмеченная неделя), «Закончились» → `Особые дни закрыты` (неделя закрыта на текущем дне, счёт фаз продолжается). Одна подпись на оба исхода запрещена: человек должен видеть, что отменяет. Вид и длительность бара — `undo-bar.v4.dc.html`.

Моя прежняя формулировка «оба зовут бар с подписью „Особые дни сняты“» была недописана, а не спорна: я знал про два действия и назвал одну подпись.

## ③ Карточка недели наверху стопки — **исключение остаётся, дописываю в строку**

Не снимайте ни подъём, ни тест. Обоснование: шаг «Остальное» — стопка условных пунктов, и порядок в ней не декоративный, а по близости к сегодняшнему дню. Отмеченная неделя — то, что человек **ведёт прямо сейчас**; замеры — то, что можно сделать когда угодно. Ставить ведущееся ниже необязательного значит требовать прокрутки ради главного.

Строка (`checkin-morning.v4.dc.html`, «место строки периода в стопке») получает исключение:

> Строка «Особые дни» стоит между «Утренней рутиной» и «Замерами» — **кроме отмеченной недели**: при `cycleWeekTop` строка поднимается первой в шаге, а «Замеры» откладываются. Исключение намеренное: ведущаяся неделя ближе к сегодняшнему дню, чем необязательный замер. Закреплено тестом `cycle-v4-contract.test.js` «week period moves cycle row to top and defers measurements» — тест не снимается.

Виноват: строка описывала спокойное состояние и молчала про отмеченное. Это тот самый случай «поверхность от состояния метка не отличает», о который мы уже спотыкались.

---

# Раздел 4. Два спора

## Б.9 · **Беру вашу формулировку почти дословно**

Признаю: комментарий у бликов я прочёл наоборот — он объясняет, **почему их приходится гасить отдельно** (флаг на родителе укрывает поддерево, снять его нельзя, иначе умрёт подъём уровня), а не отказывается гасить. Спора «правило против теста» нет, есть два неназванных исключения. Тест не снимаем.

Строка (`spinners.dc.html`, ярус «Движение», строка «функциональное движение и ОС»):

> Функциональное движение по системной настройке **не гасится**: `functionalAnimationsEnabled()` возвращает `true` всегда. Исключений два, оба про декор внутри функционального поддерева: **дуга ожидания** не гаснет, а меняет вид движения — вместо вращения дыхание прозрачностью (`heys-boot-breathe 1.6s`, кадры гоняют только `opacity`); **блики воды** гасятся адресно по псевдоэлементу, потому что флаг стоит на родителе `.widget-water__fill` и правилом `:not(.animate-always *)` укрывает всё поддерево. **Подъём уровня, капля и рябь не гасятся никогда** — это ответ на жест, а не украшение.

Одно уточнение к вашей версии: я добавил «почему» к бликам. Без причины следующий сводящий увидит исключение и снимет его как непоследовательность — ровно то, что уже случилось с коммитом `38c2f763`.

## Б.10 · **Закрыто. Снимаю оговорки**

Ход мой, делаю сегодня: из строк уходят все «кроме классики», а строка про 91 мёртвый селектор снимается как ложная. `migrateClassicThemeId` — хороший довод: выбрать классику нельзя даже вручную, значит наборов четыре и в контракте, и в коде.

---

# Раздел 3. Добавки — пять ответов

Вводная моя устарела, принимаю: блок написан, четыре дефекта закрыты, и переотвеченные 20 вопросов — то, чего мне не хватало. Отвечаю на пять ваших вопросов, дальше рисую кадры.

**1. Групп четыре — «Утро · С едой · Вечер · По случаю».** Деление блока v4 верное, легаси-трёхчастное неверно. Причина: `anytime` в «Утро» — это ложь по смыслу. Добавка любого времени не утренняя; вливая её в утро, экран говорит человеку «прими сейчас», хотя правило приёма другое. `afterTrain` тем же образом не «вечер». Четвёртая группа честнее трёх, даже если чаще пустая — а пустая она скрывается (ваш п. 9).

**2. Поверхность одна — блок вкладки. Легаси-карточка дневника снимается.** Это прямое правило «один кадр — один источник»: та же сущность, нарисованная в двух местах по-разному, всегда расходится, и уже расошлась — деление групп, полоска прогресса, напоминание по часам, долгое нажатие. Главный файл — блок вкладки. Что при снятии **не теряется**, я проверю по вашему разбору и назову строкой; заранее вижу два кандидата на перенос: полоска прогресса (решу отдельно, скорее нет — счётчик «N из M» уже отвечает) и научная карточка (см. п. 5).

**3. Чип «не в курсе» — остаётся видимым.** Ваша правка правильная, я её принимаю как решение. Логика та же, что с прочерком на пустом дне: отметка **была**, человек её поставил; спрятать её — соврать про историю. Пометка «· не в курсе» и класс `is-stale` идут в контракт.

**4. «Всё сразу» — остаётся.** Это не дубль пилюль: пилюля отмечает **группу**, «Всё сразу» — **день**. Разные объёмы, и у человека с курсом из трёх позиций в трёх группах пилюли требуют трёх нажатий вместо одного. Плюс «Снять всё» — единственный способ откатить случайное «Всё сразу» одним движением. В шапке блока — верное место.

**5. Долгое нажатие в блок v4 не переносим.** Причина ровно та, которой вы закрыли свайп (запись № 5 раздела 5): у удержания нет видимой подсказки, и функция, о которой нельзя догадаться, для большинства не существует. Научная карточка — не действие дня, ей место в экране курса, где позиция открывается тапом. В блоке вкладки чип делает одно: отмечает и снимает.

Итого рисую: блок в четырёх состояниях (пусто · отмечена группа · всё принято · курса нет), частичная группа, чип «не в курсе». Кадры будут в `nutrition-tab`, зона моя.

---

# Раздел 5. Семь записей — решения

**1. Цикл, словесная часть — да, заголовок несёт число.** Это правка продукта, не кадра: «Норма выше на N %» вместо глухого «Особый период». Смысл запроса именно в этом — цена поправки должна быть числом, иначе пересчёт остался невидимым.
Из трёх подписей в **продукт идёт одна**: «Норма приподнята во второй половине цикла» под графиком нормы. Две другие — «Пометка стоит только в дни 15–28» и «Пустые точки — дни 26–28 и 1–5» — **пояснения канваса**, в интерфейс не идут: первая объясняет разметку кадра, вторая уже сказана всплывающей подсказкой по тапу, а дублировать её подписью значит объяснять график дважды.

**2. Запрос замера — согласен целиком, отдельная задача.** Ваш довод про генератор чужой несведённой зоны верен: дописывать правило в `reports-insights` до её сведения — оставлять найдёныша. Ставлю задачу связкой: сведение `reports-insights` + перенос запроса в «Сделай сегодня». До тех пор строка «где живёт каждый кадр» получает пометку, что кадр адресован блоку, которого ещё нет.

**3. Регистрация, зазор 8 против 12 — строка верна, ход ваш, сейчас не трогаем.** Согласен с решением не менять: тридцать пять заплаток нельзя снять слепо, результат надо смотреть глазами по шагам. Но это **долг**, а не норма, и я записываю его строкой «долг канваса · зазоры регистрации»: пока расстояние есть сумма контейнерного `gap` и детского `marginTop`, ни одно число нельзя ни прочитать, ни проверить. Чинится одной правкой контейнера, когда зона откроется для работы.

**4. Лист копирования не перекрашен — идёт в v4, отдельной задачей, до сведения кадров зоны.** Ваш порядок правильный: если сводить кадры раньше перекраски, вердикты придётся переписывать сразу после. 38 мест на запасных значениях — это не «немного не в наборе», это лист, который не меняется вместе с темой вовсе.

**5. Свайп по строке состава — снимаем свайп.** Ваш аргумент решает: у свайпа нет видимой подсказки, и человек, который о нём не знает, не может удалить продукт **никак**. Это не разница в пикселях, а недоступное действие. Строка `food-heal` остаётся как есть, крестик 14 px в цели 44 — единственный способ удалить.
Копирование и перенос переезжают в **лист действий приёма**, где у них уже есть свои кадры («Питание · перенос приёма», «Питание · копирование продукта» — нарисованы 3 сентября, лист снизу, одна форма на обе операции).

**6. Главная кнопка приёма — переводится на `--v4-act`, надпись меняется той же правкой.** Здесь ваш вопрос упирается в решение владельца **от 3 сентября**, которого у вас ещё не было: *«кнопка цвет с темой МЕНЯЕТ»* — `.btn` берёт `var(--acs)`/`var(--on-acs)`, и токен `--on-acs` держит четыре значения (тёмный текст в трёх наборах, белый только в светло-синем). Значит оранжевая кнопка в синем наборе — **не намеренное состояние, а незаменённая роль**, и правило `[data-palette='blue'] … .meal-time-cta` с надписью `--v4-ink` уходит вместе с заливкой.
Одной правкой: заливка `--v4-sand-act` → `--v4-act`, надпись → парная роль текста этого набора. Проверьте контраст после замены — в светло-синем это белый (6,79), не `--v4-ink`.

**7. «Повторить сегодня» — остаётся. Снимается моя строка.** Ваш довод решает: одно нажатие против модалки с выбором дня, выбором продуктов и подтверждением — это не строгость контракта, а потеря функции ради формальности. Верна `nutrition-tab` с **четырьмя** строками; строка food-meal «„Повторить сегодня“ снято» **снимается совсем**, а «блок действий» переписывается на четыре строки-меню.
Правило «при расхождении верен food-meal» я не отменяю — но оно про то, **как выглядит**, а не про то, **что можно**. Мой промах: я применил его к строке, которая убирала действие, вместо того чтобы вынести вопрос. Признак, по которому надо было поймать, у нас записан, и я его пропустил.

---

# Раздел 6. Ваши уточнения

**6.1 · «Понятно» — вы правы, в блоке приёма снимать нечего.** Я имел в виду именно деталь совета (`day/_advice.js:1246`, `:1372`) и ошибся адресом: думал, что она и есть блок приёма.
Решение: **в детали совета «Понятно» остаётся, тест не роняем.** Там это не дубль креста, а закрытие детали — крест закрывает панель, «Понятно» отвечает на совет. В блоке приёма берите только цель 44 у креста.

**6.1 · Весь файл на роли — да, весь, не разбивайте.** Семьдесят литералов вместо трёх — не увеличение объёма, а моя недооценка: я назвал три, которые видел в кадре, а остальное не считал. Слейт и небесно-синий в песочном наборе — это тот же дефект, что копирование продукта, и он даст ту же поломку в тёмных наборах. Про градиент: `rgba(245, 158, 11, 0.15)` на `:461` — он и есть, я написал по памяти неточно.
Соответствие «правило → семейство» в протоколе экрана — жду, проверю и запишу строкой.

**6.2 · Оба листа, но по очереди — как вы и предложили.** `heys_move_modal_v1.js` сейчас, источник 45 % (моя строка старше, ваш замер 50 % — правьте по строке). `heys_day_copy_meal_modal_v1.js` — отдельная задача после ответа владельца, и это запись № 4, которую я закрыл выше: **идёт в v4**.
**Моя ошибка адресации:** три кадра зоны описывают именно второй файл, а строка называла первый. Исправляю строку — она назовёт оба файла и скажет, какой кадр к какому относится.

**6.3 · Канвас придёт сегодня.** Собираю пакет: `v4-canvas.css` с `--red:#a83c22`, плюс одиннадцать канвасов, которые правились с прошлой сборки. Пометка в вашем протоколе «канвас пока расходится» после сборки снимается.

---

# Что делаю я

Строками контракта сегодня: подпись бара отмены (две), исключение карточки недели, два исключения движения, снятие оговорок «кроме классики» и ложной строки про 91 селектор, пять решений по добавкам, семь решений раздела 5, три уточнения раздела 6, новая строка «выход · что стирается».

Кадрами: блок «Добавки» в четырёх состояниях плюс частичная группа и чип «не в курсе».

Пакетом: сборка с новым `--red` — сегодня.

**Ваши четыре правки без отдельной команды — начинайте.** 🌸 и Йод мелкие и однозначные; советы приёма и лист копирования по объёму ближе к перекраске, но оба названы строками, и спорить в них не с чем. Пятая, `--v4-bad-text`, ждёт только моего пакета — он будет раньше, чем вы дойдёте до неё.
