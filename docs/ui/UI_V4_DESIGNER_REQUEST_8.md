# Заход к дизайнеру № 8 — открытое на 15 сентября 2026 г.

Собрано из журнала [`UI_V4_FINDINGS.md`](UI_V4_FINDINGS.md) — только записи,
которые ждут ответа. Разобранное и закрытое лежит в
[`UI_V4_FINDINGS_HISTORY.md`](UI_V4_FINDINGS_HISTORY.md) и сюда не попало.

**Источник правды — сам журнал.** Здесь срез: заголовок, чей ход и ссылка на
разбор. Пересказывать разбор второй раз значит заставить читать одно и то же.

**Объём.** Записей — 13, зон — 9. Ждут вашего ответа — 10; решено без вас, за
вами перерисовка — 3.

| Зона             | Записей |
| ---------------- | ------- |
| Главная          | 3       |
| Силовая          | 2       |
| Чек-ин утра      | 2       |
| Актив            | 1       |
| Дата и календарь | 1       |
| Кабинет куратора | 1       |
| Мессенджер       | 1       |
| Отчёты и инсайты | 1       |
| Питание          | 1       |

---

# Ждут вашего ответа

Пока ответа нет, эти места стоят: сводить код не по чему, а угадать значит
развести код и кадр.

## Главная

### В дефолтной раскладке «+8» набран общим классом, а не кеглем строки

**Чей ход.** Дизайнера: поправить число в дефолтной раскладке на 26.

Разбор —
[`health-trend-default-layout-delta-size`](UI_V4_FINDINGS.md#health-trend-default-layout-delta-size).

### Шаги: одна строка рисует плитку «Как сейчас», другая говорит, что такого вида нет

**Чей ход.** Ваш: переписать строку «шаги» под три вида — или сказать, что кадр
«Шаги · Как сейчас» нарисован только как картинка типа в каталоге и видом быть
не должен. Второй ответ снимает вид из листа, и это правка одной строки кода.

Разбор —
[`steps-now-tile-vs-no-now-view`](UI_V4_FINDINGS.md#steps-now-tile-vs-no-now-view).

## Силовая

### Словарь называет ступень «сброс 1», а оба кадра дроп-сета пишут «дроп»

**Чей ход.** Дизайнера: либо подписи ступеней в кадрах меняются на «сброс 1» и
«сброс 2», либо из строки словаря уходит пункт про подписи ступеней.

Разбор —
[`strength-drop-stage-label-word-conflict`](UI_V4_FINDINGS.md#strength-drop-stage-label-word-conflict).

### Силовая: кадры отстают от решений и спорят с логикой экрана

**Чей ход.** Дизайнера: перерисовать звезду в конец строки; сказать, ряд
фильтров при 14 группах — одна строка с прокруткой или перенос; дорисовать выход
в «Связка · создание». Владельца: строить ли перестановку упражнений и шаблоны
тренировок — это новые функции, а не правка вида.

Разбор —
[`strength-builder-frames-behind-decisions`](UI_V4_FINDINGS.md#strength-builder-frames-behind-decisions).

## Чек-ин утра

### Две строки контракта дают шагу веса разный верхний отступ

**Чей ход.** Дизайнера: вычистить хвост «у шага веса 14 сверху» из строки «вид
шага».

Разбор —
[`checkin-step-top-padding-two-rows`](UI_V4_FINDINGS.md#checkin-step-top-padding-two-rows).

### Чек-ин · остальное: «Последний кофе» спрашивают утром — про день, которого ещё не было

**Чей ход.** Дизайнера — кадр и слова. Продуктовое решение уже принято.

Разбор —
[`checkin-last-coffee-asked-before-the-day`](UI_V4_FINDINGS.md#checkin-last-coffee-asked-before-the-day).

## Актив

### Актив: список «Минуты по зонам» нарисован, но такой поверхности в продукте нет

**Чей ход.** Ваш, один вопрос: список «Минуты по зонам» — это новая карточка на
«Активе» или замена пилюль в карточке тренировки? Если замена — назовите зону 1;
если новая карточка — скажите, стоит ли она у каждой тренировки дня или только у
силовой.

Разбор —
[`activity-zone-minutes-surface`](UI_V4_FINDINGS.md#activity-zone-minutes-surface).

## Дата и календарь

### Календарь · легенда: кадр спорит со строкой «вид клетки», часть спора решил владелец

**Чей ход.** Дизайнера: переписать строку «вид шторки календаря» под строчный
регистр и привести форму метки цикла к одной — в клетке, в образце легенды и в
обмерах. Код ждёт обеих строк.

Разбор —
[`calendar-legend-frame-behind-its-own-contract`](UI_V4_FINDINGS.md#calendar-legend-frame-behind-its-own-contract).

## Кабинет куратора

### Лист куратора: условие подписи «задним числом» написано наоборот от смысла

**Чей ход.** Дизайнера: переформулировать условие — «правка сделана позже дня, к
которому относится запись» или наоборот, — и назвать, на каких типах строк
подпись стоит (кадр рисует её только у воды).

Разбор —
[`curator-sheet-backdated-caption-condition-inverted`](UI_V4_FINDINGS.md#curator-sheet-backdated-caption-condition-inverted).

## Отчёты и инсайты

### Ярус «Метаболизм» должен стать двумя входами, а экрана за вторым входом нет

**Чей ход.** Дизайнера: кадры двух экранов за входами. Он назвал это отдельной
задачей в ответе четвёртого захода.

Разбор —
[`insights-metabolism-tier-entries-await-frame`](UI_V4_FINDINGS.md#insights-metabolism-tier-entries-await-frame).

---

# Решено без вас — к сведению

Здесь ждать было нельзя: правило платформы или явный дефект. Код уже стоит по
решению, за вами — перерисовать кадр, чтобы он не спорил с продуктом.

## Главная

### Кадры Главной рисуют цифры пропорциональными — каскад канваса гасит моноцифры

**Чей ход.** Дизайнера: в `<style>` home-widgets.v4 поставить `.n` после `.v`
или вписать `font-variant-numeric: tabular-nums` в сам `.v`, чтобы кадры
рисовали то, что названо в строках. Проверить тот же приём в остальных канвасах,
где шорткат `font:` стоит ниже `.n`.

Разбор —
[`home-widgets-canvas-tnum-cancelled`](UI_V4_FINDINGS.md#home-widgets-canvas-tnum-cancelled).

## Мессенджер

### Время в пузыре: правило кадра — отдельной строкой, а рисуется в строке текста

**Чей ход.** Дизайнера: в `.bt` дописать `display:block` (или перенести класс на
`<div>`), чтобы кадры рисовали то, что задано правилом.

Разбор —
[`messenger-bubble-time-inline-span`](UI_V4_FINDINGS.md#messenger-bubble-time-inline-span).

## Питание

### «Отмена» в листах копирования и переноса — серая, а кадр рисует акцентом

**Чей ход.** Дизайнера: перерисовать шесть «Отмен» в food-meal.v4 на `--ink-2` и
назвать тон строкой контракта «вид · лист копирования и переноса», чтобы
следующий сводящий не вернул акцент. Перекрашивать надо саму «Отмену», а не
класс `.btn2`: тем же классом нарисованы настоящие вторичные действия —
«Добавить продукт», «Завершить приём», «Собрать новый набор», «Скрыть», — им
акцент по делу.

Разбор —
[`transfer-sheet-cancel-accent`](UI_V4_FINDINGS.md#transfer-sheet-cancel-accent).
