# Composer · закрытие 23 строк `≠` из ACCEPTANCE.md

**Когда:** после правки 11 строк контракта и пересборки листа (2026-08-20
вечер). **Scope:** только код; контракт не трогать, кроме повторной сверки
вердикта. **Сводка:** 141 строк · **113** `=` · **23** `≠` · **5** `—` · **80
%** **Verify:** `pnpm bundle:legacy:auto --files=<…>` + `pnpm dev:local` +
vitest по зоне.

---

## P0 · сначала (ломают обещание)

### 1. HOME ·38 · вода на закрытом дне

**Контракт:** вода и сон — факт без порогов «к этому часу». **Факт:** `by_hour`
/ порог от «сейчас» на прошлом дне — `heys_widgets_ui_v1.js` (~3709, ~1826).
**Сделать:** на `!isToday` не применять текущий час к ожиданию/окраске; только
факт за день. **Smoke:** vitest/widget или сценарий closed day + вода без «к
этому часу».

### 2. checkin ·30 · пуш 14:00

**Контракт:** один push в 14:00 local, только при включённых уведомлениях.
**Факт:** `setTimeout` во вкладке — `heys_morning_checkin_v1.js` (~874).
**Сделать:** перенести на существующий push/notification pipeline продукта;
переживает закрытие вкладки. **Smoke:** unit на schedule + cancel при «Сделал»
(`:914`).

---

## P1 · продуктовые дыры

| Ref        | Файлы (старт)                                   | Задача                                                        |
| ---------- | ----------------------------------------------- | ------------------------------------------------------------- |
| HOME 11    | `730-widgets-dashboard.css` ~2922               | **`второго gap нет`** — снять media query gap/pad 6 на ≤480px |
| HOME 35    | `heys_widgets_variants_v4.js` ~118              | `isDefault: true` для вида «За месяц» у динамики веса         |
| HOME 23    | `heys_widgets_weight_dynamics_v4.js`            | Окно спарклайна: 7д → 2/3/4 нед → месяц                       |
| HOME 36    | `heys_day_utils.js` ~1282                       | Красное сокращение выходного в капсуле «сегодня»              |
| HOME 29    | `heys_widgets_ui_v1.js` ~1672                   | Дорисовка линии **320 ms**, не 1200 ms                        |
| HOME 18    | `730-widgets-dashboard.css`, palette roles      | Good `#7a8a5e` → светлее в тёмных наборах                     |
| WATER 19   | `730-widgets-dashboard.css` ~10002              | Норма «из N» — сплошные #6b5f4f / #5a6474, не alpha .55       |
| WATER 26   | `730-widgets-dashboard.css`, `MOTION_POLICY.md` | reduced-motion: без капли/круга, 160 ms level                 |
| checkin 21 | `heys_steps_v1.js` ~2744                        | Шаг слайдера цели **500** на всём диапазоне                   |
| checkin 22 | `heys_steps_v1.js`                              | Пол 7000 + подпись коридора когда сработал                    |

---

## P2 · polish (звук, карточка, мелочи)

**Звук** (`heys_audio_v1.js`, `heys_day_day_handlers.js`): тон, огибающая,
anti-spam (>4/2s или явное решение), платформенный haptic, момент (+240 ms /
reduced-motion сразу). Строки WATER 28–30, 36–38.

**Карточка воды:** WATER 45 `нет виджета и нет кнопки` — анимация уровня в
карточке без FAB.

**Прочее:** WATER 10 уровень, 18 раскладка, checkin 28 (убрать «В курсе нет» при
пустом курсе).

---

## Не входит в этот прогон

- 11 строк **контракт** — ✅ закрыты дизайном 20.08
- **5** строк **`—`** — отдельная оценка часов (свой объём, память, спарклайн
  карточки, утро, вариация)

После каждого блока — правка вердикта (`≠` → `=` + факт) в
[`verdicts/<зона>.json`](./verdicts/<зона>.json) и
`node scripts/ui-v4-check-contract-drift.mjs --rehash <зона>`. Прежний генератор
`build-acceptance-filled.mjs` снят: он хранил вердикты внутри себя и при запуске
переписывал рукописную часть листа устаревшими числами.
