# Промпт: довести продукт до контракта v4

Состояние на **26 августа 2026, сессия**. Пакет дизайна — шестнадцатая сборка,
**1402 строки** контракта в 16 зонах (6 зон без контракта — см.
«Заблокировано»).

Снимок вердиктов: `node scripts/ui-v4-check-contract-drift.mjs --list`

| Вердикт | Строк | Доля |
| ------- | ----- | ---- |
| `=`     | 1121  | 80 % |
| `≠`     | 178   | 13 % |
| `?`     | 5     | <1 % |
| `—`     | 97    | 7 %  |

**Открыто для кода:** 178 `≠` + 5 `?` = **183 строки** (было 189).

---

## 1. Где источник правды

**Строки контракта, и только они.** Пакет лежит в
`docs/ui/handoff-v4/canvas/Переработка дизайна приложения/design_handoff_heys_v4/`;
строка — это `<div class="spec"><b>ИМЯ</b><span data-v="ЗНАЧЕНИЕ">`.

Не сверяйся по производным. Ни `README.md` пакета, ни `INDEX.md`, ни приёмочные
листы, ни `UI_V4_FINDINGS.md` источником не являются — все они отстают на шаг, и
за один вечер каждый из них соврал. Проза «Решений» — пересказ: при расхождении
верен контракт, при споре двух канвасов — тот, что назван строкой `источник`.

Кадры — вторая величина. **Контракт старше кадра**; отступление называется вслух
в протоколе и поимённо в тесте сверки.

Начинай с `node scripts/ui-v4-check-contract-drift.mjs --list` и перечитывай
свои строки перед каждым крупным блоком: пакет правится отдельными коммитами, в
том числе посреди работы над экраном.

## 2. Сделано (включая эту сессию)

### Ранее сведено

- Снимок вердиктов заведён и ведётся в `docs/ui/ui-v4-contract-verdicts.json`.
- Файл расхождений (`UI_V4_FINDINGS.md`) очищен; живые записи — только
  дизайнерские блокеры (см. §6).
- Три роли линии, единый знак ожидания, политика звука и вибрации, празднование
  одно (тихая минута), вставка кода PIN.

### Закрыто в этой сессии

| Задача                                 | Файлы / проверка                                                                                                                 |
| -------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------- |
| **2a — раскладка виджетов в облаке**   | `heys_widgets_core_v1.js`, `heys_storage_layer_v1.js` · `widgets-layout-cloud-rules.test.js`                                     |
| **Лестница слоёв**                     | `002-ui-v4-palette-roles.css`, легаси-подложки · `layer-ladder.test.js`                                                          |
| **Долгое нажатие 350 мс**              | `heys_long_press_v1.js` · `long-press-product-rules.test.js`                                                                     |
| **Питание — a11y + три чипа профиля**  | `heys_day_nutrition_v1.js`, `732-ui-v4-nutrition.css` · `nutrition-tab-v4-contract-fixes.test.js` (чипы, reduce-motion, aria)    |
| **Чек-ин — progressbar «Шаг N из 5»**  | `heys_step_modal_v1.js` · `morning-checkin-v4-a11y-smoke.test.js`                                                                |
| **Анкета — согласие на шаге 1**        | `heys_trial_intake_v1.js` · `intake-consent-first.test.js`, `intake-v4-blocked-action.test.js`                                   |
| **«Повторить сегодня» — строка листа** | `day/_meals.js`, `heys_day_meals_bundle_v1.js` · `nutrition-repeat-today-sheet.test.js`                                          |
| **TDZ `navigateAndScrollToMeal`**      | `day/_meals.js` (объявление выше `repeatTodayMeal`) · `meal-plate-guide.test.js` 9/9                                             |
| **Gamification test fix**              | `gamification-xp-past-day-gate.test.js` (5 dispatch с `date`)                                                                    |
| **Nutrition tests (клетчатка + a11y)** | Тесты, не код · solo green; guard-файлы `subscription-curator-guard`, `nutrition-v4-structure` обновлены под «Повторить сегодня» |
| **«Два устройства» → `≠`**             | Код — облако (`client_kv_store`, LWW); канвас home-widgets — device-only. Вердикт `≠`, ждём строку в пакете дизайна              |

## 3. Заблокировано — в код не тащить

| Зона                                                                        | Причина                                                                                                                                                                                                                    |
| --------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `home-widgets` (канвас целиком)                                             | Пакет дизайна: строка «два устройства» ещё device-only; облачная редакция не приехала. Код уже облачный — вердикт `≠`, не править под старый канвас                                                                        |
| cycle                                                                       | **0 строк [data-contract], «МАКЕТ НЕ СОГЛАСОВАН»** — **не реализовывать** UI, wiring, движок v4, метки из профиля. Спека из чата 26.08 — решение владельца, **не канвас-контракт**, пока пакет не обновит cycle.v4.dc.html |
| `tab-insights`, `tab-activity`, `tab-reports`, `food-add`, `food-add-short` | Помечены «МАКЕТ НЕ СОГЛАСОВАН» — **0 строк контракта**. Реализовывать нельзя                                                                                                                                               |
| Ночь до 03:00 (капсула «Ночь на …»)                                         | Контракт назван, кадра нет — долг дизайнера (`date-remainders`)                                                                                                                                                            |

## 4. Открыто: 183 строки

### Кластер питания/день — **69/69 зелёный**

Пять файлов (repeat-today, meal-plate-guide, gamification gate, nutrition-tab
fixes, nutrition-tab states) — solo `pnpm vitest run <file>` из `apps/web`,
26.08: **22/22** (`contract-fixes`) и **24/24** (`states`) зелёные по одному.
Код продукта не менялся: «красные» были guard-тестами (клетчатка без закрытого
дня; `textContent` смешивал `—` и sr-only; счётчик paywall 11→12;
`openMoodEditor`/`Оценки приёма` vs контракт «действия приёма»).

Полный прогон ~454 файла (26.08, воркеры остановлены): **450 passed, 1 failed**
(`integrate-agents.test.js` — merge/rollback, не продукт), 3 skipped, 5686
tests. Флак: `login-error-codes-contract`, teardown в `advice-sound-toggle`.
Следующий приоритет — чек-ин a11y (батч B); cycle остановлен (§3).

### Очередь по приоритету

**Первое — доступность, где вердикт ещё `≠`.**

- **Чек-ин:** капсула сна и дорожка шагов/совета не озвучены (`checkin-morning`,
  9 `≠` + 3 `?`).
- **Питание:** тесты кластера зелёные; остальные `≠` по зоне — строки контракта,
  не регрессии.

**Второе — крупный системный шрифт и zoom.**

- `index.html`: `maximum-scale=1.0, user-scalable=no` — зум запрещён явно.
- ~98 % размеров в CSS жёсткие px; системное увеличение не доходит.
- Зона `settings-system` (13 `≠`) + сквозные строки.

**Третье — восемнадцать листов разбора плитки.**

- Двенадцать сейчас (данные есть), шесть позже (нужна история).
- До второй части тап по «историческим» шести — лист «за сегодня» одной строкой;
  промежуточного листа нет.
- Основной код: `heys_widgets_ui_v1.js`, `heys_widgets_variants_v4.js`,
  `730-widgets` CSS.

**Четвёртое — механические строки зон (не home-widgets).**

| Зона              | `≠`        | Типичный долг                                |
| ----------------- | ---------- | -------------------------------------------- |
| `registration`    | 14         | прогресс, поля, a11y                         |
| `settings-system` | 13         | шрифт, zoom, ярусы                           |
| `tips`            | 13         | копия, слои                                  |
| `nutrition-tab`   | 10         | зоны цвета, оставшиеся строки                |
| `checkin-morning` | 9          | сон, совет, a11y                             |
| `gamification`    | 9          | строки уровня (часть `=` после фикса тестов) |
| прочие            | ≤15 каждая | по `--list`                                  |

**Пятое — решения владельца, ещё не в коде.**

- Профиль — три группы вместо восьми аккордеонов (адрес каждого блока).
- Куратор правит добавки: четырёх действий журнала нет ни на клиенте, ни на
  сервере.

**Шестое — сквозные правила `home-widgets` (52 `≠`).**

- Пятнадцать сквозных правил тянут ~57 зонных отсылок.
- **Не чинить**, пока не приедет обновлённый пакет канваса (§3). Исключение:
  строки, уже закрытые кодом и помеченные `=` или осознанным `≠` (облако).

## 5. Следующие батчи для субагентов

Делить **по файлам**, не по темам — так параллель без конфликтов.

### ~~Батч A~~ — закрыт (guard-тесты, не продукт)

`nutrition-tab-v4-contract-fixes.test.js` (22/22 solo),
`nutrition-tab-v4-states.test.js` (22/22 solo); кластер с meals/gamification —
69/69 solo. Вердикты `=` по шкале зон, клетчатке и a11y оправданы: продукт не
ломался, обновлялись guard-файлы и ожидания контракта.

### Батч B — Чек-ин: a11y сна и советов _(следующий)_

**Файлы:** `apps/web/heys_step_modal_v1.js`, стили чек-ина в
`apps/web/styles/modules/` (grep `checkin-morning`, `mc-`), новый или
расширенный `apps/web/__tests__/morning-checkin-v4-a11y-smoke.test.js`

**Цель:** закрыть `≠` по капсуле сна и дорожке шагов/совета; progressbar уже
`=`.

**Не трогать:** `heys_day_nutrition_v1.js`, `heys_trial_intake_v1.js`.

### Батч C — Системный шрифт и zoom

**Файлы:** `apps/web/index.html` (viewport),
`apps/web/styles/modules/000-base-and-gamification.css`,
`apps/web/styles/modules/002-ui-v4-palette-roles.css` (типографика-токены), зона
`settings-system` — исходники настроек (grep `settings-system` в `apps/web/`)

**Цель:** убрать запрет zoom / пропустить системное масштабирование; не ломать
геометрию v4. Сверка со строками `settings-system` из `--list`.

**Не трогать:** виджеты, питание, чек-ин.

### Батч D — Листы разбора плитки (первая дюжина)

**Файлы:** `apps/web/heys_widgets_ui_v1.js`,
`apps/web/heys_widgets_variants_v4.js`,
`apps/web/styles/modules/730-ui-v4-widgets.css` (или актуальный widgets CSS)

**Цель:** двенадцать листов с данными; для шести «исторических» — заглушка «за
сегодня». Тесты — новый контрактный файл по образцу
`nutrition-v4-canvas-geometry.test.js`.

**Не трогать:** `heys_widgets_core_v1.js` (облако/layout — уже сделано),
`002-ui-v4-palette-roles.css` (лестница — уже `=`).

---

## 6. Правила работы

**Вердикт ставит тот, кто сводил экран, и в той же задаче.** Строка без вердикта
значит «никто не смотрел». Инструмент переноса —
`scratchpad/verdicts/apply-verdict.mjs`, хеши руками не считать.

**Вердикт — утверждение о коде, а не о тексте контракта.** Ставя `=`, назови
файл и строку, по которым это видно. «Похоже, что сделано» — это `?`.

**Закрытая запись расхождений сразу уезжает в `UI_V4_FINDINGS_HISTORY.md`**
вместе с решением.

**Отрицательный вывод требует двух независимых способов.** «Этого нет нигде» —
утверждение о всём множестве; один поиск даёт гипотезу.

**Кириллицу искать только через ripgrep** — shell-ный `grep -i` здесь регистр
кириллицы не сворачивает.

**Prettier на CSS не запускать** — переформатирует файл целиком и задевает чужие
зоны.

**Факты, снятые во время параллельной правки, перепроверять.**

## 7. Проверки

- `pnpm ui:v4:check` — неопределённые роли и дрейф контракта. Оба зелёными до
  конца задачи.
- `npx vitest run --root .` из `apps/web` — известный флак:
  `login-error-codes-contract` изредка падает в общем прогоне, в одиночку
  зелёный.
- Стыки, таймеры, роли и редкие даты — смоук-симуляцией, а не просьбой к
  человеку собрать условия.
- После правки легаси-файла, видимого в браузере:
  `pnpm bundle:legacy:auto --files=<свои файлы>`, затем `pnpm dev:local` и живая
  проверка.

## 8. Ждёт ответа — в код не тащить

Живые записи — в [`UI_V4_FINDINGS.md`](UI_V4_FINDINGS.md):

- **Три роли линии** не покрывают девять других процентов в восьми зонах (в т.ч.
  две строки самого `home-widgets`: волна 14 % / 22 %). Тест:
  `line-roles-v4.test.js`.
- **Бар отмены:** «Вернуть» vs «Отменить» — противоречие в канвасе.
- **Прокрутка:** общее правило «в начало при смене вкладки» vs «Питание» —
  сохранять при возврате. Код делает второе.
- **Ночь до 03:00** — кадра нет, долг дизайнера.
- **Три чипа «Питания» без полей профиля** — закрыто кодом этой сессией; при
  `--rehash` убрать из FINDINGS, если ещё висит.

## 9. Границы

Коммит — на усмотрение агента, source-only:
`HEYS_COMMIT_SOURCE_ONLY=1 git commit -F - -- <явные пути>`.

**Push, deploy, PR и любая внешняя публикация — только по отдельной прямой
команде.** Накоплено больше полутора сотен локальных коммитов, ничего не
отправлено.
