# Мёртвый цвет — список для чистки описей

Мёртвый цвет — шестнадцатеричное значение, лежащее в таблице рядом с подписью, к
которому код не обращается ни разу. Он опаснее живого: проходит обе описи
(`UI_V4_SAND_ROLES_INVENTORY.md` и счёт запасных значений) как настоящий долг,
съедает разметку владельца и однажды его «чинят» — переводят на роль, которую
никто не увидит.

Найден 31 августа при сведении кабинета: таблица состояний анкеты несла
восемнадцать таких значений. Ниже — тот же признак, развёрнутый на все зоны.
**Ничего не правил: зоны чужие.**

## Как искал

Признак: таблица, из которой берут только подпись. Проверка в три шага — (1)
найти именованные таблицы с тремя и более цветами; (2) выкинуть те, что уходят в
стиль целиком (`...NAME`, `style: NAME`) — там цвет живой; (3) для каждого
цветового ключа поискать чтение по имени (`.key`, `['key']`, деструктуризацию)
во всём остальном файле и по всем исходникам без `public/`.

**Что метод не видит.** Таблицу, которую индексируют переменной
(`TABLE[state].color`), он бы записал в мёртвые ошибочно, поэтому такие вынесены
отдельно и не считаются находкой. Так отсеялись `qualityColors`
(`pi_ui_cards.js`), `ZONE_META`, `CONFIRM_STYLES`: у них цвет и есть значение,
которое достают ключом. По той же причине из списка убраны `FALLBACK_CATEGORIES`
и `FALLBACK_PRIORITY_LEVELS` — их читает `pi_constants.js:74,78`
(`category.color`, `priorityLevel.color`), и `TYPE_LABELS` библиографии — её
цвет читает `_kernel/heys_kernel_bibliography_ui_v1.js:133`.

Каждая строка ниже проверена руками поиском по всем исходникам, а не только
скриптом.

## Подтверждено: цвет не читает никто

| файл                         | таблица                                                | мёртвых значений | что там                                                                                            |
| ---------------------------- | ------------------------------------------------------ | ---------------- | -------------------------------------------------------------------------------------------------- |
| `heys_consents_v1.js`        | `friendlySummaries`                                    | **18**           | вся таблица не упоминается нигде, кроме своего объявления: 6 × `color`, `borderColor`, `textColor` |
| `heys_gamification_v1.js`    | `RARITY_COLORS`                                        | **5**            | экспортируется как «константы для UI», читателей нет ни одного                                     |
| `heys_expandable_card_v1.js` | `COLORS`                                               | **5**            | `error`, `success`, `warning`, `bgDark`, `bgLight`; соседние ключи той же таблицы живые            |
| `heys_refeed_v1.js`          | `REFEED_ZONES`                                         | **4**            | `textColor` у четырёх зон; сами зоны читают, цвет текста — нет                                     |
| `heys_sparkline_utils_v1.js` | `SPARKLINE_CONFIG.colors`                              | **3**            | `forecast`, `retention`, `streakGold`; соседние цвета той же таблицы читают                        |
| `heys_iw_v41.js`             | `SATIETY_MODEL_CONFIG`, `METABOLIC_FLEXIBILITY_CONFIG` | **2**            | `.color` в файле не читается ни разу                                                               |
| `heys_trial_queue_v1.js`     | `INTAKE_STATUS`                                        | 18               | **снято** 31 августа коммитом `7ac7a8e3c`                                                          |

Итого к чистке: **37 значений** в шести файлах, плюс восемнадцать уже снятых.

## Почему это стоит сделать до разметки

Владелец садится размечать 581 роль и 2235 запасных значений. Тридцать семь из
них не существуют на экране: разметка на них уйдёт впустую, а разметив, их
однажды переведут на роли — и получится роль, которую никто никогда не увидит,
но которую все будут поддерживать.

Отдельно стоит `friendlySummaries`: это не цвет мимо набора, а восемьдесят пять
строк мёртвого текста с цветом внутри. Решать по нему нужно не «на какую роль
перевести», а «нужен ли экран, который его показывал».

## Сведение с описями (31 августа)

**Опись ролей `UI_V4_SAND_ROLES_INVENTORY.md` — пересечений нет.** Она описывает
роли `--v4-sand-*` в тринадцати модулях стилей, а мёртвый цвет весь лежит в
коде. Чистить там нечего.

**Опись литералов `UI_V4_BARE_LITERALS_INVENTORY.md` — четыре места из 2235.**

| строка описи                                | места                               | почему мёртвое                                                                  |
| ------------------------------------------- | ----------------------------------- | ------------------------------------------------------------------------------- |
| `heys_gamification_v1.js` → `RARITY_COLORS` | 1 (`#eab308`)                       | таблица экспортируется как «константы для UI», читателей нет ни одного          |
| `consents.js` → `CONSENT_TEXTS`             | 3 (`#fef3c7`, `#f59e0b`, `#92400e`) | это тёплая тройка из `friendlySummaries` — ветки, к которой не обращается никто |

Остальные 33 значения в опись не попали: они холодные (опись берёт оттенок
12–55°) либо лежат в файлах, которых опись не смотрела.

### Почему эти четыре нельзя было заметить

Опись приписывает литерал **ближайшему объявлению выше**. `friendlySummaries` —
свойство внутри `CONSENT_TEXTS`, а к самой `CONSENT_TEXTS` обращаются двадцать
раз. То есть мёртвая ветка спряталась под именем живой таблицы: по строке описи
видно живое имя, и разметчик разметит её как рабочую.

Это общий признак, а не частный случай: **любая мёртвая ветка внутри живой
таблицы будет выглядеть в описи живой.** Проверять нужно не имя из колонки «где
в коде», а путь до самого значения.

### Что это значит для разметки

Чистка не на критическом пути: четыре места из 2235 внимания владельца почти не
съедают. Полезнее другое — знать, что колонка «где в коде» называет ближайшее
объявление, а не владельца значения.

## Список со строками (для сверки счётчиками)

Каждое значение проверено **двумя способами**: чтением ключа по имени внутри
файла и поиском по всем исходникам без `public/` — включая внешних читателей
экспорта. Второй способ дважды менял вывод, поэтому он здесь не формальность.

```
heys_consents_v1.js:257,258,259   friendlySummaries  #dbeafe #3b82f6 #1e40af
heys_consents_v1.js:271,272,273   friendlySummaries  #dcfce7 #22c55e #166534
heys_consents_v1.js:285,286,287   friendlySummaries  #fce7f3 #ec4899 #9d174d
heys_consents_v1.js:299,300,301   friendlySummaries  #fef3c7 #f59e0b #92400e
heys_consents_v1.js:313,314,315   friendlySummaries  #ecfdf5 #10b981 #065f46
heys_consents_v1.js:327,328,329   friendlySummaries  #eef2ff #6366f1 #312e81
heys_gamification_v1.js:206-210   RARITY_COLORS      #94a3b8 #3b82f6 #a855f7 #eab308 #ef4444
heys_expandable_card_v1.js:13,14,15,19,20  COLORS    #22c55e #eab308 #ef4444 #f8fafc #1e293b
heys_iw_v41.js:82-86              METABOLIC_FLEXIBILITY_CONFIG.levels[].color
heys_iw_v41.js:235-239            SATIETY_MODEL_CONFIG.levels[].color
heys_sparkline_utils_v1.js:49,58,61  SPARKLINE_CONFIG.colors  #9ca3af #ec4899 #f59e0b
heys_refeed_v1.js:26,27,28,29     REFEED_ZONES[].textColor  #fff #fff #000 #fff
```

**Что проверка чуть не испортила.** `.textColor` читают снаружи —
`heys_day_hero_metrics.js:39,56` и `heys_scales_v1.js:379`. Но читают они **зоны
отношения**, а не зоны рефида: у первых идентификаторы `crash`, `low`, `good`,
`binge`, у вторых — `refeed_ok`, `refeed_over`, `refeed_under`, `refeed_binge`,
и внешний потребитель рефида (`heys_day_goal_progress_v1.js:160`) берёт из зоны
`color`, `icon`, `name`, но не `textColor`. Совпади имя ключа — и владелец вычел
бы из описи живые места.

Отсюда правило для любого такого списка: **совпадение имени ключа не значит
совпадения таблицы.** Проверять нужно, из какой таблицы пришёл объект у
читателя, а не то, что ключ где-то читается.
