# Мёртвый цвет — список для чистки описей

Мёртвый цвет — шестнадцатеричное значение, лежащее в таблице рядом с подписью, к
которому код не обращается ни разу. Он опаснее живого: проходит обе описи
(`UI_V4_SAND_ROLES_INVENTORY.md` и счёт запасных значений) как настоящий долг,
съедает разметку владельца и однажды его «чинят» — переводят на роль, которую
никто не увидит.

Найден 31 августа при сведении кабинета: таблица состояний анкеты несла
восемнадцать таких значений. Ниже — тот же признак, развёрнутый на все зоны.
**Ничего не правил: зоны чужие.**

## Как искал

Признак: таблица, из которой берут только подпись. Проверка в три шага — (1)
найти именованные таблицы с тремя и более цветами; (2) выкинуть те, что уходят в
стиль целиком (`...NAME`, `style: NAME`) — там цвет живой; (3) для каждого
цветового ключа поискать чтение по имени (`.key`, `['key']`, деструктуризацию)
во всём остальном файле и по всем исходникам без `public/`.

**Что метод не видит.** Таблицу, которую индексируют переменной
(`TABLE[state].color`), он бы записал в мёртвые ошибочно, поэтому такие вынесены
отдельно и не считаются находкой. Так отсеялись `qualityColors`
(`pi_ui_cards.js`), `ZONE_META`, `CONFIRM_STYLES`: у них цвет и есть значение,
которое достают ключом. По той же причине из списка убраны `FALLBACK_CATEGORIES`
и `FALLBACK_PRIORITY_LEVELS` — их читает `pi_constants.js:74,78`
(`category.color`, `priorityLevel.color`), и `TYPE_LABELS` библиографии — её
цвет читает `_kernel/heys_kernel_bibliography_ui_v1.js:133`.

Каждая строка ниже проверена руками поиском по всем исходникам, а не только
скриптом.

## Подтверждено: цвет не читает никто

| файл                         | таблица                                                | мёртвых значений | что там                                                                                            |
| ---------------------------- | ------------------------------------------------------ | ---------------- | -------------------------------------------------------------------------------------------------- |
| `heys_consents_v1.js`        | `friendlySummaries`                                    | **18**           | вся таблица не упоминается нигде, кроме своего объявления: 6 × `color`, `borderColor`, `textColor` |
| `heys_gamification_v1.js`    | `RARITY_COLORS`                                        | **5**            | экспортируется как «константы для UI», читателей нет ни одного                                     |
| `heys_expandable_card_v1.js` | `COLORS`                                               | **5**            | `error`, `success`, `warning`, `bgDark`, `bgLight`; соседние ключи той же таблицы живые            |
| `heys_refeed_v1.js`          | `REFEED_ZONES`                                         | **4**            | `textColor` у четырёх зон; сами зоны читают, цвет текста — нет                                     |
| `heys_sparkline_utils_v1.js` | `SPARKLINE_CONFIG.colors`                              | **3**            | `forecast`, `retention`, `streakGold`; соседние цвета той же таблицы читают                        |
| `heys_iw_v41.js`             | `SATIETY_MODEL_CONFIG`, `METABOLIC_FLEXIBILITY_CONFIG` | **2**            | `.color` в файле не читается ни разу                                                               |
| `heys_trial_queue_v1.js`     | `INTAKE_STATUS`                                        | 18               | **снято** 31 августа коммитом `7ac7a8e3c`                                                          |

Итого к чистке: **37 значений** в шести файлах, плюс восемнадцать уже снятых.

## Почему это стоит сделать до разметки

Владелец садится размечать 581 роль и 2235 запасных значений. Тридцать семь из
них не существуют на экране: разметка на них уйдёт впустую, а разметив, их
однажды переведут на роли — и получится роль, которую никто никогда не увидит,
но которую все будут поддерживать.

Отдельно стоит `friendlySummaries`: это не цвет мимо набора, а восемьдесят пять
строк мёртвого текста с цветом внутри. Решать по нему нужно не «на какую роль
перевести», а «нужен ли экран, который его показывал».
