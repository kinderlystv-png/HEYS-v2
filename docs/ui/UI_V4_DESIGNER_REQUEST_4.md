# Четвёртый заход к дизайнеру — накопленное с 1 сентября

Собрано 13 сентября 2026 из журнала [`UI_V4_FINDINGS.md`](UI_V4_FINDINGS.md) —
только открытые записи, то есть те, что ждут ответа. Решённое без дизайнера и
закрытое лежит в [`UI_V4_FINDINGS_HISTORY.md`](UI_V4_FINDINGS_HISTORY.md) и сюда
не попало.

**Источник правды — сам журнал.** Здесь срез: у каждой записи оставлено только
«что предлагаю», а разбор — «что нашли» и «чем это грозит» — по ссылке. Журнал
пополняется по ходу сведения экранов, поэтому срез верен на момент сборки.

**Объём.** Записей — 49, зон — 14. Предыдущий заход третий, 1 сентября, по
Конструктору; всё, что ниже, заведено после него.

| Зона                   | Записей |
| ---------------------- | ------- |
| Чек-ин утра            | 12      |
| Отчёты и инсайты       | 8       |
| Питание                | 5       |
| Вода                   | 4       |
| Главная                | 3       |
| Силовая                | 3       |
| Мелкие мёртвые правила | 3       |
| Календарь              | 3       |
| Мессенджер             | 2       |
| Дата и остатки         | 2       |
| Карточка продукта      | 1       |
| Настройки              | 1       |
| Кабинет куратора       | 1       |
| Подписка               | 1       |

---

## Чек-ин утра

### Выбранная оценка сна отличается только размером на 5 %

Строку контракта «вид · выбранная оценка сна»: чем именно отличается выбранная —
заливкой, рамкой, тоном подписи, — и ролями палитры. Мы сведём код под неё и
снимем мёртвые правила.

Разбор —
[`sleep-quality-selected-only-scales`](UI_V4_FINDINGS.md#sleep-quality-selected-only-scales).

### Средняя полоса дрейфа талии — состояния нет ни в одном кадре

Дизайнера: он сказал «дорисую среднее состояние» и «впишу оба уточнения
строкой». Как кадр придёт — сведём.

Разбор —
[`waist-middle-band-no-frame`](UI_V4_FINDINGS.md#waist-middle-band-no-frame).

### Кадр резервного вопроса рисует «не вели» прежним тоном 10 и 16 %

Перекрасить точки «не вели» в кадре шторки (и в кадре причины пропуска, если они
там есть) в 30 %, как в «Активе».

Разбор —
[`reserve-calendar-none-dots-old-tone`](UI_V4_FINDINGS.md#reserve-calendar-none-dots-old-tone).

### Чек-ин · развилка: строка контракта зовёт «Перед чек-ином» тоном `--ink-4`, кадр и разбор — `--ink-2`

Поправить прозу «вид развилки» на `var(--ink-2)` — за него кадр и разбор. Если
верна проза, перерисовать кадр и переснять разбор.

Разбор —
[`checkin-fork-caption-tone`](UI_V4_FINDINGS.md#checkin-fork-caption-tone).

### Чек-ин · остальное: доза добавки в кадре без разряда тысяч, а числа того же канваса — с разрядом

Перерисовать дозу в кадре с разрядом либо назвать строкой контракта, что дозы
добавок разряда не получают (и тогда сказать почему).

Разбор —
[`checkin-supplements-thousands-space`](UI_V4_FINDINGS.md#checkin-supplements-thousands-space).

### Чек-ин: на шаге веса серия 5, в итоге 6 — правило серии такой прибавки не даёт

Решение владельца: либо серия остаётся калорийной и в кадре «записано» тоже 5
(тогда перерисовать кадр), либо «законченный чек-ин» действительно добавляет
сегодняшний день — и тогда это правило называется строкой контракта, вместе с
тем, что происходит с ним вечером, если день по калориям в зону не попал.

Разбор —
[`checkin-streak-plus-one-on-summary`](UI_V4_FINDINGS.md#checkin-streak-plus-one-on-summary).

### Чек-ин · остальное: пилюли кофе не помещаются в строку на 375 — поля карточки по контракту шире, чем в кадре

Либо назвать строкой контракта, что карточка кофе — исключение и несёт поля
кадра (13/14), либо укоротить подписи пилюль, либо разрешить им кегль мельче
12,5 в этом ряду. Выбор за дизайнером: любое из трёх снимает перенос, но меняет
разные вещи.

Разбор —
[`checkin-coffee-pills-wrap-on-375`](UI_V4_FINDINGS.md#checkin-coffee-pills-wrap-on-375).

### Чек-ин · как вы сегодня: кадр подписывает 8 и 7 словом «хорошо», продукт держит нейтральный словарь

Перерисовать подписи в кадре нейтральным словарём продукта («выше обычного»),
либо снять запрет строкой контракта и назвать полный список слов для 1–10, чтобы
его можно было сверять.

Разбор —
[`checkin-mood-scale-words`](UI_V4_FINDINGS.md#checkin-mood-scale-words).

### Чек-ин: два кадра на девяти днях без замеров говорят разное

Перерисовать «Чек-ин · остальное» на днях до порога (скажем, «Прошло 5 дней с
прошлых») либо назвать порог заново.

Разбор —
[`checkin-measurements-overdue-frames-disagree`](UI_V4_FINDINGS.md#checkin-measurements-overdue-frames-disagree).

### Чек-ин · замеры: подпись под кнопкой центрируется, владелец просит сноску слева снизу

Перерисовать кадр «Чек-ин · замеры» с подписью слева внизу блока и переписать
строку «· 19»: выключка `left`, место — подвал карточки. Заодно назвать отступ
от того, что стоит выше: сейчас 2px привязаны к кнопке.

Разбор —
[`checkin-measurements-caption-centered`](UI_V4_FINDINGS.md#checkin-measurements-caption-centered).

### Чек-ин · замеры: переключатель стороны не объясняет, что сторону достаточно держать одну

Добавить сноску при ряду «Сторона» — смысл: обе стороны мерить не нужно,
выберите одну и держитесь её. Формулировка за копирайтом
(`apps/landing/COPY_VOICE.md`), место — рядом с рядом стороны или в той же
подвальной сноске, что и «Можно заполнить только талию»; вместе они и составят
подвал блока.

Разбор —
[`checkin-measurements-same-side-note`](UI_V4_FINDINGS.md#checkin-measurements-same-side-note).

### Чек-ин · остальное: «Последний кофе» спрашивают утром — про день, которого ещё не было

**Решение уже принято.** Карточка остаётся в утреннем чек-ине и спрашивает про
**вчера**: ответ пишется во вчерашнюю запись дня, порог считается до вчерашнего
отбоя. Вечерний вопрос отвергнут — вечернего входа, в который человек заходит
так же надёжно, как в чек-ин, нет. Разбор —
[`UI_V4_OWNER_DECISIONS.md`](UI_V4_OWNER_DECISIONS.md).

Перерисовать кадр «Чек-ин · остальное» так, чтобы день был назван, и переписать
строки «последний кофе» и «кофе не обязателен»: сейчас ни одна не говорит, про
какой день вопрос. Без слова «вчера» на экране человек ответит про сегодня, и
данные испортятся сильнее нынешнего — поэтому код ждёт кадр, а не идёт вперёд.

Разбор —
[`checkin-last-coffee-asked-before-the-day`](UI_V4_FINDINGS.md#checkin-last-coffee-asked-before-the-day).

## Отчёты и инсайты

### Инсайты · «Что заметили»: по какому правилу на первом слое три строки, а не девятнадцать

Ничего не выключать — разложить по двум слоям, как требует правило «простое по
умолчанию»:

Разбор —
[`insights-what-we-noticed-first-layer-rule`](UI_V4_FINDINGS.md#insights-what-we-noticed-first-layer-rule).

### Отчёты: нижняя половина вкладки осталась в прежнем оформлении

Решить судьбу трёх блоков отдельно от отступов: они либо переносятся в кадры
зоны и переводятся на набор, либо уезжают в другое место («Актив», карточка
дня), либо снимаются. Пока решения нет, код их не трогает.

Разбор —
[`reports-tab-second-half-is-pre-v4`](UI_V4_FINDINGS.md#reports-tab-second-half-is-pre-v4).

### Инсайты: карточка риска срыва не выходит даже на предельных данных

Замерить сам счёт на этих данных и назвать число: если он, скажем, 38 при пороге
60 — вопрос к порогу и к дизайнеру; если снимок пуст — это дефект расчёта и
чинится кодом.

Разбор —
[`insights-relapse-risk-card-unreachable`](UI_V4_FINDINGS.md#insights-relapse-risk-card-unreachable).

### Инсайты: счёт риска на предельных данных — 39 при пороге 60

Спросить дизайнера: карточка с «elevated» (40) или порог остаётся 60 и кадр
перерисовывается на «повышенный»? Порог в коде не двигаю: строка контракта
«порог карточки риска срыва» прямо говорит «60 не опускаем — риск обязан быть
редким», и менять его без нового решения нельзя.

Разбор —
[`insights-relapse-risk-threshold-measured`](UI_V4_FINDINGS.md#insights-relapse-risk-threshold-measured).

### Предупреждения видят ноль калорий в днях, где еда записана

Проверить, что приходит в индексе на момент расчёта, и решить: ждать индекс или
считать по собственным макросам позиции. Молча возвращать ноль нельзя — это
«число, которого нет», и по контракту зоны такая строка не должна показываться
вовсе.

Разбор — [`ews-eaten-kcal-zero`](UI_V4_FINDINGS.md#ews-eaten-kcal-zero).

### Инсайты: подпись «Подробно» потеряла свою строку в кадре

Вернуть строку подписи: кегль, вес и тон. Если она снята намеренно и подпись
теперь наследует что-то общее — назвать, что именно наследует, чтобы было с чем
сверять.

Разбор —
[`insights-detail-link-label-lost-its-line`](UI_V4_FINDINGS.md#insights-detail-link-label-lost-its-line).

### Инсайты: правило зоны требует «вы», а её собственный ярус называется «Сделай сегодня»

Переименовать ярус — «Сегодня стоит», «Что сделать сегодня» или «Главное на
сегодня»; выбор за копирайтом. Либо назвать подпись яруса исключением и сказать
почему: заголовок как призыв — решение защитимое, но его надо принять вслух, а
не оставлять расхождением.

Разбор —
[`insights-tier-label-says-ty`](UI_V4_FINDINGS.md#insights-tier-label-says-ty).

### Инсайты · «Подробно»: карточек метаболизма четыре, а кадр рисует одну строку

Назвать состав яруса: сколько карточек, в каком порядке и что делать с теми, у
кого числа ещё нет. Если верна одна строка — сказать, куда деваются остальные
четыре: в отдельный экран, в раскрывашку или вовсе.

Разбор —
[`insights-detail-metabolism-cards-not-in-frame`](UI_V4_FINDINGS.md#insights-detail-metabolism-cards-not-in-frame).

## Питание

### Питание: четыре кадра рисуют снятое состояние героя при переборе

Переснять три кадра зон и герой кадра «перебор» по строкам «перебор» и «состав
героя»; в `<style>` канваса красить `b` по состоянию (`.hn.warn b`,
`.hn.over b`) вместо инлайна на родителе, иначе строки «· 04» остаются
непроверяемыми.

Разбор —
[`nutrition-hero-over-label-and-tone`](UI_V4_FINDINGS.md#nutrition-hero-over-label-and-tone).

### Питание: кадр листа правки приёма рисует свайп, снятый решением 3 сентября

Переснять кадр со строками состава и крестиком 14 px в цели 44, снять строки
контракта «· 06…11» и подпись про свайп.

Разбор —
[`nutrition-meal-sheet-swipe-frame`](UI_V4_FINDINGS.md#nutrition-meal-sheet-swipe-frame).

### Питание: кадр выносит пустой приём в отдельную карточку, строка контракта запрещает

Переснять кадр одним списком либо снять запрет в строке «пустой приём».

Разбор —
[`nutrition-empty-meal-own-card`](UI_V4_FINDINGS.md#nutrition-empty-meal-own-card).

### Питание: кадр «приём раскрыт» помечен разом `stop` и `protocol`

Оставить один атрибут. Если кадр иллюстративный — `protocol`; если он
нормативный — назвать строкой контракта, что именно на вкладке «Питания» он
описывает.

Разбор —
[`nutrition-expanded-meal-double-demo`](UI_V4_FINDINGS.md#nutrition-expanded-meal-double-demo).

### Питание: числа в двух кадрах не сходятся со своим же составом

Пересчитать числа в кадрах по формуле продукта либо назвать строкой контракта,
что суммы в кадрах иллюстративные и сверке не подлежат.

Разбор —
[`nutrition-frame-numbers-do-not-add-up`](UI_V4_FINDINGS.md#nutrition-frame-numbers-do-not-add-up).

## Вода

### Вода · «Чипы объёма»: кадры рисуют ряд у плавающей кнопки воды, которой в продукте нет

Свести три строки к одному месту ряда: либо кадры перерисовать на карточку воды,
либо строкой контракта сказать, что «Чипы объёма» — протокол снятой ветки.
Заодно назвать состав чипов в карточке быстрых действий: «два самых частых»
контракта против четырёх пресетов кода.

Разбор —
[`water-chips-frames-draw-removed-fab-row`](UI_V4_FINDINGS.md#water-chips-frames-draw-removed-fab-row).

### Вода · карточка «Кольцо»: два канваса рисуют её по-разному, продукт следует второму

Назвать владельца карточки строкой контракта и привести отстающий кадр к нему;
заодно решить про подписи дней под кривой — оставить (и дорисовать в кадре) или
снять.

Разбор —
[`water-ring-card-two-canvases-disagree`](UI_V4_FINDINGS.md#water-ring-card-two-canvases-disagree).

### Вода · «Чипы объёма · нажатие»: кадр сжимает чип, контракт гасит его

Либо убрать `scale(.96)` из кадра, либо назвать сжатие местным отличием зоны
отдельной строкой.

Разбор —
[`water-chip-press-scale-vs-dim`](UI_V4_FINDINGS.md#water-chip-press-scale-vs-dim).

### Вода: карточка в продукте собрана не как кадр «Кольцо»

Назвать канон: карточка «Кольцо» или «Полоса» (в контракте описаны оба вида, и в
обоих «−200» стоит перед «+200»), и сказать, остаются ли «последний раз» и
подписи дней. Дальше сведём одним заходом.

Разбор —
[`water-card-ring-vs-strip-layout`](UI_V4_FINDINGS.md#water-card-ring-vs-strip-layout).

## Главная

### Листы смены вида ставят выбранный вид первым, а контракт это прямо запрещает

Перерисовать два кадра по возрастанию формата — либо снять из строки «порядок
карточек в листе» запрет на порядок выбора и назвать выбранную карточку первой.

Разбор —
[`home-variant-sheet-order-by-choice`](UI_V4_FINDINGS.md#home-variant-sheet-order-by-choice).

### Кадр «Калории · состояние · В норме» рисует заливку 30 % при доле 67 %

Перерисовать заливку кадра на 67 % (1 289 из 1 931) — либо поменять числа кадра
под 30 %.

Разбор — [`home-kcal-norm-bar-30`](UI_V4_FINDINGS.md#home-kcal-norm-bar-30).

### У вида «График» динамики веса нет своего кадра, а превью и правило спорят

Нарисовать кадр «Динамика веса · График» — или строкой контракта назвать, что у
этого вида в шапке и что крупным числом.

Разбор —
[`home-weight-chart-view-kicker`](UI_V4_FINDINGS.md#home-weight-chart-view-kicker).

## Силовая

### Силовая: кадры отстают от решений и спорят с логикой экрана

Дизайнера: перерисовать звезду в конец строки; сказать, ряд фильтров при 14
группах — одна строка с прокруткой или перенос; дорисовать выход в «Связка ·
создание». Владельца: строить ли перестановку упражнений и шаблоны тренировок —
это новые функции, а не правка вида.

Разбор —
[`strength-builder-frames-behind-decisions`](UI_V4_FINDINGS.md#strength-builder-frames-behind-decisions).

### Силовая · «Разминка и дроп-сет»: пара разошлась по цвету и счёту ·

Слово на кнопке — вопрос дизайнеру: «Сброс» против «дроп» в ярлыке той же
таблицы.

Разбор —
[`strength-warmup-drop-frame-vs-product`](UI_V4_FINDINGS.md#strength-warmup-drop-frame-vs-product).

### Силовая: правило зоны красит сделанный номер заливкой, кадр — обводкой

Оставить одно. Довод за заливку: в списке из восьми упражнений обводка читается
как «выделено», а не как «закрыто», и отличить её от текущего можно только по
цвету. Довод за обводку: закрытое не должно перетягивать внимание с текущего.
Решение за дизайнером, но названо должно быть одним источником — сейчас их два и
они спорят.

Разбор —
[`strength-done-number-fill-or-outline`](UI_V4_FINDINGS.md#strength-done-number-fill-or-outline).

## Мелкие мёртвые правила

### Подсветка фокуса строки поиска написана на имя, которого код не выводит

Либо строка контракта про фокус — и тогда мы правим имя класса, либо решение
«фокус не показываем» — и тогда правило снимается.

Разбор —
[`search-field-focus-never-applies`](UI_V4_FINDINGS.md#search-field-focus-never-applies).

### Четыре правила переключателя в разборе вреда — их никто не рисует

Снять четыре правила, если переключателя в кадрах разбора нет.

Разбор — [`harm-radio-dead-rules`](UI_V4_FINDINGS.md#harm-radio-dead-rules).

### Девять правил кнопок шага «Шаги» — их не рисует ни одна строка кода

Удалить девять правил. Если кнопки шага задумывались и не доехали — сказать об
этом, и тогда правила остаются с пометкой «ждут разметки», а не молча.

Разбор — [`mc-steps-btn-dead-rules`](UI_V4_FINDINGS.md#mc-steps-btn-dead-rules).

## Календарь

### Календарь · легенда: кадр спорит со строкой «вид клетки», часть спора решил владелец

Переписать строку «вид клетки» под решение владельца (сегодня — заливка, выбран
— обводка, образцы легенды те же) и назвать одну форму метки цикла: лента сверху
либо полоска слева, но одинаково в клетке, в образце легенды и в обмерах.
Регистр дней назвать заново.

Разбор —
[`calendar-legend-frame-behind-its-own-contract`](UI_V4_FINDINGS.md#calendar-legend-frame-behind-its-own-contract).

### Календарь: полосу «Серия · N дней» и подпись прогноза цикла не рисует ни кадр, ни строка

Решить: оставить (тогда строка контракта и место в кадре) или снять.

Разбор —
[`calendar-sheet-streak-strip-has-no-frame`](UI_V4_FINDINGS.md#calendar-sheet-streak-strip-has-no-frame).

### Календарь: контракт требует подложку без размытия, продукт размывает по общему правилу

Решение владельца: либо блюр остаётся и строка зоны переписывается, либо в
инварианте появляется именованный список исключений из двух шторок. Код до
ответа не тронут.

Разбор —
[`calendar-sheet-scrim-blur-vs-project-rule`](UI_V4_FINDINGS.md#calendar-sheet-scrim-blur-vs-project-rule).

## Мессенджер

### Мессенджер: подзаголовок шапки и инициалы куратора — кадры рисуют данные, которых нет

Механику подзаголовка: при каком условии какая строка и откуда она берётся
(например, «прочитал(а) · время» — момент, когда куратор открыл последнее своё
сообщение клиента; «отвечает обычно …» — только при посчитанном среднем). Для
инициалов одного слова — одна буква или две.

Разбор —
[`messenger-header-subtitle-no-source`](UI_V4_FINDINGS.md#messenger-header-subtitle-no-source).

### Мессенджер: кадры опускают то, что путь к состоянию обязательно показывает

По каждому пункту — строка контракта или правка кадра.

Разбор —
[`messenger-frames-omit-real-path`](UI_V4_FINDINGS.md#messenger-frames-omit-real-path).

## Дата и остатки

### Дата: строку «вид липкого слоя» заменили новой, а старую из контракта не убрали

Убрать строку «вид липкого слоя» из контракта: её содержание целиком заменено
новой.

Разбор —
[`date-sticky-two-lines-old-one-not-deleted`](UI_V4_FINDINGS.md#date-sticky-two-lines-old-one-not-deleted).

### Дата: верхнее поле липкого ряда — 11 у вкладки против 16 у строки зоны

Сказать, к чему относится 16: к ряду вообще или только к ряду вне вкладки
«Питание». Код не трогаем до ответа — цена ошибки здесь не отступ в пять
пикселей, а сдвиг шапки целой вкладки.

Разбор —
[`date-sticky-row-padding-owned-by-tab`](UI_V4_FINDINGS.md#date-sticky-row-padding-owned-by-tab).

## Карточка продукта

### Раздел «Нутриенты · детализация» в правке продукта — в кадрах его нет

Дизайнера: где в правке живут подробные нутриенты — в этом шаге вторым слоем
(тогда нужен кадр и строка контракта), в шаге «Дополнительно», как у создания,
или в правке их нет вовсе.

Разбор —
[`product-edit-nutrients-accordion-no-frame`](UI_V4_FINDINGS.md#product-edit-nutrients-accordion-no-frame).

## Настройки

### Состав настроек: строки контракта спорят между собой, двух рядов в продукте нет

Дизайнера: один перечень рядов корневого списка с «Системой», «Данными и
выгрузкой» и «Настроить подробно» на своих местах; куда ведёт ряд «Дневник» — в
кадрах не нарисовано, продукт ведёт к ряду чипов вкладки.

Разбор —
[`settings-app-tier-rows-conflict`](UI_V4_FINDINGS.md#settings-app-tier-rows-conflict).

## Кабинет куратора

### Лист куратора: граница «число словами» и подписи без источника данных

Строкой контракта: до какого числа счёт словами (или «всегда словами, кроме …»).
По трём подписям — механика: при каком условии каждая показывается и откуда
берётся (например, «задним числом» — дата правки раньше даты её записи). Без
условия подписи не рисуем.

Разбор —
[`curator-sheet-number-words-and-provenance`](UI_V4_FINDINGS.md#curator-sheet-number-words-and-provenance).

## Подписка

### Подписка · тост на действии: кадр пишет одну фразу, а код — свою на каждое действие

Решить строкой контракта: одна фраза для всех действий (как в кадре) или
«действие + причина» по шаблону, например «Приём пищи не добавить — только
чтение». Код до ответа не тронут.

Разбор —
[`subscription-blocked-toast-per-action-copy`](UI_V4_FINDINGS.md#subscription-blocked-toast-per-action-copy).
