# Расхождения с контрактом v4 — открытые вопросы

Здесь лежит только то, что **ждёт решения**. Закрытая запись уезжает в
[`UI_V4_FINDINGS_HISTORY.md`](UI_V4_FINDINGS_HISTORY.md) вместе с решением —
иначе дизайнер получает файл, в котором нужное тонет среди уже разобранного.

Сюда идёт то, что нашлось при сведении экрана с пакетом
[`docs/ui/handoff-v4/canvas/Переработка дизайна приложения/`](handoff-v4/README.md)
и **меняет контракт, кадр или приёмочный лист**. Не баг-трекер: дефект кода, где
контракт прав и однозначен, чинится молча и живёт в протоколе своего экрана.

**Формат записи:** что нашли · чем это грозит · что предлагаю · чей ход.

**Статусы:** `?` ждёт дизайнера · `!` решено без дизайнера (тогда рядом — почему
нельзя было ждать).

---

**Разложено по адресату:** дизайнер отвечает за кадры, контракт и приёмочный
лист; владелец — за продуктовые и палитровые решения; зона — за свой код.
Запись, у которой ход дизайнера закрыт, уезжает в архив целиком, а её открытый
хвост остаётся здесь короткой записью со ссылкой на разбор.

---

## Ждут дизайнера

<a id="messenger-four-contract-nodes"></a>

### `?` Четыре узла messenger без строки контракта

**Что нашли.** При переводе зоны на роли набора нашлись четыре места, которым
контракт не называет ни цвета, ни роли: полоса прокрутки ленты, фон своей
звуковой записи (белая `rgba`), подложка ожидающего оверлея и заглушка вложения.
Плюс семь литералов затемнения лайтбокса в поле ввода — тоже без строки.

**Ответ дизайнера (пакет 48).** Согласен дописать строки — дешевле сейчас.
Разбор:
[`UI_V4_FINDINGS_HISTORY.md#otvet22-messenger-contract-nodes-2026-09-07`](UI_V4_FINDINGS_HISTORY.md#otvet22-messenger-contract-nodes-2026-09-07).

**Что осталось.** Дизайнер дописывает контракт по таблице ниже; код не меняем до
строк.

| Узел                        | Селектор в продукте                                                  | Сейчас в коде                                                      |
| --------------------------- | -------------------------------------------------------------------- | ------------------------------------------------------------------ |
| Полоса прокрутки ленты      | `.messenger-thread` (`::-webkit-scrollbar-thumb`, `scrollbar-color`) | `rgba(100, 116, 139, 0.22)` — без роли                             |
| Фон своей звуковой записи   | `.msg-audio` (свой пузырь)                                           | фон `rgba(255,255,255,0.16)`, border/play `rgba(255,255,255,0.18)` |
| Подложка ожидающего оверлея | `.msg-attachment-pending`                                            | `rgba(0,0,0,0.4)`, текст `var(--v4-bg)`                            |
| Заглушка вложения           | `.msg-attachment-item`                                               | `var(--v4-accent-bg, #f0dcc6)`                                     |

Семь литералов затемнения лайтбокса в поле ввода — отдельный долг, не входят в
эти четыре.

### `?` Планирование: кнопка в строке задачи цели — припуск не влезает · planning

**Что нашли.** `.planning-goals-workspace__task button` — текстовая кнопка в
сетке `18px | 1fr | auto`, `min-height` ряда 34 px, `gap` 7 px. Припуск ±15 px
по вертикали наезжает на соседнюю колонку и соседний ряд.

**Чем это грозит.** Либо увеличиваем высоту ряда/зазор в кадре, либо переносим
действие в меню 44 px.

**Что предлагаю.** Тап по всей строке задачи или отдельная иконка 32→44 с
увеличением `min-height` ряда до 44.

**Чей ход.** Дизайнер: переснять карточку цели или закрепить исключение.

### `?` Отчёты/инсайты: узкий столбик энергии — припуск ±8 px на соседа · reports-insights

**Что нашли.** `.adv-analytics__energy-bar` — интерактивный столбик шириной 16
px в плотном ряду графика; симметричный припуск до 44×44 наезжает на соседний
столбик и подпись.

**Чем это грозит.** Либо увеличиваем шаг ряда/зазор в кадре, либо переносим
действие на всю строку метрики.

**Что предлагаю.** Отдельный hit-row 44 px под столбиком или исключение в
контракте «области нажатия» для узких bar-узлов.

**Чей ход.** Дизайнер: переснять ряд advanced analytics или закрепить
исключение.

### `?` Отчёты: «развернуть неделю» — одна строка 15 px · reports-insights

**Что нашли.** `.monthly-week-card__expand-btn` — текстовая ссылка `line-height`
≈15 px в плотном списке недель; вертикальный припуск ±15 px перекрывает строки
соседних недель.

**Чем это gрозит.** Цель 44 px либо раздувает карточку, либо крадёт тап у
соседней недели.

**Что предлагаю.** Тап по всей шапке недели (как у `.cascade-card__header`) или
отдельная chevron-кнопка 32→44 с увеличением `padding` ряда в кадре.

**Чей ход.** Дизайнер: переснять monthly week card или закрепить hit-row.

---

## Ждут владельца

<a id="norm-correction-nc5-stable-girths"></a>

### `?` norm-correction · NC5 · строка 07 · текст про обхваты недостижим на Pro

**Ответ дизайнера (пакет 48).** Нет — в
[`ОТВЕТ-отступления-для-дизайнера.md`](handoff-v4/canvas/Переработка%20дизайна%20приложения/design_handoff_heys_v4/ОТВЕТ-отступления-для-дизайнера.md)
и
[`ОТВЕТ-расхождения-findings.md`](handoff-v4/canvas/Переработка%20дизайна%20приложения/design_handoff_heys_v4/ОТВЕТ-расхождения-findings.md)
строки ·07 и ветка «вес и обхваты» не разбирались.

**Что нашли.** Кадр «Сверка · норма снизилась · 07» требует прозу «Три недели
вес и обхваты держатся на месте». Продукт умеет показать её только когда в
снимке решения зафиксированы **не менее двух стабильных обхватов за 14 дней**
(талия, бедро или бицепс). Серверное окно куратора отдаёт **только талию** —
других обхватов в данных нет. Поэтому кураторский Pro-flow сохраняет либо
«замеров не было», либо «есть только талия», и до текста канваса ·07 не доходит.
Соседняя ветка «без обхватов» уже сведена и закрыта отдельным кадром.

**Что видит человек сейчас (Pro, куратор применил снижение).**

| Ситуация                                  | Что видит клиент                                                                                                                                        | Что **не** видит                                            |
| ----------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------- |
| Замеров обхватов не было                  | Карточка «Норму подстроили под факт» с текстом «Три недели **вес** держится на месте. **Замеров обхватов не было**…» и строкой «Замер талии · не было»  | Текст канваса ·07 про «вес **и обхваты** держатся на месте» |
| Есть замеры **только талии**              | Та же карточка, но другой текст: «…вес держится на месте, но **стабильность обхватов не подтверждена**. Расчёт расхода подстроили по доступным данным…» | Текст канваса ·07; отдельного кадра под «только талия» нет  |
| Два и более стабильных обхвата за 14 дней | Текст канваса ·07 дословно                                                                                                                              | —                                                           |

Сколько клиентов попадает в каждую строку — **не знаем** (нет агрегата по
evidence в проде).

**Чем это грозит.** Закрыть строку ·07 в «=» без решения — фиктивное совпадение:
экран с полным текстом про обхваты **ни разу не показывается** в текущем
кураторском контуре. Зона norm-correction не закроется, пока вердикт «?».

**Варианты (нужен выбор владельца).**

|       | Суть                                                                                                                                                                                     | Цена                                                                                             | Что получает человек                                                                                              |
| ----- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------- |
| **A** | Сервер начинает отдавать куратору **бедро и бицепс** (не только талию); клиент считает стабильность по тем же правилам, что уже есть в движке                                            | Миграция RPC и выгрузки окна; проверка, что замеры реально пишутся клиентами; затем сведение ·07 | При двух стабильных обхватах — текст канваса ·07 как задумано                                                     |
| **B** | Кадр ·07 **ограничить** тем, что сервер может подтвердить сегодня: убрать из контракта обещание «обхваты держатся», оставить кадр только для гипотетического будущего или снять с релиза | Решение дизайнера по контракту/кадру; правка канваса; пересъёмка вердиктов NC5                   | Честный контракт без текста, который продукт не может показать                                                    |
| **C** | **Отдельная проза**, когда есть только талия (третья ветка рядом с «без обхватов» и «вес и обхваты»)                                                                                     | Текст от дизайнера + кадр/строки контракта + код ветки; не требует новых полей сервера           | Клиент с замерами талии видит осмысленное объяснение, а не общий fallback «стабильность обхватов не подтверждена» |

**Рекомендация.** **C**, если расширять сервер сейчас дорого: талия уже
приходит, а fallback-text не совпадает ни с канвасом, ни с честностью «без
обхватов». **A** — если продуктовая цель «обхваты в решении куратора» важнее
срока.

**Если решения нет.** Вердикт «Сверка · норма снизилась · 07» остаётся «?»; зона
norm-correction не закрывается.

**Чей ход.** Владелец (продукт + server-contract). После ответа — дизайнер
(ветка/контракт) и код.

<a id="strength-g3-owner-confirm-tail"></a>

### `?` Карточка плана Г3: подтвердить разнос «Перенести»/«Пропустить» · strength-builder

**Хвост после ответа дизайнера (пакет 48).** Продукт верен, кадр Г3 устарел —
дизайнер переснимает под два исхода. Разбор:
[`UI_V4_FINDINGS_HISTORY.md#otvet22-g3-plan-card-reresolve-2026-09-07`](UI_V4_FINDINGS_HISTORY.md#otvet22-g3-plan-card-reresolve-2026-09-07).

**Что осталось.** Владелец подтверждает, что разнос `moved` ≠ `skipped`
окончателен и кадр можно обновлять без отката продукта.

<a id="nutrition-advice-replacement-owner-tail"></a>

### `?` «Совет с заменой»: нужна ли механика замены · nutrition-tab / tips

**Хвост после ответа дизайнера (пакет 48).** Строка замены снята с контракта
дневника; место замены — карточка совета, когда механика появится. Разбор:
[`UI_V4_FINDINGS_HISTORY.md#otvet22-advice-replacement-location-2026-09-07`](UI_V4_FINDINGS_HISTORY.md#otvet22-advice-replacement-location-2026-09-07).

**Что осталось.** Решение владельца № 35 (блок совета в приёме): нужна ли замена
вообще и как рисовать «было → станет».

---

## Ждут нас

<a id="p48-deviation-classes-key-list"></a>

### `?` Пакет 48 · 146 строк «композиция/UX» и «функциональный поток» — список ключей

**Что просит дизайнер.** Для двух классов отступлений (124 + 22 = 146 строк)
нужен список ключей с одной строкой «что именно разошлось» на каждый. Критерий
классификации — у дизайнера (исход действия vs порядок/группировка). Правила:
[`UI_V4_FINDINGS_HISTORY.md#p48-composition-ux-review-criterion-2026-09-05`](UI_V4_FINDINGS_HISTORY.md#p48-composition-ux-review-criterion-2026-09-05).

**Чей ход.** Кодер — собрать список; дизайнер — разобрать поштучно.

<a id="p48-deviation-singletons-facts"></a>

### `?` Пакет 48 · четыре одиночных отступления — факты из кода

**Что просит дизайнер.** Факты по четырём ключам (не класс):
`app-splash · уменьшенное движение`; `curator-edits · переход по строке`;
`registration · Документ · конец · 05`; `service-curator · фича`. Сводка:
[`UI_V4_FINDINGS_HISTORY.md#p48-deviation-singletons-2026-09-05`](UI_V4_FINDINGS_HISTORY.md#p48-deviation-singletons-2026-09-05).

**Чей ход.** Кодер — ответить фактами; дизайнер — закрыть в один заход.

<a id="p48-dead-css-cleanup"></a>

### `!` Полоса 5: мёртвые CSS-классы в инвентаре тач-целей · planning / settings

**Решение дизайнера (пакет 48).** Удалять — задела под будущий экран нет.
Разбор:
[`UI_V4_FINDINGS_HISTORY.md#otvet22-dead-css-remove-2026-09-07`](UI_V4_FINDINGS_HISTORY.md#otvet22-dead-css-remove-2026-09-07).

**Что осталось в коде.** Подтвердить мёртвость и вычистить селекторы:
`.tab-settings-item`, `.planning-filter-btn`, `.planning-modal__segment-button`,
`.planning-modal__icon-btn`, `.planning-goals-workspace__focus-actions button`,
`.chrono-overview__toggle`, `.chrono-duration__target-period-btn`,
`.curator-dropdown-switch__tab`.
