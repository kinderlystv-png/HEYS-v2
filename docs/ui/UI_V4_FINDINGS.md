# Расхождения с контрактом v4 — открытые вопросы

Здесь лежит только то, что **ждёт решения**. Закрытая запись уезжает в
[`UI_V4_FINDINGS_HISTORY.md`](UI_V4_FINDINGS_HISTORY.md) вместе с решением —
иначе дизайнер получает файл, в котором нужное тонет среди уже разобранного.

Сюда идёт то, что нашлось при сведении экрана с пакетом
[`docs/ui/handoff-v4/canvas/Переработка дизайна приложения/`](handoff-v4/README.md)
и **меняет контракт, кадр или приёмочный лист**. Не баг-трекер: дефект кода, где
контракт прав и однозначен, чинится молча и живёт в протоколе своего экрана.

**Формат записи:** что нашли · чем это грозит · что предлагаю · чей ход.

**Статусы:** `?` ждёт дизайнера · `!` решено без дизайнера (тогда рядом — почему
нельзя было ждать).

---

## Ждут ответа · прочие

### `?` Лист «вопрос о дате»: равный выбор дат vs склонение к сегодня · nutrition-tab

**Что нашли.** Кадр даёт две равнозначные строки-даты и кнопку «Записать на …»;
продукт (`day/_meals.js`) показывает предупреждение с главной «Перейти на
сегодня» и вторичной «Всё-таки записать на {дата}». Смысл один — не дать
записать в прошлый день по невнимательности.

**Чем это грозит.** Свести к кадру — убрать дефолт «сегодня»; оставить код —
расходится с равным выбором на макете.

**Что предлагаю.** Оставить склонение к сегодня (меньше ошибок) и обновить кадр/
контракт под продуктовый flow.

**Чей ход.** Владельца/дизайнера.

---

### `?` Лист «вопрос о дате»: кнопка «Отмена» рядом с удалением · nutrition-tab

**Что нашли.** Кадр 08 даёт отдельную «Отмену» под основным действием. Лист
закрывается подложкой и крестиком; третий выход рядом с «Удалить приём» путает.

**Что предлагаю.** Не добавлять «Отмену»; убрать строку из кадра или пометить
`protocol`.

**Чей ход.** Дизайнера.

---

### `?` «Совет с заменой»: строка «Заменить → …» в карточке совета · nutrition-tab

**Что нашли.** Контракт требует строку замены продукта внутри карточки совета.
MealOptimizerSection предлагает продукты кнопками «+», без «Заменить → …».

**Чем это грозит.** Нужна отдельная механика замены (не только добавления) и
копия как у замены под строкой состава.

**Чей ход.** Продукт после решения №35 (блок совета в приёме).

---

### `?` Раскрытый приём в списке: «Состав и нутриенты» и inline-замена · nutrition-tab

**Что нашли.** Кадр «приём раскрыт» показывает продукты, «Замена: …» и ссылку
«Состав и нутриенты» внутри строки дневника. NutritionTabV4 открывает лист
правки по тапу, inline-раскрытия нет.

**Чем это грозит.** Крупная доработка списка приёмов, не косметика листа.

**Чей ход.** Дизайнера/владельца — оставить лист или вернуть inline-раскрытие.

---

### `?` Вторичный текст 12 px на `--c2` недобирает 0,02 в песочном наборе

**Что нашли.** Пара «`--v4-ink-2` на `--v4-hero`» при 12 px даёт **4,48** при
пороге 4,5 (на `--v4-surface` — 4,61, проходит). `--v4-ink-2` — штатная ступень
лестницы для вторичного текста, поэтому это касается любого 12 px вторичного
текста на `--c2`, а не одного блока.

**Что сделали.** Оставили роль. Локальное исключение не заводили: подняться на
ступень (100 % чернил) значит сплющить иерархию ради 0,4 % контраста, а завести
промежуточную роль запрещено правилом лестницы.

**Решение дизайнера 3 сентября (вечер) и почему запись остаётся открытой.**
Решено ввести порог кегля: вторичный текст на `--c2` начинается с 12,5 px, «при
12,5/500 пара берёт 4,58». Кегль поднят — правило продукта принято и записано.
Но **контраста это не чинит, и вот почему**: отношение по WCAG 2 не зависит от
размера и веса, от них зависит только порог, а «крупным» текст становится с
18,66 px полужирного или 24 px обычного. При 12,5 px порог остаётся 4,5,
отношение — те же 4,48. Числа 4,58 ни одна ступень чернил на `--c2` не даёт: 55
% → 4,48, 56 % → 4,64, 60 % → 5,33.

**И соседняя ступень не подходит — проверено замером.** `--v4-ink-data` (56 %)
даёт в песочном 4,64, но в синем **3,89**, то есть хуже нынешнего. Причина в
самой палитре: `--v4-ink-2` в светлых наборах объявлена литеральным чёрным
(`rgba(0,0,0,.55)`), а `--v4-ink-data` выводится из чернил набора, и в синем это
тёмно-синий `#101826`. Две ступени лежат на разных лестницах, и «шаг вниз» в
синем оказывается шагом вверх по светлоте.

**Чей ход.** Дизайнера и владельца палитры: либо `--v4-ink-2` в светлых наборах
выводится из чернил набора наравне с `--v4-ink-data`, либо ступень 55 %
углубляется, либо недобор 0,02 в одном наборе записывается отступлением. Порог
12,5 px остаётся в силе как типографика — он просто решает другую задачу.

**Дополнение того же дня: порог уехал в контракт и там столкнулся с прежним
текстом строки.** Пакет 3 сентября записал решение в строку `home-widgets` →
«контраст на плитках» как правило продукта для **любого** вторичного текста на
`--c2`. Но в этой же строке оставлен прежний текст 31 августа, который,
наоборот, регулирует текст **мельче 12 px** на плитках — «не бледнее 56 %
чернил», — то есть его существование допускает. Половины противоречат друг
другу, и код следует старшей: на «Калориях», единственной плитке Главной с
заливкой `--v4-hero`, вторичный текст стоит 8,5 px
(`.widget-calories__hero-bar-cap`, `.widget-calories__line-meta`) и 9 px
(`.widget-v4-kicker`), все тоном `--v4-ink-data` (56 %). По прежней половине это
верно, по новой — запрещено. Строка стоит `?` до ответа: остаётся порог кегля
или ступень тона.

**Ответ дизайнера 3 сентября (вечер): его часть закрыта, хвост остаётся.** «Вы
правы арифметически: порог кегля отношение не меняет.» Недобор 0,02 записан
**отступлением, а не починкой** — ступень ink-2 не двигается, промежуточная роль
запрещена, ink-data в синем даёт 3,89. Порог 12,5 px остаётся как типографика.

Две половины строки «контраст на плитках» разведены: порог адресован ступени
ink-2 (55 %), «не бледнее 56 %» — ступени ink-data, на которой стоят 8,5 px
подписи «Калорий». Противоречия в строке больше нет, оговорка добавлена.

**Чей ход теперь.** Владельца палитры, и вопрос сузился до одного:
`--v4-ink-data` на `--c2` в синем даёт **3,89**. `ink-2` в светлых наборах —
литеральный чёрный, `ink-data` выводится из чернил набора, ступени лежат на
разных лестницах. Это палитра, не плитка, и на сведение экранов не влияет.

---

### `?` Лента плана: безопасная часть реализована, три решения остаются внешними · strength-builder

**Что нашли.** Пакет из 77 строк нельзя свести одной визуальной правкой.
`Начать сейчас` на будущем дне не определяет дату факта; кадр переноса склеивает
pre-move, picker и два post-move дня; `День не состоялся` не имеет owner-факта,
отличающего pending-пропуск от допустимого backfill.

**Что сделали без дизайнера.** После data-safety слоя будущая карточка
показывает первые четыре упражнения, реальные дозы, связки и остаток;
owner-неделя имеет семь честных состояний. Перенос гидратит даты authoritative
batch-запросом, оставляет cold-cache и network error fail-closed, показывает
вертикальный picker и след на исходной и целевой датах. Явный skipped не
предлагает ретроактивный resume. `moved` исключён из нагрузки и пути программы;
эвристический `missed-pending` не выдуман. Узкий regression-набор: 151/151.

**Чей ход.** Владельца/дизайнера — определить early-start, owner-поле мезоцикла
и missed-pending, разнести composite переноса на четыре состояния и выбрать
owner-canvas общей карточки дня. Непроверенная на 375 px геометрия остаётся `?`;
только доказанные логические отступления типизируются как `≠`.

**Ответ дизайнера 3 сентября (вечер): не его.** Три решения — early-start,
owner-поле мезоцикла и missed-pending — остаются **владельца**. Разнесение
composite-переноса на четыре состояния дизайнер забрал себе отдельной задачей:
на код это не влияет и сведение не держит.

---

### `?` Карточка плана на «Активе»: кадр Г3 устарел по копии и действиям · strength-builder

**Что нашли.** Stop-кадр «Перенос · след в обе стороны» (Г3) рисует заголовок
«День B · верх тела» 15 px, мету «7 упражнений · назначил Артём» 11,5 px, CTA
«Начать тренировку» и одну вторичную «Не смогу сегодня». Продукт на
`activity-v4-program` держит «Сегодня по программе» 13,5 px, состав в прозе,
кнопку «Начать» и раздельные «Перенести»/«Пропустить» — зафиксировано
`activity-plan-card-frame.test.js`.

**Чем это грозит.** Свести к кадру — откатить осознанный разнос переноса и
пропуска и продуктовую копию карточки; оставить код — контрактные строки 07–10
остаются `≠`, пока кадр не обновят.

**Что предлагаю.** Обновить кадр Г3 и строки контракта под текущий продуктовый
flow (две вторичные кнопки, заголовок «Сегодня по программе», короткий CTA). Не
откатывать продукт к устаревшему коллажу.

**Чей ход.** Дизайнера — обновить кадр и контракт; владельца — подтвердить, что
разнос «Перенести»/«Пропустить» окончателен.

---

### `?` Цикл: запрос 26 августа выполнен наполовину — числа пересчитаны, слова остались прежними

**Что нашли.** Зона `cycle` сведена целиком (278 строк). Числовая часть запроса
`REQUEST-cycle.md` выполнена полностью и точно: множители калорий 0/0/+3/+5,
вода 10/0/0/5, инсулиновая волна переехала с дней 1–5 на 20–28, окно исключения
из тренда стало 26–28 и 1–5. Не выполнена словесная часть.

> **Уточнение 3 сентября.** Словесная часть выполнена там, где её искали:
> подписи фаз — свои слова, медицинских названий в интерфейсе нет, 🌸 снято из
> `heys_cycle_ui_v1.js` и из шага чек-ина, и это закреплено
> `cycle-v4-contract.test.js:181`. **Утечка осталась ровно одна — виджет «Цикл»
> на Главной**, и гейт её не видит по построению: он читает `CYCLE_UI_SRC` и
> `STEPS_SRC` и не открывает файл виджетов. Строка контракта однозначна, поэтому
> эмодзи снимается без обсуждения вместе с расширением гейта на этот файл.
> Открытым остаётся только инсайт — ход владельца, как и было.
>
> Мелочь рядом: `heys_day_water_state.js:87` считает
> `cycleNote = '🌸 Особый период'`, но это поле **не читает ни один рендерер**
> (проверено поиском по всему `apps/web`) — на экран не попадает.

| пункт запроса                                                                  | в продукте                                                                                                                                                                                                                                                            |
| ------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| «эмодзи 🌸 не переносится — метка рисуется формой»                             | 03.09: снято везде, кроме виджета Главной — `phase.icon` на `heys_widgets_ui_v1.js:6934` и `:6979` (рендерер подключён на `:1737`, виджет зарегистрирован `heys_widgets_registry_v1.js:642`)                                                                          |
| инсайт называет цену поправки числом                                           | заголовок инсайта — «Особый период» (`heys_cycle_ui_v1.js:595`), числа нет                                                                                                                                                                                            |
| «Пометка стоит только в дни 15–28» под карточкой нормы                         | строки нет ни в одном файле                                                                                                                                                                                                                                           |
| «Норма приподнята во второй половине цикла» под графиком                       | строки нет                                                                                                                                                                                                                                                            |
| «Пустые точки — дни 26–28 и 1–5» под графиком веса                             | есть только во всплывающей подсказке по тапу                                                                                                                                                                                                                          |
| подтверждение выключения: «вопрос в чек-ине исчезнет, цель вернётся к базовой» | ~~другой текст — про сохранение прошлых отметок~~ **снято 03.09:** контракт с тех пор сказал обратное — «выключение не переписывает прошлое», и текст в коде (`heys_health_features_v1.js:378`) ему соответствует. Пункт запроса 26.08 отменён более поздним решением |

**Чем это грозит.** Главное здесь — инсайт. Запрос прямо объясняет, зачем нужна
строка `средняя надбавка`: «чтобы цена поправки была числом». Прежние проценты
молча съедали пятую часть цели −1,8 кг, и весь пересчёт делался ради того, чтобы
человек видел цену. Числа исправлены, а показывать их по-прежнему нечему:
заголовок инсайта не называет ни процента, ни ккал. Эмодзи — мелочь, но оно
единственное место, где фаза названа символом, и запрос снял его намеренно.

**Что предлагаю.** Подтвердить, что заголовок инсайта должен нести число («Норма
выше на N %»), — и тогда это правка продукта, а не кадра. Эмодзи снять без
обсуждения: запрос однозначен. Три отсутствующие подписи под карточками —
сказать, нужны ли они в продукте или это пояснения самого канваса; сейчас они
существуют только в кадрах.

**Чей ход.** Владельца — по инсайту. Остальное чинится по уже данному запросу.

**Ответ дизайнера 3 сентября (вечер): запись сузилась до одного пункта.** Инсайт
с числом — **владельца**. Эмодзи 🌸 в виджете Главной снимается **без
обсуждения**, запрос однозначен: это работа, а не вопрос. Три подписи под
карточками — пояснения канваса, в продукт они не идут, и расхождением не
считаются.

---

### `?` Strength-builder: А1б/А2 против Г4 и К — одно состояние продукта, разные числа · strength-builder

**Что нашли.** Три кадра описывают один и тот же DOM-стейт «весовое упражнение
открыто в списке» и расходятся между собой; гейт
`strength-builder-calm-canvas-contract` показал это 13 строками после сведения
Г4 (`425403748`) и К (`f176fa7ca`).

| Строки                       | А1б / А2 говорят                                                                                   | Г4 / К говорят                                                             | Где в коде                                                                                                                                                                        |
| ---------------------------- | -------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| А1б · 04, 05                 | шапка «Силовая · грудь, спина, плечи» / «пн, 8 авг · начата в 18:40»                               | Г4 «Правка веса в сессии · 01»: имя открытого упражнения / «подход N из M» | `heys_strength_builder_ui_v1.js` ветка `is-exercise-open`                                                                                                                         |
| А1б · 28, 30, 31, 33, 35, 36 | `.sb-aps-head` 10px, `.sb-aps` gap 6px, номера ink .56/.62, текущий номер на фоне, кольцо поля 2px | Г4: padding 0, gap 0, номера `--gr`, текущий `--acs`, кольцо 1.5px         | `750-strength-builder.css` блок `.is-weight-entry.is-exercise-open`; класс вешается на **любое** открытое `weight_reps` (`builder_ui:1886`), а не только на правку веса куратором |
| А1б · 14, 44 · А2 · 15, 23   | заголовок свёрнутой карточки 13px                                                                  | К «Спорное · тап по закрытому во время отдыха · 14»: 12.5px                | `750:3391` `.sb-ex--collapsed .sb-ex-title b` — на всех свёрнутых, не только во время отдыха                                                                                      |

**Чем это грозит.** Любой из трёх кадров можно «свести» только сломав два
других: вердикты `=` на А1б/А2 и на Г4/К стояли одновременно, при том что
рендерится одно из двух. Подгонка чисел под зелёный запрещена; гейт calm держит
эти 13 строк поимённым списком `CANVAS_CONFLICTS` и падает, если спор исчезнет
сам.

**Что предлагаю.** Дизайнер называет источник для состояния «открытое весовое
упражнение» и для свёрнутой карточки. Если источник — Г4/К, строки А1б/А2
переписываются в пакете; если А1б/А2 — в коде `is-weight-entry` сужается до
контекста правки куратора, а 12.5px — до `sb-root--rest-docked`. До ответа 12
строк А1б/А2 стоят в `?`.

**Чей ход.** Дизайнер.

### `?` Strength-builder: Г1 «Программа · цикл» и Г2 «назначено против сделано» — два экрана, не один · strength-builder

**Что нашли.** В канвасе `strength-builder.v4.dc.html` раздел **Г** явно
разделён: **Г1** (`data-oid="Г1"`, `вид · экран цикла`, строки «Программа · цикл
· 01–33») — обзор цикла, фазы, «эта неделя»; **Г2** (`data-oid="Г2"`,
`вид · отчёт цикла`, строки «Программа · назначено против сделано · 01–37») —
полноэкранный отчёт с героем «План выполнен на 87 %», карточками упражнений и
парами «Назначено / Сделано». В коде **Г1** уже есть: `CycleScreen` в
`heys_strength_proposal_ui_v1.js`, вход `ProgramNextLine.openPath()` в
`heys_day_trainings_v1.js:3189+`, гейт
`strength-builder-cycle-v4-canvas-contract.test.js`. **Г2** не сведён: живой
`PlanVsDoneScreen` (`heys_strength_superset_ui_v1.js:2114+`) — in-session sheet
из шторки ⋯ (`plan-vs-done`), простые строки «план · / факт ·», без геометрии
кадра Г2. Вердикты Г2 ·01–37 стоят `?` с явным f: «full-screen отчёт цикла,
геометрия не сведена с in-session sheet». Контракт `вид · отчёт цикла` несёт
внутреннее напряжение: заголовок кадра — «Отчёт по циклу», но в prose указан
«охват кадра — ДЕНЬ» (как канон screen 19). Решение 1 сентября в том же канвасе
(`вид · режим порядка`): пункт шторки «Назначено против сделано» **снят** — «оно
живёт в программе куратора»; код шторки пункт пока оставляет.

**Чем это грозит.** Слияние Г2 в `PlanVsDoneScreen` или перенос CycleScreen под
строки «назначено против сделано» сломает уже сведённый Г1 и контрактные
семейства строк. Смешение in-session sheet и полноэкранного отчёta Pro оставит
218 `?` без честного пути закрытия и сохранит расхождение с канвасом.

**Что предлагаю (инженерия).** **Вариант B — разнести контракт и продукт на три
поверхности**, не A (не перестраивать цикл внутрь PlanVsDone):

1. **Г1** — `CycleScreen` (готово), вход «программа ›» / `ProgramNextLine`.
2. **Г2** — новый полноэкранный `CycleReportScreen` (или аналог) по кадру Г2
   ·01–37; навигация из Г1 или карточки программы — **нужно подтверждение
   владельца**.
3. **In-session** — либо убрать пункт шторки по решению 1 сентября, либо
   оставить упрощённый day-level compare с **отдельным** контрактным семейством
   (не подменять строки Г2).

**Чей ход.** Владельца/дизайнера — (1) вход на Г2, (2) day vs cycle в заголовке
Г2, (3) судьба пункта шторки. Рекомендация инженерии: **B**, Г1 не трогать.

---

### `!` Крестик шапки приёма ушёл с акцента в чернила при миграции литералов · food-meal

**Что нашли.** Проверка `meal-time-v4-structure` требует `background: #c67139` у
глифа `.mc-modal--meal-create .mc-header-btn--close`, а в коде теперь
`var(--v4-ink-3, rgba(0, 0, 0, 0.45))`. Тест красный, и он в деплойном контуре.

**Почему это не просто устаревший тест.** `#c67139` — это **значение роли
`--v4-act`** в песочном наборе. Правильная миграция такого литерала —
`var(--v4-act, #c67139)`; перевод в `--v4-ink-3` меняет вид: терракотовый
крестик становится серым. То есть здесь совпали два дефекта разного рода — тест
сторожит литерал вместо роли, и миграция подменила роль.

**Что сделали без дизайнера.** Ничего в коде: зона `food-meal` занята сессией
сведения, и цвет крестика — вопрос её кадра, а не попутной уборки. Находка
передана владельцу зоны с точным диагнозом, чинится вместе с вердиктом кадра:
роль по кадру плюс проверка, сторожащая роль, а не литерал.

**Чей ход.** Сессии зоны `food-meal`; дизайнеру — только если кадр действительно
рисует крестик нейтральным, а не акцентным.
