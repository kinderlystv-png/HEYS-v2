# Реестр занятых файлов — раздача Cursor и Claude

Зачем файл. Cursor работает в другой программе и из сессии Claude **не виден**:
ни его файлы, ни его прогресс наблюдать нельзя. Единственная защита от
столкновения — держать здесь список того, что уже отдано, и считать эти пути
занятыми, пока владелец не скажет «закрыто». Реестр ведёт диспетчерская сессия
(«Агент-дирижёр контроля макетов»).

Правило одно: **новая задача Cursor не выдаётся, пока её файлы пересекаются с
занятой строкой этой таблицы.** Не «постараться не пересечься» — не выдаётся.

## Занято Cursor

| #   | задача                                | файлы                                                                                     | состояние                       |
| --- | ------------------------------------- | ----------------------------------------------------------------------------------------- | ------------------------------- |
| 1   | Эмодзи-набор Главной уходит целиком   | `apps/web/heys_widgets_registry_v1.js`, `apps/web/heys_widgets_ui_v1.js` (чипы 5026/5029) | в работе                        |
| 2   | Дубли `data-oid` в `visual-harness`   | `scripts/lib/ui-v4-canvas-index.mjs`, `apps/web/scripts/ui-v4-visual-*.mjs`               | **закрыта**, коммит `1301f65ef` |
| 3   | Сократить список неопределённых ролей | `apps/web/styles/modules/**`                                                              | в работе                        |
| 4   | Тесты, охраняющие литерал вместо роли | `apps/web/__tests__/**`                                                                   | в работе                        |
| 5   | `pnpm test:e2e:smoke`                 | `TESTS/e2e/**`, `.env.local`                                                              | в работе                        |
| 6   | Заливочные роли на тексте, 49 мест    | `apps/web/styles/modules/*.css`, `apps/web/styles/heys-components.css`                    | в очереди за задачей 3          |

Задачи 3 и 6 делят `styles/modules` намеренно последовательно: параллельно они
столкнутся построчно.

## Занято сессиями Claude

| сессия                                               | зона                                                              | состояние |
| ---------------------------------------------------- | ----------------------------------------------------------------- | --------- |
| `Спецификация текущего поведения` (`local_599885b3`) | `strength-builder` — сверка кадров, вердикты                      | в работе  |
| `Агент-дирижёр контроля макетов` (эта)               | диспетчеризация; свои правки — только по прямой просьбе владельца | —         |

## Запрет для всех, кто не Claude-сессия зоны

`docs/ui/verdicts/**`, `docs/ui/handoff-v4/**`,
`scripts/ui-v4-check-contract-drift.mjs`, `scripts/lib/ui-v4-verdicts.mjs`.
Вердикт ставит тот, кто сводил экран, и в той же задаче — это правило
`CLAUDE.md`, а не удобство: пакетная простановка вердиктов опаснее пакетной
правки кода, потому что неверный вердикт молча становится правдой для
следующего, кто откроет снимок.
