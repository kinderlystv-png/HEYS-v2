# Вопросы дизайнеру и решения на подтверждение — срез на 31 августа 2026

Собрано из журнала расхождений [`UI_V4_FINDINGS.md`](UI_V4_FINDINGS.md) — из
открытых записей, то есть тех, что ещё ждут ответа. Закрытые лежат отдельно в
[`UI_V4_FINDINGS_HISTORY.md`](UI_V4_FINDINGS_HISTORY.md) и сюда не попали.

**Источник правды — сам журнал.** Этот файл его срез на 31 августа: он собран
для одного захода к дизайнеру и владельцу. Ответы уходят в журнал, а не сюда.
Журнал пополняется по ходу сведения экранов, поэтому срез верен на момент
сборки, а расходиться с журналом он начинает с первой новой записи.

На дату среза открыто **56 записей**: 43 со знаком `?` (ждут ответа) и 13 со
знаком `!` (решено без дизайнера, решение уже стоит в коде).

- **Раздел 1** — 37 вопросов, где ход дизайнера, плюс 6 записей, где ход не его
  (они собраны отдельной таблицей в конце раздела, чтобы счёт открытого
  сходился).
- **Раздел 2** — все 13 решений, принятых без дизайнера.

Каждый пункт назван заголовком записи из журнала — по нему запись ищется
поиском.

---

## Раздел 1 — вопросы дизайнеру

### Дата (`date-remainders`) — 4 вопроса

#### Три кадра живут в чужом канвасе, и их не сверяет никто

**Расходится.** В `date-remainders.v4.dc.html` лежат три кадра не из этой зоны:
`Совет · отмена с таймером` и `Совет · оценка после свайпа` (обоих в
`tips.v4.dc.html` нет) и `Служебное · за входом куратора` — у него своей зоны
нет вовсе. Это 122 строки разбора. Проверено пофайлово: `Советы · не сохранено`
в `tips` есть и сверяется там, а эти три — только здесь.

**Грозит.** 122 строки навсегда останутся без вердикта и при этом будут
числиться долгом зоны «Дата». Хуже: если кто-то закроет их «—» как чужие, они
исчезнут из виду, не будучи разобранными нигде.

**Предлагаем.** Два кадра совета перенести в `tips.v4.dc.html` к их родне, а
служебный раздел либо завести своей зоной, либо назвать строкой, что он вне
разбора.

#### Дата: капсула в контракте и в кадрах разной высоты, а зона всё ещё 330 px

**Расходится.** Строка `вид капсулы` просит кружки 34, капсулу 36 и зазор 7; оба
кадра дают кружок 34×44 и зазор ряда 9. Прежний третий вариант (кружок 44
залитый) снят — осталось расхождение на двоих, и строка отстала от кадров.
Заодно: все 24 кадра зоны нарисованы на 330 px (`width:330px`), хотя для
кабинета вы 31 августа перешли на 375 и записали это строкой контракта.

**Грозит.** Кодер сверяет капсулу и получает два числа зазора; плюс любые выводы
о переносах в этой зоне сделаны на неверной ширине — ровно та причина, по
которой кабинет перерисовывали.

**Предлагаем.** Привести строку `вид капсулы` к числам кадров (зазор 9, кружок
34×44) и перерисовать зону на 375 тем же заходом, что кабинет.

#### Дата: контракт называет заливкой чужого дня --tint, в наборе для неё своя роль

**Расходится.** Строка `вид чужого дня` говорит: «заливка капсулы и обоих
кружков меняется на --tint». В продукте у капсулы прошлого дня своя роль
`--v4-past`. В песочном наборе они почти совпадают (`#f3e0d2` против `#f6e6dd`),
в синем расходятся принципиально: `--v4-past` там сиреневый `#e4e0f2`, а
`--v4-tint` — розовый `#fbe6e2`.

**Грозит.** Свести к одной роли «для чистоты» — значит перекрасить капсулу в
синем наборе, а это видимое решение, а не уборка. Оставить как есть — держать
вторую тонированную поверхность, о которой контракт не знает.

**Предлагаем.** Ничего не меняли. Нужен ответ: сиреневая капсула прошлого дня в
синем наборе — это решение или наследство? Это тот же узел, что в вопросе про
подложку `--tint` ниже: один ответ закроет оба места.

#### Дата: разбор кадров зоны противоречит её же именованным строкам в восьми местах

**Расходится.** Кроме капсулы: клетка календаря — радиус 12 против строки
«радиус 14», число 13 против «12,5», точка 5 px против «4 px», зазор 4 против
«через 3»; ряд сокращений дней — 600 и 42 % против «9,5 px/700 тоном чернил 40
%»; «Сегодня» в чужом дне — залитая пилюля 10,5 px против «11 px/700 тоном
--ac»; «сегодня» в легенде — плашка против начертания, которое сама же зона
просила «снять в коде».

**Грозит.** Каждое такое место приходится разбирать заново при следующем заходе.
Сейчас они перечислены поимённо в тесте сверки — но это список отступлений кода
от кадра, которого не должно быть, если кадр перерисован.

**Предлагаем.** Перерисовать кадры зоны по её же строкам. Код менять не нужно:
он следует строкам.

### Спиннеры (`spinners`) — 1 вопрос

#### Спиннеры: кадры ступеней спорят с контрактом того же канваса

**Расходится.** Зона сведена целиком: из 174 строк совпало 120, пятнадцать
разошлись — и почти все одинаково, кадр рисует одно, а строка контракта на этом
же канвасе говорит другое. Продукт следует контракту.

| что                      | кадры рисуют                           | контракт того же канваса                                              |
| ------------------------ | -------------------------------------- | --------------------------------------------------------------------- |
| знак отказа              | восклицательный знак в круге r 9       | «вид знака»: третьей геометрии и второго цвета нет — в продукте крест |
| знак в кнопке            | дуга 15, обводка 3                     | «вид знака в кнопке»: 18 и 2,5                                        |
| кнопка «Повторить»       | призрачный текст 44, акцентная заливка | «вид ступеней»: фон --c2, чернила 58 %, 13/700, высота 48, отступ 22  |
| заголовок второй неудачи | 700 17/1,3                             | «вид подписи»: 15/700                                                 |
| причина ступени          | 500 12,5/1,55 тоном 55 %               | «вид подписи»: 12/500 тоном 50 %                                      |
| «Написать куратору»      | главная кнопка                         | «вид ступеней»: строка под кнопкой, 11,5/500 вторичным                |
| «Повторить ещё раз»      | отдельное слово на второй неудаче      | кнопка одна и та же — «Повторить»                                     |

**Грозит.** Два расхождения безобидны (кегли подписей), а два меняют смысл.
Крест говорит «не вышло», восклицательный — «внимание, посмотри». А «Написать
куратору» на экране, где приложение не запустилось, единственный живой выход к
человеку: от веса контрола зависит, найдёт его человек или нет.

**Предлагаем.** Кадры переснять по контракту — он старше, и продукт уже собран
по нему. Отдельно подтвердить два смысловых: знак отказа крестом и «Написать
куратору» ссылкой, а не кнопкой. Если по второму решение изменилось в пользу
кнопки — это правка продукта, а не кадра.

### Чек-ин и развилка разбора (`checkin-morning`) — 4 вопроса

#### Чек-ин: два кадра требуют текст, которого в продукте нет

**Расходится.** Из тридцати пяти кадров зоны копия совпала в тридцати трёх. Кадр
`Чек-ин · замеры просрочены` требует «Тридцать секунд в конце обычного душа —
достаточно», в продукте стоит «Выберите, что именно — от этого зависит
инсулиновая волна дня». Кадр `Чек-ин · остальное на неделе периода` требует
состояние «замер идёт…» — такого текста нет ни в одном файле. Обе фразы искали
по всему `apps/web`, а не только по чек-ину.

**Грозит.** Первая фраза не украшение: она отвечает на вопрос «сколько холода
достаточно», а нынешняя подпись говорит про механику волны, то есть про другое.
Вторая описывает состояние процесса, которого в продукте может не быть вовсе.

**Предлагаем.** По первой сказать: заменяет она нынешнюю подпись или идёт рядом.
По второй — есть ли у замера состояние «идёт», или кадр рисует несуществующее.

#### Чек-ин: пилюля-ответ в кадрах светлее и жирнее, чем в коде

**Расходится.** Кадры рисуют невыбранную пилюлю-ответ («Было», «до 12:00»,
«Сделал») текстом 700 тоном чернил 62 %; в коде `.mc-pill--choice` — вес 600 и
полные чернила. Обводка: кадр даёт `inset 1,5 px` чернилами 16 %, код — 22 %.
Строка контракта тут не помогает: `вид карточки шага` отсылает к
`виду строк-ответов`, а та описывает другие кнопки — выходы развилки вчерашнего
дня (фон --c1, чернила 55 %, 12 px/700). У пилюль пятого шага и фон другой
(--bg), и обводка, которой у тех нет вовсе.

**Грозит.** Это не один экран: пилюли-ответы стоят на всех пяти шагах чек-ина и
в развилке вчерашнего дня. Перекрасить по кадру — заметная правка всего ритуала;
оставить как есть — держать расхождение, которое разбор будет называть на каждом
прогоне.

**Предлагаем.** Нужна либо отдельная строка про пилюлю-ответ (вес, тон текста,
тон обводки), либо прямое «верны кадры» — тогда это правка на один заход с
проверкой всех пяти шагов глазами. Сейчас пилюля не описана ни одной строкой.

#### Развилка разбора: у значения и заливки три ступени, у кадра одна

**Расходится.** Кадры красят и число («110 %»), и заливку дорожки одним тоном:
`var(--ac)` и `var(--gr2)`. В продукте три ступени по тому, куда уведён
ползунок: норма — зелёный `--v4-sand-green-ink`, недобор — тёплый `#a86b2a`,
перебор — акцент; заливка меняется теми же тремя градиентами.

**Грозит.** Ступени несут смысл, которого в кадре нет: человек видит цветом,
ушёл он от нормы или нет, ещё до того, как прочитает число. Свести к одному тону
— потерять сигнал; оставить — держать расхождение, которое разбор называет на
каждом прогоне.

**Предлагаем.** Ступени оставили. Заливочный тон акцента заменён на текстовый
(`--ac` вместо `--acs`): на второй поверхности он давал 2,9:1 — ниже порога для
11,5 px, то есть кадр здесь заодно чинил и контраст. Нужна либо строка контракта
про три ступени оценки еды, либо прямое «верны кадры, ступеней не надо».

#### Развилка разбора: подвал «силы для пачки» с зазором 6, у пяти соседей 8

**Расходится.** Пять кадров развилки («вчерашний день · 14», «пачка незакрытых
дней · 10», «пачка после очистки · 10», «день из пачки · 15», «пустой день из
пачки · 15») дают подвалу зазор 8; кадр «сила для пачки · 24» — 6. Подвал в коде
один на все экраны развилки.

**Грозит.** Ничем, пока не решено завести второй подвал ради одного экрана.
Записано, чтобы шестой кадр не выглядел непрочитанным.

**Предлагаем.** Оставили 8 — большинством и потому, что подвал один. Подтвердить
8 либо назвать причину, по которой у этого экрана свой подвал.

### Карточка продукта (`product-card`) — 2 вопроса

#### Карточка продукта: шаг вредности сравнивает не то же, что кадр

**Расходится.** Кадр «Продукт · вредность и модерация» рисует две карточки в ряд
поровну: «оценка ИИ» и «расчёт по формуле», вторая выбрана — то есть человек
сравнивает две посчитанные оценки и выбирает, какую оставить. В продукте на этом
месте одна карточка «Расчёт системы» и под ней радиогруппа: «Оставить расчёт
системы» или «Поставить свою оценку». Оба значения в продукте есть —
`calculatedHarm` считает формула, `manualHarm` приходит из вставленной строки
продукта, — но названы они иначе и показаны не рядом.

**Грозит.** Расхождение не в форме, а в том, что написано человеку. Вставленную
строку обычно даёт ИИ: кнопка рядом так и называется — «Промпт для ИИ». Тогда
«своя оценка» в продукте это и есть «оценка ИИ» кадра, и подпись вводит в
заблуждение: человек думает, что ставит своё, а оставляет чужое. Но ту же строку
можно набрать руками — и тогда правильнее продуктовая подпись, а кадровая врёт.
Решение меняет не вёрстку, а смысл выбора на экране, где человек назначает
продукту вредность.

**Предлагаем.** Развести два случая в самом контракте: значение из вставленной
строки и значение, введённое руками, — это разные источники, и подпись должна
называть тот, что был на самом деле. Если источник различить нельзя, честнее
нейтральное «из описания продукта», чем выбор между «ИИ» и «своя» наугад.

**Что сделано пока.** Ничего: строки кадра оставлены без вердикта, форма не
менялась. Менять карточки на радиогруппу или наоборот до ответа значит закрепить
одну из двух трактовок молча.

#### Карточка продукта: проза раздела «Штрихкод» описывает прошлую версию кадров — три кадра из трёх

**Расходится.** Проза раздела не сходится ни с разбором, ни с разметкой кадров,
и так во всех трёх проверенных подряд.

| строка прозы           | проза говорит                                                                                            | разбор и разметка кадра                                                                                      |
| ---------------------- | -------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------ |
| `вид · кадр наведения` | рамка 176 × 116, уголок 26, обводка 2,5, тон `--acs`, крест в цели 44, кнопка обводкой высотой 48        | 230 × 150, уголок 34, обводка 3, `#cf8144`, крест 30 × 30, кнопка заливкой высотой от 44                     |
| `вид · код не найден`  | карточка `--c1` радиусом 20 полями 16, код моно чернилами 42 %, заголовок 13,5, проза 11,5, ряд обводкой | карточка `--c2` радиусом 22 полями 20, код тоном `--ac`, заголовок 14/1,4, проза 12/1,5, ряд заливкой `--c1` |
| `вид · найден`         | ярус «Найден по штрихкоду» 10,5 px/600 тоном `--ac`                                                      | строка 12 px/600 тоном `--gr` на карточке `--gr-bg` радиусом 16                                              |

Отдельно спорит цвет рамки прицела: `вид · кадр наведения` называет роль
`--acs`, а строка `цвета не из палитры` того же контракта — `#cf8144` как
системный слой камеры без роли.

**Грозит.** Три из трёх — уже не единичный спор, а состояние раздела: проза
осталась от прошлой версии кадров. Пока она там, каждая пара «проза против
разбора» решается тем, кто открыл файл. В разделе «Создание продукта» двенадцать
строк прозы и больше двухсот строк разбора, и там цена ошибки выше — это поля,
обязательность и публикация в общую базу, а не радиус уголка. Кабинет по такой
же причине переделывали трижды.

**Предлагаем.** Перегенерировать прозу раздела из текущей разметки — тем же
способом, каким собран разбор. Если 176 × 116 и остальные числа прозы это новое
намерение, а не остаток, тогда устарели кадры — и перерисовывать нужно их, а
разбор, приёмочный лист и продукт идут следом одним заходом.

**Что сделано пока.** Свели по разбору; три строки прозы получили `≠` с
указанием, что именно разошлось.

### Актив (`tab-activity`) — 4 вопроса

#### Актив: кадр «день собран» рисует программу строкой, а «план назначен» — карточкой

**Расходится.** Два кадра одной зоны показывают одно состояние — план куратора
на сегодня — по-разному. «Актив · план назначен» даёт карточку `.grp`: заголовок
«Сегодня по программе», пилюля «от куратора», проза, «Начать» и ряд из двух.
«Актив · день собран» на том же месте рисует компактную строку списка `.cd`:
«Программа · сегодня силовая · Ноги и спина · 5 упражнений» и ссылку «начать ›».
Кадр вдобавок спорит сам с собой: сверху план не начат, а ниже в ярусе «Сегодня»
та же тренировка уже выполнена («силовая 45 мин · 1,9 т объёма · 385 ккал») —
одновременно этих состояний не бывает, и контракт прямо говорит, что назначенный
план до старта даёт «не начаты», а не число.

**Грозит.** Кодер, сводивший экран по «плану назначенному», сделал карточку;
следующий, сводящий по «дню собранному», увидит строку и решит, что карточка
лишняя. Ровно так зона переделывается дважды.

**Предлагаем.** Оставить карточку и убрать строку программы из кадра «день
собран» — либо назвать строкой контракта, когда программа показывается строкой,
а когда карточкой. Заодно поправить внутреннее противоречие кадра: либо план не
начат и тренировок в ярусе нет, либо тренировка выполнена и плана уже нет.

#### Актив: кадр карточки силовой рисует журнал, а продукт — идущую тренировку

**Расходится.** Кадр «Тренировки · журнал подходов» показывает завершённый
результат: имя и время, «Конструктор · 5 упражнений · 1,9 т фактического
объёма», чипы зон, список упражнений («4 × 8 · 80 кг»), кнопка «Открыть
конструктор». В продукте на этом месте карточка идущей тренировки: тикающий
таймер, «7 / 24 подходов», полоса выполнения, «● идёт», «Продолжить тренировку»,
«Упражнение 3 из 5 · Тяга штанги».

**Грозит.** Свести по кадру — значит убрать таймер и прогресс: пока тренировка
идёт, человек смотрит в карточку именно за ними, и список упражнений этого не
заменяет. Свести по продукту — оставить кадр неисполненным и не знать, какой вид
канон.

**Предлагаем.** Взяли третье: оболочку привели к `.grp` кадра (поверхность,
радиус, поля, кнопка), внутренний состав не трогали. Нужен второй кадр —
«тренировка идёт» — либо слово, что журнальный вид показывается только после
закрытия последнего подхода. Второе дешевле: кадр в пакете нарисован с полным
составом и временем «18:40», то есть после тренировки.

#### Актив: строка «переключатель „Месяц" в календаре» спорит со строкой 25

**Расходится.** Сверены все 286 строк зоны. Строка
`переключатель «Месяц» в календаре` говорит: «Кадр календаря в режиме месяца не
рисуется до решения: возвращать листание или оставить только 28 дней». Но
решение уже лежит в том же контракте — строка `режим «Месяц» с листанием`
описывает сетку с подписью месяца и стрелками ‹ ›, а кадр «Календарь зарядки»
рисует строку листания поэлементно (`‹`, `Август 2026`, `режим «Месяц»`).

**Грозит.** Две строки одного контракта требуют разного, и кодер выбирает
сторону сам. Взяли сторону строки 25 и кадра — листание реализовано. Пока строка
43 стоит как есть, следующий проход может её «починить» обратно и снять
листание.

**Предлагаем.** Убрать строку `переключатель «Месяц» в календаре` или переписать
её в прошедшем времени как запись о принятом решении.

#### Актив: тон «сегодня» в календаре — 16 %, и такой ступени в наборе нет

**Расходится.** Строка `вид · календарь зарядки` называет пять тонов точки, три
из них мимо ступеней набора: «не вели» 10 %, «сегодня» 16 %, легенда 50 %.
Ступеней чернил четыре — 55, 45, 38, 30 %, и список ролей заморожен
(`scripts/ui-v4-check-undefined-roles.mjs`). Взяты ближайшие; «не вели»
дополнительно приглушён прозрачностью 0,34, иначе он не отличается от «сегодня».

**Грозит.** В кадре «сегодня» и «не вели» различаются шестью процентами, а в
наборе — ничем: обе сводятся к 30 %. Рядом в сетке это две одинаковые серые
точки, и легенда их не спасает.

**Предлагаем.** Либо развести существующими ступенями (например «сегодня» 38 %,
«не вели» 30 %), либо назвать «сегодня» обводкой, а не заливкой.

### Кабинет куратора (`curator-cabinet`) — 4 вопроса

#### Кабинет: шесть строк контракта устарели относительно собственных кадров

**Расходится.** Из 444 строк зоны закрыто 415 совпадений и 15 названных
отступлений. Шесть строк не закрыть — они противоречат кадрам того же пакета.

| строка                                   | что говорит                                      | что в кадре                                                       |
| ---------------------------------------- | ------------------------------------------------ | ----------------------------------------------------------------- |
| `сетка и грунт кабинета`                 | «экран 330 px»                                   | кадры на 375, закреплено строкой `ширина кадров 375`              |
| `вход куратора с телефона`               | «кадры нарисованы шириной 330 px»                | то же                                                             |
| `шкала кеглей кабинета`                  | «имя вкладки 15 px/700»                          | «Заявки» и «Диагностика» рисуют заголовок 17 px/800               |
| `меню клиента вместо пяти кружков`       | «дневник, подписка, анкета, настройки, удаление» | кадр даёт анкету, подписку, диагностику, копирование id, удаление |
| `карточка · строка клиента в «Клиентах»` | строка 64 с разделителем, «⋯» 32×32              | кадр рисует карточку `.grp` с кнопкой 44 и «⋯» 40                 |
| `карточка · нижняя строка клиента`       | кнопка «Открыть дневник» «на --c2 тоном --ac»    | разметка того же кадра даёт `--acs` с `--on-acs`                  |

Ещё две пары спорят между собой, и там сторона выбрана с причиной в вердикте:
`карточка · состояние системы` (две кнопки) против `диагностика · три действия`;
`карточка · пустая вкладка «Заявки»` (счёт справа) против
`ярус «Где ещё есть работа» без чисел`. В обоих взята новая строка.

**Грозит.** Разбор выведен из разметки канваса, и там, где он расходится с
собственным кадром, доверять нельзя ни ему, ни прозе: кодер выбирает сторону сам
— ровно та ситуация, из-за которой кабинет переделывали трижды.

**Факты из кода** (проверено 31 августа при сведении кабинета). Кабинет больше
не 330 px: он занимает экран целиком, до `max-width: 520px` колонкой на широком
(`734-ui-v4-curator-panel.css`) — строка «экран 330 px» устарела не только
против кадров, но и против продукта. Заголовок вкладки в коде уже 17 px/800
(`.cur-cab__tab-title`), то есть код следует кадрам, и менять придётся строку
«имя вкладки 15 px/700».

**Про «Дневник клиента» в кадре меню.** Отдельного расхождения тут нет: сама
аннотация с `data-anchor="Дневник клиента"` говорит правильное — «главное
действие названо словом, остальное ушло в лист». Второй механикой входа дневник
делает строка `меню клиента вместо пяти кружков` из таблицы выше, где он
перечислен пунктом листа. Разведено 31 августа, чтобы вы не искали
несуществующий кадр.

**Предлагаем.** Перегенерировать разбор из текущей разметки и снять дублирующие
редакции: строки про 330 px, состав меню и «строку клиента» описывают прошлую
версию кадров.

#### Кабинет: две пары строк контракта описывают одно и то же по-разному

**Расходится.** Первая пара — строка клиента:
`карточка · строка клиента в «Клиентах»` даёт строку высотой 64, разделитель 1
px, «⋯» 32×32; `вид · карточка клиента` — карточку радиусом 20, поля 16, зазор
10, «⋯» кругом. По второй экран уже сведён, и иначе быть не могло: в карточке
живут метки дня, серия и превью сообщения — в строку высотой 64 они не
помещаются. Первая строка при этом описывает состав шапки точно (аватар 34, имя
12,5/700, телефон 11/600), и в этой части код ей следует. Вторая пара — кегль
имени вкладки: `шкала кеглей кабинета` говорит 15 px/700, кадр
`Заявки · пусто · 02` рисует 800 17 px/1.15 трекингом −.01em. Код следует кадру.

**Грозит.** Правило «контракт старше кадра» здесь не помогает: спорят контракт с
контрактом и контракт с кадром. Любой вердикт — выбор стороны, сделанный
реализующим, и следующий увидит вторую строку и «починит» обратно.

**Предлагаем.** Взяли карточку и кадр — то, по чему экран уже работает; оба
отступления названы поимённо в вердиктах. Ещё два мелких отличия: телефон стоит
под именем, а не с ним через пробел (в 330 px они переносятся), и «⋯» — круг 40,
а не 32: 32 px меньше комфортной цели нажатия. Нужно снять устаревшую строку из
каждой пары либо свести их. Если строка высотой 64 — осознанный откат от
карточки, это уже решение о вкладке целиком, а не о геометрии одного элемента.

#### Кабинет · макет не знает половины живого экрана

**Расходится.** Это вы подняли сами 30 августа, разбирая четыре снимка живого
кабинета. Элементов нет ни в одном кадре зоны: группа «В коридоре» с числами
разницы и нормы (панель, шестая из семи групп); подвал «⚙ Настройки: Макс.
слотов · Длительность триала» (очередь); кнопка «Обновить» под списком;
«Проверьте текущий этап», пилюли «Ожидает» / «Анкета: готово к разбору»,
«Обновлено 13 авг, 13:46» (очередь · анкеты); кружок-иконка вместо инициалов,
красный «✕», метка `organic`, дата решения строкой (очередь · решения).

**Грозит.** Кодер приводит экран к макету, который не называет половину
элементов, и каждый заход решает за дизайнера, что с ними делать. Отсюда три
захода по одному экрану.

**Ответ на ваш вопрос про «В коридоре».** Это результат расчёта, а не
кураторская стадия: `heys_norm_correction_v1.js:1318` даёт `in_corridor`, когда
расчёт дошёл до конца и расхождение факта с формулой попало в мёртвую зону (≤ 2
%, порог назначен владельцем 30 августа). На таком отрезке 2 % — шум записей и
весов, а не обмен, и поправка не применяется. Значит место группы — нижний ярус,
ниже «молчат» и ниже «расчёт разошёлся», рядом с «копят данные» и «всё ровно»:
работы там нет. Владелец решил показывать этих клиентов с числами, а не прятать
— видно, что механика поправки работает. Заодно: «Всё ровно» в коде — остаточное
состояние (`else` в конце цепочки), туда попадают те, по кому решение принято
раньше, но не сегодня; стоит рисовать его как «решение принято ранее», а не как
пустоту.

**Предлагаем.** По каждому элементу сказать одно из двух: остаётся (тогда вид и
место) или снимается (тогда явно «снято»). До ответа эти четыре экрана не
трогаем.

#### Клиентский виджет заявки: три градиента вне набора

**Расходится.** Строка `градиентов в кабинете нет` закрыта — в кабинете их не
осталось. Но в том же файле `heys_trial_queue_v1.js` живёт клиентский виджет
статуса заявки, и в нём три градиента: кнопка «Запросить триал» (зелёный при
доступной очереди, синий при закрытой) и кнопка «Запросить снова» (синий).

**Грозит.** Правило набора сформулировано шире кабинета: «градиент был
единственным приёмом, которого в наборе нет вовсе». Пока эти три живы, гейт
кабинета зелёный, а приём в продукте остался.

**Предлагаем.** Не править вслепую: экран заявки клиента кадрами кабинета не
описан, и своя перекраска разойдётся с его канвасом при сведении. Нужен ответ,
входит ли клиентский виджет заявки в пакет и каким канвасом он описан —
градиенты снимем в той же задаче.

### Главная и виджеты (`home-widgets`) — 5 вопросов

#### Кнопка настройки экрана: вид не выбран, а в продукте уже стоит один из трёх

**Расходится.** Три кадра «Кнопка настройки · 48 обводка / 48 шалфей / 40 малая»
помечены `data-demo="protocol"`, и подпись говорит прямо: «Выбранный вид будет
нарисован в четырёх палитрах, два оставшихся — протокол сравнения». Выбор ещё не
сделан. Доводы кадров: 48 px обводкой акцентом 1,5 px — «заметна, но не спорит с
терракотовым „+"»; 48 px залитая шалфеем — «красиво, но шалфей в системе значит
„идёт хорошо"»; 40 px как малые кнопки справа — «точно не спорит, но легко не
заметить».

**Грозит.** В продукте с самого начала стоит третий — 40 px, тот, чей минус
назван самим кадром. Это единственный вход в расстановку экрана: не нашли кнопку
— не нашли и всю возможность собрать Главную под себя.

**Предлагаем.** Ничего не решали: строка «демо» запрещает реализовывать
`protocol`-кадры, код не трогали; заодно завели гейт
`widgets-bd-sheet-canvas-razbor`, который падает, если таблица пар сошлётся на
кадр без метки `stop`. Нужно выбрать вид и нарисовать его в четырёх палитрах.
Если выбор упадёт на 48 px, поменяется размер, тон и тень одной кнопки — работа
на полчаса, но её нужно назначить, а не оставить как есть по умолчанию.

#### Вода: две строки зоны рисуют плитку 1×1 по-разному

**Расходится.** Вы 30 августа переписали строку `вид · плитка воды 1×1`:
«Подпись „Вода" 9,5 px/600 чернилами 50 % в левом верхнем углу, число 12 px/600
в правом верхнем углу (17 px было у прежней раскладки с числом внизу — снято)».
Соседняя строка `раскладка плитки` осталась прежней и говорит обратное: «„Вода"
слева внизу, факт справа внизу, норма мелким справа сверху; обе нижние строки по
нижнему краю — базовые линии совпадают». Это разная раскладка, а не разница в
пикселях.

**Грозит.** Код следует `раскладке плитки` (число справа внизу), и её вердикт
стоит `=`. Взять новую строку буквально — уронить вердикт соседней и потерять
норму, о которой новая строка молчит вовсе.

**Предлагаем.** Ничего не меняли — плитка вдобавок живёт в файле зоны
(`730-widgets-dashboard.css`), который правит другая сессия. Владелец 31.08
решил: спрашиваем дизайнера, сами не разнимаем. Габарит спорным не является и в
вопрос не входит: строка «колонка» задаёт ширину формулой `(ширина − 56) / 4` ≈
79,8 px на 375 px, что сходится с «80 × 64» новой строки; 68 × 64 из строки
«лист» — это превью карточки в каталоге видов, другая поверхность. Нужно свести
две строки к одной раскладке и сказать, где в ней норма.

#### Каталог: кадр рисует одну колонку и рамку 68×64, контракт требует плитку своего формата

**Расходится.** Кадр «Каталог · нет места» рисует список одной колонкой: слева
превью строго 68×64, справа имя виджета и строка формата («Тепловая карта» /
«Месяц целиком · 2×2»). Строка контракта `каталог` говорит другое: каждая строка
рисует плитку «в её дефолтном виде и формате на числах открытого дня — тем же
кодом, что на Главной». В рамку 68×64 это не помещается: 2×2 и 3×2 пришлось бы
сжимать, то есть показывать не тот формат, который человек получит. Продукт идёт
по контракту: сетка в две колонки, превью своего размера (68×64, 143×64,
143×136, 218×136 со сжатием). Кадр к тому же спорит сам с собой — подписывает
строку «Месяц целиком · 2×2», а превью рядом рисует 68×64, размер формата 1×1.

**Грозит.** Пока ничем: продукт стоит на контракте. Но три строки разбора (09,
13, 14) остаются `≠`, и следующий проход снова упрётся в тот же вопрос.

**Предлагаем.** Оставили как есть — контракт старше кадра, и его требование
содержательнее: человек видит ровно ту плитку, которую добавит. Либо кадр
перерисовывается под контракт, либо строка `каталог` переписывается под единую
рамку — и тогда нужно сказать, что делать с форматами крупнее 1×1. Если имя
виджета рядом с превью всё же нужно (превью показывает ключ плитки, но не её
название в каталоге), это отдельная небольшая правка поверх текущей сетки.

#### «Недосып за 7 дней» и «норма 7,5» не помещаются в одну строку 2×1

**Расходится.** В кадре «Шторка · Сон» у вида «Долг за неделю» шапка плитки —
ключ «Недосып за 7 дней» слева и «норма 7,5» справа. Замер продуктовыми стилями:
ключ 84 px, мета 43 px, вместе 127 px при 121 px внутренней ширины плитки 2×1
(143 − 22 поля). Ключ переносится на вторую строку и съедает высоту столбиков.
То же у воды, вид «Ритм дня»: ключ «Вода · 1,7 / 2,7 л» (71 px) и «3 ч без воды»
(59 px) дают 135 px при тех же 121 px, и ключ ещё растёт с числами — «10,5 /
12,0 л» шире.

**Грозит.** Плитка не ломается — 22 px переноса умещаются в 64 px высоты, — но
шапка читается как ошибка вёрстки, а не как решение.

**Предлагаем.** Сами не сокращали: оба слова взяты из кадра, и выбор, что
укоротить, продуктовый. Варианты: «Недосып · 7 дней» (экономит ~8 px и как раз
влезает) или мета без слова «норма». Оба случая — одна развилка: в 2×1 ключ и
мета вместе живут примерно до 115 px, и это стоит знать при написании новых
подписей.

#### Лист веса: кадр даёт ввод веса прямо в листе, продукт уводит в день

**Расходится.** В кадре «Разбор · Вес» под нормой стоит ввод: две пилюли цифр
(«91» и «1») через запятую, на бумаге --c1, 700 24 px тоном --ac — то есть поле
записи веса внутри самого листа. В продукте действие листа «Записать вес» уводит
на вкладку дня и открывает там редактор.

**Грозит.** Ничем сегодня: путь работает. Но это разница функции, а не
геометрии, и правкой стилей её не закрыть. Пока она не решена, четыре строки
разбора стоят `≠`.

**Предлагаем.** Не строили. Ввод числа внутри листа — это своя клавиатура, своя
проверка, своё сохранение и своё поведение при закрытии; строки контракта про
такое поле нет ни одной, а «запись» говорит только про «нажатие на карточку».
Владелец 31.08 решил отложить до следующего пакета дизайна: сейчас не строим,
запись остаётся открытой, ход ваш. Нужен ответ — нужен ли ввод веса прямо в
листе. Если да, нужна строка о том, что происходит при сохранении и при закрытии
листа без сохранения; если нет — из кадра стоит убрать пилюли, чтобы разбор не
показывал несуществующее поле.

### Питание (`nutrition-tab`) — 2 вопроса

#### Держатели в «Питании» ждать нечего — данные приходят сразу

**Расходится.** Строка «держатель места» требует держатели у кольца воды,
графика недели, полос дневника и дорожек итогов дня. Их нет, и это не «не
успели»: данные читаются синхронно из локального хранилища обычным `lsGet`
(`heys_day_water_v1.js:29-30`), ни сети, ни промиса, ни состояния загрузки в
`heys_day_nutrition_v1.js` нет вовсе. Держатель там не показался бы никогда.
Соседняя строка `выход, удаление данных, производительность` говорит прямо
обратное: «дневник и итоги рисуются из локальных данных сразу; после первого
кадра приходят кольцо воды, её недельный график и волна приёмов — места под них
держатся с начала». Код не делает ни того, ни другого: `buildWeekSeries`
вызывается синхронно в теле рендера (`heys_day_water_v1.js:351`).

**Грозит.** Для человека ничем: раз ожидания нет, при появлении данных ничего и
не сдвигается. Но строка остаётся невыполнимой, и следующий, кто её возьмёт,
потратит время на мёртвый UI.

**Предлагаем.** Сначала свести две строки между собой — сейчас они описывают
разную механику загрузки, — и потом сказать, снимаются держатели или у этих
элементов должно появиться ожидание, которого сейчас нет по построению. Размеры,
которые строка называет, совпадают: кольцо 58 обводкой 6, кривая 56.

#### Питание: две иконки в листе действий рисуют другие предметы

**Расходится.** В листе «Действия с приёмом» четыре пункта, у двух иконка в коде
изображает не то, что в кадре. «Переместить на другой день»: кадр — календарь со
стрелкой (прямоугольник 18×16 радиусом 3 плюс `M8 3v4M16 3v4M14 13l3 3-3 3`),
код — обмен стрелками (`M4 7h11l-3-3M20 17H9l3 3`). «Сохранить набором»: кадр —
документ с загнутым углом, код — закладка (`M5 4h14v16l-7-4-7 4z`). У двух
оставшихся пунктов («Повторить сегодня», «Копировать приём») кривые совпадают.

**Грозит.** Обмен стрелками и календарь означают разное, и закладка с документом
тоже. Иконка тут единственное, что отличает четыре одинаковых по длине строки
друг от друга при беглом взгляде.

**Предлагаем.** Ничего не меняли. Календарь достроить можно — данные кадра
полные, — но помощник `svgIcon` рисует ровно одну кривую, а календарю нужен ещё
прямоугольник: это правка механизма, а не иконки. У «Сохранить набором» данные
кадра обрезаны (см. вопрос про предел длины `data-v` ниже), достраивать их
нельзя вовсе. Сначала нужен поднятый предел длины, после этого обе иконки
правятся одним заходом вместе с расширением помощника до нескольких фигур.

### Приём пищи (`food-meal`) — 1 вопрос

#### Собранный приём: два кадра одного экрана дают разный набор кнопок

**Расходится.** Кадр «Добавление · приём собран» строит подвал так: ряд с
«Добавить ещё» главной кнопкой и квадратом 48 с камерой, под ним «Завершить
приём» вторичной, ещё ниже «Сохранить как набор» простым текстом. Кадр «Приём
собран · фото» — тот же экран с раскрытым ярусом фото — заканчивается «Готово»
главной кнопкой. Строки контракта про подвал этого экрана нет ни одной; строка
`границы` называет экраны приёма «от „плюса" до „Готово"», то есть словом из
второго кадра. В продукте третий вариант: «Добавить ещё» и «Сохранить как набор»
двумя одинаковыми призрачными кнопками в ряд, под ними «Готово» главной, фото
вынесено ярусом выше с плиткой «Добавить».

**Грозит.** Кадр делает главной кнопкой «Добавить ещё», то есть подталкивает
продолжать сборку, а продукт делает главной «Готово», то есть завершать. Это
разные подсказки о том, что человек должен сделать дальше.

**Предлагаем.** Ничего не меняли: развести два кадра нельзя, это не «контракт
против кадра», а кадр против кадра при молчащем контракте. Нужно сказать, какой
из двух подвалов верен и что делать со вторым, и завести строку про иерархию
кнопок этого экрана — сейчас её нет, и следующий разбор упрётся в тот же вопрос.

### Советы (`tips`) — 2 вопроса

#### Советы: у плашки совета крестик и «Позже» делают одно и то же

**Расходится.** Кадр «Совет · всплывающий» рисует и крестик 26 px в шапке
карточки, и кнопку «Позже» в ряду ниже. В коде оба ведут в один `dismissToast`.

**Грозит.** Два контрола с одним исходом на маленькой плашке. Либо у них разный
смысл — и тогда он нигде не назван, либо смысл один, и один из них лишний.

**Предлагаем.** Собрали как нарисовано: крестик стоит, оба гасят плашку. Если
«Позже» задумывалось как «напомнить позже» — это другая механика, и её надо
называть строкой контракта.

#### Советы: в кадре шторки есть заметка под списком, которой нет в продукте

**Расходится.** Кадр «Советы · шторка», элемент 17: коробка --c2 радиусом 14 с
текстом «Не забудьте про восстановление и сон.» — стоит после последней
карточки, перед строкой подсказок. Ни одна именованная строка зоны её не
описывает, а в продукте между списком и подсказками нет ничего.

**Грозит.** По виду это заметка куратора к сегодняшним советам — то есть новая
сущность с источником данных, а не оформление. Собрать её «как нарисовано»
значит выдумать, откуда берётся текст и когда он появляется.

**Предлагаем.** Не собирали. Нужна строка контракта: чей это текст, когда
показывается, что делать при его отсутствии.

### Регистрация (`registration`) — 1 вопрос

#### Регистрация: кадр красит «избыток» тем же тоном, что «ниже нормы»

**Расходится.** Кадр «Регистрация · рост и вес», элемент 21: слово категории ИМТ
(«избыток» при 26,2) набрано тоном `--ac2` — тем же, которым кадр «Регистрация ·
ниже нормы имт» красит заголовок предупреждения «ИМТ цели 17,0 — ниже нормы». В
коде тревожный тон стоит только у недостатка веса (ИМТ < 18,5), «избыток» идёт
приглушёнными чернилами.

**Грозит.** Это не оформление, а сигнал. Строка `желаемый вес` той же зоны
говорит про предупреждения: «предупреждение, не предел». Покрасить «избыток»
тревожным тоном на третьем шаге регистрации — сказать человеку, что с ним что-то
не так, до того как он вообще вошёл в продукт. Именованной строки, которая это
требует, в зоне нет.

**Предлагаем.** Не меняли. Если тревожный тон нужен обоим краям нормы — это
решение, и его стоит назвать строкой; если тревожат только недостатком —
перекрасить слово в кадре.

### Геймификация (`gamification`) — 1 вопрос

#### Геймификация: кадр «Уровни» рисует номер уровня квадратом, а строка — текстом

**Расходится.** Строка `вид строки уровня в списке`: «номер и титул слева 12,5
px/600, порог XP справа 11,5 px/600 тоном чернил 45 %… пройденные гаснут до 45
%, текущий — чернилами с весом 700». Кадр «Уровни» (элементы 16, 19, 20) рисует
номер квадратом 34×34 радиусом 12 с заливкой по состоянию: пройденный —
`--gr-bg` тоном `--gr`, текущий — `--acs` тоном `--on-acs`, будущий — `--c2`
приглушёнными чернилами. То есть ровно как медаль достижения.

**Грозит.** Это не оттенок, а другой элемент. Квадраты сделали бы лестницу
уровней визуально равной списку достижений — возможно, так и задумано, но тогда
строка описывает снятый вид.

**Предлагаем.** Код оставлен на строке (номер текстом). Если квадраты нужны —
это правка строки, и заодно надо сказать, что делать с состоянием «текущий»: у
достижений заливка означает «получено», а у уровней текущий и пройденный
одинаково «достигнуты».

### Поправка нормы (`norm-correction`) — 1 вопрос

#### Поправка: у канваса два красных, у набора один

**Расходится.** В канвасе объявлены две красные роли: `--red` `#b4442a` и
`--val-bad` `#a8382b` (обе — `standalone/app-splash.v4.html:382`). Разводятся по
смыслу — строка `роли цвета` говорит: «снижение — `--val-bad` только на числе Δ,
а не на всей карточке: снижение не катастрофа». То есть `--val-bad` задуман как
более мягкий красный «для числа». В продуктовой палитре роль одна:
`--v4-bad-text` `#b4442a` — это `--red`. Число разницы в листе поправки берёт
её, и снижение окрашено красным тревоги.

**Грозит.** Замысел «снижаем тихо» не доезжает до экрана. Разница между
`#a8382b` и `#b4442a` невелика, но она системная: если вторая роль нужна, её
надо завести один раз и во всём наборе, а не подмешивать локально в одном
экране. Ровно так и появляются цвета мимо палитры, за которыми потом гоняется
`pnpm ui:v4:check`.

**Предлагаем.** Ничего не подмешивали: число Δ берёт `--v4-bad-text`,
расхождение названо отступлением в тесте разбора
(`.cur-sheet__rec-delta | цвет`). Нужен ответ: либо `--val-bad` — это и есть
`--red` и второе значение лишнее, либо в наборе нужна отдельная роль «значение
хуже» со своим значением, и тогда её надо назвать для всех зон сразу.

### Отчёты и Инсайты (`reports-insights`) — 2 вопроса

#### Инсайты: проза строки БЖУ расходится с собственным кадром

**Расходится.** Строка `карточка · БЖУ по приёмам` описывает полосу доли так:
«под строкой полоса доли высотой 4 радиусом 999». Кадр «Инсайты · ярус Питание»
рисует иначе: класс `.mrow` — флекс по центру, то есть полоса стоит в строке
между именем и числом, а `.mbar` задаёт высоту 8. Покадровый разбор высоту не
называет вовсе — только ширины сегментов.

**Грозит.** Это третий случай, когда проза строки расходится с разметкой кадра,
из которого она выведена. Первые два были в кабинете: разбор называл кнопку
«Открыть дневник» стоящей «на --c2 тоном --ac», тогда как разметка давала
`--acs`, и строка `карточка · строка клиента` описывала строку 64 px там, где
кадр рисует карточку. Кодер каждый раз выбирает сторону сам.

**Предлагаем.** Реализована разметка: полоса в строке высотой 8, отступление
названо в вердикте и в тесте сверки. Перегенерировать прозу этой строки из
текущей разметки — или, если полоса под строкой была осознанным решением,
поправить кадр, чтобы разметка и проза говорили одно.

**Наблюдение по существу, не расхождение.** У полосы в строке есть дефект,
видный на живых данных: она делит ширину с именем приёма, и у приёма с длинным
названием («Обед с длинным названием · 13:05») сжимается вдвое. Доли двух
приёмов тогда нельзя сравнить глазом — а ради сравнения строка и читается.
Полоса под строкой этого лишена. Возможно, именно поэтому проза и говорит «под
строкой».

#### Метки вилок планера: три источника дают три разных вида

**Расходится.** Строка `карточка · планер „Что съесть сейчас"` описывает метки
вилок так: «высота 26, радиус 8, фон `--bg`, рамка 1 px чернил 14 %, шрифт 11
px/600 `--tx`». Класс `.pchip` в `v4-canvas.css`, которым эти же метки
нарисованы во всех кадрах, задаёт другое: `min-height: 34`,
`border-radius: 999px`, фон `#fffaf1`, `font: 700 11.5px`, цвет
`rgba(0,0,0,.55)`, обводка `inset 1px rgba(80,50,20,.12)`. Продукт сейчас
посередине: высота 26 (по строке), радиус 999 (по классу), шрифт 10,5/600 (ни то
ни другое), обводка 1,5 px чернил 14 % (по кадру, с объяснением в коде).

**Грозит.** Разница между «радиус 8» и «радиус 999» — это разница между плашкой
и пилюлей: в первом случае метка читается как поле с числом, во втором как
чип-фильтр, на который можно нажать. Метки не нажимаются.

**Предлагаем.** Сами не решали: в остальных спорах этой зоны кадры были согласны
между собой и побеждали прозу, а здесь спорят проза и класс самого канваса — два
голоса дизайна, а не дизайн против кода. Нужно сказать: `.pchip` или строка.
Правка после ответа — четыре значения в одном правиле.

### Обновление PWA (`pwa-update`) — 1 вопрос

#### Обновление PWA: значок облака нарисован не тем, что в продукте

**Расходится.** Зона сведена целиком (159 строк). Разошлись десять, из них
восемь — один и тот же хвост дуги, отступление по которому уже записано в коде.
Остаются два, и оба про рисунок.

| что                        | кадр рисует                                                                                                                  | в продукте                                                                                                                           |
| -------------------------- | ---------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------ |
| значок «нужно обновить»    | три кривые: облако `M18 10h-1.3A5 5 0 108 16h9a3.5 3.5 0 001-6.9z`, стрелка `M12 12v5`, наконечник `M9.5 14.5L12 17l2.5-2.5` | одна кривая `cloudDown`: `M12 12v7m0 0l-3.2-3.2M12 19l3.2-3.2M17.5 16.2A3.9 3.9 0 0016.6 8.6h-1.2A6 6 0 004 10.2a3.5 3.5 0 00.4 6.2` |
| заголовок последней стадии | «Готово!»                                                                                                                    | «Готово»                                                                                                                             |

**Грозит.** Смысл значка один — облако со стрелкой вниз, но пропорции и толщина
у двух вариантов разные, и на 26 px это заметно: у кадра стрелка короче и
вписана в облако, у продукта длиннее и выходит за него. Пока не сказано, чей
рисунок верный, любая сверка значка будет спорить сама с собой. Восклицательный
знак — мелочь, но правило зоны «слова на экране» запрещает восклицания в
подписях ожидания, и кадр им противоречит.

**Предлагаем.** Значок: подтвердить продуктовый `cloudDown` и переснять кадр —
он совпадает с остальными значками набора по толщине. Если верен кадровый, дать
его отдельным файлом: руками из трёх кривых он не собирается. Заголовок: снять
восклицательный знак в кадре, продукт уже без него.

### Набор ролей (кросс-зонное) — 1 вопрос

#### Подложка `--tint`: в наборе две роли, и код взял не ту

**Расходится.** Просроченная строка замеров в чек-ине залита ролью подложки.
Прежняя запись говорила, что роль одна и разъехались значения; проверка 31
августа этого не подтвердила. Ролей в наборе две, и одна точно равна канвасу:
`--v4-tint` `#f6e6dd` — ровно значение канваса `--tint`; `--v4-sand-tint`
`#f3e0d2` — сама по себе. `--v4-tint` заведена 21 августа и уже используется
больше шестидесяти раз в `730-widgets-dashboard.css`, а также в воде, метриках,
PWA и на «Активе». `--v4-sand-tint` живёт своей жизнью в
`000-base-and-gamification.css`.

**Грозит.** Пока запись стояла в прежней формулировке, каждый заход по кадрам
заводил одно и то же отступление и ждал ответа о значении. Отвечать было не о
чем: значение канваса в наборе есть, просто под другим именем.

**Предлагаем.** Вопрос сузился до одного: что означает `--v4-sand-tint` и зачем
она отдельно от `--v4-tint`. Если это та же подложка — заменить в коде на
`--v4-tint` и роль убрать; если разные по смыслу — назвать, где какая. Сами не
меняли: роль относится ко всем четырём наборам. Тот же узел, что вопрос про
заливку чужого дня в зоне «Дата», — один ответ закроет оба места.

### Метод разбора (весь пакет) — 1 вопрос

#### Разбор графики обрезает длинные кривые — шесть иконок непроверяемы

**Расходится.** Раздел «Разбор кадров · графика» режет значение `data-v`
примерно на 95 знаках, и обрыв приходится на середину токена координат. Пример
по зоне «Дата»: `Дата · прошлый день, прокручено · рисунок 08` кончается на
`C168,20 172,4…`, дальше идёт заливка; из 135 строк-рисунков этой зоны так
обрываются три. Строка `как читать графику` говорит: «точки приведены как есть:
они задают форму, и приблизительная форма это другой рисунок» — для обрезанных
кривых это правило исполнить нельзя. Пересчитано по всему пакету: 6 обрезанных
из 2261 — `food-meal` 2, `date-remainders` 1, `home-widgets` 1, `nutrition-tab`
1, `pwa-update` 1.

**Грозит.** Немного по числу, но именно эти строки нельзя ни свести, ни честно
закрыть вердиктом: сверять не с чем. В соседней зоне такая же кривая оказалась
настоящим расхождением — иконка листа рисовалась другой формой, — так что цена
обрыва не нулевая.

**Предлагаем.** Снять предел длины у `data-v` в генераторе разбора хотя бы для
строк графики: там значение и есть данные, а не пересказ, — и пересобрать пакет.
Шесть строк, но без них у метода появляется слепое пятно, о котором никто не
знает, пока не наткнётся. От этого же ответа зависит вторая иконка «Питания»
(вопрос про иконки листа действий выше).

### Открыто, но ход не дизайнера — 6 записей

Эти шесть записей стоят со знаком `?`, но ждут не вас. Перечислены, чтобы список
открытого сходился.

| запись                                                                                      | чей ход                                         | суть                                                                                                                                                                                                     |
| ------------------------------------------------------------------------------------------- | ----------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| «Цикл: запрос 26 августа выполнен наполовину — числа пересчитаны, слова остались прежними»  | владельца — по инсайту                          | числа сделаны точно (множители 0/0/+3/+5, вода 10/0/0/5, волна 20–28, окно 26–28 и 1–5), но заголовок инсайта «Особый период» не называет ни процента, ни ккал; остальное чинится по уже данному запросу |
| «Набор не покрывает два тона, которые просит канвас»                                        | владельца палитры, и не сейчас                  | дорожка заглушки просит чернила 9 % (в наборе 8 и 12), рамка метки планера — 14 % (в наборе 12 и 18). Оба через `color-mix` и оба в списке исключений; смотреть все разом на третьем случае              |
| «Запрос замера живёт в карточке сверки, а не в „Сделай сегодня"»                            | владельца                                       | задача на стыке двух зон, делать вместе со сведением `reports-insights` (869 строк её контракта пока без вердикта). Тот же ответ закроет кадр `Замер · включение с согласием`                            |
| «Регистрация: строка просит у карточек шага зазор 8, в коде 12 плюс тридцать пять заплаток» | наш, отдельной задачей с доступом к живому флоу | строка верна, дизайнеру делать нечего: контейнер стоит на `gap-3` = 12 px, а разницу догоняют 35 отдельных `marginTop`                                                                                   |
| «Питание: лист копирования и переноса не перекрашен в v4 вовсе»                             | владельца                                       | `heys_day_copy_meal_modal_v1.js` прошёл мимо перекраски: ни одного правила в CSS, 48 инлайновых `style`, цвета прежнего набора, 38 мест на запасных значениях                                            |
| «Правка приёма: свайп по строке состава против прямого запрета контракта»                   | владельца                                       | контракт говорит «свайпа во флоу нет», а в листе правки строка состава — свайп с «Копировать / Переместить / Удалить», и крестика нет вовсе. Снять жест — потерять два действия                          |

---

## Раздел 2 — решения, принятые без дизайнера

Все тринадцать уже стоят в коде и работают в продукте. Каждое принято
реализующим, потому что ждать было нельзя или потому что источники спорили между
собой. Нужен ваш ответ по каждому — «так и оставить» или «переиграть».

### 1. Штрихкод: цвет ручного ввода и менеджера кодов переведён на роль акцента

**Решение.** Экраны ручного ввода кода и менеджера штрихкодов
(`.aps-barcode-submit`, `.aps-barcode-manager-add`,
`.aps-barcode-manager-remove`) перекрашены с `#434587` на роль акцента набора.
Геометрию и состав не трогали.

**Почему так.** В карточке продукта тринадцать кадров, из них четыре про сканер:
наведение, не найден, найден, состояния. Экрана за кнопкой «Ввести код вручную»
— она есть в двух кадрах и в контракте — среди них нет; нет и менеджера
штрихкодов. Проверено двумя способами: списком кадров `data-screen-label` и
поиском по словам «вручную», «цифрами», «менеджер» — слово «менеджер» в канвасе
не встречается вовсе. Ждать было нечего: `#434587` — фиолетовый снятой системы,
которого в наборе больше нет ни при каком решении, а заливка главного действия в
этом потоке уже задана рядом (`.aps-create-submit-btn`).

**Что в коде.** Роль акцента вместо `#434587`; форма и состав экранов прежние.

**Если решить иначе.** Тут решать нечего по цвету, но нужен ваш ход по другому:
нарисовать кадр ручного ввода кода и сказать, остаётся ли менеджер штрихкодов в
релизе. Кнопка «Ввести код вручную» стоит в каждом состоянии сканера и в кадре
наведения — это запасной выход из всех четырёх ветвей, и пока экран за ней не
описан, он молча остаётся в прежней системе, куда бы ни ушёл остальной поток.
Если менеджер остаётся — он тоже без кадра.

### 2. Кикер листа «Тренд здоровья» — «За 30 дней»

**Решение.** Кикер листа «Тренд здоровья» получил текст «За 30 дней» вместо
приехавшего из кадра «Тренд здоровья · 30 дней».

**Почему так.** Заголовок листа — «Тренд здоровья», он стоит двумя строками
выше: одно и то же слово шло подряд 15 px жирным и 10 px капителью. Остальные
семнадцать листов держат один приём — кикер называет срез, а не виджет:
«Осталось на сегодня», «Утром», «Этой ночью», «За день», «Сейчас», «Сегодня
вечером». «За 30 дней» — тот же срез, тот же приём, и совпадает с осью графика
этого же листа («30 дней назад» → «сегодня»). Ждать было нельзя: правка кадра
приехала внутрь уже сведённого листа, и оставить прежний дубль «Вес за месяц»
значило вернуть ту самую ошибку, которую кадр только что чинил.

**Что в коде.** `heroKicker: 'За 30 дней'` в `heys_widgets_variants_v4.js:2750`.

**Если решить иначе.** Нужно назвать свой срез — правка одной строки. Но если
вернуть имя виджета, приём кикера ломается на всех восемнадцати листах: одно
исключение превращает правило в случайность, и следующий лист скопирует
исключение.

### 3. Дорожка на развилке разбора — 26 и засечки 8

**Решение.** Дорожка развилки разбора вчерашнего дня приведена к контракту:
высота 26, засечки через 8 (три кадра развилки — «вчера по ощущениям · 19», «по
ощущениям своё число · 16», «сила для пачки · 19» — рисовали 24 и 7).

**Почему так.** Строка `вид дорожки` зоны `checkin-morning` задаёт продукту одну
дорожку — высота 26, радиус 999, фон `--c2`, заливка `--gr2` той же высоты,
ручка 20 фоном `--bg`, засечки под дорожкой через 8. Соседняя строка `дорожка`
прямо перечисляет, где эта дорожка стоит: «у самочувствия, цели по шагам и
оценки пачки», то есть и на развилке. Дорожка мастера уже 26 и живёт в другом
файле — два ползунка одного продукта через экран друг от друга были бы разной
высоты.

**Что в коде.** 26 и 8; отступление от кадра названо в списке исключений гейта и
в вердиктах трёх строк.

**Если решить иначе.** Если 24 на развилке была намеренной, строке `вид дорожки`
нужна оговорка про второй размер: сейчас она читается как «одна дорожка на
продукт», и следующий сводящий экран сделает ровно то же самое.

### 4. Подвал расстановки — «Потяните плитку, чтобы поменять порядок»

**Решение.** В подвале режима расстановки оставлена продуктовая подпись
«Потяните плитку, чтобы поменять порядок» вместо «Долгое нажатие — взять виджет»
из кадра «Главная · дефолтная раскладка».

**Почему так.** Две строки контракта запрещают удержание прямо.
`Жест перетаскивания`: «в режиме расстановки — сразу при касании и сдвиге, без
удержания». `Долгое нажатие · правило продукта` перечисляет шесть смыслов
удержания и добавляет: «Перетаскивание виджетов в режиме расстановки —
единственное исключение: оно начинается сразу при касании и сдвиге, без
удержания». Подпись обучающая: человек прочитает и будет держать палец, а на
сенсорном экране лишнее удержание читается как «не сработало».

**Что в коде.** Продуктовая подпись; слова кадра занесены в список отступлений
гейта копии с этой причиной.

**Если решить иначе.** Если удержание в расстановке всё-таки задумано, менять
придётся не подпись, а обе строки контракта: сейчас они запрещают его дважды.

**Как нашлось.** Дырой в самом гейте копии: слова кадра Главной складывались в
набор «подложка» и отфильтровывались из всех кадров, включая сам кадр-источник,
— то есть не проверялись нигде.

### 5. Ярус «На чём основано» — форма кадра, а не голые строки

**Решение.** Ярус собран карточкой, как в кадрах, а не «голыми строками», как
описывает строка контракта.

**Почему так.** Строка `вид · ярус „На чём основано"` описывает «имя и год 11,5
px/600 тоном `--ac` строками с зазором 6, каждая — ссылка». Восемь кадров (два
листа × четыре палитры) рисуют карточку `.cd` на `--c1` радиусом 20 со строками
`.row` 12,5 px/600, отбивка 13 px, разделитель 1 px, а справа колонка силы
доказательства («сильное») 10,5 px/600. Описание голых строк не оставляет места
колонке силы, которую требует соседняя строка того же контракта: «у записи
реестра есть strength… нет strength у записи — колонка справа пустая». Выполнить
обе строки буквально нельзя, а восемь кадров во всех четырёх палитрах согласны
между собой.

**Что в коде.** Форма кадра; отступление названо комментарием в
`apps/web/styles/modules/734-ui-v4-insights.css` у правил
`.insights-v4-sources__*`.

**Если решить иначе.** Если голые строки задуманы всерьёз, нужно сказать, где в
них показывать силу доказательства — иначе вторая строка контракта останется
невыполнимой.

**Попутно.** Две строки контракта отстали от кода: «дневная часть к реестру пока
не подключена — 189 PMID вкраплены в файлы расчётов» (подключена,
`apps/web/heys_day_bibliography_v1.js`, механизм — общий
`_kernel/heys_kernel_bibliography_v1.js`) и «записей в реестре дневной части
сейчас три» (их двадцать пять). Переписывать не нужно — порядок из них выполнен
ровно как назначено: сначала реестр, потом ярус.

### 6. Скелетона в Отчётах и Инсайтах нет

**Решение.** Скелетон в `reports-insights` не строится: компонент `SkeletonCard`
и 110 строк его CSS сняты, разрешение из гейта убрано, список исключений снова
пуст. Панель ничего не показывает, пока считает.

**Почему так.** Две строки контракта просят скелетон («полосы 11–13 px радиусом
999 тоном `#f4f4f3`»), но знак ожидания в продукте один — спиннер из
`spinners.v4.dc.html`: круг 56 на `--c2`, дуга в `--acs`, и там прямо сказано
«второго вида ожидания нет». Скелетонов в продукте нет ни одного, нигде. Шкала
порогов спиннера: до 300 мс не показывается ничего, 300 мс — 2 с голый спиннер,
дольше 2 с с подписью; строка «расчёт и отказ» («без спиннера и слова
„загрузка": расчёт местный и занимает доли секунды») — это и есть первая ступень
шкалы, а не спор с ней.

**Что в коде.** Скелетона нет. Он и не рисовался: `SkeletonCard` в
`insights/pi_ui_dashboard.js` был объявлен, экспортирован и ни разу не вызван, а
гейт «ни один живой держатель не пульсирует» считал его живым, потому что
проверял употребление класса — а класс употреблялся внутри мёртвой функции.

**Если решить иначе.** Скелетон означал бы второй вид ожидания в продукте, что
контракт спиннеров запрещает прямо. Проще убрать обе просьбы о скелетоне из
`reports-insights`: если расчёт когда-нибудь перевалит за 300 мс, ответ уже есть
— это спиннер, а не полосы.

### 7. «Шкала кеглей» Отчётов не стала гейтом

**Решение.** Взяты соседние строки контракта, а не сводная «шкала кеглей»; гейта
на шкалу не заводили.

**Почему так.** Строка `шкала кеглей` перечисляет шесть размеров — 30/800,
20–21/800, 12–12,5/600–700, 10,5–11/600, проза 12/1,55, сноска 11/1,55 — и
заканчивается «других кеглей в зоне нет». Соседние строки того же контракта
требуют других: `вид · баланс за неделю` — «число со знаком 44 px/600»;
`вид · панель и состояния` — заголовок карточки «15–16 px/700» и кнопки «13
px/700»; `вид · ярус „На чём основано"` — «11,5 px/600». А названных самой
шкалой 30/800 и 20–21/800 в зоне нет вовсе: ни один кадр их не рисует. Гейт,
заведённый буквально, падал бы на четырёх местах, сведённых точно по контракту,
— и его отключили бы в первый день вместе с настоящей пользой.

**Что в коде.** Соседние строки. Вместо гейта на шкалу
`reports-insights-v4-canon.test.js` держит то, что проверяемо без спора: ярусы
набраны одинаково, кнопки одной геометрии, крупные числа обязательно
моноцифрами, третьего вида карточки нет.

**Если решить иначе.** Шкалу надо пересобрать по тому, что зона рисует на самом
деле: сейчас крупное число зоны — 44/600 в герое и 22/600 в плитках, а 30/800 и
20–21/800 похожи на остаток от более раннего пакета.

### 8. Выбор окна в Отчётах — текстовые вкладки, а не чипы-пилюли

**Решение.** Переключатель окна 7 / 14 / 30 собран по кадру: текстовые вкладки с
зазором 16, кегль 11,5/600, выбранная тоном `--ac` с подчёркиванием 2 px тоном
`--acs`, область нажатия 44×44 через отрицательные поля.

**Почему так.** Строка `вид · шапка и период` просит ряд чипов: «выбранный на
--acs тоном --on-acs, остальные на --c2 тоном --ac, высота 28, радиус 999, шрифт
11 px/600, зазор 6». Кадры Отчётов во всех четырёх палитрах рисуют вкладки. Взят
кадр: он согласован в четырёх палитрах, у него уже заложена область нажатия 44,
и он тише — переключатель окна не должен звать громче имени экрана. Пилюля 28 с
заливкой была бы самым заметным элементом строки.

**Что в коде.** Вкладки. Значок календаря перед границами окна добавлен (строка
его не упоминает вовсе), единица «дней» оставлена только у выбранного окна, как
в кадре: три «дней» подряд удлиняли ряд втрое. Границы окна: кадр даёт 11 px/500
чернил 38 %, строка просила 10,5 px/600 чернил 42 % моноцифрами.

**Если решить иначе.** Если пилюли задуманы всерьёз, придётся переписать четыре
кадра — и переключатель станет заметнее имени экрана.

### 9. Три разделителя Отчётов переведены на `--v4-line`

**Решение.** Три разделителя зоны, заданные чёрным литералом (`rgba(0,0,0,.07)`,
`.08`, `.09`), переведены на роль `--v4-line` и убраны из списка долгов гейта.

**Почему так.** Обоснование в гейте линий говорило: «блок песочно-залочен,
`--v4-line` перевернётся в синем тёмном». Оно не выдержало проверки: песочный
замок держит семейство тона, а не значение. `--v4-sand-surface` и
`--v4-sand-hero` в тёмных наборах становятся `#23201b` и `#2f2820` — чёрная
линия на них не видна вовсе. В двух наборах из четырёх лента дней шла без
разделителей, подвал героя — без верхней границы, а строки «Сделай сегодня»
сливались в один блок.

**Что в коде.** `--v4-line` во всех трёх местах. Прохладный оттенок линии в
синем тёмном на тёплой карточке сочли меньшей бедой, чем отсутствующий
разделитель в половине наборов.

**Если решить иначе.** Если тёплая линия на песочно-залоченных блоках важна,
нужна роль под неё: сейчас файл палитры запрещает заводить четвёртую роль линии,
и выбирать приходится из трёх.

**Попутно найдено рядом.** `.reports-v4-days__left` брала `--v4-ink-3` с
запасным `rgba(0,0,0,.55)`: роль даёт 45 %, запасное обещает 55 — код читался
вернее, чем рисовал. Кадр просит 55 %, переведено на `--v4-ink-2`. Гейт
правдивости запасных значений такое не ловит: он сторожит поверхности, а не
ступени чернил; расширение гейта передано владельцу.

### 10. Минус вклада в разборе Score — чернилами, не красным

**Решение.** Отрицательный вклад в каскаде разбора Score остаётся на чернилах;
плюс идёт зелёным `--gr`.

**Почему так.** Строка `карточка · каскад разбора Score` задаёт «плюс `--gr`,
минус `--val-bad`». Строка `роли цвета` той же зоны запрещает это прямо:
«`--val-bad` — только разрушающее действие. Красный не значит „плохое число", он
значит „действие, которое не отменить"». Отрицательный вклад в Score — ровно
плохое число. Взята зонная строка: она общая, объясняет себя и старше частной.
Красный в зоне один, и потратив его на «−2», мы обесцениваем его там, где он
действительно нужен, — и учим человека, что красное в Инсайтах ничего не решает.

**Что в коде.** Минус на чернилах, от плюса его отличает знак и отсутствие
зелёного.

**Если решить иначе.** Либо убрать `--val-bad` из строки каскада, либо разрешить
красный на числах в строке `роли цвета`. Сейчас две строки зоны противоречат
друг другу, и красный в Инсайтах перестаёт значить «не отменить».

**Как нашлось.** Гейтом канона зоны, который сам считает обращения к
`--v4-bad-text` и падает на третьем: написан утром, к вечеру поймал собственную
правку автора.

### 11. Счётчик в шапке Инсайтов — «N дней данных из 30»

**Решение.** Оставлен текст «18 дней данных из 30», 11 px/500, чернила 38 %.

**Почему так.** Строка `счётчик в шапке` задаёт «N дней данных из 30» и правило
счёта — по всей 30-дневной истории, а не внутри окна. Строка
`вид · шапка зрелости` описывает ту же строку иначе: «18 дней данных» 10,5
px/600 моноцифрами тоном `--ac` и, если порог близко, «ещё 12» чернилами 42 %.
Кадр Инсайтов рисует первый вариант — то есть два источника из трёх сходятся, и
это ровно то, что стоит в коде. «Из 30» короче и говорит и счёт, и потолок; «из
30» и «ещё 12» — одно и то же знание, сказанное дважды.

**Что в коде.** «18 дней данных из 30», 11 px/500, чернила 38 %; отступление
названо в списке теста сверки.

**Если решить иначе.** `--ac` сделал бы счётчик акцентным, а он справочный. В
любом случае две строки контракта нужно свести в одну.

### 12. Копирование и перенос приёма помечены `≠`, а не переведены походя

**Решение.** 85 строк разбора трёх кадров `food-meal` («Действие · копировать»,
«копировать без целей», «перенести») помечены `≠` с причиной, экран на роли
набора не переводили.

**Почему так.** Экран собран целиком инлайновыми стилями: 48 блоков `style:`
против четырёх `className` на 688 строк файла `heys_day_copy_meal_modal_v1.js`.
Роли там довоенные, с запасными значениями чужого набора: `--acc` с синим
`#3b82f6`, `--border` с `#e2e8f0`, `--card` с `#fff`, `--text` с `#111827`,
`--muted` с `#64748b`. Перевод инлайнового экрана на роли — это его
переписывание, а не правка, и делать его в середине сведения зоны значит смешать
два разных риска.

**Что в коде.** Экран как был: синие акценты вместо терракоты, белые карточки
вместо песочной бумаги. Важнее другое — **этого не видит ни один гейт**:
поставленный гейт чужих запасных значений читает CSS-модули, а инлайн в JS туда
не попадает; гейты ролей и линий тоже по CSS. Экран, покрашенный мимо набора,
для всей машинерии проверок выглядит чистым.

**Сколько ещё такого.** Легаси-роли довоенного набора (`--acc`, `--border`,
`--card`, `--text`, `--muted`, `--bg`, `--surface`) живут в инлайне в 24 файлах,
338 подстановок. Копирование приёма — крайний случай по доле, а не единственный:
у него 45 ролей на 46 инлайновых блоков при четырёх классах. Для сравнения:
`heys_supplements_v1.js` — 32 роли при 196 инлайнах и 22 классах,
`heys_trial_queue_v1.js` — 17 при 122 и 62, `heys_app_shell_v1.js` — 37 при 65
и 217. Половина всех подстановок приходится на пять файлов.

**Если решить иначе.** Это ход владельца, двумя отдельными задачами: перевод
самого экрана (несколько часов, но именно работа — переписывание экрана) и
четвёртая проверка гейтов по `var(--легаси-роль)` в `style:` внутри JS. Без
второй три нынешних гейта дают ложное «чисто» на всём, что покрашено инлайном.

### 13. Две вкладки одной зоны в синей палитре — сводятся к общим ролям набора

**Решение.** Поверхности карточек зоны Отчётов и Инсайтов сводятся к общим ролям
набора; правка идёт не в этой зоне, а отдельной задачей по всему модулю.

**Почему так.** Поверхности взяты из трёх разных семейств, и в синих наборах они
расходятся: лента дней, динамика и сон — `--v4-sand-surface` (`#f7efe2` в обоих,
тёплая); отказ, «На сегодня всё» и ярус источников — `--v4-surface` (`#f7efe2`
sand, `#eef3f9` blue — холодная); «Стоит внимания» и «Что заметили» —
`--v4-sand-surface-soft` (`#fffaf1`, тёплая). В песочных наборах разницы нет
вовсе, поэтому глазами это не всплывало. В синей палитре карточки Отчётов
остаются песочными, а половина карточек Инсайтов становится холодной — на двух
вкладках, про которые строка `вид · чего в Инсайтах не бывает` говорит: «одна
вкладка одного продукта, а не два разных экрана». Всего в зоне 30 обращений к
семейству `--v4-sand-*`.

**Что в коде.** Пока ничего не менялось. Решено владельцем 31 августа: развилки
нет. «Роль набора в модуле — это ошибка по определению: модуль не должен знать,
что палитра песочная. Синий обязан быть синим целиком». Исключение одно и оно не
про набор, а про роль: там, где контракт виджета прямо называет терракоту героем
(Калории, Оценка дня), цвет остаётся терракотовым во всех четырёх палитрах — но
выражаться это должно отдельной ролью `--v4-hero-act`, а не именем набора. К
поверхностям карточек исключение не относится: оно про терракоту акцента.

**Если решить иначе.** Порядок задан владельцем и менять его тут нельзя: сперва
храповик на новые вхождения, потом список всех 658 мест по модулю и виджету с
количеством — в нём владелец отметит геройские места, — и только потом
механическая замена отдельной задачей. Тридцать мест этой зоны входят в тот
список; комментарий о решении от 12 августа в файле палитр («Sand UI language:
painted modules keep warm look on every theme») переписывается там же, иначе
следующий прочтёт старое и снова возьмёт `--v4-sand-*`.
