# 🔬 Рефакторинг heys_predictive_insights_v1.js

> **Промпт создан:** 2026-01-10  
> **Статус:** Готов к выполнению  
> **Оценка времени:** 12-16 часов (крупный файл!)

---

## 📌 TL;DR

**Цель:** Разделить монолитный файл `heys_predictive_insights_v1.js` (**10,410 строк**) на логические модули с сохранением всей функциональности.

**Что делаем:**
1. Разделить на **10-12 модулей** по доменам (файл значительно больше ожидаемого!)
2. Сохранить IIFE-архитектуру (не переводить на ES modules)
3. Обеспечить backward-compatible API через `HEYS.PredictiveInsights`
4. Добавить JSDoc документацию
5. Написать интеграционные тесты

**Зачем:**
- Улучшить maintainability (сейчас **10,410 строк** в одном файле — это ~380 KB!)
- Упростить отладку и тестирование
- Уменьшить cognitive load при чтении кода
- Подготовить к TypeScript миграции (v3)

**Время:** ~12-16 часов (увеличено из-за реального размера)

---

## 📊 Аудит текущего состояния (ФИНАЛЬНЫЙ)

### Метрики файла

| Метрика | Значение |
|---------|----------|
| **Всего строк** | **10,410** |
| **Размер** | ~380 KB |
| **React компонентов** | **~72** (огромное количество!) |
| **Хелпер-функций** | **~44+** |
| **Константы/конфиги** | 15+ объектов |

### Найденные компоненты (~72 шт, основные)

| Компонент | Назначение |
|-----------|------------|
| `InsightsTab` | Главная вкладка инсайтов |
| `HealthRingsGrid` | Кольца здоровья (4 категории) |
| `StatusHero` | Герой-статус дня 0-100 |
| `CompactInsightCard` | Компактная карточка инсайта |
| `InsightCard` | Базовая карточка инсайта |
| `CrashRiskGauge` | Индикатор риска срыва |
| `WeeklyWrapCard` | Карточка недельного саммари |
| `HeatmapWidget` | Тепловая карта активности |
| `TrendSparkline` | Микро-график тренда |
| `MetricBadge` | Бейдж метрики |
| `InsightPopup` | Popup с деталями инсайта |
| `ScienceTooltip` | Tooltip с научным обоснованием |
| `QuickActionButton` | Кнопка быстрого действия |
| `ExpandableSection` | Раскрывающаяся секция |
| `DataInventoryCard` | Карточка инвентаря данных |
| `WeeklyRingsGrid` | Недельная сетка колец |
| `DayDetailPopup` | Popup детали дня |
| `MacroProgressBar` | Прогресс-бар макросов |
| `NutrientRing` | Кольцо нутриента |
| `ActivityBadge` | Бейдж активности |
| `SleepQualityIndicator` | Индикатор качества сна |
| `StressLevelBadge` | Бейдж уровня стресса |
| `WaterProgressRing` | Кольцо прогресса воды |
| `CalorieBalanceCard` | Карточка баланса калорий |
| `WeightTrendChart` | График тренда веса |
| `GoalProgressWidget` | Виджет прогресса цели |
| `PhenotypeBadge` | Бейдж фенотипа |
| `InsightCategoryFilter` | Фильтр категорий инсайтов |
| `PriorityFilter` | Фильтр приоритетов |
| ... и ещё ~44 компонента |

### Логические блоки кода

| Блок | Строки | Описание |
|------|--------|----------|
| **Константы и конфиги** | 1-800 | SCIENCE_INFO, PRIORITY_LEVELS, категории, зоны, метрики |
| **Утилиты и хелперы** | 800-1500 | форматтеры, валидаторы, parseры |
| **Расчёты статуса** | 1500-2500 | calculateDayStatus, getRiskLevel, scoring |
| **Паттерн-анализ** | 2500-3500 | detectPatterns, analyzeWeeklyTrends, correlations |
| **Prediction Engine** | 3500-4500 | predictWeight, forecastProgress, estimations |
| **Data Inventory** | 4500-5200 | анализ полноты данных |
| **Health Score** | 5200-6000 | расчёт health score, факторы |
| **Weekly Wrap** | 6000-7000 | недельная аналитика |
| **React компоненты (базовые)** | 7000-8500 | InsightsTab, StatusHero, базовые карточки |
| **React компоненты (виджеты)** | 8500-10000 | HeatmapWidget, WeeklyWrapCard, графики |
| **Экспорт и API** | 10000-10410 | HEYS.PredictiveInsights = {...} |

### Зависимости модуля

**Внутренние (HEYS.*):**
- `HEYS.Status` — расчёт статуса дня
- `HEYS.InsulinWave` — данные инсулиновой волны
- `HEYS.ratioZones` — зоны ratio калорий
- `HEYS.Cycle` — менструальный цикл
- `HEYS.Refeed` — refeed days
- `HEYS.Gamification` — XP и достижения
- `HEYS.utils` — утилиты (lsGet, lsSet, formatDate)

**Внешние (глобальные):**
- `React` — UI
- `window.HEYS` — глобальный namespace

---

## 🎯 Целевая архитектура

### Структура файлов после рефакторинга

```
apps/web/
├── heys_predictive_insights_v1.js          # Главный файл (facade, ~200 строк)
├── insights/
│   ├── insights_constants.js               # Константы и конфиги (~400 строк)
│   ├── insights_calculations.js            # Функции расчётов (~800 строк)
│   ├── insights_patterns.js                # Анализ паттернов (~600 строк)
│   ├── insights_predictions.js             # Прогнозы (~600 строк)
│   ├── insights_components.js              # UI компоненты (~900 строк)
│   └── insights_utils.js                   # Утилиты (~200 строк)
```

### API контракт (сохранить!)

```javascript
// Весь публичный API должен остаться доступным:
HEYS.PredictiveInsights = {
  // Расчёты
  analyze: (historyDays, profile, norms) => {...},
  calculateDayStatus: (dayData, profile) => {...},
  getRiskLevel: (factors) => {...},
  
  // Паттерны
  detectPatterns: (days) => {...},
  analyzeWeeklyTrends: (days) => {...},
  
  // Прогнозы
  predictWeight: (history, goal) => {...},
  forecastProgress: (data) => {...},
  
  // Фильтры
  getInsightsByPriority: (insights, priority) => {...},
  getInsightsByCategory: (insights, category) => {...},
  
  // Компоненты
  InsightsTab,
  HealthRingsGrid,
  StatusHero,
  CrashRiskGauge,
  WeeklyWrapCard,
  HeatmapWidget,
  // ... все 13 компонентов
  
  // Константы (read-only)
  SCIENCE_INFO,
  PRIORITY_LEVELS,
  CATEGORIES,
};
```

---

## 📋 Пошаговый план рефакторинга

### Этап 1: Подготовка (30 мин)
- [ ] Создать папку `apps/web/insights/`
- [ ] Создать backup текущего файла
- [ ] Настроить порядок загрузки в `index.html` (insights_*.js → heys_predictive_insights_v1.js)

### Этап 2: Экстракция констант (1 час)
- [ ] Создать `insights_constants.js`
- [ ] Перенести: `SCIENCE_INFO`, `PRIORITY_LEVELS`, `CATEGORIES`, `ACTIONABILITY`, конфиги зон
- [ ] Экспортировать через `HEYS.Insights.Constants = {...}`
- [ ] Проверить что основной файл видит константы

### Этап 3: Экстракция утилит (30 мин)
- [ ] Создать `insights_utils.js`
- [ ] Перенести: форматтеры дат, валидаторы, хелперы
- [ ] Экспортировать через `HEYS.Insights.Utils = {...}`

### Этап 4: Экстракция расчётов (1.5 часа)
- [ ] Создать `insights_calculations.js`
- [ ] Перенести: `calculateDayStatus`, `getRiskLevel`, `getInsights`, связанные функции
- [ ] Обновить зависимости на `HEYS.Insights.Constants`
- [ ] Экспортировать через `HEYS.Insights.Calculations = {...}`

### Этап 5: Экстракция паттернов (1 час)
- [ ] Создать `insights_patterns.js`
- [ ] Перенести: `detectPatterns`, `analyzeWeeklyTrends`, `findCorrelations`
- [ ] Экспортировать через `HEYS.Insights.Patterns = {...}`

### Этап 6: Экстракция прогнозов (1 час)
- [ ] Создать `insights_predictions.js`
- [ ] Перенести: `predictWeight`, `forecastProgress`, `estimateGoalDate`
- [ ] Экспортировать через `HEYS.Insights.Predictions = {...}`

### Этап 7: Экстракция компонентов (1.5 часа)
- [ ] Создать `insights_components.js`
- [ ] Перенести все 13 React компонентов
- [ ] Сохранить внутренние зависимости между компонентами
- [ ] Экспортировать через `HEYS.Insights.Components = {...}`

### Этап 8: Facade и интеграция (1 час)
- [ ] Обновить `heys_predictive_insights_v1.js` как facade
- [ ] Собрать все экспорты в единый `HEYS.PredictiveInsights`
- [ ] Добавить backward-compatible aliases
- [ ] Проверить что весь старый API работает

### Этап 9: Тестирование (1 час)
- [ ] Smoke test в браузере
- [ ] Проверить все компоненты в UI
- [ ] Проверить все API методы в консоли
- [ ] Проверить что нет ошибок в console

---

## ⚠️ Критические правила безопасности

### 1. НЕ менять публичный API
```javascript
// ❌ НЕЛЬЗЯ
HEYS.Insights.analyze(...) // Новый путь

// ✅ НУЖНО сохранить
HEYS.PredictiveInsights.analyze(...) // Старый путь должен работать
```

### 2. Сохранить IIFE архитектуру
```javascript
// Каждый модуль — отдельный IIFE
(function(global) {
  const HEYS = global.HEYS = global.HEYS || {};
  HEYS.Insights = HEYS.Insights || {};
  
  // ... код модуля
  
  HEYS.Insights.Constants = { ... };
})(typeof window !== 'undefined' ? window : global);
```

### 3. Порядок загрузки в index.html
```html
<!-- Сначала модули -->
<script src="insights/insights_constants.js"></script>
<script src="insights/insights_utils.js"></script>
<script src="insights/insights_calculations.js"></script>
<script src="insights/insights_patterns.js"></script>
<script src="insights/insights_predictions.js"></script>
<script src="insights/insights_components.js"></script>
<!-- Потом facade -->
<script src="heys_predictive_insights_v1.js"></script>
```

### 4. Не терять зависимости
```javascript
// В каждом модуле проверять наличие зависимостей
const { Status, InsulinWave, ratioZones } = HEYS;
if (!Status) console.warn('[Insights] HEYS.Status not loaded');
```

---

## 🧪 Чеклист тестирования

### Функциональные тесты
- [ ] `HEYS.PredictiveInsights.analyze()` возвращает корректный объект
- [ ] `HEYS.PredictiveInsights.calculateDayStatus()` работает
- [ ] `HEYS.PredictiveInsights.getRiskLevel()` возвращает уровень
- [ ] `HEYS.PredictiveInsights.detectPatterns()` находит паттерны
- [ ] `HEYS.PredictiveInsights.predictWeight()` даёт прогноз

### UI тесты
- [ ] `InsightsTab` рендерится без ошибок
- [ ] `HealthRingsGrid` показывает 4 кольца
- [ ] `StatusHero` отображает статус 0-100
- [ ] `CrashRiskGauge` показывает риск
- [ ] `WeeklyWrapCard` показывает саммари недели
- [ ] `HeatmapWidget` отображает тепловую карту
- [ ] Все popup'ы открываются по клику

### Регрессионные тесты
- [ ] Нет ошибок в консоли при загрузке
- [ ] Все советы отображаются корректно
- [ ] Научные ссылки (PMID) работают
- [ ] Dark mode работает

---

## 📚 Связанные файлы

| Файл | Назначение |
|------|------------|
| `heys_predictive_insights_v1.js` | Текущий монолит (рефакторим) |
| `heys_status_v1.js` | Зависимость (Status Score) |
| `heys_insulin_wave_v1.js` | Зависимость (инсулиновая волна) |
| `heys_advice_v1.js` | Связанный модуль (советы) |
| `DATA_MODEL_REFERENCE.md` | Документация модели данных |
| `index.html` | Порядок загрузки скриптов |

---

## 📝 Примечания для исполнителя

1. **Перед началом работы:**
   - Прочитать весь файл `heys_predictive_insights_v1.js`
   - Найти все места использования через `grep_search`
   - Создать backup

2. **Во время работы:**
   - После каждого этапа проверять в браузере
   - Не коммитить промежуточные состояния
   - Логировать проблемы

3. **После завершения:**
   - `pnpm type-check && pnpm build`
   - Full smoke test в браузере
   - Обновить `DATA_MODEL_REFERENCE.md` если нужно

---

## Changelog промпта

| Версия | Дата | Изменения |
|--------|------|-----------|
| 1.0 | 2026-01-10 | Создан после детального аудита. Реальный размер: 3543 строки, 13 компонентов |
