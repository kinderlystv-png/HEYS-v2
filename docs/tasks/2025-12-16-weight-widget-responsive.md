# 🏋️ Виджет веса — Адаптивные форматы для всех размеров

> **Дата**: 2025-12-16  
> **Статус**: 📋 Дизайн-система  
> **Файлы**: `heys_widgets_ui_v1.js`, `widget_data.js`

---

## 📌 TL;DR

- **Цель**: Максимально полезное использование пространства виджета веса для каждого размера
- **Размеры**: 8 вариантов (mini 1×1, compact 2×2, medium 3×2, wide 4×2, tall3 2×3, tall 2×4, wide3 4×3, large 4×4)
- **Зачем**: Пользователь сам выбирает сколько деталей хочет видеть

---

## 📊 Доступные данные о весе

| Данные | Описание | Источник |
|--------|----------|----------|
| `current` | Текущий вес (кг) | `day.weightMorning` или `prof.weight` |
| `goal` | Целевой вес (кг) | `prof.weightGoal` |
| `trend` | Тренд кг/день | `weightTrend.slope` |
| `monthChange` | Прогноз на месяц (кг) | `slope × 30` |
| `weekChange` | Изменение за неделю (кг) | `slope × 7` |
| `daysToGoal` | Дней до цели | `(current - goal) / trend` |
| `sparklineData` | Точки для графика | 7-30 дней |
| `dataPoints` | Кол-во измерений | для оценки точности |
| `excludedDays` | Исключённые дни | цикл/refeed |
| `bmi` | Индекс массы тела | `weight / (height/100)²` |
| `bmiCategory` | Категория BMI | normal/overweight/obese |
| `cycleInfo` | Влияние цикла | hasWaterRetention |

---

## 🎨 Матрица контента по размерам

### Mini (1×1) — Только число
```
┌─────┐
│87.3 │
└─────┘
```

**Показываем:**
- ✅ Число веса (87.3)
- ❌ Всё остальное

**CSS**: `font-size: 18px`, `font-weight: 700`

---

### Compact (2×2) — Вес + тренд
```
┌───────────┐
│  ⚖️ Вес   │
│  87.3 кг  │
│    ↓      │
└───────────┘
```

**Показываем:**
- ✅ Заголовок с иконкой
- ✅ Текущий вес (крупно)
- ✅ Стрелка тренда (↓↑→)
- ❌ Числовой тренд
- ❌ Цель

---

### Medium (3×2) — Вес + цель + тренд
```
┌─────────────────┐
│ ⚖️ Вес          │
│ 87.3 кг    ↓    │
│ Цель: 80 кг     │
└─────────────────┘
```

**Показываем:**
- ✅ Заголовок
- ✅ Текущий вес
- ✅ Стрелка + текст тренда ("−0.5 кг/нед")
- ✅ Цель (если есть)
- ❌ График
- ❌ Прогноз

---

### Wide (4×2) — Вес + мини-график 7 дней
```
┌───────────────────────────────┐
│ ⚖️ Вес                         │
│ 87.3 кг  ↓ −0.5     [▁▂▃▂▁▁▁] │
│ Цель: 80 кг  •  ~14 нед       │
└───────────────────────────────┘
```

**Показываем:**
- ✅ Текущий вес
- ✅ Числовой тренд (−0.5 кг/нед)
- ✅ **Мини-спарклайн** (7 точек, height: 24px)
- ✅ Цель + прогноз достижения
- ❌ BMI
- ❌ Прогноз на месяц

---

### Tall3 (2×3) — Вертикальный с прогрессом
```
┌───────────┐
│  ⚖️ Вес   │
│           │
│  87.3 кг  │
│     ↓     │
│ −0.5/нед  │
│           │
│ ████████░ │
│ 80 кг     │
└───────────┘
```

**Показываем:**
- ✅ Текущий вес (крупно)
- ✅ Тренд (стрелка + число)
- ✅ **Прогресс-бар к цели** (вертикальный)
- ✅ Цель
- ❌ График

---

### Tall (2×4) — Вертикальный с графиком
```
┌───────────┐
│  ⚖️ Вес   │
│  87.3 кг  │
│     ↓     │
│ −0.5/нед  │
│           │
│    ╭─╮    │
│   ╱  ╲╮   │
│  ╱    ╰─  │
│           │
│ Цель: 80  │
│ ~14 недель│
└───────────┘
```

**Показываем:**
- ✅ Текущий вес
- ✅ Тренд
- ✅ **Вертикальный график** (7 дней, height: ~60px)
- ✅ Цель + прогноз достижения
- ❌ BMI

---

### Wide3 (4×3) — Широкий с полным графиком
```
┌───────────────────────────────┐
│ ⚖️ Вес                        │
│                               │
│ 87.3 кг   ↓ −0.5 кг/нед       │
│                               │
│     ╭──╮                      │
│    ╱   ╲╮                     │
│   ╱     ╰──────               │
│ 15 16 17 18 19 20 21          │
│                               │
│ Цель: 80 кг  •  Осталось ~14 нед │
└───────────────────────────────┘
```

**Показываем:**
- ✅ Текущий вес (крупно)
- ✅ Числовой тренд
- ✅ **График 7 дней** (height: ~80px)
- ✅ Метки дней
- ✅ Цель + прогноз
- ❌ BMI
- ❌ Прогноз на месяц

---

### Large (4×4) — Полная аналитика
```
┌───────────────────────────────┐
│ ⚖️ Вес                   BMI: │
│ 87.3 кг  ↓ −0.5 кг/нед   26.4 │
│                               │
│        ╭──╮        --- Цель   │
│       ╱   ╲╮                  │
│      ╱     ╰──────            │
│     ╱            ╲            │
│    ╱              ╰ · · ·     │
│ 10 11 12 13 14 15 16   →30    │
│                               │
│ 📊 Прогноз на месяц: −2.1 кг  │
│ 🎯 До цели (80 кг): ~14 нед   │
│ 🌸 Тренд: чистый (без цикла)  │
└───────────────────────────────┘
```

**Показываем:**
- ✅ Текущий вес (крупно)
- ✅ Числовой тренд
- ✅ BMI (число + категория)
- ✅ **Полный график 14 дней + прогноз**
- ✅ Линия цели на графике
- ✅ Метки дней + прогнозные точки пунктиром
- ✅ Прогноз на месяц
- ✅ Время до цели
- ✅ Индикатор "чистого тренда" (если есть исключённые дни цикла)

---

## 🎯 Сводная таблица

| Размер | Cols×Rows | Вес | Тренд | Цель | График | Прогноз | BMI |
|--------|-----------|-----|-------|------|--------|---------|-----|
| mini   | 1×1       | ✅ число | ❌ | ❌ | ❌ | ❌ | ❌ |
| compact| 2×2       | ✅ | ✅ стрелка | ❌ | ❌ | ❌ | ❌ |
| medium | 3×2       | ✅ | ✅ + текст | ✅ | ❌ | ❌ | ❌ |
| wide   | 4×2       | ✅ | ✅ | ✅ | ✅ мини 7д | ✅ недель | ❌ |
| tall3  | 2×3       | ✅ | ✅ | ✅ прогресс | ❌ | ❌ | ❌ |
| tall   | 2×4       | ✅ | ✅ | ✅ | ✅ верт 7д | ✅ | ❌ |
| wide3  | 4×3       | ✅ | ✅ | ✅ | ✅ 7д | ✅ | ❌ |
| large  | 4×4       | ✅ | ✅ | ✅ | ✅ 14д+прогноз | ✅ полный | ✅ |

---

## 📐 Размеры элементов

### Шрифты веса по размерам:
| Размер | Шрифт веса | Шрифт тренда | Шрифт доп. |
|--------|------------|--------------|------------|
| mini   | 18px bold  | —            | —          |
| compact| 28px bold  | 12px         | —          |
| medium | 32px bold  | 14px         | 11px       |
| wide   | 28px bold  | 14px         | 11px       |
| tall3  | 32px bold  | 14px         | 11px       |
| tall   | 28px bold  | 13px         | 11px       |
| wide3  | 32px bold  | 14px         | 12px       |
| large  | 36px bold  | 16px         | 12px       |

### Размеры графика:
| Размер | Width | Height | Точек | Прогноз |
|--------|-------|--------|-------|---------|
| wide   | 80px  | 24px   | 7     | ❌      |
| tall   | 60px  | 60px   | 7     | ❌      |
| wide3  | 200px | 80px   | 7     | ❌      |
| large  | 280px | 120px  | 14    | ✅ 7д   |

---

## 🔧 Расширение widget_data.js

```javascript
getWeightData() {
  const day = this._getDay();
  const prof = this._getProfile();
  const trend = this._calculateWeightTrend();
  const sparkline = this._getWeightSparkline(14); // до 14 дней
  
  const current = day?.weightMorning || prof?.weight || null;
  const goal = prof?.weightGoal || null;
  
  // BMI
  const bmi = prof?.weight && prof?.height 
    ? (prof.weight / Math.pow(prof.height / 100, 2)).toFixed(1)
    : null;
  
  // Прогноз
  const weekChange = trend ? (trend * 7).toFixed(1) : null;
  const monthChange = trend ? (trend * 30).toFixed(1) : null;
  
  // Дней до цели (если тренд в нужном направлении)
  let daysToGoal = null;
  if (current && goal && trend) {
    const diff = current - goal;
    if ((diff > 0 && trend < 0) || (diff < 0 && trend > 0)) {
      daysToGoal = Math.abs(diff / trend);
    }
  }
  
  // Исключённые дни (цикл/refeed)
  const excludedDays = sparkline?.filter(d => d.excluded)?.length || 0;
  
  return {
    current,
    goal,
    trend,                    // кг/день
    weekChange,               // −0.5
    monthChange,              // −2.1
    daysToGoal,               // 98 (дней)
    weeksToGoal: daysToGoal ? Math.round(daysToGoal / 7) : null,
    bmi: bmi ? parseFloat(bmi) : null,
    bmiCategory: this._getBMICategory(bmi),
    sparkline,                // массив точек
    dataPoints: sparkline?.length || 0,
    excludedDays,
    hasCleanTrend: excludedDays > 0
  };
}
```

---

## 📋 Чеклист реализации

### Phase 1: Data Layer (~30 мин)
- [ ] Расширить `getWeightData()` в widget_data.js
- [ ] Добавить `_getWeightSparkline(days)` — массив точек
- [ ] Добавить `_getBMICategory(bmi)` — категория BMI
- [ ] Добавить `_calculateDaysToGoal()` — прогноз достижения

### Phase 2: WeightWidgetContent (~60 мин)
- [ ] Рефакторинг на switch по размеру
- [ ] Mini layout (1×1)
- [ ] Compact layout (2×2)
- [ ] Medium layout (3×2)
- [ ] Wide layout (4×2) + мини-спарклайн
- [ ] Tall3 layout (2×3) + прогресс-бар
- [ ] Tall layout (2×4) + вертикальный график
- [ ] Wide3 layout (4×3) + график
- [ ] Large layout (4×4) + полная аналитика

### Phase 3: Mini Sparkline Component (~30 мин)
- [ ] `WeightMiniSparkline` — SVG компонент
- [ ] Поддержка горизонтального и вертикального режимов
- [ ] Анимация появления

### Phase 4: CSS (~20 мин)
- [ ] `.widget-weight--mini`
- [ ] `.widget-weight--compact`
- [ ] `.widget-weight--medium`
- [ ] `.widget-weight--wide`
- [ ] `.widget-weight--tall3`
- [ ] `.widget-weight--tall`
- [ ] `.widget-weight--wide3`
- [ ] `.widget-weight--large`

---

## 🎨 Цветовая схема

| Состояние | Цвет | Использование |
|-----------|------|---------------|
| Снижается | `#22c55e` (green) | ↓ стрелка, тренд линия |
| Растёт | `#ef4444` (red) | ↑ стрелка, тренд линия |
| Стабильно | `#3b82f6` (blue) | → стрелка |
| Цель | `#8b5cf6` (purple) | линия цели, прогресс |
| Прогноз | `#9ca3af` (gray) | пунктир, прогнозные точки |
| Цикл | `#ec4899` (pink) | зоны задержки воды |

---

## Время: ~2.5 часа

