# 🔧 Рефакторинг heys_day_v12.js — God Object → модульная архитектура

**Дата**: 2025-12-09  
**Время**: ~4-6 часов (можно делить на фазы)  
**Риск**: 🟡 Средний (требует тщательного тестирования)  
**Приоритет**: 🔴 Высокий (файл блокирует дальнейшее развитие)

---

## 🔍 Глубокий аудит промпта

### 📊 Оценка готовности: 90%

| Критерий | Оценка | Комментарий |
|----------|--------|-------------|
| Конкретность | ⭐⭐⭐⭐⭐ | Чёткие границы, метрики, целевая структура |
| Контекст | ⭐⭐⭐⭐⭐ | Полный dependency list, внешние зависимости |
| Блокеры учтены | ⭐⭐⭐⭐⭐ | Phase 0 с критическими проверками |
| Оверкилл | ⭐⭐⭐⭐☆ | Оправдан — 15K строк требуют разбиения |
| Соответствие правилам | ⭐⭐⭐⭐⭐ | UMD, HEYS.*, IIFE — всё учтено |

### ⚠️ Выявленные риски

| Риск | Вероятность | Импакт | Митигация |
|------|-------------|--------|-----------|
| **Stale closures в setDay** | 🟡 Средняя | 🔴 Высокий | Все компоненты используют `setDay(prev => ...)` — ✅ корректно |
| **~80 useState в DayTab** | — | — | Вынос компонентов уменьшит зависимости |
| **158 вызовов HEYS.*** | 🟢 Низкая | 🟡 Средний | Проверить доступность глобалов ДО выноса |
| **Потеря кастомных comparators** | 🟡 Средняя | 🔴 Высокий | Скопировать memo comparators ПОЛНОСТЬЮ |
| **Порядок загрузки скриптов** | 🟢 Низкая | 🔴 Критический | Добавить новые скрипты ПЕРЕД heys_day_v12.js |
| **Внешние зависимости HEYS.Day.*** | 🟡 Средняя | 🔴 Критический | См. секцию "Внешние API" ниже |
| **Custom Events** | 🟢 Низкая | 🟡 Средний | 7 событий — не трогать dispatch/listen |

### 🔌 Внешние API (НЕЛЬЗЯ ЛОМАТЬ!)

**DayTab экспортирует через `HEYS.Day.*`:**
```javascript
HEYS.Day.getStreak()      // Используется в heys_app_v12.js (5 мест)
HEYS.Day.addMeal()        // Используется в heys_app_v12.js (1 место)
HEYS.Day.requestFlush()   // Используется в heys_app_v12.js (3 места)
```

**Custom Events (7 шт):**
- `heys:day-updated` — слушают heys_app_v12.js, heys_cycle_v1.js
- Dispatch из: heys_step_modal_v1.js, heys_steps_v1.js, heys_cycle_v1.js

⚠️ **При рефакторинге эти экспорты должны остаться в heys_day_v12.js!**

### ✅ Что отлично в промпте
- Фазы от низкого к высокому риску — правильный подход
- Паттерн выноса (IIFE + HEYS namespace) — соответствует проекту
- Метрики успеха — измеримые цели

---

## 📊 Текущее состояние

| Метрика | Значение | Норма | Статус |
|---------|----------|-------|--------|
| Строк кода | **15,647** | <500 | 🔴 31x превышение |
| React компонентов | 6 в одном файле | 1 | 🔴 God Object |
| if/else statements | 1,184 | <50 | 🔴 Высокая цикломатическая сложность |
| React.createElement | 1,621 | - | 🟡 Много inline JSX |
| Секций (// ===) | ~80 | - | 🟡 Нужна группировка |

### Компоненты внутри файла

| Компонент | Строки (примерно) | Ответственность |
|-----------|-------------------|-----------------|
| `LazyPhotoThumb` | 87-396 | Lazy loading фото с IntersectionObserver |
| `MealAddProduct` | 1307-1538 | Форма добавления продукта в приём |
| `MealCard` | 1608-2224 | Карточка приёма пищи |
| `ProductRow` | 1539-1607 | Строка продукта в приёме |
| `AdviceCard` | 2225-2430 | Карточка совета |
| `DayTab` | 2431-15647 | **GOD OBJECT** — всё остальное |

### Основные секции DayTab

| Секция | Строки | Описание |
|--------|--------|----------|
| Meal Quality Scoring | 538-1306 | Расчёт качества приёма |
| Hero Metrics Cards | 6713-7363 | Карточки калорий/воды/шагов |
| Sparklines (kcal) | 7924-9224 | График калорий |
| Sparklines (weight) | 9224-9908 | График веса |
| Statistics Block | 10281-11484 | Блок статистики |
| Water Card | 12217-12536 | Карточка воды |
| Insulin Wave | 13219-13597 | Индикатор инсулиновой волны |
| Advice Integration | 13633-13900 | Интеграция советов |
| Modals (10+) | разбросаны | Множество модальных окон |

---

## 🎯 Цели рефакторинга

### 1. Улучшение читаемости
- [ ] Один файл = один компонент/модуль
- [ ] Понятные имена файлов
- [ ] Уменьшение когнитивной нагрузки

### 2. Оптимизация производительности
- [ ] Code splitting для lazy loading
- [ ] Уменьшение bundle size
- [ ] Более точная мемоизация

### 3. Повышение тестируемости
- [ ] Изолированные модули легче тестировать
- [ ] Возможность мокать зависимости
- [ ] Unit тесты для расчётов

### 4. Упрощение поддержки
- [ ] Локализация изменений
- [ ] Меньше merge conflicts
- [ ] Проще onboarding новых разработчиков

---

## 📁 Целевая структура

```
apps/web/
├── heys_day_v12.js              # Только DayTab (оркестрация) — ~2000 строк
├── heys_day_components/
│   ├── MealCard.js              # MealCard + ProductRow — ~800 строк
│   ├── MealAddProduct.js        # Добавление продукта — ~300 строк
│   ├── PhotoGallery.js          # LazyPhotoThumb + галерея — ~400 строк
│   ├── AdviceCard.js            # Карточка совета — ~250 строк
│   ├── HeroMetrics.js           # Карточки метрик — ~700 строк
│   ├── WaterCard.js             # Карточка воды — ~400 строк
│   ├── InsulinWaveIndicator.js  # Индикатор волны — ~500 строк
│   └── StatisticsBlock.js       # Блок статистики — ~1200 строк
├── heys_day_charts/
│   ├── KcalSparkline.js         # График калорий — ~1500 строк
│   ├── WeightSparkline.js       # График веса — ~800 строк
│   └── MoodSparkline.js         # Мини-график настроения — ~200 строк
├── heys_day_modals/
│   ├── TimePickerModal.js       # Выбор времени — ~150 строк
│   ├── ZoneMinutesPicker.js     # Выбор минут тренировки — ~200 строк
│   ├── SleepQualityPicker.js    # Оценка сна — ~150 строк
│   ├── DayScorePicker.js        # Оценка дня — ~150 строк
│   └── EditGramsModal.js        # Редактирование граммов — ~200 строк
└── heys_day_scoring/
    ├── mealQualityScore.js      # Расчёт качества приёма — ~400 строк
    ├── nutrientColors.js        # Цвета нутриентов — ~300 строк
    └── dayScoreCalc.js          # Расчёт оценки дня — ~200 строк
```

**Итого**: ~2000 (DayTab) + ~9000 (модули) = ~11000 строк  
**Сокращение**: ~4600 строк за счёт удаления дублирования и мёртвого кода

---

## 🔄 Пошаговый план (безопасный рефакторинг)

### Фаза 0: Подготовка и блокеры (30 мин) ⚠️ КРИТИЧНО

**Перед началом работы ОБЯЗАТЕЛЬНО:**

```bash
# 1. Проверить текущее состояние
git status
git diff --stat

# 2. Создать branch
git checkout -b refactor/day-v12-modularize

# 3. Бэкап (на случай если что-то пойдёт не так)
cp apps/web/heys_day_v12.js apps/web/heys_day_v12.pre-refactor.js

# 4. Проверить что проект работает
pnpm type-check && pnpm build

# 5. Открыть localhost:3001 и сделать скриншоты:
#    - Вкладка "День" 
#    - Карточка приёма пищи (развёрнутая)
#    - График калорий
#    - Модалка добавления продукта
```

**Создать папки:**
```bash
mkdir -p apps/web/heys_day_components
mkdir -p apps/web/heys_day_charts  
mkdir -p apps/web/heys_day_modals
mkdir -p apps/web/heys_day_scoring
```

**Проверить порядок загрузки скриптов в index.html:**
Новые файлы должны загружаться ПОСЛЕ:
- `heys_day_utils.js`
- `heys_day_hooks.js`
- `heys_models_v1.js`
- `heys_insulin_wave_v1.js`

И ПЕРЕД:
- `heys_day_v12.js`

**⚠️ Проверить внешние API (НЕЛЬЗЯ ЛОМАТЬ!):**
```bash
# Эти экспорты используются в других файлах:
grep -n "HEYS\.Day\." apps/web/heys_app_v12.js
# Должны найтись: getStreak, addMeal, requestFlush
```

**Убедиться что эти функции остаются в heys_day_v12.js после рефакторинга:**
- `HEYS.Day.getStreak()` — экспорт streak для header
- `HEYS.Day.addMeal()` — добавление приёма из shortcut
- `HEYS.Day.requestFlush()` — принудительное обновление

**Проверить E2E тесты:**
```bash
# Запустить базовые тесты чтобы зафиксировать baseline
pnpm test:e2e -- --grep "food-logging|basic"
```

**Чеклист Phase 0:**
- [ ] Git branch создан
- [ ] Бэкап создан
- [ ] `pnpm type-check && pnpm build` проходят
- [ ] E2E тесты проходят: `pnpm test:e2e -- --grep "food-logging|basic"`
- [ ] Скриншоты сделаны (3 приёма + статистика + тренировки)
- [ ] Папки созданы (`heys_day_scoring/`, `heys_day_components/`, и т.д.)
- [ ] Baseline функционала проверен вручную
- [ ] **🔌 Внешний API работает**: `HEYS.Day.getStreak()`, `HEYS.Day.addMeal()`, `HEYS.Day.requestFlush()`

---

### Фаза 1: Выносим scoring логику (30 мин) ⚡ НИЗКИЙ РИСК
**Почему первым**: Это чистые функции без React, легко тестировать.

**Файл `heys_day_scoring/mealQualityScore.js`:**
```javascript
// Константы для выноса (строки 538-575):
MEAL_KCAL_DISTRIBUTION, MEAL_KCAL_ABSOLUTE, IDEAL_MACROS, isMainMealType, safeRatio

// Функции для выноса:
getMealQualityScore()      // строки ~970-1205
getMealQualityBadges()     // строки ~1205-1306
calcKcalScore()            // вспомогательная
calcMacroScore()           // вспомогательная
calcCarbQuality()          // вспомогательная
calcFatQuality()           // вспомогательная
calcGiHarmScore()          // вспомогательная
```

**Файл `heys_day_scoring/nutrientColors.js`:**
```javascript
// Константы (строки 577-595):
NUTRIENT_COLORS = { good: '#16a34a', medium: '#ca8a04', bad: '#dc2626' }

// Функции для выноса:
getNutrientColor()         // строки 596-780
getNutrientTooltip()       // строки 780-900
getDailyNutrientColor()    // строки 808-890
getDailyNutrientTooltip()  // рядом
```

**Зависимости scoring (нужны из DayTab scope):**
- `getMealType()` из `heys_day_utils.js` — ✅ доступен через `HEYS.dayUtils`
- `M.mealTotals()` из `heys_models_v1.js` — ✅ доступен через `HEYS.models`

**Экспорт:**
```javascript
HEYS.DayScoring = {
  getMealQualityScore,
  getMealQualityBadges,
  getNutrientColor,
  getNutrientTooltip,
  getDailyNutrientColor,
  getDailyNutrientTooltip,
  MEAL_KCAL_DISTRIBUTION,
  MEAL_KCAL_ABSOLUTE,
  IDEAL_MACROS,
  NUTRIENT_COLORS
};
```

**Тест после фазы 1:**
- [ ] Добавить продукт → качество приёма показывается
- [ ] Цвета нутриентов корректные (зелёный/жёлтый/красный)
- [ ] `pnpm type-check && pnpm build` проходят

---

### Фаза 2: Выносим модальные окна (45 мин) ⚡ НИЗКИЙ РИСК
**Почему вторым**: Модалки изолированы, имеют чёткие props.

**Модалки для выноса (найти в DayTab ~строки 2999-4100):**

| Модалка | Props | Зависимости |
|---------|-------|-------------|
| `TimePickerModal` | `{isOpen, value, onChange, onClose}` | `HEYS.dayPickers.WheelPicker` |
| `ZoneMinutesPicker` | `{isOpen, zones, onChange, onClose}` | — |
| `SleepQualityPicker` | `{isOpen, value, onChange, onClose}` | — |
| `DayScorePicker` | `{isOpen, value, onChange, onClose}` | — |
| `EditGramsModal` | `{isOpen, grams, product, onChange, onClose}` | slider logic |

**Паттерн каждой модалки:**
```javascript
// heys_day_modals/TimePickerModal.js
;(function(global){
  const HEYS = global.HEYS = global.HEYS || {};
  const React = global.React;
  
  function TimePickerModal({ isOpen, value, onChange, onClose }) {
    if (!isOpen) return null;
    // ... код модалки
  }
  
  HEYS.DayModals = HEYS.DayModals || {};
  HEYS.DayModals.TimePickerModal = TimePickerModal;
})(typeof window !== 'undefined' ? window : global);
```

**Тест после фазы 2:**
- [ ] Каждая модалка открывается
- [ ] Значение меняется и сохраняется
- [ ] Закрытие работает (Escape, overlay click)

---

### Фаза 3: Выносим графики (1.5 ч) 🟡 СРЕДНИЙ РИСК
**Почему третьим**: Графики сложные, но относительно изолированы.

**⚠️ ВНИМАНИЕ: Графики имеют много внутреннего state!**

**KcalSparkline.js (~строки 7924-9224):**
```
Props:
- sparkData: Array<{date, kcal, optimum, ratio, mealCount, ...}>
- period: 7 | 14 | 30
- onPointClick: (date) => void
- selectedDate: string
- isMobile: boolean

Внутренний state:
- sliderValue, sliderIndex — для интерактивного просмотра
- zoomLevel, panOffset — для zoom/pan
- brushStart, brushEnd — для выделения диапазона
- hoveredPoint — для tooltip

Зависимости:
- HEYS.ratioZones — цвета по ratio
- HEYS.Cycle.getWaterRetentionInfo — для розовых зон
- Функции: bezierY(), computeForecast(), getWeekendRanges()

⚠️ Много SVG path генерации — не ломать!
```

**WeightSparkline.js (~строки 9224-9908):**
```
Props:
- weightData: Array<{date, weight, cycleDay}>
- period: 7 | 14 | 30
- onPointClick: (date) => void
- weightGoal: number

Зависимости:
- HEYS.Cycle.shouldExcludeFromWeightTrend
- HEYS.Cycle.getWaterRetentionInfo
```

**MoodSparkline.js (~строки 2933-2998):**
```
Props:
- moodData: Array<{time, mood, wellbeing}>
- compact: boolean

Зависимости: минимальные
```

**Тест после фазы 3:**
- [ ] Графики рендерятся на всех периодах (7/14/30)
- [ ] Интерактивность работает (hover, click, slider)
- [ ] Streak-линии отображаются
- [ ] Розовые зоны цикла видны
- [ ] Zoom/pan работает (если был)

---

### Фаза 4: Выносим компоненты карточек (1.5 ч) 🟡 СРЕДНИЙ РИСК
**Почему четвёртым**: Зависят от scoring и других модулей.

---

#### 4.1. MealCard.js (самый сложный!)

**Строки в оригинале**: 1608-2224

**Props (полный список — 22 шт!):**
```javascript
{
  meal,                  // Meal object
  mealIndex,             // number
  displayIndex,          // number
  products,              // Array<Product>
  pIndex,                // {byId: Map, byName: Map}
  date,                  // string YYYY-MM-DD
  setDay,                // React.Dispatch
  isMobile,              // boolean
  isExpanded,            // boolean
  onToggleExpand,        // (mealIndex, meals) => void
  onChangeMealType,      // (mealIndex, newType) => void
  onChangeTime,          // (mealIndex, time) => void
  onChangeMood,          // (mealIndex, value) => void
  onChangeWellbeing,     // (mealIndex, value) => void
  onChangeStress,        // (mealIndex, value) => void
  onRemoveMeal,          // (mealIndex) => void
  openEditGramsModal,    // (mealIndex, itemId, grams, product) => void
  openTimeEditor,        // (mealIndex) => void
  openMoodEditor,        // (mealIndex) => void
  setGrams,              // (mealIndex, itemId, grams) => void
  removeItem,            // (mealIndex, itemId) => void
  isMealStale,           // (meal) => boolean
  allMeals,              // Array<Meal>
  isNewItem              // (itemId) => boolean
}
```

**Глобалы HEYS.* (обязательно проверить доступность!):**
```javascript
HEYS.InsulinWave.calculateForMeal()
HEYS.InsulinWave.getStatusText()
HEYS.InsulinWave.getStatusColor()
HEYS.insulinWaveData          // глобальное состояние волны
HEYS.dayUtils.haptic          // тактильная связь
HEYS.SwipeableRow             // mobile swipe
HEYS.analytics.trackClick()   // аналитика
HEYS.cloud.deleteMealPhoto()  // удаление фото
```

**Внутренние компоненты (рендерятся внутри):**
- `LazyPhotoThumb` — фото
- `MealAddProduct` — кнопка добавления
- `ProductRow` — строки продуктов (desktop)

**⚠️ КРИТИЧНО: Кастомный comparator!**
```javascript
// ОБЯЗАТЕЛЬНО скопировать при выносе:
React.memo(MealCard, (prevProps, nextProps) => {
  if (prevProps.meal !== nextProps.meal) return false;
  if (prevProps.meal?.mealType !== nextProps.meal?.mealType) return false;
  if (prevProps.meal?.name !== nextProps.meal?.name) return false;
  if (prevProps.meal?.time !== nextProps.meal?.time) return false;
  if (prevProps.meal?.items?.length !== nextProps.meal?.items?.length) return false;
  if (prevProps.meal?.photos?.length !== nextProps.meal?.photos?.length) return false;
  if (prevProps.mealIndex !== nextProps.mealIndex) return false;
  if (prevProps.displayIndex !== nextProps.displayIndex) return false;
  if (prevProps.isExpanded !== nextProps.isExpanded) return false;
  if (prevProps.allMeals !== nextProps.allMeals) return false;
  return true;
});
```

---

#### 4.2. ProductRow.js

**Строки в оригинале**: 1539-1607

**Props:**
```javascript
{
  item,        // {id, product_id?, name, grams, ...inline nutrients}
  mealIndex,   // number
  isNew,       // boolean (fly-in анимация)
  pIndex,      // индекс продуктов
  setGrams,    // handler
  removeItem   // handler
}
```

**Зависимости (из scope, нужно передать или импортировать):**
```javascript
getProductFromItem()  // из heys_day_utils
per100()              // из heys_day_utils
scale()               // из heys_day_utils
r1()                  // округление
```

---

#### 4.3. MealAddProduct.js

**Строки в оригинале**: 1307-1538

**Props:**
```javascript
{
  mi,        // mealIndex
  products,  // Array<Product>
  date,      // string
  day,       // Day object
  setDay     // React.Dispatch
}
```

**Глобалы:**
```javascript
HEYS.showCheckin.addProduct()
HEYS.cloud.uploadMealPhoto()
HEYS.currentClientId
```

**⚠️ КРИТИЧНО: Кастомный comparator!**
```javascript
React.memo(MealAddProduct, (prev, next) => {
  if (prev.mi !== next.mi) return false;
  if (prev.products !== next.products) return false;
  if (prev.day?.meals?.[prev.mi]?.items !== next.day?.meals?.[next.mi]?.items) return false;
  return true;
});
```

---

#### 4.4-4.8. Остальные компоненты

| Компонент | Строки | Сложность | Зависимости |
|-----------|--------|-----------|-------------|
| `LazyPhotoThumb` | 87-396 | 🟡 Средняя | IntersectionObserver, HEYS.sounds |
| `AdviceCard` | 2225-2430 | 🟢 Низкая | haptic только |
| `HeroMetrics` | 6713-7363 | 🟡 Средняя | dayTot, normAbs, popups |
| `WaterCard` | 12217-12536 | 🟡 Средняя | waterGoal, handlers |
| `InsulinWaveIndicator` | 13219-13597 | 🟡 Средняя | HEYS.InsulinWave |
| `StatisticsBlock` | 10281-11484 | 🔴 Высокая | много popups, TDEE |

**Тест после фазы 4:**
- [ ] Все карточки отображаются
- [ ] Добавление продукта работает
- [ ] Swipe на mobile работает
- [ ] Фото загружаются lazy
- [ ] Инсулиновая волна в карточке обновляется

---

### Фаза 5: Рефакторинг DayTab (1 ч) 🟡 СРЕДНИЙ РИСК
**Почему последним**: После выноса модулей DayTab станет оркестратором.

1. Импортировать все вынесенные модули
2. Удалить inline-код, заменить на импорты
3. Упростить state management
4. Добавить `React.lazy()` для тяжёлых компонентов

**Целевой размер DayTab**: ~2000 строк (оркестрация + hooks + state)

### Фаза 6: Оптимизация и тесты (30 мин)
- [ ] `pnpm type-check && pnpm build`
- [ ] Ручное тестирование всех функций
- [ ] Проверка HMR
- [ ] Проверка bundle size

---

## ⚠️ Риски и митигация

| Риск | Вероятность | Митигация |
|------|-------------|-----------|
| Сломать существующий UI | Средняя | Тестирование после каждой фазы |
| Циклические зависимости | Низкая | Строгий порядок импортов |
| Потеря мемоизации | Средняя | Проверить React.memo сохранён |
| Увеличение bundle | Низкая | Code splitting с React.lazy |
| HMR перестанет работать | Низкая | Сохранить IIFE паттерн |

---

## 📋 Чеклист готовности

### После каждой фазы:
- [ ] `pnpm type-check` проходит
- [ ] `pnpm build` проходит
- [ ] HMR работает (изменение → обновление без перезагрузки)
- [ ] Функционал не сломан (ручной тест)

### После всего рефакторинга:
- [ ] Все компоненты рендерятся
- [ ] Добавление продукта работает
- [ ] Графики интерактивны
- [ ] Модалки открываются/закрываются
- [ ] Инсулиновая волна обновляется
- [ ] Советы показываются
- [ ] Dark mode работает
- [ ] Mobile UI корректен

---

## 🔗 Ключевые файлы для чтения

| Файл | Зачем |
|------|-------|
| `apps/web/heys_day_v12.js` | Исходный файл (15647 строк) |
| `apps/web/heys_day_utils.js` | Утилиты (уже вынесены) |
| `apps/web/heys_day_hooks.js` | React хуки (уже вынесены) |
| `apps/web/heys_day_pickers.js` | Пикеры (уже вынесены) |
| `apps/web/heys_models_v1.js` | Модели данных |
| `apps/web/heys_insulin_wave_v1.js` | Модуль инсулиновой волны |
| `apps/web/heys_advice_v1.js` | Модуль советов |

---

## 📝 Паттерн выноса компонента

```javascript
// heys_day_components/MealCard.js
;(function(global){
  const HEYS = global.HEYS = global.HEYS || {};
  const React = global.React;
  
  // Импорт зависимостей через HEYS namespace
  const U = HEYS.dayUtils || {};
  const M = HEYS.models || {};
  const Scoring = HEYS.DayScoring || {};
  
  // Компонент
  const MealCard = React.memo(function MealCard(props) {
    // ... код компонента
  });
  
  // Экспорт
  HEYS.DayComponents = HEYS.DayComponents || {};
  HEYS.DayComponents.MealCard = MealCard;
  
})(typeof window !== 'undefined' ? window : global);
```

---

## 🎯 Метрики успеха

| Метрика | До | После | Цель |
|---------|-----|-------|------|
| heys_day_v12.js строк | 15,647 | ~2,000 | ✅ |
| Файлов компонентов | 1 | ~15 | ✅ |
| Среднее строк/файл | 15,647 | ~700 | ✅ |
| Время загрузки | baseline | -20% | 🎯 |
| Время HMR | baseline | -30% | 🎯 |

---

## 🔙 Rollback план

**Если что-то сломалось:**

```bash
# Вариант 1: Откат к бэкапу
cp apps/web/heys_day_v12.pre-refactor.js apps/web/heys_day_v12.js

# Вариант 2: Откат через git
git checkout apps/web/heys_day_v12.js
git checkout apps/web/index.html  # если менялся порядок скриптов

# Вариант 3: Полный откат branch
git checkout main
git branch -D refactor/day-v12-modularize
```

**После отката:**
1. `pnpm dev` — проверить что работает
2. Удалить созданные папки если нужно
3. Проанализировать что пошло не так

---

## 🟠 Оверкилл-анализ

### ✅ Оправдано:
- 15,647 строк — это реально God Object, разбиение необходимо
- 6 фаз — безопасный инкрементальный подход
- Детальные dependency lists — предотвращают ошибки

### ⚠️ Можно упростить:
- Можно начать только с Фазы 1 (scoring) и оценить эффект
- Фазы 3-4 можно объединить если scoring прошёл гладко
- Некоторые модалки можно оставить inline (мало кода)

### ❌ Потенциально лишнее:
- Отдельные файлы для MoodSparkline (~200 строк) — можно оставить inline
- DayScorePicker, SleepQualityPicker — очень похожи, можно один универсальный

---

## 📋 Соответствие copilot-instructions.md

- ✅ UMD формат (IIFE) — без import/export
- ✅ HEYS.* namespace — не дублировать утилиты
- ✅ localStorage через U.lsSet/lsGet — проверить в выносимом коде
- ✅ Порядок загрузки скриптов — учтён в Phase 0
- ✅ React.memo сохранён — кастомные comparators задокументированы
- ⚠️ Проверить что setDay везде функциональный (`prev => ...`)

---

## Changelog

| Версия | Дата | Изменения |
|--------|------|-----------|
| 1.1.0 | 2025-12-09 | Глубокий аудит: Phase 0 с блокерами, полные dependency lists, rollback план, оверкилл-анализ |
| 1.0.0 | 2025-12-09 | Первоначальная версия промпта |
