# 🍽️ Refeed Day Manual Marker — Маркер загрузочного дня

> **Дата**: 2025-12-12  
> **Критичность**: 🟡 СРЕДНЯЯ  
> **Время**: ~2-3 часа  
> **Статус**: 🆕 Новый

---

## 🎯 Цель

Добавить возможность **вручную отметить день как refeed/loading day** в утреннем чек-ине, с визуальной индикацией и автоматическим исключением из расчёта тренда веса.

### Зачем?

1. **Контроль клиента** — явная маркировка "сегодня можно больше" даёт чувство контроля
2. **Осознанность куратора** — видно что загрузка была запланирована, а не спонтанная
3. **Точность метрик** — исключение refeed дней из weight trend избавляет от ложных тревог о наборе веса
4. **Интеграция с системой долга** — уже существует автоматический `needsRefeed` в `caloricDebt`, но нужен и ручной контроль

---

## 📋 Требования

### Функциональные

1. **Шаг в morning checkin** — новый опциональный шаг "Загрузочный день?" после веса
   - Показывается только если:
     - `caloricDebt.needsRefeed === true` (автоматическое предложение)
     - ИЛИ пользователь имеет право вручную выбрать (для кураторов / advanced users)
   - Варианты ответа:
     - ✅ "Да, сегодня загрузка" → `isRefeedDay: true`
     - ❌ "Нет, обычный день" → `isRefeedDay: false`
     - 🤷 Пропустить (canSkip: true) → не меняем значение

2. **Хранение в DayRecord**
   ```typescript
   interface DayRecord {
     // ... existing fields
     isRefeedDay?: boolean | null;  // null = не указано, true = refeed, false = обычный
   }
   ```

3. **Исключение из weight trend**
   - Модифицировать `weightTrend` useMemo (heys_day_v12.js:8024-8128)
   - Фильтровать дни с `isRefeedDay === true` аналогично `cycleDay` исключению
   - Показывать badge "Тренд без загрузок (X дней исключено)" если были исключения

4. **Визуальная индикация**
   - **В календаре** — иконка 🍽️ или 📈 рядом с датой
   - **В карточке дня** — badge "Загрузочный день" с цветом:
     - 🟢 Зелёный — `refeed_ok` (съедено в пределах adjustedOptimum)
     - 🔴 Красный — `refeed_over` (съедено > adjustedOptimum + 200 ккал)
   - **В debt card** — если день отмечен как refeed вручную, показать бейдж

5. **Интеграция с caloric debt**
   - Если `caloricDebt.needsRefeed === true` И `day.isRefeedDay !== true` → показать предложение
   - Если `day.isRefeedDay === true` → показать статус выполнения:
     - "Загрузка: 2100/2100 ккал ✅" (в пределах)
     - "Загрузка: 2400/2100 ккал ⚠️ +300" (перебор)

---

## 🏗️ Архитектура

### Ключевые файлы

| Файл | Изменения |
|------|-----------|
| `heys_models_v1.js` | Добавить `isRefeedDay?: boolean \| null` в DayRecord |
| `heys_steps_v1.js` | Создать `RefeedDayStepComponent` |
| `heys_morning_checkin_v1.js` | Добавить шаг `'refeedDay'` условно |
| `heys_day_v12.js` | Модифицировать `weightTrend` useMemo, добавить UI badge |
| `heys_advice_v1.js` | Добавить совет `refeed_day_reminder` при `needsRefeed && !isRefeedDay` |
| `DATA_MODEL_REFERENCE.md` | Документировать новое поле |

### Логика показа шага

```javascript
// В getCheckinSteps()
if (HEYS.caloricDebt?.needsRefeed || profile.allowManualRefeed) {
  steps.push('refeedDay');
}
```

### RefeedDayStepComponent

```javascript
function RefeedDayStepComponent({ data, onChange }) {
  const caloricDebt = HEYS.caloricDebt || {};
  const isRefeedDay = data.isRefeedDay ?? null;
  
  return (
    <div className="refeed-step">
      <h3>🍽️ Загрузочный день?</h3>
      {caloricDebt.needsRefeed && (
        <p className="hint">
          Система рекомендует загрузку: долг {caloricDebt.debt} ккал
        </p>
      )}
      
      <div className="options">
        <button 
          className={isRefeedDay === true ? 'active' : ''}
          onClick={() => onChange({ isRefeedDay: true })}
        >
          ✅ Да, загрузка
        </button>
        <button 
          className={isRefeedDay === false ? 'active' : ''}
          onClick={() => onChange({ isRefeedDay: false })}
        >
          ❌ Обычный день
        </button>
      </div>
      
      {isRefeedDay === true && (
        <div className="info">
          💡 Сегодня можно съесть {caloricDebt.adjustedOptimum} ккал (норма +{caloricDebt.refeedBoost})
        </div>
      )}
    </div>
  );
}
```

---

## ✅ Чеклист реализации

### Phase 1: Модель данных (15 мин)

- [ ] Добавить `isRefeedDay?: boolean | null` в `heys_models_v1.js` DayRecord interface
- [ ] Документировать в `DATA_MODEL_REFERENCE.md`

### Phase 2: UI Шаг в чек-ине (45 мин)

- [ ] Создать `RefeedDayStepComponent` в `heys_steps_v1.js`
- [ ] Зарегистрировать шаг через `registerStep('refeedDay', ...)`
- [ ] Добавить условную логику в `getCheckinSteps()` (heys_morning_checkin_v1.js)
- [ ] Сохранение значения в `day.isRefeedDay` через onChange

### Phase 3: Исключение из weight trend (30 мин)

- [ ] Модифицировать `weightTrend` useMemo (heys_day_v12.js:8024)
- [ ] Добавить фильтрацию: `const shouldExclude = dayData.isRefeedDay === true || shouldExcludeFromWeightTrend(...)`
- [ ] Добавить счётчик `refeedDaysExcluded` в return object
- [ ] UI badge: "Тренд (исключено: 2 цикл + 1 загрузка)" если есть исключения

### Phase 4: Визуальная индикация (45 мин)

- [ ] **Календарь** — добавить иконку 🍽️ для дней с `isRefeedDay === true`
- [ ] **День** — Refeed badge в шапке дня:
  ```javascript
  day.isRefeedDay && React.createElement('div', { 
    className: 'refeed-badge ' + (eatenKcal <= adjustedOptimum + 200 ? 'ok' : 'over') 
  },
    eatenKcal <= adjustedOptimum + 200 
      ? '🍽️ Загрузка: ' + eatenKcal + '/' + adjustedOptimum + ' ✅'
      : '🍽️ Загрузка: ' + eatenKcal + '/' + adjustedOptimum + ' ⚠️ +' + (eatenKcal - adjustedOptimum)
  )
  ```
- [ ] **Debt card** — показать "День отмечен как загрузочный" если `isRefeedDay === true`

### Phase 5: Advice integration (15 мин)

- [ ] Добавить совет `refeed_day_reminder`:
  ```javascript
  if (caloricDebt?.needsRefeed && day.isRefeedDay !== true) {
    advice.push({
      id: 'refeed_day_reminder',
      icon: '🍽️',
      text: 'Система рекомендует загрузочный день',
      details: 'Долг ' + caloricDebt.debt + ' ккал. Отметь день в утреннем чек-ине.',
      action: 'Отметить',
      onAction: () => { /* Открыть modal для установки isRefeedDay */ }
    });
  }
  ```

### Phase 6: Testing & Polish (30 мин)

- [ ] Тест: отметить день как refeed → вес +2кг → тренд НЕ показывает набор
- [ ] Тест: refeed day с перебором → показывается "⚠️ +300" badge
- [ ] Тест: автоматический `needsRefeed` → показывается предложение в чек-ине
- [ ] Проверка mobile UX: min-h-11 для кнопок, haptic feedback
- [ ] `pnpm type-check && pnpm build`

---

## 🎨 UX детали

### Цвета

```css
.refeed-badge.ok {
  background: #dcfce7;
  color: #166534;
  border: 1px solid #86efac;
}

.refeed-badge.over {
  background: #fee2e2;
  color: #991b1b;
  border: 1px solid #fca5a5;
}
```

### Анимация

- При выборе "Да, загрузка" — confetti эффект (легкий, 5 частиц)
- Haptic 'medium' при выборе варианта
- Smooth expand для info блока с лимитом калорий

### Mobile

- Кнопки min-h-11 (44px) для touch targets
- Keyboard support: Enter для подтверждения, Escape для пропуска
- Swipe не применяется (обычные кнопки)

---

## 🔬 Edge Cases

1. **Редактирование прошлого дня** — можно менять `isRefeedDay` через отдельную модалку
2. **Bulk mode** — refeed дни остаются актуальными, но adjustedOptimum = optimum × 1.15 (профицит)
3. **Maintenance** — refeed дни могут быть для разнообразия, не только при дефиците
4. **Автоматический + ручной** — если `needsRefeed === true` И пользователь выбрал "Нет" → сохранить `isRefeedDay: false` явно
5. **Вес не взвешивался** — refeed day всё равно можно отметить, просто не будет влияния на тренд

---

## 📊 Метрики успеха

1. **Функциональность** — refeed дни исключаются из weight trend
2. **UX** — пользователь видит предложение при автоматическом `needsRefeed`
3. **Визуализация** — badge статуса загрузки (ok/over) очевиден
4. **Совместимость** — не ломает существующую логику `caloricDebt`
5. **Build** — `pnpm build` проходит без ошибок

---

## 🚨 Риски

| Риск | Вероятность | Митигация |
|------|-------------|-----------|
| Конфликт с автоматическим `needsRefeed` | Средняя | Явная логика: ручной `isRefeedDay` приоритетнее |
| Пользователь злоупотребляет маркером | Низкая | Показывать статистику "Refeed дней за месяц: X" |
| Путаница с cycle days | Низкая | Разные иконки (🍽️ vs 🌸), разные цвета |
| Сломается weight trend | Низкая | Копировать паттерн `cycleDay` исключения |

---

## 💡 Будущие улучшения (не в этом PR)

1. **Refeed streak** — "3 загрузочных дня за месяц" с оптимальным интервалом
2. **Рекомендация интервала** — "Следующая загрузка рекомендована через 5-7 дней"
3. **Refeed templates** — "Легкая загрузка +20%", "Полная загрузка +35%"
4. **Analytics** — отслеживание эффективности refeed дней (вес через N дней после)

---

## Changelog

| Дата | Изменения |
|------|-----------|
| 2025-12-12 | Создание промпта на основе анализа требований |
