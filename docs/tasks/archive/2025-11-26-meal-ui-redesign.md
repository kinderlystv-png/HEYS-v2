# Промпт: Фикс Mobile UI приёмов пищи

**Дата создания**: 2025-11-26  
**Приоритет**: 🔴 Высокий  
**Время выполнения**: ~10 минут

---

## 📋 Проблема (по скриншоту iPhone 14 Pro Max, 430px)

**Grid-карточки работают правильно!** Видно:

- ✅ Название продукта сверху
- ✅ Граммы слева, калории справа
- ✅ БЖУ с лейблами (У: Б: Ж:)
- ✅ Кнопка × в углу

**Реальная проблема**:

1. **Dropdown поиска перекрывает карточку "Яйцо варёное"** — z-index конфликт
2. **Карточка "Яйцо варёное" частично видна** под dropdown — нужно закрыть
   dropdown после выбора

---

## 🎯 Цель

1. Dropdown поиска должен закрываться после выбора продукта
2. Dropdown не должен перекрывать уже добавленные продукты визуально

---

## 📁 Ключевые файлы

| Файл                       | Назначение                           |
| -------------------------- | ------------------------------------ |
| `apps/web/styles/main.css` | z-index для `.suggest-list`          |
| `apps/web/heys_day_v12.js` | `MealAddProduct` — закрытие dropdown |

---

## ✅ Задачи

### Шаг 1: Проверить закрытие dropdown после выбора продукта

**Файл**: `apps/web/heys_day_v12.js`, функция `addProductAndFocusGrams`

Сейчас уже есть:

```js
setSearch('');
setOpen(false);
```

- [ ] Проверить что `setOpen(false)` вызывается при клике на продукт в dropdown

---

### Шаг 2: Убрать конфликт CSS для .tr-sum

**Файл**: `apps/web/styles/main.css`, строки 2378-2387

**Проблема**: Дублирующее правило переопределяет стили:

```css
/* ЭТО КОНФЛИКТУЕТ */
.meals-table .tr-sum {
  display: table-row !important;
}
```

**Решение**: Удалить или уточнить селектор:

```css
/* Удалить строки 2378-2387 — они конфликтуют с более специфичным правилом выше */
```

- [ ] Удалить дублирующее правило для `.meals-table .tr-sum`

---

### Шаг 3: Добавить z-index для карточек продуктов

**Файл**: `apps/web/styles/main.css`

```css
@media (max-width: 768px) {
  /* Dropdown поиска поверх всего */
  .suggest-list {
    z-index: 100 !important;
  }

  /* Карточки продуктов ниже dropdown */
  .meals-table:not(.daily-summary) tbody tr:not(.tr-sum) {
    z-index: 1;
  }
}
```

- [ ] Явные z-index для слоёв

---

## 🧪 Тестирование

- [ ] Выбрать продукт из dropdown → dropdown закрывается
- [ ] Карточки продуктов не перекрываются друг другом
- [ ] Dropdown поверх карточек (пока открыт)
- [ ] iPhone 14 Pro Max в DevTools — всё читаемо

---

## ⚠️ Ограничения

- Минимум правок — баг небольшой
- Grid layout уже работает — не ломать
