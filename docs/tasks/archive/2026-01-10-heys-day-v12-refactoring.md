# 🔧 Рефакторинг монолитных JS-файлов HEYS

> **Версия**: 2.0 | **Дата**: 2026-01-10 | **Приоритет**: HIGH

---

## 📌 TL;DR

**Цель**: Безопасно разбить 4 монолитных файла (~49,000 строк) на модули по domain-driven принципу

**Файлы для рефакторинга** (в порядке выполнения):
1. `heys_predictive_insights_v1.js` — 10,410 строк 🟢 LOW RISK
2. `heys_insulin_wave_v1.js` — 8,741 строк 🟡 MEDIUM RISK  
3. `heys_storage_supabase_v1.js` — 6,010 строк 🔴 HIGH RISK
4. `heys_day_v12.js` — 23,645 строк 🔴 HIGH RISK

**Зачем**:
- Файлы невозможно поддерживать (>6-23K строк каждый)
- 57 useEffect'ов только в heys_day_v12.js
- Долгая загрузка из-за размера
- Невозможно тестировать отдельные части
- Сложно находить и фиксить баги

**Время**: ~40-60 часов (4 модуля по ~10-15 часов)

---

## 🗺️ Граф зависимостей

```
┌─────────────────────────────────────────────────────────────────────────┐
│                        HEYS DEPENDENCY GRAPH                            │
│                    (→ = reads/depends on)                               │
└─────────────────────────────────────────────────────────────────────────┘

                    ┌──────────────────────┐
                    │   heys_day_v12.js    │  ◀──── LAST TO REFACTOR
                    │    (23,645 lines)    │        (depends on ALL)
                    └──────────┬───────────┘
                               │
          ┌────────────────────┼────────────────────┐
          │                    │                    │
          ▼                    ▼                    ▼
┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐
│ insulin_wave    │  │ storage_supabase│  │ predictive_     │
│ (8,741 lines)   │  │ (6,010 lines)   │  │ insights        │
│ 🟡 MEDIUM       │  │ 🔴 HIGH         │  │ (10,410 lines)  │
└────────┬────────┘  └────────┬────────┘  │ 🟢 LOW          │
         │                    │           └─────────────────┘
         │                    │                    │
         ▼                    ▼                    │ reads
   ┌───────────┐        ┌───────────┐             │
   │  Cycle    │        │ products  │◀────────────┘
   │  utils    │        │ ratioZones│
   │  products │        │ Gamification
   └───────────┘        └───────────┘

SAFE REFACTORING ORDER: PI → IW → SS → DY
```

### Матрица зависимостей

| Файл | Читает из | Пишет в | Используется в |
|------|-----------|---------|----------------|
| **predictive_insights** | Status, InsulinWave, ratioZones, Cycle, Refeed, Gamification, utils.lsGet | Только читает! | Day, Reports, InsightsTab |
| **insulin_wave** | Cycle, utils.lsGet/lsSet, products | waveHistory (localStorage) | Day, PredictiveInsights, Advice |
| **storage_supabase** | — | PostgreSQL, localStorage | Все модули через HEYS.cloud |
| **heys_day_v12** | ВСЕ вышеперечисленные | localStorage через store | UI главного экрана |

---

## 🎯 Порядок рефакторинга

### Почему этот порядок?

1. **PredictiveInsights ПЕРВЫЙ** — read-only модуль, не мутирует данные, изолирован
2. **InsulinWave ВТОРОЙ** — пишет только в localStorage (waveHistory), не критичен
3. **Storage ТРЕТИЙ** — критичный, но изолированный API (sync/auth)
4. **Day ПОСЛЕДНИЙ** — зависит от ВСЕХ, максимальный риск

### Риски и митигация

| Модуль | Риск | Причина | Митигация |
|--------|------|---------|-----------|
| PredictiveInsights | 🟢 LOW | Только читает данные | Smoke test после каждого split |
| InsulinWave | 🟡 MEDIUM | Пишет waveHistory | Backup localStorage перед тестами |
| Storage | 🔴 HIGH | Auth, sync с PostgreSQL | Тест на dev аккаунте |
| Day | 🔴 HIGH | Зависит от всего | Рефакторить ПОСЛЕДНИМ |

---

## 📊 Аудит файла

### Метрики
| Параметр | Значение |
|----------|----------|
| **Строк кода** | 23,645 |
| **React компонентов** | ~45-50 |
| **useEffect вызовов** | 57 |
| **useState вызовов** | ~80+ |
| **useMemo вызовов** | ~40+ |
| **Экспортируется в** | `window.HEYS.Day` |

### Структура файла (IIFE паттерн)
```javascript
(function(global) {
  const HEYS = global.HEYS = global.HEYS || {};
  const { useState, useEffect, useMemo, ... } = React;
  
  // Утилиты и хелперы (строки 1-500)
  // UI-компоненты (строки 500-20000)
  // Основной DayTab (строки 20000-23000)
  // Экспорт HEYS.Day (строки 23000-23645)
  
})(typeof window !== 'undefined' ? window : global);
```

### Основные компоненты (найдены)

#### Meal & Products (~6000 строк)
- `MealProductCard` — карточка продукта в приёме
- `MealCard` — карточка приёма пищи
- `MealSection` — секция приёмов за день
- `SearchOverlay` — поиск продуктов
- `CreateProductStep` — создание продукта
- `EditProductModal` — редактирование продукта
- `ProductSearchInput` — инпут поиска

#### Stats & Progress (~4000 строк)
- `ProgressBarComponent` — прогресс-бар калорий
- `GoalProgressBar` — прогресс к цели
- `MacroCard` — карточка макроса
- `MacrosGrid` — сетка макросов
- `StatusCard` — карточка статуса

#### Day Controls (~3000 строк)
- `DayDatePicker` — выбор даты
- `DayNavigator` — навигация по дням
- `DayHeader` — заголовок дня
- `DayWrapper` — обёртка дня с данными

#### Modals (~4000 строк)
- `StepModal` — пошаговый модал
- `CalendarModal` — календарь
- `SettingsModal` — настройки
- `TrainingModal` — добавление тренировки
- `WaterModal` — трекер воды

#### Analytics (~3000 строк)
- `InsulinWaveCard` — инсулиновая волна
- `AdviceCard` — карточка совета
- `StatsOverview` — обзор статистики
- `WeekHeatmap` — тепловая карта недели

#### Main Component (~3000 строк)
- `DayTab` — основной компонент вкладки дня

---

## 🎯 План рефакторинга

### Фаза 1: Подготовка (4 часа)
**Цель**: Создать инфраструктуру без изменения текущего кода

1. **Создать структуру папок**:
   ```
   apps/web/
   ├── heys_day_v12.js (оригинал, без изменений)
   ├── day/
   │   ├── index.js (barrel export)
   │   ├── components/
   │   │   ├── meals/
   │   │   ├── stats/
   │   │   ├── modals/
   │   │   ├── analytics/
   │   │   └── navigation/
   │   ├── hooks/
   │   └── utils/
   ```

2. **Создать тестовый harness**:
   - Snapshot текущего состояния
   - Список всех публичных API
   - Тесты критических путей

3. **Документировать зависимости**:
   - Какие компоненты используют какие хуки
   - Какие пропсы передаются
   - Какие глобальные переменные используются

### Фаза 2: Выделение модулей (12 часов)
**Цель**: Постепенно выделить компоненты в отдельные файлы

#### Порядок выделения (от независимых к зависимым):

**2.1. Утилиты** (2 часа)
```
day/utils/
├── calculations.js    — formatTime, calcMealTotals, etc.
├── formatters.js      — formatKcal, formatMacro, etc.
├── validators.js      — validateMeal, validateProduct, etc.
└── constants.js       — MEAL_TYPES, MACRO_COLORS, etc.
```

**2.2. Базовые UI-компоненты** (2 часа)
```
day/components/
├── Badge.js
├── ProgressRing.js
├── Tooltip.js
├── IconButton.js
└── Card.js
```

**2.3. Доменные компоненты** (8 часов)
```
day/components/meals/
├── MealProductCard.js
├── MealCard.js
├── MealSection.js
├── ProductSearchInput.js
└── index.js

day/components/stats/
├── MacroCard.js
├── MacrosGrid.js
├── GoalProgressBar.js
├── ProgressBarComponent.js
└── index.js

day/components/modals/
├── StepModal.js
├── TrainingModal.js
├── WaterModal.js
├── CalendarModal.js
└── index.js

day/components/analytics/
├── InsulinWaveCard.js
├── AdviceCard.js
├── WeekHeatmap.js
└── index.js
```

**2.4. Хуки** (существующий `heys_day_hooks.js` расширить)
```javascript
// day/hooks/index.js
export { useDayData } from './useDayData';
export { useMeals } from './useMeals';
export { useNutrition } from './useNutrition';
export { useWater } from './useWater';
export { useTraining } from './useTraining';
```

### Фаза 3: Интеграция (8 часов)
**Цель**: Заменить оригинальный код на модули

1. **Создать новый entry point**:
   ```javascript
   // day/DayTab.js
   import { MealSection } from './components/meals';
   import { MacrosGrid } from './components/stats';
   import { useDayData } from './hooks';
   // ... сборка DayTab из модулей
   ```

2. **Обновить heys_day_v12.js**:
   ```javascript
   // Импортировать модули через script tags
   // или использовать простой bundler
   ```

3. **Сохранить обратную совместимость**:
   ```javascript
   // В конце файла
   window.HEYS.Day = {
     DayTab,
     MealCard,
     // ... все публичные компоненты
   };
   ```

---

## ⚠️ Критические правила безопасности

### НЕ ДЕЛАТЬ
1. ❌ Не менять сигнатуры функций
2. ❌ Не переименовывать компоненты
3. ❌ Не менять структуру props
4. ❌ Не удалять "неиспользуемый" код без проверки
5. ❌ Не менять логику useEffect dependencies
6. ❌ Не трогать `window.HEYS.Day` экспорт

### ДЕЛАТЬ
1. ✅ Один компонент = один файл
2. ✅ Сохранять все комментарии
3. ✅ Проверять после каждого выделения
4. ✅ Коммит после каждого успешного шага
5. ✅ Запускать приложение после каждого изменения
6. ✅ Сверять с оригиналом через diff

---

## 🧪 Критерии приёмки

### Функциональные
- [ ] Все приёмы пищи отображаются корректно
- [ ] Добавление/редактирование/удаление продуктов работает
- [ ] Поиск продуктов работает
- [ ] Создание продуктов работает
- [ ] Трекер воды работает
- [ ] Тренировки работают
- [ ] Инсулиновая волна отображается
- [ ] Советы показываются
- [ ] Навигация по дням работает
- [ ] Статистика отображается

### Технические
- [ ] `pnpm type-check` проходит
- [ ] `pnpm build` проходит
- [ ] Нет ошибок в консоли
- [ ] Размер бандла не увеличился более чем на 5%
- [ ] `window.HEYS.Day` содержит все экспорты
- [ ] HMR работает корректно

### Метрики после рефакторинга
| Файл | Строк (target) |
|------|----------------|
| heys_day_v12.js (main) | < 3000 |
| day/components/meals/* | ~2500 |
| day/components/stats/* | ~1500 |
| day/components/modals/* | ~2000 |
| day/components/analytics/* | ~1500 |
| day/hooks/* | ~1000 |
| day/utils/* | ~500 |

---

## 📁 Ключевые файлы

| Файл | Назначение |
|------|------------|
| `apps/web/heys_day_v12.js` | Исходный монолит (23,645 строк) |
| `apps/web/heys_day_hooks.js` | Существующие хуки (расширить) |
| `apps/web/heys_models_v1.js` | Модели данных |
| `apps/web/heys_core_v12.js` | Core утилиты |
| `docs/DATA_MODEL_REFERENCE.md` | Справочник данных |

---

## 🚀 Первые шаги

1. **Создать бэкап**:
   ```bash
   cp apps/web/heys_day_v12.js apps/web/heys_day_v12.backup.js
   ```

2. **Создать структуру папок**:
   ```bash
   mkdir -p apps/web/day/{components/{meals,stats,modals,analytics,navigation},hooks,utils}
   ```

3. **Начать с утилит** — они не имеют зависимостей и безопасны для выделения

4. **После каждого файла**:
   - Запустить `pnpm dev`
   - Проверить работу в браузере
   - Коммит если всё работает

---

## 📝 Чеклист для каждого выделяемого компонента

- [ ] Найти границы компонента в оригинале
- [ ] Определить все зависимости (imports, props)
- [ ] Создать новый файл
- [ ] Скопировать код
- [ ] Добавить необходимые импорты
- [ ] Экспортировать компонент
- [ ] Импортировать в родительский файл
- [ ] Проверить работу
- [ ] Удалить старый код из оригинала (ТОЛЬКО после проверки)

---

## 🔄 Rollback план

При любых проблемах:
```bash
# Восстановить из бэкапа
cp apps/web/heys_day_v12.backup.js apps/web/heys_day_v12.js

# Или через git
git checkout apps/web/heys_day_v12.js
```

---

**Автор**: AI Assistant  
**Для ревью**: @kinderlystv-png
