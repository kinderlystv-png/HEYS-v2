# Mobile UX: Полный статус и финальные правки

**Created**: 2025-11-25  
**Status**: ✅ ЗАВЕРШЕНО  
**Replaces**: `MOBILE_AUDIT_RESULTS.md`, `MOBILE_UX_IMPROVEMENT_PLAN.md`,
`2025-11-24-mobile-ux-phase-2.md`

---

## 🎯 Контекст

Мобильная адаптация HEYS v12 для Day View. Задача: комфортное использование на
телефонах без горизонтального скролла и с компактным layout.

---

## ✅ ЧТО СДЕЛАНО

| Фаза     | Статус | Что                                                     |
| -------- | ------ | ------------------------------------------------------- |
| Phase 1  | ✅     | Структурный рефакторинг (heys_app_v12.js)               |
| Phase 2  | ✅     | Touch targets 44px, safe-area, breakpoints              |
| Phase 3a | ✅     | Горизонтальный скролл исправлен                         |
| Phase 3b | ✅     | Компактный layout (padding 8px, gap 6px)                |
| Phase 3c | ✅     | Modern polish (smooth scroll, focus-visible, selection) |
| PWA      | ✅     | manifest.json, иконки, meta-теги                        |

### Современные фичи (уже есть):

- `safe-area-inset` — `.hdr`, `.tabs`, `.wrap`
- `touch-action: manipulation` — `.btn`, `input`
- `-webkit-tap-highlight-color: transparent`
- `overscroll-behavior-y: none`
- `scroll-behavior: smooth` + `prefers-reduced-motion`
- `:focus-visible` outline
- `::selection` branding

---

## 🔍 ИЗВЕСТНОЕ ОГРАНИЧЕНИЕ (не баг)

### iOS Safari Auto-Zoom

**Ситуация**: В `main.css` строка 1719 — `font-size: 14px` на input.  
iOS Safari зумит страницу при фокусе на input с font-size < 16px.

**Почему НЕ исправляем**:

1. **Компромисс дизайна**: 14px выбран для компактности на mobile. 16px сделает
   layout менее плотным.
2. **Не критично**: Zoom происходит только при фокусе, пользователь может
   отзумить.
3. **Альтернатива хуже**: `maximum-scale=1` блокирует ВСЮ возможность зума
   (accessibility issue).

**Если пользователи пожалуются** — тогда изменить на 16px:

```css
/* main.css строка ~1719 */
font-size: 16px !important; /* Было 14px */
```

---

## 📋 Чеклист (всё выполнено)

### Mobile:

- [x] Нет горизонтального скролла
- [x] Компактный layout
- [x] Touch targets ≥ 36px
- [x] Safe-area для iPhone notch

### Desktop:

- [x] Layout не сломан
- [x] Focus states видимые

### Build:

- [x] CSS версия обновлена (?v=1009)

---

## 🚫 НЕ ДЕЛАТЬ (оверкилл)

- ❌ Swipe navigation — усложнение
- ❌ Pull-to-refresh — конфликт с браузером
- ❌ Virtual scrolling — нет лагов
- ❌ Skeleton loaders — данные локальные
- ❌ Dark mode — отдельная задача

---

## 📝 Архивация

Этот промпт можно переместить в `docs/tasks/archive/` — задача завершена.

Архивированные файлы (корневой `archive/` удалён — использовать git history):

- `MOBILE_AUDIT_RESULTS.md`
- `MOBILE_UX_IMPROVEMENT_PLAN.md`
- `docs/tasks/archive/2025-11-24-mobile-ux-phase-2.md`
