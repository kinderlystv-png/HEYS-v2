---
template-version: 3.1.0
created: 2025-11-24
updated: 2025-11-24
purpose: Mobile UX Phase 2 (Option B) — touch-first polish for legacy HEYS v12
optimization:
  Task-First template — Multi-phase guidance mapped to Mobile UX plan
changelog: initial draft based on MOBILE_UX_IMPROVEMENT_PLAN.md
---

# Task: Mobile UX Improvement Plan — Phase 2 foundation

> **PROMPT**: Выполнить обязательные шаги из Mobile UX Improvement Plan (Option
> B) для комфортного использования HEYS v12 на телефонах. Фокус на CSS
> (Phase 2) + build pipeline фиксы для зависимостей.

## 🎯 WHY (Бизнес-контекст)

**Problem**: Legacy стили заточены под desktop: маленькие тач-таргеты,
отсутствие safe-area, верстка «ломается» на ширине < 768px, сборка часто падает
из-за неготовых workspace-пакетов.

**Impact**: Пользователи HEYS, которые ведут дневник питания с телефона,
вынуждены масштабировать страницу и часто промахиваются по кнопкам/инпутам;
команда не может прогонять `pnpm build` перед релизом.

**Value**: Комфортное управление рационом с телефона (touch-компоненты ≥44px,
safe-area на iOS), предсказуемый build, что ускоряет QA и выпуск фич.

---

## 🤖 Output Preferences

**Workflow**: Implement directly — план расписан в
MOBILE_UX_IMPROVEMENT_PLAN.md, движемся по шагам Phase 2 (CSS) + build fix.

**Code style**: Follow `.github/copilot-instructions.md` — русские комментарии в
legacy JS, минимум новых зависимостей, никаких TypeScript-рефакторингов для v12.

---

## 📋 WHAT (Чек-лист задач)

### ✅ Already Done (для контекста — НЕ ДЕЛАТЬ ЗАНОВО)

- [x] **Phase 1** — `heys_app_v12.js` извлечён из `index.html`, подключен через
      `<script defer>` ✅
- [x] **App shell** — `overscroll-behavior-y: none`,
      `-webkit-tap-highlight-color: transparent` ✅
- [x] **Touch targets** — `.btn`, `input`, `textarea`, `select` имеют
      `min-height: 44px` ✅
- [x] **Active states** — `:active` стили для `.btn`, `.tab`, `.card.clickable`,
      `input` ✅
- [x] **Mobile breakpoints** — `@media (max-width: 640px)` и
      `(max-width: 400px)` добавлены ✅
- [x] **Modal adaptation** — `.modal` и `.modal-backdrop` адаптированы для
      мобильных ✅
- [x] **Viewport** — `viewport-fit=cover` добавлен в `index.html` ✅
- [x] **Build pipeline** — `build:deps` скрипт и удаление `expo/tsconfig.base`
      выполнено ✅

### Must Have (критично для релиза — ОСТАЛОСЬ ДОДЕЛАТЬ)

- [ ] **Safe areas для iOS** — `apps/web/styles/main.css`
  - **Why**: Контент может прятаться за notch/home indicator на iPhone —
    единственная оставшаяся задача Phase 2.
  - **Acceptance**: `.hdr`, `.tabs`, `.page` используют `env(safe-area-inset-*)`
    с fallback через `max()` (см. MOBILE_UX_IMPROVEMENT_PLAN.md → Phase 2 → Шаг
    2.2).
  - **Files**: `apps/web/styles/main.css` (строки ~462-490)
  - **Важно**: НЕ трогать существующие padding значения — только обернуть их
    через `max(базовое_значение, env(safe-area-inset-*))`

### Should Have (важно, но не блокер)

- [ ] **Mobile UX testing** — проверить в Chrome DevTools device mode (iPhone 14
      / Pixel 7)
  - **Why**: Убедиться что safe-area отступы корректны и UI комфортен на
    реальных размерах.
  - **Acceptance**: Минимум скриншот или запись экрана с демонстрацией что
    контент не обрезан на iPhone с notch.
  - **Checklist**:
    - Вкладки не перекрываются notch/home indicator
    - Кнопки удобно нажимать (≥44px target)
    - Модальные окна не выходят за экран на 320px
    - Виртуальная клавиатура не перекрывает inputs (браузер скроллит
      автоматически)

- [ ] **Functional sanity** — после CSS правок убедиться что ничего не сломано
  - **Acceptance**: `window.HEYS.cloud.getStatus()` → `'online'`; данные
    дня/продукты сохраняются в `localStorage` и синхронизируются с Supabase.
  - **Важно**: Если появились ошибки в консоли или перестала работать
    синхронизация — откатить CSS правки и перепроверить.

### Could Have (nice to have)

- [ ] **MOBILE_UX_IMPROVEMENT_PLAN.md update** — добавить чекбокс "✅ Phase 2
      Safe Areas — DONE" в секцию Phase 2 → Шаг 2.2.

---

## ✅ DONE (Критерии приёмки)

### Functional

- [ ] **Safe-area отступы** — `.hdr`, `.tabs`, `.page` используют
      `env(safe-area-inset-*)` через `max()`.
- [ ] **Визуальная проверка** — контент не обрезан на iPhone с notch (проверено
      через Chrome DevTools Device Mode с iPhone 14 Pro).
- [ ] **Ничего не сломано** — приложение работает,
      `window.HEYS.cloud.getStatus()` → `'online'`, данные сохраняются.
- [ ] **Build проходит** — `pnpm --filter @heys/web run build` успешен (warnings
      про `<script>` без `type="module"` допустимы).

### Quality Gates

- [ ] **Testing Strategy**: Manual only (Chrome DevTools Device Mode → iPhone 14
      Pro / Pixel 7).
  - **Что проверять**: safe-area отступы на iPhone, кликабельность элементов,
    модальные окна на 320px, сохранение данных.
  - **Критерий**: Контент не обрезан, всё работает как до правки.
- [ ] **Type safety**: N/A (меняем только CSS, не трогаем TS/JS).
- [ ] **Linting**: N/A (CSS файл вне покрытия eslint).
- [ ] **Tests**: N/A (unit тесты не покрывают CSS; при желании можно запустить
      `pnpm test:all`, но не обязательно).
- [ ] **Build**: `pnpm --filter @heys/web run build` PASS (обязательно —
      проверяем что CSS не сломал сборку).

### Performance

- [ ] **Размер файла**: `main.css` прирастает максимум на 10-20 строк (safe-area
      правки минимальны).
- [ ] **Нет новых JS слушателей**: меняем только CSS, Phase 2 не добавляет
      touch/scroll handlers (они в Phase 3).

### Documentation

- [ ] PR description содержит ссылку:
      `Task prompt: docs/tasks/2025-11-24-mobile-ux-phase-2.md`.
- [ ] В разделе "Testing" PR перечислены реальные устройства/эмуляторы.

---

## 🤖 AI Context (Technical Specs)

### ⚠️ Что УЖЕ СДЕЛАНО (НЕ ТРОГАТЬ)

- Phase 1 (structural refactoring) — DONE ✅
- Phase 2 (базовые CSS правки) — DONE ✅ (кроме safe-area)
- Build pipeline fix — DONE ✅

### 🎯 Что ОСТАЛОСЬ (единственная задача)

Добавить safe-area отступы в **3 CSS-класса**:

- `.hdr` (строка ~462 в `main.css`)
- `.tabs` (строка ~463)
- `.page` (строка ~490)

**Паттерн замены**:

```css
/* БЫЛО */
padding: 16px 18px 12px;

/* СТАНЕТ */
padding: 16px max(18px, env(safe-area-inset-left)) 12px
  max(18px, env(safe-area-inset-right));
padding-top: max(16px, env(safe-area-inset-top));
```

### 🚫 КРИТИЧЕСКИЕ ПРАВИЛА

- **НЕ менять** существующие стили — только добавить `env()` обёртки.
- **НЕ трогать** JS файлы — Phase 2 это только CSS.
- **НЕ удалять** существующие правила — только расширяем через `max()`.
- **НЕ добавлять** новые классы/селекторы — работаем только с `.hdr`, `.tabs`,
  `.page`.

---

## 📂 Quick Reference

| Item          | Location                        | Notes                                                      |
| ------------- | ------------------------------- | ---------------------------------------------------------- |
| Mobile plan   | `MOBILE_UX_IMPROVEMENT_PLAN.md` | Используем Option B (Phase 2) как источник требований      |
| Legacy styles | `apps/web/styles/main.css`      | Все правки здесь; комментарии на русском                   |
| Entry HTML    | `apps/web/index.html`           | Добавить `viewport-fit=cover`, оставить порядок `<script>` |
| Build scripts | `apps/web/package.json`         | `build:deps`, `build`, `build:analyze`                     |
| TS config     | `tsconfig.json`                 | Удалена ссылка на `expo/tsconfig.base`                     |

### Commands

```bash
# Dev server
pnpm --filter @heys/web run dev

# Production build validation
pnpm --filter @heys/web run build

# Device testing shortcut
npx playwright open --device='Galaxy S5' http://localhost:3001   # optional helper
```

---

## 📝 Notes

- **Priority**: medium (большая часть Phase 2 уже сделана, осталась только
  safe-area).
- **Complexity**: XS — только 3 CSS-правила (~15 минут работы).
- **Blockers**: Нет (всё готово, осталась косметическая правка).
- **Related Tasks**:
  - Phase 1 — DONE ✅
  - Phase 2 (основное) — DONE ✅
  - Phase 2 (safe-area) — IN PROGRESS 🔄 (этот промпт)
  - Phase 3 (свайпы) — опционально, создать отдельный промпт при необходимости
  - Phase 4 (performance) — опционально, только при реальных лагах
- **Created**: 2025-11-24 (адаптировано из MOBILE_UX_IMPROVEMENT_PLAN Option B).
- **Updated**: 2025-11-24 — скорректирован под реальное состояние (Phase 1+2 уже
  выполнены).

---

## 🚀 Usage & Next Steps

### Immediate Action

Единственная оставшаяся задача Phase 2:

```bash
# 1. Открыть apps/web/styles/main.css
# 2. Найти классы .hdr, .tabs, .page (строки ~462-490)
# 3. Добавить env(safe-area-inset-*) обёртки через max()
# 4. Проверить в Chrome DevTools Device Mode (iPhone 14 Pro)
# 5. Запустить pnpm --filter @heys/web run build
# 6. Закоммитить: "feat: add iOS safe-area support for mobile UX"
```

### After Completion

1. **PR description**:
   `Based on docs/tasks/2025-11-24-mobile-ux-phase-2.md (safe-area only)`
2. **MOBILE_UX_IMPROVEMENT_PLAN.md**: Отметить ✅ Phase 2 → Шаг 2.2 (Safe Areas)
3. **Testing checkpoint**:
   - Протестировать UX на реальном телефоне (минимум 5 минут активного
     использования)
   - Оценить: нужны ли Phase 3 (свайпы) и Phase 4 (performance)?
   - Если UX комфортный → Phase 3/4 можно отложить
   - Если есть проблемы → создать отдельные prompts для конкретных фикс

### Phase 3/4 Decision

**НЕ начинать Phase 3/4 без:**

- Реального тестирования на телефоне
- Конкретных жалоб пользователей
- Измеренных метрик (лаги, неудобство)
