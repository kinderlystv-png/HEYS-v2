# 🔗 Диагностика трендов — Калории ↔ Вес

**Дата**: 2025-11-29  
**Приоритет**: 🟢 Низкий (улучшение существующего)  
**Время**: ~30 минут  
**Статус**: ✅ ГОТОВ К РЕАЛИЗАЦИИ  
**Версия**: 3.1 (после глубокого аудита)

---

## 🔍 Глубокий аудит (v3.1)

### ✅ Что УЖЕ есть в коде

**Файл**: `heys_day_v12.js`

| Компонент | Строка | Статус |
|-----------|--------|--------|
| `weightTrend` useMemo | 3359 | ✅ Есть: direction, diff, text |
| `kcalTrend` useMemo | 3588 | ✅ Есть: direction, avgKcal, text |
| correlation-block UI | 4456-4481 | ✅ Есть: 4 варианта |
| CSS стили | main.css:4000-4040 | ✅ Есть: .positive/.warning/.mixed |
| Dark mode стили | main.css:6103-6123 | ✅ Есть |

**Вывод**: Базовая инфраструктура ГОТОВА. Нужно только:
1. Расширить тексты (добавить совет)
2. Покрыть edge cases (`plateau`, `no_data`)

### 🚨 Фаза 0: Потенциальные блокеры

#### 0.1 ⚠️ Пропущены edge cases

**Проблема**: Текущий код не обрабатывает:
- `plateau` (вес стабилен, калории в норме)
- `no_data` (мало данных для анализа)
- `weightTrend === null` (нет данных о весе)

**Текущий код**:
```javascript
(kcalTrend && weightTrend) && React.createElement(...)
```

**Риск**: Если нет данных — блок просто скрыт. Пользователь не понимает почему.

**Решение**: Добавить fallback диагноз "Добавьте данные для анализа".

#### 0.2 ⚠️ Несовпадение периодов

**Факт**:
- `weightTrend` = **всегда 7 дней** (hardcoded)
- `kcalTrend` = **зависит от chartPeriod** (7/14/30)

**Риск**: Сравниваем тренд веса за 7 дней с калориями за 30 дней — некорректно!

**Решение**: 
1. **Вариант A** (простой): Оставить как есть, но указать период в UI
2. **Вариант B** (правильный): Синхронизировать периоды

**Рекомендация**: Вариант A (меньше изменений, достаточно для MVP).

#### 0.3 ⚠️ Нет числовых значений в тексте

**Текущий текст**: "Дефицит работает! Вес снижается"

**Проблема**: Не показывает СКОЛЬКО снизился вес.

**Факт**: `weightTrend.diff` УЖЕ содержит число!

**Решение**: Использовать `weightTrend.diff` в тексте.

#### 0.4 ✅ Проверка на оверкилл

| Аспект | Оценка | Комментарий |
|--------|--------|-------------|
| 6 диагнозов | ✅ Норм | 4 уже есть, +2 edge case |
| WEIGHT_KCAL_DIAGNOSES объект | ⚠️ Оверкилл? | Можно inline |
| getDiagnosis() функция | ⚠️ Оверкилл? | Можно switch/case |
| Новый CSS | ❌ Не нужен | Существующие стили достаточны |

**Вывод**: Упростить реализацию — inline логика вместо отдельных объектов.

---

## 🏗️ Архитектура: Советы vs Корреляции

### Модуль советов (Advice) — ГЛАВНЫЙ

**Файл**: `heys_advice_v1.js` (103+ советов)

**Охват**: ВСЁ
- ✅ Моментальные ("что сделать сейчас")
- ✅ Трендовые ("что происходит за период")
- ✅ Корреляционные (сон↔аппетит, стресс↔сладкое)
- ✅ Достижения, мотивация
- ✅ Персонализация

**UI**: FAB панель с тостами

---

### Блок корреляций (Correlation) — ТОЧЕЧНЫЙ

**Место**: Карточка спарклайна калорий/веса

**Охват**: ТОЛЬКО связь калорий ↔ вес

**Формат**: **ОДИН диагноз + совет**

**UI**: Компактный блок под спарклайном (существующий `.correlation-block`)

---

## 📋 Описание

Улучшить существующий блок корреляций — добавить СОВЕТ к каждому диагнозу.

**Сейчас** (4 варианта, только диагноз):
```
🎯 Дефицит работает! Вес снижается
⚠️ Избыток калорий → рост веса
🤔 Вес растёт при дефиците — проверь продукты
💪 Отличная активность компенсирует калории!
```

**Цель**: Формат "Диагноз + Совет" + edge cases

---

## 🎯 Диагнозы калории↔вес (6 вариантов)

| ID | Условие | Emoji | Диагноз | Совет |
|----|---------|-------|---------|-------|
| `working` | deficit && down | 🎯 | Дефицит работает! {diff}кг | Продолжай в том же духе |
| `warning` | excess && up | ⚠️ | Избыток → +{diff}кг | Сократи порции на 10-15% |
| `paradox_up` | deficit && up | 🤔 | Дефицит, но +{diff}кг | Вероятно вода — подожди 2-3 дня |
| `paradox_down` | excess && down | 💪 | Избыток, но {diff}кг | Высокая активность! Следи за сном |
| `plateau` | same && same | 📊 | Баланс: вес стабилен | Для изменений нужен дефицит |
| `no_data` | !weightTrend | 📉 | Мало данных о весе | Добавляй вес каждое утро |

**Примечание**: `{diff}` = `weightTrend.diff` (уже вычислено!)

---

## 🔧 Реализация (МИНИМАЛЬНАЯ)

### Шаг 1: Обновить текст в существующем блоке

**Файл**: `apps/web/heys_day_v12.js`, строка ~4470

**Было**:
```javascript
React.createElement('span', { className: 'correlation-text' },
  kcalTrend.direction === 'deficit' && weightTrend.direction === 'down' 
    ? 'Дефицит работает! Вес снижается' :
  ...
)
```

**Стало**:
```javascript
React.createElement('span', { className: 'correlation-text' },
  kcalTrend.direction === 'deficit' && weightTrend.direction === 'down' 
    ? 'Дефицит работает! ' + r1(weightTrend.diff) + 'кг — продолжай!' :
  kcalTrend.direction === 'excess' && weightTrend.direction === 'up' 
    ? 'Избыток → +' + r1(Math.abs(weightTrend.diff)) + 'кг. Сократи порции' :
  kcalTrend.direction === 'deficit' && weightTrend.direction === 'up' 
    ? '+' + r1(weightTrend.diff) + 'кг при дефиците — вероятно вода' :
  kcalTrend.direction === 'excess' && weightTrend.direction === 'down' 
    ? r1(weightTrend.diff) + 'кг! Активность компенсирует' :
  // plateau: оба same
  kcalTrend.direction === 'same' && weightTrend.direction === 'same'
    ? 'Баланс: вес стабилен' :
  'Калории в норме'
)
```

### Шаг 2: Добавить fallback для отсутствия данных

**Добавить ПЕРЕД блоком корреляций** (строка ~4455):

```javascript
// Fallback если нет данных о весе
!weightTrend && kcalTrend && React.createElement('div', { 
  className: 'correlation-block'
},
  React.createElement('span', { className: 'correlation-icon' }, '📉'),
  React.createElement('span', { className: 'correlation-text' },
    'Добавь вес для анализа связи калорий и веса'
  )
),
```

### Шаг 3: Готово! 

**Не нужно**:
- ❌ Новый CSS (существующий достаточен)
- ❌ WEIGHT_KCAL_DIAGNOSES объект (оверкилл)
- ❌ getDiagnosis() функция (оверкилл)
- ❌ Отдельные `.diagnosis-row` / `.advice-row` (усложнение)

---

## ✅ Критерии готовности

- [ ] 6 вариантов диагнозов с советами (inline)
- [ ] Fallback для отсутствия данных о весе
- [ ] Числовые значения в тексте ({diff}кг)
- [ ] Тест: работает при наличии данных
- [ ] Тест: fallback при отсутствии weightTrend
- [ ] `pnpm type-check && pnpm build`

---

## 🔗 Связанные файлы

| Файл | Что менять |
|------|-----------|
| `apps/web/heys_day_v12.js` | ~20 строк в блоке корреляций |
| `apps/web/styles/main.css` | ❌ НЕ ТРОГАТЬ |
| `apps/web/heys_advice_v1.js` | ❌ НЕ ТРОГАТЬ |

---

## 💡 WOW-идеи (v2, после MVP)

### 🌟 Анимированный прогресс-бар

```
🎯 Дефицит работает! -0.8кг
[████████░░] 80% к цели месяца
```

### 🌟 Сравнение с прошлой неделей

```
🎯 -0.8кг (vs -0.3кг прошлая неделя)
↑ На 167% лучше!
```

### 🌟 Персональный паттерн (30+ дней данных)

```
📊 Твой паттерн: вес падает на 3-4 день дефицита
Сейчас: день 2 → завтра увидишь результат!
```

### 🌟 Умные советы из Advice Module

**Интеграция**: При клике на correlation-block — открыть связанный совет из advice module:
- `working` → показать совет про streak
- `warning` → показать совет про переедание
- `paradox_up` → показать совет про воду

Это сохраняет **Advice как главный модуль**, а correlation block — как **точку входа**.

---

## 📈 Оценка качества промпта

| Критерий | v3.0 | v3.1 |
|----------|------|------|
| Понимание кода | ⚠️ | ✅ Полный анализ 3 файлов |
| Edge cases | ❌ | ✅ plateau, no_data |
| Период данных | ❌ | ⚠️ Указано как риск |
| Числовые значения | ❌ | ✅ Добавлены |
| Оверкилл | ⚠️ | ✅ Убран (минимум изменений) |
| CSS | ⚠️ Новый | ✅ Существующий |
| Время | 45 мин | **30 мин** |
| **Общий балл** | 7/10 | **9/10** |
