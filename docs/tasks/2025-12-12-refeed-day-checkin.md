# 🔄 Загрузочный день (Refeed Day) — Осознанная отметка в чек-ине

**Дата**: 2025-12-12  
**Время**: ~2-3 часа  
**Приоритет**: Средний  
**Статус**: 🆕 Новый

---

## Phase 0 — обязательные проверки перед реализацией

- [ ] `git status` / `git diff --stat` — зафиксировать текущие изменения (сейчас
      в работе: heys_steps_v1.js, heys_morning_checkin_v1.js, heys_day_v12.js,
      heys_ratio_zones_v1.js)
- [ ] Проверить существующий порядок шагов в `heys_morning_checkin_v1.js`:
      сейчас `refeed` уже добавляется условно — решить, оставить условие или
      сделать безусловно после `sleepQuality` (как в задаче)
- [ ] Убедиться, что новые helper-ы не ломают текущий API
      `HEYS.ratioZones.getZone(ratio)` (используется в sparkline/heatmap/advice)
- [ ] Сверить сохранение дня через `lsGet/lsSet` с clientId namespace
      (`heys_dayv2_{date}`) и cloud sync (не писать напрямую в localStorage)
- [ ] Проверить streak/weight-trend расчёты в `heys_day_v12.js` (streak блок
      ~6267, weight-trend ~8024) — добавить refeed без поломки существующей
      логики цикла
- [ ] Уточнить, нужны ли стили из `styles/modules/300-modals-and-day.css`
      (рефид-стили уже есть) — не плодить inline/Tailwind
- [ ] В UI с графиками учесть существующие жесты zoom/pan/haptic, чтобы новые
      бейджи/попапы не ломали обработчики
- [ ] В `heys_advice_v1.js` — проверить приоритет/дедуп/кэш, чтобы новые
      refeed-советы не конфликтовали с блокировкой `kcal_excess_*`

---

## Концепция

**Загрузочный день (Refeed Day)** — это **осознанное** решение клиента съесть
больше нормы. В отличие от срыва (crash/binge), это контролируемый процесс:

- Клиент **заранее планирует** перебор
- Куратор **видит** что это не срыв, а план
- Система **не ругает** за превышение нормы
- **Учёт всё равно ведётся** — клиент трекает еду

### Зачем это нужно?

1. **Метаболическое обоснование**: После нескольких дней дефицита refeed
   помогает восстановить гормональный баланс (лептин, тестостерон)
2. **Психологический контроль**: Клиент не чувствует вину — это часть плана
3. **Прозрачность для куратора**: Куратор понимает почему клиент переел
4. **Gamification**: Не ломает streak (при разумном переборе)

---

## ✅ Принятые решения

1. **Статус дня**: Вариант B — вводим `refeed_ok/refeed_over`, снимаем
   "стыдящие" предупреждения при ratio < 1.35

2. **Streak**: сохраняем серию если `ratio >= 0.70 && ratio < 1.35`

3. **Редактирование**: Да, можно включать/выключать refeed не только в чек-ине,
   но и кнопкой 🔄 в карточке калорий

4. **Режим цели**: Шаг доступен всем (deficit/maintenance/bulk). Тексты
   одинаковые — "загрузочный день" понятен в любом режиме

5. **Причины**: Фиксированный список (4-5 вариантов), без "Другое" для простоты

6. **Место в чек-ине**: Сразу после `sleepQuality`, перед `cycle/measurements`

7. **Тренд веса**: Да, исключать refeed-дни из расчёта "чистого" тренда веса (по
   аналогии с женским циклом) — скачок веса после refeed = вода/гликоген, не жир

---

## Важные уточнения (чтобы не сделать лишнего и не сломать существующий код)

1. **Refeed ≠ автоподнятие нормы.** Отметка клиента — это в первую очередь
   **метка намерения** + изменение тональности/статусов. Автоматическое
   повышение `optimum` у нас уже делает система **Caloric Debt / needsRefeed**.
   Лучше явно развести:
   - `caloricDebt.needsRefeed` = система рекомендует refeed (подсказка)
   - `day.isRefeedDay` = клиент осознанно отметил (факт/намерение)

2. **Не ломать API `HEYS.ratioZones`.** `HEYS.ratioZones.getZone(ratio)` уже
   используется в разных местах. Изменение сигнатуры (добавление `options`)
   может тихо поломать вызовы.

   Рекомендация: добавить отдельный helper (в `heys_day_v12.js` или внутри
   ratioZones как новый метод), который учитывает `dayData`, но оставляет
   `getZone(ratio)` как есть.

3. **Цель refeed дня — "контроль без вины", но не "безлимит".** В UI обязательно
   держать границы: мягкий "зелёный" диапазон до `ratio < 1.35`, а после —
   честная красная подсветка.

4. **Хранение.** Поля должны лежать в `heys_dayv2_{date}` (через StepModal utils
   `lsGet/lsSet`, чтобы корректно работал namespace clientId и синк дня).

---

## Ключевые файлы

| Файл                           | Что делать                                                                             |
| ------------------------------ | -------------------------------------------------------------------------------------- |
| `heys_steps_v1.js`             | Новый шаг `refeed` для утреннего чек-ина                                               |
| `heys_morning_checkin_v1.js`   | Добавить `refeed` в `getCheckinSteps()`                                                |
| `heys_day_v12.js`              | UI: бейдж на графике, карточка калорий + **кнопка 🔄 для включения/выключения refeed** |
| `heys_advice_v1.js`            | Специальные советы для refeed day                                                      |
| `heys_ratio_zones_v1.js`       | Новый метод `getDayZone(ratio, dayData)` — не ломаем старый `getZone`                  |
| `docs/DATA_MODEL_REFERENCE.md` | Документация нового поля                                                               |

---

## Задачи

### 1. Модель данных — DayRecord

Добавить поле в структуру дня:

```javascript
// В DayRecord (heys_day_v12.js, DATA_MODEL_REFERENCE.md)
{
  isRefeedDay: boolean,        // Загрузочный день (отмечен в чек-ине или кнопкой)
  refeedReason: string | null, // Опционально: причина ("тренировка", "праздник", "refeed после дефицита")
  refeedNote: string | null    // Опционально: заметка клиента
}
```

---

### 2. Шаг в утреннем чек-ине

**Файл**: `heys_steps_v1.js`

Создать новый шаг `refeed`:

```javascript
// RefeedStepComponent
// - Переключатель (toggle): "Сегодня загрузочный день"
// - При включении → показать dropdown с причиной:
//   - "После нескольких дней дефицита"
//   - "День рождения / праздник"
//   - "Тяжёлая тренировка"
//   - "Просто нужен отдых от диеты"
// - БЕЗ поля заметки (для простоты)
```

**UI шага**:

```
┌────────────────────────────────────────┐
│  🔄  Сегодня загрузочный день?         │
│                                         │
│  ○ Нет, обычный день                   │
│  ● Да, планирую есть больше            │
│                                         │
│  Причина: [После дней дефицита   ▼]    │
│                                         │
│  💡 Учёт важен даже в загрузочный день! │
└────────────────────────────────────────┘
```

**Причины (фиксированный список)**:

```javascript
const REFEED_REASONS = [
  { id: 'deficit', label: 'После дней дефицита', icon: '📉' },
  { id: 'holiday', label: 'Праздник / событие', icon: '🎉' },
  { id: 'training', label: 'Интенсивная тренировка', icon: '🏋️' },
  { id: 'rest', label: 'Отдых от диеты', icon: '😌' },
];
```

**Регистрация шага**:

```javascript
registerStep('refeed', {
  title: 'Загрузочный день',
  hint: 'Планируете есть больше?',
  icon: '🔄',
  component: RefeedStepComponent,
  getInitialData: () => {
    // Читаем текущее значение из dayData (если уже было установлено кнопкой)
    const todayKey = getTodayKey();
    const dayData = lsGet(`heys_dayv2_${todayKey}`, {});
    return {
      isRefeed: dayData.isRefeedDay || false,
      reason: dayData.refeedReason || null,
    };
  },
  onSave: (data) => {
    const todayKey = getTodayKey();
    const dayData = lsGet(`heys_dayv2_${todayKey}`, {});
    dayData.isRefeedDay = data.isRefeed;
    dayData.refeedReason = data.reason;
    lsSet(`heys_dayv2_${todayKey}`, dayData);
  },
  canSkip: true, // Можно пропустить → обычный день
});
```

---

### 3. Добавить шаг в чек-ин

**Файл**: `heys_morning_checkin_v1.js`

В функции `getCheckinSteps(profile)`:

```javascript
function getCheckinSteps(profile) {
  const steps = [];
  // ... существующая логика ...

  // Базовые шаги
  if (!hasProfileSteps) {
    steps.push('weight');
  }
  steps.push('sleepTime', 'sleepQuality');

  // 🔄 Загрузочный день — СРАЗУ ПОСЛЕ sleepQuality
  steps.push('refeed');

  // Условные шаги (cycle, measurements)
  if (
    HEYS.Steps &&
    HEYS.Steps.shouldShowCycleStep &&
    HEYS.Steps.shouldShowCycleStep()
  ) {
    steps.push('cycle');
  }
  if (
    HEYS.Steps &&
    HEYS.Steps.shouldShowMeasurements &&
    HEYS.Steps.shouldShowMeasurements()
  ) {
    steps.push('measurements');
  }

  // ... остальные шаги
}
```

**Итоговый порядок**:
`weight → sleepTime → sleepQuality → **refeed** → [cycle] → [measurements] → cold_exposure → morning_mood → stepsGoal → morningRoutine`

---

### 4. Кнопка 🔄 в карточке калорий (toggle refeed в течение дня)

**Файл**: `heys_day_v12.js` → блок с optimum/remaining

Добавить кнопку для включения/выключения refeed в любой момент:

```javascript
// В UI карточки калорий, рядом с заголовком или в меню
<button
  onClick={() => toggleRefeedDay()}
  className={`refeed-toggle ${day.isRefeedDay ? 'active' : ''}`}
>
  🔄 {day.isRefeedDay ? 'Refeed ✓' : 'Refeed'}
</button>;

// Функция toggle
function toggleRefeedDay() {
  const newValue = !day.isRefeedDay;
  // Если включаем — показать popup с выбором причины
  if (newValue) {
    showRefeedReasonPopup((reason) => {
      updateDay({ isRefeedDay: true, refeedReason: reason });
    });
  } else {
    updateDay({ isRefeedDay: false, refeedReason: null });
  }
}
```

---

### 5. UI на графике калорий (sparkline)

**Файл**: `heys_day_v12.js` → `KcalSparkline`

Если день отмечен как refeed:

- **Точка на графике**: Оранжевая обводка или заливка
- **Маленькая иконка 🔄** рядом с точкой (если ratio > 1.0)
- **Tooltip**: "Загрузочный день: [причина]"

```javascript
// В рендере точки графика
const isRefeed = dayData.isRefeedDay;
const pointColor = isRefeed ? '#f59e0b' : getRatioColor(ratio); // Оранжевый для refeed
const showRefeedBadge = isRefeed; // Всегда показываем для refeed-дней
```

---

### 6. UI в карточке цели по калориям

**Файл**: `heys_day_v12.js` → блок с optimum/remaining

Если `day.isRefeedDay`:

```
┌─────────────────────────────────────────────┐
│  🔄 ЗАГРУЗОЧНЫЙ ДЕНЬ  [Выкл.]              │
│  Причина: После дней дефицита               │
├─────────────────────────────────────────────┤
│  Норма: 2000 ккал                [████████] │
│  Съедено: 2400 ккал (+20%)                  │
│  ✓ В пределах refeed нормы                  │
├─────────────────────────────────────────────┤
│  💡 Учёт важен даже в загрузочный день!     │
└─────────────────────────────────────────────┘
```

**Поведение**:

- Бейдж 🔄 в заголовке + кнопка [Выкл.] для отмены refeed
- Показать причину (если указана)
- Цвет прогресса — **зелёный до 1.35**, оранжевый если выше
- Подсказка "учёт важен" — напоминание трекать

---

### 7. Новый метод в ratioZones

**Файл**: `heys_ratio_zones_v1.js`

Добавить **новый метод** (не ломаем существующий `getZone`):

```javascript
// Добавить в ratioZones объект:
getDayZone(ratio, dayData) {
  if (dayData?.isRefeedDay) {
    if (ratio < 0.70) return { id: 'refeed_under', name: 'Маловато для refeed', color: '#f59e0b', textColor: '#000' };
    if (ratio < 1.35) return { id: 'refeed_ok', name: 'Загрузочный день ✓', color: '#22c55e', textColor: '#fff' };
    return { id: 'refeed_over', name: 'Даже для refeed много!', color: '#ef4444', textColor: '#fff' };
  }
  return this.getZone(ratio);
},

// Для streak расчёта
isRefeedStreakDay(ratio) {
  return ratio >= 0.70 && ratio < 1.35;
}
```

**Новые зоны для refeed**:

| ID             | Диапазон    | Цвет       | Название                 |
| -------------- | ----------- | ---------- | ------------------------ |
| `refeed_under` | < 0.70      | 🟡 Жёлтый  | "Маловато для refeed"    |
| `refeed_ok`    | 0.70 - 1.35 | 🟢 Зелёный | "Загрузочный день ✓"     |
| `refeed_over`  | > 1.35      | 🔴 Красный | "Даже для refeed много!" |

---

### 8. Советы для refeed дня

**Файл**: `heys_advice_v1.js`

Добавить советы:

| ID                          | Условие                                      | Текст                                               |
| --------------------------- | -------------------------------------------- | --------------------------------------------------- |
| `refeed_planned`            | `isRefeedDay && ratio < 0.5`                 | "Загрузочный день — можно есть смелее! 🔄"          |
| `refeed_on_track`           | `isRefeedDay && ratio 0.9-1.2`               | "Загрузочный день идёт по плану ✓"                  |
| `refeed_protein_focus`      | `isRefeedDay && proteinPct < 0.8`            | "В refeed day белок особенно важен — идёт в мышцы!" |
| `refeed_dont_skip_tracking` | `isRefeedDay && mealCount < 2 && hour >= 14` | "Не забывай трекать даже в загрузочный день!"       |
| `refeed_morning_carbs`      | `isRefeedDay && hour < 12 && carbsPct < 0.3` | "Утром refeed — лучшее время для углеводов 🥐"      |

**Блокировка обычных советов о переборе** при `isRefeedDay`:

- `kcal_excess_critical` — НЕ показывать если refeed и ratio < 1.35
- `kcal_excess_mild` — НЕ показывать если refeed
- `crash_support` (про переедание) — НЕ показывать если refeed

---

### 9. Streak логика

**Файл**: `heys_day_v12.js` → расчёт streak

```javascript
// Обновить функцию isStreakDay
function isStreakDay(ratio, dayData) {
  if (dayData?.isRefeedDay) {
    // Refeed сохраняет streak если в пределах 0.70-1.35
    return HEYS.ratioZones.isRefeedStreakDay(ratio);
  }
  return HEYS.ratioZones.isSuccess(ratio); // Обычная логика (0.75-1.15)
}
```

---

### 10. Интеграция с Caloric Debt

**Связь**: Если система определила `needsRefeed: true`, предложить отметить
refeed:

```javascript
// В RefeedStepComponent или в карточке калорий
const caloricDebt = getCaloricDebt(); // Из контекста
const suggestRefeed = caloricDebt?.needsRefeed && !day.isRefeedDay;

// UI подсказка
{
  suggestRefeed && (
    <div className="refeed-suggestion">
      💡 Система рекомендует refeed — накопился дефицит {caloricDebt.debt} ккал
      <button onClick={() => toggleRefeedDay()}>Отметить refeed</button>
    </div>
  );
}
```

---

### 11. Отображение для куратора

**Где**: Обзор дня клиента (если есть кабинет куратора)

Куратор должен видеть:

- Бейдж 🔄 рядом с датой
- Причину refeed
- Что это **осознанный** выбор, не срыв

**Формат в экспорте/API**:

```json
{
  "date": "2025-12-12",
  "isRefeedDay": true,
  "refeedReason": "deficit",
  "ratio": 1.28,
  "status": "refeed_ok"
}
```

---

### 12. Исключение из тренда веса

**Файл**: `heys_day_v12.js` → WeightSparkline / расчёт тренда

По аналогии с женским циклом, исключать refeed-дни из расчёта "чистого" тренда
веса:

```javascript
// В функции расчёта тренда веса
function shouldExcludeFromWeightTrend(dayData) {
  // Женский цикл — уже есть
  if (HEYS.Cycle?.shouldExcludeFromWeightTrend(dayData.cycleDay)) return true;

  // Refeed день — НОВОЕ
  if (dayData.isRefeedDay) return true;

  return false;
}
```

**Визуализация на sparkline веса**:

- Оранжевая обводка для refeed-дней (по аналогии с розовыми для цикла)
- Бейдж "🔄 чистый" — тренд рассчитан без refeed-дней
- Сноска под графиком: "Дни refeed исключены из тренда (задержка воды)"

---

## Критерии готовности

- [ ] Шаг `refeed` в `heys_steps_v1.js`
- [ ] Шаг добавлен в `getCheckinSteps()` после `sleepQuality`
- [ ] Поле `isRefeedDay` сохраняется в DayRecord
- [ ] Кнопка 🔄 в карточке калорий для toggle в течение дня
- [ ] Бейдж 🔄 на sparkline для refeed-дней
- [ ] Карточка калорий показывает статус refeed
- [ ] Метод `getDayZone(ratio, dayData)` в ratioZones
- [ ] Минимум 5 советов для refeed дня
- [ ] Обычные советы о переборе блокируются для refeed
- [ ] Streak логика: сохраняем при ratio 0.70-1.35
- [ ] Refeed-дни исключаются из тренда веса
- [ ] Документация в DATA_MODEL_REFERENCE.md

---

## Тестирование

1. **Утренний чек-ин**:
   - [ ] Шаг показывается после sleepQuality
   - [ ] Radio-кнопки работают
   - [ ] Причина показывается только при "Да"
   - [ ] Можно пропустить

2. **Кнопка в карточке калорий**:
   - [ ] Toggle включает/выключает refeed
   - [ ] При включении появляется popup с причиной
   - [ ] При выключении причина сбрасывается

3. **UI дня**:
   - [ ] Бейдж на графике (sparkline)
   - [ ] Карточка калорий с refeed-статусом
   - [ ] Цвета: зелёный до 1.35, красный после

4. **Советы**:
   - [ ] Советы для refeed показываются
   - [ ] Обычные советы о переборе НЕ показываются

5. **Streak**:
   - [ ] Refeed 0.70-1.35 сохраняет streak
   - [ ] Refeed > 1.35 ломает streak
   - [ ] Refeed < 0.70 ломает streak

6. **Тренд веса**:
   - [ ] Refeed-дни исключены из расчёта тренда
   - [ ] Оранжевая обводка на точках refeed-дней
   - [ ] Сноска "🔄 чистый" если есть исключённые дни

---

## Примеры использования

### Сценарий 1: После недели дефицита

Клиент 5 дней ел с дефицитом -20%. Утром отмечает refeed → система не ругает за
+30% калорий, streak сохраняется.

### Сценарий 2: День рождения

Клиент знает что будет торт. Отмечает refeed кнопкой в карточке калорий с
причиной "Праздник" → куратор видит осознанный выбор.

### Сценарий 3: После тяжёлой тренировки

Вчера была силовая, сегодня мышцам нужна еда → отмечает refeed с причиной
"Тренировка".

### Сценарий 4: Забыл отметить утром

Клиент в обед понял что сегодня будет праздничный ужин → нажимает кнопку 🔄 в
карточке калорий, выбирает причину "Праздник".

---

## Связанные задачи

- [💰 Caloric Debt System](../DATA_MODEL_REFERENCE.md#-caloric-debt-калорийный-долг)
  — интеграция с `needsRefeed`
- Кабинет куратора — отображение refeed-статуса клиента

---

## Что уточнить по текущему коду

- `heys_morning_checkin_v1.js`: шаг `refeed` уже добавляется условно — решить,
  нужно ли оставить условие или сделать безусловно после `sleepQuality` (по
  задаче).
- `heys_ratio_zones_v1.js`: не трогаем сигнатуру `getZone(ratio)`, добавляем
  новый helper для dayData.
- `heys_day_v12.js`: streak-блок (~6267) и weight-trend (~8024) сейчас учитывают
  только цикл; refeed нужно добавить туда же без поломки calc.
- Sparkline/карточка калорий используют жесты zoom/pan/haptic — новые
  бейджи/попапы не должны блокировать существующие обработчики.
- Сохранение дня и toggle: только через `lsGet/lsSet` с namespace
  `heys_dayv2_{date}`, без прямого `localStorage`.
- `heys_advice_v1.js`: следовать приоритетам/дедупу, не дублировать
  `kcal_excess_*` при refeed < 1.35.

---

## Риск-матрица

| Риск                                                         | Вероятность | Импакт  | Митигация                                                                                     |
| ------------------------------------------------------------ | ----------- | ------- | --------------------------------------------------------------------------------------------- |
| Сломать вызовы `getZone(ratio)` (heatmap/строки/советы)      | Средняя     | Высокий | Новый helper `getDayZone` без изменения сигнатуры `getZone`; регресс на графиках/советах.     |
| Неправильный порядок шагов чек-ина                           | Средняя     | Средний | Проверить финальный список шагов и условные шаги (cycle/measurements); UI smoke-тест чек-ина. |
| Streak/weight-trend не учтут refeed или сломают цикл         | Средняя     | Высокий | Добавить refeed в те же функции, что уже проверяют цикл; покрыть тест-кейсы streak/weight.    |
| Toggle refeed сохраняет день без clientId                    | Низкая      | Высокий | Использовать `lsGet/lsSet` с `heys_dayv2_{date}`; избегать прямых `localStorage`.             |
| Новые попапы ломают жесты графика                            | Низкая      | Средний | Проверить zoom/pan/haptic после добавления бейджей/иконок; не перекрывать обработчики.        |
| Дублирующие советы или отсутствие блокировки `kcal_excess_*` | Средняя     | Средний | Отфильтровать советы при `isRefeedDay`; обновить кэш/приоритеты.                              |

---

## Rollback план

1. Сохранить бэкап затронутых файлов перед правками: `heys_steps_v1.js`,
   `heys_morning_checkin_v1.js`, `heys_day_v12.js`, `heys_ratio_zones_v1.js`,
   `heys_advice_v1.js`, `docs/DATA_MODEL_REFERENCE.md`.
2. При сбое — восстановить бэкап или `git checkout -- <file>` (по согласованию).
3. Перезапустить HMR / обновить страницу, убедиться что чек-ин и графики
   работают.

---

## Оценка готовности и время

- Готовность промпта: ~85% (добавлены Phase 0, риски, rollback; остаётся
  уточнить порядок шага refeed vs. текущее условие в коде).
- Оценка времени: ~2–3 часа (учитывая обновление 5+ файлов и UI проверку).
