# Рефакторинг: Вынос MealAddProduct за пределы DayTab

**Дата**: 2025-12-01  
**Время**: ~60-90 мин  
**Приоритет**: 🟡 Средний (архитектура/maintainability)

---

## ⚠️ QUICK FIX НЕ ПРИМЕНЁН

**Текущий статус кода:**
- `MealAddProduct` — обычная `function` внутри DayTab (строка 432)
- `gramsModal` state — lifted в DayTab (строка 1302) ✅
- useMemo **НЕ применён** — функция пересоздаётся на каждый ре-рендер

**Проблема остаётся:**
При добавлении приёма пищи модалка "мигает" из-за unmount/remount MealAddProduct.

---

## 📌 Варианты решения

### Вариант A: Quick Fix (useMemo) — 5 мин
```javascript
// apps/web/heys_day_v12.js
// Обернуть MealAddProduct в useMemo

// Было (строка 432):
function MealAddProduct({mi}){

// Стало:
const MealAddProduct = React.useMemo(() => function MealAddProduct({mi}){
  // ... код ...
}, [gramsModal, setGramsModal, day, setDay, products, pIndex, date, prodSig]);
```

**⚠️ Проблема**: gramsModal в deps → при изменении граммов useMemo пересоздаёт функцию → перерендер модалки.

### Вариант B: Полный рефакторинг — 60-90 мин
Вынести MealAddProduct за пределы DayTab как отдельный компонент с React.memo.

---

## Исходная проблема

`MealAddProduct` — компонент **~800 строк** (строки 432-1234), определённый **внутри** функции `DayTab`.

### Конкретный сценарий проблемы:
1. Пользователь добавляет новый приём пищи ("+ Приём")
2. **Быстро** открывает поиск продуктов в этом приёме
3. Модалка добавления **2-3 раза перерендеривается** (визуально "мигает")
4. State поиска может сброситься

### Почему это происходит:
1. `setDay({...day, meals: [...meals, newMeal]})` → DayTab ре-рендер
2. Возможно autosave делает ещё `setDay()` → ещё ре-рендер
3. При каждом ре-рендере DayTab создаётся **новая функция** `MealAddProduct`
4. React считает её **новым типом компонента** → размонтирует старый → монтирует новый
5. Все `useState` внутри MealAddProduct **сбрасываются**

**⚠️ Уточнение**: `gramsModal` уже вынесен в DayTab (строка 1301)! Это частичный фикс. Но пересоздание самой функции MealAddProduct всё равно происходит.

### Симптомы:
- [x] Модалка "мигает" 2-3 раза при быстром добавлении после "+ Приём"
- [ ] Потеря фокуса в поле поиска (проверить)
- [ ] Анимация калорий перезапускается (проверить)

---

## Решение

Вынести `MealAddProduct` **за пределы** `DayTab` как отдельный компонент с `React.memo`.

**Альтернатива (быстрее, менее инвазивно):**
```javascript
// Вместо вынося можно использовать useMemo:
const MealAddProductMemo = React.useMemo(() => {
  return function MealAddProduct({mi}) { ... }
}, []); // Пустые deps = функция создаётся 1 раз
```
⚠️ Но это хак. Правильное решение — вынести компонент.

---

## Ключевые файлы

| Файл | Строки | Описание |
|------|--------|----------|
| `apps/web/heys_day_v12.js` | 10,600 | Основной файл, содержит DayTab и MealAddProduct |
| `apps/web/heys_day_utils.js` | 906 | Утилиты (U) — haptic, lsGet, lsSet, etc. |
| `apps/web/heys_day_hooks.js` | — | React hooks |
| `apps/web/heys_dev_utils.js` | — | DEV.warn и др. |

---

## ⚠️ ФАЗА 0: Подготовка и блокеры

### 0.1 Измерить baseline (ОБЯЗАТЕЛЬНО!)
- [ ] Открыть React DevTools → Profiler
- [ ] Записать количество ре-рендеров MealAddProduct при:
  - Открытии dropdown поиска
  - Вводе текста в поиск
  - Добавлении продукта
- [ ] **Записать числа**: `____ ре-рендеров до рефакторинга`

### 0.2 Создать backup
- [ ] `cp apps/web/heys_day_v12.js apps/web/heys_day_v12.backup.js`
- [ ] Или git stash если есть uncommitted changes

### 0.3 Проверить что build работает
- [ ] `pnpm type-check && pnpm build` — PASS ✅

### 0.4 Составить полный список closure-зависимостей

**Из кода (проверено):**
```javascript
// Уже lifted в DayTab (OK):
- gramsModal, setGramsModal (строка 1301)

// Из closure DayTab — НУЖНО передать через props:
- day, setDay                    // данные дня
- products                       // массив продуктов (есть в props DayTab)
- date                           // текущая дата YYYY-MM-DD
- pIndex                         // useMemo buildProductIndex(products)
- prodSig                        // useMemo productsSignature(products)

// Глобальные — доступны через window.HEYS:
- haptic → HEYS.dayUtils.haptic (или U.haptic)
- uid → HEYS.dayUtils.uid
- lsGet/lsSet → HEYS.utils.lsGet / HEYS.utils.lsSet
- computePopularProducts → HEYS.dayUtils.computePopularProducts
- buildProductIndex → HEYS.dayUtils.buildProductIndex
- DEV.warn → window.DEV.warn

// Используются внутри (НЕ из closure):
- getProductFromItem, per100 — импортируются из U
- window.HEYS.SmartSearchWithTypos — поиск
- window.HEYS.models — порции
- window.HEYS.store — избранное
```

### 0.5 Решить: куда выносить?

**Вариант A: В тот же файл перед DayTab** (~800 строк кода)
- ✅ Проще, меньше изменений
- ❌ Файл и так 10,600 строк

**Вариант B: В отдельный файл `heys_meal_add_product.js`**
- ✅ Чистая архитектура, проще читать
- ✅ Можно будет подключить lazy loading
- ❌ Нужно добавить `<script>` в index.html
- ❌ Следить за порядком загрузки

**Рекомендация**: Вариант A сначала (MVP), потом можно вынести в отдельный файл.

---

## Задачи

### 1. Анализ зависимостей MealAddProduct от closure DayTab

- [ ] Открыть файл, найти строки 432-1234
- [ ] Сверить список переменных с Фазой 0.4
- [ ] Подтвердить что ничего не упущено

**Ожидаемый props interface:**
```javascript
{
  mi,                    // number — индекс приёма пищи
  gramsModal,           // object|null — { product, mealIndex, gramsValue }
  setGramsModal,        // function — lifted setter
  day,                  // object — данные дня
  setDay,               // function — setter дня
  products,             // array — список продуктов
  pIndex,               // object — { byId: Map, byName: Map }
  date                  // string — YYYY-MM-DD
}
```

### 2. Подготовить компонент к выносу

- [ ] Найти начало функции: `function MealAddProduct({mi}){` (строка 432)
- [ ] Найти конец функции (примерно строка 1234 — после `createPortal` для grams modal)
- [ ] Подсчитать строки: `1234 - 432 = ~802 строки`

### 3. Вынести MealAddProduct за пределы DayTab

**Место вставки:** ПЕРЕД `HEYS.DayTab=function DayTab(props){` (примерно строка 77)

```javascript
// === MealAddProduct Component (extracted for performance) ===
const MealAddProduct = React.memo(function MealAddProduct({ 
  mi, 
  gramsModal, 
  setGramsModal, 
  day, 
  setDay, 
  products, 
  pIndex,
  date 
}) {
  // 🔧 Получаем утилиты из глобального namespace
  const U = window.HEYS?.dayUtils || {};
  const haptic = U.haptic || (() => {});
  const uid = U.uid || ((p) => p + Math.random().toString(36).slice(2,8));
  const lsGet = (k, d) => (window.HEYS?.utils?.lsGet || ((k,d) => d))(k, d);
  const lsSet = (k, v) => (window.HEYS?.utils?.lsSet || (() => {}))(k, v);
  const computePopularProducts = U.computePopularProducts || (() => []);
  const productsSignature = U.productsSignature || (() => '');
  
  // ... остальной код MealAddProduct ...
});
```

### 4. Заменить closure-переменные на props/globals

| Было (closure) | Стало |
|----------------|-------|
| `gramsModal` | `props.gramsModal` (или деструктуризация) |
| `setGramsModal` | `props.setGramsModal` |
| `day` | `props.day` |
| `setDay` | `props.setDay` |
| `products` | `props.products` |
| `pIndex` | `props.pIndex` |
| `date` | `props.date` |
| `prodSig` | вычислить внутри: `useMemo(() => productsSignature(products), [products])` |
| `DEV.warn` | `window.DEV?.warn || console.warn` |

### 5. Обновить вызов MealAddProduct в DayTab

**Строка ~3511:**
```javascript
// Было:
React.createElement(MealAddProduct, {mi})

// Стало:
React.createElement(MealAddProduct, {
  mi,
  gramsModal,
  setGramsModal,
  day,
  setDay,
  products,
  pIndex,
  date
})
```

### 6. Добавить оптимизацию сравнения (опционально)

```javascript
const MealAddProduct = React.memo(function MealAddProduct(props) {
  // ...
}, (prevProps, nextProps) => {
  // Оптимизированное сравнение — перерендерить только если:
  // 1. Изменился mi
  // 2. Изменился gramsModal для ЭТОГО приёма
  // 3. Изменились items в ЭТОМ приёме
  // 4. Изменились products (редко)
  
  if (prevProps.mi !== nextProps.mi) return false;
  
  // gramsModal — сравниваем только если он для нашего mi
  const prevGrams = prevProps.gramsModal?.mealIndex === prevProps.mi ? prevProps.gramsModal : null;
  const nextGrams = nextProps.gramsModal?.mealIndex === nextProps.mi ? nextProps.gramsModal : null;
  if (prevGrams !== nextGrams) return false;
  
  // meals — сравниваем только items нашего приёма
  const prevItems = prevProps.day?.meals?.[prevProps.mi]?.items;
  const nextItems = nextProps.day?.meals?.[nextProps.mi]?.items;
  if (prevItems !== nextItems) return false;
  
  // products — сравниваем по reference (обычно не меняется)
  if (prevProps.products !== nextProps.products) return false;
  
  return true; // Не перерендеривать
});
```

### 7. Тестирование

**Функциональные тесты:**
- [ ] Открыть dropdown поиска → работает
- [ ] Ввести текст в поиск → фильтрует продукты
- [ ] Выбрать продукт → открывается модалка граммов (mobile)
- [ ] Изменить граммы слайдером → анимация kcal плавная
- [ ] Подтвердить → продукт добавлен
- [ ] Избранное (звёздочка) → работает
- [ ] Порции → кнопки работают

**Performance тесты:**
- [ ] React DevTools Profiler → записать ре-рендеры MealAddProduct
- [ ] **Результат**: `____ ре-рендеров после рефакторинга` (должно быть 1-3, не 70+)

### 8. Cleanup и финализация

- [ ] Удалить неиспользуемые переменные из DayTab
- [ ] Проверить console на ошибки
- [ ] `pnpm type-check && pnpm lint && pnpm build` — PASS ✅
- [ ] Удалить backup файл

---

## Риски и митигация

| Риск | Вероятность | Влияние | Митигация |
|------|-------------|---------|-----------|
| Много closure-зависимостей | 🟡 Средняя | 🔴 Высокое | Фаза 0.4 — полный список заранее |
| CSS классы не найдены | 🟢 Низкая | 🟡 Среднее | CSS в том же bundle, проверить визуально |
| window.HEYS не готов при вызове | 🟡 Средняя | 🔴 Высокое | Защитные проверки `window.HEYS?.utils?.lsGet` |
| Большой diff = сложно review | 🔴 Высокая | 🟡 Среднее | Разбить на 2 коммита: 1) подготовка, 2) вынос |
| Сломается после рефакторинга | 🟡 Средняя | 🔴 Высокое | Backup + тестирование по чеклисту |
| Overkill: проблема не в этом | 🟡 Средняя | 🟡 Среднее | Фаза 0.1 — измерить baseline! |

### ⚠️ Признаки что это OVERKILL:

- Если в Profiler видим 1-5 ре-рендеров, а не 70+ → проблема в другом
- Если gramsModal уже работает стабильно → симптом решён
- Если единственная проблема — потеря фокуса → можно решить проще (useRef + focus restore)

**Правило**: Если Фаза 0.1 показывает < 10 ре-рендеров — **остановиться** и пересмотреть задачу.

---

## Критерии готовности

- [ ] `MealAddProduct` определён ВНЕ `DayTab` (перед ним в том же файле)
- [ ] Обёрнут в `React.memo`
- [ ] Все зависимости передаются через props или window.HEYS
- [ ] Количество ре-рендеров MealAddProduct = 1-3 при открытии модалки (не 70+)
- [ ] Все функциональные тесты из п.7 — PASS
- [ ] `pnpm type-check && pnpm lint && pnpm build` — PASS

---

## 🚀 WOW-улучшения (опционально, после MVP)

### 1. Отдельный файл + Lazy Loading
```javascript
// heys_meal_add_product.js (новый файл)
// + добавить <script defer> в index.html

// В DayTab:
const MealAddProduct = window.HEYS?.MealAddProduct || (() => null);
```
**Эффект**: Быстрее первый рендер DayTab, компонент загрузится параллельно.

### 2. Виртуализация списка продуктов
```javascript
// Если products > 100, использовать react-window
import { FixedSizeList } from 'react-window';
// Рендерим только видимые 10-15 элементов
```
**Эффект**: При 1000+ продуктов — instant scroll без лагов.

### 3. Skeleton loading для поиска
```javascript
// Пока загружается SmartSearchWithTypos:
{isSearching && <div className="skeleton-list">...</div>}
```
**Эффект**: Приятный UX, нет "мигания" при поиске.

### 4. Swipe-to-add на продуктах
```javascript
// Свайп вправо = добавить 100г
// Свайп влево = открыть модалку граммов
```
**Эффект**: Быстрое добавление одной рукой на mobile.

### 5. Voice input для поиска (WOW!)
```javascript
// Web Speech API:
const recognition = new webkitSpeechRecognition();
recognition.lang = 'ru-RU';
recognition.onresult = (e) => setSearch(e.results[0][0].transcript);
```
**Эффект**: "Привет HEYS, добавь овсянку" — и она добавлена! 🎤

---

## Связанные файлы

- `apps/web/heys_day_v12.js` — основной файл (10,600 строк)
- `apps/web/heys_day_utils.js` — утилиты (U)
- `apps/web/heys_day_hooks.js` — React hooks
- `apps/web/heys_dev_utils.js` — DEV.warn

---

## Альтернативы (если рефакторинг слишком сложный)

### A. useMemo для компонента (быстрый хак)
```javascript
// Внутри DayTab:
const MealAddProductMemo = React.useMemo(() => {
  return function MealAddProduct({mi}) { ... };
}, []); // Создаётся 1 раз
```
⚠️ Работает, но это антипаттерн. Код остаётся внутри DayTab.

### B. useRef + stable identity
```javascript
const MealAddProductRef = React.useRef(null);
if (!MealAddProductRef.current) {
  MealAddProductRef.current = function MealAddProduct({mi}) { ... };
}
const MealAddProduct = MealAddProductRef.current;
```
⚠️ Ещё больший хак. НЕ рекомендуется.

### C. Zustand/Jotai для shared state
```javascript
// Глобальный store вместо lifted state
const useGramsModal = create((set) => ({
  gramsModal: null,
  setGramsModal: (val) => set({ gramsModal: val })
}));
```
⚠️ Overkill для одной модалки. Но хорошо масштабируется.

**Рекомендация**: Прямой рефакторинг (вынос за пределы DayTab) — это правильное и чистое решение.

---

## Changelog

| Версия | Дата | Изменения |
|--------|------|-----------|
| 1.1 | 2025-12-01 | Глубокий аудит: добавлена Фаза 0 (блокеры), уточнён список зависимостей, WOW-улучшения, метрика overkill |
| 1.0 | 2025-12-01 | Первоначальная версия промпта |
