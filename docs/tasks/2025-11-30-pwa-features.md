# PWA Features — Расширение возможностей

**Дата**: 2025-11-30  
**Приоритет**: Высокий  
**Время**: ~60 мин  
**Статус**: 🔲 В очереди

---

## ⚠️ Открытые вопросы (решить перед началом)

### Критичные
1. **Push Notifications** — оставляем или убираем? `setTimeout` не работает при закрытом приложении. Полноценные push требуют backend.
2. **Background Sync** — нужен ли? Уже есть `heys_storage_supabase_v1.js` с online/offline синхронизацией.
3. **Какой streak показывать на badge?**
   - Текущий streak дней в норме (ratio 0.75-1.15)?
   - Или количество дней подряд с записями?

### Уточнения
4. **Shortcuts: `add-meal`** — какой приём пищи добавлять? Последний незаполненный? Или показывать выбор?
5. **Wake Lock** — только при активной тренировке (таймер идёт)? Или при любом открытии секции тренировок?

---

## 🔴 Выявленные проблемы

### Фаза 1 — Shortcuts
- ❌ Нужны иконки 96x96 PNG для shortcuts — их нет в проекте!
- ❌ После действия (`?action=add-meal`) нужно очистить URL через `history.replaceState`
- ❌ `add-water` — нужна проверка существует ли день (создать если нет)

### Фаза 2 — Badge API
- ⚠️ Streak считается в `heys_day_v12.js` — нужно найти где именно и переиспользовать
- ⚠️ Когда обновлять badge? При каждом сохранении дня? При загрузке?

### Фаза 3 — Persistent Storage
- ⚠️ Запрос может быть отклонён — показывать предупреждение пользователю?

### Фаза 4 — Push Notifications
- ❌ `setTimeout` умрёт при закрытии вкладки — напоминания не сработают
- ❌ Нужен Service Worker + Notification Scheduler API или backend
- 🤔 **Решение**: Оставить только запрос разрешения + базовую инфраструктуру без расписания?

### Фаза 5 — Background Sync
- ❌ Нужна очередь `heys_pending_sync` — как её заполнять? Перехватывать ВСЕ записи в localStorage?
- ⚠️ Возможно дублирует функционал Supabase sync

### Фаза 6 — Wake Lock
- ❌ Какой селектор/момент для "открытия секции тренировки"?
- ❌ Wake Lock отпускается при переходе в другую вкладку — нужен `visibilitychange` listener
- ❌ Нужен таймер 30 мин для автоматического освобождения

### Фаза 7 — Periodic Background Sync
- ⚠️ Ограниченная поддержка браузерами (Chrome только если сайт часто используется)
- 🤔 **Решение**: Убрать как малополезную?

---

## 💡 Дополнительные WOW-фичи (опционально)

| Фича | Описание | Сложность | Эффект |
|------|----------|-----------|--------|
| **Install Prompt** | Красивый баннер "Установить приложение" с анимацией | S | 🔥🔥🔥 |
| **Splash Screen** | Кастомный splash screen при запуске PWA | XS (CSS) | 🔥🔥 |
| **Vibration Patterns** | Разные паттерны вибрации для событий (streak, level up) | XS | 🔥 |
| **Display Override** | `display_override: ["window-controls-overlay"]` — современный PWA look | S | 🔥🔥 |

---

## 📋 Описание

Добавить недостающие PWA возможности для улучшения UX:
- Shortcuts (быстрые действия с иконки)
- Badge API (бейдж streak на иконке)
- Persistent Storage (защита данных от очистки)
- Push Notifications (напоминания)
- Background Sync (офлайн-синхронизация)
- Periodic Background Sync (фоновое обновление)
- Screen Wake Lock (экран не гаснет при тренировке)

---

## 📁 Ключевые файлы

| Файл | Назначение |
|------|------------|
| `apps/web/public/manifest.json` | PWA манифест — shortcuts |
| `apps/web/public/sw.js` | Service Worker — sync, notifications |
| `apps/web/index.html` | Регистрация SW |
| `apps/web/heys_app_v12.js` | Badge API, Wake Lock, Persistent Storage |
| `apps/web/heys_day_v12.js` | Wake Lock при тренировке, streak расчёт |

---

## ✅ Задачи

### Фаза 0: Подготовка (15 мин)

- [ ] **0.1** Найти где считается streak в `heys_day_v12.js` — переиспользовать для Badge API
- [ ] **0.2** Создать 4 иконки 96x96 для shortcuts (или использовать emoji fallback)
- [ ] **0.3** Проверить регистрацию Service Worker в `index.html`
- [ ] **0.4** Решить открытые вопросы выше

---

### Фаза 1: Manifest Shortcuts (10 мин)

- [ ] **1.1** Добавить `shortcuts` в `manifest.json`:
  ```
  - "➕ Добавить еду" → /?action=add-meal
  - "💧 Добавить воду" → /?action=add-water  
  - "🏃 Тренировка" → /?action=training
  - "📊 Статистика" → /?tab=reports
  ```

- [ ] **1.2** Обработать `?action=` параметры в `heys_app_v12.js`:
  - При загрузке проверять URL параметры
  - `add-meal` → открыть модалку добавления еды (последний незаполненный приём?)
  - `add-water` → добавить 250мл воды (создать день если нет)
  - `training` → открыть вкладку День, секция тренировки
  - **После действия**: `history.replaceState({}, '', '/')` — очистить URL

---

### Фаза 2: Badge API (15 мин)

- [ ] **2.1** Создать функцию `HEYS.badge.update(count)` в `heys_app_v12.js`

- [ ] **2.2** Показывать streak на бейдже:
  - При загрузке: `navigator.setAppBadge(currentStreak)`
  - При изменении streak: обновлять бейдж
  - Если streak = 0: `navigator.clearAppBadge()`

- [ ] **2.3** Fallback для браузеров без поддержки:
  ```javascript
  if ('setAppBadge' in navigator) { ... }
  ```

- [ ] **2.4** Интеграция с существующим расчётом streak (найти в Фазе 0)

---

### Фаза 3: Persistent Storage (5 мин)

- [ ] **3.1** При первом запуске запросить persistent storage:
  ```javascript
  if (navigator.storage && navigator.storage.persist) {
    const granted = await navigator.storage.persist();
    console.log('[HEYS] Persistent storage:', granted ? 'granted' : 'denied');
  }
  ```

- [ ] **3.2** Добавить в инициализацию приложения (после выбора клиента)

- [ ] **3.3** (Опционально) Показать предупреждение если отклонено

---

### Фаза 4: Push Notifications — Подготовка (20 мин)

> ⚠️ **Ограничение**: Локальные напоминания работают только при открытом приложении

- [ ] **4.1** Запрос разрешения на уведомления:
  ```javascript
  const permission = await Notification.requestPermission();
  ```

- [ ] **4.2** Создать функцию локальных уведомлений `HEYS.notify.show(title, options)`:
  - Использовать `ServiceWorkerRegistration.showNotification()`
  
- [ ] **4.3** Добавить настройку в профиле: "Напоминания о еде" (вкл/выкл)

- [ ] **4.4** ~~Расписание локальных напоминаний~~ — **УБРАНО** (не работает при закрытом приложении)

---

### Фаза 5: Background Sync (15 мин)

> ⚠️ **Вопрос**: Нужен ли, если есть Supabase sync?

- [ ] **5.1** Зарегистрировать sync в SW:
  ```javascript
  // При офлайн-сохранении
  navigator.serviceWorker.ready.then(reg => {
    reg.sync.register('heys-sync');
  });
  ```

- [ ] **5.2** Обработать `sync` event в `sw.js`:
  ```javascript
  self.addEventListener('sync', event => {
    if (event.tag === 'heys-sync') {
      event.waitUntil(syncPendingData());
    }
  });
  ```

- [ ] **5.3** Очередь офлайн-изменений в localStorage: `heys_pending_sync`

---

### Фаза 6: Screen Wake Lock (10 мин)

- [ ] **6.1** При открытии секции тренировки — запрашивать Wake Lock:
  ```javascript
  let wakeLock = null;
  if ('wakeLock' in navigator) {
    wakeLock = await navigator.wakeLock.request('screen');
  }
  ```

- [ ] **6.2** Добавить `visibilitychange` listener для переполучения lock при возврате

- [ ] **6.3** Освобождать при:
  - Закрытии секции тренировки
  - Через 30 мин (таймер)
  - При переходе на другую вкладку приложения

- [ ] **6.4** Визуальный индикатор "🔒 Экран не погаснет" в секции тренировки

---

### Фаза 7: Periodic Background Sync — ~~Опционально~~ УБРАНО

> ❌ Ограниченная поддержка браузерами, малополезно для HEYS

---

### Фаза 8: Install Prompt (WOW-эффект) (15 мин)

- [ ] **8.1** Перехватить `beforeinstallprompt` event
- [ ] **8.2** Показать красивый баннер "Установить HEYS" с анимацией
- [ ] **8.3** Сохранить флаг "не показывать снова" в localStorage
- [ ] **8.4** Показывать только после 3 дней использования или 5 записей

---

## 🧪 Тестирование

- [ ] Chrome DevTools → Application → Manifest → проверить shortcuts
- [ ] Установить PWA на Android — проверить shortcuts, badge
- [ ] Проверить badge обновляется при изменении streak
- [ ] Открыть тренировку → экран не должен гаснуть
- [ ] Свернуть/развернуть → Wake Lock должен восстановиться
- [ ] Проверить Install Prompt появляется

---

## 📝 Примечания

- Push Notifications полноценные (с сервера) требуют backend — делаем только инфраструктуру
- ~~Periodic Sync~~ — убрано (ограниченная поддержка)
- Badge API работает только в установленном PWA
- Wake Lock автоматически отпускается при сворачивании приложения — нужен `visibilitychange`

---

## 🔗 Документация

- [Shortcuts](https://developer.mozilla.org/en-US/docs/Web/Manifest/shortcuts)
- [Badging API](https://developer.mozilla.org/en-US/docs/Web/API/Badging_API)
- [Persistent Storage](https://developer.mozilla.org/en-US/docs/Web/API/StorageManager/persist)
- [Background Sync](https://developer.mozilla.org/en-US/docs/Web/API/Background_Synchronization_API)
- [Screen Wake Lock](https://developer.mozilla.org/en-US/docs/Web/API/Screen_Wake_Lock_API)
- [beforeinstallprompt](https://developer.mozilla.org/en-US/docs/Web/API/Window/beforeinstallprompt_event)
