# Аудит попапов и универсальный компонент

## 📌 TL;DR

- **Цель:** Провести полный аудит всех попапов и спроектировать единый
  универсальный компонент, который покрывает разные типы (info, form, context,
  wizard, confirm) без потери их визуальных отличий и функциональности.
- **Что делаем:**
  1. Собрать список всех попапов и разбить на категории.
  2. Выявить общие паттерны (backdrop, swipe, кнопки, фокус, позиционирование).
  3. Спроектировать API универсального попапа с пресетами под категории.
  4. Подготовить план миграции (какие попапы переносить первыми, критерии
     готовности).
- **Зачем:** уменьшить дублирование кода, повысить консистентность UX, упростить
  поддержку и устранение багов, сократить bundle.
- **Время:** ~2–3 часа (аудит + дизайн API + план миграции; миграция отдельно).

## 🎯 Цели и рамки

- Полный перечень попапов в кодовой базе с привязкой к файлам и категориям.
- Единая типология (info, form, context, wizard, confirm, panel/expanded) с
  четкими критериями.
- Описание общих требований: доступность (aria), закрытие
  (backdrop/escape/swipe), блокировка скролла, позиционирование, анимации.
- Проект API универсального компонента (пропсы, пресеты, расширяемость под
  mobile/desktop).
- План миграции: порядок, риски, quick wins.

## 🗂 Категории попапов (канон для аудита)

- **Info:** только отображение, без действий (sparkline, tdee, metric, debt
  science, TEF, formula popups).
- **Form:** ввод данных/валидация (time picker, training picker, zone picker,
  sleep/day score, product modal, merge modal).
- **Context (anchor):** маленькие всплывающие карточки у элемента (macroBadge,
  weekNorm/Deficit, balanceDay, mealQuality).
- **Wizard/Step:** многошаговые процессы (утренний чек-ин StepModal, charts
  modal с табами).
- **Confirm:** подтверждения опасных действий (ConfirmModal, специфичные
  confirm-попапы).
- **Panel/Expanded:** расширенные панели/what-if (predictive insights и
  подобные, если есть).

## 🔍 Что собрать в результате аудита

Для каждого попапа:

- Файл и функция/место вызова.
- Категория (из списка выше).
- Особенности: позиционирование (fixed/anchor), backdrop (да/нет), закрытие
  (кнопка/клик вне/escape/swipe), есть ли формы, нужны ли trapFocus/autoFocus,
  анимации, размеры, mobile-специфика.
- Повторяемые паттерны: swipe, кнопки закрытия, backdrop, блокировка скролла,
  фокус-менеджмент.
- Известные баги/UX-долг (если есть).

## 🛠 Требования к универсальному компоненту

- Пресеты под категории (info/form/context/wizard/confirm/panel) с разумными
  дефолтами.
- Опции: backdrop, closeButton, clickOutside, swipeToDismiss, escapeKey,
  trapFocus, preventScroll, animation (fade/slide/scale), size (sm/md/lg/full),
  position (center/top/bottom/anchor), zIndex.
- Поддержка anchor-позиционирования с умным размещением (above/below, offset,
  viewport clamping).
- Хуки/слоты: header/title/subtitle, body, footer/actions, custom close button,
  optional swipe hint.
- A11y: `role="dialog"`, `aria-modal`, `aria-labelledby`, `aria-describedby`,
  фокус-ловушка, возврат фокуса.

## 🚦 План работ (аудит → дизайн → план миграции)

1. **Аудит**
   - Поиск и перечень всех попапов по категориям (см. чеклист выше).
   - Фиксация паттернов закрытия и позиционирования.
2. **Дизайн API**
   - Сформировать пропсы универсального компонента и пресеты.
   - Описать пример использования для каждой категории.
3. **План миграции**
   - Определить порядок переноса (начать с info и confirm как самые простые).
   - Критерии готовности: визуальное соответствие, a11y, закрытие, блокировка
     скролла.
   - Риски и откаты: список попапов, которые требуют спец-логики (anchor,
     wizard).

## ✅ Критерии приёмки аудита

- Есть таблица всех попапов: имя, файл, категория, ключевые особенности.
- Есть финализированная схема пропсов универсального попапа и пресетов.
- Есть план миграции с приоритетами и рисками.

## 🧪 Что не делаем в этом промпте

- Не переписываем конкретные попапы (только дизайн и план).
- Не вносим визуальные изменения в существующие попапы.

## 📎 Полезные опоры

- Текущие хелперы: `PopupWithBackdrop`, `useSwipeToDismiss`, `PopupCloseButton`
  в `apps/web/heys_day_v12.js` (можно реиспользовать/расширить).
- Стайлгайд: Tailwind + BEM (`styles/heys-components.css`), без инлайновых
  стилей кроме динамики.
