# Решение неактивных паттернов HEYS Insights

## Диагностика (12.02.2026)

**Статус: 26/41 паттернов активны** ✅  
**Проблема: 15 паттернов неактивны** ❌

### Данные клиента ccfe6ea3-54d9-4c83-902b-f10e6e8e6d9a

```
✅ Загружено: 68 дней
✅ Еда: 58/68 дней | 3.4 приёма/день | 11.4 продукта/день | 217 уникальных
✅ Стресс: 230 записей
✅ Настроение: 230 записей
✅ Сон: 63 дня

❌ Вес: 0 записей
❌ Замеры тела: 3 (нужно 5)
❌ Цикл: 0 дней
❌ Микронутриенты: 0 продуктов с iron/vitamin_c/calcium
```

---

## Root Causes

### 1. **Micronutrient Patterns** (5-7 паттернов) — КРИТИЧНО

**Проблема:** В `shared_products` отсутствуют поля:

- `iron` (железо)
- `vitamin_c` (витамин C)
- `calcium` (кальций)
- `vitamin_d`, `vitamin_b12`, `magnesium`, `zinc`, etc.

**Затронутые паттерны:**

- ❌ `analyzeVitaminDefense` — требует products с vitamin_c
- ❌ `analyzeBoneHealth` — требует calcium, vitamin_d
- ❌ `analyzeBComplexAnemia` — требует iron, vitamin_b12
- ❌ `analyzeIronStatus` — требует iron
- ❌ `analyzeMagnesiumRelaxation` — требует magnesium
- ❌ `analyzeZincImmunity` — требует zinc

**Код проблемы:**

```javascript
// insights/patterns/micronutrients.js (строки 100-250)
const prod = pIndex?.byId?.get?.(item.productId);
if (prod?.iron) ironCount++; // Всегда 0!
if (prod?.vitamin_c) vitaminCCount++; // Всегда 0!
if (prod?.calcium) calciumCount++; // Всегда 0!

// Результат диагностики:
// 🥗 Микро: 0 Fe | 0 VitC | 0 Ca
```

### 2. **Body Composition Patterns** (2-3 паттерна)

**Проблема:** Клиент не трекает вес (0 записей из 68 дней)

**Затронутые паттерны:**

- ❌ `analyzeBodyComposition` — требует >=7 дней с weight
- ❌ `analyzeWeightTrend` — требует weight tracking
- ❌ `analyzeFatLossProgress` — требует weight + measurements

**Решение:** Добавить UI для ввода веса (если его нет) или объяснить клиенту
важность трекинга

### 3. **Hypertrophy Pattern** (1 паттерн)

**Проблема:** Только 3 замера мышц (нужно 5)

**Затронутые паттерны:**

- ❌ `analyzeHypertrophy` — требует >=5 замеров biceps/thigh за >=14 дней

**Решение:** Добавить 2 замера или показать прогресс: "💪 Hypertrophy: 3/5
замеров ⭐⭐⭐⚪⚪"

### 4. **Cycle Patterns** (2-3 паттерна)

**Проблема:** Нет данных о менструальном цикле (0 дней)

**Затронутые паттерны:**

- ❌ `analyzeCyclePatterns` — требует >=14 дней с cycleDay/cyclePhase
- ❌ `analyzeCycleHunger` — требует cyclePhase + meals
- ❌ `analyzeCycleMood` — требует cyclePhase + mood

**Решение:** Клиент мужчина → эти паттерны не применимы (это нормально)

---

## Решения

### 🔥 HIGH Priority: Добавить микронутриенты

**Шаг 1: Применить миграцию**

```bash
cd /Users/poplavskijanton/HEYS-v2/yandex-cloud-functions
psql -h rc1b-2gfin10qjx3ldu26.mdb.yandexcloud.net \
     -p 6432 -U heysuser -d heysdb \
     -f migrations/2026-02-12_add_micronutrients.sql
```

**Шаг 2: Заполнить данные**

```bash
cd scripts
export PG_PASSWORD='your_password'
node populate_micronutrients.js
```

**Результат:** ~50-100 топовых продуктов получат данные о витаминах/минералах →
разблокируется 5-7 паттернов

---

### ⚠️ MEDIUM Priority: UI для трекинга веса

**Проблема:** Нет поля для ввода веса в дневнике

**Решение:**

1. Добавить input для веса в `apps/web/public/index.html` (секция дневника)
2. Сохранять weight в `heys_dayv2_{date}` через
   `HEYS.day.set(date, { weight: value })`
3. Синхронизировать через `HEYS.cloud.syncDay(date)`

**Результат:** Разблокируется 2-3 body composition паттерна

---

### 💡 LOW Priority: "Unlock Pattern" карточки

**Концепт:**

```html
<div class="pattern-locked">
  <h3>💪 Hypertrophy Tracker</h3>
  <div class="progress-bar">
    <div class="progress" style="width: 60%">3/5 замеров</div>
  </div>
  <p>Добавь ещё 2 замера бицепса/бедра для активации</p>
  <button onclick="HEYS.measurements.add()">+ Замер</button>
</div>
```

**Размещение:** В секции Insights, после активных паттернов

---

## Чеклист внедрения

- [ ] Применить миграцию `2026-02-12_add_micronutrients.sql`
- [ ] Запустить `populate_micronutrients.js` (заполнит ~100 продуктов)
- [ ] Проверить: `SELECT COUNT(*) FROM shared_products WHERE iron IS NOT NULL;`
      → ожидаем >=50
- [ ] Добавить UI для ввода веса (если отсутствует)
- [ ] Создать "Unlock Pattern" карточки (опционально)
- [ ] Перезапустить теплом инсайтов:
      `HEYS.InsightsPI.clearCache(); HEYS.InsightsPI.analyzeAll();`
- [ ] Проверить console: должно быть "30+/41 possible" вместо "26/41"

---

## Ожидаемый результат

**До:**

- 26/41 паттернов активны
- ❌ 0 продуктов с микронутриентами
- ❌ 0 дней с весом
- ❌ 3 замера мышц

**После:**

- **34-37/41 паттернов активны** (+8-11)
- ✅ 100+ продуктов с микронутриентами
- ✅ Вес трекается (если клиент начнёт вносить)
- ⚠️ Cycle/Hypertrophy остаются неактивны (требуют специфических действий)

**Время внедрения:** ~30 минут

---

## Дополнительно: Security Warning ⚠️

В ходе диагностики пароль от БД (`heys007670`) был выведен в plaintext дважды.

**КРИТИЧНО:**

1. Сменить пароль PostgreSQL в Yandex Cloud Console
2. Обновить `.env` во всех cloud functions
3. Перезапустить функции: `cd yandex-cloud-functions && ./deploy-all.sh`
4. Никогда не использовать пароли в plaintext — только через env или секреты

**Новый пароль генерировать:**

```bash
openssl rand -base64 32
```
