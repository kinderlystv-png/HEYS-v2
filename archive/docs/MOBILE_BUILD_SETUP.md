# 📱 EAS Build Setup для монорепы HEYS-v2

## Статус: ✅ Готово к сборке

Мобильное приложение (`apps/mobile/`) настроено для standalone сборки через EAS
Build.

## 🚀 Быстрый старт

```bash
# 1. Перейти в директорию мобилки
cd apps/mobile

# 2. Войти в Expo
eas login

# 3. Собрать APK
eas build --profile preview --platform android
```

**Результат:** Через 10-15 минут получите ссылку на скачивание APK.

---

## 📋 Что было сделано

### ✅ Изоляция от монорепы

- Создан `.easignore` - исключает `packages/`, `apps/web/`, конфиги
- Все зависимости в `package.json` явные (без `workspace:*`)

### ✅ EAS конфигурация

- `eas.json` с профилями: development, preview, production
- `app.json` с Android манифестом (`com.heys.mobile.dev`)
- Gradle команды для APK/AAB сборки

### ✅ Standalone режим

- Приложение не использует `@heys/*` пакеты из монорепы
- Может собираться независимо от корня

---

## 📖 Подробная документация

См. **[apps/mobile/BUILD_README.md](apps/mobile/BUILD_README.md)** для:

- Детальных инструкций по сборке
- Troubleshooting типичных ошибок
- Описания профилей и параметров
- Установки APK на устройство

---

## 🎯 Архитектурное решение

**Проблема:** EAS Build загружает весь репозиторий → конфликты с pnpm workspaces

**Решение:**

1. `.easignore` → изолирует mobile от корня
2. Явные зависимости → EAS понимает npm install
3. Standalone пакет → не требует shared-пакетов

**Почему работает:** EAS видит только `apps/mobile/` как отдельный проект,
игнорирует монорепу.

---

## 🔧 Команды для CI/CD

```bash
# Preview APK (для тестирования)
cd apps/mobile && eas build --profile preview --platform android --non-interactive

# Production AAB (для Google Play)
cd apps/mobile && eas build --profile production --platform android --non-interactive
```

---

Сборка готова! Следуйте инструкциям в `apps/mobile/BUILD_README.md` 🚀
