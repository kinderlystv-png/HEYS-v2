# Mobile UX Fix: Day View — Компактный дизайн

**Date**: November 25, 2025  
**Status**: ✅ ФАЗА 2 ВЫПОЛНЕНА → АУДИТ + РЕКОМЕНДАЦИИ **Approach**: Оценка
результата + современные улучшения

---

## ✅ ФАЗА 1: ЗАВЕРШЕНО

- [x] Нет горизонтального скролла
- [x] Все карточки в одну колонку
- [x] Элементы влезают в экран

---

## ✅ ФАЗА 2: ЗАВЕРШЕНО

- [x] Padding карточек уменьшен (8px)
- [x] Gap уменьшен (6px)
- [x] Input'ы компактнее (36px)
- [x] Зоны тренировок компактнее
- [x] Фиолетовая таблица компактнее

---

## 🔍 АУДИТ ФАЗЫ 2

### ✅ Что сделано правильно:

1. **Padding/gap уменьшены** — больше контента на экране
2. **Input'ы 36px** — достаточно для touch, но компактнее
3. **Сохранены safe-area-inset** — для iPhone с "чёлкой"
4. **touch-action: manipulation** — уже есть на кнопках

### 🟡 Что можно улучшить (не критично):

| Проблема                             | Статус                    | Рекомендация                             |
| ------------------------------------ | ------------------------- | ---------------------------------------- |
| `font-size: 14px` на input           | Может вызвать zoom на iOS | Оставить 16px или добавить meta viewport |
| Нет `scroll-behavior: smooth`        | Не критично               | Добавить для плавности                   |
| Нет `overscroll-behavior` на `.page` | Есть на body              | ОК                                       |

### ❌ Потенциальный баг:

**iOS Safari auto-zoom**: Если `font-size < 16px` на `<input>`, Safari зумит
страницу при фокусе.

**Решение**: Уже есть в index.html:

```html
<meta
  name="viewport"
  content="width=device-width, initial-scale=1, maximum-scale=1"
/>
```

Если нет `maximum-scale=1` — добавить.

---

## 🚀 ФАЗА 3: СОВРЕМЕННЫЕ УЛУЧШЕНИЯ (опционально)

### Что УЖЕ есть в проекте (не трогаем):

| Фича                          | Статус  | Где                      |
| ----------------------------- | ------- | ------------------------ |
| `safe-area-inset`             | ✅ Есть | `.hdr`, `.tabs`, `.wrap` |
| `touch-action: manipulation`  | ✅ Есть | `.btn`, `input`          |
| `-webkit-tap-highlight-color` | ✅ Есть | `html, body`             |
| `overscroll-behavior-y: none` | ✅ Есть | `html, body`             |
| `user-select: none`           | ✅ Есть | `.btn`, `.tab`           |

### Рекомендации (НЕ оверкилл, полезно):

#### 3.1: Pull-to-refresh отключение (если PWA)

```css
/* Уже есть: overscroll-behavior-y: none на body */
```

✅ **Не нужно** — уже реализовано

#### 3.2: Smooth scroll для навигации

```css
html {
  scroll-behavior: smooth;
}

@media (prefers-reduced-motion: reduce) {
  html {
    scroll-behavior: auto;
  }
}
```

**Польза**: Плавный скролл к якорям  
**Риск**: Минимальный  
**Вердикт**: 👍 Добавить

#### 3.3: Better focus states для accessibility

```css
/* Видимый focus для keyboard navigation */
input:focus-visible,
select:focus-visible,
textarea:focus-visible,
.btn:focus-visible {
  outline: 2px solid var(--acc);
  outline-offset: 2px;
}
```

**Польза**: Accessibility, современный стандарт  
**Риск**: Нулевой  
**Вердикт**: 👍 Добавить

#### 3.4: Улучшенный selection color

```css
::selection {
  background: var(--acc);
  color: white;
}
```

**Польза**: Брендированный selection  
**Риск**: Нулевой  
**Вердикт**: 👍 Добавить

#### 3.5: Prevent text selection на интерактивных элементах

```css
/* Уже есть на .btn, добавить на карточки если нужно */
.card.clickable {
  user-select: none;
}
```

✅ **Частично есть** — можно расширить

#### 3.6: Haptic-like feedback (visual)

```css
/* Уже есть: */
.btn:active {
  transform: scale(0.98);
  opacity: 0.8;
}
```

✅ **Уже реализовано**

---

## 📋 ИТОГОВЫЙ ПЛАН ФАЗЫ 3

### Добавить (5 минут, 0 риска):

```css
/* === MODERN POLISH === */

html {
  scroll-behavior: smooth;
}

@media (prefers-reduced-motion: reduce) {
  html {
    scroll-behavior: auto;
  }
}

/* Focus states для accessibility */
input:focus-visible,
select:focus-visible,
textarea:focus-visible,
.btn:focus-visible {
  outline: 2px solid var(--acc);
  outline-offset: 2px;
}

/* Брендированный selection */
::selection {
  background: var(--acc);
  color: white;
}
```

### НЕ добавлять (оверкилл):

- ❌ CSS animations/transitions везде — не nutrition app стиль
- ❌ Skeleton loaders — данные локальные, быстро
- ❌ Dark mode — отдельная задача, не в рамках mobile fix
- ❌ Кастомные scrollbars на mobile — не нужны
- ❌ Gesture hints — усложнение

---

## 📊 Чеклист проверки

### После всех фаз:

- [x] Нет горизонтального скролла
- [x] Компактный layout
- [ ] Smooth scroll работает
- [ ] Focus-visible видимый
- [ ] iOS Safari не зумит при фокусе

### Desktop:

- [ ] Layout не сломан
- [ ] Focus states не мешают

---

## 🎯 Готов к Фазе 3?

Если да — добавляю 3 современных улучшения:

1. `scroll-behavior: smooth`
2. `:focus-visible` outline
3. `::selection` branding

**Время**: ~2 минуты  
**Риск**: Нулевой  
**Польза**: Современный polish + accessibility
