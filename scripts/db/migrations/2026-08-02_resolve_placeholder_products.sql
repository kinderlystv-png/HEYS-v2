-- 2026-08-02 — две последние карточки без внятного названия.
--
-- KPD — по словам владельца это порошковая добавка клетчатки (смесь
-- компонентов). Название раскрыто; категории «добавки» в списке нет, поэтому
-- остаётся «прочее» — это честный случай для такой категории.
--
-- Two Peaks — разовый продукт, попавший в общий каталог до появления режима
-- «Разово в этот приём» (apps/web/heys_add_product_step_v1.js: createMode
-- 'oneTime', для него публикация в общую базу принудительно отключена).
-- Клиентских ссылок нет ни одной, опознать продукт невозможно — удаляется.
BEGIN;

UPDATE public.shared_products
SET name = 'Клетчатка порошковая (смесь пищевых волокон)',
    name_norm = 'клетчатка порошковая (смесь пищевых волокон)'
WHERE name = 'KPD';

DELETE FROM public.shared_products WHERE name = 'Two Peaks';

COMMIT;
