# strength-builder · «Назначено против сделано» · needsCode by owner

Источник: `scripts/.sb-assigned-vs-done-verdict-handoff.json` (`needsCode`, 35
строк).  
Сверка: `PlanVsDoneScreen`
`apps/web/strength/heys_strength_superset_ui_v1.js:2114–2157`, mount
`apps/web/strength/heys_strength_builder_ui_v1.js:1466–1471`, CSS
`apps/web/styles/modules/750-strength-builder.css:6726–6760` (+ эталон
`.sb-cycle-top` `:6885–6898`).  
Open question «вид · отчёт цикла vs plan-vs-done» — вне scope (не
классифицировали).

## 1. Summary

| bucket          | count | keys (compact)                 |
| --------------- | ----: | ------------------------------ |
| **builder_ui**  |     0 | —                              |
| **750**         |     3 | 02, 12, 32                     |
| **superset_ui** |    32 | 01, 03–11, 13–31, 33–37, текст |
| **другое**      |     0 | —                              |

Первичный владелец = кто ведёт правку (DOM/логика → superset_ui; чистая
геометрия/типографика на существующих классах → 750). Строки с двумя
`ownerFiles` — в §4.

---

## 2. builder_ui · `apps/web/strength/heys_strength_builder_ui_v1.js`

Нет строк: mount `view === 'plan-vs-done'` только прокидывает `training` /
`onBack` / `onClose` (`:1466–1471`); все gap — разметка/CSS экрана, не
роутинг/shell.

---

## 3. 750 · `apps/web/styles/modules/750-strength-builder.css`

1. **02** · column gap 3px у заголовка · `.sb-head-title` без
   `flex-direction:column; gap:3px` (`:92–95`); эталон `.sb-cycle-top-main`
   `:6893–6898`.
2. **12** · margin-bottom 8px + border-left по статусу · `.sb-plan-vs-row`
   `margin-bottom:10px` (`:6736`), нет `border-left:3px` по статусу (классы
   статуса — superset_ui, §4).
3. **32** · строка объёма без разделителя · селекторы `.cd .row` для блока
   объёма отсутствуют (DOM `.cd` — superset_ui, §4).

---

## 4. superset_ui · `apps/web/strength/heys_strength_superset_ui_v1.js`

1. **01** · шапка Г2 без боковых кнопок · рендер `.sb-head` с ←/✕
   (`:2133–2143`), не `.top` как в кадре Г2; padding `.sb-head` `:750:82–90`, не
   `.sb-cycle-top` `:6885–6890`.
2. **03** · «Отчёт по циклу» · заголовок «Назначено против сделано» `:2138`.
3. **04** · ключ «недели 1–2 · 6 назначено» · подзаголовок «Отклонения от плана
   куратора» `:2139`.
4. **06** · сводная `.grp` 87 % · нет карточки «План выполнен на 87 %», только
   `.sb-plan-vs-row`.
5. **07** · строка dot+процент · нет flex-ряда сводки (center, gap 8px).
6. **08** · dot 11px · индикатор сводки не рендерится.
7. **09** · «План выполнен на 87 %» · процент цикла не вычисляется/не
   показывается.
8. **10** · «20 подходов из 23 · два отклонения» · агрегат под сводкой
   отсутствует.
9. **11** · margin-top 10px между сводкой и списком · нет обёртки
   `.grp`/`margin-top:10px` (CSS `:750` после DOM).
10. **13** · шапка карточки row gap 7px · `.sb-plan-vs-row` column
    (`:750:6734–6735`), не row dot+имя.
11. **14** · dot 9px у упражнения · статусный dot не рендерится.
12. **16** · пояснение отклонения · нет строки «Вес выше плана — это прогресс» /
    аналогов.
13. **17** · две колонки «Назначено»/«Сделано» · вместо них span «план ·»/«факт
    ·» `:2152–2153`.
14. **18** · ячейка «Назначено» · двухколоночные ячейки с `--bg` + inset-рамкой
    не реализованы.
15. **19** · метка «Назначено» uppercase · «план ·» `:2152`; span 500 11px
    (`:750:6752–6754`), не 600 9px uppercase.
16. **20** · «до 70 кг» · `planExerciseSummary` `:476–494` без «до»; «4 подхода
    · 70 кг».
17. **21** · ячейка «Сделано» `--gr-bg` · нет отдельной зелёной ячейки; только
    `is-diff` border (`:750:6743–6745`).
18. **22** · факт зелёным · факт в mut-цвете span, нет семантики «выше плана =
    `--gr`».
19. **23** · нейтральный dot · `same=true` → `is-match` без dot rgba(ink,.22).
20. **24** · dot/бордер `--val-bad` · нет статуса недобора.
21. **25** · ячейка пропуска `--tint` · состояние «пропущено» не реализовано.
22. **26** · текст «пропущено» · при отсутствии live — actual «—» `:2129`, не
    «пропущено».
23. **28** · блок `.cd` · секция объёма отсутствует.
24. **29** · строки объёма · нет строк внизу экрана.
25. **30** · «Объём назначенного» · строка не рендерится; `plannedVolume` в ядре
    готов (`apps/web/_kernel/heys_kernel_strength_v1.js` — read-only, правок не
    требует).
26. **31** · «41 т» · агрегат тоннажа назначенного не показан.
27. **33** · «26 т» · `totalVolume` не показан.
28. **34** · CTA-ряд gap 8px / margin-top 12px · нижний ряд кнопок отсутствует
    (стили `:750` после DOM).
29. **35** · «Написать куратору» · кнопка отсутствует (реф. паттерн в
    `heys_strength_proposal_ui_v1.js:558`).
30. **36** · «Отчёт за неделю» · вторичная CTA отсутствует.
31. **37** · сноска `.sm` · footnote про перенос/пропуск не рендерится.
32. **текст** · полная цепочка copy Г2 · только заголовок меню + пары «план
    ·/факт ·» по `planSnapshot` `:2116–2155`.

---

## 5. другое

Нет строк с обязательной правкой вне трёх bucket-файлов.  
Read-only зависимости (не менять в этом scope):
`apps/web/_kernel/heys_kernel_strength_v1.js` (`plannedVolume` / `totalVolume`
для §30–33), canvas Г2 `strength-builder.v4.dc.html:594–628`.

---

## 6. Overlap · несколько ownerFiles

**Статус 2026-09-05:** superset_ui + 731 закрыли 34/35 needsCode (остался **01**
— кнопки шапки vs кадр Г2). См. `scripts/.sb-assigned-vs-done-fixes-done.json`.

| key       | ведёт       | вторично    | почему                                                                |
| --------- | ----------- | ----------- | --------------------------------------------------------------------- |
| 01        | superset_ui | 750         | DOM шапки (:2133–2143) vs padding/класс `.sb-head` vs `.sb-cycle-top` |
| 06–09     | superset_ui | 750         | сводная `.grp`, dot, типографика — разметка + новые селекторы         |
| 11        | superset_ui | 750         | обёртка списка → затем `margin-top:10px`                              |
| 12        | 750         | superset_ui | margin `:6736` — CSS; `border-left` — классы статуса в разметке       |
| 13–16     | superset_ui | 750         | структура карточки + flex/dot/пояснение                               |
| 17–22     | superset_ui | 750         | двухколоночные ячейки, copy, цвета                                    |
| 23–25     | superset_ui | 750         | статусы match/bad/skip — классы + border/dot CSS                      |
| 28–29     | superset_ui | 750         | блок `.cd` и `.row`                                                   |
| 30–31, 33 | superset_ui | —           | wiring UI к kernel; ядро не трогаем                                   |
| 32        | 750         | superset_ui | `border-bottom:none` на `.row` после появления `.cd`                  |
| 34–37     | superset_ui | 750         | CTA, footnote — DOM + `.pill44`/`.sm` стили                           |
| 19–20     | superset_ui | 750         | copy/`planExerciseSummary` + кегль span `:6752–6754`                  |
| текст     | superset_ui | 750         | весь экран; CSS догоняет после разметки                               |

**builder_ui** ни в одной overlap-строке не фигурирует: mount уже достаточен для
текущего `PlanVsDoneScreen`.

---

Generated: 2026-09-05 · read-only classification audit.
