# Owner Decision #53 — early start handoff (lane 2)

## Поведение

- Будущий назначенный день: кнопка **«Начать сейчас»** (PlanCard,
  `canStartNow`).
- По клику: `earlyStartAssignedPlan` переносит назначение на **сегодня**, пишет
  **started** факт в сегодня (`materializePlanStartInStore`), на исходной дате
  `plan.status=moved` + `movedTo`, на целевой `movedFrom` + `transferId`.
- Fail-closed: `planSlotForDate` unknown → cold_cache; `useTodayPlanSlot` +
  disabled CTA; сеть/batch не подтвердил пустой сегодня — старт недоступен.
- После успеха: `HEYS.ui.setSelectedDate(today)` + отложенное открытие
  конструктора (`__strengthPendingBuilderOpen`).

## Тесты

- `apps/web/__tests__/day-plan-early-start-v53.test.js`
- Обновлён `apps/web/__tests__/activity-plan-card-frame.test.js`

## Не в scope lane 2

- owner-поле мезоцикла, missed-pending — остаются открытыми по
  UI_V4_OWNER_DECISIONS #53.
