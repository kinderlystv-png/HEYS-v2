# 🐛 Анализ проблемы: Неверный расчет калорий тренировок

**Дата анализа**: 10 ноября 2025 г.  
**Статус**: 🔴 КРИТИЧЕСКИЙ БАГ  
**Влияние**: Завышение калорий тренировок на 50-100%

---

## 📊 Симптомы проблемы

### Пользовательские данные:

- **Вес**: 90 кг
- **Настроенные MET зоны** (из скриншота профиля):
  - Зона 1: MET = **2** → ожидается 2.2 кал/мин
  - Зона 2: MET = **3** → ожидается 3.8 кал/мин
  - Зона 3: MET = **5** → ожидается 7 кал/мин
  - Зона 4: MET = **8** → ожидается 11.8 кал/мин

### Фактический расчет (из скриншота тренировки):

```
Зона 1: 10 мин × ? = 40 ккал    → фактически 4.0 кал/мин  ❌
Зона 2: 4 мин × ? = 38 ккал     → фактически 9.5 кал/мин  ❌
Зона 3: 32 мин × ? = 403.2 ккал → фактически 12.6 кал/мин ❌
Зона 4: 13 мин × ? = 205.4 ккал → фактически 15.8 кал/мин ❌
────────────────────────────────────────────────────────────
Итого: 59 минут = 686.6 ккал (ЗАВЫШЕНО!)
```

### Обратный инжиниринг показывает:

Система использует **дефолтные значения `[2.5, 6, 8, 10]`** вместо
пользовательских `[2, 3, 5, 8]`!

---

## 🔍 Root Cause Analysis

### 1️⃣ Место ошибки: `apps/web/heys_day_v12.js:351`

```javascript
// ❌ НЕПРАВИЛЬНО: регистр поля
const z = (lsGet('heys_profile', {}).zones || []).map((x) => +x.met || 0);
//                                                             ^^^
//                                                         строчное 'met'
const mets = [2.5, 6, 8, 10].map((_, i) => z[i] || [2.5, 6, 8, 10][i]);
```

**Проблема**: Код ищет поле `x.met` (строчными буквами), но в профиле
сохраняется как `x.MET` (заглавными)!

---

### 2️⃣ Где сохраняются зоны: `apps/web/heys_user_v12.js`

#### Структура объекта зон (строка 63):

```javascript
const defaultZones = React.useMemo(() => {
  const maxHR = Math.max(0, 220 - toNum(profile.age || 0));
  const z = (fromPct, toPct) => ({
    hrFrom: Math.round(maxHR * fromPct),
    hrTo: Math.round(maxHR * toPct),
  });
  return [
    { name: 'Бытовая активность (ходьба)', ...z(0.5, 0.6), MET: 2.5 }, // ✅ Заглавное MET
    { name: 'Умеренная активность', ...z(0.6, 0.75), MET: 6 }, // ✅ Заглавное MET
    { name: 'Аэробная (кардио)', ...z(0.75, 0.85), MET: 8 }, // ✅ Заглавное MET
    { name: 'Анаэробная', ...z(0.85, 0.95), MET: 10 }, // ✅ Заглавное MET
  ];
}, [profile.age]);
```

#### Сохранение (строки 65, 111):

```javascript
const [zones, setZones] = React.useState(lsGet('heys_hr_zones', defaultZones));
// ...
React.useEffect(() => {
  lsSet('heys_hr_zones', zones); // Сохраняет с MET (заглавное)
}, [zones]);
```

---

### 3️⃣ Несоответствие источников данных

#### В `heys_day_v12.js` (строка 351):

```javascript
// ❌ Читает из 'heys_profile'
const z = (lsGet('heys_profile', {}).zones || []).map((x) => +x.met || 0);
```

#### В `heys_user_v12.js` (строки 65, 111):

```javascript
// ✅ Сохраняет в 'heys_hr_zones'
const [zones, setZones] = React.useState(lsGet('heys_hr_zones', defaultZones));
lsSet('heys_hr_zones', zones);
```

**Проблема #2**: `heys_day_v12.js` читает из `'heys_profile'`, но данные
сохраняются в `'heys_hr_zones'`!

---

### 4️⃣ Аналогичная ошибка в `heys_reports_v12.js`

#### Строка 308:

```javascript
const z = (zones || []).map((x) => +x.met || 0); // ❌ Строчное 'met'
```

#### Строка 1091:

```javascript
const zones = (profile.zones || []).map((z) => ({ met: +z.met || 0 })).length // ❌ Строчное 'met'
  ? (profile.zones || []).map((z) => ({ met: +z.met || 0 }))
  : [{ met: 2.5 }, { met: 6 }, { met: 8 }, { met: 10 }];
```

---

## 🎯 Полная диагностика

### Цепочка ошибок:

1. **Пользователь настраивает зоны** в UI → `heys_user_v12.js`
2. **Данные сохраняются** в `localStorage['heys_hr_zones']` с полем `MET`
   (заглавное)
3. **`heys_day_v12.js` пытается прочитать**:
   - ❌ Из `'heys_profile'.zones` (неправильный ключ)
   - ❌ Ищет поле `x.met` (неправильный регистр)
4. **Результат**: `z = [0, 0, 0, 0]` (все значения null/0)
5. **Fallback**: Используются дефолтные `[2.5, 6, 8, 10]`
6. **Итог**: Калории завышены на 50-100% ❌

---

## 💥 Влияние на пользователей

### Сравнение расчетов (вес 90 кг):

| Зона      | Минуты | Правильно (MET пользователя) | Фактически (дефолт MET)  | Разница  |
| --------- | ------ | ---------------------------- | ------------------------ | -------- |
| 1         | 10     | 10 × 2.2 = **22 ккал**       | 10 × 4.0 = **40 ккал**   | +82%     |
| 2         | 4      | 4 × 3.8 = **15.2 ккал**      | 4 × 9.5 = **38 ккал**    | +150%    |
| 3         | 32     | 32 × 7 = **224 ккал**        | 32 × 12.6 = **403 ккал** | +80%     |
| 4         | 13     | 13 × 11.8 = **153 ккал**     | 13 × 15.8 = **205 ккал** | +34%     |
| **Итого** | **59** | **414.2 ккал** ✅            | **686.6 ккал** ❌        | **+66%** |

**Критичность**: Завышение на 272 ккал (~66%) искажает весь дневной баланс
калорий!

---

## 🔧 План исправления

### Фикс #1: Исправить регистр поля (КРИТИЧНО)

**Файл**: `apps/web/heys_day_v12.js:351`

```javascript
// БЫЛО:
const z = (lsGet('heys_profile', {}).zones || []).map((x) => +x.met || 0);

// ДОЛЖНО БЫТЬ:
const z = (lsGet('heys_profile', {}).zones || []).map((x) => +x.MET || 0);
//                                                             ^^^
```

### Фикс #2: Исправить источник данных (КРИТИЧНО)

**Файл**: `apps/web/heys_day_v12.js:351`

```javascript
// БЫЛО:
const z = (lsGet('heys_profile', {}).zones || []).map(x => +x.MET || 0);

// ДОЛЖНО БЫТЬ:
const z = (lsGet('heys_hr_zones', []).map(x => +x.MET || 0);
//               ^^^^^^^^^^^^^^^^
```

### Фикс #3: Исправить в отчетах

**Файл**: `apps/web/heys_reports_v12.js:308, 1091`

```javascript
// БЫЛО:
const z = (zones || []).map((x) => +x.met || 0);

// ДОЛЖНО БЫТЬ:
const z = (zones || []).map((x) => +x.MET || 0);
```

### Фикс #4: Синхронизировать с профилем (ОПЦИОНАЛЬНО)

Добавить синхронизацию зон в `heys_profile.zones` для консистентности:

```javascript
// В heys_user_v12.js после сохранения зон:
React.useEffect(() => {
  lsSet('heys_hr_zones', zones);

  // Sync to profile for consistency
  const profile = lsGet('heys_profile', {});
  lsSet('heys_profile', { ...profile, zones });
}, [zones]);
```

---

## ✅ Чеклист тестирования после фикса

1. [ ] Открыть "Профиль" → настроить пользовательские MET зоны
2. [ ] Сохранить и перезагрузить страницу
3. [ ] Открыть "День" → добавить тренировку с минутами в зонах
4. [ ] Проверить расчет калорий (должен соответствовать формуле
       `минуты × MET × вес × 0.0175`)
5. [ ] Проверить localStorage: `localStorage.getItem('heys_hr_zones')` должен
       содержать поля `MET`
6. [ ] Открыть "Отчеты" → проверить правильность расчета тренировок за период
7. [ ] Regression test: дефолтные зоны должны применяться для новых
       пользователей

---

## 📚 Уроки на будущее

1. **Case sensitivity**: JavaScript чувствителен к регистру → использовать
   единый стиль (camelCase или UPPERCASE)
2. **Единый источник данных**: Не дублировать данные в `heys_profile` и
   `heys_hr_zones`
3. **Тестирование граничных случаев**: Проверять fallback на дефолтные значения
4. **Логирование**: Добавить `DEV.log()` для отладки расчета калорий тренировок

---

## 🚀 Приоритет

**Критичность**: 🔴 HIGH  
**Estimated impact**: 100% пользователей с кастомными зонами  
**Estimated effort**: 15 минут (3 строки кода)  
**Рекомендация**: Зафиксить немедленно в текущем спринте
